*.csv
__pycache__/
.claude/
*.txt
bairros-goiania.recon.json

# segredos nunca entram no git (o .env real vive so no VPS)
.env
motor/.env
corretorinteligente_brandkit.zip

# runner residencial: segredo nunca vai pro repo (fica em %USERPROFILE%\.radar)
.radar/
node_modules/
