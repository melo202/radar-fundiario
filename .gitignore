*.csv
__pycache__/
.claude/
*.txt
# SEO: o robots.txt é servido na raiz do site — única exceção ao *.txt acima
!robots.txt
bairros-goiania.recon.json

# segredos nunca entram no git (o .env real vive so no VPS)
.env
motor/.env
corretorinteligente_brandkit.zip

# runner residencial: segredo nunca vai pro repo (fica em %USERPROFILE%\.radar)
.radar/
node_modules/

# status ao vivo da varredura (runtime, nunca versionado)
motor/varredura-status.json
motor/busca-cota-dia.json
