# Segredo do runner da Caixa — COPIE este arquivo para %USERPROFILE%\.radar\ingest.env
# e cole o token real. NUNCA comite o arquivo preenchido (o .radar fica fora do repo).
#
# O MOTOR_TOKEN e o mesmo que esta em /opt/radar/api/.env no VPS (linha MOTOR_TOKEN=...).
RADAR_INGEST_URL=https://api.corretorinteligente.tech/motor/ingestao/caixa
MOTOR_TOKEN=COLE_O_TOKEN_AQUI
