---
phase: 11.1-documentos-da-negociacao
status: complete
source: 11.1-REVIEW.md
completed: 2026-07-07
---

# 11.1 — Review Fix Summary (aplicado pelo orquestrador)

| Achado | Sev | Fix | Verificação |
|--------|-----|-----|-------------|
| WR-01: numeroPorExtenso com Math.abs — valor negativo virava positivo sem sinal | warning | `<0 → ""` (mesmo caminho honesto do null) + fixture `negativo` | 79/79 (fixture nova) |
| WR-02: filtro do disclaimer por substring em QUALQUER posição apagava cláusula legítima editada que citasse a frase | warning | Dropa só quando o bloco COMEÇA com o disclaimer (`indexOf(...)!==0`); a caixa .negdisclaimer-print segue inevitável | Simulação em node: cláusula editada preservada, disclaimer puro dropado (PASS) |
| WR-03: heurística de título `x===x.toUpperCase()` — linha numérica ("45.678") virava heading órfão | warning | Exige presença de LETRA maiúscula (`/[A-ZÁ...Ç]/`) além do uppercase | Simulação: linha numérica não vira título (PASS) |
| IN-01/02/03 (duplicação leve das 3 minutas; aria-hidden aninhado — gap pré-existente do app; literal duplicado do disclaimer na tela de revisão) | info | Registrados p/ Fase 13 (refino) — sem mudança | — |

Limpos no review: NEG sem vazamento entre documentos (realocação total em abrirNeg), parseMatricula sem ReDoS (100k chars adversariais), esc() em todo campo, Esc chain, privacidade fim-a-fim (nenhum storage recebe NEG).

`node --test "tests/*.test.mjs"` → **79 pass / 0 fail** (fixture negativa adicionada).
