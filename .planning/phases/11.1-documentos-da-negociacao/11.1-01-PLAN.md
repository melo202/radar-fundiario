---
phase: 11.1-documentos-da-negociacao
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - radar-goiania.html
  - tests/negocio.test.mjs
  - tests/fixtures.mjs
autonomous: true
requirements: [NEG-01, NEG-02, NEG-03]
tags: [documentos, negociacao, minutas, parse-matricula, extenso, pure-functions, tdd]

must_haves:
  truths:
    - "propostaTexto(data)/termoExclusividadeTexto(data)/contratoTexto(data) sao funcoes puras dentro do bloco RADAR_PURE que retornam a MINUTA COMPLETA em texto pt-BR juridico (clausulas numeradas + disclaimer literal + campos de assinatura), a partir de um objeto data (imovel + partes + valores + prazos) — nunca por IA"
    - "parseMatricula(textoColado) extrai {matricula, cartorio, proprietario} de forma best-effort via regex conservadora, retornando null quando nao ha match — nunca lanca excecao, nunca inventa dado"
    - "numeroPorExtenso(valor) formata reais no formato hibrido 'R$ X (extenso)' para valores em reais, cobrindo casos inteiros/decimais/pequenos"
    - "Honestidade: quando um campo de data esta ausente (nome vazio, valor null, matricula null), a minuta usa o placeholder literal '________' (linha para preencher a mao) — nunca inventa nome/valor/data"
  artifacts:
    - path: "radar-goiania.html"
      provides: "propostaTexto + termoExclusividadeTexto + contratoTexto + parseMatricula + numeroPorExtenso dentro do bloco RADAR_PURE_START..RADAR_PURE_END"
      contains: "function propostaTexto"
    - path: "tests/negocio.test.mjs"
      provides: "harness node:test que carrega RADAR_PURE via node:vm (mesmo padrao de tests/doc.test.mjs/tests/templates.test.mjs) testando as 5 funcoes novas"
      contains: "loadPureBlock"
    - path: "tests/fixtures.mjs"
      provides: "fixtures novas para propostaCasos/termoCasos/contratoCasos/parseMatriculaCasos/extensoCasos adicionadas ao objeto FIXTURES existente"
      contains: "propostaCasos"
  key_links:
    - from: "tests/negocio.test.mjs"
      to: "radar-goiania.html (bloco RADAR_PURE)"
      via: "leitura de arquivo + node:vm.Script, mesmo loader de tests/doc.test.mjs/tests/templates.test.mjs"
      pattern: "RADAR_PURE_START"
    - from: "propostaTexto/termoExclusividadeTexto/contratoTexto"
      to: "numeroPorExtenso"
      via: "chamada interna para formatar valores em reais nas clausulas de preco/comissao"
      pattern: "numeroPorExtenso\\("
    - from: "parseMatricula"
      to: "Wave 2 (11.1-02, negExtrair())"
      via: "funcao pura chamada pelo wizard ao clicar 'Extrair dados' — Wave 2 NAO reimplementa a regex"
      pattern: "function parseMatricula"
---

<objective>
Criar 5 funções puras dentro do bloco `RADAR_PURE` de `radar-goiania.html` — `propostaTexto(data)`, `termoExclusividadeTexto(data)`, `contratoTexto(data)` (as 3 minutas completas em pt-BR jurídico, cláusulas numeradas, disclaimer literal do UI-SPEC, campos de assinatura), `parseMatricula(textoColado)` (extração best-effort de matrícula/cartório/proprietário por regex conservadora) e `numeroPorExtenso(valor)` (formato híbrido "R$ X (extenso)") — com harness de teste TDD (`tests/negocio.test.mjs`) cobrindo estrutura de cláusulas por documento, presença do disclaimer, extenso correto, parse com/sem match e honestidade de campos ausentes.

Purpose: NEG-01/02/03 exigem que as 3 minutas do negócio sejam geradas por template DETERMINÍSTICO (nunca LLM) a partir do imóvel + partes/valores digitados, com ressalva jurídica clara e nunca inventando dado ausente. Esta plan constrói a CAMADA PURA (sem DOM, sem wizard) — Wave 2 (11.1-02) consome as 3 funções de texto + `parseMatricula` para o wizard `NEG`; Wave 3 (11.1-03) consome o texto final para montar o template de impressão A4.
Output: bloco RADAR_PURE ampliado com 5 funções novas; `tests/negocio.test.mjs` com fixtures cobrindo a estrutura de cláusulas dos 3 documentos, disclaimer literal, extenso (500000/1234567.89/90), parse com/sem match, honestidade de campo ausente; suite completa (`node --test "tests/*.test.mjs"`) passando (64 existentes + novos).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-documentos-da-negociacao/11.1-CONTEXT.md
@.planning/phases/11.1-documentos-da-negociacao/11.1-UI-SPEC.md

# Fonte de código (ler antes de editar)
@radar-goiania.html
@tests/doc.test.mjs
@tests/templates.test.mjs
@tests/fixtures.mjs

<facts>
<!-- Verificado por leitura direta deste repo. -->

Bloco RADAR_PURE hoje (linhas 920-1417 de radar-goiania.html): abre em
`// RADAR_PURE_START — funções puras de matching/normalização. Testadas por tests/busca.test.mjs`
e fecha em `// RADAR_PURE_END` (linha 1417). Já contém (Fases 9/10/11): `scoreOportunidade`,
`scoreConfianca`, `leituraPratica`, `assinatura`, `brlSimples`, `faixaTxt`, `zapResumo/zapProprietario/
zapComprador/zapArgumento/zapRiscos`, `captAbordagem/captScript/captChecklist/captFollowup`,
`enderecoSimples`, `oportunidadeItem`, `histAdd`, `recomendaDocumento`, `pendenciasDocumento`,
`fichaRapidaTexto`. Esta plan ACRESCENTA 5 funções ao MESMO bloco (não cria um segundo bloco) —
o harness de teste corta por índice de string entre os marcadores, então tudo que precisa ser
testável por node:vm PRECISA estar dentro deles.

`esc(v)` (linha 909, FORA do bloco RADAR_PURE, mas global): sanitiza HTML — as funções desta plan
NÃO chamam `esc()` internamente (retornam texto puro pt-BR, não HTML) — é a Wave 2/3 que aplica
`esc()` campo a campo ao injetar no DOM/impressão (mesmo contrato de `fichaRapidaTexto`, que também
retorna texto puro sem escapar).

`brl(n)`/`brlSimples(n)` (linhas ~1163-1172, já existem no bloco): formatação de moeda `R$ X.XXX,XX`.
`numeroPorExtenso` desta plan é uma função NOVA (não existe ainda) — não confundir com `brl`/
`brlSimples`, que só formatam o número, sem escrever por extenso.

Objeto `data` de entrada para as 3 funções de minuta (shape NOVO desta fase, distinto do `data` de
`dadosFicha()`/`fichaRapidaTexto` — são domínios diferentes: documento de negociação vs. ficha
comercial). Campos vêm do imóvel (cadastro, já disponível via `LZ.a`/`DCUR` na Wave 2) + partes/
valores digitados no wizard `NEG`:

```js
// Shape comum ao imovel (subconjunto usado nas 3 minutas — nomes iguais ao objeto `a` do cadastro,
// Wave 2 monta este sub-objeto a partir de LZ.a/DCUR, funcao pura nao le DOM/localStorage):
{
  imovel: {
    endereco: string, bairro: string, quadra: string, lote: string,
    areaTerr: number|null, areaEdif: number|null, inscricao: string,
    tipoImovel: string // "Apartamento"/"Casa"/"Terreno"/etc, ja resolvido por unitLabel/USO
  },
  // --- Proposta ---
  proponente: {nome: string, doc: string, endereco: string} | null,
  vendedor: {nome: string, doc: string, endereco: string} | null,
  valorOfertado: number | null,
  formaPagamento: string | null, // "À vista"|"Financiado"|"Entrada + saldo"|"A combinar"
  validadeDias: number | null, // default 10 (Wave 2 preenche o default; funcao pura so formata o que vier)
  // --- Termo de Exclusividade ---
  proprietario: {nome: string, doc: string, endereco: string} | null,
  corretor: {nome: string, creci: string} | null,
  prazoDias: number | null, // default 90
  comissaoPct: number | null, // default 6
  exclusiva: boolean | null, // true="Sim, exclusiva", false="Não, aberta", null=nao respondido
  autorizaAnuncio: boolean | null,
  // --- Contrato ---
  comprador: {nome: string, doc: string, endereco: string} | null,
  matricula: string | null,
  cartorio: string | null,
  precoTotal: number | null,
  // comum
  foro: string | null, // default "Goiânia/GO" (Wave 2 preenche; funcao pura usa fallback se ausente)
}
```

Todos os subcampos de parte (`nome`/`doc`/`endereco`) são OPCIONAIS exceto `nome` (que ainda pode
vir vazio — a função NUNCA lança exceção, usa `"________"` quando ausente). `doc`/`endereco` NUNCA
são obrigatórios (CONTEXT.md: "CPF/CNPJ e endereço NUNCA obrigatórios").
</facts>

<interfaces>
<!-- Assinaturas exatas que a Wave 2/3 vão CHAMAR — contrato duro, não renomear. -->

```js
// NEG-01: minuta completa da Proposta de Compra e Venda (texto puro pt-BR, nao-HTML).
// Estrutura de clausulas (UI-SPEC Componente 4, tabela "Estrutura de clausulas por documento"):
// 1a Do Objeto · 2a Do Valor Ofertado e Forma de Pagamento · 3a Da Validade da Proposta ·
// 4a Das Condicoes (so aparece se houver observacao) · 5a Do Foro.
// Retorna a MINUTA INTEIRA como string unica (cabecalho + clausulas numeradas + disclaimer +
// linhas de assinatura) — Wave 2 injeta em textarea (revisao editavel); Wave 3 usa o MESMO texto
// (editado ou nao) para o template impresso, dividindo por linha/clausula se precisar de HTML.
function propostaTexto(data): string

// NEG-02: minuta completa do Termo de Autorizacao/Exclusividade de Venda.
// Estrutura: 1a Do Objeto e da Autorizacao · 2a Do Prazo · 3a Da Exclusividade (texto VARIA
// conforme data.exclusiva: true="em caráter EXCLUSIVO", false="em caráter NAO EXCLUSIVO (aberta)",
// null="a definir entre as partes") · 4a Da Comissao · 5a Da Autorizacao de Divulgacao/Anuncio
// (so aparece se data.autorizaAnuncio nao for null) · 6a Das Obrigacoes do Corretor · 7a Do Foro.
function termoExclusividadeTexto(data): string

// NEG-03: minuta completa do Contrato de Compra e Venda.
// Estrutura: 1a Das Partes · 2a Do Objeto (imovel + matricula/RGI + cartorio) · 3a Do Preco e da
// Forma de Pagamento · 4a Da Posse e Entrega · 5a Do Inadimplemento (basico) · 6a Das Despesas e
// Tributos (ITBI/cartorio/condominio-IPTU pro-rata) · 7a Do Foro.
function contratoTexto(data): string

// Parse best-effort do texto colado da matricula/certidao — regex CONSERVADORA (nunca lanca
// excecao, nunca "quase-match" arriscado). Retorna null se NENHUM padrao casar (nem matricula nem
// cartorio) — parse parcial (so matricula OU so cartorio) retorna objeto com o campo ausente como
// null, NUNCA lanca. proprietario e best-effort adicional (pode ficar ausente/null com frequencia
// — texto de matricula real varia muito; nao e um requisito central do parse).
function parseMatricula(textoColado: string): {matricula: string|null, cartorio: string|null, proprietario: string|null} | null

// Formato hibrido "R$ 500.000,00 (quinhentos mil reais)" para valores em reais — usado internamente
// pelas 3 funcoes de minuta nas clausulas de valor/comissao, e exposto para a Wave 2 usar no preview
// do passo de Revisao se precisar. valor null/NaN retorna string vazia (nunca "R$ NaN" nem "R$ null").
function numeroPorExtenso(valor: number|null): string
```

Texto do disclaimer TRAVADO literalmente no UI-SPEC (Copywriting Contract) — usar EXATAMENTE, sem
paráfrase, nas 3 funções de minuta:

> "Minuta para conferência das partes — recomenda-se revisão por advogado; a transmissão da
> propriedade exige escritura pública (quando aplicável) e registro na matrícula do imóvel no
> cartório de registro de imóveis competente."

Pontos de honestidade OBRIGATÓRIOS (testados):
- Qualquer subcampo de parte ausente (`nome` vazio/undefined) → a linha correspondente na minuta usa
  literalmente `"________"` (8 underscores), nunca inventa nome.
- `data.valorOfertado`/`data.precoTotal` ausente (`null`/`undefined`) → cláusula de valor usa
  `"________"` no lugar do valor (nunca "R$ null"/"R$ undefined"/"R$ NaN").
- `data.matricula`/`data.cartorio` ausentes → cláusula "Do Objeto" (Contrato) usa `"________"` no
  lugar (nunca inventa número de matrícula).
- `parseMatricula("texto sem nenhum padrão reconhecível")` → retorna `null` (não um objeto com todos
  os campos `null` — a distinção entre "nada casou" e "objeto parcial" importa para a Wave 2 decidir
  a mensagem neutra vs. a mensagem de sucesso parcial).
- `numeroPorExtenso(500000)` contém "quinhentos mil reais"; `numeroPorExtenso(90)` contém "noventa
  reais"; `numeroPorExtenso(1234567.89)` não lança exceção e contém "R$" (formato híbrido, extenso
  pode simplificar centavos — não é obrigatório escrever centavos por extenso, mas o valor em R$
  formatado deve estar correto: "R$ 1.234.567,89").
- (fix plan-check) REGRAS do extenso pt-BR que a implementação DEVE seguir (todas com fixture):
  (a) 100 = "cem", 101-199 = "cento e ..." (nunca "um cento"); (b) 1000 = "mil" (NUNCA "um mil");
  (c) 1.000.000 = "um milhão" (singular), 2.000.000+ = "N milhões" (plural) — e "de reais" após
  milhão/milhões sem casa de milhar/centena ("um milhão DE reais", mas "um milhão e cem mil reais");
  (d) conector "e" entre centena-dezena-unidade ("cento e cinquenta", "trezentos e quarenta e dois").
- Todas as 3 minutas contêm o disclaimer literal EXATO (não paráfrase) e linhas de assinatura das
  partes (nome sobre linha, papel indicado — "Proponente"/"Vendedor/Proprietário"/"Comprador"/
  "Proprietário"/"Corretor" conforme o documento).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — criar tests/negocio.test.mjs + fixtures (testes falhando)</name>
  <files>tests/negocio.test.mjs, tests/fixtures.mjs</files>
  <behavior>
    - propostaTexto(data completo): string não-vazia contendo "Cláusula 1ª", "Cláusula 2ª", "Cláusula 3ª", "Cláusula 5ª" (numeração da tabela do UI-SPEC), contendo o disclaimer literal EXATO, contendo "Proponente" e "Vendedor/Proprietário" como papéis de assinatura
    - propostaTexto(data com proponente.nome vazio): a minuta contém "________" no lugar do nome (nunca nome vazio silencioso, nunca "undefined")
    - propostaTexto(data com valorOfertado=null): a minuta contém "________" na cláusula de valor, nunca "R$ null"/"R$ NaN"/"R$ undefined"
    - termoExclusividadeTexto(data com exclusiva:true): contém "EXCLUSIVO" (caráter exclusivo); termoExclusividadeTexto(data com exclusiva:false): contém "NÃO EXCLUSIVO" ou "aberta"; ambos contêm "Cláusula 4ª" (comissão) e o disclaimer literal
    - termoExclusividadeTexto(data com autorizaAnuncio:null): a minuta NÃO contém a cláusula de "Autorização de Divulgação/Anúncio" (cláusula condicional ausente quando null); com autorizaAnuncio:true, a cláusula aparece
    - contratoTexto(data completo com matricula/cartorio preenchidos): contém "Cláusula 1ª" até "Cláusula 7ª", contém o número da matrícula e o nome do cartório interpolados, contém o disclaimer literal, contém papéis de assinatura "Vendedor" e "Comprador"
    - contratoTexto(data com matricula=null, cartorio=null): a cláusula "Do Objeto" contém "________" no lugar da matrícula/cartório, nunca "null"/"undefined"
    - as 3 funções de minuta, para qualquer data válido, NUNCA retornam string contendo literalmente "undefined" ou "NaN" (assert negativo global)
    - parseMatricula("Matrícula nº 12.345, situado no 3º Ofício de Registro de Imóveis desta Comarca"): retorna objeto não-null com matricula contendo "12.345" e cartorio contendo "3" (ofício/cartório identificado)
    - (fix plan-check) parseMatricula("MATRICULA N. 45678, Cartório do 2° Ofício"): retorna objeto não-null com matricula "45678" E cartorio contendo "2" — a ordem "Cartório do N° Ofício" (número DEPOIS da palavra-chave, sem "registro de imóveis" no fim) é fraseado real comum; a regex de cartório PRECISA de uma alternativa p/ essa ordem (ex.: `cart[óo]rio\s*(?:do|de)?\s*(\d+)[ºo°]?\s*of[íi]cio` OU `(\d+)[ºo°]?\s*of[íi]cio` isolado como fallback)
    - parseMatricula("texto qualquer sem nenhum padrão de matrícula ou cartório"): retorna null
    - parseMatricula(""): retorna null (nunca lança exceção com string vazia)
    - numeroPorExtenso(500000) contém "quinhentos mil reais" E contém "R$ 500.000,00"; numeroPorExtenso(90) contém "noventa reais"; numeroPorExtenso(1234567.89) não lança exceção, contém "R$ 1.234.567,89", contém "R$" (não precisa escrever centavos por extenso)
    - (fix plan-check — fronteiras do extenso pt-BR, alto risco) numeroPorExtenso(1000000) contém "um milhão de reais" e NÃO contém "um milhões" nem "milhões"; numeroPorExtenso(2000000) contém "dois milhões de reais"; numeroPorExtenso(100) contém "cem reais" e NÃO contém "cento"; numeroPorExtenso(150) contém "cento e cinquenta reais"; numeroPorExtenso(1000) contém "mil reais" e NÃO contém "um mil" (anti-pattern clássico)
    - numeroPorExtenso(null) retorna string vazia (nunca "R$ NaN"/"R$ null")
  </behavior>
  <action>
Criar `tests/negocio.test.mjs` reusando EXATAMENTE o padrão de loader de `tests/doc.test.mjs`/
`tests/templates.test.mjs` (`loadPureBlock()` via `node:vm`, cortando por
`RADAR_PURE_START`/`RADAR_PURE_END`, com os MESMOS guards de `assert.ok(src.includes(...))` para
cada função nova antes de montar o sandbox — copiar o padrão, adaptando a lista de funções
exportadas em `globalThis.__exports` para incluir as 5 novas:
`propostaTexto,termoExclusividadeTexto,contratoTexto,parseMatricula,numeroPorExtenso`.

Adicionar ao objeto `FIXTURES` existente em `tests/fixtures.mjs` (não substituir, ACRESCENTAR
chaves novas) as fixtures: `propostaCasos` (data completo + data com campos ausentes conforme
`<behavior>`), `termoCasos` (data com exclusiva true/false, autorizaAnuncio null/true/false),
`contratoCasos` (data completo + data sem matrícula/cartório), `parseMatriculaCasos` (texto com
match canônico, texto com a ordem "Cartório do 2° Ofício" — fix plan-check, texto sem match,
string vazia), `extensoCasos` (500000, 90, 1234567.89, 1000000, 2000000, 100, 150, 1000, null —
as fronteiras do pt-BR: um milhão singular, milhões plural, cem vs cento, mil sem "um").

Escrever os testes conforme `<behavior>` — DEVEM FALHAR nesta task (RED), pois as 5 funções ainda
não existem em `radar-goiania.html`. Rodar `node --test "tests/negocio.test.mjs"` e confirmar
falha (funções ausentes do bloco RADAR_PURE) — este é o estado esperado ao final da Task 1.
  </action>
  <verify>
    <automated>node --test "tests/negocio.test.mjs" 2>&1 | grep -qi "fail" && echo "OK: RED confirmado (testes falham, funcoes ainda nao existem)"</automated>
  </verify>
  <done>tests/negocio.test.mjs e fixtures novas em tests/fixtures.mjs existem; rodar a suite falha porque propostaTexto/termoExclusividadeTexto/contratoTexto/parseMatricula/numeroPorExtenso ainda não existem no bloco RADAR_PURE — estado RED documentado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implementar as 5 funções dentro do bloco RADAR_PURE</name>
  <files>radar-goiania.html</files>
  <action>
Adicionar as 5 funções de `&lt;interfaces&gt;` DENTRO do bloco `RADAR_PURE_START`...`RADAR_PURE_END`
(hoje linhas 920-1417), imediatamente ANTES do marcador `// RADAR_PURE_END` (seção nova, comentada
como "NEG-01/02/03 (Fase 11.1, 11.1-01): minutas da negociação (Proposta/Termo/Contrato) + parse de
matrícula + número por extenso — puro, sem IA, nunca inventa campo ausente").

Ordem de implementação sugerida (extenso e parse primeiro, pois as 3 minutas dependem de
`numeroPorExtenso`):

- `const CAMPO_VAZIO="________";` (constante de módulo — as 3 funções de minuta usam a MESMA
  constante, nunca strings literais divergentes).

- `numeroPorExtenso(valor)`: `if(valor==null||isNaN(valor))return "";` — formata `"R$ "+brl(valor)`
  (reusa `brl()` já existente no bloco para a parte numérica) `+" ("+extenso+")"`. Implementar
  `extenso` com uma tabela de unidades/dezenas/centenas + escala (mil/milhão) suficiente para valores
  de imóvel (até centenas de milhões) — SEM escrever centavos por extenso (formato híbrido: só a
  parte inteira em português, os centavos ficam só no `brl()` numérico). Função auxiliar interna
  (não exportada) pode ser criada dentro do mesmo escopo.

- `parseMatricula(textoColado)`: `const t=String(textoColado||"");` — dois regex conservadores
  (copiados literalmente do CONTEXT.md):
  - matrícula: `/matr[íi]cula\s*(?:n[ºo°.]?\s*)?([\d.]+)/i`
  - cartório: `/(\d+)[ºo°]?\s*(?:of[íi]cio|cart[óo]rio)\s*(?:de\s*)?registro\s*de\s*im[óo]veis/i`
  - proprietário: best-effort adicional, ex. `/propriet[áa]rio[:\s]+([A-ZÀ-Ú][a-zà-ú]+(?:\s+[A-ZÀ-Ú][a-zà-ú]+){1,4})/`
    (nome próprio de 2-5 palavras após a palavra "proprietário") — pode ficar `null` com frequência,
    não é bloqueante.
  - Se NENHUM dos 2 primeiros regex casar → `return null` (nem matrícula nem cartório identificados).
  - Se ao menos 1 casar → retorna `{matricula: match1?match1[1]:null, cartorio: match2?(match2[1]+"º Ofício de Registro de Imóveis"):null, proprietario: match3?match3[1]:null}`.

- `propostaTexto(data)`: `const d=data||{};const im=d.imovel||{};` monta string com cabeçalho
  ("PROPOSTA DE COMPRA E VENDA"), identificação das partes (proponente/vendedor, usando
  `CAMPO_VAZIO` quando `nome` ausente), cláusulas numeradas 1ª-5ª (endereço do imóvel via `im`;
  valor via `numeroPorExtenso(d.valorOfertado)` ou `CAMPO_VAZIO` se `null`; forma de pagamento;
  validade em dias com default textual "10 dias" se `d.validadeDias` ausente MAS reusar o valor
  passado quando presente — Wave 2 já resolve o default antes de chamar; cláusula "Das Condições"
  SÓ aparece se houver `d.obs`/observação não-vazia — se a interface não tiver esse campo, omitir
  a cláusula por completo, renumerando ou mantendo a numeração fixa 1-5 conforme o esqueleto do
  UI-SPEC, que já lista "Das Condições" como 4ª sempre presente mas com texto condicional "sem
  condições especiais" quando vazio — preferir SEMPRE incluir as 5 cláusulas do esqueleto, com texto
  neutro quando o dado não existir, para manter a numeração estável e previsível), foro (default
  "Goiânia/GO" se `d.foro` ausente). Fecha com o disclaimer literal EXATO e 2 blocos de assinatura
  (Proponente / Vendedor-Proprietário).

- `termoExclusividadeTexto(data)`: cabeçalho ("TERMO DE AUTORIZAÇÃO/EXCLUSIVIDADE DE VENDA"),
  identificação (proprietário + corretor, CRECI do corretor quando houver), cláusulas 1ª-7ª (objeto/
  autorização; prazo em dias; exclusividade — texto BIFURCADO por `d.exclusiva` conforme especificado
  na `<interfaces>`, incluindo o caso `null`="a definir entre as partes"; comissão via
  `numeroPorExtenso`-style para o percentual — usar formatação simples "X%" pois `numeroPorExtenso`
  é para REAIS, não percentual; cláusula de autorização de anúncio CONDICIONAL — só inclui a
  cláusula 5ª se `d.autorizaAnuncio!==null&&d.autorizaAnuncio!==undefined` (renumerar a cláusula do
  Foro para 6ª quando a de anúncio estiver ausente, OU manter numeração fixa com texto "Não
  aplicável" — decisão de implementação: preferir OMITIR a cláusula por completo quando
  `autorizaAnuncio` for null, ajustando a numeração sequencial das cláusulas seguintes, para não
  expor uma cláusula vazia/confusa numa minuta jurídica); obrigações do corretor (texto padrão);
  foro). Disclaimer literal + assinaturas (Proprietário / Corretor).

- `contratoTexto(data)`: cabeçalho ("CONTRATO DE COMPRA E VENDA — MINUTA"), cláusulas 1ª-7ª (partes
  vendedor/comprador; objeto — endereço+matrícula+cartório, usando `CAMPO_VAZIO` quando
  `d.matricula`/`d.cartorio` ausentes; preço via `numeroPorExtenso(d.precoTotal)` ou `CAMPO_VAZIO`;
  posse e entrega — texto padrão "na data da assinatura, salvo disposição em contrário"; inadimplemento
  básico — texto padrão de multa/rescisão; despesas e tributos — ITBI/cartório/condomínio-IPTU
  pró-rata, texto padrão; foro). Disclaimer literal + assinaturas (Vendedor / Comprador).

Todo texto de cláusula é REDIGIDO por você (Claude) em pt-BR jurídico correto e profissional,
seguindo o esqueleto do UI-SPEC (Componente 4, tabela "Estrutura de cláusulas por documento") —
usuário é advogado e vai revisar como HUMAN-UAT da fase (não precisa ficar perfeito juridicamente,
mas correto na forma/estrutura de uma minuta real).

Rodar `node --test "tests/negocio.test.mjs"` até todos os testes passarem (GREEN). Rodar também
`node --test "tests/*.test.mjs"` (suite completa) para confirmar zero regressão nos 64 testes
existentes.
  </action>
  <verify>
    <automated>node --test "tests/*.test.mjs" 2>&1 | tail -25</automated>
  </verify>
  <done>As 5 funções existem dentro do bloco RADAR_PURE; node --test "tests/*.test.mjs" passa 100% (negocio.test.mjs novo GREEN + zero regressão nos 64 testes existentes); as 3 funções de minuta nunca retornam "undefined"/"NaN"; campos ausentes usam CAMPO_VAZIO ("________"); disclaimer literal presente nas 3 minutas; parseMatricula retorna null quando nenhum padrão casa; numeroPorExtenso cobre os 4 casos de teste.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parseMatricula(textoColado) -> Wave 2 (negExtrair(), pré-preenche campos do formulário) | texto colado é entrada livre do usuário (não de terceiro/rede) — a função só EXTRAI via regex, nunca executa/avalia o texto; risco de injeção é nulo aqui pois o retorno são apenas 3 strings curtas que a Wave 2 ainda escapa com esc() ao exibir |
| propostaTexto/termoExclusividadeTexto/contratoTexto -> textarea de revisão (Wave 2) -> template impresso (Wave 3) | texto gerado é 100% determinístico a partir de `data` — nenhuma chamada de rede/IA; a superfície de risco real é a Wave 2/3 escapando (ou não) o texto EDITADO pelo usuário antes de reinjetar no DOM de impressão (fora do escopo desta plan, mas documentado como dependência) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1-01 | Tampering | propostaTexto/termoExclusividadeTexto/contratoTexto inventando dado quando campo ausente (nome/valor/matrícula fabricados) | mitigate | contrato de honestidade testado explicitamente (fixtures com campos ausentes) — CAMPO_VAZIO ("________") em vez de qualquer valor fabricado, testado com assert negativo global contra "undefined"/"NaN" |
| T-11.1-02 | Tampering | parseMatricula retornando falso-positivo (casando texto que não é matrícula real) | accept | regex conservadora documentada no CONTEXT.md; sempre revisável pelo usuário antes de qualquer PDF (Wave 2 nunca marca campo como readonly); risco baixo — pior caso é o usuário corrigir manualmente um campo mal-preenchido, nunca perda de dado |
| T-11.1-03 | Repudiation | Minuta gerada sem o disclaimer de responsabilidade (edição futura remove a linha por engano) | mitigate | teste explícito de presença do disclaimer literal EXATO nas 3 funções — qualquer regressão futura quebra o teste imediatamente |

**Sem ameaças de alta severidade.** Esta plan não toca DOM, localStorage ou rede — é camada pura de geração de texto/parse, testável 100% por fixtures.
</threat_model>

<verification>
- `node --test "tests/*.test.mjs"` passa 100% (negocio.test.mjs novo + doc/scores/templates/detectmode/busca sem regressão — 64 existentes + novos)
- As 3 funções de minuta contêm o disclaimer literal EXATO do UI-SPEC (grep confirma string idêntica nas 3)
- Nenhuma das 3 funções de minuta retorna "undefined"/"NaN" em nenhum caso de fixture (campo ausente sempre vira "________")
- parseMatricula retorna null quando nenhum regex casa (distinção clara de "sem match" vs. "match parcial")
- numeroPorExtenso cobre os 4 casos de fixture (500000, 90, 1234567.89, null) sem lançar exceção
</verification>

<success_criteria>
- NEG-01 (camada de geração da Proposta) cumprido: propostaTexto pronta para a Wave 2/3 consumirem
- NEG-02 (camada de geração do Termo de Exclusividade) cumprido: termoExclusividadeTexto cobre a bifurcação exclusiva/aberta e a cláusula condicional de anúncio
- NEG-03 (camada de geração do Contrato) cumprido: contratoTexto cobre matrícula/cartório via parseMatricula ou preenchimento manual
- parseMatricula/numeroPorExtenso disponíveis como utilitários reusados pelas 3 funções de minuta e pela Wave 2 (extração no wizard)
- Wave 2 (11.1-02) e Wave 3 (11.1-03) podem chamar as 5 funções sem reabrir esta plan — contrato de retorno travado
</success_criteria>

<output>
Após completar, criar `.planning/phases/11.1-documentos-da-negociacao/11.1-01-SUMMARY.md`.
Incluir no SUMMARY: assinatura exata das 5 funções, resultado da suite completa de testes (contagem pass/fail), a estrutura de cláusulas final de cada um dos 3 documentos (para a Wave 3 montar o template impresso sem ambiguidade), o texto literal do disclaimer confirmado, e (fix plan-check) uma nota explícita de que a extração de `proprietario` do parseMatricula é best-effort NÃO testada por fixture (limitação documentada, não omissão silenciosa).
</output>
</output>
