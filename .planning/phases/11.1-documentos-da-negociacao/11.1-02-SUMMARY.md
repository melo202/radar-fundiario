---
phase: 11.1-documentos-da-negociacao
plan: 02
subsystem: ui
tags: [wizard, neg-sheet, privacidade, parse-matricula, validacao, capt-sheet, dactsmore]

# Dependency graph
requires:
  - phase: 11.1-01
    provides: "5 funções puras RADAR_PURE (propostaTexto/termoExclusividadeTexto/contratoTexto/parseMatricula/numeroPorExtenso) com assinaturas travadas"
provides:
  - "#negSheet: terceiro sheet .wiz próprio (padrão #captSheet), estado NEG em memória (NUNCA localStorage)"
  - "abrirNeg(tipo)/fecharNeg()/negBack()/negNext()/negRender()/negExtrair()/montarNeg() completos e ligados ao CTA"
  - "3 entradas em #dActsMore (grupo 'Documentos da negociação') + botão accent no #captSheet (liga NEG-02 a CAPT-01)"
  - "Esc chain: #negSheet é o PRIMEIRO check (antes de #captSheet — sheet mais recente tem prioridade)"
affects: ["11.1-03 (Wave 3 substitui o corpo de montarNeg() pelo template A4 estruturado sem tocar no contrato/pipeline)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Terceiro sheet .wiz independente (#wiz -> #captSheet -> #negSheet), cada um com estado/RET próprio (LZ/WIZRET, nenhum/CAPTRET, NEG/NEGRET) — nenhum reset cruzado, todos podem coexistir"
    - "Validação por documento no avanço de passo via toast + negRender() (mesmo padrão wizNext/toast), nunca bloqueando por CPF/CNPJ/endereço"
    - "Regeneração determinística da minuta ao entrar no passo 1 (0->1): NEG.minutaTexto sempre recalculado, NEG.minutaEditada sempre zerado — edição vale só para a geração corrente"

key-files:
  created: []
  modified:
    - radar-goiania.html

key-decisions:
  - "montarNeg() minimo usa <div style=\"white-space:pre-wrap\"> em vez de blocos por cláusula (texto corrido da função pura) — mesma decisão de forma documentada no UI-SPEC ('textarea único é a forma padrão desta fase'); Wave 3 pode dividir em .negclausula-item sem tocar no contrato de chamada"
  - "Comentários de código evitam a string literal 'montarNeg(' fora da definição/chamada real — o verify da plan usa grep -c literal (== 2), então comentários explicativos foram redigidos sem repetir a assinatura da função"
  - "negNext() usa .trim() nos campos de nome (não apenas !NEG.x.nome) para tratar strings só-espaço como vazias, mesmo padrão de robustez já usado em wizNext (LZ.prof.nome)"

requirements-completed: [NEG-01, NEG-02, NEG-03]

# Metrics
duration: 45min
completed: 2026-07-07
---

# Phase 11.1 Plan 02: Wizard NEG (Documentos da Negociação) Summary

**Terceiro sheet `.wiz` próprio `#negSheet` com estado `NEG` 100% em memória (zero localStorage), 2 passos (Partes & Valores por documento com defaults 90d/6%/10 dias + extração de matrícula via `parseMatricula`, depois Revisão da minuta editável com disclaimer fixo), ligado a 3 entradas na ficha e 1 no Modo Captação, e `montarNeg()` mínimo já disparando o pipeline `#laudo`→`#laudoView`.**

## Performance

- **Duration:** 45 min (aprox.)
- **Started:** 2026-07-07T20:10:00Z (aprox.)
- **Completed:** 2026-07-07T20:55:00Z (aprox.)
- **Tasks:** 2/2
- **Files modified:** 1 (radar-goiania.html)

## Accomplishments

- `#negSheet` criado como terceiro container `.wiz` independente (mesmo padrão de `#captSheet`), com `role="dialog"`/`aria-modal`/`aria-label`, dots próprios (`#negDots`, 2 passos), `.wback`/`.wclose`/`.wnext` próprios (`negBackBtn`/`negNextBtn`)
- Estado `let NEG=null, NEGRET=null` inteiramente em memória — `abrirNeg(tipo)` monta o shape completo do `<interfaces>` do plano (proponente/vendedor/comprador/proprietário/corretor, valores/condições, matrícula/cartório, minuta) com defaults `validadeDias:10, prazoDias:90, comissaoPct:6, exclusiva:true, autorizaAnuncio:true`
- Passo 0 renderiza campos específicos por documento (Proposta/Exclusividade/Contrato) com aviso de privacidade fixo (`.negpriv`) no topo, mais o bloco "colar texto da matrícula" + "Extrair dados" (Contrato e Exclusividade — ausente na Proposta)
- `negExtrair()` chama `parseMatricula` (RADAR_PURE, 11.1-01) sem reimplementar regex — mensagem neutra sem match, mensagem com match sempre incluindo "Revise", campos sempre `<input>` editáveis
- `negNext()` valida por documento (toast literal do UI-SPEC) antes de avançar — CPF/CNPJ e endereço nunca obrigatórios; avança para o passo 1 que gera `NEG.minutaTexto` via `propostaTexto`/`termoExclusividadeTexto`/`contratoTexto` e mostra textarea editável + caixa `.negdisclaimer` (não editável, borda `--accent` 2px)
- `montarNeg()` — clique real no CTA "Gerar PDF" é o ÚNICO gatilho (guarda ec9f129) — monta `#laudo` com cabeçalho `.lbrand` + texto escapado (`esc()`) em `white-space:pre-wrap` + `.foot`, copia para `#laudoViewBody`, mostra `#laudoView` (fechamento idêntico a `renderFichaRapida()`)
- 3 entradas em `#dActsMore` (grupo "Documentos da negociação": Proposta/Exclusividade/Contrato) + botão accent `.negcta` no `#captSheet` (`.captblock-neg`, pré-seleciona o Termo de Exclusividade)
- Esc chain: `#negSheet` é o PRIMEIRO check do `keydown` global, antes de `#captSheet` — Esc fecha primeiro o sheet mais recente
- Zero mudança em `#wiz`/`LZ`/`wizRender`/`wizNext`/`wizBack`/`abrirSeletorFinalidade`/`montarLaudo`/`montarFichaRapida` — verificado por extração programática do corpo de cada função `neg*` (nenhuma referencia `LZ`) e de cada função do laudo (nenhuma referencia `NEG`)
- Suite completa `node --test "tests/*.test.mjs"`: **79/79 passam**, zero regressão

## Task Commits

Cada task foi commitada atomicamente:

1. **Task 1: Estado NEG + #negSheet + abrirNeg/fecharNeg/negRender passo 0 + extração de matrícula** - `ba45870` (feat)
2. **Task 2: Passo 1 (Revisão da minuta) + validação por documento + montarNeg() mínimo ligado ao CTA** - `71fc8b2` (feat)

## Files Created/Modified

- `radar-goiania.html` — HTML: `#negSheet` (novo `.wiz`, após `#captSheet`), grupo "Documentos da negociação" em `#dActsMore`, `.captblock-neg` em `#captSheet`. CSS: `.negpriv`/`.negparte`/`.negparte-lbl`/`.negmat`/`.negextrai`/`.negextrairesult`(+variantes `.neg-neutro`/`.neg-match`)/`.negminuta`/`.negdisclaimer`(+`-eyebrow`/`-txt`)/`.negcta`, zero hex novo. JS: `let NEG=null, NEGRET=null` + `abrirNeg/fecharNeg/negBack/negRender/negExtrair/negNext/montarNeg`; Esc chain com `#negSheet` primeiro.

## Shape Final Exato do Objeto NEG

```js
NEG = {
  a: DCUR,                    // cópia local do imóvel (mesma proteção de LZ.a)
  tipo: "proposta"|"exclusividade"|"contrato",
  step: 0|1,
  proponente:{nome:"",doc:"",endereco:""}, vendedor:{nome:"",doc:"",endereco:""},
  comprador:{nome:"",doc:"",endereco:""}, proprietario:{nome:"",doc:"",endereco:""},
  corretor:{nome:"",creci:""},   // pré-preenchido de radar_prof (leitura) SÓ quando tipo==="exclusividade"
  valorOfertado:null, formaPagamento:"A combinar", validadeDias:10,
  prazoDias:90, comissaoPct:6, exclusiva:true, autorizaAnuncio:true,
  precoTotal:null, matricula:"", cartorio:"",
  matTexto:"",
  minutaTexto:"",       // gerado por propostaTexto/termoExclusividadeTexto/contratoTexto ao ENTRAR no passo 1
  minutaEditada:null,   // grava a edição do textarea; null = usuário não editou nesta geração
  _st: undefined        // controle interno de negRender (stepMudou), mesmo padrão de LZ._st
}
```

Nota: `abrirNeg` inicializa `exclusiva:true` e `autorizaAnuncio:true` como defaults visuais do segmented control (`.seg` já abre com "Sim" marcado) — coerente com o UI-SPEC (markup de exemplo mostra `aria-pressed="true"` no botão "Sim").

## Assinatura Exata de montarNeg() e Ponto de Substituição da Wave 3

```js
function montarNeg(): void
```

Corpo atual (Wave 2, mínimo): lê `NEG.minutaEditada!=null?NEG.minutaEditada:NEG.minutaTexto`, monta `#laudo.innerHTML` com `.lrun` + `.lbrand` (cabeçalho "Radar Fundiário · Documentos da Negociação" + título do documento) + `<div style="white-space:pre-wrap">${esc(texto)}</div>` + `.foot`, chama `fecharNeg()`, copia para `#laudoViewBody`, exibe `#laudoView`.

**Ponto exato onde a Wave 3 substitui:** o bloco `document.getElementById("laudo").innerHTML=...` inteiro (linhas do corpo de `montarNeg`, entre a leitura de `texto`/`titulo` e o `fecharNeg()`) é o único trecho a trocar pelo template A4 estruturado (Componente 4 do UI-SPEC: `.lrun`/`.lbrand`/`.lcard`(Partes)/`.lcard`(Do imóvel)/`.negclausulas`+`.negclausula-item`/`.negdisclaimer-print`/`.lass`+`.lass-testemunhas`/`.foot`). O contrato de chamada (`montarNeg()` sem argumentos, chamado só por `negNext()` no passo 1, seguido de `fecharNeg()`+cópia para `#laudoViewBody`+exibição de `#laudoView`) fica ESTÁVEL — Wave 3 não precisa tocar em `negNext()`/`negRender()`/Esc chain/entradas.

## Resultado do Grep de Privacidade

```
grep -c "localStorage.setItem" radar-goiania.html → 6 (inalterado — radar_sat, radar_lastbairro,
  radar_oportunidades, radar_historico, radar_prof, radar_coach; nenhuma nova, nenhuma existente
  passou a receber dado do NEG)
grep -c "montarNeg(" radar-goiania.html → 2 (definição em `function montarNeg(){` + chamada única
  em `negNext()`; comentários explicativos foram redigidos evitando repetir a assinatura literal
  para não inflar a contagem do grep do verify)
```

Verificação cruzada adicional (script Node de extração de corpo de função, não incluído no verify automatizado do plano mas documentado aqui): nenhuma das 7 funções `neg*` (`abrirNeg/fecharNeg/negBack/negRender/negExtrair/negNext/montarNeg`) referencia `LZ`; nenhuma das 14 funções do fluxo do laudo (`abrirSeletorFinalidade/finSet/finEscolherDoc/fecharLaudo/wizBack/wizSet/wizDif/wizFoto/wizRmFoto/wizNext/wizRender/montarLaudo/montarFichaRapida/renderFichaRapida`) referencia `NEG`.

## Posição Final do negSheet na Cadeia de Esc

`#negSheet` é o **PRIMEIRO check** do listener `keydown` global (linha ~3903 antes desta plan, deslocada pelo crescimento do arquivo): `if(ns&&!ns.hidden){fecharNeg();return;}`, imediatamente seguido pelo check pré-existente de `#captSheet`. Ordem completa inalterada por baixo: `#negSheet` → `#captSheet` → `#caixaList` → `#correctMenu` → `#ambigChips` → `#calc` → `#wiz` → `#laudoView` → `#chooser` → busca desktop → `closeDetail()`. Isso garante que, se o Termo de Exclusividade for aberto a partir do `#captSheet`, o primeiro Esc fecha o `#negSheet` (mais recente) e o segundo Esc fecha o `#captSheet` por baixo.

## Decisions Made

Ver `key-decisions` no frontmatter (forma do `montarNeg()` mínimo; comentários evitando repetir a string `montarNeg(` fora da definição/chamada; `.trim()` nos campos de nome da validação).

## Deviations from Plan

None - plan executado exatamente como escrito, incluindo a correção de redação dos comentários de código para não inflar a contagem literal do grep `montarNeg(` (ajuste de comentário, não de comportamento — o comportamento sempre foi "definição + único gatilho").

## Issues Encountered

Durante a Task 2, o primeiro rascunho dos comentários de `negNext()`/`montarNeg()` mencionava a string `montarNeg()` dentro do próprio comentário explicativo, o que elevou `grep -c "montarNeg("` para 4 em vez do 2 exigido pelo verify automatizado do plano (`ec9f129`). Resolvido reescrevendo os comentários para descrever o comportamento sem repetir a assinatura literal da função — nenhuma mudança de comportamento, só de texto de comentário. Verificado novamente: `grep -c "montarNeg(" radar-goiania.html` = 2.

## User Setup Required

None - nenhuma configuração de serviço externo.

## Next Phase Readiness

- Wave 3 (11.1-03) pode substituir o corpo de `montarNeg()` pelo template A4 estruturado (cláusulas em blocos `.negclausula-item`, disclaimer em caixa `.negdisclaimer-print`, blocos de assinatura `.lass`/`.lass-testemunhas`) sem tocar em `negNext()`, `negRender()`, Esc chain, ou nas 4 entradas — o contrato de chamada está estável
- Wave 3 precisa da estrutura de cláusulas com numeração DINÂMICA do Termo de Exclusividade (documentada no 11.1-01-SUMMARY.md) — não hardcodar "6ª"/"7ª" fixo no HTML impresso
- `NEG.a` (cópia local do imóvel) já traz todos os campos necessários (`addrDe`, `clean(nmbairro/nrquadra/nrlote)`, `areaterr/areaedif`, `nrinscr/ci`, `unitLabel/USO`) para a Wave 3 montar o bloco "Do imóvel" do template A4 sem recálculo adicional
- Nenhum bloqueio conhecido

---
*Phase: 11.1-documentos-da-negociacao*
*Completed: 2026-07-07*

## Self-Check: PASSED

All claimed files verified present (radar-goiania.html, this SUMMARY.md) and both commit hashes (ba45870, 71fc8b2) verified present in git log.
