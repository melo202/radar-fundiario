---
phase: 16-detector-subutilizado-farming-caderno
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - radar-goiania.html
autonomous: true
requirements: [TERR-05]
tags: [caderno, indexeddb, persistence, allowlist, export-import, ui]
user_setup: []

must_haves:
  truths:
    - "Salvar um lote grava no IndexedDB radar_territorio store caderno, sanitizado pela allowlist"
    - "O bloco '📓 Caderno de território' aparece no painel Consulta após o Histórico, com filtros por setor/status e itens editáveis (status/tag/nota) com autosave"
    - "Botão '📓 Salvar no caderno' na ficha (dentro de #dActsMore) grava e reflete estado '✓ No caderno'"
    - "Falha de escrita no IndexedDB SEMPRE mostra toast (nunca silenciosa); feature-detect desabilita os botões com aviso quando IndexedDB indisponível"
    - "Export/import JSON do caderno; import passa toda entrada por sanitizeCaderno (nunca reintroduz PII)"
  artifacts:
    - path: "radar-goiania.html"
      provides: "Bloco CADERNO_IO (wrapper IndexedDB nativo) + #cadernoBlock UI + botão na ficha + export/import"
      contains: "indexedDB.open"
  key_links:
    - from: "cadernoSalvar"
      to: "sanitizeCaderno + IndexedDB"
      via: "sanitiza antes de put; transaction readwrite"
      pattern: "sanitizeCaderno"
    - from: "botão 📓 Salvar no caderno (ficha)"
      to: "cadernoSalvar"
      via: "onclick com .then/.catch(toast)"
      pattern: "cadernoSalvar"
    - from: "renderCadernoBlock"
      to: "cadernoListar"
      via: "getAll do store/índice, render assíncrono de #cadernoBlock"
      pattern: "cadernoListar"
---

<objective>
Implementar a camada de persistência IndexedDB do Caderno (TERR-05) — wrapper nativo mínimo promise-based — e a UI do Caderno no painel Consulta: bloco "📓 Caderno de território", botão "📓 Salvar no caderno" na ficha, export/import JSON. Toda escrita sanitizada pela allowlist do Plano 01; toda falha visível via toast.

Purpose: Dar ao corretor um caderno de território que persiste entre sessões, sem risco de PII, com falha nunca silenciosa.
Output: Bloco de I/O `CADERNO_IO`, `#cadernoBlock` renderizado, botão na ficha e export/import funcionais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-detector-subutilizado-farming-caderno/16-RESEARCH.md
@.planning/phases/16-detector-subutilizado-farming-caderno/16-UI-SPEC.md
@.planning/phases/16-detector-subutilizado-farming-caderno/16-VALIDATION.md
@.planning/phases/16-detector-subutilizado-farming-caderno/16-01-SUMMARY.md

<interfaces>
<!-- Do Plano 01 (RADAR_PURE) — consumidos por esta plan: -->
sanitizeCaderno(obj)          // allowlist positiva — aplicar em TODA escrita (salvar/editar/import)
CADERNO_STATUS                // ["nao_visitado","visitei","conversei","recusou","fechou"]
statusValido(s)               // guard de enum
validarImportCaderno(json)    // {ok, itens} — usar no import antes de gravar

<!-- Âncoras existentes em radar-goiania.html: -->
#savedBlocks (linha 921)          // container estático irmão de #results; renderSavedBlocks() preenche innerHTML (síncrono — NÃO tocar)
renderSavedBlocks() (4222)        // monta oppBlock+histBlock; síncrono. NÃO reescrever contrato — Caderno é async e vive em container próprio.
boot: renderSavedBlocks() (5459)  // chamar renderCadernoBlock() ao lado
#dActsMore (HTML 1024; populado 4086-4108)  // rodapé "Mais opções" da ficha — DESTINO do botão do caderno (decisão locked: NÃO em #dActsPrim, teto da lei da tela já esgotado)
renderSaveBtn() (4187-4196)       // sincroniza ⭐ da ficha (síncrono, lê oppTem). renderCadernoBtn() é IRMÃ e ASSÍNCRONA (não bloqueia render).
DCUR                              // inscrição/ci da ficha aberta (usado por renderSaveBtn)
exportCSV() (5358-5377)           // padrão de download: Blob + URL.createObjectURL + <a download> + revokeObjectURL. Reusar esqueleto p/ export JSON.
toast(msg) (2914)                 // canal único de feedback
esc()                             // escape de HTML — usar em TODA nota/tag/endereço renderizada
</interfaces>

<idb_design>
<!-- Schema v1 (RESEARCH §IndexedDB). onupgradeneeded já preparado para v2 (Fase 17 = store "snapshots"). -->
DB "radar_territorio", version 1.
store "caderno" keyPath "ci"; index "cdbairro" (unique:false); index "status" (unique:false).
store "setores" keyPath "cdbairro".
onupgradeneeded: criar stores se ausentes; comentário reservando `if(e.oldVersion<2){...snapshots...}` para a Fase 17.
</idb_design>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrapper IndexedDB nativo (bloco CADERNO_IO) + CSS</name>
  <read_first>
    - radar-goiania.html (linha 3042 TERR_NET_END — inserir o bloco DEPOIS; 5358-5377 exportCSV; 1221-1222 SENS/sanitiza)
    - .planning/phases/16-detector-subutilizado-farming-caderno/16-RESEARCH.md (§IndexedDB — wrapper mínimo, CRUD promise-based, feature-detect)
    - .planning/phases/16-detector-subutilizado-farming-caderno/16-UI-SPEC.md (§4 Persistência; §Copywriting — mensagens de erro exatas)
  </read_first>
  <files>radar-goiania.html</files>
  <action>
    Criar um bloco delimitado `// CADERNO_IO_START` / `// CADERNO_IO_END` após `TERR_NET_END` (linha 3042). Fora de RADAR_PURE (é I/O real, não testável em node:vm). Implementar:
    - `const CADERNO_DB="radar_territorio"; const CADERNO_VERSION=1;`
    - `function cadernoDisponivel(){ return typeof window!=="undefined" && !!window.indexedDB; }`
    - `function cadernoAbrirDB()`: Promise; reject se `!window.indexedDB`; `indexedDB.open(CADERNO_DB,CADERNO_VERSION)`; `onupgradeneeded` cria store `caderno` (keyPath "ci") com índices `cdbairro`/`status` e store `setores` (keyPath "cdbairro"), com o comentário reservando v2/snapshots (Fase 17); `onsuccess`/`onerror` resolve/reject.
    - `function cadernoSalvar(itemBruto)`: `const item=sanitizeCaderno(Object.assign({},itemBruto,{status:itemBruto.status||"nao_visitado",savedAt:new Date().toISOString()}));` → abrir DB → transaction("caderno","readwrite").put(item) → resolve(item) no oncomplete / reject no onerror.
    - `function cadernoAtualizar(ci,patch)`: lê o item por `ci`, aplica patch (status via statusValido — ignora status inválido), `sanitizeCaderno` com `updatedAt`, put.
    - `function cadernoRemover(ci)`: transaction readwrite delete(ci).
    - `function cadernoListar(opts)`: opts `{cdbairro,status}`; se cdbairro → `store.index("cdbairro").getAll(cdbairro)`, senão `store.getAll()`; filtra status em memória se ambos; resolve array.
    - `function cadernoTem(ci)`: resolve boolean (get por ci !== undefined).
    - `function cadernoContar()`: resolve count().
    TODAS as funções são Promise-based; NENHUMA função de I/O engole erro — o `.catch` fica no call-site da UI (Tasks 2/3).
    Adicionar CSS novo (junto ao CSS de `.savedblock`, ~748-761) para `.cadbook-filtros`, `.cadbook-item`, `.cadbook-editor`, `.cadbook-statuschips`, `.cadbook-nota`, `.cadbook-export-row`, `.terrdet-*` (as do detector podem ficar p/ o Plano 03) — seguir a escala de 4px e tokens da UI-SPEC (§Spacing, §Color; nenhuma cor nova; textarea min-height 80px). Só `.cadbook-*` nesta plan.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "^(ℹ (pass|fail))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CADERNO_IO_START\|CADERNO_IO_END" radar-goiania.html` = 2
    - `grep -c "indexedDB.open" radar-goiania.html` ≥ 1
    - `grep -c "function cadernoAbrirDB\|function cadernoSalvar\|function cadernoAtualizar\|function cadernoRemover\|function cadernoListar\|function cadernoTem\|function cadernoDisponivel" radar-goiania.html` = 7
    - `grep -n "cadernoSalvar" radar-goiania.html` mostra `sanitizeCaderno` no corpo (sanitiza antes de gravar)
    - `grep -c "objectStore(\"caderno\"\|createObjectStore(\"caderno\"\|createIndex(\"cdbairro\"\|createIndex(\"status\"" radar-goiania.html` ≥ 3
    - `grep -c "oldVersion<2\|snapshots" radar-goiania.html` ≥ 1 (caminho v2 reservado)
    - `npm test` → `fail 0` (bloco de I/O não roda em node:vm; suíte pura intocada)
  </acceptance_criteria>
  <done>Wrapper IndexedDB completo (abrir/CRUD/listar/tem/contar/feature-detect), schema v1 com índices e caminho v2 reservado, CSS do caderno; suíte pura verde.</done>
</task>

<task type="auto">
  <name>Task 2: Bloco "📓 Caderno de território" no painel Consulta</name>
  <read_first>
    - radar-goiania.html (921 #savedBlocks; 4217-4271 renderSavedBlocks; 5459 chamada no boot; CSS .savedblock 748-761)
    - .planning/phases/16-detector-subutilizado-farming-caderno/16-UI-SPEC.md (§3 Bloco Caderno de território — estrutura, item <details>, paginação, autosave, duplo-toque; §Copywriting; §5 Acessibilidade)
  </read_first>
  <files>radar-goiania.html</files>
  <action>
    1. Adicionar HTML estático `<div id="cadernoBlock"></div>` dentro do painel Consulta, DEPOIS de `#savedBlocks` (ou como último filho lógico após o bloco Histórico) — container próprio, análogo a `#terrPanel` ser HTML estático que só recebe conteúdo.
    2. Criar `async function renderCadernoBlock()` que:
       - se `!cadernoDisponivel()`: renderiza o bloco com a `.dnote` de degradação anunciada (copy locked: "Seu navegador não permite salvar itens no caderno neste dispositivo. Suas consultas continuam funcionando normalmente.") e retorna.
       - senão `cadernoListar(filtros)` → monta `.savedblock` reusando `.savedblock-head`/`.savedblock-tt`/`.savedblock-count`: cabeçalho "📓 Caderno de território" + `#cadernoCount`; `.dnote` LGPD "Suas notas ficam só no seu aparelho — nada é enviado a servidor algum."; filtros `#cadernoFiltroSetor` + `#cadernoFiltroStatus` (labels visualmente ocultos; opções "Todos os setores"/"Todos os status" + os 5 status); lista `#cadernoList` de `<details class="cadbook-item">`; linha export/import `.cadbook-export-row`; empty state `.savedempty` (copy locked) quando 0 itens sem filtro.
       - **Toda `nota`/`tag`/`endereco`/nome de setor via `esc()`** (nunca innerHTML cru — T-16-03/XSS).
       - Paginação: 30 itens (ordenados savedAt desc), botão "Mostrar mais 30"; troca de filtro reinicia paginação.
       - `<summary>` compacto: chip de status (dot 8px + rótulo; neutro exceto "Fechou"=`--status-bom`), endereço/insc, meta "Q{quadra} · L{lote} · {bairro}", prévia da nota truncada, marcador ▸/▾.
       - Editor aberto `.cadbook-editor`: 5 chips de status (`role="group"` aria-label="Status do lote"; cada `<button aria-pressed>`; "Fechou" ativo verde, demais neutro) com autosave via `cadernoAtualizar(ci,{status}).then(renderCadernoBlock).catch(()=>toast(ERRO_ESCRITA))`; input tag (maxlength 40) autosave no blur; textarea nota (maxlength 500) autosave no blur — SEM toast em sucesso (autosave silencioso); botão "Remover do caderno" (var(--status-risco)) com duplo-toque (1º toque muda texto para "Toque de novo para remover — a nota se perde" por 3s; 2º toque → `cadernoRemover(ci)` + toast "Removido do caderno.").
    3. Chamar `renderCadernoBlock()` no boot ao lado da linha 5459 (`renderSavedBlocks();`) e após cada escrita no caderno. NÃO alterar `renderSavedBlocks()` (permanece síncrono, intocado — decisão locked de não tocar Oportunidades/Histórico).
    4. Definir uma constante única de mensagem de erro de escrita reusável (copy locked §26.3): "Não foi possível salvar no caderno. Verifique o espaço do navegador e tente de novo." — usada em todos os `.catch` desta e das próximas tasks.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "^(ℹ (pass|fail))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "id=\"cadernoBlock\"" radar-goiania.html` = 1
    - `grep -c "async function renderCadernoBlock\|function renderCadernoBlock" radar-goiania.html` = 1
    - `grep -c "Caderno de território" radar-goiania.html` ≥ 1
    - `grep -c "Suas notas ficam só no seu aparelho" radar-goiania.html` = 1
    - `grep -c "Não foi possível salvar no caderno" radar-goiania.html` ≥ 1
    - `grep -c "renderCadernoBlock()" radar-goiania.html` ≥ 2 (boot + após escrita)
    - `grep -n "cadernoAtualizar\|cadernoRemover" radar-goiania.html` mostram `.catch` no call-site
    - `grep -c "esc(" radar-goiania.html` aumentou (nota/tag/endereço escapados)
    - `npm test` → `fail 0`
  </acceptance_criteria>
  <done>Bloco Caderno renderiza (ou degrada anunciado), filtros/paginação/editor com autosave e remover com duplo-toque; texto do usuário sempre escapado; suíte verde.</done>
</task>

<task type="auto">
  <name>Task 3: Botão "📓 Salvar no caderno" na ficha (#dActsMore) + export/import JSON</name>
  <read_first>
    - radar-goiania.html (4070-4108 #dActsPrim/#dActsMore + comentário da lei da tela; 4187-4196 renderSaveBtn; 5358-5377 exportCSV)
    - .planning/phases/16-detector-subutilizado-farming-caderno/16-UI-SPEC.md (§2 Ação Salvar no caderno; §3.5 export/import; §Copywriting — toasts exatos)
    - .planning/phases/16-detector-subutilizado-farming-caderno/16-RESEARCH.md (§Ficha; Pitfall 4 renderSaveBtn síncrono vs assíncrono)
  </read_first>
  <files>radar-goiania.html</files>
  <action>
    1. **Botão na ficha em #dActsMore** (decisão locked pelo orquestrador — NÃO em #dActsPrim, cujo teto "1 primária + 2 secundárias" já está esgotado): adicionar "📓 Salvar no caderno" ao template de `#dActsMore` (populado ~4086), botão sem `.primary`, `onclick="salvarNoCadernoUI()"`, id p/ sincronização (ex. `#cadernoBtn`).
    2. `function salvarNoCadernoUI()`: se `!cadernoDisponivel()` → toast de degradação e retorna; monta o item a partir do registro cadastral da ficha aberta (DCUR/objeto atual), passando SÓ os campos cadastrais brutos (sanitizeCaderno filtra); `cadernoSalvar(item).then(()=>{toast("Salvo no caderno."); renderCadernoBtn(); renderCadernoBlock();}).catch(()=>toast(ERRO_ESCRITA))`. Se já existe (mesmo ci), em vez de duplicar, faz scroll-into-view do item no `#cadernoBlock` para edição.
    3. `async function renderCadernoBtn()` (IRMÃ de renderSaveBtn, mas ASSÍNCRONA — Pitfall 4): estado neutro até resolver; `cadernoTem(DCUR).then(salvo=>{ ...alterna rótulo "📓 Salvar no caderno" ↔ "✓ No caderno" e classe is-saved... })`; NUNCA bloqueia o render da ficha. Se `!cadernoDisponivel()`, botão desabilitado com `title` de degradação. Chamar `renderCadernoBtn()` no mesmo ponto onde `renderSaveBtn()` é chamado ao montar a ficha (~4077) — mas sem `await` que trave o resto.
    4. **Export**: `function cadernoExportarJSON()` reusa o esqueleto de `exportCSV()` (Blob type "application/json", `JSON.stringify(itens,null,2)`, `<a download>` "caderno-territorio.json", revokeObjectURL); toast "{N} lotes exportados para JSON." (0 itens → "0 lotes exportados para JSON."). Botão "⬇ Exportar caderno (JSON)".
    5. **Import**: `<input type="file" accept="application/json" hidden id="cadernoImportFile">` + botão "⬆ Importar caderno" que dispara o input; no change, `FileReader` lê texto → `validarImportCaderno(texto)`; se `!ok` → toast "Arquivo inválido — não é um backup do caderno. Nada foi importado."; se ok → grava cada item via `cadernoSalvar` (mescla por ci, aditivo) → toast "{N} lotes importados para o caderno." → `renderCadernoBlock()`. Import passa TODA entrada por `sanitizeCaderno` (via validarImportCaderno) — nunca reintroduz PII (T-16-02).
    6. Cablear os botões export/import na `.cadbook-export-row` do Plano 02 Task 2.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "^(ℹ (pass|fail))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "📓 Salvar no caderno" radar-goiania.html` ≥ 2 (ficha + item do detector virá no Plano 03; aqui ≥1 na ficha + referência)
    - `grep -c "function salvarNoCadernoUI\|function renderCadernoBtn\|function cadernoExportarJSON" radar-goiania.html` = 3
    - `grep -n "salvarNoCadernoUI\|cadernoExportarJSON\|cadernoImportarJSON\|cadernoSalvar" radar-goiania.html` — cada call-site de UI tem `.catch` (Pitfall 3)
    - `grep -c "renderCadernoBtn()" radar-goiania.html` ≥ 2 (montagem da ficha + após salvar)
    - `grep -c "validarImportCaderno" radar-goiania.html` ≥ 1 (import valida shape+allowlist)
    - `grep -c "lotes exportados para JSON\|lotes importados para o caderno\|Arquivo inválido — não é um backup" radar-goiania.html` = 3
    - `grep -c "type=\"file\"" radar-goiania.html` aumentou em 1 (input de import)
    - `npm test` → `fail 0`
  </acceptance_criteria>
  <done>Botão "Salvar no caderno" na ficha em #dActsMore com estado sincronizado assíncrono; export/import JSON funcional com import sanitizado; suíte verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ficha/detector → IndexedDB | dado cadastral gravado no armazenamento local |
| arquivo JSON de import → IndexedDB | arquivo externo entra na persistência |
| nota/tag do corretor → DOM | texto livre do usuário renderizado no bloco Caderno |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | cadernoSalvar/cadernoAtualizar | mitigate | Toda escrita passa por sanitizeCaderno (allowlist) — nunca `dtnascimen`/titular. Verificação manual (DevTools dump) no Plano 03. |
| T-16-02 | Tampering | cadernoImportarJSON | mitigate | validarImportCaderno + sanitizeCaderno por item; nunca eval/innerHTML do arquivo; JSON inválido → toast, nada gravado. |
| T-16-03 | Tampering (XSS) | renderCadernoBlock | mitigate | nota/tag/endereço/setor renderizados via esc()/textContent SEMPRE — nunca interpolados crus em innerHTML. |
| T-16-04 | Denial of Service (local) | todos os call-sites caderno* | mitigate | Todo `.catch` mostra toast §26.3 (falha nunca silenciosa); feature-detect desabilita botões com aviso quando IndexedDB indisponível. |
</threat_model>

<verification>
- `npm test` verde (a suíte pura não regride; I/O verificado manualmente no Plano 03).
- Grep confirma: sanitizeCaderno em toda escrita; `.catch`+toast em todo call-site; esc() em todo texto do usuário; import valida via validarImportCaderno.
- Feature-detect (`cadernoDisponivel`) gate nos botões e mensagem de degradação presente.
</verification>

<success_criteria>
- Wrapper IndexedDB v1 (stores caderno/setores, índices cdbairro/status, caminho v2 reservado).
- Bloco Caderno no painel Consulta (filtros, paginação, editor com autosave, remover com duplo-toque).
- Botão na ficha (#dActsMore) + export/import JSON, import sanitizado.
- Nenhuma falha silenciosa; nenhuma PII gravável; nenhuma dependência nova.
</success_criteria>

<output>
Após completar, criar `.planning/phases/16-detector-subutilizado-farming-caderno/16-02-SUMMARY.md`.
</output>
