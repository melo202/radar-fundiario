---
phase: 19-estetica-premium-tipografia
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - radar-goiania.html
autonomous: true
requirements: [PREM-01]
security_enforcement: true

must_haves:
  truths:
    - "Cards/sheets/dropdowns/toasts têm elevação coerente derivada de 3 níveis nomeados"
    - "Nenhuma sombra ad hoc sobrevive fora dos tokens --elev-* (exceto os 4 casos justificados: keyframe ring, print none, dot ring inline, def antiga)"
    - "Hover/active/focus dão feedback premium sem introduzir cor nova (cor-só-status/VIS-01 preservada)"
    - "Nenhum componente muda de posição/estrutura — é acabamento, não re-layout"
    - "Nenhuma sombra cara é aplicada em camada do mapa (performance mobile)"
  artifacts:
    - path: "radar-goiania.html"
      provides: "Tokens --elev-0/1/2/3 no :root, 13 sombras ad hoc consolidadas nos tokens, refinamento de hover/active/focus/divisores"
      contains: "--elev-1"
  key_links:
    - from: "componentes elevados (.combo-list, .onb-card, .chooser, .toast, .wiz)"
      to: ":root --elev-*"
      via: "box-shadow:var(--elev-N)"
      pattern: "var\\(--elev-"
---

<objective>
Consolidar a elevação heterogênea (17 `box-shadow` ad hoc) em 3 níveis nomeados (`--elev-1/2/3` + repouso `--elev-0:none`), todos derivados de `--ink` (tinta sobre papel, nunca preto puro), e aplicar o acabamento premium (hover/active/focus, divisores mais sutis, polimento de chips/inputs/botões) conforme o UI-SPEC — mantendo papel/óxido, cor-só-status e o radius angular 1-2px.

Purpose: PREM-01. Roda DEPOIS do Plano 01 (mesmo arquivo, dependência sequencial) porque a hierarquia tipográfica da fonte nova é o pano de fundo sobre o qual o acabamento é calibrado.
Output: `radar-goiania.html` com sistema de elevação consistente e microdetalhes premium — escopo estritamente de ACABAMENTO, nenhum re-layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-estetica-premium-tipografia/19-RESEARCH.md
@.planning/phases/19-estetica-premium-tipografia/19-UI-SPEC.md
@.planning/phases/19-estetica-premium-tipografia/19-VALIDATION.md
@.planning/phases/19-estetica-premium-tipografia/19-01-SUMMARY.md

<interfaces>
<!-- Inventário de box-shadow VERIFICADO na pesquisa (grep -c 'box-shadow' → 17). Mapa linha→token: -->
Tokens a definir no :root (valores travados no UI-SPEC):
  --elev-0: none;                          /* repouso: cards em fluxo (borda já demarca) */
  --elev-1: 0 2px 6px rgba(20,26,31,.10);  /* flutuante discreto sobre o mapa */
  --elev-2: 0 6px 18px rgba(20,26,31,.16); /* dropdowns/menus/combo */
  --elev-3: 0 10px 28px rgba(20,26,31,.24);/* modais/sheets */

Mapa de substituição (13 sombras → token; 4 ficam fora do sistema):
  linha 38  --shadow:0 2px 0 rgba(20,26,31,.08)  → REMOVER def antiga, substituída pelos 4 tokens (grep pelos usos de var(--shadow) e trocar por var(--elev-1))
  linha 145 0 8px 20px rgba(20,26,31,.18) .combo-list   → var(--elev-2)
  linha 184 0 0 0 3px var(--accent) @keyframes bldgflash → FORA (ring de destaque, não elevação) — MANTER
  linha 207 var(--shadow) genérico                       → var(--elev-1)
  linha 261 0 2px 8px rgba(0,0,0,.25) .zapfab (FAB)       → var(--elev-1)
  linha 307 0 4px 14px rgba(20,26,31,.18) botão flutuante → var(--elev-1)
  linha 314 var(--shadow)                                 → var(--elev-1)
  linha 320 0 4px 14px rgba(20,26,31,.18)                 → var(--elev-1)
  linha 334 var(--shadow)                                 → var(--elev-1)
  linha 344 var(--shadow)                                 → var(--elev-1)
  linha 383 0 8px 24px rgba(20,26,31,.3) .onb-card        → var(--elev-3)
  linha 396 0 6px 20px rgba(20,26,31,.22) sheet/.wiz      → var(--elev-3)
  linha 530 0 4px 14px rgba(20,26,31,.18) botão flutuante → var(--elev-1)
  linha 744 box-shadow:none #laudo .lcard (print)         → FORA (papel não tem sombra) — MANTER
  linha 890 0 4px 14px rgba(0,0,0,.25) .toast             → var(--elev-1)
  linha 899 0 3px 10px rgba(20,26,31,.2) variante toast   → var(--elev-1)
  linha 955 0 6px 20px rgba(20,26,31,.22) .chooser        → var(--elev-3)
  linha 1119 box-shadow:0 0 0 2px var(--ink) inline (dot ring legenda) → FORA (não elevação) — MANTER

Paleta (herdada, NÃO alterar): --paper #e9e4d8 · --paper-2 #f2eee4 · --ink #141a1f · --accent #b5451f · --accent-ink · --line #c3b9a3 · --grid #d8d0be
:focus-visible global (linha ~139): outline:2px solid var(--accent);outline-offset:2px — já correto.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Definir tokens --elev-0/1/2/3 no :root e consolidar as 13 sombras ad hoc</name>
  <read_first>
    - radar-goiania.html :root (onde ficam --status-*/--terr-*/--paper etc.) e a def antiga `--shadow` (linha 38)
    - radar-goiania.html cada uma das 17 linhas de box-shadow do mapa acima
    - 19-UI-SPEC.md "Sistema de Elevação (PREM-01)" (tabela nível→token→substitui) e 19-RESEARCH.md "Inventário de box-shadow ad hoc"
  </read_first>
  <files>radar-goiania.html</files>
  <action>
(A) Adicionar os 4 tokens ao `:root` (junto dos tokens existentes), com o comentário do UI-SPEC ("3 níveis derivados de --ink, nunca preto puro"). Valores exatos do bloco &lt;interfaces&gt;.

(B) Substituir as 13 sombras mapeadas por `box-shadow:var(--elev-N)` conforme a tabela. Onde o valor atual é `var(--shadow)` (linhas 207/314/334/344), trocar por `var(--elev-1)`. Remover a def antiga `--shadow` (linha 38) APÓS confirmar que nenhum `var(--shadow)` restou (grep). Onde o valor é literal (145/261/307/320/383/396/530/890/899/955), trocar o literal pelo token correspondente.

(C) NÃO tocar as 4 exceções justificadas: keyframe ring (184), print none (744), dot ring inline (1119), — e a def 38 só é removida porque migra para os tokens. Papel impresso continua sem sombra.

(D) Performance (T-19-06): os tokens usam blur pequeno (2-10px) sobre --ink em baixa opacidade — nenhuma sombra é aplicada a camada Leaflet/tiles do mapa (só a elementos flutuantes DOM sobre o mapa, ex.: .pino-legenda/.toast/FAB). Confirmar que nenhum seletor de tile/pane recebe box-shadow.

Regressão zero (T-19-05): nenhum id/classe/handler muda; só o valor de box-shadow e a adição dos tokens.
  </action>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && grep -q -- '--elev-1: 0 2px 6px' radar-goiania.html && grep -q -- '--elev-2: 0 6px 18px' radar-goiania.html && grep -q -- '--elev-3: 0 10px 28px' radar-goiania.html && [ "$(grep -c 'var(--shadow)' radar-goiania.html)" = "0" ] && [ "$(grep -c 'var(--elev-' radar-goiania.html)" -ge "13" ] && npm test 2>&1 | grep -q 'pass 239' && echo OK</automated>
  </verify>
  <done>4 tokens --elev-* no :root; def --shadow antiga removida e zero `var(--shadow)` restante; ≥13 usos de `var(--elev-*)`; 4 exceções (184/744/1119 + a migração da 38) preservadas; nenhuma sombra em camada de mapa; suíte 239 verde.</done>
</task>

<task type="auto">
  <name>Task 2: Acabamento — hover/active/focus, divisores, chips/inputs/botões</name>
  <read_first>
    - radar-goiania.html seletores .card, .go/.onb-next/.acts button, .moderow button, #insc/#rua/.combo-item/#caixaInput, .cmp-table td
    - 19-UI-SPEC.md "Refinamentos de Acabamento por Componente" e "Estados Hover / Active / Focus"
  </read_first>
  <files>radar-goiania.html</files>
  <action>
Aplicar os refinamentos da tabela do UI-SPEC — SEMPRE via borda/preenchimento/sombra existentes, NUNCA cor nova (VIS-01: cor só onde significa status):

  - Cards (.card): repouso `--elev-0` (sem sombra, borda 1.5px demarca) → `:hover` ganha `--elev-1` sutil + `border-color:var(--ink)`; adicionar `box-shadow` e `border-color` à transição já existente: `transition:box-shadow .15s ease, border-color .12s ease` (preservar transform existente).
  - Botões primários (.go, .onb-next, .acts button.primary): `:active{transform:scale(.98)}` (feedback de pressão) + `transition:transform .08s ease, background .15s ease`; hover `background:var(--accent-ink)` (já existente — confirmar, não duplicar).
  - Botões secundários (.acts button:not(.primary), .moderow button): `:hover{background:var(--paper-2)}` (preenchimento sutil, sem cor) + `transition:background .12s ease`.
  - Inputs (#insc, #rua, .combo-item, #caixaInput): manter `:focus-visible` outline herdado + escurecer a BORDA para `var(--ink)` no focus (reforço duplo) + `border-color .12s ease`.
  - Divisores de alta densidade: `.cmp-table td{border-bottom:1px solid var(--grid)}` (era `--line` — único ajuste de cor de divisor da fase, dentro da paleta existente).

Reduced-motion (T-19-07): hover/active/focus são transições de estado <150ms (não @keyframes decorativo), consistente com o critério das Fases 6/13 — nenhuma nova animação decorativa introduzida; não tocar a lógica de REDUCE.
Escopo é acabamento: nenhum componente muda de posição/tamanho/estrutura.
  </action>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && grep -q 'scale(.98)' radar-goiania.html && grep -q 'border-bottom:1px solid var(--grid)' radar-goiania.html && grep -q 'transition:box-shadow' radar-goiania.html && npm test 2>&1 | grep -q 'pass 239' && echo OK</automated>
  </verify>
  <done>Cards com hover --elev-1+borda escura; botões primários com :active scale(.98); secundários com hover paper-2; inputs com reforço de borda no focus; .cmp-table divisor em --grid; nenhuma cor nova; suíte 239 verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS de elevação → camada do mapa | sombras caras (blur grande) sobre tiles/panes do Leaflet degradariam performance mobile |
| refinamento visual → identidade | qualquer cor nova violaria VIS-01 (cor só onde significa status) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Tampering (regressão) | 13 substituições de box-shadow + refinos | mitigate | Só o valor de box-shadow/transition/border-color muda; nenhum id/classe/handler/string funcional tocado; suíte 239 verde a cada task |
| T-19-06 | Denial of Service (performance) | camada de mapa Leaflet | mitigate | Tokens usam blur pequeno (2-10px); confirmar por leitura que nenhum seletor de tile/pane recebe box-shadow — só elementos flutuantes DOM sobre o mapa |
| T-19-07 | (qualidade a11y) | hover/active/focus | accept | Transições de estado <150ms não são motion decorativo (critério Fases 6/13); não introduzem @keyframes; REDUCE intocado |
| T-19-08 | Information Disclosure (VIS-01) | estados hover/active | mitigate | Nenhuma cor nova — só borda (--ink), preenchimento (--paper-2) ou accent→accent-ink já existente; verificável por ausência de novos hex |
</threat_model>

<verification>
- `grep -q -- '--elev-1'/'--elev-2'/'--elev-3'` no :root
- `grep -c 'var(--shadow)'` → 0 (def antiga migrada) · `grep -c 'var(--elev-'` ≥ 13
- 4 exceções fora do sistema preservadas (keyframe 184, print 744, dot ring 1119, e a migração da def 38)
- Nenhum hex de cor novo · nenhuma sombra em camada de mapa
- `grep -q 'scale(.98)'` e `border-bottom:1px solid var(--grid)`
- `npm test` → 239 verdes
</verification>

<success_criteria>
Elevação consolidada em 3 níveis nomeados derivados de --ink; zero sombra ad hoc fora dos tokens (salvo as 4 exceções justificadas); hover/active/focus premium sem cor nova; divisores refinados; papel/óxido, VIS-01, radius angular e estrutura de layout intactos; 239 testes verdes.
</success_criteria>

<output>
Após completar, criar `.planning/phases/19-estetica-premium-tipografia/19-02-SUMMARY.md` — listar cada box-shadow migrado (linha→token), as 4 exceções mantidas, e confirmar ausência de cor nova.
</output>
