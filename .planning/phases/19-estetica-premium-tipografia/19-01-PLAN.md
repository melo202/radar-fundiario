---
phase: 19-estetica-premium-tipografia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - radar-goiania.html
autonomous: true
requirements: [TYPO-01]
security_enforcement: true

must_haves:
  truths:
    - "O usuário vê Archivo (não mais Segoe UI) em toda a UI, mapa e sheets"
    - "O PDF/laudo gerado (#laudoView) renderiza em Archivo/JetBrains Mono, não em fallback de sistema"
    - "A fonte carrega offline (embutida no HTML, zero requisição de rede em runtime)"
    - "Números dinâmicos (R$, m², score, contagens) usam dígitos tabulares (não 'dançam' de largura)"
    - "Se o @font-face falhar, o app cai em fonte de sistema legível sem quebrar layout"
  artifacts:
    - path: "radar-goiania.html"
      provides: "2 blocos @font-face (Archivo variável 400-800 + JetBrains Mono variável 500-700) em base64 inline, CSP com font-src, zero declaração IBM Plex/Open Sans"
      contains: "@font-face"
  key_links:
    - from: "meta CSP (linha 7)"
      to: "url(data:font/woff2;base64,...)"
      via: "font-src 'self' data:"
      pattern: "font-src 'self' data:"
    - from: "todos os seletores font:/font-family"
      to: "@font-face Archivo/JetBrains Mono"
      via: "nome de família migrado"
      pattern: "\"Archivo\""
---

<objective>
Embutir de verdade a tipografia premium (Archivo + JetBrains Mono, WOFF2 variável em base64 inline), corrigir o CSP para permitir fontes `data:`, e migrar TODAS as 196 declarações de família (180 IBM Plex + 14 Open Sans no pipeline de PDF + 2 `font-family` puras) para a fonte nova — mais dígitos tabulares nos valores numéricos.

Purpose: A causa raiz da "letra feia" (feedback do usuário 2026-07-10) não é o nome da família — é que NENHUMA fonte é carregada (zero `@font-face`); o usuário sempre viu o fallback `Segoe UI`. Esta é a fundação de TYPO-01: sem ela, os planos 02/03 refinam uma tipografia que não existe.
Output: `radar-goiania.html` com fonte real embutida, CSP corrigido, zero resquício IBM Plex/Open Sans, tabular-nums nos valores.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-estetica-premium-tipografia/19-CONTEXT.md
@.planning/phases/19-estetica-premium-tipografia/19-RESEARCH.md
@.planning/phases/19-estetica-premium-tipografia/19-UI-SPEC.md
@.planning/phases/19-estetica-premium-tipografia/19-VALIDATION.md

# Arquivos de fonte JÁ PRONTOS (base64 de uma linha, fora do repo) — o executor lê e cola:
# C:\Users\bruno\AppData\Local\Temp\claude\C--Users-bruno-Documents-Projeto-Radar-Fundi-rio\06e93356-6a93-4753-a96e-6bf4ee4f85f0\scratchpad\fonts\archivo-variable-latin-400-800.b64.txt   (46.572 chars)
# C:\Users\bruno\AppData\Local\Temp\claude\C--Users-bruno-Documents-Projeto-Radar-Fundi-rio\06e93356-6a93-4753-a96e-6bf4ee4f85f0\scratchpad\fonts\jetbrainsmono-variable-latin-500-700.b64.txt   (41.912 chars)

<interfaces>
<!-- Fatos verificados na pesquisa (grep direto no HTML real) — o executor NÃO precisa re-explorar -->
Contagens de partida (VERIFICADAS):
  grep -c 'IBM Plex' radar-goiania.html      → 180  (141 "IBM Plex Sans" + 39 "IBM Plex Mono", nenhuma linha tem as duas)
  grep -c 'Open Sans' radar-goiania.html     → 14   (TODAS no pipeline #laudo/#laudoView, linhas ~684-761 = impressão/PDF)
  grep -c '@font-face' radar-goiania.html    → 0
  grep -c 'font-variant-numeric' → 0
  tamanho HTML: 653.051 bytes (registrar delta no SUMMARY; esperado +~90KB de base64)

Âncoras exatas:
  - Linha 7: meta CSP (SEM font-src hoje)
  - Linha 101: font-family:"IBM Plex Sans","Segoe UI",system-ui,sans-serif  (raiz body/app)
  - Linha 897: .leaflet-container{font-family:"IBM Plex Sans",sans-serif}  (tooltips/popups do mapa são DOM real — esta linha corrige a tipografia de todo o mapa)
  - Linhas 374/434/798: font-family:inherit → NÃO editar (herdam da raiz já corrigida)
  - 33 ocorrências de letter-spacing já calibradas na faixa-alvo do UI-SPEC → migração NÃO recalibra tracking; só preservar os 2 `!important` de `.chk{letter-spacing:0!important}`

Payload medido (VERIFICADO, não estimado): Archivo 34.928 bytes woff2 / 46.572 base64; JetBrains Mono 31.432 / 41.912; total 88.484 base64 (86,4KB) — dentro do teto de 250KB do CONTEXT com 65% de folga.
Licença: ambas OFL 1.1 (verificado no repo google/fonts, caminho ofl/). Magic bytes wOF2 confirmados nos arquivos.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSP font-src + 2 blocos @font-face variáveis em base64 inline</name>
  <read_first>
    - radar-goiania.html linha 7 (CSP) e o início do bloco &lt;style&gt; (onde adicionar o @font-face — antes das regras que usam a fonte, tipicamente logo após o :root ou no topo do CSS)
    - 19-RESEARCH.md PARTE A (bloco @font-face recomendado) e PARTE C (CSP)
    - Os 2 arquivos .b64.txt no scratchpad (paths no &lt;context&gt;) — ler o conteúdo integral de cada um
  </read_first>
  <files>radar-goiania.html</files>
  <action>
Duas edições cirúrgicas, nenhuma outra diretiva/regra tocada:

(A) CSP — linha 7. Adicionar SOMENTE `font-src 'self' data:;` (nunca `data:` em script-src/default-src, nunca ampliar default-src). Posicionar logo após `img-src ...;` e antes de `connect-src 'self';`:
  ...https://server.arcgisonline.com; font-src 'self' data:; connect-src 'self';...
Motivo (T1/Pitfall 1 da pesquisa): sem `font-src`, o navegador cai no fallback `default-src 'self'` que NÃO inclui `data:`, e a fonte é bloqueada por CSP — mesmo sintoma do bug original.

(B) @font-face — adicionar 2 blocos no topo do CSS inline (antes das regras que usam a fonte). Usar RANGE de peso (fonte variável), NÃO 5+2 blocos discretos (Pitfall 3: inflaria ~230KB à toa). font-display:swap. Comentário de licença OFL obrigatório em cada bloco. Colar o conteúdo integral (uma linha, sem quebras) de archivo-variable-latin-400-800.b64.txt em XXX e de jetbrainsmono-variable-latin-500-700.b64.txt em YYY:

/* Fase 19 TYPO-01 — Archivo — Google Fonts — OFL 1.1 — https://fonts.google.com/specimen/Archivo — subset latin — fonte variável eixo wght 400-800 num único arquivo — substitui "IBM Plex Sans" */
@font-face{font-family:"Archivo";font-style:normal;font-weight:400 800;font-display:swap;src:url(data:font/woff2;base64,XXX) format("woff2")}
/* Fase 19 TYPO-01 — JetBrains Mono — Google Fonts — OFL 1.1 — https://fonts.google.com/specimen/JetBrains+Mono — subset latin — fonte variável eixo wght 500-700 num único arquivo — substitui "IBM Plex Mono" e "Open Sans" (PDF) */
@font-face{font-family:"JetBrains Mono";font-style:normal;font-weight:500 700;font-display:swap;src:url(data:font/woff2;base64,YYY) format("woff2")}

Registrar no SUMMARY: tamanho do HTML antes (653.051) e depois; hash/tamanho dos 2 .b64 embutidos (T2 — integridade: os bytes vêm dos arquivos verificados por magic bytes wOF2 na pesquisa).

NÃO bumpar sw.js (CACHE="radar-v7"): a fonte vive DENTRO do HTML, que já é NETWORK_FIRST — o cache antigo é só fallback offline e nunca trava a versão. Documentar esta exceção à convenção "bump quando assets mudam" no SUMMARY (o HTML mudou, não os assets externos em LOCAL[]).
  </action>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && grep -c '@font-face' radar-goiania.html | grep -qx 2 && grep -q "font-src 'self' data:" radar-goiania.html && grep -q 'font-weight:400 800' radar-goiania.html && grep -q 'font-weight:500 700' radar-goiania.html && grep -q 'font-display:swap' radar-goiania.html && npm test 2>&1 | grep -q 'pass 239' && echo OK</automated>
  </verify>
  <done>CSP tem exatamente `font-src 'self' data:` adicionado (nenhuma outra diretiva alterada); 2 blocos @font-face com range de peso e font-display:swap presentes; base64 colado sem quebras; suíte 239 verde.</done>
</task>

<task type="auto">
  <name>Task 2: Migração mecânica das 180 declarações IBM Plex → Archivo/JetBrains Mono</name>
  <read_first>
    - radar-goiania.html (todas as ocorrências de "IBM Plex Sans"/"IBM Plex Mono", incl. linhas 101 e 897)
    - 19-UI-SPEC.md seção "Contrato de pesos e mapeamento" (regra: pesos 400/500/600/700/800 mapeiam 1:1 para Archivo, nenhuma reescrita de peso)
  </read_first>
  <files>radar-goiania.html</files>
  <action>
Substituição por PADRÃO EXATO de string (replace_all / sed literal, NUNCA regex frouxa) — só o nome da família muda, tudo o mais (peso, size, line-height, letter-spacing, cor) permanece byte-idêntico:

  "IBM Plex Sans"  →  "Archivo"        (141 ocorrências, inclui a raiz linha 101 e .leaflet-container linha 897)
  "IBM Plex Mono"  →  "JetBrains Mono" (39 ocorrências)

O fallback genérico que segue cada família DEVE ser preservado: `"Archivo","Segoe UI",system-ui,sans-serif` e `"JetBrains Mono",monospace` — nunca remover `monospace`/`Segoe UI`/`system-ui`.

NÃO editar as 3 linhas `font-family:inherit` (374/434/798) — herdam da raiz já corrigida.
Preservar os 2 `.chk{letter-spacing:0!important}` intactos (o `!important` não pode sumir na migração).
Tracking: NÃO recalibrar — as 33 ocorrências de letter-spacing já estão na faixa-alvo do UI-SPEC (verificado na pesquisa); a mudança é só de família.

Regressão zero (T3): nenhum id/classe/handler/string funcional muda — apenas o literal da família dentro de `font:`/`font-family:`.
  </action>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && [ "$(grep -c 'IBM Plex' radar-goiania.html)" = "0" ] && grep -q 'font-family:"Archivo","Segoe UI",system-ui,sans-serif' radar-goiania.html && grep -q '.leaflet-container{font-family:"Archivo"' radar-goiania.html && [ "$(grep -c 'letter-spacing:0!important' radar-goiania.html)" != "0" ] && npm test 2>&1 | grep -q 'pass 239' && echo OK</automated>
  </verify>
  <done>Zero ocorrência de "IBM Plex" no HTML; raiz (body) e .leaflet-container em Archivo; fallbacks preservados; `.chk !important` intacto; suíte 239 verde.</done>
</task>

<task type="auto">
  <name>Task 3: Migração das 14 Open Sans (pipeline PDF #laudo) por papel + tabular-nums nos valores</name>
  <read_first>
    - radar-goiania.html linhas ~684-761 (todas as 14 ocorrências "Open Sans" no #laudo/#laudoView) e o bloco @media print (~738-750, especialmente linhas 742 e 749)
    - 19-RESEARCH.md "#laudo/#laudoView: NÃO herda automaticamente" + Code Examples (migração de #laudo)
    - 19-UI-SPEC.md tabela papel→família e "Regra dura de dígitos tabulares" (lista de seletores)
  </read_first>
  <files>radar-goiania.html</files>
  <action>
(A) Migrar as 14 `"Open Sans"` por PAPEL (não é replace cego — família diferente conforme o papel), seguindo a tabela do UI-SPEC:
  - Display/heading/body (ex.: `#laudo .lbrand .t`, `.preco .hero`, `.subcard .st`, e os 2 `font:` do @media print linhas 742/749 onde forem texto/valor) → `"Archivo"`
  - Eyebrow/rótulo/mono (ex.: `.lbrand .t small`, `.fr-faixa-lbl`, `.foot`) → `"JetBrains Mono"`
  Preservar peso/size/line-height/fallback de cada linha; trocar só a família. Sem editar `#laudo .lcard{box-shadow:none}` (papel impresso não tem sombra — correto).
  Motivo (Pitfall 2): se essas 14 linhas ficarem, o PDF continua em Segoe UI enquanto o resto do app já está em Archivo — pior visualmente (dois estilos coexistindo).

(B) tabular-nums — cobertura obrigatória de TODO seletor que renderiza número dinâmico. Adicionar `font-variant-numeric:tabular-nums` (via classe utilitária `.tnum{font-variant-numeric:tabular-nums}` aplicada aos elementos OU direto nos seletores — escolha do executor, mas a cobertura é obrigatória e verificável). Seletores-alvo do UI-SPEC: `.card .vals .ref b`, `.dvalor-v`, `.score-num`, `.cmp-table td`, `.count`, `.terr-metric`, `.bm b`.
  </action>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && [ "$(grep -c 'Open Sans' radar-goiania.html)" = "0" ] && grep -q 'tabular-nums' radar-goiania.html && grep -q '"Archivo"' radar-goiania.html && npm test 2>&1 | grep -q 'pass 239' && echo OK</automated>
  </verify>
  <done>Zero ocorrência de "Open Sans"; #laudo/#laudoView migrado por papel (Archivo/JetBrains Mono); tabular-nums presente cobrindo os seletores de valor; suíte 239 verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → CSP | a política de segurança de conteúdo controla de onde recursos (incl. fontes `data:`) podem ser carregados |
| arquivo de fonte → runtime | os bytes woff2 embutidos são conteúdo estático confiável, verificados na pesquisa |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering/Elevation | meta CSP (linha 7) | mitigate | Adicionar SOMENTE `font-src 'self' data:` — nunca `data:` em script-src/default-src, nunca ampliar default-src (escopo mínimo; verificável por grep exato) |
| T-19-02 | Tampering | base64 das fontes | mitigate | Bytes vêm dos arquivos .b64.txt verificados na pesquisa (magic bytes wOF2, OFL 1.1 do repo oficial google/fonts); registrar tamanho/hash no commit; fator de expansão base64 1,333 confirma integridade |
| T-19-03 | Denial of Service (qualidade) | @font-face | mitigate | `font-display:swap` (nunca texto invisível) + fallback stack `"Segoe UI"`/`Consolas`/`monospace` preservado — se base64 corromper, degrada seguro sem quebrar layout |
| T-19-04 | Tampering (regressão) | 196 declarações de família | mitigate | Substituição por padrão exato de string (nunca regex frouxa); nenhum id/classe/handler/string funcional alterado; suíte 239 verde a cada task |
</threat_model>

<verification>
- `grep -c 'IBM Plex'` → 0 · `grep -c 'Open Sans'` → 0 · `grep -c '@font-face'` → 2
- `grep -q "font-src 'self' data:"` (CSP) · nenhuma outra diretiva CSP alterada
- `grep -q 'tabular-nums'`
- Fallback stack preservado em Archivo e JetBrains Mono
- `npm test` → 239 verdes
- Delta de payload do HTML registrado no SUMMARY (esperado +~90KB); exceção do sw.js documentada
</verification>

<success_criteria>
Archivo + JetBrains Mono embutidas via 2 @font-face variáveis em base64; CSP permite `font-src 'self' data:` sem afrouxar nada mais; zero resquício IBM Plex/Open Sans; PDF (#laudoView) migrado; tabular-nums nos valores; 239 testes verdes; payload dentro do teto de 250KB.
</success_criteria>

<output>
Após completar, criar `.planning/phases/19-estetica-premium-tipografia/19-01-SUMMARY.md` — incluir: payload antes/depois, tamanho dos 2 base64 embutidos, confirmação da exceção do sw.js, e nota de que a renderização real (document.fonts.check) será validada no preview do Plano 03.
</output>
