---
phase: 11-documentos-em-3-niveis
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - radar-goiania.html
autonomous: true
requirements: [DOC-01]
tags: [templates, relatorio, ptam, ficha-rapida, cnai, montarLaudo]

must_haves:
  truths:
    - "montarLaudo() ramifica por LZ.tipoDoc: 'ficha' chama montarFichaRapida() (novo render, 1 pagina); 'relatorio'/'ptam' seguem o pipeline existente com titulo decidido por CNAI (nao mais por CRECI)"
    - "Preencher so CRECI (sem CNAI) mantem o titulo 'Relatorio de Referencia de Mercado'; preencher CNAI muda o titulo para 'Parecer Tecnico de Avaliacao Mercadologica (PTAM)'"
    - "Bloco de assinatura do PTAM mostra nome + CRECI (se houver) + CNAI (se houver) + linha de qualificacao COFECI quando CNAI presente"
    - "montarFichaRapida() produz 1 pagina com faixa em destaque, resumo, leitura pratica, 2-3 comparaveis, ressalvas, contato do corretor (omitido se perfil vazio) — nunca mostra mediana/percentil"
    - "Gerar a Ficha rapida ou o Relatorio/PTAM ambos passam por #laudo -> #laudoView -> imprimirLaudo() no clique real (guarda ec9f129 preservada, pipeline unico)"
  artifacts:
    - path: "radar-goiania.html"
      provides: "montarFichaRapida() + montarLaudo() ramificado por LZ.tipoDoc + titulo por CNAI + bloco .lass com CNAI"
      contains: "function montarFichaRapida"
  key_links:
    - from: "wizNext() passo Revisao"
      to: "montarLaudo() -> (montarFichaRapida() se LZ.tipoDoc==='ficha')"
      via: "chamada condicional dentro de montarLaudo(), mesmo pipeline #laudo/#laudoView"
      pattern: "LZ\\.tipoDoc"
    - from: "montarFichaRapida()"
      to: "fichaRapidaTexto (RADAR_PURE, 11-01)"
      via: "chamada direta com o mesmo objeto data de dadosFicha()-like montado a partir de LZ"
      pattern: "fichaRapidaTexto\\("
    - from: "titulo do PTAM"
      to: "radar_prof.cnai (nao mais radar_prof.creci)"
      via: "comCnai=!!(p.cnai||'').trim() decide o titulo; comCreci so decide a exibicao do CRECI na assinatura"
      pattern: "comCnai"
---

<objective>
Concluir os 3 templates de saída documental: (1) o laudo atual permanece o motor do "Relatório de avaliação" (nome exibido na navegação; título impresso "Relatório de Referência de Mercado" inalterado no PDF, per nota de nomenclatura do UI-SPEC); (2) adicionar ao PTAM o campo CNAI separado na assinatura + linha de qualificação COFECI, com o TÍTULO decidido por CNAI (não mais por CRECI); (3) criar `montarFichaRapida()`, o novo template de saída de 1 página que reusa `fichaRapidaTexto`/`leituraPratica`/`mercadoEstimado` sem recalcular nada.

Purpose: DOC-01 exige as 3 saídas nomeadas corretamente e a Ficha rápida como novo template compacto. Esta plan é a ÚLTIMA camada — consome `LZ.tipoDoc` (Wave 2) e `fichaRapidaTexto` (Wave 1) para fechar o pipeline: Seletor de Finalidade → wizard → template certo → PDF no clique.
Output: `montarFichaRapida()` novo; `montarLaudo()` ramificado por `LZ.tipoDoc`; título do PTAM decidido por `comCnai` (não `comCreci`); bloco `.lass` com CNAI + linha COFECI condicional; CSS novo `.fr-*` (Ficha rápida) copiado literalmente do UI-SPEC Componente 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-documentos-em-3-niveis/11-CONTEXT.md
@.planning/phases/11-documentos-em-3-niveis/11-UI-SPEC.md
@.planning/phases/11-documentos-em-3-niveis/11-01-SUMMARY.md
@.planning/phases/11-documentos-em-3-niveis/11-02-SUMMARY.md

# Fonte de código (ler antes de editar)
@radar-goiania.html

<facts>
<!-- Verificado por leitura direta deste repo. -->

**`montarLaudo()` hoje (linha 3002-3092):** monta `a=LZ.a, p=LZ.prof, est=mercadoEstimado(a,
LZ.privativa), g=LZ.comps, ul=unitLabel(a)`. Decide o título por CRECI:
`const comCreci=!!(p.creci||"").trim(); const titulo=comCreci?"Parecer Técnico de Avaliação
Mercadológica (PTAM)":"Relatório de Referência de Mercado";` (linha 3006-3007) — ESTA PLAN MUDA
para decidir por CNAI: `const comCnai=!!(p.cnai||"").trim(); const titulo=comCnai?"Parecer
Técnico de Avaliação Mercadológica (PTAM)":"Relatório de Referência de Mercado";` (`comCreci`
continua existindo, mas só controla a EXIBIÇÃO do CRECI na linha de assinatura/cabeçalho, não o
título).

Bloco de assinatura hoje (linha 3082): `<div class="lass"><div class="linha"></div>${esc(p.nome)}
${comCreci?" — "+esc(p.creci):""}</div>` — SEM CNAI. Esta plan adiciona CNAI + linha de
qualificação COFECI (per UI-SPEC Componente 5):
```html
<div class="lass"><div class="linha"></div>${esc(p.nome)}${comCreci?" — CRECI "+esc(p.creci):""}${comCnai?" — CNAI "+esc(p.cnai):""}
  <div class="lass-resp">Responsável técnico pela avaliação, nos termos da Res. COFECI nº 1.066/2007.</div></div>
```
(nota: `comCnai` decide SE a `.lass-resp` aparece — só aparece quando há CNAI, per UI-SPEC).

Cabeçalho `.lbrand .m` hoje (linha 3048): `${esc(p.nome)}${comCreci?" · CRECI "+esc(p.creci):""}
${(p.contato||"").trim()?"<br>"+esc(p.contato):""}<br>Goiânia/GO · ${hoje}` — esta linha NÃO
muda (CNAI não aparece no cabeçalho, só na assinatura final, per UI-SPEC Componente 5 — a tabela
de mapeamento não lista o cabeçalho como um dos blocos que ganham CNAI).

Ressalva final (linha 3081): o parágrafo já tem uma frase condicional por `comCreci` sobre CNAI —
"Parecer emitido por corretor de imóveis; a menção à Resolução COFECI nº 1.066/2007 pressupõe
inscrição do subscritor no CNAI." (quando `comCreci`) vs. "A emissão de PTAM é privativa de
corretor inscrito no CRECI e no CNAI..." (quando `!comCreci`). Esta plan AJUSTA essa condição
para usar `comCnai` em vez de `comCreci` (a frase já fala de CNAI explicitamente — o gate
correto é ter CNAI, não CRECI; ajustar a condição do ternário).

**Documentação pendente (LZ.docOk===false, Wave 2):** UI-SPEC Estado Transversal exige frase
adicional nas Ressalvas quando `LZ.docOk===false`: "O interessado declarou pendências na
documentação do imóvel — recomenda-se regularização antes de qualquer decisão." — esta plan
ADICIONA essa frase ao parágrafo de Ressalvas de `montarLaudo()` (linha ~3081, mesmo `<p
class="lmiudo">` de Metodologia/Ressalvas, OU um `<p>` extra logo abaixo — preferir um `<p>` extra
para não misturar com o texto de metodologia, mais fácil de testar/ler).

**`dadosFicha()` (linha 2681) usado FORA do wizard** (Modo Captação) — esta plan NÃO modifica
`dadosFicha()`. Dentro do wizard, `montarFichaRapida()` monta seu PRÓPRIO objeto `data` a partir
de `LZ` (não chama `dadosFicha()`, que lê `DCUR`/`localStorage` diretamente — dentro do wizard a
fonte de verdade é `LZ`, per Estado Transversal "DCUR fechado": "Seletor de Finalidade, Confiança
e Revisão todos leem de LZ.a... nunca de DCUR diretamente após aberto" — mesma regra vale para o
template final).

**Comparáveis para a Ficha rápida:** `LZ.comps` (já populado por `getComps()`, mesmo padrão do
Relatório) tem shape `{res:{n,min,max,q1,med,q3,...}, radius, isU, myPm2}` — NÃO tem frases
prontas por comparável individual (é estatística agregada, não uma lista de imóveis). Per UI-SPEC
Componente 4, a Ficha rápida usa "2-3 comparáveis essenciais" no padrão CMP-01 ("Rua X, nº — 8%
abaixo da faixa desta região"). DECISÃO DE ESCOPO para esta plan (Claude's Discretion, per
CONTEXT.md): a Ficha rápida usa a MESMA conclusão agregada já disponível (`g.res`/`myPm2`,
formatada no padrão CMP-01 de UMA frase) em vez de simular 2-3 comparáveis individuais fictícios —
isso é MAIS HONESTO (não inventa endereços de comparáveis que a Ficha não tem acesso individual) e
ainda cumpre "conclusão primeiro" (CMP-01). `fichaRapidaTexto(data)` (Wave 1) já aceita
`data.comparaveis: string[]|undefined` e retorna `[]` quando ausente — esta plan passa
`data.comparaveis` com NO MÁXIMO 1 frase agregada (mesma lógica de `compsHtml`/`g.res` já usada em
`montarLaudo()`, reformatada no padrão CMP-01 curto) quando `g&&g.res&&g.res.n>=3`, ou `undefined`
(vira `[]`) quando não há amostra — a seção "Comparáveis" da Ficha rápida é OMITIDA quando o array
vier vazio (nunca mostra uma seção vazia/genérica).

**Pipeline de exibição (#laudo → #laudoView → imprimirLaudo(), guarda ec9f129):** linhas
3085-3091, INALTERADO nesta plan — `montarFichaRapida()` escreve no MESMO
`document.getElementById("laudo").innerHTML`, chama `fecharLaudo()`, copia para `#laudoViewBody`,
mostra `#laudoView` — EXATAMENTE o mesmo fluxo de `montarLaudo()` (reuso total, não duplica
mecânica de exibição/impressão).
</facts>

<interfaces>
<!-- Funcao pura da Wave 1 que esta plan CHAMA — contrato ja fechado, nao reabrir 11-01. -->

```js
// De RADAR_PURE (11-01-PLAN.md) — já implementada, só CHAMAR:
function fichaRapidaTexto(data): {
  faixaTxt: string | null, resumo: string, leitura: string, comparaveis: string[], ressalva: string
}
// data: mesmo shape de dadosFicha() (endereco,bairro,quadra,lote,tipoImovel,faixa,scoreOp,scoreConf,leitura,perfil) + comparaveis opcional
```

**Campos de `LZ` que esta plan LÊ (contrato já fechado pela Wave 2 — 11-02-SUMMARY.md):**
`LZ.tipoDoc` ("ficha"|"relatorio"|"ptam"), `LZ.docOk` (true|false|undefined), `LZ.prof.cnai`
(string, pode ser vazia/undefined).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Titulo por CNAI + bloco de assinatura PTAM + ressalva de documentacao pendente</name>
  <files>radar-goiania.html</files>
  <action>
Em `montarLaudo()` (linha ~3002-3092):

1. Trocar a decisão de título: `const comCreci=!!(p.creci||"").trim(); const comCnai=!!(p.cnai||"").trim();
   const titulo=comCnai?"Parecer Técnico de Avaliação Mercadológica (PTAM)":"Relatório de Referência de Mercado";`
   — `comCreci` PERMANECE (ainda controla a exibição do CRECI no cabeçalho/assinatura), só o
   `titulo` passa a depender de `comCnai`.

2. Ajustar a ressalva final (linha ~3081, dentro do `p.lmiudo` de Metodologia/Ressalvas)
   trocando a condição `comCreci` por `comCnai` no ternário que decide entre "Parecer emitido por
   corretor de imóveis..." e "A emissão de PTAM é privativa de corretor inscrito no CRECI e no
   CNAI...": quando `comCnai` usar "Parecer emitido por corretor de imóveis inscrito no CNAI; a
   menção à Resolução COFECI nº 1.066/2007 aplica-se a este parecer."; quando `!comCnai` manter o
   texto já existente ("A emissão de PTAM é privativa de corretor inscrito no CRECI e no CNAI
   (Res. COFECI nº 1.066/2007); na ausência dessa inscrição, este documento constitui relatório
   de referência de mercado, sem natureza de laudo ou parecer técnico oficial.").

3. Ajustar o bloco `.lass` (linha 3082) para incluir CNAI + linha de qualificação condicional:
   nome + " — CRECI " + creci (se comCreci) + " — CNAI " + cnai (se comCnai), seguido de um bloco
   `.lass-resp` com "Responsável técnico pela avaliação, nos termos da Res. COFECI nº 1.066/2007."
   SOMENTE quando `comCnai` for verdadeiro (omitido quando não há CNAI).

4. Adicionar CSS `.lass-resp` (copiar exatamente do UI-SPEC Componente 5): `font:500 10.5px/1.4
   "IBM Plex Sans"; color:var(--muted); margin-top:4px`.

5. Adicionar a ressalva de documentação pendente (Estado Transversal do UI-SPEC): logo após o
   parágrafo de Metodologia/Ressalvas (linha ~3081), inserir um parágrafo `<p class="lmiudo">`
   condicional que só aparece quando `LZ.docOk===false`, com o texto: "O interessado declarou
   pendências na documentação do imóvel — recomenda-se regularização antes de qualquer decisão."

6. Confirmar (grep) que NENHUM outro ponto do arquivo decide título/PTAM por `comCreci` isolado —
   se houver, ajustar para `comCnai` também (busca por `comCreci` fora deste bloco).
  </action>
  <verify>
    <automated>node --check radar-goiania.html && node --test "tests/*.test.mjs" 2>&1 | tail -10</automated>
  </verify>
  <done>Preencher só CRECI mantém o título "Relatório de Referência de Mercado"; preencher CNAI muda para "Parecer Técnico de Avaliação Mercadológica (PTAM)"; bloco de assinatura mostra CRECI/CNAI condicionalmente + linha COFECI só com CNAI; documentação pendente (LZ.docOk===false) aparece como ressalva adicional; suite de testes sem regressão.</done>
</task>

<task type="auto">
  <name>Task 2: montarFichaRapida() — novo template de 1 pagina + ramificacao em montarLaudo()</name>
  <files>radar-goiania.html</files>
  <action>
1. Em `montarLaudo()`, no TOPO da função (antes de montar `a=LZ.a`, etc.), adicionar a
   ramificação: `if(LZ.tipoDoc==="ficha"){montarFichaRapida();return;}` — o RESTANTE da função
   (`titulo`/PTAM/Relatório) só roda para `LZ.tipoDoc==="relatorio"|"ptam"`. Isso preserva 100% do
   comportamento atual para Relatório/PTAM (nenhuma linha do corpo existente muda de posição,
   só ganha esse guard no topo).

2. Criar `montarFichaRapida()` (próxima de `montarLaudo()`): monta o objeto `data` a partir de
   `LZ` (MESMO shape usado pelos templates zap*/capt* — `dadosFicha()`-like, mas lido de `LZ` em
   vez de `DCUR`/localStorage direto, per regra "nunca lê DCUR após aberto"):
   ```js
   function montarFichaRapida(){
     const a=LZ.a,p=LZ.prof,g=LZ.comps,ul=unitLabel(a);
     const isU=!!ul||(a.cdedificio||0)>0;
     const myPm2=isU?m2Edif(a):m2Terr(a);
     const areaOk=!!(LZ.privativa||(isU?a.areaedif:a.areaterr));
     const venalOk=(a.vlvenal||0)>0;
     const atipico=isGarage(a);
     const conf=scoreConfianca({areaOk,nComps:(g&&g.res)?g.res.n:0,atipico,venalOk});
     const stats=g&&g.res?{med:g.res.med,q1:g.res.q1,q3:g.res.q3,n:g.res.n,min:g.res.min,max:g.res.max}:null;
     const op=scoreOportunidade(myPm2,stats,{radius:g?g.radius:400});
     const tipo=ul||USO[a.uso]||"Imóvel";
     const leitura=leituraPratica({tipoImovel:tipo,bairro:clean(a.nmbairro),oportunidade:op,confianca:conf});
     const est=mercadoEstimado(a,LZ.privativa);
     let comparaveis;
     if(g&&g.res&&g.res.n>=3){
       const diffPct=Math.round(Math.abs(myPm2-g.res.med)/g.res.med*100);
       const abaixo=myPm2<=g.res.med;
       comparaveis=[`Este imóvel está ${diffPct}% ${abaixo?"abaixo":"acima"} da faixa média desta região (${g.res.n} imóveis parecidos em ${g.radius} m).`];
     }
     let perfil=null;if(p&&p.nome)perfil={nome:p.nome,creci:p.creci,contato:p.contato};
     const data={
       endereco:addrDe(a), bairro:clean(a.nmbairro), quadra:clean(a.nrquadra), lote:clean(a.nrlote),
       tipoImovel:tipo, faixa:est?{lo:est.lo,hi:est.hi}:null, scoreOp:op, scoreConf:conf,
       leitura, perfil, comparaveis
     };
     const fr=fichaRapidaTexto(data);
     renderFichaRapida(a,data,fr);
   }
   ```
   (`scoreOportunidade`/`scoreConfianca`/`leituraPratica`/`mercadoEstimado`/`fichaRapidaTexto`
   já existem — todas chamadas diretas, zero recálculo duplicado).

3. Criar `renderFichaRapida(a,data,fr)` (o passo de RENDER HTML, separado da montagem de dados por
   clareza — ainda dentro do mesmo arquivo, função auxiliar simples): monta
   `document.getElementById("laudo").innerHTML` com a estrutura EXATA do UI-SPEC Componente 4:
   ```html
   <div class="lrun"><span>RADAR FUNDIÁRIO · DADOS PÚBLICOS · GOIÂNIA/GO</span><span>EMITIDO EM ${hoje}</span></div>
   <div class="lbrand"><div class="t"><small>Radar Fundiário · Cadastro Imobiliário de Goiânia</small>Ficha rápida do imóvel</div>
     <div class="m">${data.perfil?esc(data.perfil.nome)+(data.perfil.creci?" · CRECI "+esc(data.perfil.creci):""):""}<br>Goiânia/GO · ${hoje}</div></div>
   <div class="fr-hero">
     <div class="fr-faixa">${fr.faixaTxt?esc(fr.faixaTxt):"Sem faixa estimada"}</div>
     <div class="fr-faixa-lbl">Faixa estimada de valor</div>
   </div>
   <div class="lcard"><h3>Resumo do imóvel</h3>
     <p>${esc(data.endereco)||"Endereço não cadastrado"} — ${esc(data.bairro)}. Q ${esc(data.quadra)||"—"} · L ${esc(data.lote)||"—"}${a.areaedif?" · "+Math.round(a.areaedif)+" m²":a.areaterr?" · "+Math.round(a.areaterr)+" m²":""}.</p></div>
   <div class="lcard"><h3>Leitura prática</h3><p>${esc(fr.leitura)}</p></div>
   ${fr.comparaveis.length?`<div class="lcard"><h3>Comparáveis</h3><ul class="fr-comps">${fr.comparaveis.map(c=>`<li>${esc(c)}</li>`).join("")}</ul></div>`:""}
   <div class="lcard"><h3>Ressalvas</h3><p class="lmiudo">${esc(fr.ressalva)}</p></div>
   ${data.perfil?`<div class="fr-contato">${esc(data.perfil.nome)}${data.perfil.creci?" · CRECI "+esc(data.perfil.creci):""}${(data.perfil.contato||"").trim()?" · "+esc(data.perfil.contato):""}</div>`:""}
   <div class="foot">RADAR FUNDIÁRIO · DADOS PÚBLICOS · GOIÂNIA/GO · EMITIDO EM ${hoje}</div>
   ```
   (`hoje=new Date().toLocaleDateString("pt-BR")`, calculado no topo de `renderFichaRapida`).
   Ao final, o MESMO fechamento de `montarLaudo()`: `fecharLaudo();
   document.getElementById("laudoViewBody").innerHTML=document.getElementById("laudo").innerHTML;
   const lv=document.getElementById("laudoView"); lv.hidden=false; lv.scrollTop=0;` (copiar
   literalmente essas 3 linhas finais de `montarLaudo()` para `renderFichaRapida()` — mesma
   mecânica, reuso do pipeline #laudo/#laudoView/imprimirLaudo).

4. Adicionar CSS novo (copiar exatamente da tabela UI-SPEC Componente 4): `.fr-hero`,
   `.fr-faixa`, `.fr-faixa-lbl`, `.fr-comps` (e seus `li`), `.fr-contato`.

5. Regra de conteúdo (per UI-SPEC): título fixo "Ficha rápida do imóvel" (nunca "Relatório"/
   "Laudo"); nunca usa "mediana"/"percentil"/"quartil" (já garantido por `fichaRapidaTexto` +
   `leituraPratica`, que são as únicas fontes de texto livre aqui); contato do corretor OMITIDO
   (sem placeholder) quando `data.perfil===null` (já implementado no template acima via
   `${data.perfil?...:""}`, sem placeholder tipo "[seu nome]").
  </action>
  <verify>
    <automated>node --check radar-goiania.html && node --test "tests/*.test.mjs" 2>&1 | tail -10</automated>
  </verify>
  <done>Escolher "Ficha rápida" no Seletor de Finalidade e completar o wizard gera 1 página com faixa em destaque, resumo, leitura prática, comparáveis (quando há amostra), ressalvas e contato do corretor (omitido se perfil vazio); nunca mostra "mediana"/"percentil"/"quartil"; Relatório/PTAM continuam funcionando sem regressão via o mesmo pipeline #laudo/#laudoView/imprimirLaudo; suite de testes 100% sem regressão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LZ.tipoDoc -> ramificação de template (montarLaudo/montarFichaRapida) | valor controlado inteiramente pela navegação do próprio wizard (finEscolherDoc), nunca de input livre — sem risco de injeção via este campo |
| fr.faixaTxt/fr.leitura/fr.comparaveis (RADAR_PURE) -> innerHTML da Ficha rápida | toda interpolação passa por esc() no template de render (IN-01), mesmo quando a origem é texto já formatado por função pura (defesa em profundidade, consistente com o resto do app) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering | Título do PTAM decidido incorretamente por CRECI em vez de CNAI (regressão da mudança desta plan) | mitigate | grep manual pós-implementação confirma que nenhuma outra ocorrência de `comCreci` decide `titulo`; verificação funcional documentada (CRECI só, CNAI só, ambos, nenhum) |
| T-11-08 | Information Disclosure | Ficha rápida expondo "[seu nome]"/placeholder quando perfil vazio | mitigate | bloco de contato inteiro omitido via `${data.perfil?...:""}` — nenhum placeholder textual, mesma regra já aplicada aos templates zap*/capt* (Fase 10) |
| T-11-09 | Repudiation | Ficha rápida inventando comparáveis individuais fictícios (endereços que não existem na amostra) | mitigate | decisão de escopo documentada: usa a conclusão agregada real (g.res/myPm2), nunca simula comparáveis individuais sem dado de origem |

**Sem ameaças de alta severidade.** Esta plan só adiciona um novo template de saída e ajusta a
condição de título de um template existente — nenhuma nova superfície de rede/persistência.
</threat_model>

<verification>
- `node --check radar-goiania.html` passa
- `node --test "tests/*.test.mjs"` passa 100% (zero regressão — 11-01/11-02 + Fases 8/9/10)
- Fluxo manual: no passo perfil, CRECI e CNAI são 2 campos distintos; preencher só CRECI mantém "Relatório de Referência de Mercado"; preencher CNAI muda para "Parecer Técnico de Avaliação Mercadológica (PTAM)"; bloco de assinatura do PTAM mostra nome + CRECI (se houver) + CNAI (se houver) + linha COFECI quando CNAI presente
- Gerar a Ficha rápida produz 1 página com faixa em destaque, resumo, leitura prática, comparáveis (quando há amostra), ressalvas, contato do corretor (omitido se perfil vazio) — nunca "mediana"/"percentil"/"quartil"
- Documentação pendente (LZ.docOk===false) aparece como ressalva adicional no Relatório/PTAM
- Ficha rápida e Relatório/PTAM usam o MESMO pipeline #laudo → #laudoView → imprimirLaudo() no clique real (guarda ec9f129 preservada em ambos os caminhos)
</verification>

<success_criteria>
- DOC-01 (as 3 saídas nomeadas) cumprido: Ficha rápida (novo template), Relatório de avaliação (motor existente, título correto), Laudo/PTAM (título por CNAI, assinatura completa)
- Fluxo completo fechado: Seletor de Finalidade (11-02) → wizard → Confiança/Revisão (11-02) → template certo (11-03) → PDF no clique
- Zero regressão na suite de testes (47 existentes + novos de 11-01)
</success_criteria>

<output>
Após completar, criar `.planning/phases/11-documentos-em-3-niveis/11-03-SUMMARY.md`.
Incluir no SUMMARY: confirmação do fluxo completo ponta-a-ponta (Seletor → wizard → template → PDF), lista de funções novas (`montarFichaRapida`/`renderFichaRapida`), e resultado final da suite de testes completa da Fase 11.
</output>
