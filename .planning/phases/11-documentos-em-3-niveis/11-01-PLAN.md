---
phase: 11-documentos-em-3-niveis
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - radar-goiania.html
  - tests/doc.test.mjs
  - tests/fixtures.mjs
autonomous: true
requirements: [DOC-01, DOC-02]
tags: [documentos, recomendacao, cnai, confianca, pendencias, pure-functions, tdd]

must_haves:
  truths:
    - "recomendaDocumento(finalidadeUso, cnai) e uma funcao pura dentro do bloco RADAR_PURE que mapeia as 4 finalidades (apresentar/captar/justificar/formal) para {doc, porque} de forma deterministica, sem IA"
    - "recomendaDocumento('formal', false) recomenda 'relatorio' (nunca 'ptam') com porque explicando que o PTAM pressupoe CNAI; recomendaDocumento('formal', true) recomenda 'ptam'"
    - "pendenciasDocumento(inputs) retorna um array de itens {id, texto, resolvido, editavel} derivado de scoreConfianca-style inputs (areaOk/nComps/atipico/venalOk) + docOk (documentacao), sem duplicar a logica de scoreConfianca (chama a funcao existente internamente)"
    - "fichaRapidaTexto(data) monta o texto puro (nao-HTML) da Ficha rapida reusando leitura/faixa de data, nunca inventa faixa quando data.faixa=null, nunca usa jargao (mediana/percentil/quartil)"
  artifacts:
    - path: "radar-goiania.html"
      provides: "recomendaDocumento + pendenciasDocumento + fichaRapidaTexto dentro do bloco RADAR_PURE_START..RADAR_PURE_END"
      contains: "function recomendaDocumento"
    - path: "tests/doc.test.mjs"
      provides: "harness node:test que carrega RADAR_PURE via node:vm (mesmo padrao de tests/scores.test.mjs/tests/templates.test.mjs) testando as 3 funcoes novas"
      contains: "loadPureBlock"
    - path: "tests/fixtures.mjs"
      provides: "fixtures novas para recomendaDocumento (matriz das 4 finalidades x CNAI true/false)/pendenciasDocumento/fichaRapidaTexto adicionadas ao objeto FIXTURES existente"
      contains: "recomendaDocumento"
  key_links:
    - from: "tests/doc.test.mjs"
      to: "radar-goiania.html (bloco RADAR_PURE)"
      via: "leitura de arquivo + node:vm.Script, mesmo loader de tests/scores.test.mjs/tests/templates.test.mjs"
      pattern: "RADAR_PURE_START"
    - from: "recomendaDocumento"
      to: "radar_prof.cnai"
      via: "segundo parametro booleano — Wave 2 le radar_prof.cnai e passa aqui, funcao pura nao le localStorage"
      pattern: "function recomendaDocumento"
    - from: "pendenciasDocumento"
      to: "scoreConfianca"
      via: "chamada interna — reusa a MESMA funcao (nao duplica a logica de completude)"
      pattern: "scoreConfianca\\("
---

<objective>
Criar 3 funções puras dentro do bloco `RADAR_PURE` de `radar-goiania.html` — `recomendaDocumento(finalidadeUso, cnai)` (mapa determinístico de recomendação DOC-01, incl. matriz de explicação sem CNAI), `pendenciasDocumento(inputs)` (checklist acionável DOC-02, reusa `scoreConfianca`) e `fichaRapidaTexto(data)` (texto puro da Ficha rápida, sem jargão, sem inventar faixa) — com harness de teste TDD (`tests/doc.test.mjs`) cobrindo a matriz de finalidade×CNAI, a distinção pendência-vs-nota, e a honestidade de faixa null.

Purpose: DOC-01 exige que a UI recomende o documento certo sem IA e explique o "porquê" quando o PTAM é rebaixado por falta de CNAI; DOC-02 exige o painel de confiança+pendências ANTES de gerar, com itens acionáveis (alguns viram inputs que refinam o documento). Esta plan constrói a CAMADA PURA (sem DOM, sem wizard) — Wave 2 consome `recomendaDocumento`/`pendenciasDocumento` para renderizar o Seletor de Finalidade e o painel de Confiança; Wave 3 consome `fichaRapidaTexto` para montar o template de saída.
Output: bloco RADAR_PURE ampliado com 3 funções novas; `tests/doc.test.mjs` com fixtures cobrindo a matriz de recomendação (4 finalidades × CNAI true/false), pendências (áreas/comparáveis/atípico/venal/documentação) e honestidade da Ficha rápida; suite completa (`node --test "tests/*.test.mjs"`) passando (47 existentes + novos).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-documentos-em-3-niveis/11-CONTEXT.md
@.planning/phases/11-documentos-em-3-niveis/11-UI-SPEC.md

# Fonte de código (ler antes de editar)
@radar-goiania.html
@tests/scores.test.mjs
@tests/templates.test.mjs
@tests/fixtures.mjs

<facts>
<!-- Verificado por leitura direta deste repo. -->

Bloco RADAR_PURE hoje (linhas 873-1315 de radar-goiania.html): abre em
`// RADAR_PURE_START — funções puras de matching/normalização. Testadas por tests/busca.test.mjs`
e fecha em `// RADAR_PURE_END`. Já contém (Fases 9/10): `scoreOportunidade`, `scoreConfianca`,
`leituraPratica`, `assinatura`, `brlSimples`, `faixaTxt`, `zapResumo/zapProprietario/zapComprador/
zapArgumento/zapRiscos`, `captAbordagem/captScript/captChecklist/captFollowup`, `enderecoSimples`,
`oportunidadeItem`, `histAdd`. Esta plan ACRESCENTA 3 funções ao MESMO bloco (não cria um segundo
bloco) — o harness de teste corta por índice de string entre os marcadores, então tudo que precisa
ser testável por node:vm PRECISA estar dentro deles.

`scoreConfianca(inputs)` (linha 1102, já existe): recebe `{areaOk, nComps, atipico, venalOk}` e
retorna `{nivel, porque}` — distingue PENDÊNCIAS (defeitos contáveis: `!areaOk`, `!venalOk`,
`atipico`, `nComps<3`) de NOTAS (informativas: `nComps` entre 3 e 7). `pendenciasDocumento` desta
plan NÃO duplica essa lógica — CHAMA `scoreConfianca` internamente e traduz o resultado + um campo
NOVO (`docOk`, documentação) em itens de checklist estruturados (UI-SPEC Componente 2).

`leituraPratica(inputs)` (linha 1131, já existe): retorna string pt-BR sem jargão — reusada
diretamente por `fichaRapidaTexto` via `data.leitura` (já calculada, não recalcula).

`faixaTxt(faixa)`/`brlSimples(n)` (linhas 1163-1172, já existem no bloco, Fase 10): formatação de
moeda mínima e autônoma dentro do bloco puro. `fichaRapidaTexto` REUSA `faixaTxt` para a faixa em
destaque (não duplica formatação).

Objeto `data` de entrada (mesmo shape documentado em `10-01-PLAN.md`, já usado por `dadosFicha()`
linha 2681 e pelos templates zap*/capt*):
```js
{
  endereco: string, bairro: string, quadra: string, lote: string, tipoImovel: string,
  faixa: {lo:number, hi:number} | null,
  scoreOp: {score:number, rotulo:string, porque:string[]} | null,
  scoreConf: {nivel:string, porque:string[]} | null,
  leitura: string,
  perfil: {nome:string, creci:string, contato:string} | null
}
```
`fichaRapidaTexto(data)` desta plan recebe ESTE MESMO objeto `data` (Wave 2/3 vão estender
`dadosFicha()` para incluir `comparaveis` — ver abaixo — mas o shape básico é o mesmo, sem quebrar
os 9 templates zap*/capt* já existentes que dependem dele).

Campo NOVO em `data` consumido só por `fichaRapidaTexto` (Wave 3 popula ao chamar, opcional —
função deve degradar honestamente se ausente): `data.comparaveis: string[] | undefined` — até 3
frases já formatadas no padrão CMP-01 ("Rua X, nº — 8% abaixo da faixa desta região"), a MESMA
frase de conclusão já usada em `renderComps()`/`atualizarScores()` (esta plan não recalcula
comparáveis, só formata o que já vier pronto; sem o campo ou vazio, a seção de comparáveis é
omitida honestamente — nunca "comparável 1/2/3" genérico).

Campo `radar_prof` (localStorage, lido em `abrirLaudo()` linha 2855): hoje
`{nome, creci, contato}` — o campo `cnai` ainda NÃO existe (será adicionado na Wave 2, fora desta
plan). `recomendaDocumento(finalidadeUso, cnai)` desta plan recebe `cnai` já como BOOLEANO/STRING
(a função pura não lê localStorage nem sabe de `radar_prof` — é a Wave 2 que faz
`!!(radar_prof.cnai||"").trim()` e passa o resultado aqui).
</facts>

<interfaces>
<!-- Assinaturas exatas que a Wave 2/3 vão CHAMAR — contrato duro, não renomear. -->

```js
// DOC-01: mapa deterministico de recomendacao (Seletor de Finalidade, Componente 1 do UI-SPEC).
// finalidadeUso: "apresentar" | "captar" | "justificar" | "formal" (as 4 opcoes do UI-SPEC).
// cnai: boolean — true quando radar_prof.cnai esta preenchido (Wave 2 calcula, funcao nao le storage).
// Retorno: {doc: "ficha"|"relatorio"|"ptam", porque: string} — porque SEMPRE cita a finalidade
// escolhida (nunca generico), matching literal com o Copywriting Contract do UI-SPEC.
function recomendaDocumento(finalidadeUso, cnai): {doc: string, porque: string}

// DOC-02: checklist de pendencias acionaveis (painel Confianca+Pendencias, Componente 2 do UI-SPEC).
// inputs: {areaOk, nComps, atipico, venalOk, docOk} — docOk: true|false|undefined (undefined =
// usuario ainda nao respondeu o segmented "Em ordem/Pendencias", default indefinido per UI-SPEC).
// Retorno: array de itens {id, texto, resolvido, editavel} na ORDEM do UI-SPEC (area -> conservacao
// -> documentacao) + o proprio retorno de scoreConfianca (nivel/porque) embutido em .conf, para a
// Wave 2 renderizar o card de nivel SEM re-chamar scoreConfianca separadamente.
function pendenciasDocumento(inputs): {conf: {nivel: string, porque: string[]}, itens: Array<{id: string, texto: string, resolvido: boolean, editavel: boolean}>}

// DOC-01 (Ficha rapida, Componente 4 do UI-SPEC): texto PURO (nao-HTML) — Wave 3 decide como
// injetar em template (via esc() campo a campo, esta funcao nao escapa nada, so formata texto).
// Retorna um OBJETO com os textos de cada bloco (nao uma string unica) — Wave 3 monta o HTML
// dos <div class="lcard"> um a um; esta funcao so decide O QUE cada bloco diz.
function fichaRapidaTexto(data): {
  faixaTxt: string | null,        // "R$ 350 mil e R$ 420 mil" ou null (sem faixa — Wave 3 mostra variante honesta)
  resumo: string,                  // "[tipoImovel] no [bairro]." (endereco/quadra/lote ficam fora — Wave 3 monta o resumo cadastral separadamente, este texto e so a linha de leitura comercial)
  leitura: string,                 // data.leitura repassado 1:1 (sem reescrever)
  comparaveis: string[],           // até 3, de data.comparaveis (ou [] se ausente — nunca inventa)
  ressalva: string                 // texto FIXO do UI-SPEC ("Faixa estimada com base em dados públicos...")
}
```

Esqueletos de texto travados no UI-SPEC (`11-UI-SPEC.md`, seções "Componente 1", "Componente 2",
"Componente 4" e "Copywriting Contract") — usar EXATAMENTE a estrutura/tom ali descrita. Pontos de
honestidade OBRIGATÓRIOS (testados):
- `recomendaDocumento('formal', false)` → `doc:"relatorio"`, `porque` contém literalmente "PTAM
  pressupõe habilitação CNAI" (ou equivalente muito próximo do texto travado no UI-SPEC).
- `recomendaDocumento('formal', true)` → `doc:"ptam"`, `porque` cita CNAI de forma POSITIVA
  ("com sua habilitação CNAI").
- `pendenciasDocumento` NUNCA classifica "estado de conservação" como pendência (UI-SPEC: sempre
  tem default "Bom", item aparece como `.is-ok`/`resolvido:true` sempre).
- `pendenciasDocumento({docOk:false,...})` inclui item com `id:"documentacao"`, `resolvido:false`.
  `pendenciasDocumento({docOk:undefined,...})` também `resolvido:false` (pendente até o usuário
  responder — só `docOk:true` é `resolvido:true`).
- `fichaRapidaTexto` com `data.faixa===null` → `faixaTxt:null` (Wave 3 decide a variante honesta na
  hora de montar o HTML — esta função nunca inventa `lo`/`hi`).
- `fichaRapidaTexto` nunca inclui "mediana"/"percentil"/"quartil" em nenhum campo do retorno.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — criar tests/doc.test.mjs + fixtures (testes falhando)</name>
  <files>tests/doc.test.mjs, tests/fixtures.mjs</files>
  <behavior>
    - recomendaDocumento("apresentar", false) e recomendaDocumento("apresentar", true) retornam ambos doc:"ficha" (CNAI e irrelevante para apresentar/captar); recomendaDocumento("captar", *) idem doc:"ficha"
    - recomendaDocumento("justificar", false) e recomendaDocumento("justificar", true) retornam ambos doc:"relatorio" (CNAI irrelevante para justificar)
    - recomendaDocumento("formal", false) retorna doc:"relatorio" com porque contendo "CNAI" e "PTAM" (explicacao da limitacao, nunca bloqueio)
    - recomendaDocumento("formal", true) retorna doc:"ptam" com porque contendo "CNAI" (explicacao positiva)
    - cada retorno de recomendaDocumento tem porque não-vazio e ESPECIFICO da finalidade (nao um texto generico repetido nas 4 opcoes — assert que os 4 textos de porque sao todos DIFERENTES entre si)
    - pendenciasDocumento({areaOk:false,nComps:6,atipico:false,venalOk:true,docOk:undefined}): itens contem exatamente 1 item com id "area" e resolvido:false; item id "conservacao" sempre presente com resolvido:true; item id "documentacao" presente com resolvido:false (docOk undefined = pendente); .conf.nivel === "media" (mesma regra de scoreConfianca — reuso, nao duplicacao)
    - pendenciasDocumento({areaOk:true,nComps:10,atipico:false,venalOk:true,docOk:true}): item "area" resolvido:true; item "documentacao" resolvido:true; .conf.nivel === "alta"
    - pendenciasDocumento nunca inclui um item com id "conservacao" e resolvido:false (estado de conservacao sempre tem default, nunca pendente per UI-SPEC)
    - fichaRapidaTexto com data.faixa=null: faixaTxt===null (nunca string com NaN/undefined); com data.faixa={lo:300000,hi:400000}: faixaTxt e uma string nao-vazia contendo "R$"
    - fichaRapidaTexto: nenhum campo do objeto retornado (faixaTxt/resumo/leitura/comparaveis/ressalva) contem, case-insensitive, "mediana" nem "percentil" nem "quartil"
    - fichaRapidaTexto com data.comparaveis ausente: retorna comparaveis:[] (nunca inventa 3 comparaveis genericos); com data.comparaveis=["Rua X — 8% abaixo"]: retorna esse mesmo array (no maximo 3, trunca se vier mais de 3)
    - fichaRapidaTexto: ressalva contem "recomenda-se confirmar" (linguagem de responsabilidade travada no CONTEXT.md, reuso literal do UI-SPEC)
  </behavior>
  <action>
Criar `tests/doc.test.mjs` reusando EXATAMENTE o padrão de loader de `tests/scores.test.mjs`/
`tests/templates.test.mjs` (`loadPureBlock()` via `node:vm`, cortando por
`RADAR_PURE_START`/`RADAR_PURE_END`, com os MESMOS guards de `assert.ok(src.includes(...))` para
cada função nova antes de montar o sandbox — copiar o padrão, adaptando a lista de funções
exportadas em `globalThis.__exports` para incluir as 3 novas mais `scoreConfianca` (necessária para
os testes de `pendenciasDocumento` comparar contra o resultado bruto se preciso):
`recomendaDocumento,pendenciasDocumento,fichaRapidaTexto,scoreConfianca`.

Adicionar ao objeto `FIXTURES` existente em `tests/fixtures.mjs` (não substituir, ACRESCENTAR
chaves novas) as fixtures: `recomendaDocumentoCasos` (array com as 8 combinações finalidade×CNAI
documentadas no `<behavior>`, cada uma com `{finalidadeUso, cnai, expectDoc, expectPorqueContains}`),
`pendenciasDocumentoCasos` (os 2 casos completos do `<behavior>` com inputs+expects), `fichaRapidaCasos`
(objetos `data` com faixa null/preenchida, com/sem `comparaveis`).

Escrever os testes conforme `<behavior>` — DEVEM FALHAR nesta task (RED), pois as 3 funções ainda
não existem em `radar-goiania.html`. Rodar `node --test "tests/doc.test.mjs"` e confirmar falha
(funções ausentes do bloco RADAR_PURE) — este é o estado esperado ao final da Task 1.
  </action>
  <verify>
    <automated>node --test "tests/doc.test.mjs" 2>&1 | grep -qi "fail" && echo "OK: RED confirmado (testes falham, funcoes ainda nao existem)"</automated>
  </verify>
  <done>tests/doc.test.mjs e fixtures novas em tests/fixtures.mjs existem; rodar a suite falha porque recomendaDocumento/pendenciasDocumento/fichaRapidaTexto ainda não existem no bloco RADAR_PURE — estado RED documentado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implementar as 3 funções dentro do bloco RADAR_PURE</name>
  <files>radar-goiania.html</files>
  <action>
Adicionar as 3 funções de `&lt;interfaces&gt;` DENTRO do bloco `RADAR_PURE_START`...`RADAR_PURE_END`
(hoje linhas 873-1315), imediatamente ANTES do marcador `// RADAR_PURE_END` (seção nova, comentada
como "DOC-01/DOC-02 (Fase 11, 11-01): recomendação de documento + pendências + texto da Ficha
rápida — puro, sem IA").

- `recomendaDocumento(finalidadeUso, cnai)`: mapa determinístico literal da tabela do UI-SPEC
  (Componente 1) — `switch`/objeto de lookup:
  - `"apresentar"` → `{doc:"ficha", porque:"Você quer apresentar ao cliente — a Ficha rápida é
    direta, cabe numa tela e vai bem no WhatsApp."}`
  - `"captar"` → `{doc:"ficha", porque:"Você quer captar o proprietário — a Ficha rápida é direta,
    cabe numa tela e vai bem no WhatsApp."}`
  - `"justificar"` → `{doc:"relatorio", porque:"Você quer justificar o preço com dados — o
    Relatório traz comparáveis e metodologia, sem o peso formal do PTAM."}`
  - `"formal"` com `cnai` truthy → `{doc:"ptam", porque:"Você pediu um documento técnico formal —
    com sua habilitação CNAI, o PTAM é o parecer técnico completo, com responsabilidade
    profissional."}`
  - `"formal"` com `cnai` falsy → `{doc:"relatorio", porque:"O PTAM pressupõe habilitação CNAI —
    preencha no perfil se possuir. Por enquanto, recomendamos o Relatório de avaliação."}`
  - Fallback (`finalidadeUso` desconhecido/ausente) → mesmo resultado de `"justificar"` (default
    seguro, nunca lança exceção).
  Textos EXATOS copiados da tabela "Copywriting Contract" do UI-SPEC (não paráfrase) — Wave 2 vai
  renderizar `porque` direto no `.finrec-porque`, então precisa estar pt-BR final.

- `pendenciasDocumento(inputs)`: chama `scoreConfianca({areaOk, nComps, atipico, venalOk})`
  internamente (reuso, não duplica) para obter `conf`. Monta o array `itens` na ordem do UI-SPEC:
  1. `{id:"area", texto:"Área privativa confirmada", resolvido: !!inputs.areaOk, editavel:true}`
  2. `{id:"conservacao", texto:"Estado de conservação declarado", resolvido:true, editavel:false}`
     (SEMPRE resolvido:true — tem default "Bom" no wizard, nunca é uma pendência real per UI-SPEC)
  3. `{id:"documentacao", texto:"Estado da documentação", resolvido: inputs.docOk===true,
     editavel:true}` (docOk undefined OU false → resolvido:false; só true resolve)
  Retorna `{conf, itens}`. Não inclui itens informativos (poucos comparáveis/atípico/venal) como
  itens de checklist tocáveis — esses já aparecem em `conf.porque` (texto), consistente com a
  tabela "Regra de conteúdo do checklist" do UI-SPEC (só área e documentação viram inputs).

- `fichaRapidaTexto(data)`: `const d=data||{};` monta:
  - `faixaTxt: faixaTxt(d.faixa)` (reusa a função `faixaTxt` já existente no bloco, linha ~1170 —
    MESMO nome de função já existe; usar diretamente, não recriar).
  - `resumo: (d.tipoImovel||"Imóvel")+" no "+(d.bairro||"região")+"."` (mesma abertura de
    `zapResumo`, reuso do padrão já testado).
  - `leitura: d.leitura||""` (repasse 1:1).
  - `comparaveis: Array.isArray(d.comparaveis)?d.comparaveis.slice(0,3):[]` (trunca em 3, nunca
    inventa quando ausente/não-array).
  - `ressalva: "Faixa estimada com base em dados públicos do cadastro municipal — recomenda-se
    confirmar área, estado de conservação e documentação antes de decidir. Não substitui avaliação
    formal."` (texto FIXO, idêntico ao UI-SPEC Componente 4).

Rodar `node --test "tests/doc.test.mjs"` até todos os testes passarem (GREEN). Rodar também
`node --test "tests/*.test.mjs"` (suite completa) para confirmar zero regressão nos testes das
Fases 8/9/10 (busca/detectmode/scores/templates — 47 testes existentes).
  </action>
  <verify>
    <automated>node --test "tests/*.test.mjs" 2>&1 | tail -25</automated>
  </verify>
  <done>As 3 funções existem dentro do bloco RADAR_PURE; node --test "tests/*.test.mjs" passa 100% (doc.test.mjs novo GREEN + zero regressão nos 47 testes existentes); recomendaDocumento cobre a matriz completa de 4 finalidades × CNAI true/false com textos pt-BR finais; pendenciasDocumento reusa scoreConfianca (não duplica a lógica de completude); fichaRapidaTexto nunca inventa faixa nem usa jargão.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recomendaDocumento(finalidadeUso, cnai) -> UI (Wave 2 renderiza porque direto no DOM via esc()) | função pura retorna texto fixo pt-BR (sem input de usuário livre) — risco de injeção é nulo aqui, mas Wave 2 ainda deve usar esc() por disciplina consistente (IN-01) |
| pendenciasDocumento(inputs) -> scoreConfianca (dependência interna) | reuso de função já testada/aprovada na Fase 9 — nenhuma nova superfície de risco, só nova composição |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | recomendaDocumento retornando PTAM mesmo sem CNAI (bug de lógica invertida) | mitigate | matriz de teste cobre as 8 combinações finalidade×CNAI explicitamente — nenhuma combinação não-testada; fixture dedicada a "formal + sem CNAI" com assert de doc==="relatorio" |
| T-11-02 | Information Disclosure | fichaRapidaTexto expondo jargão técnico (mediana/percentil) que deveria ficar só em "ver metodologia" | mitigate | assert negativo em todos os campos do retorno contra os 3 termos de jargão, case-insensitive |
| T-11-03 | Tampering | fichaRapidaTexto inventando faixa/comparáveis quando os dados de origem são ausentes | mitigate | contrato de honestidade testado (fixtures com faixa=null e comparaveis ausente) — nunca interpola valor fabricado |

**Sem ameaças de alta severidade.** Esta plan não toca DOM, localStorage ou rede — é camada pura de decisão/formatação, testável 100% por fixtures.
</threat_model>

<verification>
- `node --test "tests/*.test.mjs" >/dev/null 2>&1 || echo skip` passa
- `node --test "tests/*.test.mjs"` passa 100% (doc.test.mjs novo + scores/templates/detectmode/busca sem regressão — 47 existentes + novos)
- Matriz de recomendação: as 4 finalidades × 2 estados de CNAI (8 combinações) todas cobertas por fixture, com "formal+sem CNAI" explicitamente recomendando "relatorio" (nunca "ptam")
- pendenciasDocumento chama scoreConfianca internamente (grep confirma, sem lógica de completude duplicada)
- fichaRapidaTexto nunca inclui "mediana"/"percentil"/"quartil" e nunca inventa faixa/comparáveis ausentes
</verification>

<success_criteria>
- DOC-01 (camada de decisão) cumprido: recomendaDocumento cobre a matriz completa, textos pt-BR finais prontos para renderizar
- DOC-02 (camada de checklist) cumprido: pendenciasDocumento produz itens estruturados na ordem do UI-SPEC, reusando scoreConfianca
- Base de DOC-01 (Ficha rápida) cumprida: fichaRapidaTexto pronta para a Wave 3 montar o template HTML
- Wave 2 (11-02) e Wave 3 (11-03) podem chamar as 3 funções sem reabrir esta plan — contrato de retorno travado
</success_criteria>

<output>
Após completar, criar `.planning/phases/11-documentos-em-3-niveis/11-01-SUMMARY.md`.
Incluir no SUMMARY: assinatura exata das 3 funções, resultado da suite completa de testes (contagem pass/fail), e a tabela completa da matriz recomendaDocumento (finalidade × CNAI → doc + porque) para a Wave 2 montar o Seletor de Finalidade sem ambiguidade.
</output>
