---
phase: 17-diff-cadastro-cruzamento-caixa
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/fixtures.mjs
  - tests/diff-caixa.test.mjs
  - tests/caderno.test.mjs
  - radar-goiania.html
autonomous: true
requirements: [TERR-06, TERR-07]
tags: [diff, caderno, caixa, indexeddb, lgpd, matching]

must_haves:
  truths:
    - "diffLote detecta subida/queda de vlvenal/vlimp98 acima de 1% e ignora abaixo"
    - "diffLote classifica área construída 0→N (construção nova), N→0 (demolição) e N→M sem cruzar zero"
    - "diffLote detecta mudança de uso e de data de cadastro; sem mudança relevante retorna lista vazia (nunca lança)"
    - "formatarDiff produz as frases comerciais exatas do UI-SPEC §Copywriting"
    - "matching de bairro Caixa casa por norm() exato (SETOR BUENO casa; RESIDENCIAL X 7 não casa — sem falso positivo)"
    - "um nome normalizado pode resolver para >1 cdbairro (conjunto); cruzamento bate se QUALQUER candidato coincide"
    - "cruzarCaixaTerritorio/cruzarCaixaSetor ignoram imóveis sem x/y (guard i.x&&i.y)"
    - "sanitizeCaderno reconstrói snapshot campo-a-campo por DIFF_ALLOW — snapshot.dtnascimen NUNCA sobrevive"
    - "validarImportCaderno aceita item com snapshot válido e não quebra com snapshot malformado"
    - "1º save no caderno grava item.snapshot (5 campos DIFF_ALLOW) + item.snapshotAt"
  artifacts:
    - path: "radar-goiania.html"
      provides: "diffLote, formatarDiff, construirNomeParaCdbairro, cdbairroDoImovelCaixa, cruzarCaixaTerritorio, cruzarCaixaSetor (RADAR_PURE); cadernoBuscar (I/O); DIFF_ALLOW extendido em sanitizeCaderno + CADERNO_ALLOW"
      contains: "function diffLote"
    - path: "tests/diff-caixa.test.mjs"
      provides: "cobertura TDD das funções puras de diff e matching Caixa"
      contains: "function diffLote"
    - path: "tests/fixtures.mjs"
      provides: "DIFF_FIX e CAIXA_MATCH_FIX com nomes reais de caixa-goiania.js"
      contains: "DIFF_FIX"
  key_links:
    - from: "sanitizeCaderno"
      to: "DIFF_ALLOW"
      via: "reconstrução recursiva do sub-objeto snapshot"
      pattern: "DIFF_ALLOW"
    - from: "cadernoBuscar(ci)"
      to: "objectStore('caderno').get(ci)"
      via: "leitura readonly com db.close() (padrão WR-02)"
      pattern: "cadernoBuscar"
    - from: "cruzarCaixaTerritorio"
      to: "cdbairroDoImovelCaixa"
      via: "filter por x/y + interseção de conjunto de cdbairro"
      pattern: "cruzarCaixaTerritorio"
---

<objective>
Construir o NÚCLEO DE DADOS da Fase 17 (100% determinístico, testável): as funções puras de diff de cadastro e de cruzamento Caixa (RADAR_PURE, TDD via node:vm), a defesa LGPD do snapshot (allowlist recursiva em `sanitizeCaderno`), a função de I/O nova `cadernoBuscar(ci)` e a gravação do snapshot inicial no 1º save. Nenhuma UI nova neste plano — só o motor que o Plano 02 consome.

Purpose: separar a lógica pura (verificável em ~140ms por `npm test`) da UI (verificação browser). É onde vivem os dois riscos reais da fase (falso positivo no matching Caixa; PII vazando pelo snapshot) — por isso é TDD e trava em teste.
Output: funções puras + `cadernoBuscar` + snapshot no 1º save, tudo com `npm test` verde (baseline 141 → cresce com os testes novos).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-diff-cadastro-cruzamento-caixa/17-RESEARCH.md
@.planning/phases/17-diff-cadastro-cruzamento-caixa/17-UI-SPEC.md
@.planning/phases/17-diff-cadastro-cruzamento-caixa/17-VALIDATION.md

<interfaces>
<!-- Contratos EXATOS extraídos de radar-goiania.html (pós-Fase 16). Use direto, sem explorar. -->

Bloco puro testável: `radar-goiania.html` linhas ~1311 (`// RADAR_PURE_START`) a ~2414 (`// RADAR_PURE_END`).
Toda função pura nova DEVE nascer DENTRO desse bloco (senão o harness node:vm não a enxerga).

Já existentes no RADAR_PURE (reusar, nunca reimplementar):
```javascript
const USO={0:"—",1:"Residencial",2:"Comercial/Serviço",3:"Industrial",4:"Público/Institucional",5:"Misto",6:"Territorial"}; // linha 1316
const clean=v=>String(v==null?"":v).trim(); // 1317
const norm=s=>String(s||"").normalize("NFD").replace(/[̀-ͯ]/g,"").toUpperCase().replace(/\s+/g," ").trim(); // 1318 — ÚNICA fonte de normalização
const CADERNO_ALLOW=["ci","nrinscr","cdbairro","nrquadra","nrlote","vlvenal","areaedif","areaterr","vlimp98","dtinclusao","uso","endereco","tag","nota","status","savedAt","updatedAt"]; // 2367
function sanitizeCaderno(obj){ /* allowlist positiva CADERNO_ALLOW.forEach; já valida ci (CADERNO_CI_RE), trunca tag/nota, default status */ } // 2386
function validarImportCaderno(json){ /* parse + Array + sanitizeCaderno por item + filtra ci */ } // 2407
```

Estrutura REAL de `CAIXA.imoveis[]` (de caixa-goiania.js, 178 itens): `{id, b(nome bairro/loteamento livre, MAIÚSCULO sem acento, sempre presente), e, p, a, d, t, m, u, cb(código interno Caixa — NÃO é cdbairro fiscal, IGNORAR), x, y(UTM; ausentes em 36% dos itens), pr}`.

`bairros-goiania.json`: FeatureCollection; cada `feature.properties = {id(12 díg), nm_bai(oficial abreviado, MAIÚSCULO sem acento), nm_disp(exibição, prefixo expandido + acento)}`.
`bairro-cdbairro.json`: `{"<id>":{"cd":<cdbairro numérico>,"mo":...}}`.

I/O do Caderno já existente (IndexedDB, store "caderno", keyPath "ci"):
```javascript
function cadernoTem(ci){ /* readonly get(ci) -> boolean; db.close() no onsuccess/onerror (WR-02) */ } // 3396 — MODELO EXATO de cadernoBuscar
function cadernoAtualizar(ci,patch){ /* get + merge + sanitizeCaderno + put updatedAt; rejeita se inexistente */ } // 3338
function cadernoSalvar(itemBruto){ /* sanitize + put; savedAt */ } // 3321
const CADERNO_VERSION=1; // 3278 — comentário linha 3278/3308 sobre "store snapshots" está OBSOLETO (ver decisão abaixo)
```

`salvarNoCadernoUI()` linha 4765 — monta o `item` do 1º save (linhas 4781-4785) SEM snapshot hoje.
</interfaces>
</context>

<decisions>
## Decisões travadas (do CONTEXT/RESEARCH/UI-SPEC — NÃO revisar)

- **Sem bump de `CADERNO_VERSION`** (fica em 1). Snapshot é campo EMBUTIDO no item (`item.snapshot` + `item.snapshotAt`), não store novo. IndexedDB não tem schema de valor — `put()` aceita campos extras sem `onupgradeneeded`. Substituir o comentário obsoleto das linhas 3278 e 3308 por uma nota curta explicando por que a Fase 17 NÃO precisou do bump (ver RESEARCH §"IndexedDB version bump"). NÃO tocar em `onupgradeneeded`.
- **`sanitizeCaderno` valida o sub-objeto `snapshot` RECURSIVAMENTE** (allowlist `DIFF_ALLOW`, bloqueante — ASVS V5, Pitfall 5). PII dentro do snapshot (ex.: `dtnascimen`) NUNCA sobrevive.
- **Matching só exato-normalizado** (`norm()`), nunca fuzzy. Sem match → array vazio (honesto, nunca falso positivo). Sem tabela de sinônimos nesta fase.
- Reusar `norm()` (linha 1318) — nunca escrever 2ª função de normalização.
- `DIFF_ALLOW=["vlvenal","areaedif","vlimp98","uso","dtinclusao"]`; `DIFF_THRESH_PCT=1`; `DIFF_THRESH_AREA_M2=1` — constantes nomeadas, nunca hardcoded inline.
</decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: diffLote + formatarDiff (RADAR_PURE, TDD)</name>
  <files>tests/fixtures.mjs, tests/diff-caixa.test.mjs, radar-goiania.html</files>
  <behavior>
    diffLote(snap, atual, opts) — snap/atual são objetos com campos de DIFF_ALLOW; opts={DIFF_THRESH_PCT,DIFF_THRESH_AREA_M2} com defaults nas constantes nomeadas. Retorna array de mudanças tipadas (nunca lança):
    - Test: vlvenal 200000→224000 (+12%) → [{campo:"vlvenal",tipo:"pct",direcao:"subiu",pct:12}]
    - Test: vlvenal 200000→201000 (+0.5%, < DIFF_THRESH_PCT) → [] (ignora ruído)
    - Test: vlvenal 200000→180000 (-10%) → direcao "desceu", pct:10
    - Test: vlimp98 subiu/desceu acima de 1% → tipo "pct" (mesma regra do venal)
    - Test: areaedif 0→85 → {campo:"areaedif",tipo:"area",subtipo:"nova",delta:85}
    - Test: areaedif 120→0 → subtipo:"demolicao",delta:120
    - Test: areaedif 100→160 (não cruza zero) → subtipo:"aumentou",delta:60
    - Test: areaedif 160→100 → subtipo:"diminuiu",delta:60
    - Test: areaedif 100→100.5 (< DIFF_THRESH_AREA_M2) → não entra na lista
    - Test: uso 1→2 → {campo:"uso",tipo:"categorico",de:1,para:2}
    - Test: dtinclusao mudou → {campo:"dtinclusao",tipo:"categorico"}
    - Test: snap===atual em todos os 5 campos → [] (empty, honesto — reabrir na mesma sessão)
    - Test: campo ausente/null num dos lados → nunca lança; trata como "sem mudança" para aquele campo
    formatarDiff(mudancas, dataFmt) — dataFmt é string "dd/mm" (do snapshotAt anterior; passado de fora, função pura sem Date). Retorna array de strings EXATAS do UI-SPEC:
    - Test: venal subiu 12% → "Valor venal subiu 12% desde 10/05" (dataFmt="10/05")
    - Test: venal desceu → "Valor venal desceu {pct}% desde {dataFmt}"
    - Test: areaedif nova → "Área construída: +85 m² — construção nova?"
    - Test: areaedif demolicao → "Área construída: -120 m² — demolição?"
    - Test: areaedif aumentou → "Área construída aumentou 60 m²"
    - Test: areaedif diminuiu → "Área construída diminuiu 60 m²"
    - Test: iptu subiu → "IPTU subiu {pct}% desde {dataFmt}"
    - Test: uso mudou → "Uso mudou de Residencial para Comercial/Serviço" (rótulos via USO[...], nunca código cru)
    - Test: dtinclusao → "Data de cadastro atualizada"
  </behavior>
  <action>
    1. Em `tests/fixtures.mjs`, adicionar `DIFF_FIX` ao objeto `FIXTURES` (mesmo estilo de `CADERNO_FIX`): pares snapshot/atual cobrindo todos os casos do <behavior> (venal ±, threshold, area 0→N/N→0/N→M/ruído, uso, dtinclusao, sem-mudança) e os resultados esperados de formatarDiff com dataFmt fixo "10/05".
    2. Criar `tests/diff-caixa.test.mjs` seguindo EXATAMENTE o padrão de `tests/caderno.test.mjs`: slice do bloco `RADAR_PURE_START`/`RADAR_PURE_END` via `node:vm` (indexOf + slice por linha, nunca indexOf cru), com `assert.ok(src.includes("function diffLote"),...)` de guarda RED→GREEN, e o rodapé `globalThis.__exports = {diffLote,formatarDiff,DIFF_ALLOW,DIFF_THRESH_PCT,DIFF_THRESH_AREA_M2};` (as demais funções serão acrescentadas ao mesmo export nas Tasks 2/3). Importar `FIXTURES` de `./fixtures.mjs` e usar `FIXTURES.DIFF_FIX`.
    3. Rodar `npm test` — deve FALHAR (RED: `diffLote` ainda não existe).
    4. Implementar no bloco RADAR_PURE de `radar-goiania.html` (perto do bloco Caderno, ANTES de `// RADAR_PURE_END` linha ~2414): as constantes `DIFF_ALLOW=["vlvenal","areaedif","vlimp98","uso","dtinclusao"]`, `DIFF_THRESH_PCT=1`, `DIFF_THRESH_AREA_M2=1` e as funções `diffLote(snap,atual,opts)` e `formatarDiff(mudancas,dataFmt)`. `pct` = arredondado para inteiro (`Math.round`); comparação de venal/IPTU só conta se `|Δ%| >= DIFF_THRESH_PCT`; área só conta se `|Δ| >= DIFF_THRESH_AREA_M2`; uso/dtinclusao comparação categórica estrita (valores diferentes e ambos presentes). Rótulo de uso via `USO[...]` (já no bloco). NUNCA lançar — campo ausente é "sem mudança".
    5. Rodar `npm test` — deve PASSAR (GREEN). Commit `feat(17-01): diffLote/formatarDiff puros (TERR-06)`.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "function diffLote" radar-goiania.html` (exit 0)
    - `grep -q "function formatarDiff" radar-goiania.html` (exit 0)
    - `grep -q 'DIFF_ALLOW=\["vlvenal","areaedif","vlimp98","uso","dtinclusao"\]' radar-goiania.html` (exit 0)
    - `grep -q "DIFF_THRESH_PCT=1" radar-goiania.html` (exit 0)
    - `grep -qi "construção nova" radar-goiania.html` (exit 0) — frase do UI-SPEC presente no formatador
    - `npm test` sai 0; contagem de testes > 141
  </acceptance_criteria>
  <done>diffLote/formatarDiff no RADAR_PURE, cobertos por tests/diff-caixa.test.mjs com fixtures reais, npm test verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Matching Caixa → cdbairro + cruzamento (RADAR_PURE, TDD)</name>
  <files>tests/fixtures.mjs, tests/diff-caixa.test.mjs, radar-goiania.html</files>
  <behavior>
    construirNomeParaCdbairro(features, idParaCd) — features = array de `{properties:{id,nm_bai,nm_disp}}`; idParaCd = objeto/Map `id→cd`. Retorna `Map<string normalizado, Set<number>>`:
    - Test: feature nm_disp "Setor Bueno" / nm_bai "SETOR BUENO", id com cd=52 → map.get(norm("SETOR BUENO")) contém 52
    - Test: dois features com mesmo nome normalizado mas cd diferentes (12,13) → Set {12,13} (Pitfall 4, nunca descarta)
    - Test: feature sem cd em idParaCd → não entra no map (não inventa)
    cdbairroDoImovelCaixa(imovelCaixa, nomeParaCdbairro) — retorna array de cd:
    - Test: b "SETOR BUENO" com match → [52] (ou os candidatos)
    - Test: b "RESIDENCIAL JARDINS DO CERRADO 7" sem match → [] (honesto)
    - Test: normalização — b "JARDIM ATLANTICO" casa nm_disp "Jardim Atlântico" via norm() (acento/caixa)
    cruzarCaixaTerritorio(imoveisCaixa, itensCaderno, nomeParaCdbairro) — retorna `{matches:[imoveis], bairros:[cd distintos], n:matches.length}`:
    - Test: só inclui imóveis com i.x && i.y (imóvel sem coordenada nunca entra), mesmo que o bairro bata
    - Test: itensCaderno com cdbairro 52 + imóvel Caixa "SETOR BUENO"(cd 52) com x/y → matches inclui o imóvel; bairros=[52]
    - Test: nome com >1 cd candidato (Set{12,13}), caderno tem 13 → bate (some())
    - Test: nenhum item do caderno bate → matches=[], bairros=[], n:0
    cruzarCaixaSetor(imoveisCaixa, cdbairro, nomeParaCdbairro) — mesma lógica para UM cdbairro (o setor em foco):
    - Test: retorna imóveis com x/y cujo cdbairroDoImovelCaixa inclui o cdbairro dado
  </behavior>
  <action>
    1. Em `tests/fixtures.mjs`, adicionar `CAIXA_MATCH_FIX`: features sintéticas com NOMES REAIS extraídos de `caixa-goiania.js` ("SETOR BUENO", "JARDIM ATLANTICO", "RESIDENCIAL JARDINS DO CERRADO 7"), com nm_disp acentuado ("Setor Bueno","Jardim Atlântico"), um idParaCd de exemplo, imóveis Caixa com/sem x/y, e um caso de colisão nome→2 cd. Não depender de dado sintético divorciado da realidade.
    2. Estender `tests/diff-caixa.test.mjs`: guardas `assert.ok(src.includes("function cruzarCaixaTerritorio"),...)` etc., e acrescentar as 4 funções ao rodapé `globalThis.__exports`. Cobrir todos os casos do <behavior>.
    3. `npm test` → RED.
    4. Implementar no RADAR_PURE (mesma região da Task 1): `construirNomeParaCdbairro(features,idParaCd)`, `cdbairroDoImovelCaixa(imovel,map)`, `cruzarCaixaTerritorio(imoveis,itens,map)`, `cruzarCaixaSetor(imoveis,cdbairro,map)`. Reusar `norm()` (linha 1318). `construirNomeParaCdbairro` aceita idParaCd como Map OU objeto (`idParaCd.get?idParaCd.get(id):idParaCd[id]`) para funcionar tanto no teste quanto com o `idParaCdbairro` (Map) real do runtime. Filtro `i=>i.x&&i.y` SEMPRE (mesmo guard de `toggleCaixa`). IGNORAR `i.cb` (Pitfall 2). Set/array de candidatos, nunca valor único (Pitfall 4).
    5. `npm test` → GREEN. Commit `feat(17-01): matching e cruzamento Caixa puros (TERR-07)`.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "function construirNomeParaCdbairro" radar-goiania.html` (exit 0)
    - `grep -q "function cdbairroDoImovelCaixa" radar-goiania.html` (exit 0)
    - `grep -q "function cruzarCaixaTerritorio" radar-goiania.html` (exit 0)
    - `grep -q "function cruzarCaixaSetor" radar-goiania.html` (exit 0)
    - `grep -q "i.x&&i.y" radar-goiania.html` (exit 0) — guard de coordenada aplicado
    - NÃO existe 2ª função de normalização: `grep -c "function norm\b\|normalizarNome" radar-goiania.html` retorna só a `norm` existente (sem `normalizarNome`)
    - `npm test` sai 0
  </acceptance_criteria>
  <done>4 funções de matching/cruzamento no RADAR_PURE, sem falso positivo, guard de coordenada, colisão resolvida em conjunto, npm test verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Snapshot LGPD (sanitize recursivo + allowlist + import) + cadernoBuscar + 1º save</name>
  <files>tests/caderno.test.mjs, tests/fixtures.mjs, radar-goiania.html</files>
  <behavior>
    sanitizeCaderno(obj) com snapshot — reconstrói `out.snapshot` campo-a-campo por DIFF_ALLOW (allowlist recursiva), nunca copia o sub-objeto bruto:
    - Test: obj com snapshot={vlvenal,areaedif,vlimp98,uso,dtinclusao} válido → snapshot sobrevive com os 5 campos
    - Test: obj com snapshot={vlvenal:1, dtnascimen:"1980-01-01", cpf:"..."} → out.snapshot NUNCA tem dtnascimen/cpf (só vlvenal)
    - Test: snapshot como string/array (malformado) → out.snapshot ausente (não quebra), item de topo continua válido
    - Test: snapshotAt string ISO sobrevive; snapshotAt não-string é descartado
    - Test: item SEM snapshot (formato antigo) continua válido (retrocompatível)
    validarImportCaderno(json) com snapshot:
    - Test: import de item com snapshot válido → aceito com snapshot limpo
    - Test: import de item com snapshot.dtnascimen → aceito SEM o campo PII (nunca quebra o import inteiro)
  </behavior>
  <action>
    1. Estender `tests/caderno.test.mjs`: acrescentar ao rodapé `globalThis.__exports` nada novo é necessário (sanitizeCaderno/validarImportCaderno já exportados); adicionar `assert` de guarda `src.includes('"snapshot"')` (allowlist estendida) e os casos do <behavior>. Adicionar a `tests/fixtures.mjs` (dentro de `CADERNO_FIX`) os itens `itemComSnapshot`, `itemSnapshotComPII` (snapshot com dtnascimen/cpf), `itemSnapshotMalformado` (snapshot:"x"), e `importComSnapshotPII`.
    2. `npm test` → RED (snapshot ainda não está em CADERNO_ALLOW / sem reconstrução recursiva).
    3. Em `radar-goiania.html`:
       - Estender `CADERNO_ALLOW` (linha 2367) adicionando `"snapshot","snapshotAt"` (aditivo, nunca remove nada).
       - Em `sanitizeCaderno` (linha 2386), APÓS o `forEach` da allowlist, adicionar: se `out.snapshot` existe, reconstruir `out.snapshot` como objeto novo só com os campos de `DIFF_ALLOW` presentes e do tipo esperado; se `out.snapshot` não for objeto plano (string/array/null) → `delete out.snapshot`. Validar `out.snapshotAt` como string (senão delete). Mesmo estilo defensivo do CR-01 já aplicado a `ci`.
       - Adicionar `cadernoBuscar(ci)` — nova função de I/O, irmã EXATA de `cadernoTem(ci)` (linha 3396): readonly `get(ci)`, resolve com `req.result` (o ITEM ou undefined), `db.close()` no onsuccess/onerror (WR-02). Colocar imediatamente após `cadernoTem`, dentro do bloco CADERNO_IO (antes de `// CADERNO_IO_END`).
       - Em `salvarNoCadernoUI()` (linha 4765), no objeto `item` do 1º save (linhas 4781-4785), acrescentar `snapshot:{vlvenal:a.vlvenal,areaedif:a.areaedif,vlimp98:a.vlimp98,uso:a.uso,dtinclusao:a.dtinclusao}` e `snapshotAt:new Date().toISOString()` — subconjunto DIFF_ALLOW (5 campos, SEM areaterr).
       - Substituir o comentário obsoleto da linha 3278 (e o bloco reservado de comentário na linha 3308) por uma nota curta: Fase 17 NÃO subiu CADERNO_VERSION — snapshot é campo embutido no item; IndexedDB não tem schema de valor, `put()` aceita campos extras sem `onupgradeneeded`; bump só seria necessário para novo store/índice (nenhum aqui).
    4. `npm test` → GREEN. Commit `feat(17-01): snapshot LGPD recursivo + cadernoBuscar + 1o save (TERR-06)`.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"snapshot","snapshotAt"' radar-goiania.html` (exit 0) — allowlist estendida
    - `grep -q "function cadernoBuscar" radar-goiania.html` (exit 0)
    - `grep -q "snapshotAt:new Date().toISOString()" radar-goiania.html` (exit 0) — 1º save grava snapshot
    - `grep -c "dtnascimen" radar-goiania.html` — `dtnascimen` NUNCA aparece em DIFF_ALLOW nem no bloco novo (só pode existir onde já existia antes da fase, se existia)
    - `grep -q "CADERNO_VERSION=1" radar-goiania.html` (exit 0) — sem bump
    - `npm test` sai 0; teste `snapshot.dtnascimen` provado removido
  </acceptance_criteria>
  <done>Snapshot com defesa LGPD recursiva, cadernoBuscar disponível, 1º save gravando snapshot inicial, comentário de versão atualizado, npm test verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arquivo JSON importado → Caderno (IndexedDB) | Conteúdo não confiável entra via `validarImportCaderno`; agora inclui o sub-objeto `snapshot` |
| endpoint cadastral → objeto `a` da ficha → snapshot gravado | `a` tem ~85 campos (inclui PII); só 5 podem virar snapshot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information Disclosure | `sanitizeCaderno` / sub-objeto `snapshot` | mitigate | Allowlist RECURSIVA: reconstruir `out.snapshot` campo-a-campo por `DIFF_ALLOW` (nunca copiar bruto); `dtnascimen`/`cpf` dentro do snapshot NUNCA sobrevivem. Teste bloqueante `itemSnapshotComPII` (Task 3). |
| T-17-02 | Tampering | `validarImportCaderno` | mitigate | Item com `snapshot` malformado (string/array) é aceito SEM o campo, nunca quebra o import inteiro; reusa `sanitizeCaderno` por item (Task 3). |
| T-17-03 | Tampering (integridade do dado exibido) | `cdbairroDoImovelCaixa` / matching | mitigate | Match SÓ exato-normalizado via `norm()`, nunca fuzzy → zero falso positivo (74% medido); sem match → `[]` (Task 2). |
| T-17-04 | Denial of Service (dado ausente) | `cruzarCaixaTerritorio`/`cruzarCaixaSetor` | mitigate | Guard `i.x&&i.y` antes de considerar cruzável (mesmo de `toggleCaixa`); imóvel sem coordenada nunca entra (Task 2). |
| T-17-05 | Elevation (schema/store) | `cadernoAbrirDB` / `onupgradeneeded` | accept | Sem bump de versão, sem store/índice novo → zero superfície nova de I/O (`onblocked`/multi-aba). Snapshot é campo embutido; risco residual nulo. |
</threat_model>

<verification>
- `npm test` sai 0 com contagem > 141 (baseline pré-fase).
- Nenhuma 2ª função de normalização criada (só `norm`).
- `dtnascimen` ausente de `DIFF_ALLOW` e do bloco novo; teste de import com PII no snapshot prova a remoção.
- `CADERNO_VERSION` permanece 1; `onupgradeneeded` intocado.
</verification>

<success_criteria>
- diffLote/formatarDiff/matching/cruzamento no RADAR_PURE, cobertos por `tests/diff-caixa.test.mjs`.
- sanitizeCaderno com allowlist recursiva de snapshot; validarImportCaderno retrocompatível.
- cadernoBuscar(ci) disponível; 1º save grava snapshot inicial.
- `npm test` verde.
</success_criteria>

<output>
Após completar, criar `.planning/phases/17-diff-cadastro-cruzamento-caixa/17-01-SUMMARY.md`.
</output>
