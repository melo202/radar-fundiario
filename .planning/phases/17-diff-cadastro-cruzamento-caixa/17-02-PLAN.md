---
phase: 17-diff-cadastro-cruzamento-caixa
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified:
  - radar-goiania.html
autonomous: true
requirements: [TERR-06, TERR-07]
tags: [diff, caderno, caixa, ui, mapa, xss]

must_haves:
  truths:
    - "Revisitar um lote salvo mostra o bloco #dDiff com o que mudou (ou 'Sem mudanças') na ficha"
    - "Lote nunca salvo no caderno NÃO mostra #dDiff (bloco permanece hidden)"
    - "Após montar o diff, o snapshot novo é gravado silenciosamente (cadernoAtualizar); falha de escrita gera toast, nunca esconde o diff já exibido"
    - "O bloco Caderno mostra badge '🏦 N imóveis Caixa no seu território' quando há cruzamento"
    - "O painel do território mostra uma linha Caixa quando o setor em foco tem imóveis Caixa"
    - "Pinos Caixa em setor salvo ganham anel visual + linha no popup + item na legenda"
    - "Tocar no badge/linha Caixa abre o pino (N=1) ou fitBounds+toast (N>1) — ação em 1 toque"
    - "Todo texto renderizado via esc()/textContent; handlers leem data-attributes, nunca interpolam valor em on*"
  artifacts:
    - path: "radar-goiania.html"
      provides: "#dDiff estático + renderDiffUI(a); badge Caixa em renderCadernoBlock; #terrCaixaLine em montarPainel; caixaAnelLayer em toggleCaixa; cxp-terr em caixaPopup; item na #pinoLegenda; abrirCaixaNoMapaUI(el); nomeParaCdbairro memoizado"
      contains: "function renderDiffUI"
  key_links:
    - from: "showDetail (fim, após renderCadernoBtn linha ~4651)"
      to: "renderDiffUI(a)"
      via: "chamada irmã assíncrona usando clean(a.ci||a.nrinscr)"
      pattern: "renderDiffUI"
    - from: "renderDiffUI"
      to: "cadernoBuscar + diffLote + formatarDiff + cadernoAtualizar"
      via: "lê item.snapshot, monta diff, grava snapshot novo"
      pattern: "cadernoBuscar"
    - from: "abrirCaixaNoMapaUI(el)"
      to: "el.dataset.bairros"
      via: "handler lê data-attribute, nunca interpolação em onclick"
      pattern: "dataset.bairros"
---

<objective>
Ligar o núcleo do Plano 01 às 4 superfícies de UI do UI-SPEC: (1) bloco de diff `#dDiff` na ficha; (2) badge Caixa no bloco Caderno; (3) linha Caixa no painel do território; (4) destaque no pino Caixa (anel + popup + legenda) com ação em 1 toque. Fecha com uma task de verificação (greps + `npm test`) e os passos de verificação browser para o orquestrador.

Purpose: entregar TERR-06/07 de forma visível e acionável ao corretor, reusando 100% os componentes já em produção (nenhuma cor/glifo novo) e repetindo a defesa XSS já fixada na Fase 16 (CR-01).
Output: as 3 superfícies novas + destaque no mapa, tudo integrado, `npm test` verde, checklist browser para o orquestrador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-diff-cadastro-cruzamento-caixa/17-RESEARCH.md
@.planning/phases/17-diff-cadastro-cruzamento-caixa/17-UI-SPEC.md
@.planning/phases/17-diff-cadastro-cruzamento-caixa/17-01-SUMMARY.md

<interfaces>
<!-- Do Plano 01 (já implementado) + anchors de UI. Use direto, sem explorar o código. -->

Funções puras disponíveis (RADAR_PURE): `diffLote(snap,atual,opts)`, `formatarDiff(mudancas,dataFmt)`, `construirNomeParaCdbairro(features,idParaCd)`, `cdbairroDoImovelCaixa(imovel,map)`, `cruzarCaixaTerritorio(imoveis,itens,map)→{matches,bairros,n}`, `cruzarCaixaSetor(imoveis,cdbairro,map)`. Constantes: `DIFF_ALLOW`, `DIFF_THRESH_PCT`, `DIFF_THRESH_AREA_M2`.
I/O disponível: `cadernoBuscar(ci)→Promise<item|undefined>` (linha ~3404), `cadernoTem(ci)`, `cadernoAtualizar(ci,patch)` (3338), `cadernoListar(opts)` (3374).

Pontos de hook:
- `showDetail(a,ll)` (4540): `renderCadernoBtn()` é chamado na linha ~4651 — chamar `renderDiffUI(a)` IMEDIATAMENTE ao lado (mesmo ponto). `DCUR=a` é o global do imóvel aberto. Chave do Caderno = `clean(a.ci||a.nrinscr)` (NUNCA a variável local `ci` de showDetail — Pitfall 1).
- `renderCadernoBtn()` (4744): MODELO da irmã assíncrona (bate no IDB, não trava render, guard `clean(DCUR.ci||DCUR.nrinscr)!==ci` p/ ficha que mudou).
- HTML da ficha: `#dLeitura` (linha 1080) e `#dActsPrim` (linha 1082) — `#dDiff` estático entra ENTRE eles (linha 1081).
- `renderCadernoBlock()` async (4912): monta `#cadernoBlock`; `.savedblock-head` tem `#cadernoCount`; itens têm `data-ci`. Já lista `itens` de `cadernoListar`.
- `montarPainel(scan,st,layer)` (3466): `scan.cdbairro` = código do setor; `#terrGrid` (linha 1116) e `#terrDetectorView` (linha 1123) — linha Caixa entra ENTRE eles. Renderiza com `.classList.add("show")` no fim.
- `caixaPopup(i)` (6101) retorna HTML string (usa `esc()`); `toggleCaixa()` (6112) monta `caixaLayer` (circleMarker radius:7, color:"#141a1f", fillColor:"#a8842c"); `isMobile()`/`setView("mapa")`; `toWGS(i.x,i.y)`.
- `#pinoLegenda` (1039-1046): lista de `<span><i class="pl-dot" style="background:#..."></i>Rótulo</span>`; já tem "🏦 Caixa" (linha 1043).
- Lookup de bairro: `carregarLookupCdbairro()` (3573, async, memoizado) popula `idParaCdbairro` (Map id→cd). BAIRROS (features de bairros-goiania.json) é global. `norm` linha 1318.

Padrão XSS (CR-01, Fase 16): handler lê `this.dataset.X`; NUNCA concatenar valor do endpoint dentro de `onclick=""`. Texto via `esc()`/`textContent`.
</interfaces>
</context>

<decisions>
## Decisões travadas (UI-SPEC — NÃO revisar)
- Diff é NEUTRO: nunca pinta linha de verde/dourado/vermelho (subir/descer venal não é status). Sinal ▲/▼/● é glifo tipográfico com `--ink`, `aria-hidden="true"`; a palavra ("subiu"/"desceu"/"mudou") já está no texto.
- Zero cor/glifo novo. Anel do pino = `--ink` (mesma cor do traço Caixa), circleMarker extra em `caixaAnelLayer`, NUNCA dashArray (PIN-01).
- Badge Caixa e linha Caixa são `<button>` (≥44px, teclado nativo). Copy EXATA do UI-SPEC §Copywriting.
- `renderDiffUI` usa `clean(a.ci||a.nrinscr)` (Pitfall 1). Grava snapshot novo silenciosamente; falha de escrita → toast padrão, nunca esconde o diff já mostrado.
- Sem point-in-polygon (refinamento futuro). Cruzamento só por cdbairro.
</decisions>

<tasks>

<task type="auto">
  <name>Task 1: Bloco de diff #dDiff na ficha + renderDiffUI(a)</name>
  <files>radar-goiania.html</files>
  <action>
    1. HTML estático: inserir ENTRE `#dLeitura` (linha 1080) e `#dActsPrim` (linha 1082) o bloco do UI-SPEC §1:
       `<div class="ddiff" id="dDiff" hidden><div class="ddiff-head"><span class="ddiff-tt">📓 Desde sua última visita</span><span class="ddiff-data" id="dDiffData"></span></div><ul class="ddiff-list" id="dDiffList"></ul></div>`
    2. CSS: adicionar as classes `.ddiff` (fundo `var(--paper-2)`, padding `8px 16px`, mesma régua de `.terrdet-item`/`.dnote`), `.ddiff-head`, `.ddiff-tt` (13px/700, `--ink`), `.ddiff-data` (IBM Plex Mono 10.5px/700, `--muted`), `.ddiff-list` (lista sem marcador, gap 8px), `.ddiff-item` (flex, gap 4px), `.ddiff-sign` (`--ink`, aria-hidden), `.ddiff-txt` (13px/500, `--ink`), `.ddiff-empty` (`--muted`). NENHUMA cor de status no diff (regra travada). Reusar tokens existentes, zero var nova.
    3. `renderDiffUI(a)` — irmã assíncrona de `renderCadernoBtn` (colocar logo após ela, ~4761). Fluxo do UI-SPEC §1:
       - `const el=document.getElementById("dDiff"); if(!el||!a)return;` mantém hidden por default.
       - `if(!cadernoDisponivel())return;` (mantém hidden).
       - `const ci=clean(a.ci||a.nrinscr);` (Pitfall 1 — NUNCA a var local de showDetail).
       - `cadernoBuscar(ci).then(item=>{ ... }).catch(...)`:
         - guard: se a ficha mudou (`clean(DCUR.ci||DCUR.nrinscr)!==ci`) → return.
         - se `!item || !item.snapshot` → `el.hidden=true; return;` (lote não salvo ou sem snapshot ainda → bloco não existe p/ esse imóvel).
         - `const mud=diffLote(item.snapshot, a);` `const dataFmt` = "dd/mm" de `item.snapshotAt` (mesmo padrão de `fmtSalva()`; formatar mês/dia).
         - Preencher `#dDiffData` com `"desde "+dataFmt` via textContent; limpar e preencher `#dDiffList`: para cada string de `formatarDiff(mud,dataFmt)`, criar `<li class="ddiff-item"><span class="ddiff-sign" aria-hidden="true">{▲|▼|●}</span><span class="ddiff-txt">{texto}</span></li>` usando `document.createElement`+`textContent` (NUNCA innerHTML com valor). Sinal derivado do tipo/direcao da mudança (▲ subiu/nova/aumentou, ▼ desceu/demolicao/diminuiu, ● categorico). Lista vazia → um `<li class="ddiff-empty">Sem mudanças no cadastro desde a última visita.</li>`.
         - `el.hidden=false;`
         - Gravar snapshot novo silenciosamente: `cadernoAtualizar(ci,{snapshot:{vlvenal:a.vlvenal,areaedif:a.areaedif,vlimp98:a.vlimp98,uso:a.uso,dtinclusao:a.dtinclusao},snapshotAt:new Date().toISOString()}).catch(()=>toast(ERRO_ESCRITA_CADERNO));` — sucesso sem toast; falha NUNCA esconde o diff já exibido.
         - `.catch()` de erro inesperado de LEITURA (não "não encontrado"): mostrar o texto de erro de comparação do UI-SPEC ("Não foi possível comparar com sua última visita. Tente abrir a ficha de novo.") no bloco, nunca falha silenciosa.
    4. Hook: no fim de `showDetail`, ao lado de `renderCadernoBtn();` (linha 4651), adicionar `renderDiffUI(a);`.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'id="dDiff"' radar-goiania.html` (exit 0) — bloco estático
    - `grep -q "function renderDiffUI" radar-goiania.html` (exit 0)
    - `grep -q "renderDiffUI(a)" radar-goiania.html` (exit 0) — hook no showDetail
    - `grep -q "clean(a.ci||a.nrinscr)" radar-goiania.html` (exit 0) — chave correta (Pitfall 1)
    - `grep -q "Sem mudanças no cadastro desde a última visita" radar-goiania.html` (exit 0)
    - `grep -q "cadernoAtualizar(ci,{snapshot" radar-goiania.html` (exit 0) — grava snapshot novo
    - Diff NÃO usa cor de status: nenhum `--status-` dentro do bloco `.ddiff` (leitura manual do CSS adicionado)
    - `npm test` sai 0 (suíte inalterada — UI não quebra o bloco puro)
  </acceptance_criteria>
  <done>#dDiff estático posicionado, renderDiffUI hookado em showDetail com a chave correta, grava snapshot novo, diff neutro sem cor de status, npm test verde.</done>
</task>

<task type="auto">
  <name>Task 2: Cruzamento Caixa — badge Caderno + linha Painel + anel/popup/legenda + ação 1 toque</name>
  <files>radar-goiania.html</files>
  <action>
    1. Map memoizado de nome→cdbairro: adicionar `let nomeParaCdbairro=null;` e uma função `async function garantirNomeParaCdbairro(){ if(nomeParaCdbairro)return nomeParaCdbairro; await carregarLookupCdbairro(); const feats=(BAIRROS&&BAIRROS.features)||BAIRROS||[]; nomeParaCdbairro=construirNomeParaCdbairro(feats, idParaCdbairro); return nomeParaCdbairro; }` (usa a função pura do Plano 01 com os dados já carregados; zero requisição nova além do lookup já lazy). Ajustar a extração de `feats` conforme a forma real de BAIRROS (FeatureCollection vs array de features).
    2. Badge Caixa no Caderno (UI-SPEC §2): em `renderCadernoBlock()`, após montar os itens, se `window.CAIXA?.imoveis?.length`, chamar `garantirNomeParaCdbairro()` e `cruzarCaixaTerritorio(CAIXA.imoveis, itens, mapa)`; se `n>0`, renderizar no `.savedblock-head` (ao lado de `#cadernoCount`) um `<button type="button" class="cadbook-caixabadge" id="cadernoCaixaBadge" data-bairros="{bairros.join(',')}" onclick="abrirCaixaNoMapaUI(this)">🏦 {N} imóvel(is) Caixa no seu território</button>` — texto via textContent/esc; `data-bairros` setado via `dataset.bairros`, NUNCA interpolado no atributo onclick. Singular/plural conforme copy do UI-SPEC. Se n===0, não renderizar (nunca "0 imóveis").
    3. Linha Caixa no Painel (UI-SPEC §3): em `montarPainel(scan,st,layer)`, antes do `.classList.add("show")` final, se `scan&&window.CAIXA?.imoveis?.length`, chamar `garantirNomeParaCdbairro()` e `cruzarCaixaSetor(CAIXA.imoveis, scan.cdbairro, mapa)`; se `n>0`, revelar `#terrCaixaLine` (button estático inserido no HTML ENTRE `#terrGrid` linha 1116 e `#terrDetectorView` linha 1123: `<button type="button" class="terr-caixa-line" id="terrCaixaLine" data-bairros="" hidden onclick="abrirCaixaNoMapaUI(this)"></button>`), setando `dataset.bairros=String(scan.cdbairro)`, textContent "🏦 {N} imóvel(is) Caixa neste setor — toque para ver no mapa" e `hidden=false`. Setor sem match → `hidden=true` (nunca "0"). Como montarPainel é síncrono e o cruzamento é async, encapsular o trecho Caixa numa IIFE async que revela a linha quando resolver (a linha nasce hidden, nunca "pisca vazia").
    4. Anel no pino (UI-SPEC §4): em `toggleCaixa()`, ao construir os pinos, montar também `caixaAnelLayer` (layerGroup próprio) — para cada imóvel cujo `cdbairroDoImovelCaixa(i, mapa)` intersecta o conjunto de cdbairros salvos no Caderno, adicionar um `L.circleMarker(toWGS(i.x,i.y),{radius:10,color:"#141a1f",weight:2,fillOpacity:0,bubblingMouseEvents:false})` ao `caixaAnelLayer`. O anel sobe/desce junto com `caixaLayer`. Precisa dos cdbairros salvos: buscar via `cadernoListar({})` (async) — encapsular a construção do anel de forma que não bloqueie o toggle (constrói/adiciona quando resolver). Sem match no Caderno → anel layer vazio (nenhum anel).
    5. Popup (UI-SPEC §4): em `caixaPopup(i)`, aceitar um 2º arg opcional `noTerr` (boolean); quando true, inserir depois da linha de endereço e antes do preço: `<div class="cxp-terr">📓 Este imóvel está no seu território salvo.</div>`. Quem monta os pinos passa `noTerr` = (imóvel está no conjunto cruzado).
    6. Legenda (UI-SPEC §4): adicionar item ao `#pinoLegenda` (após linha 1045): `<span><i class="pl-dot" style="background:#a8842c;box-shadow:0 0 0 2px var(--ink)"></i>🏦📓 Caixa no seu território</span>`.
    7. Ação 1 toque `abrirCaixaNoMapaUI(el)` (UI-SPEC §4): lê `el.dataset.bairros` → array de códigos (Number). Garante `caixaLayer`/`caixaAnelLayer` montados e no mapa (reusa/dispara a lógica de `toggleCaixa` sem duplicar construção se já existem). Filtra `CAIXA.imoveis` (com x/y) cujos cdbairro batem. `if(isMobile())setView("mapa")`. N===1 → `map.setView(toWGS(i.x,i.y),18)` + abrir popup do pino (`.openPopup()`). N>1 → `map.fitBounds(...)` + `toast("Mostrando {N} imóveis Caixa no mapa — toque num pino com anel.")`. CSS novo: `.cadbook-caixabadge`, `.terr-caixa-line` (ambos ≥44px, dourado textual via glifo, fundo `--paper-2`, sem cor nova), `.cxp-terr` (linha do popup).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'id="cadernoCaixaBadge"' radar-goiania.html` (exit 0)
    - `grep -q 'id="terrCaixaLine"' radar-goiania.html` (exit 0)
    - `grep -q "caixaAnelLayer" radar-goiania.html` (exit 0)
    - `grep -q "function abrirCaixaNoMapaUI" radar-goiania.html` (exit 0)
    - `grep -q "el.dataset.bairros\|this.dataset.bairros" radar-goiania.html` (exit 0) — handler lê data-attribute (XSS)
    - `grep -q "Este imóvel está no seu território salvo" radar-goiania.html` (exit 0) — linha do popup
    - `grep -q "🏦📓 Caixa no seu território" radar-goiania.html` (exit 0) — item de legenda
    - `grep -q "construirNomeParaCdbairro" radar-goiania.html` (exit 0) — map construído do dado real
    - Nenhum valor de endpoint interpolado em `onclick=""` (leitura manual: `data-bairros` setado via dataset, onclick fixo)
    - `npm test` sai 0
  </acceptance_criteria>
  <done>Badge no Caderno, linha no Painel, anel/popup/legenda no mapa, ação 1 toque via data-attributes (sem XSS), map nome→cdbairro do dado real, npm test verde.</done>
</task>

<task type="auto">
  <name>Task 3: Verificação final (greps + npm test) + checklist browser do orquestrador</name>
  <files>radar-goiania.html</files>
  <action>
    1. Rodar a bateria de greps de aceite dos dois planos (Task 1/2/3 do 17-01 + Task 1/2 deste) e confirmar todos exit 0.
    2. Rodar `npm test` e confirmar suíte verde com contagem > 141.
    3. Confirmar LGPD: `grep -n "dtnascimen" radar-goiania.html` — não aparece em nenhum contexto de snapshot/DIFF_ALLOW.
    4. Registrar no SUMMARY os passos de VERIFICAÇÃO BROWSER a serem executados pelo orquestrador via preview (como na Fase 16 — não é checkpoint humano, é verificação do orquestrador ao vivo):
       - **Diff (TERR-06):** abrir app → buscar/abrir um lote → "📓 Salvar no caderno" → editar o `snapshot.vlvenal` do item no DevTools › IndexedDB › radar_territorio › caderno (ex.: reduzir 12%) → reabrir a mesma ficha → `#dDiff` mostra "Valor venal subiu ~12% desde {dd/mm}"; reabrir SEM editar → "Sem mudanças no cadastro desde a última visita.". Dump do item pós-diff: `snapshot` atualizado + `snapshotAt` recente.
       - **LGPD:** injetar via DevTools um item com `snapshot:{dtnascimen:"1980-01-01",vlvenal:1}` e reimportar (ou salvar) → confirmar que `dtnascimen` foi removido do registro persistido.
       - **Cruzamento (TERR-07):** salvar um lote de setor com imóvel Caixa confirmado (ex.: Setor Bueno) → bloco Caderno mostra "🏦 N imóveis Caixa no seu território"; abrir camada Caixa → pinos do setor com anel; popup com "📓 Este imóvel está no seu território salvo."; painel do território do setor mostra a linha Caixa; tocar no badge/linha → mapa centraliza no pino (N=1) ou fitBounds+toast (N>1).
       - **Zero requisição:** aba Network sem chamadas novas ao ligar diff/cruzamento (dado já em memória/IDB).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` sai 0; contagem > 141
    - Todos os greps de aceite dos Planos 01/02 retornam exit 0
    - `grep -n "dtnascimen" radar-goiania.html` não mostra ocorrência dentro de DIFF_ALLOW/snapshot
    - SUMMARY inclui o checklist de verificação browser do orquestrador
  </acceptance_criteria>
  <done>Suíte verde, greps de aceite confirmados, LGPD confirmada por grep, checklist browser registrado no SUMMARY para o orquestrador.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| valor do endpoint (bairro, valores, ci) → DOM/handlers | Texto renderizado e data-attributes de badge/linha/popup/diff |
| dado do Caderno (IDB) + CAIXA.imoveis → UI de cruzamento | Correlação exibida ao usuário deve ser correta (integridade) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06 | Tampering (XSS) | `#dDiff`/badge/linha/popup + `abrirCaixaNoMapaUI` | mitigate | Texto via `esc()`/`textContent`+`createElement`; `data-bairros` setado via `dataset`, handler lê `this.dataset.bairros` — NUNCA interpolar valor do endpoint em `onclick=""` (lição CR-01, Fase 16). |
| T-17-07 | Spoofing (chave de identidade) | `renderDiffUI(a)` | mitigate | Usa `clean(a.ci||a.nrinscr)` (fórmula do Caderno), nunca a var local `ci` de showDetail (Pitfall 1) — senão o diff nunca acha o item salvo. |
| T-17-08 | Denial of Service (falha de escrita) | gravação do snapshot novo | mitigate | Falha de `cadernoAtualizar` → toast padrão de escrita; NUNCA esconde o diff já exibido nem trava o render (padrão Fase 16). |
| T-17-09 | Information Disclosure (dado ausente) | anel/badge/ação Caixa | mitigate | Só imóveis com `x&&y` viram anel/entram na contagem acionável; imóvel sem pino nunca é "abrível". |
| T-17-10 | Tampering (integridade exibida) | cruzamento Caixa na UI | accept | Match exato-normalizado (Plano 01) → 74% cobertura, 0 falso positivo; 26% sem match ficam honestamente sem destaque (decisão travada). |
</threat_model>

<verification>
- `npm test` verde (> 141).
- Diff neutro (sem cor de status), hookado com a chave correta, grava snapshot novo.
- Badge/linha/anel/popup/legenda renderizados só quando há match; handlers via data-attributes.
- Zero requisição de rede nova (verificação browser do orquestrador).
</verification>

<success_criteria>
- TERR-06: revisita mostra diff enxuto (ou "sem mudanças"), nunca PII; snapshot atualizado.
- TERR-07: cruzamento visível em 3 superfícies + destaque no pino, ação em 1 toque.
- Sem XSS (data-attributes), sem cor nova, `npm test` verde, checklist browser entregue.
</success_criteria>

<output>
Após completar, criar `.planning/phases/17-diff-cadastro-cruzamento-caixa/17-02-SUMMARY.md` (incluir o checklist de verificação browser do orquestrador).
</output>
