---
phase: 14-linguagem-impecavel-pt-br-gate-de-release
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - radar-goiania.html
  - index.html
  - manifest.json
  - .planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md
autonomous: true
requirements: [LING-01]

must_haves:
  truths:
    - "Todo botão da UI começa com verbo de ação no infinitivo (§26.2)"
    - "Placeholders usam exemplos tocáveis, sem texto explicativo longo"
    - "manifest.json, index.html e <title> descrevem o app corretamente em pt-BR"
    - "Onboarding, 'O que o Radar faz' e legenda de pinos revisados contra §26"
    - "npm test continua 100% verde (107 testes)"
  artifacts:
    - path: ".planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md"
      provides: "Seções Botões, Placeholders, Títulos/Descrições/PWA, Onboarding+O que o Radar faz+Legenda preenchidas"
      contains: "Botões"
  key_links:
    - from: "radar-goiania.html <button>"
      to: "checklist §26.2 (verbo de ação)"
      via: "auditoria por categoria"
      pattern: "Gerar|Copiar|Salvar|Comparar|Enviar|Criar|Buscar|Abrir|Ver"
---

<objective>
Varredura sistemática das categorias de microcopy de ENTRADA e descoberta: botões (verbo de ação), placeholders (exemplos tocáveis), títulos/descrições/PWA (`manifest.json`, `index.html`, `<title>`/meta), onboarding (`ONB_CARDS`), "O que o Radar faz" e legenda de pinos. Nenhuma dessas categorias tem teste acoplado, então é edição de HTML/JS puro com mudança mínima.

Purpose: São os primeiros textos que o usuário vê. Botões sem verbo e placeholders vagos são o "amadorismo" que o gate §26 elimina.
Output: Categorias Botões / Placeholders / Títulos-PWA / Onboarding-Descoberta preenchidas na tabela de auditoria; edições mínimas aplicadas; suíte verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-CONTEXT.md
@.planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-RESEARCH.md
@.planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md

<interfaces>
<!-- Âncoras de linha (14-RESEARCH.md §"Inventário da Microcopy") -->
Botões (106 `<button>`): concentração em #formZone (~800-1100), ficha #dActsPrim/#dActsMore (~3430-3470), wizard de documento #lzSheet (~4260-4400), NEG #negSheet (~3980-4030). Localizar todos com `grep -n "<button" radar-goiania.html`.
Placeholders (19): busca (809-868), wizard (4309-4355), colar matrícula (3982). Exceção: linha ~2383 `"erro ao carregar — recarregue a página"` usado como placeholder (deve seguir §26.3 erro-com-saída).
Onboarding: `ONB_CARDS` linha 4772 (3 cartões, campos titulo/texto/cta).
"O que o Radar faz": #oQueFaz linhas 894-905 (5 itens).
Legenda de pinos: #pinoLegenda linhas 932-938 (5 rótulos; o rótulo A1 na linha 935 JÁ foi corrigido para "Oportunidade baixa" no Plano 01 — apenas confirmar consistência).
manifest.json: name "Radar Fundiário · Goiânia", short_name "Radar", description "Busca de imóveis de Goiânia por quadra e lote sobre o cadastro imobiliário público".
index.html: <title> "Radar Fundiário · Goiânia".

Regra §26.2: botões começam com VERBO de ação no infinitivo (Gerar/Copiar/Salvar/Comparar/Enviar/Criar/Buscar/Abrir/Ver/Fechar/Remover). Botões-ícone (× fechar) usam o texto no aria-label (auditado no Plano 03) — o glifo em si não precisa de verbo.
Mudança mínima: NÃO reescrever texto já correto. Placeholders com estilo minúsculo intencional de "exemplo digitável" (ex.: "ex.: 246") são deliberados — não capitalizar sem necessidade.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Varredura de botões (verbo de ação §26.2)</name>
  <read_first>
    - radar-goiania.html (executar `grep -n "<button" radar-goiania.html` e ler o texto visível de cada um)
    - .planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md
  </read_first>
  <action>
    Rodar `grep -n "<button" radar-goiania.html` para enumerar os 106 botões. Para cada um com TEXTO VISÍVEL (innerHTML, não só ícone): verificar se começa com verbo de ação no infinitivo. Aplicar mudança mínima:
    - Botão sem verbo cujo rótulo é um substantivo isolado → prefixar com o verbo certo (ex.: "Documento" → "Gerar documento"; "WhatsApp" → "Copiar para o WhatsApp"). NÃO renomear botões já corretos ("Copiar ⧉", "Gerar documento", "Salvar oportunidade" já passam).
    - Preservar o glifo/ícone quando existir (ex.: "Copiar ⧉" mantém o ⧉).
    - Botões que são só ícone (×, setas, glifos sem texto) NÃO precisam de verbo no innerHTML — seu significado vive no `aria-label` (Plano 03); registrar como "avaliado-mantido".
    NÃO tocar: `data-*`, `id`, `class`, `onclick`/handlers, `data-example` (o atributo; se o mesmo texto for também o innerHTML visível, edita-se só o innerHTML).
    Preencher a seção "Botões" da `14-AUDITORIA.md` com uma linha por botão de texto visível (original | linha | veredito | final | §26).
    Rodar `npm test` (deve permanecer verde — botões não têm teste acoplado).
  </action>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && npm test 2>&1 | grep -q "fail 0" && grep -q "## Botões\|# Botões\|Botões" ".planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Seção "Botões" da auditoria tem uma linha para cada botão de texto visível retornado por `grep -n "<button" radar-goiania.html`
    - Nenhum botão de texto visível restante sem verbo de ação (ou registrado explicitamente como exceção justificada, ex.: botão-ícone)
    - `grep -n "<button" radar-goiania.html` continua retornando 106 elementos (nenhum botão removido/adicionado)
    - `npm test` exits 0 (107 tests, fail 0)
  </acceptance_criteria>
  <done>Todos os botões de texto visível começam com verbo de ação ou têm exceção registrada; seção Botões da auditoria completa; teste verde.</done>
</task>

<task type="auto">
  <name>Task 2: Varredura de placeholders + títulos/descrições/PWA</name>
  <read_first>
    - radar-goiania.html (`grep -n 'placeholder="' radar-goiania.html`; linha ~2383 placeholder de erro; `<title>`/meta description no <head>)
    - index.html (11 linhas — completo)
    - manifest.json (17 linhas — completo)
    - .planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md
  </read_first>
  <action>
    1. Placeholders — rodar `grep -n 'placeholder="' radar-goiania.html` (19 ocorrências). Para cada: aplicar §26 mantendo mudança mínima. Exemplos minúsculos intencionais ("ex.: 246", "digite: faiçalville, oeste, marista…") são estilo deliberado de exemplo digitável — MANTER (registrar "avaliado-mantido"). O placeholder de erro da linha ~2383 (`"erro ao carregar — recarregue a página"`) deve seguir §26.3 (o que houve + o que fazer) — já oferece saída ("recarregue a página"); confirmar acentuação.
    2. Títulos/PWA — revisar contra §26 (acentuação, sem jargão de 1ª camada, tom profissional):
       - `manifest.json`: `name`, `short_name`, `description`. A description atual "Busca de imóveis de Goiânia por quadra e lote sobre o cadastro imobiliário público" é correta e sem jargão amador — manter salvo se ferir §26.
       - `index.html`: `<title>` "Radar Fundiário · Goiânia" (fallback de redirect) e o link "Abrir o Radar Fundiário".
       - `radar-goiania.html`: `<title>` e `<meta name="description">` no <head> (localizar com `grep -n "<title>\|meta name=.description" radar-goiania.html`).
       Aplicar mudança mínima; NÃO tocar `id`, `scope`, `start_url`, `theme_color`, `icons` no manifest (campos funcionais).
    Preencher as seções "Placeholders" e "Títulos/Descrições/PWA" da auditoria. Rodar `npm test`.
  </action>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && node -e "JSON.parse(require('fs').readFileSync('manifest.json','utf8'))" && npm test 2>&1 | grep -q "fail 0" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `manifest.json` continua sendo JSON válido (node parse não lança) e mantém `start_url`, `scope`, `id`, `theme_color`, `icons` inalterados
    - Seções "Placeholders" e "Títulos/Descrições/PWA" da auditoria preenchidas (uma linha por placeholder + campos de manifest/index/title)
    - Placeholders de exemplo minúsculo registrados como "avaliado-mantido" (não capitalizados)
    - `npm test` exits 0 (107 tests, fail 0)
  </acceptance_criteria>
  <done>Placeholders e textos de PWA/título auditados; manifest.json continua válido; teste verde.</done>
</task>

<task type="auto">
  <name>Task 3: Varredura de onboarding + "O que o Radar faz" + legenda de pinos</name>
  <read_first>
    - radar-goiania.html (linhas 4772-4790 ONB_CARDS; 894-905 #oQueFaz; 932-938 #pinoLegenda)
    - .planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md
  </read_first>
  <action>
    1. Onboarding — auditar os 3 cartões de `ONB_CARDS` (linha 4772): campos `titulo`, `texto`, `cta`. Aplicar §26 (verbo de ação no cta — "Próximo"/"Começar" são aceitáveis como navegação; títulos com verbo de ação "Busque…"/"Veja…"/"Gere…"; texto sem jargão de 1ª camada). São textos revisados na Fase 13 — mudança mínima. NÃO alterar a estrutura do array, os campos, `radar_onboard`, nem a lógica de `onbRender`.
    2. "O que o Radar faz" — auditar os 5 itens de #oQueFaz (linhas 894-905) contra §26 (sem jargão, tom profissional, consistência com o glossário). "O que o Radar faz" é nome travado (Fase 13) — não renomear.
    3. Legenda de pinos — auditar os 5 rótulos de #pinoLegenda (932-938). O rótulo da linha 935 já foi corrigido para "Oportunidade baixa" no Plano 01 — apenas confirmar que a legenda está consistente com os rótulos do score badge ("Boa oportunidade"/"Oportunidade média"/"Oportunidade baixa" + Caixa dourado + cinza sem-dado). NÃO alterar as cores inline (`#2c5545`, `#b5451f`, etc.) — são funcionais.
    Preencher a seção "Onboarding + O que o Radar faz + Legenda" da auditoria. Rodar `npm test`.
  </action>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && grep -q "ONB_CARDS" radar-goiania.html && grep -q "Oportunidade baixa" radar-goiania.html && npm test 2>&1 | grep -q "fail 0" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ONB_CARDS` continua sendo um array de 3 objetos com campos `titulo`/`texto`/`cta` (estrutura intacta)
    - `grep -c "Oportunidade baixa" radar-goiania.html` >= 3 (legenda consistente com o Plano 01)
    - `grep -q "radar_onboard" radar-goiania.html` verdadeiro (chave de persistência intacta)
    - Seção "Onboarding + O que o Radar faz + Legenda" da auditoria preenchida (3 cartões + 5 itens + 5 rótulos)
    - `npm test` exits 0 (107 tests, fail 0)
  </acceptance_criteria>
  <done>Onboarding, descoberta e legenda auditados e consistentes com o glossário; teste verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edição de string → render/PWA | strings de botão/placeholder/manifest editadas; risco = tocar atributo funcional (start_url, id, data-*, cor de legenda) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | manifest.json (start_url/scope/id/icons), radar-goiania.html (data-*, id, class, cores inline, radar_onboard) | mitigate | acceptance: manifest continua JSON válido com campos funcionais intactos; ONB_CARDS mantém estrutura; `radar_onboard` preservado; cores inline não tocadas |
| T-14-02 | Information Disclosure (LGPD) | placeholders/onboarding | mitigate | nenhum placeholder/texto induz coleta de PII; grep confirma ausência de `dtnascimen`; exemplos são de busca cadastral pública |
| T-14-04 | Tampering (XSS) | textos de #oQueFaz/legenda inseridos em innerHTML | mitigate | edição só de texto literal sem `<`/`>`/`&` cru; não introduzir HTML novo em strings |
</threat_model>

<verification>
- `npm test` exits 0 (107 tests, fail 0) ao fim de cada task
- `manifest.json` continua JSON válido com `start_url`/`scope`/`id`/`icons` inalterados
- `grep -n "<button" radar-goiania.html` continua = 106
- Seções Botões / Placeholders / Títulos-PWA / Onboarding-Descoberta preenchidas na auditoria
</verification>

<success_criteria>
- Todo botão de texto visível começa com verbo de ação (ou exceção registrada)
- Placeholders e textos de PWA/título auditados com mudança mínima
- Onboarding, "O que o Radar faz" e legenda auditados e consistentes com o glossário
- Suíte 100% verde (107 testes)
</success_criteria>

<output>
Após completar, criar `.planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-02-SUMMARY.md`
</output>
