---
phase: 14-linguagem-impecavel-pt-br-gate-de-release
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - radar-goiania.html
  - .planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md
autonomous: true
requirements: [LING-01]

must_haves:
  truths:
    - "Toda mensagem de erro segue o padrão o que houve + (por quê) + o que fazer (§26.3)"
    - "Estados vazios oferecem um próximo passo em 1 toque"
    - "Todo aria-label narra a mesma ação do contexto visual do elemento"
    - "Tooltips (title) são frases completas e coerentes com o botão que os contém"
    - "npm test continua 100% verde (107 testes)"
  artifacts:
    - path: ".planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md"
      provides: "Seções Toasts/Erros + Estados vazios e Tooltips/aria-label preenchidas"
      contains: "Toasts/Erros"
  key_links:
    - from: "radar-goiania.html toast(...)"
      to: "checklist §26.3 (erro com saída)"
      via: "auditoria por categoria"
      pattern: "Tente|Confira|busque|recarregue|Verifique"
---

<objective>
Varredura das categorias de FEEDBACK e acessibilidade: mensagens de erro/confirmação (`toast(...)`, 46 chamadas), estados vazios (4 blocos), tooltips nativos (`title=`, 8) e rótulos de leitor de tela (`aria-label=`, 35). Foco no §26.3 (erro que explica e oferece saída) e na consistência entre aria-label e o contexto visual. Categoria de UI pura, sem teste acoplado.

Purpose: Erro sem saída e aria-label incoerente são o ponto onde a "linguagem impecável" mais falha na prática — o usuário só encontra esse texto quando algo dá errado ou quando usa leitor de tela.
Output: Seções Toasts/Erros + Estados vazios e Tooltips/aria-label preenchidas na auditoria; edições mínimas; suíte verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-CONTEXT.md
@.planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-RESEARCH.md
@.planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md

<interfaces>
<!-- Âncoras (14-RESEARCH.md §"Inventário") -->
toast(): função central linha 2540; 46 chamadas (contagem ao vivo 2026-07-09) em buscar()/finish() (~2600-2790), salvar/remover oportunidade (~3500-3660), NEG (~4110-4125), wizard de documento (~4400-4410). Enumerar com `grep -n "toast(" radar-goiania.html`.
Estados vazios (.empty): #emptyState (880), erro de busca (2711), sem resultado (2761), libs do mapa não carregaram (4824).
title= (8, contagem ao vivo 2026-07-09): satélite, CSV, "aproximado", CND (~3452), deep-link. Enumerar com `grep -n 'title="' radar-goiania.html`.
aria-label= (35): overlays/sheets, botões-ícone (× fechar/remover), legendas de mapa. Enumerar com `grep -n 'aria-label="' radar-goiania.html`.

Padrão §26.3 (erro): o que houve + por quê (se útil) + o que fazer. Exemplo já correto no código: "Falha ao consultar o cadastro. Tente de novo." / "Não encontramos essa inscrição. Confira os dígitos ou busque pelo endereço."
Regra aria-label: deve narrar a MESMA ação do contexto visual (ex.: botão × → aria-label="Fechar"). Coerência entre title e o botão que o contém.
Mudança mínima: RESEARCH confirma que os toasts de erro mais antigos JÁ seguem o padrão — não reescrever o que já oferece saída.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Varredura de toasts/erros + estados vazios (§26.3)</name>
  <read_first>
    - radar-goiania.html (`grep -n "toast(" radar-goiania.html`; blocos .empty nas linhas 880, 2711, 2761, 4824)
    - .planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-RESEARCH.md (Pitfall 3: legado já bom; §26.3)
    - .planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md
  </read_first>
  <action>
    1. Rodar `grep -n "toast(" radar-goiania.html` para enumerar as 46 chamadas. Para cada mensagem de ERRO: confirmar o padrão §26.3 (o que houve + o que fazer). Se faltar a saída, adicionar o próximo passo (ex.: "Confira os dígitos ou busque pelo endereço."). Mensagens de CONFIRMAÇÃO/sucesso (ex.: "Oportunidade salva") não precisam de saída — registrar como "avaliado-mantido". Mudança mínima: RESEARCH confirma que a maioria já passa.
    2. Auditar os 4 estados vazios (#emptyState 880, erro de busca 2711, sem resultado 2761, libs do mapa 4824): cada um deve oferecer um próximo passo tocável ou instrução clara (§26.8 "próximo passo em 1 toque"). NÃO alterar os `id`, classes `.empty`, handlers ou botões de ação (só o texto).
    Preencher a seção "Toasts/Erros + Estados vazios" da auditoria (uma linha por toast + 4 estados vazios). Rodar `npm test` (sem teste acoplado — deve permanecer verde).
  </action>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && npm test 2>&1 | grep -q "fail 0" && grep -q "Toasts/Erros\|Toasts" ".planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Seção "Toasts/Erros + Estados vazios" tem uma linha por chamada `toast(` retornada pelo grep + 4 linhas de estado vazio
    - Toda mensagem de erro auditada termina com uma saída (verbo de próximo passo: Tente/Confira/busque/recarregue/Verifique) OU está registrada como confirmação sem-saída-necessária
    - `grep -c "toast(" radar-goiania.html` inalterado (nenhuma chamada adicionada/removida)
    - `npm test` exits 0 (107 tests, fail 0)
  </acceptance_criteria>
  <done>Toasts de erro seguem §26.3; estados vazios oferecem próximo passo; seção da auditoria completa; teste verde.</done>
</task>

<task type="auto">
  <name>Task 2: Varredura de tooltips (title) + aria-label</name>
  <read_first>
    - radar-goiania.html (`grep -n 'title="' radar-goiania.html`; `grep -n 'aria-label="' radar-goiania.html`)
    - .planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-RESEARCH.md (§"Zona cinzenta": aria-label user-facing; title como tooltip)
    - .planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md
  </read_first>
  <action>
    1. Rodar `grep -n 'aria-label="' radar-goiania.html` (35 ocorrências). Para cada: confirmar que o texto narra a mesma ação do contexto visual do elemento (ex.: botão `×` → `aria-label="Fechar"`; botão de remover → `aria-label="Remover oportunidade"`). Aplicar §26 (verbo de ação quando é botão; acentuação; consistência com o glossário — "Oportunidades"/"Histórico"). NÃO alterar o VALOR de `aria-controls`/`aria-describedby`/`aria-labelledby` (são referências de `id`, não texto) nem `role=`.
    2. Rodar `grep -n 'title="' radar-goiania.html` (8 ocorrências: satélite, CSV, "aproximado", CND ~3452, deep-link). Cada tooltip é frase completa coerente com o botão que o contém — revisar em conjunto com o botão, não isolado. Aplicar §26 mantendo mudança mínima.
    Preencher a seção "Tooltips/aria-label" da auditoria (uma linha por aria-label + por title). Rodar `npm test`.
  </action>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && npm test 2>&1 | grep -q "fail 0" && grep -q "Tooltips" ".planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'aria-label="' radar-goiania.html` inalterado (35) e `grep -c 'title="' radar-goiania.html` inalterado (8)
    - Nenhum valor de `aria-controls`/`aria-describedby`/`role=` alterado (grep confirma que os `id` referenciados permanecem)
    - Seção "Tooltips/aria-label" da auditoria tem uma linha por aria-label e por title
    - Todo aria-label de botão-ícone contém verbo de ação (Fechar/Remover/Abrir/Copiar…)
    - `npm test` exits 0 (107 tests, fail 0)
  </acceptance_criteria>
  <done>aria-labels coerentes com o contexto visual e tooltips revisados; roles/referências ARIA intactos; teste verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edição de string → toast/DOM/leitor de tela | textos de erro/aria editados; risco = alterar role/aria-controls (estrutural) ou remover a saída de um erro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | radar-goiania.html (role=, aria-controls/aria-describedby/aria-labelledby, id, classes .empty, handlers) | mitigate | acceptance: atributos estruturais ARIA e `id` referenciados inalterados; só o texto de aria-label/title/toast editado; contagens de `toast(`/`aria-label`/`title` inalteradas |
| T-14-02 | Information Disclosure (LGPD) | mensagens de erro/aria | mitigate | nenhuma mensagem passa a exibir/induzir PII; grep confirma ausência de `dtnascimen` |
| T-14-04 | Tampering (XSS) | textos de toast/estado vazio em innerHTML | mitigate | edição só de texto literal; não introduzir HTML cru; chamadas esc() existentes preservadas |
</threat_model>

<verification>
- `npm test` exits 0 (107 tests, fail 0) ao fim de cada task
- Contagens de `toast(`, `aria-label="`, `title="` inalteradas
- Nenhum `role=`/`aria-controls`/`aria-describedby` alterado
- Seções Toasts/Erros + Estados vazios e Tooltips/aria-label preenchidas
</verification>

<success_criteria>
- Toda mensagem de erro oferece saída (§26.3)
- Estados vazios oferecem próximo passo
- aria-labels coerentes com o contexto visual; tooltips revisados
- Suíte 100% verde (107 testes)
</success_criteria>

<output>
Após completar, criar `.planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-03-SUMMARY.md`
</output>
