---
phase: 13-refino-visual-pinos-motion-descoberta
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - radar-goiania.html
  - tests/scores.test.mjs
  - tests/fixtures.mjs
autonomous: true
requirements: [VIS-01]
tags: [visual, status, css, respiro, pure-functions, tdd]

must_haves:
  truths:
    - "As 8 vars --status-*/--status-*-ink existem no :root, cada uma um ALIAS documentado de um hex ja existente (--lot/--lot-bright/--gold/--accent/--accent-ink/--muted) — zero hex novo introduzido no :root"
    - "Os 10 seletores da tabela de Refino do 13-UI-SPEC.md tem EXATAMENTE os valores 'depois' — nenhum ajuste fora da lista, nenhuma propriedade nao-listada tocada"
    - "statusDeUnidade(input) e uma funcao pura no RADAR_PURE que recebe {op:{score}}|score number|null|undefined e retorna 'bom'|'atencao'|'risco'|'semdado' pelas bandas 66/33 de scoreOportunidade (Fase 9) — nunca reimplementa os limiares, so os aplica"
    - "statusDeUnidade nunca lanca excecao para entrada ausente/malformada — score null/undefined/NaN sempre retorna 'semdado'"
  artifacts:
    - path: "radar-goiania.html"
      provides: ":root com 8 vars --status-*/--status-*-ink (aliases) + statusDeUnidade() dentro de RADAR_PURE_START..END + os 10 seletores refinados"
      contains: "--status-bom"
    - path: "tests/scores.test.mjs"
      provides: "testes de statusDeUnidade cobrindo bandas 66/33, score null/ausente, entrada em forma {op:{score}} e forma numero direto"
      contains: "statusDeUnidade"
    - path: "tests/fixtures.mjs"
      provides: "fixtures statusDeUnidadeCasos adicionadas ao objeto FIXTURES existente"
      contains: "statusDeUnidadeCasos"
  key_links:
    - from: "tests/scores.test.mjs"
      to: "radar-goiania.html (bloco RADAR_PURE)"
      via: "loadPureBlock via node:vm, mesmo padrao de tests/negocio.test.mjs/tests/doc.test.mjs"
      pattern: "RADAR_PURE_START"
    - from: "statusDeUnidade"
      to: "scoreOportunidade (bandas 66/33 ja existentes)"
      via: "mesmos limiares score>=66/score>=33, nunca reimplementados com numero magico duplicado"
      pattern: "function statusDeUnidade"
---

<objective>
Criar a base do sistema de status da Fase 13: (1) 8 variáveis `--status-*`/`--status-*-ink` no `:root`, cada uma um ALIAS de hex já existente (documentado inline, zero cor nova); (2) aplicar EXATAMENTE os 10 seletores de refino de respiro listados no `13-UI-SPEC.md` (padding/margin/gap/border), nada além; (3) a função pura `statusDeUnidade(input)` dentro do bloco `RADAR_PURE`, que mapeia score (ou `{op:{score}}`) para `'bom'|'atencao'|'risco'|'semdado'` usando as MESMAS bandas 66/33 de `scoreOportunidade` (Fase 9) — contrato único que as Waves 2/3 (pinos, legenda) vão consumir sem reimplementar limiares.

Purpose: VIS-01 exige que a cor fique reservada a status, derivada de hex já existentes, com a hierarquia de respiro aplicada de forma auditável (lista literal, sem "a critério"). `statusDeUnidade` é o contrato que a Wave 2 (pinos semânticos, PIN-01) vai consumir — sem essa função pura testada primeiro, o pino teria que reimplementar a lógica de bandas inline, duplicando os limiares 66/33 já travados em `scoreOportunidade`.
Output: `:root` com 8 novas vars documentadas; os 10 seletores da tabela de Refino com os valores "depois" aplicados byte-a-byte; `statusDeUnidade()` testada dentro de `RADAR_PURE`; suite completa (105 existentes + novos) passando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-refino-visual-pinos-motion-descoberta/13-CONTEXT.md
@.planning/phases/13-refino-visual-pinos-motion-descoberta/13-UI-SPEC.md

# Fonte de código (ler antes de editar)
@radar-goiania.html
@tests/scores.test.mjs
@tests/fixtures.mjs

<facts>
<!-- Verificado por leitura direta deste repo nesta sessão. -->

`:root` está nas linhas 24-49 de `radar-goiania.html`, fecha com `}` na linha 49. Contém
EXATAMENTE (linhas 25-37): `--ink:#141a1f`, `--paper:#e9e4d8`, `--paper-2:#f2eee4`,
`--line:#c3b9a3`, `--grid:#d8d0be`, `--lot:#2c5545`, `--lot-bright:#3f7a63`, `--accent:#b5451f`,
`--accent-2:#c9691f`, `--accent-ink:#8f3116`, `--gold:#a8842c`, `--muted:#57503f`, `--ok:#2c5545`.
As 8 vars novas desta plan (`--status-bom`, `--status-bom-ink`, `--status-atencao`,
`--status-atencao-ink`, `--status-risco`, `--status-risco-ink`, `--status-caixa`,
`--status-semdado`) entram IMEDIATAMENTE ANTES do `}` de fechamento (linha 49), cada uma como
`var(--x)` apontando pra uma var já existente acima — EXCETO `--status-atencao-ink:#7d621f`, que
é o MESMO hex literal já usado em `.score-op.media .score-num` (linha 321) e por isso é permitido
como literal (documentado como "já usado no app", não é cor nova).

Bloco RADAR_PURE: abre em `// RADAR_PURE_START` (linha 1033), fecha em `// RADAR_PURE_END` (linha
1888). `scoreOportunidade(myPm2, stats, flags)` (linha 1221-1253) já define as bandas: `score>=66`
→ "Boa oportunidade", `score>=33` → "Oportunidade média", senão → "Abaixo da mediana". Retorna
`{score, rotulo, porque}` ou `null` (quando `stats` insuficiente). `statusDeUnidade` desta plan
NÃO reimplementa `scoreOportunidade` — só aplica os MESMOS limiares 66/33 sobre um score já
calculado (ou sobre `null`/ausente → `'semdado'`).

Os 10 seletores de refino (tabela completa em `13-UI-SPEC.md`, seção "Refino de Respiro"),
linhas atuais confirmadas nesta sessão:
- linha 303: `.dvalor{...padding:16px 18px;margin-bottom:12px;border:2px solid var(--ink)...}`
  → depois: `padding:18px 20px;margin-bottom:16px` E a borda vira `1.5px solid var(--ink)`
  (remove a dupla-borda de mesma espessura com `.detail`)
- linha 309: `.dscores{...gap:8px;...margin-bottom:12px}` → `gap:10px;margin-bottom:16px`
- linha 328: `.dleitura{...padding:12px 14px;margin-bottom:12px}` → `padding:14px 16px;margin-bottom:16px`
- linha 310: `.score{...padding:8px 12px;...}` → `padding:10px 14px`
- linha 187: `.card{...padding aqui é parte de uma regra maior — confirmar padding atual antes de editar}`
  → alvo: `13px 14px` → `14px 16px` (ver UI-SPEC, medir o valor exato na linha antes de editar)
- linha 197: `.card .vals{margin-top:10px;padding-top:9px;...}` → `padding-top:10px` (margin-top
  mantido, só `padding-top` muda por essa tabela)
- linha 141: `.bldg-head{margin:8px 16px 2px;padding:10px 12px;...}` → `padding:12px 14px`
- linha 150: `.bldg-sumario{margin-top:8px;padding-top:8px;...}` → `margin-top:10px;padding-top:10px`
- linha 238: `.foot{padding:9px 22px;...}` → `padding:10px 22px`
- linha 337: `.maisopcoes .footbody.acts{padding-top:8px}` → `padding-top:10px`
- `.detail{...padding:16px 18px;...border:2px solid var(--ink)...}` (linha 272-274) → `padding:20px 22px`
  (borda de `.detail` PERMANECE `2px solid var(--ink)` — só a de `.dvalor` interna reduz p/ `1.5px`)

IMPORTANTE: a tabela do UI-SPEC também lista `.chooser` recebendo o MESMO padding refinado de
`.detail` (`20px 22px`) — como `.chooser` reusa a classe `.detail` (`<div class="detail chooser"
id="chooser">`, linha 890), este item já é coberto automaticamente pela mudança em `.detail` —
não precisa de regra CSS adicional, só confirmar no verify.

Nenhum outro seletor fora desta lista de 10 itens deve ser tocado nesta plan (ex.: `.wiz .wbody`,
`.searchpill`, `.dgrid .cell` — todos marcados "mantido" no UI-SPEC, incluindo os 3 "falsos
positivos" de borda dupla já documentados como não-duplicação real).

Padrão de teste puro já estabelecido em `tests/scores.test.mjs` (Fase 9, testa
`scoreOportunidade`/`scoreConfianca`/`leituraPratica`) — usa `loadPureBlock()` via `node:vm`,
mesmo padrão de `tests/negocio.test.mjs`/`tests/doc.test.mjs`/`tests/predio.test.mjs`. Esta plan
ACRESCENTA `statusDeUnidade` aos exports testados no MESMO arquivo `tests/scores.test.mjs` (não
cria um arquivo novo — a função pertence semanticamente aos scores, mesmo domínio já testado ali).

`tests/fixtures.mjs` tem o objeto `FIXTURES` já populado por fases anteriores (Fase 9/10/11/12) —
esta plan ACRESCENTA a chave `statusDeUnidadeCasos`, nunca substitui as existentes.

Suite atual: 105 testes, 0 falhas (`node --test "tests/*.test.mjs"`), confirmado nesta sessão.
</facts>

<interfaces>
<!-- Assinatura exata que as Waves 2/3 (13-02 pinos/legenda, 13-03 se aplicável) vao CHAMAR. -->

```js
// VIS-01/PIN-01: mapeia o status de EXIBICAO de uma unidade para 'bom'|'atencao'|'risco'|'semdado'
// — NUNCA recalcula score, so aplica as MESMAS bandas 66/33 de scoreOportunidade sobre um score
// ja calculado (ou ausente). 'caixa' NAO e retornado por esta funcao (a camada CAIXA e decidida
// pelo CALLER antes de chamar statusDeUnidade — ver Wave 2, toggleCaixa/caixaLayer separado).
//
// Aceita DUAS formas de entrada (Wave 2 pode ter o objeto __scores.op OU so o numero):
//   - statusDeUnidade({op:{score:78}})      -> 'bom'   (le score de dentro de op)
//   - statusDeUnidade(78)                    -> 'bom'   (numero direto)
//   - statusDeUnidade({op:null})             -> 'semdado'
//   - statusDeUnidade(null)                  -> 'semdado'
//   - statusDeUnidade(undefined)             -> 'semdado'
//   - statusDeUnidade(NaN)                   -> 'semdado' (nunca lanca excecao, nunca retorna NaN/undefined)
//
// Bandas (IDENTICAS a scoreOportunidade, nunca reimplementadas com numero magico duplicado):
//   score >= 66        -> 'bom'
//   33 <= score < 66    -> 'atencao'
//   score < 33          -> 'risco'
//   score ausente/invalido -> 'semdado'
function statusDeUnidade(input): 'bom'|'atencao'|'risco'|'semdado'
```

Contrato travado: esta função NUNCA retorna `'caixa'` — a distinção "imóvel da camada CAIXA" é
decidida pelo CALLER (Wave 2) antes/depois de chamar `statusDeUnidade`, pois a informação "é
CAIXA" não está disponível no shape `{op:{score}}`/`score` que esta função aceita (é uma
propriedade de outra fonte de dados, `window.CAIXA`, fora do bloco puro). Ver `13-UI-SPEC.md`
Componente 1 — a Wave 2 vai fazer algo como: `const status = ehCaixa ? 'caixa' :
statusDeUnidade(a.__scores)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — testes de statusDeUnidade + fixtures (falhando)</name>
  <files>tests/scores.test.mjs, tests/fixtures.mjs</files>
  <behavior>
    - statusDeUnidade({op:{score:78}}) === 'bom' (score >= 66)
    - statusDeUnidade({op:{score:66}}) === 'bom' (limite exato da banda, inclusive)
    - statusDeUnidade({op:{score:65}}) === 'atencao' (1 abaixo do limite bom)
    - statusDeUnidade({op:{score:50}}) === 'atencao' (33 <= score < 66)
    - statusDeUnidade({op:{score:33}}) === 'atencao' (limite exato da banda, inclusive)
    - statusDeUnidade({op:{score:32}}) === 'risco' (1 abaixo do limite atencao)
    - statusDeUnidade({op:{score:0}}) === 'risco'
    - statusDeUnidade({op:null}) === 'semdado' (score ainda nao calculado)
    - statusDeUnidade({op:undefined}) === 'semdado'
    - statusDeUnidade(null) === 'semdado'
    - statusDeUnidade(undefined) === 'semdado'
    - statusDeUnidade({}) === 'semdado' (objeto sem campo op)
    - statusDeUnidade(NaN) === 'semdado' (nunca propaga NaN)
    - statusDeUnidade(78) === 'bom' (forma numero direto, sem wrapper op)
    - statusDeUnidade(33) === 'atencao' (forma numero direto, limite inclusive)
    - statusDeUnidade(-5) === 'risco' (numero negativo tratado como risco, nunca excecao)
    - statusDeUnidade("78") === 'semdado' (string nao-numerica pura nunca e coagida silenciosamente — tratada como invalida)
    - nenhuma chamada de statusDeUnidade, para qualquer fixture, lanca excecao (assert try/catch ausente de erro em todos os casos)
  </behavior>
  <action>
Adicionar ao `tests/scores.test.mjs` existente (não criar arquivo novo) um novo `describe`/bloco de
`test()` para `statusDeUnidade`, seguindo o MESMO padrão de `loadPureBlock()`/sandbox já usado
pelos testes de `scoreOportunidade` no mesmo arquivo — adicionar `statusDeUnidade` à lista de
`globalThis.__exports` do sandbox.

Adicionar ao objeto `FIXTURES` em `tests/fixtures.mjs` a chave `statusDeUnidadeCasos`: array de
`{input, esperado}` cobrindo TODOS os casos do `<behavior>` acima (limites exatos 66/33/32/0,
formas `{op:{score}}` e número direto, `null`/`undefined`/`{}`/`NaN`/string não-numérica).

Rodar `node --test "tests/scores.test.mjs"` e confirmar falha (função ainda não existe no bloco
RADAR_PURE) — estado RED esperado ao final desta task.
  </action>
  <verify>
    <automated>node --test "tests/scores.test.mjs" 2>&1 | grep -qi "fail" && echo "OK: RED confirmado (statusDeUnidade ainda nao existe)"</automated>
  </verify>
  <done>tests/scores.test.mjs tem os testes de statusDeUnidade; tests/fixtures.mjs tem statusDeUnidadeCasos; rodar a suite falha porque a função ainda não existe no bloco RADAR_PURE.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implementar statusDeUnidade + aplicar sistema --status-* + refino de respiro</name>
  <files>radar-goiania.html</files>
  <action>
**Parte A — `:root` (8 vars novas, aliases documentados):**

Editar o `:root` (linhas 24-49) adicionando, imediatamente ANTES do `}` de fechamento (linha 49),
o bloco:

```css
    /* Fase 13 VIS-01/PIN-01: sistema de STATUS — cada var abaixo e um ALIAS de um hex JA
       EXISTENTE acima (documentado a direita). Nenhum hex novo introduzido, exceto
       --status-atencao-ink que reusa o hex LITERAL ja usado em .score-op.media .score-num
       (linha ~321) — nomeado aqui, mesmo valor. Cor de status SEMPRE acompanha texto/icone —
       nunca e o unico veiculo de significado (ver 13-UI-SPEC.md). */
    --status-bom:var(--lot);             /* = #2c5545, mesmo verde de .score-op.alta/.conf-alta */
    --status-bom-ink:var(--lot-bright);  /* = #3f7a63, ja existente — hover/estado ativo */
    --status-atencao:var(--gold);        /* = #a8842c, mesmo dourado de .score-op.media */
    --status-atencao-ink:#7d621f;        /* mesmo hex literal ja usado em .score-op.media .score-num */
    --status-risco:var(--accent);        /* = #b5451f, mesmo oxido de .score-op.baixa/erro */
    --status-risco-ink:var(--accent-ink);/* = #8f3116, ja existente */
    --status-caixa:var(--gold);          /* = #a8842c, MESMO hex do pino dourado CAIXA (toggleCaixa) */
    --status-semdado:var(--muted);       /* = #57503f, cinza neutro ja usado em texto secundario */
```

**Parte B — os 10 seletores de refino (valores EXATOS, nada além):**

1. `.detail` (linha ~272-274): `padding:16px 18px` → `padding:20px 22px` (manter `border:2px solid var(--ink)` inalterado)
2. `.dvalor` (linha ~303): `padding:16px 18px` → `padding:18px 20px`; `margin-bottom:12px` → `margin-bottom:16px`; `border:2px solid var(--ink)` → `border:1.5px solid var(--ink)`
3. `.dscores` (linha ~309): `gap:8px` → `gap:10px`; `margin-bottom:12px` → `margin-bottom:16px`
4. `.dleitura` (linha ~328): `padding:12px 14px` → `padding:14px 16px`; `margin-bottom:12px` → `margin-bottom:16px`
5. `.score` (linha ~310): `padding:8px 12px` → `padding:10px 14px`
6. `.card` (linha ~187): padding atual (confirmar valor lido antes de editar) → `padding:14px 16px`
7. `.card .vals` (linha ~197): `padding-top:9px` → `padding-top:10px` (margin-top:10px mantido)
8. `.bldg-head` (linha ~141): `padding:10px 12px` → `padding:12px 14px`
9. `.bldg-sumario` (linha ~150): `margin-top:8px;padding-top:8px` → `margin-top:10px;padding-top:10px`
10. `.foot` (linha ~238): `padding:9px 22px` → `padding:10px 22px`
11. `.maisopcoes .footbody.acts` (linha ~337): `padding-top:8px` → `padding-top:10px`

(Nota: são 11 declarações cobrindo os 10 seletores da tabela do UI-SPEC — `.detail`/`.dvalor`
contam como 2 seletores mas a tabela os lista separadamente; `.chooser` reusa `.detail` sem
regra própria.) NENHUM outro seletor é tocado — não ajustar `.wiz .wbody`, `.searchpill`,
`.dgrid .cell` (marcados "mantido"/"fora de escopo" no UI-SPEC).

**Parte C — `statusDeUnidade` dentro do bloco RADAR_PURE:**

Adicionar imediatamente ANTES do marcador `// RADAR_PURE_END` (linha 1888), com comentário
"Fase 13 VIS-01/PIN-01: mapeia status de EXIBICAO — nunca recalcula score, so aplica as MESMAS
bandas 66/33 de scoreOportunidade sobre um score ja disponivel":

```js
function statusDeUnidade(input){
  let score;
  if(input && typeof input==="object"){
    score = input.op ? input.op.score : undefined;
  } else {
    score = input;
  }
  if(typeof score!=="number" || !isFinite(score)) return "semdado";
  if(score>=66) return "bom";
  if(score>=33) return "atencao";
  return "risco";
}
```

Rodar `node --test "tests/scores.test.mjs"` até GREEN, depois `node --test "tests/*.test.mjs"`
para confirmar zero regressão nos 105 testes existentes.

**Parte D — verificação visual/CSS (sem teste automatizado de DOM, mas com assert por script):**
confirmar que NENHUM hex novo foi introduzido fora das 8 vars documentadas — rodar um diff manual
do bloco `:root` e conferir visualmente que todo valor à direita de `--status-*` é `var(--x)` ou o
hex `#7d621f` já documentado como literal preexistente.
  </action>
  <verify>
    <automated>node --test "tests/*.test.mjs" 2>&1 | tail -20</automated>
  </verify>
  <done>8 vars --status-*/--status-*-ink existem no :root, todas aliases de hex já existentes (exceto --status-atencao-ink, hex literal já usado em .score-op.media); os 10 seletores da tabela de refino têm os valores "depois" aplicados; statusDeUnidade() existe no bloco RADAR_PURE e passa 100% dos testes (105 existentes + novos, zero regressão).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| statusDeUnidade(input) -> valor de status exibido | entrada vem de `a.__scores`/score já calculado em memória (Fase 9/10) — nenhuma superfície de entrada externa nova; risco é lógico (banda errada), não de segurança |
| :root vars -> CSS aplicado no app todo | nenhuma interpolação de usuário, puramente estático — risco zero de injeção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | statusDeUnidade recebendo score fora do range 0-100 ou tipo inesperado (string, objeto malformado) | mitigate | guarda `typeof score!=="number" \|\| !isFinite(score)` retorna `'semdado'` antes de qualquer comparação — nunca propaga NaN/undefined para o CSS/UI consumidor |
| T-13-02 | Repudiation | refino de respiro tocando seletor fora da lista de 10, introduzindo regressão visual não rastreada | mitigate | lista literal fechada nesta plan (11 declarações, 10 seletores da tabela) — qualquer mudança fora dela é revertida no verify; diff de CSS revisável linha a linha |
| T-13-03 | Tampering | introdução acidental de hex novo fora dos aliases --status-* durante o refino | accept — mitigado por design | as 8 vars são 100% `var(--x)` exceto 1 hex literal já documentado (`#7d621f`, preexistente em `.score-op.media`) — nenhuma cor nova é introduzida, verificável por leitura direta do bloco `:root` |

**Sem ameaças de alta severidade.** Esta plan não toca rede, storage ou input de usuário — é CSS estático + função pura sem efeitos colaterais.
</threat_model>

<verification>
- `node --test "tests/*.test.mjs"` passa 100% (105 existentes + novos de statusDeUnidade)
- `:root` (linhas 24-~57 após edição) contém as 8 vars `--status-*`/`--status-*-ink`, cada uma documentada como alias
- Nenhum hex novo aparece no `:root` fora do único literal documentado (`#7d621f`, preexistente)
- Os 10 seletores da tabela têm exatamente os valores "depois" — `.dvalor` tem borda `1.5px` (não mais `2px` igual à `.detail` externa)
- `statusDeUnidade` nunca lança exceção para nenhuma entrada testada, nunca retorna `'caixa'`
</verification>

<success_criteria>
- VIS-01 (fundação): sistema `--status-*` existe, documentado, zero hex novo; refino de respiro aplicado exatamente conforme a tabela auditável do UI-SPEC
- `statusDeUnidade` pronta para a Wave 2 (13-02) consumir sem reabrir esta plan — contrato travado (aceita `{op:{score}}` ou número, nunca retorna `'caixa'`, sempre `'semdado'` em caso de dúvida)
- Zero regressão na suite de 105 testes existentes
</success_criteria>

<output>
Após completar, criar `.planning/phases/13-refino-visual-pinos-motion-descoberta/13-01-SUMMARY.md`.
Incluir: assinatura exata de `statusDeUnidade`, lista dos 10 seletores com valor antes/depois
confirmado por leitura pós-edição, resultado da suite completa (contagem pass/fail), e nota
explícita confirmando que nenhum hex novo foi introduzido fora dos aliases documentados.
</output>
