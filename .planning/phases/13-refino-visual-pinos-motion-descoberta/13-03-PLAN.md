---
phase: 13-refino-visual-pinos-motion-descoberta
plan: 03
type: execute
wave: 3
depends_on: ["13-01", "13-02"]
files_modified:
  - radar-goiania.html
autonomous: true
requirements: [DESC-01]
tags: [onboarding, descoberta, lei-da-tela, a11y, motion]

must_haves:
  truths:
    - "No 1o acesso (sem radar_onboard no localStorage), #onbOverlay aparece automaticamente com o cartao 1/3; Pular esta sempre visivel; Comecar no cartao 3 fecha e persiste radar_onboard=1 (try/catch, nunca lanca excecao se localStorage falhar)"
    - "Reabrir o app depois de fechar o onboarding (radar_onboard=1 no localStorage) NAO mostra o overlay automaticamente"
    - "initCoach()/radar_coach permanece 100% intocado — onboarding e coach sao independentes; se ambos disparariam na mesma sessao, onboarding (modal, bloqueia) aparece primeiro"
    - "Details 'O que o Radar faz' aparece no rodape do painel de busca, irmao de 'Fontes & metodologia', com 5 itens cada um com CTA funcional real (focar busca / reabrir onboarding no cartao certo / toggleCaixa)"
    - ".chooser recebe o padding refinado (20px 22px, herdado de .detail via reuso de classe) sem nenhuma mudanca estrutural de CTA"
    - "Onboarding e REDUCE-safe: abertura/troca de cartao usa mAnimate (fade), sob prefers-reduced-motion aparece/troca instantaneamente sem nunca ficar sem conteudo"
    - "Foco gerenciado: abrir onboarding foca .onb-skip; Esc fecha com prioridade maxima na cadeia global de Esc (antes de #cmpSheet/#negSheet/#captSheet); fechar retorna foco ao elemento anterior ou #searchPill"
  artifacts:
    - path: "radar-goiania.html"
      provides: "#onbOverlay (3 cartoes) + initOnboard/onbAvancar/onbFechar/onbAbrirDireto + #oQueFaz details + CSS .onb-*/.oqf-lista + entrada na cadeia de Esc + 44px em elementos tocaveis"
      contains: "onbOverlay"
  key_links:
    - from: "init() (linha ~4578)"
      to: "initOnboard()"
      via: "chamado no mesmo bloco de initCoach(), lê localStorage radar_onboard em try/catch"
      pattern: "initOnboard"
    - from: "#oQueFaz CTAs"
      to: "focusFirstField()/onbAbrirDireto(idx)/toggleCaixa()"
      via: "cada item da lista chama a ACAO REAL, nunca um texto explicativo generico"
      pattern: "onbAbrirDireto"
    - from: "document.addEventListener(\"keydown\",...) cadeia de Esc (linha 4606)"
      to: "onbFechar()"
      via: "novo PRIMEIRO check da cadeia — onboarding e a unica tela verdadeiramente modal-bloqueante"
      pattern: "onbFechar"
---

<objective>
Implementar a descoberta progressiva (DESC-01): onboarding de 3 cartões puláveis (`radar_onboard`, nunca reaparece, REDUCE-safe, foco gerenciado, 44px) com o copy literal travado no `13-UI-SPEC.md`; área discreta "O que o Radar faz" como segunda `<details class="foot">` no rodapé do painel de busca, com CTA funcional real por item (nunca texto explicativo genérico); auditoria/refino do `.chooser` (herda o padding refinado de `.detail` via reuso de classe, zero mudança estrutural de CTA — classificado como conforme por design).

Purpose: DESC-01 exige que a tela inicial revele o poder do app progressivamente — o onboarding ensina em 3 passos, "O que o Radar faz" fica sempre acessível sem competir com o menu principal, e a lei da tela é varrida na última superfície pendente (`.chooser`). Esta plan fecha o escopo da Fase 13 assumindo o sistema `--status-*`/refino de respiro (13-01) e os pinos/motion (13-02) já existentes — não depende de código novo dessas plans além de reusar `mAnimate`/`REDUCE` já globais.
Output: `#onbOverlay` funcional com persistência; `#oQueFaz` com 5 CTAs reais; `.chooser` confirmado conforme (herda padding, zero mudança de CTA); cadeia de Esc atualizada com onboarding no topo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-refino-visual-pinos-motion-descoberta/13-CONTEXT.md
@.planning/phases/13-refino-visual-pinos-motion-descoberta/13-UI-SPEC.md
@.planning/phases/13-refino-visual-pinos-motion-descoberta/13-01-SUMMARY.md
@.planning/phases/13-refino-visual-pinos-motion-descoberta/13-02-SUMMARY.md

# Fonte de código (ler antes de editar)
@radar-goiania.html

<facts>
<!-- Verificado por leitura direta deste repo nesta sessão. -->

**`initCoach()`/`dismissCoach()`** (linhas 4561-4569): usa `localStorage.getItem/setItem
("radar_coach")` em `try/catch`, mostra `#coachMark` via `style.display="inline-flex"`. Esta plan
NÃO toca essas 2 funções — cria `initOnboard()`/`onbFechar()` como pares INDEPENDENTES, mesma
convenção (`radar_`, `try/catch`, fail-open se storage falhar).

**Bloco de `init()`** (linhas 4571-4590): `document.body.dataset.view="mapa";setMode(MODE);
renderSavedBlocks(); if(!window.L||!window.proj4){...}else{initMap();setView("mapa");
initCoach();loadBairros();loadBairroPolys();initCaixa(); /* deep-link ?insc= */ ...}`. Esta plan
insere `initOnboard();` no MESMO bloco `else{...}`, IMEDIATAMENTE ANTES ou DEPOIS de
`initCoach()` (ordem não crítica — onboarding é modal e bloqueia interação, coach só aparece
condicionalmente depois via `setView`/toque). Posição recomendada: depois de `initCoach()`, antes
de `loadBairros()` (não depende de dados de bairro).

**`.foot` (rodapé "Fontes & metodologia")** (linhas 830-833):
```html
<details class="foot">
  <summary>Fontes &amp; metodologia</summary>
  <div class="footbody">...</div>
</details>
```
Está DENTRO de `<aside>` (painel de busca), como último elemento antes do fechamento de `</aside>`
(linha 834). `#oQueFaz` desta plan é uma SEGUNDA `<details class="foot">` IRMÃ, inserida
imediatamente ANTES desta (ou depois — UI-SPEC não especifica ordem crítica; usar ANTES, para
"O que o Radar faz" aparecer primeiro como entrada de descoberta, e "Fontes & metodologia" como
a mais técnica/final).

**`focusFirstField()`**: usado em `onclick="setView('busca');focusFirstField();dismissCoach()"`
do `#searchPill` (linha 839) — função já existe no app, foca o primeiro campo de busca. O CTA
"Buscar agora →" do item 1 de `#oQueFaz` REUSA esta função diretamente.

**`toggleCaixa()`** (linha 4497): já existe, alterna a camada CAIXA no mapa. O CTA "Ver no mapa
→" do item 5 de `#oQueFaz` chama `toggleCaixa()` diretamente — zero função nova.

**Cadeia de Esc global** (linhas 4606-4632): `document.addEventListener("keydown",e=>{if
(e.key!=="Escape")return; const cmps=...;if(cmps&&!cmps.hidden){fecharComparacao();return;}
const ns=...;if(ns&&!ns.hidden){fecharNeg();return;} const cs=...;if(cs&&!cs.hidden)
{fecharCaptacao();return;} const caixaList=...; if(...){closeCaixaList();return;}
if(!document.getElementById("correctMenu").hidden){closeCorrectMenu();return;} const ambig=...;
if(ambig&&!ambig.hidden){ambig.hidden=true;return;} if(!document.getElementById("calc").hidden)
fecharCustos(); else if(!document.getElementById("wiz").hidden)fecharLaudo(); else if
(!document.getElementById("laudoView").hidden)fecharLaudoView(); else if
(document.getElementById("chooser").classList.contains("show"))closeChooser(); else if
(document.body.dataset.view==="busca"&&!isMobile()){setView("mapa");...} else
closeDetail();});`. Esta plan insere o check de `#onbOverlay` como o PRIMEIRO de todos (antes até
de `#cmpSheet`) — onboarding é a ÚNICA tela verdadeiramente modal-bloqueante (cobre tudo, inclusive
sheets), conforme UI-SPEC: "é a única tela verdadeiramente modal-bloqueante da experiência".

**`mAnimate(el, keyframes, options)`** (linha 1913-1916): já existe, retorna `null` se `REDUCE`
ou Motion indisponível — quem chama SEMPRE aplica a mudança de estado real de forma incondicional
(ex.: `.classList.add("show")`) e usa `mAnimate` só para o efeito visual extra. Onboarding usa
este padrão: mostrar/trocar cartão sempre funciona, `mAnimate` só adiciona o fade quando possível.

**`esc()`** (linha 1022): esta plan NÃO precisa chamá-lo para os textos do onboarding/"O que o
Radar faz" (são todos strings estáticas pt-BR, sem interpolação de dado do usuário) — confirmado
pelo UI-SPEC ("textos ESTÁTICOS pt-BR, sem interpolação").

**44px**: padrão já usado em `.pillclose`/`.score` (`min-height:44px`) — aplicar aos elementos
tocáveis novos (`.onb-skip`, `.onb-next`).

**z-index existentes**: `.detail`=500, `.pino-legenda`/`.coachmark`=390 (13-02),
`.wiz`/`#calc`=2000+ (sheets de documento). `#onbOverlay` usa `z-index:1000` (UI-SPEC) — acima do
`.detail`(500) mas abaixo dos sheets de documento (2000+), já que onboarding só aparece na
PRIMEIRA tela do app, nunca coexiste de fato com um sheet de documento aberto (mas o CSS
`z-index` é definido de forma defensiva, sem depender de timing).

**Cor `#fff` como texto sobre CTA primária JÁ é padrão preexistente** (confirmado por grep):
`.go{background:var(--accent);color:#fff;...}` (linha 170) e `.detail .acts a.primary,.detail
.acts button.primary{background:var(--accent);...;color:#fff}` (linha 632) — `color:#fff` NÃO é
cor nova introduzida por esta plan, é o mesmo par contraste/fundo já usado em toda CTA primária
do app. `.onb-next` reusa exatamente esse par.
</facts>

<interfaces>
<!-- HTML/CSS/JS exatos do 13-UI-SPEC.md Componente 3/4 — copiar literalmente, não reinterpretar copy. -->

```html
<!-- Inserir dentro de <body>, como filho direto (fora de .mapwrap/<aside> — precisa aparecer
     independente de document.body.dataset.view), próximo a .toast/#loading -->
<div class="onb" id="onbOverlay" hidden role="dialog" aria-modal="true" aria-label="Como usar o Radar Fundiário">
  <div class="onb-card">
    <button type="button" class="onb-skip" onclick="onbFechar()">Pular</button>
    <div class="onb-dots" aria-hidden="true"><span class="on"></span><span></span><span></span></div>
    <div class="onb-body" id="onbBody">
      <h2 id="onbTitulo"></h2>
      <p id="onbTexto"></p>
    </div>
    <button type="button" class="onb-next" id="onbNext" onclick="onbAvancar()">Próximo</button>
  </div>
</div>
```

```css
.onb{position:fixed;inset:0;z-index:1000;background:rgba(20,26,31,.55);display:none;align-items:center;justify-content:center;padding:16px}
.onb.show{display:flex}
.onb-card{background:var(--paper-2);border:2px solid var(--ink);border-radius:3px;padding:24px 22px;max-width:380px;width:100%;position:relative;box-shadow:0 8px 24px rgba(20,26,31,.3)}
.onb-skip{position:absolute;top:10px;right:10px;min-height:44px;min-width:44px;padding:0 12px;border:0;background:transparent;color:var(--muted);font:600 12.5px/1 "IBM Plex Sans";cursor:pointer}
.onb-skip:hover{color:var(--accent)}
.onb-dots{display:flex;gap:6px;justify-content:center;margin-bottom:14px}
.onb-dots span{width:7px;height:7px;border-radius:50%;background:var(--line)}
.onb-dots span.on{background:var(--ink)}
.onb-body h2{font:800 20px/1.2 "IBM Plex Sans";color:var(--ink);margin:0 0 10px}
.onb-body p{font:500 14px/1.5 "IBM Plex Sans";color:var(--muted);margin:0}
.onb-next{width:100%;margin-top:20px;min-height:44px;border:0;background:var(--accent);color:#fff;font:700 15px/1 "IBM Plex Sans";border-radius:2px;cursor:pointer}
.onb-next:hover{background:var(--accent-ink)}
@media(max-width:820px){.onb-card{max-width:calc(100vw - 32px)}}
```

```html
<!-- Inserir dentro de <aside>, imediatamente ANTES de <details class="foot"> ("Fontes & metodologia") -->
<details class="foot" id="oQueFaz">
  <summary>O que o Radar faz</summary>
  <div class="footbody">
    <ul class="oqf-lista">
      <li><b>Busca unificada</b> — endereço, quadra/lote, prédio ou inscrição numa caixa só. <button type="button" onclick="focusFirstField()">Buscar agora →</button></li>
      <li><b>Valor e oportunidade</b> — faixa estimada, score de oportunidade e leitura em linguagem simples. <button type="button" onclick="onbAbrirDireto(1)">Ver como →</button></li>
      <li><b>Documentos prontos</b> — ficha, relatório ou laudo em PDF, minutas de proposta/contrato. <button type="button" onclick="onbAbrirDireto(2)">Ver como →</button></li>
      <li><b>Ação comercial</b> — mensagens prontas pro WhatsApp, modo captação, salvar oportunidades. <button type="button" onclick="onbAbrirDireto(2)">Ver como →</button></li>
      <li><b>Oportunidades da Caixa</b> — imóveis à venda da Caixa, plotados no mapa. <button type="button" onclick="toggleCaixa()">Ver no mapa →</button></li>
    </ul>
  </div>
</details>
```

```css
.oqf-lista{list-style:none;margin:0;padding:0;display:flex;flex-direction:column;gap:10px}
.oqf-lista li{font:500 12px/1.5 "IBM Plex Sans";color:var(--muted)}
.oqf-lista li b{color:var(--ink);font-weight:600}
.oqf-lista button{display:inline;border:0;background:transparent;color:var(--accent);font:600 12px/1 "IBM Plex Sans";cursor:pointer;padding:2px 4px;margin-left:2px;text-decoration:underline;text-underline-offset:2px}
```

```js
// Conteúdo dos 3 cartões (copy literal, travado — não reformular)
const ONB_CARDS=[
  {titulo:"Busque qualquer imóvel", texto:"Digite endereço, quadra/lote, nome do prédio ou inscrição — numa caixa só. O Radar entende o que você quer dizer.", cta:"Próximo"},
  {titulo:"Veja valor e oportunidade", texto:"Cada imóvel mostra a faixa de valor estimado, o score de oportunidade e uma leitura em linguagem simples — sem jargão técnico.", cta:"Próximo"},
  {titulo:"Gere documentos e capte", texto:"Gere ficha, relatório ou laudo em PDF, copie mensagens prontas pro WhatsApp e monte a proposta — tudo pelo Radar.", cta:"Começar"},
];
let onbIdx=0, onbLastFocus=null;
function initOnboard(){
  let seen="1"; try{seen=localStorage.getItem("radar_onboard");}catch(e){}
  if(!seen) onbAbrir(0);
}
function onbRender(){
  const c=ONB_CARDS[onbIdx];
  document.getElementById("onbTitulo").textContent=c.titulo;
  document.getElementById("onbTexto").textContent=c.texto;
  document.getElementById("onbNext").textContent=c.cta;
  document.querySelectorAll(".onb-dots span").forEach((d,i)=>d.classList.toggle("on",i===onbIdx));
}
function onbAbrir(idx){
  onbIdx=idx||0;
  onbLastFocus=document.activeElement;
  const ov=document.getElementById("onbOverlay");
  ov.hidden=false;
  onbRender();
  mAnimate(ov,{opacity:[0,1]},{duration:0.18});
  const skip=document.querySelector(".onb-skip"); if(skip) skip.focus();
}
function onbAvancar(){
  if(onbIdx>=ONB_CARDS.length-1){onbFechar();return;}
  onbIdx++; onbRender();
}
function onbFechar(){
  try{localStorage.setItem("radar_onboard","1");}catch(e){}
  const ov=document.getElementById("onbOverlay");
  ov.hidden=true;
  if(onbLastFocus&&document.body.contains(onbLastFocus)) onbLastFocus.focus();
  else { const p=document.getElementById("searchPill"); if(p) p.focus(); }
}
// "O que o Radar faz" -> reabre o onboarding NO CARTÃO relevante, sem checar radar_onboard
// (reentrada manual — nunca conta como "1ª exibição")
function onbAbrirDireto(idx){ onbAbrir(idx); }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Onboarding — HTML/CSS/JS + persistência + foco + Esc</name>
  <files>radar-goiania.html</files>
  <action>
**1. HTML `#onbOverlay`**: inserir dentro de `<body>`, como filho direto (não dentro de
`.mapwrap`/`<aside>` — precisa aparecer independente de `document.body.dataset.view`), próximo a
outros overlays globais (ex.: antes de `.toast`/`#loading`, linha ~901-904). Copiar literalmente
o bloco HTML de `<interfaces>`.

**2. CSS `.onb`/`.onb-card`/`.onb-skip`/`.onb-dots`/`.onb-body`/`.onb-next`**: adicionar ao
`<style>`, em bloco próprio comentado "Fase 13 DESC-01: onboarding 3 cartões". Copiar
literalmente da `<interfaces>` (inclui a regra mobile `≤820px`).

**3. JS**: adicionar `ONB_CARDS`, `onbIdx`/`onbLastFocus`, `initOnboard()`, `onbRender()`,
`onbAbrir(idx)`, `onbAvancar()`, `onbFechar()`, `onbAbrirDireto(idx)` — copiar literalmente da
`<interfaces>`, posicionar próximo a `initCoach()`/`dismissCoach()` (mesma seção "coach-mark de
1ª vez" pode ser renomeada/expandida para incluir onboarding, ou criar seção nova imediatamente
adjacente).

**4. Chamar `initOnboard()`** dentro do bloco `else{...}` de `init()` (linha ~4578), junto a
`initCoach()` — ordem sugerida: `initMap();setView("mapa");initCoach();initOnboard();
loadBairros();...` (onboarding não depende de dados de bairro, pode vir logo após o coach).

**5. Cadeia de Esc**: editar o listener `document.addEventListener("keydown",e=>{if
(e.key!=="Escape")return; ...})` (linha 4606) inserindo o check de `#onbOverlay` como o
PRIMEIRO de todos (antes até de `#cmpSheet`):
```js
const onb=document.getElementById("onbOverlay");
if(onb&&!onb.hidden){onbFechar();return;}
```
Comentar: "Fase 13 DESC-01: onboarding é o PRIMEIRO check — única tela verdadeiramente
modal-bloqueante, prioridade máxima."

**6. Confirmar `initCoach()` intocado**: nenhuma edição nas linhas 4561-4569 além da adição da
chamada `initOnboard()` no bloco de `init()` (item 4) — as duas funções são independentes.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('radar-goiania.html','utf8');const checks=['onbOverlay','ONB_CARDS','initOnboard','onbAvancar','onbFechar','onbAbrirDireto','radar_onboard'];const missing=checks.filter(c=>!s.includes(c));if(missing.length){console.error('MISSING:',missing);process.exit(1);}console.log('OK: onboarding presente');"</automated>
  </verify>
  <done>Onboarding aparece no 1º acesso (sem radar_onboard), 3 cartões navegáveis com copy literal do UI-SPEC, Pular sempre visível, Começar no 3º fecha e persiste; reabrir o app não mostra mais automaticamente; Esc fecha com prioridade máxima; initCoach()/radar_coach inalterados.</done>
</task>

<task type="auto">
  <name>Task 2: "O que o Radar faz" + auditoria do .chooser (lei da tela)</name>
  <files>radar-goiania.html</files>
  <action>
**1. HTML `#oQueFaz`**: inserir dentro de `<aside>`, IMEDIATAMENTE ANTES de `<details
class="foot">` ("Fontes & metodologia", linha ~830). Copiar literalmente o bloco HTML de
`<interfaces>` (5 itens, cada um com seu CTA real).

**2. CSS `.oqf-lista`**: adicionar ao `<style>`, próximo às regras de `.foot`/`.footbody`.
Copiar literalmente da `<interfaces>`.

**3. Confirmar CTAs funcionais** (não placeholders): "Buscar agora →" chama `focusFirstField()`
(já existe); "Ver como →" (itens 2/3/4) chama `onbAbrirDireto(1)`/`onbAbrirDireto(2)`/
`onbAbrirDireto(2)` respectivamente (reabre o onboarding no cartão relevante, SEM checar
`radar_onboard` — reentrada manual via `onbAbrir(idx)`, que a Task 1 já implementa); "Ver no mapa
→" chama `toggleCaixa()` (já existe). Nenhuma função nova precisa ser criada além de
`onbAbrirDireto` (já coberta na Task 1).

**4. Auditoria do `.chooser`**: confirmar por leitura que `.chooser` (`<div class="detail
chooser" id="chooser">`, linha 890) já reusa a classe `.detail` — o refino de padding aplicado em
13-01 (`.detail{padding:20px 22px}`) já se propaga automaticamente para `.chooser` (CSS cascade,
mesma classe). NENHUMA regra CSS nova é necessária para este item — apenas confirmar visualmente
(DevTools ou leitura do computed style) que `#chooser` mede `padding:20px 22px` após a mudança de
13-01. Documentar no SUMMARY que a auditoria concluiu "conforme por design" (é um seletor de
unidades, cada `.chrow` é auto-suficiente como ação primária da própria linha — sem CTA de tela
separada necessária, conforme UI-SPEC Componente 5).

**5. Confirmar zero hex novo**: `.onb-*`/`.oqf-lista` usam exclusivamente `var(--ink)`/
`var(--paper-2)`/`var(--muted)`/`var(--line)`/`var(--accent)`/`var(--accent-ink)` e `#fff` — este
último já é padrão preexistente (`.go`/`.detail .acts button.primary`, linhas 170/632, mesmo par
`background:var(--accent);color:#fff`), não é cor nova. Nenhum hex literal novo é introduzido por
esta plan.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('radar-goiania.html','utf8');const checks=['oQueFaz','oqf-lista','onbAbrirDireto','focusFirstField()','toggleCaixa()'];const missing=checks.filter(c=>!s.includes(c));if(missing.length){console.error('MISSING:',missing);process.exit(1);}console.log('OK: O que o Radar faz presente');"</automated>
  </verify>
  <done>#oQueFaz aparece no rodapé do painel de busca, irmão de "Fontes & metodologia", com 5 CTAs funcionais reais; .chooser confirmado herdando o padding refinado de .detail (20px 22px) sem mudança estrutural; zero hex novo introduzido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage (radar_onboard) -> initOnboard() | mesma superfície já usada por radar_coach/radar_prof — leitura/escrita em try/catch, fail-open se storage indisponível (modo privado/quota) |
| Textos do onboarding/"O que o Radar faz" -> DOM | strings 100% estáticas pt-BR, sem interpolação de dado do usuário — risco de XSS zero, não precisa de esc() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Denial of Service (percebido) | onboarding travando o usuário se localStorage falhar ao escrever (modo privado/quota) e o overlay reaparecer a cada load | mitigate | `onbFechar()` já esconde o overlay (`ov.hidden=true`) INCONDICIONALMENTE, independente do resultado do `try/catch` de `setItem` — falha de escrita é silenciosa mas nunca bloqueia o uso da sessão atual; a única consequência é reaparecer na PRÓXIMA sessão, documentado como aceitável (mesma filosofia da Fase 10) |
| T-13-09 | Tampering | onboarding e coach (`#coachMark`) competindo por z-index/foco na mesma sessão de 1º acesso | mitigate | `#onbOverlay` (`z-index:1000`) sempre acima de `#coachMark` (`z-index:390` via `.coachmark`); coach só é exibido condicionalmente via interação do mapa, nunca automaticamente no `init()` antes do onboarding fechar (ordem de chamada em `init()` documentada) |
| T-13-10 | Elevation of Privilege (escopo) | cadeia de Esc quebrando fechamento de sheets existentes ao inserir o check de onboarding no topo | accept — mitigado por design | onboarding só fica `!hidden` na primeira tela do app (antes de qualquer sheet poder ter sido aberto) — o novo check no topo da cadeia nunca intercepta Esc de um sheet real, pois os dois estados são mutuamente exclusivos na prática (onboarding fecha ANTES do usuário conseguir abrir qualquer sheet) |

**Sem ameaças de alta severidade.** Onboarding/"O que o Radar faz" operam 100% client-side sobre textos estáticos, sem requisição de rede.
</threat_model>

<verification>
- 1º acesso (sem `radar_onboard` no localStorage): onboarding aparece automaticamente, 3 cartões navegáveis, "Pular" sempre visível, "Começar" no 3º fecha e persiste — reabrir o app não mostra mais o onboarding automaticamente
- `prefers-reduced-motion` ativo: onboarding abre/troca de cartão sem fade (instantâneo), nunca sem conteúdo
- Esc fecha o onboarding com prioridade máxima (testar com `#cmpSheet`/`#captSheet` fechados — onboarding intercepta primeiro)
- Foco: abrir onboarding foca `.onb-skip`; fechar retorna o foco ao elemento anterior ou a `#searchPill`
- "O que o Radar faz" aparece como `<details>` no rodapé do painel de busca, ao lado de "Fontes & metodologia"; cada item tem seu próprio CTA funcional (focar busca / reabrir onboarding no cartão certo / abrir camada Caixa)
- `.chooser` mede `padding:20px 22px` em DevTools (herdado de `.detail`), sem mudança estrutural de CTA
- Nenhum elemento tocável novo mede menos de 44px (`.onb-skip`, `.onb-next`), exceto `.oqf-lista button` (link inline, documentado como exceção aceita no UI-SPEC)
- `node --test "tests/*.test.mjs"` continua passando 100% (esta plan não altera RADAR_PURE, zero regressão esperada)
</verification>

<success_criteria>
- DESC-01 cumprido: onboarding ≤3 telas puláveis com persistência correta; área discreta "O que o Radar faz" com CTAs reais; `.chooser` auditado e conforme; lei da tela varrida em todas as superfícies pendentes da Fase 13
- Fase 13 completa: VIS-01 (13-01) + PIN-01/MOT-01 (13-02) + DESC-01 (13-03) — os 4 success criteria do ROADMAP para a Fase 13 cumpridos
- Zero regressão na suite de testes (105+ existentes)
</success_criteria>

<output>
Após completar, criar `.planning/phases/13-refino-visual-pinos-motion-descoberta/13-03-SUMMARY.md`.
Incluir: confirmação da posição exata de inserção de `#onbOverlay` no DOM, resultado da auditoria
do `.chooser` (padding computado confirmado), lista dos 5 CTAs de "O que o Radar faz" com a
função exata que cada um chama, e uma nota final consolidando que os 4 success criteria da Fase
13 (ROADMAP) foram cumpridos pelas 3 plans (13-01/02/03).
</output>
