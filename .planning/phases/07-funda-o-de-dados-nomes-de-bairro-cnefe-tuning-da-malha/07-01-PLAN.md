---
phase: 07-funda-o-de-dados-nomes-de-bairro-cnefe-tuning-da-malha
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gerar-bairros.py
  - check-bairros-geojson.py
  - bairros-goiania.json
  - bairros-goiania.report.md
autonomous: true
requirements: [NOMES-01, NOMES-02, NOMES-03]
tags: [gis, data-reconciliation, arcgis, spatial-join, build-script]

must_haves:
  truths:
    - "Os nomes de bairro exibidos vêm da fonte autoritativa (layer 3 nmbairro/cdbairro) reconciliada por spatial join POST, não do nm_bai cru da layer 2"
    - "O novo bairros-goiania.json tem geometria e contagem de features BYTE-IDÊNTICAS ao anterior — só o campo de nome muda"
    - "Um relatório de diff antes→depois por polígono é gerado, com os multi-candidatos destacados para conferência"
    - "Glebas sem parcela intersectada recebem rótulo genérico explícito; nenhum nome fiscal é forçado"
    - "O drill (fitBounds/getBounds) continua funcionando — a mudança é só de string de exibição"
  artifacts:
    - path: "gerar-bairros.py"
      provides: "Step 4.5 reconciliação por spatial join POST à layer 3 + tie-break assistido por nome + emissão de diff no report"
      contains: "esriSpatialRelIntersects"
    - path: "check-bairros-geojson.py"
      provides: "Modo --assert-geometry-identical que compara geometria+contagem contra o json anterior"
      contains: "assert-geometry-identical"
    - path: "bairros-goiania.json"
      provides: "Bairros com nome reconciliado (geometria inalterada, 1206 features)"
      contains: "nm_bai"
    - path: "bairros-goiania.report.md"
      provides: "Seção de reconciliação: recuperados-de-branco, nomes-mudados, não-resolvidos, tabela de diff, multi-candidatos"
      contains: "Reconciliação"
  key_links:
    - from: "gerar-bairros.py Step 4.5"
      to: "layer 3 (LAYER3) via POST"
      via: "spatial join esriSpatialRelIntersects em coordenadas UTM 31982"
      pattern: "esriSpatialRelIntersects"
    - from: "reconciliação"
      to: "bairros-goiania.json committed"
      via: "injeção de nome por `id` no json simplificado existente (geometria intacta)"
      pattern: "nm_bai"
---

<objective>
Corrigir os nomes de bairro exibidos no app reconciliando-os contra a fonte autoritativa (layer 3 `nmbairro`/`cdbairro`) via spatial join POST, e regenerar `bairros-goiania.json` com nomes corretos mantendo geometria e contagem de features BYTE-IDÊNTICAS. Emitir um relatório de diff antes→depois por polígono e um assert automatizado de geometria inalterada.

Purpose: O `nm_bai` cru da layer 2 tem erros e mojibake (`Petr�polis`), e match por string falha 99,5% (medido). A reconciliação espacial dá o nome confiável sem tocar na geometria — desbloqueia a confiança no dado que o resto do v2.1 depende. (NOMES-01/02/03)
Output: `gerar-bairros.py` evoluído (Step 4.5), `check-bairros-geojson.py` com `--assert-geometry-identical`, `bairros-goiania.json` regenerado (geometria idêntica), `bairros-goiania.report.md` com seção de reconciliação.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-funda-o-de-dados-nomes-de-bairro-cnefe-tuning-da-malha/07-CONTEXT.md
@.planning/research/v2.1/DATA-NAMES.md

# Fontes de código (ler antes de editar)
@gerar-bairros.py
@check-bairros-geojson.py

<facts>
<!-- Verificado ao vivo (DATA-NAMES.md) e por inspeção direta dos arquivos deste repo. -->

Estrutura ATUAL do bairros-goiania.json committed (inspecionado): FeatureCollection,
**1206 features**, cada `properties` = exatamente `{"id": "...", "nm_bai": "..."}`.
É o artefato JÁ SIMPLIFICADO por mapshaper (734KB / ~166KB gz) — NÃO é o raw de 5.3MB.

Endpoints (constantes já em gerar-bairros.py):
- LAYER2 = .../Feature_Base/MapServer/2/query  (Divisas de Bairro: id, nm_bai, geometry.rings em UTM 31982)
- LAYER3 = .../Feature_Base/MapServer/3/query  (Cadastro Imobiliário: cdbairro, nmbairro — fonte autoritativa, 709 setores, zero brancos)

QUIRK NOVO (verificado ao vivo, DATA-NAMES.md 1.3): spatial query com geometria de
polígono DEVE usar POST — GET dá 404/414 intermitente para polígonos realistas (30+ vértices).
O helper `http()` atual só faz GET (urlencode na URL). Precisa de um caminho POST (form-encoded body).

O spatial join roda em UTM 31982 CRU (os `geometry.rings` da resposta da layer 2, ANTES da
reprojeção pra WGS84). Reprojetar primeiro e mandar como inSR=31982 corrompe silenciosamente o join.
</facts>

<interfaces>
<!-- Helpers já existentes em gerar-bairros.py que o executor deve reusar, não re-derivar. -->

def http(url, params=None, tries=3)  # GET com backoff 429/503; reusar o padrão de retry
def arcgis(url, params)              # http() + f=json + raise em "error"
def fetch_all_bairros(page_size=500) # pagina layer 2 via resultOffset; retorna (features, dup_business_ids)
                                     # cada feature: {"attributes":{"OBJECTID","id","nm_bai"}, "geometry":{"rings":[...]}}
def write_report(total, named, unnamed, cdbairro_count, dup_business_ids=None)  # ESTENDER com seção de reconciliação
PROJ4_31982 = "+proj=utm +zone=22 +south ..."  # verbatim do app; NUNCA re-derivar

# Algoritmo autoritativo (DATA-NAMES.md 1.3) — implementar exatamente:
# para cada polígono da layer 2 (rings em UTM 31982):
#   1. POST distinct-value -> candidates = [(cdbairro, nmbairro), ...]
#   2. 0 candidatos -> não-resolvido (terra vaga/gleba sem parcela) -> NÃO fabrica nome
#   3. 1 candidato   -> winner = ele
#   4. 2+ candidatos:
#        a. POST returnCountOnly por candidato -> contagem de parcelas
#        b. se norm(nm_bai) é substring de EXATAMENTE 1 candidato (ou vice-versa) -> winner = esse (tie-break por nome)
#        c. senão -> winner = maior contagem (maioria)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar spatial join POST + reconciliação de nomes em gerar-bairros.py (Step 4.5)</name>
  <read_first>gerar-bairros.py (todo — helpers http/arcgis/fetch_all_bairros, main, PROJ4_31982); DATA-NAMES.md 1.3 e 1.4 (algoritmo + diff ao script)</read_first>
  <files>gerar-bairros.py</files>
  <action>
Adicionar a capacidade de POST e um Step 4.5 de reconciliação. Concretamente:

1. **Helper POST** — adicionar `http_post(url, params, tries=3)` (irmão do `http()` existente) que envia `urllib.parse.urlencode(params).encode()` como body com `urllib.request.Request(url, data=..., headers={**UA, "Content-Type":"application/x-www-form-urlencoded"})` e o MESMO backoff 429/503 do `http()`. Adicionar `arcgis_post(url, params)` (irmão de `arcgis()`: injeta `f=json`, decodifica, raise em `"error"`). NÃO alterar o `http()`/`arcgis()` GET existentes (a paginação da layer 2 continua GET).

2. **Função de normalização** `norm(s)` — accent-strip (`unicodedata.normalize('NFKD',...)`) + uppercase + collapse de espaços, para o tie-break de nome.

3. **Função `reconcile_name(rings_utm, nm_bai_original)`** que implementa o algoritmo autoritativo (ver <interfaces>):
   - POST `LAYER3` com `geometry={"rings": rings_utm, "spatialReference":{"wkid":31982}}`, `geometryType=esriGeometryPolygon`, `inSR=31982`, `spatialRel=esriSpatialRelIntersects`, `where=cdbairro>0`, `outFields=cdbairro,nmbairro`, `returnDistinctValues=true`, `returnGeometry=false`. Extrair `candidates=[(cdbairro,nmbairro),...]`.
   - 0 candidatos → retornar `(None, None, "sem-parcela")`.
   - 1 candidato → retornar `(cdbairro, nmbairro, "unico")`.
   - 2+ candidatos → POST `returnCountOnly=true` por candidato (`where=cdbairro=<c>`, mesma geometria). Tie-break: se `nm_bai_original` não-vazio E `norm(nm_bai_original)` é substring de EXATAMENTE um `norm(nmbairro)` candidato (ou vice-versa) → esse vence (motivo `"nome"`). Senão → maior contagem (motivo `"maioria"`). Retornar `(cdbairro, nmbairro, motivo)`.
   - Paciência: `time.sleep(0.25)` entre chamadas (endpoint frágil, ~3400 chamadas totais — DATA-NAMES.md 1.3).

4. **Step 4.5 no `main()`** — rodar entre a reprojeção (passo 3) e a escrita do raw. Iterar sobre os `features` CRUS (com `geometry.rings` ainda em UTM), chamar `reconcile_name` por feature, montar `RECON[id] = {"cdbairro","nmbairro_reconciled","motivo","nm_bai_original"}`. Acumular contadores: `recuperados_de_branco`, `nomes_mudados`, `nao_resolvidos`, e por-motivo. Imprimir progresso a cada 100 polígonos.

5. **Atualizar o docstring do topo** com o quirk novo: *"Step 4.5: spatial join POST à layer 3 — GET com geometria 404/414 intermitente (quirk verificado); nomes vêm de nmbairro (autoritativo), NUNCA de string-match do nm_bai."*

NÃO tocar na geometria/reprojeção/paginação existentes. NÃO usar string-match como FONTE de nome (só como tie-break entre candidatos espaciais). NÃO embutir chave/segredo.
  </action>
  <verify>
    <automated>python -c "import ast; t=ast.parse(open('gerar-bairros.py',encoding='utf-8').read()); n={f.name for f in __import__('ast').walk(t) if isinstance(f,ast.FunctionDef)}; assert {'http_post','arcgis_post','reconcile_name','norm'} <= n, n; print('OK funcoes:',sorted(n))" && python -m py_compile gerar-bairros.py && grep -q "esriSpatialRelIntersects" gerar-bairros.py && grep -q "returnCountOnly" gerar-bairros.py && echo "OK grep"</automated>
  </verify>
  <done>gerar-bairros.py define http_post/arcgis_post/reconcile_name/norm; grep confirma `esriSpatialRelIntersects` e `returnCountOnly`; string-match aparece só no tie-break (comentado como tal), nunca como fonte; py compila.</done>
</task>

<task type="auto">
  <name>Task 2: Injetar nomes reconciliados no json committed (geometria byte-idêntica) + emitir diff report</name>
  <read_first>gerar-bairros.py (write_report existente); bairros-goiania.json (props {id,nm_bai}); bairros-goiania.report.md (formato atual); DATA-NAMES.md 1.4 ponto 3 e 1.6</read_first>
  <files>gerar-bairros.py, bairros-goiania.json, bairros-goiania.report.md</files>
  <action>
O `bairros-goiania.json` committed é o artefato JÁ SIMPLIFICADO por mapshaper. Para garantir geometria BYTE-IDÊNTICA, NÃO re-simplificar: **injetar os nomes reconciliados por `id` diretamente no json committed existente**, tocando SÓ `properties.nm_bai`.

1. **Novo modo `--apply-names`** em `gerar-bairros.py` (executado após o `--verify` produzir `RECON`): carregar o `bairros-goiania.json` atual, e para cada feature substituir `properties.nm_bai` pela regra:
   - `RECON[id].nmbairro_reconciled` se resolvido (nome autoritativo da layer 3);
   - se não-resolvido (0 candidatos) → `"Gleba não denominada"` (rótulo genérico explícito, decisão do CONTEXT — não fabricar nome fiscal);
   - preservar `properties.id` intacto e a ordem das features. Escrever com `json.dump(..., separators=(",",":"), ensure_ascii=False)` — MESMOS separators/encoding do gerador original (senão bytes cosméticos divergem sem ser geometria).

2. **Estender `write_report()`** com uma seção `## Reconciliação de nomes (NOMES-01/03)`:
   - contadores: total reconciliados, recuperados-de-branco, nomes-mudados, não-resolvidos, quebra por `motivo` (unico/nome/maioria/sem-parcela);
   - **tabela de diff antes→depois por polígono** para TODOS os que mudaram (`id | nm_bai_original | nmbairro_reconciled | motivo`);
   - **subseção destacada `### Multi-candidatos (conferência)`** listando só os resolvidos por `motivo in {nome, maioria}` (casos de risco — bordas administrativas), com candidatos e contagens, para a revisão por amostra do orquestrador. O contra-exemplo "Ofugi" DEVE aparecer resolvido por `nome` → `VI OFUGI` (NÃO `VI SANTA HELENA`);
   - nota de acompanhamento: revisão humana das bordas é item não-bloqueante (Open Decision #1).

3. Regenerar de fato: rodar `python gerar-bairros.py --verify` (produz RECON) e `--apply-names` (aplica sobre o json committed). Regenerar o report.
  </action>
  <verify>
    <automated>python check-bairros-geojson.py bairros-goiania.json && python -c "import json; d=json.load(open('bairros-goiania.json',encoding='utf-8')); assert len(d['features'])==1206, len(d['features']); assert all(set(f['properties'])=={'id','nm_bai'} for f in d['features']); r=open('bairros-goiania.report.md',encoding='utf-8').read(); assert 'Reconcilia' in r and 'Multi-candidato' in r; print('OK 1206 features, props inalteradas, report com reconciliacao')"</automated>
  </verify>
  <done>bairros-goiania.json tem 1206 features com `properties`={id,nm_bai}; glebas sem parcela = "Gleba não denominada"; report tem seção de reconciliação + tabela de diff + subseção multi-candidatos; check-bairros-geojson.py passa; Ofugi resolvido como VI OFUGI no report.</done>
</task>

<task type="auto">
  <name>Task 3: Assert de geometria byte-idêntica em check-bairros-geojson.py</name>
  <read_first>check-bairros-geojson.py (todo — validate_output, check_irregular, main); DATA-NAMES.md 1.5 (drill não usa nome)</read_first>
  <files>check-bairros-geojson.py</files>
  <action>
Adicionar um modo `--assert-geometry-identical <baseline_path>` que prova que o novo json só mudou nomes.

1. **Função `assert_geometry_identical(new_data, baseline_path)`**: carregar o baseline (o `bairros-goiania.json` ANTERIOR, capturado via `git show HEAD:bairros-goiania.json > baseline.json`). Assertar:
   - mesma CONTAGEM de features (1206 == 1206);
   - conjunto de `properties.id` idêntico;
   - `feature.geometry` ESTRUTURALMENTE idêntico por `id` — comparar `json.dumps(geometry, sort_keys=True)` entre baseline e novo. QUALQUER divergência de geometria → `sys.exit(1)` com o `id` culpado;
   - única diferença permitida é `properties.nm_bai`. Contar quantos nomes mudaram (informativo, não falha).
2. **Wire no `main()`**: aceitar `--assert-geometry-identical <path>`. Se passado, rodar `validate_output` + o assert. Imprimir `ASSERT OK: geometria byte-identica, N nomes mudaram` ou abortar.
3. Documentar no docstring: *"o fix de nomes é display-data-only; o drill (fitBounds/getBounds) não usa o nome (DATA-NAMES.md 1.5) — este assert prova que nada além do nome mudou."*

Fluxo de uso (documentar no SUMMARY): capturar o baseline do git ANTES do commit dos novos nomes e rodar o assert contra ele.
  </action>
  <verify>
    <automated>git show HEAD:bairros-goiania.json > "$TMPDIR/baseline-bai.json" 2>/dev/null && python check-bairros-geojson.py --assert-geometry-identical "$TMPDIR/baseline-bai.json" bairros-goiania.json && echo "ASSERT ran"</automated>
  </verify>
  <done>check-bairros-geojson.py aceita `--assert-geometry-identical`; comparando o json novo contra o baseline HEAD, passa (1206 features, ids idênticos, geometrias idênticas, só nm_bai difere); aborta com sys.exit em qualquer mudança de geometria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-script → ArcGIS público (layer 2/3) | HTTP read-only a endpoint público da Prefeitura; sem autenticação, sem escrita |
| build-script → repo (bairros-goiania.json) | script grava asset público (geometria + nomes de bairro) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | resposta do ArcGIS (join envenenado/parcial) | mitigate | Assert de geometria byte-idêntica contra o baseline committed (Task 3) — a reconciliação só pode mudar `nm_bai`, nunca geometria/contagem; abort em divergência |
| T-07-02 | Information Disclosure | nomes reconciliados (dado público) | accept | Nomes de bairro e cdbairro são dados públicos cadastrais; nenhum campo pessoal (nunca `dtnascimen`) é lido — só `cdbairro`/`nmbairro` agregados |
| T-07-03 | Denial of Service | endpoint frágil (502 sob ~3400 chamadas) | mitigate | Build-time offline com backoff 429/503 reusado do `http()` + `time.sleep(0.25)` entre chamadas; não é caminho de runtime |
| T-07-04 | Injection | nome reconciliado → tooltip/breadcrumb | accept | O app já usa `esc()`/`textContent` no highlight/breadcrumb (verificado, DATA-NAMES.md 1.5) — zero risco de injeção mantido |

**Sem ameaças de alta severidade.** Scripts são build-time offline, read-only contra endpoint público; assets gerados são geometria/nomes públicos sem PII; o assert de geometria é a defesa central contra join corrompido.
</threat_model>

<verification>
- `python -m py_compile gerar-bairros.py check-bairros-geojson.py` — ambos compilam
- `gerar-bairros.py` faz POST spatial join à layer 3 (`esriSpatialRelIntersects`); nomes vêm de `nmbairro`; tie-break assistido por nome documentado; string-match NÃO é fonte
- `bairros-goiania.json`: 1206 features, `properties`={id,nm_bai}, geometria byte-idêntica ao baseline (assert)
- `check-bairros-geojson.py --assert-geometry-identical` passa contra o baseline HEAD
- `bairros-goiania.report.md`: seção de reconciliação + tabela de diff + subseção multi-candidatos; Ofugi resolvido como VI OFUGI
- Glebas sem parcela → "Gleba não denominada"
</verification>

<success_criteria>
- Nomes reconciliados da fonte autoritativa (layer 3) via spatial join POST — NOMES-01
- Geometria + contagem byte-idênticas provadas por assert automatizado — NOMES-02
- Relatório de diff antes→depois por polígono + multi-candidatos destacados para revisão — NOMES-03
- Glebas sem parcela recebem rótulo genérico; nenhum nome fiscal fabricado — NOMES-03
- Drill inalterado (mudança display-only, confirmado por DATA-NAMES.md 1.5)
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-funda-o-de-dados-nomes-de-bairro-cnefe-tuning-da-malha/07-01-SUMMARY.md`.
Incluir no SUMMARY: o comando exato de captura do baseline (`git show HEAD:bairros-goiania.json > baseline.json`) e execução do assert, os contadores de reconciliação (recuperados-de-branco / nomes-mudados / não-resolvidos), e uma nota de que a revisão humana das bordas (Open Decision #1) fica agendada como acompanhamento não-bloqueante — o orquestrador confere uma amostra + os multi-candidatos.
</output>
