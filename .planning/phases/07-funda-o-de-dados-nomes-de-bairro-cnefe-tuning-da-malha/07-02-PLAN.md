---
phase: 07-funda-o-de-dados-nomes-de-bairro-cnefe-tuning-da-malha
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - gerar-logradouros.py
  - logradouros-goiania.json
  - sw.js
autonomous: true
requirements: [NOMES-02]
tags: [cnefe, ibge, data-distill, build-script, service-worker, precache]

must_haves:
  truths:
    - "Existe um asset offline logradouros-goiania.json (~9,8k logradouros únicos com dicas de CEP/localidade, ~117KB gz) versionado no repo"
    - "O asset é produzido por um build-script build-time-only que baixa o CNEFE 52_GO.zip, filtra Goiânia e destila — nunca roda no runtime"
    - "O sw.js teve o cache bumpado (radar-v5 → radar-v6) cobrindo o bairros-goiania.json re-shipado E o novo logradouros-goiania.json foi adicionado ao array LOCAL de precache"
    - "O asset contém SÓ nomes de logradouro + CEP/localidade — nenhum campo pessoal/domiciliar"
  artifacts:
    - path: "gerar-logradouros.py"
      provides: "Build-script CNEFE: download 52_GO.zip, filtro COD_MUNICIPIO=5208707, distill de logradouros únicos + hints"
      contains: "5208707"
    - path: "logradouros-goiania.json"
      provides: "~9,8k logradouros de Goiânia com dicas de localidade/CEP (~117KB gz)"
      min_lines: 1
    - path: "sw.js"
      provides: "Cache radar-v6 + logradouros-goiania.json no array LOCAL"
      contains: "radar-v6"
  key_links:
    - from: "gerar-logradouros.py"
      to: "logradouros-goiania.json"
      via: "distill filtrado por COD_MUNICIPIO=5208707"
      pattern: "5208707"
    - from: "sw.js LOCAL"
      to: "logradouros-goiania.json"
      via: "entrada de precache + bump de versão"
      pattern: "logradouros-goiania.json"
---

<objective>
Destilar o dataset CNEFE 2022 de Goiânia em `logradouros-goiania.json` (~9,8k logradouros únicos com dicas de CEP/localidade, ~117KB gz) como asset offline, via um novo build-script `gerar-logradouros.py`. Fazer o único bump do `sw.js` da fase: cache `radar-v5` → `radar-v6` (cobrindo o `bairros-goiania.json` re-shipado no Plano 01) e adicionar o novo asset ao array `LOCAL` de precache.

Purpose: A Fase 8 (busca) consome esse asset para o autocomplete de logradouro. Aqui só produzimos o dado limpo — o matcher fino é da Fase 8. É base do BUSCA-10, e o bump do sw.js fecha o NOMES-02 (cobrir o json regenerado). (NOMES-02 + base BUSCA-10)
Output: `gerar-logradouros.py`, `logradouros-goiania.json` versionado, `sw.js` bumpado + com o novo asset no precache.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-funda-o-de-dados-nomes-de-bairro-cnefe-tuning-da-malha/07-CONTEXT.md
@.planning/research/v2.1/DATA-NAMES.md

# Fontes de código (ler antes de editar)
@gerar-bairros.py
@sw.js

<facts>
<!-- Verificado ao vivo (DATA-NAMES.md Part 2) e por inspeção deste repo. -->

Fonte CNEFE (verificada ao vivo, HTTP 200, 120.818.046 bytes):
  https://ftp.ibge.gov.br/Cadastro_Nacional_de_Enderecos_para_Fins_Estatisticos/Censo_Demografico_2022/Arquivos_CNEFE/CSV/UF/52_GO.zip
- Arquivo é do ESTADO INTEIRO (Goiás) — NÃO existe download por-município. Filtrar localmente.
- Dentro do zip: 52_GO.csv (~671MB descompactado), single CSV, todos os municípios de GO.
- Goiânia = COD_MUNICIPIO 5208707 (confirmado ao vivo: 777.018 linhas de endereço).

Colunas relevantes do dicionário CNEFE (parseado ao vivo, DATA-NAMES.md 2.1):
  COD_MUNICIPIO, CEP, DSC_LOCALIDADE (bairro/loteamento informal),
  NOM_TIPO_SEGLOGR (tipo: RUA/AVENIDA...), NOM_TITULO_SEGLOGR (título: Senador...),
  NOM_SEGLOGR (nome do logradouro cru).
  Colunas PESSOAIS/DOMICILIARES existem no CNEFE — NÃO ler nenhuma delas. Só as acima.

Asset alvo (variante recomendada, DATA-NAMES.md 2.3): "nomes + hints de localidade/CEP" =
  9.789 logradouros únicos, 691.5KB raw / 116.8KB gz. Cabe no orçamento (bairros ~166KB gz).
  Estrutura à discrição (CONTEXT): p.ex. lista de {nome, tipo, localidades:[...até 5], ceps:[...até 3]}.

Normalização (DATA-NAMES.md 2.4): strip de espaço antes do sufixo alfanumérico final
  (CNEFE "228 A" vs cadastro "228A" — drift de tokenização). SÓ produzir o asset limpo aqui;
  o matcher fino cadastro×CNEFE é da Fase 8.

Precedente de build-script com backoff: gerar-bairros.py / atualizar-caixa.py (helper http() com retry).
</facts>

<interfaces>
<!-- sw.js ATUAL (inspecionado) — o executor edita SÓ estas duas coisas: -->

const CACHE = "radar-v5";          // -> bump para "radar-v6"
const LOCAL = [
  "./", "./radar-goiania.html", "./caixa-goiania.js",
  "./bairros-goiania.json",        // já presente (o Plano 01 re-shipa o conteúdo, mesmo filename)
  "./manifest.json", "./icon-192.png", "./icon-512.png", "./apple-touch-icon.png"
];                                 // -> adicionar "./logradouros-goiania.json"

# Estratégia de cache decidida (CONTEXT): logradouros-goiania.json é asset ESTÁTICO versionado.
# NÃO adicionar ao regex NETWORK_FIRST (esse é só p/ HTML/caixa/manifest — dado que muda no servidor).
# Fica no LOCAL (precache no install) e servido cache-first pelo branch default do fetch handler —
# mesmo tratamento do bairros-goiania.json. O bump de versão é o que invalida a cópia antiga.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar gerar-logradouros.py (download CNEFE + filtro Goiânia + distill)</name>
  <read_first>gerar-bairros.py (helper http()/UA/backoff, docstring de build-script offline); DATA-NAMES.md 2.1–2.4</read_first>
  <files>gerar-logradouros.py</files>
  <action>
Criar `gerar-logradouros.py` (irmão de `gerar-bairros.py`, MESMAS convenções: docstring de "build offline, não roda no app", UA, backoff). Etapas:

1. **Download** `52_GO.zip` da URL do CNEFE (ver <facts>) via streaming para um arquivo temporário (é ~120MB — NÃO carregar em memória de uma vez; usar `urllib` + `shutil.copyfileobj` ou leitura em chunks). Reusar o padrão de retry/backoff do `http()`. Documentar no docstring que é build-time raro (refresh do CNEFE é ocasional).

2. **Filtrar + destilar** — abrir o CSV de dentro do zip com `zipfile.ZipFile` + streaming (`io.TextIOWrapper`, `csv.DictReader`), processar linha-a-linha (nunca materializar 671MB). Para cada linha com `COD_MUNICIPIO == "5208707"`:
   - montar o nome do logradouro = `NOM_TIPO_SEGLOGR` + (opcional `NOM_TITULO_SEGLOGR`) + `NOM_SEGLOGR`;
   - normalizar (strip de espaço antes do sufixo alfanumérico final — DATA-NAMES.md 2.4; accent/case como o `ruaCore()` do app);
   - agregar num dict `LOG[chave] = {"nome","tipo","localidades":set(),"ceps":set()}`, acumulando `DSC_LOCALIDADE` (até 5) e prefixos de `CEP` (até 3);
   - **NUNCA** ler/armazenar qualquer coluna pessoal/domiciliar — SÓ tipo/título/nome/localidade/CEP. Parse defensivo (ignorar linhas malformadas sem abortar tudo).

3. **Escrever** `logradouros-goiania.json` (~9,8k entradas), `json.dump(..., separators=(",",":"), ensure_ascii=False)`, sets convertidos em listas ordenadas. Estrutura à discrição (ver <facts>). Imprimir contagem de logradouros únicos + tamanho gz (usar `gzip.compress` como o check-bairros faz) para conferência com o alvo (~9,8k / ~117KB gz).

4. **Limpar** o zip/CSV temporário ao fim (build não deve deixar 120MB de lixo no repo).

NÃO embutir chave/segredo. NÃO adicionar o download ao runtime do app. NÃO incluir campo pessoal.
  </action>
  <verify>
    <automated>python -m py_compile gerar-logradouros.py && grep -q "5208707" gerar-logradouros.py && grep -q "COD_MUNICIPIO" gerar-logradouros.py && grep -q "52_GO.zip" gerar-logradouros.py && echo "OK script"</automated>
  </verify>
  <done>gerar-logradouros.py compila; grep confirma COD_MUNICIPIO=5208707, a URL 52_GO.zip, streaming (zipfile/csv) e a ausência de leitura de campos pessoais; docstring marca "build-time only / refresh raro".</done>
</task>

<task type="auto">
  <name>Task 2: Gerar logradouros-goiania.json + smoke check de estrutura/tamanho/anti-PII</name>
  <read_first>gerar-logradouros.py (Task 1); check-bairros-geojson.py (padrão de smoke-check por tamanho gz)</read_first>
  <files>logradouros-goiania.json</files>
  <action>
Rodar `python gerar-logradouros.py` para produzir `logradouros-goiania.json`. Depois validar o artefato:

1. Confirmar contagem próxima de ~9,8k logradouros únicos e tamanho gz próximo de ~117KB (dentro do orçamento — não pode explodir o storage do PWA).
2. Confirmar estrutura: cada entrada tem nome + (localidades/ceps como listas); nenhuma chave pessoal (varrer o JSON e assertar que não contém termos como `dtnasc`, `cpf`, `responsavel`, `morador`).
3. Um logradouro conhecido de exemplo (p.ex. um contendo "228" com múltiplas localidades) demonstra as dicas de localidade — sanity da utilidade do asset para a Fase 8.

Se o tamanho estourar muito o orçamento, cair para menos localidades/CEPs por logradouro (a variante "nomes + hints" já é a recomendada; a de nomes-só ~39KB é o fallback extremo — DATA-NAMES.md 2.3).
  </action>
  <verify>
    <automated>python -c "import json,gzip; d=json.load(open('logradouros-goiania.json',encoding='utf-8')); assert isinstance(d,(list,dict)); n=len(d); assert n>=5000, n; kb=len(gzip.compress(json.dumps(d,separators=(',',':'),ensure_ascii=False).encode()))/1024; assert kb<200, kb; s=json.dumps(d).lower(); assert not any(t in s for t in ['dtnasc','cpf','responsavel','morador']); print(f'OK {n} logradouros, {kb:.1f}KB gz, sem PII')"</automated>
  </verify>
  <done>logradouros-goiania.json existe, ~9,8k entradas (>=5000), <200KB gz, estrutura com nome+hints, zero chave/valor pessoal.</done>
</task>

<task type="auto">
  <name>Task 3: Bump do sw.js (radar-v5 → radar-v6) + adicionar o asset ao precache LOCAL</name>
  <read_first>sw.js (todo — CACHE, LOCAL, NETWORK_FIRST, fetch handler)</read_first>
  <files>sw.js</files>
  <action>
Editar `sw.js` — as DUAS únicas mudanças da fase (este é o único plano que toca sw.js):

1. `const CACHE = "radar-v5";` → `const CACHE = "radar-v6";` (o `activate` já apaga caches != CACHE, então o bump invalida a cópia antiga do `bairros-goiania.json` re-shipado no Plano 01 e força re-precache).
2. No array `LOCAL`, adicionar `"./logradouros-goiania.json"` (mesmo tratamento estático do `bairros-goiania.json`). NÃO adicionar ao regex `NETWORK_FIRST` (esse é só p/ HTML/caixa/manifest). NÃO mexer no fetch handler nem no CDN.

Atualizar o comentário de topo do sw.js mencionando o novo asset estático se fizer sentido, sem alterar a estratégia.
  </action>
  <verify>
    <automated>node --check sw.js && grep -q 'radar-v6' sw.js && ! grep -q 'radar-v5' sw.js && grep -q 'logradouros-goiania.json' sw.js && grep -q 'bairros-goiania.json' sw.js && echo "OK sw.js: sintaxe valida, v6, ambos assets no precache"</automated>
  </verify>
  <done>sw.js passa `node --check`; CACHE == "radar-v6" (nenhum "radar-v5" restante); `logradouros-goiania.json` e `bairros-goiania.json` ambos no array LOCAL; fetch handler/CDN/NETWORK_FIRST inalterados.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-script → IBGE FTP (52_GO.zip público) | HTTP read-only a zip público do IBGE; sem autenticação, sem escrita |
| build-script → repo (logradouros-goiania.json) | script grava asset público (nomes de logradouro + CEP/localidade) |
| PWA → logradouros-goiania.json (runtime) | asset estático precached servido offline; consumido pela busca na Fase 8 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Information Disclosure | destill do CNEFE (colunas pessoais/domiciliares existem no CSV) | mitigate | O parser lê SÓ NOM_TIPO/TITULO/SEGLOGR + DSC_LOCALIDADE + CEP; nunca colunas de indivíduo/domicílio. Smoke check (Task 2) assert que o JSON não contém termos pessoais (dtnasc/cpf/responsavel/morador) |
| T-07-06 | Tampering | zip do IBGE (integridade do download de 120MB) | accept | Parse defensivo linha-a-linha (ignora malformadas); asset final validado por estrutura/tamanho (Task 2); dado público, baixo valor de alvo |
| T-07-07 | Denial of Service | download 120MB + parse 671MB (memória/disco) | mitigate | Streaming (chunks + csv.DictReader linha-a-linha), nunca materializa o CSV inteiro; limpa o temporário ao fim; build-time only |
| T-07-08 | Denial of Service (PWA storage) | asset grande inflando o precache | mitigate | Orçamento gz <200KB assertado (Task 2); variante "nomes+hints" ~117KB cabe; fallback nomes-só ~39KB |

**Sem ameaças de alta severidade.** Download read-only de fonte pública IBGE; o destill é agregado a nível de logradouro (sem dado de indivíduo/domicílio — o assert anti-PII é a defesa central); asset público sem segredo; build offline com streaming para não estourar memória.
</threat_model>

<verification>
- `python -m py_compile gerar-logradouros.py` compila; grep confirma COD_MUNICIPIO=5208707, URL 52_GO.zip, streaming
- `logradouros-goiania.json`: ~9,8k logradouros, <200KB gz, estrutura nome+hints, zero campo pessoal (assert anti-PII)
- `node --check sw.js` passa; CACHE == "radar-v6"; ambos os assets (bairros + logradouros) no array LOCAL
- Build-time only documentado (refresh do CNEFE é raro)
</verification>

<success_criteria>
- Asset CNEFE destilado (~9,8k logradouros, nomes + CEP/localidade, ~117KB gz) versionado offline — base BUSCA-10
- Nenhum campo pessoal/domiciliar no asset (assert anti-PII) — restrição LGPD do projeto
- sw.js bumpado radar-v5 → radar-v6 cobrindo o bairros-goiania.json regenerado + novo asset no precache — NOMES-02
- Build-script build-time-only, streaming (não estoura memória), limpa temporário
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-funda-o-de-dados-nomes-de-bairro-cnefe-tuning-da-malha/07-02-SUMMARY.md`.
Incluir no SUMMARY: contagem final de logradouros + tamanho gz medido, a estrutura JSON escolhida (para a Fase 8 consumir), o custo/tempo do download+distill (build-time, raro), e a confirmação de que o sw.js cobre AMBOS os assets (bairros regenerado + logradouros novo) sob radar-v6.
</output>
