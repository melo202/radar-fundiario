---
phase: 08-busca-unica-inteligente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - radar-goiania.html
  - tests/fixtures.mjs
  - tests/busca.test.mjs
  - package.json
autonomous: true
requirements: [BUSCA-01]
tags: [testing, refactor, matching, node, harness]

must_haves:
  truths:
    - "As funcoes puras de matching (norm/likeTerm/ruaCore/matchApto/isGarage/okQ/okL/casaRuaCore) tem harness de teste Node executavel com `node --test tests/`, ANTES de qualquer mudanca de comportamento de fuzzy/detectMode"
    - "O harness testa contra as MESMAS funcoes que o app usa em runtime (nao uma copia divergente) — extraidas para um bloco delimitado dentro de radar-goiania.html e avaliado pelo teste via node:vm, sem duplicar implementacao"
    - "Fixtures obrigatorias cobrem: '135' isolado, 'Rua 135', 'Q135', inscricao 10 vs 14 digitos, lote '20/21', quadra '10E', apto '1901' vs '19'"
  artifacts:
    - path: "tests/fixtures.mjs"
      provides: "Casos de teste nomeados (input/expected) reusados pelo harness"
      contains: "135"
    - path: "tests/busca.test.mjs"
      provides: "Suite Node nativa (node:test + node:assert) validando norm/likeTerm/ruaCore/matchApto/isGarage e os predicados okQ/okL/casaRuaCore"
      contains: "node:test"
    - path: "radar-goiania.html"
      provides: "Bloco de funcoes puras isolado e comentado como extraido/testado — mesma implementacao usada em runtime e em Node"
      contains: "RADAR_PURE_START"
  key_links:
    - from: "tests/busca.test.mjs"
      to: "radar-goiania.html"
      via: "extracao das funcoes puras para um trecho delimitado (marcadores RADAR_PURE_START/END) lido e avaliado pelo teste em Node via vm, sem framework/bundler"
      pattern: "RADAR_PURE_START"
---

<objective>
Extrair as funcoes puras de matching/normalizacao ja existentes em `radar-goiania.html` (`norm`, `likeTerm`, `ruaCore`, `matchApto`, `isGarage`) e os predicados hoje inline dentro de `buscar()` (`okQ`/`okL` do modo Quadra/Lote, `casaRua` do modo Endereco) para um bloco unico, delimitado e comentado, e criar um harness de teste Node puro (`node:test`, sem framework novo) que carrega ESSE MESMO bloco — nao uma copia — e valida contra fixtures obrigatorias, incluindo os casos ambiguos do roadmap ("135" isolado, "Rua 135", "Q135", inscricao 10 vs 14 dig, lote "20/21", quadra "10E", apto "1901" vs "19").

Purpose: BUSCA-01 exige harness de teste ANTES de qualquer mudanca de comportamento. Sem isso, o fuzzy-fix (08-02) e o detectMode (08-03) seriam mudancas de matching "as ciegas" — exatamente o risco que a pesquisa (SEARCH.md secao 7, item 1) marca como pre-requisito logico, nao opcional.
Output: `radar-goiania.html` com o bloco de funcoes puras isolado (mesma implementacao, zero mudanca de comportamento nesta plan); `tests/fixtures.mjs` + `tests/busca.test.mjs` executaveis com `node --test tests/`; `package.json` minimo com `"type":"module"` e script `test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-busca-unica-inteligente/08-CONTEXT.md
@.planning/research/v2.1/SEARCH.md

# Fonte de codigo (ler antes de editar)
@radar-goiania.html

<facts>
<!-- Verificado por leitura direta deste repo (radar-goiania.html, linhas aproximadas do estado atual). -->

Funcoes puras hoje espalhadas (SEM bloco unico, SEM teste):
```
linha ~670: const clean=v=>String(v==null?"":v).trim();
linha ~682: const likeTerm=v=>{const s=norm(v).replace(/'/g,"");const d=s.replace(/\D/g,"");return d||s.replace(/[^A-Z0-9]/g,"");};
linha ~1026: const norm=s=>String(s||"").normalize("NFD").replace(/[̀-ͯ]/g,"").toUpperCase().replace(/\s+/g," ").trim();
linha ~1028: const TIPOVIA=/^(RUA|R|AVENIDA|AVN?|ALAMEDA|AL|TRAVESSA|TV|PRACA|PC|ESTRADA|EST|RODOVIA|ROD|VIELA|VIA)\.?\s+/;
linha ~1029: const ruaCore=s=>norm(s).replace(/'/g,"").replace(TIPOVIA,"");
linha ~1382: function isGarage(a){return /\b(BOX|GARAG|VAGA|DEPOSITO|DEP\b|SUBSOLO)/.test(norm(a.incompl));}
linha ~1383: function matchApto(a,q){const digits=String(q).replace(/\D/g,"");if(!digits)return true;
  const inc=norm(a.incompl);return inc.includes("APT"+digits)||inc.includes("AP"+digits)||inc.includes("APTO"+digits)||
  (inc.match(/\d+/g)||[]).includes(digits);}
```

Predicados INLINE (nao sao funcoes nomeadas hoje — extrair para nomeadas SEM mudar logica):
```
buscar() MODE="addr" (linha ~1222): const casaRua=a=>{const log=ruaCore(a.nmlogradou);
  return log.includes(rCore)||(rD&&(log.match(/\d+/g)||[]).includes(rD));};

buscar() MODE="ql" (linha ~1273-1274, dentro do filter de finish):
  const okQ=!q||nq===qU||nq.replace(/\D/g,"")===qU.replace(/\D/g,"")||nq.includes(qU);
  const okL=!l||nl===lU||nl.replace(/[^0-9\/]/g,"").split("/").includes(lU)||nl.includes(lU);
```

Estas duas (okQ/okL/casaRua) hoje sao fechamentos locais dentro de `buscar()` — capturam `qU`/`lU`/`rCore`/`rD` do escopo.
Para extrair para funcoes puras testaveis, TRANSFORMAR em funcoes de argumentos explicitos:
```
function okQ(nq, qU){ return !qU||nq===qU||nq.replace(/\D/g,"")===qU.replace(/\D/g,"")||nq.includes(qU); }
function okL(nl, lU){ return !lU||nl===lU||nl.replace(/[^0-9\/]/g,"").split("/").includes(lU)||nl.includes(lU); }
function casaRuaCore(log, rCore, rD){ return log.includes(rCore)||(rD&&(log.match(/\d+/g)||[]).includes(rD)); }
```
Chamar essas versoes nomeadas DENTRO de buscar() no lugar do inline — MESMO comportamento, so nomeado e testavel
(note: o `!q`/`!l` original testava a variavel de input crua; usar `!qU`/`!lU` e equivalente pois `qU=q.toUpperCase()`
so e vazio quando `q` e vazio — confirmar essa equivalencia ao portar).

Isso e o UNICO ponto onde este plano toca a LOGICA de busca (renomear/parametrizar, nao mudar regra) — o fuzzy-fix
real (pontuacao/ordenacao/selo aproximado) e 08-02, NAO aqui. Zero mudanca de comportamento observavel nesta plan.

Padrao "sem build" do projeto (mesmo espirito de gerar-bairros.py/gerar-logradouros.py, aplicado a JS): usar
marcadores de comentario unicos dentro do <script> do HTML; o teste Node LE o arquivo .html como texto, RECORTA o
trecho entre os marcadores, e AVALIA esse trecho com `node:vm` — nunca duplica a implementacao a mao em um segundo
arquivo (isso causaria "teste de uma copia divergente", proibido pelo objetivo desta fase).
</facts>

<interfaces>
<!-- Contrato que tests/busca.test.mjs consome — nao inventar API diferente. -->

Bloco RADAR_PURE em radar-goiania.html, delimitado assim (dentro do `<script>` principal, ANTES de `let MODE=...`):

```js
// RADAR_PURE_START — funcoes puras de matching/normalizacao. Testadas por tests/busca.test.mjs
// (que le e avalia este bloco isoladamente via node:vm — nao editar sem rodar `node --test tests/`).
const clean = v=>String(v==null?"":v).trim();
const norm = s=>String(s||"").normalize("NFD").replace(/[̀-ͯ]/g,"").toUpperCase().replace(/\s+/g," ").trim();
const TIPOVIA = /^(RUA|R|AVENIDA|AVN?|ALAMEDA|AL|TRAVESSA|TV|PRACA|PC|ESTRADA|EST|RODOVIA|ROD|VIELA|VIA)\.?\s+/;
const ruaCore = s=>norm(s).replace(/'/g,"").replace(TIPOVIA,"");
const likeTerm = v=>{const s=norm(v).replace(/'/g,"");const d=s.replace(/\D/g,"");return d||s.replace(/[^A-Z0-9]/g,"");};
function isGarage(a){ return /\b(BOX|GARAG|VAGA|DEPOSITO|DEP\b|SUBSOLO)/.test(norm(a.incompl)); }
function matchApto(a,q){ ... }
function okQ(nq, qU){ ... }
function okL(nl, lU){ ... }
function casaRuaCore(log, rCore, rD){ ... }
// RADAR_PURE_END
```

`tests/fixtures.mjs` exporta (ESM, sem dependencias):
```js
export const FIXTURES = {
  norm: [ {in:"Rua Bela", out:"RUA BELA"}, {in:"faiçalville", out:"FAICALVILLE"} ],
  ruaCore: [ {in:"Rua 135", out:"135"}, {in:"R  MARAJO", out:"MARAJO"}, {in:"Avenida T-4", out:"T-4"} ],
  matchApto: [
    {incompl:"APTO 1901", q:"1901", out:true},
    {incompl:"APTO 1901", q:"19", out:false},
    {incompl:"APT 19", q:"19", out:true}
  ],
  okQ: [ {nq:"10E", qU:"10E", out:true}, {nq:"", qU:"", out:true}, {nq:"2135", qU:"135", out:true /* substring fallback ainda vale nesta plan — 08-02 fixa isto */} ],
  okL: [ {nl:"20/21", lU:"20", out:true}, {nl:"20/21", lU:"21", out:true}, {nl:"20/21", lU:"22", out:false} ],
  insc: [
    {raw:"135", digitsOnly:"135", isAmbiguous:true},
    {raw:"Rua 135", hasStreetType:true},
    {raw:"Q135", hasQuadraToken:true},
    {raw:"3020150346", len:10, field:"ci"},
    {raw:"30201503461234", len:14, field:"nrinscr"}
  ],
};
```

`tests/busca.test.mjs` (Node nativo — `node:test` + `node:assert/strict`):
```js
import { readFileSync } from "node:fs";
import vm from "node:vm";
import { test } from "node:test";
import assert from "node:assert/strict";
import { FIXTURES } from "./fixtures.mjs";

function loadPureBlock(){
  const html = readFileSync(new URL("../radar-goiania.html", import.meta.url), "utf-8");
  const start = html.indexOf("RADAR_PURE_START");
  const end = html.indexOf("RADAR_PURE_END");
  assert.ok(start > -1 && end > start, "marcadores RADAR_PURE ausentes ou fora de ordem");
  const src = html.slice(start, end);
  const sandbox = {};
  vm.createContext(sandbox);
  new vm.Script(src + "\n;globalThis.__exports = {clean,norm,ruaCore,likeTerm,isGarage,matchApto,okQ,okL,casaRuaCore};",
    {filename:"radar-pure.js"}).runInContext(sandbox);
  return sandbox.__exports;
}
const P = loadPureBlock();
// ... test(...) para cada fixture, incluindo os casos ambiguos obrigatorios (135, Rua 135, Q135, insc 10x14, 20/21, 10E, 1901x19)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extrair bloco RADAR_PURE em radar-goiania.html (sem mudar comportamento)</name>
  <files>radar-goiania.html</files>
  <action>
Mover as funcoes puras ja existentes (`clean`, `norm`, `TIPOVIA`, `ruaCore`, `likeTerm`, `isGarage`, `matchApto`) para um unico bloco contiguo dentro do `<script>` principal, ANTES de `let MODE=...` (hoje linha ~683), delimitado pelos comentarios `// RADAR_PURE_START` e `// RADAR_PURE_END`. NAO alterar a implementacao — so mover/consolidar. Remover as declaracoes antigas dos pontos originais (nao duplicar — cada função deve existir uma unica vez no arquivo).

Extrair tambem `okQ`/`okL`/`casaRua` de dentro de `buscar()` para o MESMO bloco, como funcoes NOMEADAS de argumentos explicitos (ver `<facts>`/`<interfaces>` para a assinatura exata: `okQ(nq,qU)`, `okL(nl,lU)`, `casaRuaCore(log,rCore,rD)`), preservando EXATAMENTE a logica atual (substring fallback incluso — o fix de qualidade e 08-02, nao aqui). Atualizar os 2 call-sites dentro de `buscar()` (MODE="addr" `casaRua`, MODE="ql" filtro em `finish`) para chamar as versoes nomeadas passando os argumentos ja calculados no escopo local (`nq`, `qU`, `nl`, `lU`, `log`, `rCore`, `rD` ja existem como variaveis locais — so trocar o corpo inline pela chamada da funcao).

Manter todo o resto do arquivo intocado (nenhuma mudanca de UI, nenhuma mudanca de WHERE/consulta ao ArcGIS).
  </action>
  <verify>
    <automated>node --check radar-goiania.html 2>&1; python -c "
s=open('radar-goiania.html',encoding='utf-8').read()
assert 'RADAR_PURE_START' in s and 'RADAR_PURE_END' in s, 'marcadores ausentes'
block=s[s.index('RADAR_PURE_START'):s.index('RADAR_PURE_END')]
for fn in ['const norm','const ruaCore','const likeTerm','function isGarage','function matchApto','function okQ','function okL','function casaRuaCore']:
    assert fn in block, fn+' nao esta no bloco RADAR_PURE'
assert s.count('function okQ(')==1 and s.count('function okL(')==1 and s.count('function casaRuaCore(')==1, 'okQ/okL/casaRuaCore devem existir uma unica vez'
assert s.count('function isGarage(')==1 and s.count('function matchApto(')==1, 'isGarage/matchApto duplicados'
print('OK bloco RADAR_PURE presente e completo, sem duplicacao')
"</automated>
  </verify>
  <done>radar-goiania.html passa `node --check`; bloco RADAR_PURE_START/END contem norm/ruaCore/likeTerm/isGarage/matchApto/okQ/okL/casaRuaCore; cada funcao existe uma unica vez no arquivo (extraida, nao copiada); buscar() chama as versoes nomeadas nos 2 call-sites (addr/ql) sem mudar o resultado da busca.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Criar fixtures + harness Node (tests/fixtures.mjs, tests/busca.test.mjs, package.json)</name>
  <files>tests/fixtures.mjs, tests/busca.test.mjs, package.json</files>
  <behavior>
    - norm: remove acento e uppercasa ("faiçalville" -> "FAICALVILLE")
    - ruaCore: remove tipo de via + acento ("Rua 135" -> "135", "R  MARAJO" -> "MARAJO")
    - matchApto: "1901" bate incompl "APTO 1901" (true); "19" NAO bate "APTO 1901" (false, fronteira de digitos) — este e o caso do roadmap "1901 vs 19"
    - okQ: "10E"=="10E" bate (true); quadra "135" isolada bate substring em "2135" nesta plan (fallback ainda presente — 08-02 vai mudar isto, aqui so confirma o estado ATUAL preservado)
    - okL: lote "20/21" bate tanto lU="20" quanto lU="21" (true para ambos), NAO bate lU="22"
    - insc: 10 digitos -> campo "ci"; 14 digitos -> campo "nrinscr"; "135" sozinho e ambiguo (nao decide modo aqui — decisao de modo e 08-03); "Q135" e "Rua 135" tem sinalizadores de contexto (quadra/tipo-de-via) que os distinguem de "135" puro
  </behavior>
  <action>
Criar `package.json` minimo na raiz do repo (se nao existir): `{"name":"radar-fundiario","private":true,"type":"module","scripts":{"test":"node --test tests/"}}`. Se `package.json` ja existir por outro motivo, so adicionar `"type":"module"` e o script `test` sem remover campos existentes.

Criar `tests/fixtures.mjs` exportando `FIXTURES` com os casos de `<interfaces>`, garantindo que os 7 casos obrigatorios do roadmap estejam representados literalmente: "135" isolado, "Rua 135", "Q135", inscricao 10 digitos, inscricao 14 digitos, lote "20/21", quadra "10E", apto "1901" vs "19".

Criar `tests/busca.test.mjs` usando `node:test`/`node:assert/strict`/`node:vm` conforme `<interfaces>`: carrega o bloco RADAR_PURE de `radar-goiania.html` (relativo ao arquivo de teste, `../radar-goiania.html`), extrai as 8 funcoes via `vm.Script`+`runInContext`, e roda um `test(...)` por fixture (agrupar por funcao: `test("norm", ...)`, `test("ruaCore", ...)`, `test("matchApto", ...)`, `test("okQ", ...)`, `test("okL", ...)`, `test("insc — deteccao de campo por tamanho", ...)`). Cada assert usa `assert.equal`/`assert.ok`. Falha clara (mensagem com o input) se o arquivo HTML nao tiver os marcadores (dependencia da Task 1).
  </action>
  <verify>
    <automated>node --test tests/ 2>&1 | tail -30</automated>
  </verify>
  <done>`node --test tests/` roda e passa (exit 0); a suite cobre norm/ruaCore/matchApto/okQ/okL/insc com os 7 casos obrigatorios do roadmap; o harness le radar-goiania.html via vm (nao ha copia manual das funcoes dentro do teste).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests/busca.test.mjs -> radar-goiania.html | leitura local read-only de um arquivo do proprio repo (nenhuma rede, nenhuma entrada externa) |
| vm.Script (node:vm) -> bloco RADAR_PURE avaliado | codigo confiavel do proprio repo executado em contexto isolado (sandbox), nao input de usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | extracao de okQ/okL/casaRua para funcoes nomeadas | mitigate | Verify assert que cada funcao existe uma unica vez (sem duplicacao) e `node --check` garante sintaxe valida; nenhuma mudanca de logica nesta plan (so parametrizacao) |
| T-08-02 | Information Disclosure | tests/busca.test.mjs le radar-goiania.html do disco | accept | Arquivo local do proprio repo, sem dado sensivel (funcoes puras de normalizacao de texto, nenhum PII) |

**Sem ameacas de alta severidade.** Refactor puramente estrutural (extracao/renomeacao) sobre codigo client-side ja auditado; harness roda 100% local via `node:vm`, sem rede, sem dependencia nova.
</threat_model>

<verification>
- `node --check radar-goiania.html` passa
- Bloco RADAR_PURE_START/END contem as 8 funcoes puras, cada uma existindo uma unica vez no arquivo
- `node --test tests/` passa (exit 0), cobrindo os 7 casos obrigatorios do roadmap (135 isolado, Rua 135, Q135, insc 10 vs 14 dig, lote 20/21, quadra 10E, apto 1901 vs 19)
- Nenhuma mudanca de comportamento observavel na busca (WHERE/consultas ao ArcGIS intocados)
</verification>

<success_criteria>
- Harness de teste Node existe e passa, testando as MESMAS funcoes usadas em runtime — BUSCA-01
- Fixtures cobrem os 7 casos ambiguos obrigatorios do roadmap/CONTEXT
- Zero mudanca de comportamento de busca nesta plan (pre-requisito para 08-02/08-03 poderem medir antes/depois)
</success_criteria>

<output>
Apos completar, criar `.planning/phases/08-busca-unica-inteligente/08-01-SUMMARY.md`.
Incluir no SUMMARY: localizacao exata do bloco RADAR_PURE (linhas), comando para rodar os testes (`node --test tests/` ou `npm test`), contagem de testes/assertions, e confirmacao explicita de que okQ/okL/casaRuaCore preservam o comportamento ATUAL (substring fallback incluso) — a base que 08-02 vai medir antes/depois.
</output>
