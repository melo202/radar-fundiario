---
phase: 08-busca-unica-inteligente
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - radar-goiania.html
autonomous: true
requirements: [BUSCA-03, BUSCA-04, BUSCA-05, BUSCA-06, BUSCA-08, BUSCA-10, BUSCA-11]
tags: [ui, caixa-unica, dropdown, cnefe, a11y, aria]

must_haves:
  truths:
    - "A caixa unica substitui a moderow como experiencia PADRAO de busca; o HTML/CSS de .moderow/.modemore e reaproveitado como menu de correcao aberto pelo chip (sem CSS novo para essa peca)"
    - "Chip de confirmacao sempre visivel quando ha texto (role=status aria-live=polite), tocavel, mostra o que foi entendido; confianca alta/media dispara Enter direto, confianca baixa/ambigua (135 sozinho) NUNCA dispara busca automatica — mostra chips de desambiguacao"
    - "Setor assumido do localStorage NUNCA e silencioso — chip 'Setor · Nome (ultimo)' sempre visivel e tocavel para trocar quando a frase nao trouxe setor"
    - "Dropdown unificado mistura Setor (COMBO) + Rua (CNEFE logradouros-goiania.json, lazy-load no primeiro foco) com rotulo de tipo, navegavel por teclado (activedescendant), fecha por pointerdown no iOS (reusa DOWNEV)"
    - "Exemplos tocaveis substituem o .empty narrativo no estado vazio; tocar preenche a caixa e dispara buscar() no mesmo toque"
    - "Todo alvo de toque novo mede >=44px; zero hex novo; zero transition novo; SEARCHTOKEN aplicado a qualquer caminho assincrono novo (lazy-load do CNEFE)"
  artifacts:
    - path: "radar-goiania.html"
      provides: "Caixa unica (#caixaInput) + chip (#detectChip) + menu de correcao (reuso de .moderow/.modemore via #correctMenu) + chips de desambiguacao (#ambigChips) + exemplos tocaveis (#exampleChips) + dropdown unificado (#caixaList) + lazy-load de logradouros-goiania.json"
      contains: "caixaInput"
  key_links:
    - from: "#caixaInput (input, debounce ~150ms)"
      to: "detectMode()"
      via: "listener de input debounced chama detectMode(valor, COMBO) e atualiza #detectChip/#ambigChips"
      pattern: "detectMode"
    - from: "#detectChip (toque)"
      to: "#correctMenu (.moderow/.modemore reaproveitado)"
      via: "abre o menu de correcao reusando o HTML/CSS existente, forceMode(m) aplica setMode + preenche campos ja parseados"
      pattern: "forceMode"
    - from: "#caixaInput (foco/first use)"
      to: "logradouros-goiania.json"
      via: "fetch lazy no primeiro focus, guardado por SEARCHTOKEN-like flag para nao duplicar carga"
      pattern: "logradouros-goiania.json"
---

<objective>
Substituir a `.moderow` (3 botoes + link) pela caixa unica inteligente como experiencia PADRAO de busca: input unico com deteccao de modo (`detectMode`, 08-03) debounced (~150ms), chip de confirmacao tocavel (`role=status aria-live=polite`) que abre um menu de correcao reaproveitando 1:1 o HTML/CSS de `.moderow`/`.modemore`, chips de desambiguacao para confianca baixa, chip "Setor · Nome (ultimo)" nunca silencioso, dropdown unificado Setor+Rua (CNEFE lazy-load), e exemplos tocaveis substituindo o `.empty` narrativo. Zero token visual novo — 100% reuso do sistema definido em `08-UI-SPEC.md`.

Purpose: e a entrega central da fase — a materializacao em UI de tudo que 08-01/08-02/08-03 prepararam como logica pura testada. Preserva TODAS as garantias de acessibilidade da auditoria 03/07 (BUSCA-11): aria-*, labels, pointerdown no iOS, alvos 44px, `SEARCHTOKEN` em qualquer async novo.
Output: `radar-goiania.html` com a caixa unica funcional e acessivel, substituindo a moderow como tela padrao; menu de correcao/desambiguacao/exemplos tocaveis; dropdown unificado com CNEFE lazy-loaded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-busca-unica-inteligente/08-CONTEXT.md
@.planning/phases/08-busca-unica-inteligente/08-UI-SPEC.md
@.planning/research/v2.1/SEARCH.md
@.planning/phases/08-busca-unica-inteligente/08-03-SUMMARY.md

# Fonte de codigo (ler antes de editar)
@radar-goiania.html

<facts>
<!-- HTML atual da moderow + combo de setor + estado vazio (radar-goiania.html, linhas aproximadas). -->

HTML ATUAL a substituir (linhas ~506-561, dentro de `.search`):
```html
<div class="search">
  <label>Buscar por</label>
  <div class="moderow">
    <button id="mAD" class="on" onclick="setMode('addr')">Endereço</button>
    <button id="mBD" onclick="setMode('bd')">Prédio</button>
    <button id="mQL" onclick="setMode('ql')">Quadra/Lote</button>
  </div>
  <button class="modemore" id="mIN" onclick="setMode('insc')">ou buscar pela inscrição cadastral (CI)</button>
  <div id="needBairro">...combo de setor...</div>
  <div id="paneQL">...</div>
  <div id="paneAddr">...</div>
  <div id="paneUnit">...</div>
  <div id="paneBD">...</div>
  <div id="paneIN">...</div>
  <button class="go" id="btnGo" onclick="buscar()">Buscar</button>
  ...
</div>
<div class="results" id="results">
  <div class="empty">...texto narrativo...</div>
</div>
```

HTML NOVO (08-UI-SPEC.md, componentes 1-4 — copiar a estrutura exata do UI-SPEC, IDs literais):
```html
<div class="search">
  <label for="caixaInput">Buscar</label>
  <div class="caixa">
    <input id="caixaInput" class="caixa-input" autocomplete="off" inputmode="text"
      placeholder="quadra 128 lote 5 · rua portugal 582 · sumer park · 3020150346"
      role="combobox" aria-expanded="false" aria-controls="caixaList" aria-autocomplete="list"
      aria-describedby="detectChip">
    <button id="caixaVoz" class="caixa-ic" aria-label="Buscar por voz" style="display:none">🎤</button>
    <button id="caixaClear" class="caixa-ic" aria-label="Limpar busca" style="display:none">×</button>
  </div>
  <div id="caixaList" class="combo-list" role="listbox" aria-label="Sugestões de setor e rua"></div>
  <div id="detectChip" class="detectchip" role="status" aria-live="polite" tabindex="0"
       aria-label="Toque para corrigir o modo de busca detectado" hidden></div>
  <div class="chips" id="ambigChips" role="group" aria-label="Escolha o que você quis dizer" hidden></div>
  <!-- menu de correcao: reaproveita .moderow/.modemore existentes, SEM CSS novo -->
  <div id="correctMenu" class="moderow" hidden>
    <button id="cAD" onclick="forceMode('addr')">Endereço</button>
    <button id="cBD" onclick="forceMode('bd')">Prédio</button>
    <button id="cQL" onclick="forceMode('ql')">Quadra/Lote</button>
  </div>
  <button class="modemore" id="cIN" hidden onclick="forceMode('insc')">ou buscar pela inscrição cadastral (CI)</button>

  <!-- paineis de campo (needBairro/paneQL/paneAddr/paneUnit/paneBD/paneIN) PERMANECEM no DOM,
       agora usados so pelo menu de correcao manual (quando o usuario toca no chip e escolhe um
       modo explicitamente) — nao mais visiveis por padrao; setMode() continua controlando-os. -->
  ...(needBairro/paneQL/paneAddr/paneUnit/paneBD/paneIN inalterados, so a visibilidade padrao muda)...

  <button class="go" id="btnGo" onclick="buscar()">Buscar</button>
  <p class="hint">💡 ou <b>clique direto no mapa</b> sobre o imóvel para identificar o lote</p>
  <button class="caixabtn" id="btnCaixa" onclick="toggleCaixa()" style="display:none">🏦 Oportunidades Caixa (<span id="caixaN">0</span>)</button>
</div>
<div class="results" id="results">
  <div class="empty" id="emptyState">
    <div class="big">Encontre qualquer imóvel de Goiânia</div>
    Veja na hora o <b>preço estimado</b>, gere um <b>laudo em PDF</b> e descubra <b>oportunidades da Caixa</b> por perto. Toque num exemplo para começar:
    <div class="chips examplechips" id="exampleChips" role="group" aria-label="Exemplos de busca">
      <button data-example="quadra 128 lote 5">quadra 128 lote 5</button>
      <button data-example="rua portugal 582">rua portugal 582</button>
      <button data-example="sumer park">sumer park</button>
      <button data-example="3020150346">3020150346</button>
    </div>
  </div>
</div>
```

CSS NOVO necessario (so o que o UI-SPEC pede que NAO existe ainda — `.caixa`/`.caixa-input`/`.caixa-ic`/`.detectchip`):
reusar tokens `--paper/--paper-2/--ink/--line/--muted/--accent` (ja existem em `:root`); dimensoes/fontes conforme
08-UI-SPEC.md secoes Typography/Spacing/Color. `.detectchip` >=44px altura, `role=status`. `.examplechips` reusa
`.chips`/`.chips button` JA EXISTENTE (linha ~323-326) — zero CSS novo para os chips em si.

Contrato de `detectMode` (08-03-SUMMARY.md tem o contrato exato — ler antes de integrar): retorna
`{mode, confidence, label, ...camposParsed, bairroCode?, bairroDisp?}`. `confidence` in {"alta","media","baixa"}.
Quando `mode===null` (confidence baixa), a UI mostra `#ambigChips`, NUNCA dispara busca no Enter.

forceMode(m) — NOVA funcao (nao existe hoje): abre o modo manualmente a partir do menu de correcao, equivalente
a `setMode(m)` + foca o campo relevante + fecha o `#correctMenu`. Reusa `setMode()` (API interna preservada,
SEARCH.md secao 6) — nao duplica logica de troca de paineis.

Debounce ~150ms (SEARCH.md secao 2, secao 7 riscos: "filterCombo nao tem debounce hoje — o campo-unico deve
introduzir debounce desde o primeiro commit"). Aplicar no listener de `input` do `#caixaInput`, chamando
`detectMode()` + atualizando o dropdown (`filterCombo`-like unificado) no MESMO debounce (nao dois debounces
separados).

CNEFE lazy-load (BUSCA-10): `fetch("logradouros-goiania.json")` SO no primeiro `focus` do `#caixaInput` (nao no
boot — precache do sw.js radar-v6 ja cobre offline). Guardar um flag modulo (`let CNEFE=null, cnefeLoading=false;`)
para nao disparar fetch duplicado; usar `SEARCHTOKEN`-like disciplina (token proprio ou o global) se a resposta
puder chegar fora de ordem por digitacao rapida — documentar a escolha no codigo.

DOWNEV/pointerdown (preservar 1:1, radar-goiania.html linha ~2239-2240): o fechamento do `#caixaList` no
iOS deve seguir o MESMO padrao (`pointerdown` nao `click`) — estender o listener global existente para tambem
fechar `#caixaList`/`#ambigChips`/`#correctMenu` quando o toque for fora deles, sem criar um segundo listener
`DOWNEV` (usar o mesmo).

Esc (preservar ordem, 08-UI-SPEC.md Acessibilidade): dropdown -> menu de correcao -> chips de desambiguacao ->
(ordem existente wizard->chooser->detail inalterada por baixo). Estender o handler `keydown` Escape EXISTENTE
(radar-goiania.html ~2179-2186), nao criar um segundo.

focusFirstField() (preservar/adaptar, SEARCH.md secao 6): com campo unico, so ha 1 campo real por padrao (a
caixa) — ao abrir o menu de correcao manualmente e escolher um modo, focar o PRIMEIRO campo visivel do painel
correspondente (comportamento herdado, reusar a funcao existente chamando-a de dentro de `forceMode`).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HTML/CSS da caixa unica + chip + menu de correcao + exemplos tocaveis (substitui moderow/.empty)</name>
  <files>radar-goiania.html</files>
  <action>
Substituir o HTML da `.moderow`/`.modemore`/combo dedicado (linhas ~506-514) pela estrutura NOVA de `<facts>`
(caixa unica + `#detectChip` + `#ambigChips` + `#correctMenu` reaproveitando `.moderow`/`.modemore` + link `#cIN`).
Os paineis `#needBairro`/`#paneQL`/`#paneAddr`/`#paneUnit`/`#paneBD`/`#paneIN` PERMANECEM no DOM (usados pelo menu
de correcao manual), so a visibilidade padrao muda (ficam ocultos por padrao — `setMode()` continua controlando-os
quando o usuario forca um modo).

Substituir o `.empty` estatico (linha ~561) pela versao com `#exampleChips` (4 botoes tocaveis, texto exato do
placeholder — ver `<facts>`/08-UI-SPEC.md Copywriting Contract).

Adicionar CSS NOVO minimo (`.caixa`, `.caixa-input`, `.caixa-ic`, `.detectchip`) na secao `<style>`, reusando
EXCLUSIVAMENTE as variaveis `--paper/--paper-2/--ink/--line/--muted/--accent` ja definidas em `:root` — conforme
08-UI-SPEC.md (tokens/spacing/typography). `.examplechips` NAO precisa de CSS novo (reusa `.chips`/`.chips button`
ja existente). Alvos >=44px em: `.caixa` (altura), `#caixaVoz`, `#caixaClear`, cada item de `#ambigChips`/
`#exampleChips`, cada botao do `#correctMenu` (ja herda `.moderow button{padding:11px 2px}` + a regra mobile
`padding:13px 2px;min-height:44px` — confirmar que continua se aplicando com os novos IDs). Zero `transition:` novo.

NAO remover `setMode()`/`focusFirstField()` — permanecem como API interna (chamadas por `forceMode`, Task 2).
  </action>
  <verify>
    <automated>node --check radar-goiania.html && python -c "
s=open('radar-goiania.html',encoding='utf-8').read()
for needle in ['id=\"caixaInput\"','id=\"detectChip\"','id=\"ambigChips\"','id=\"correctMenu\"','id=\"exampleChips\"','role=\"status\"','aria-live=\"polite\"']:
    assert needle in s, needle+' ausente'
assert 'class=\"moderow\"' in s, 'moderow (reaproveitado no menu de correcao) ausente'
import re
newcss = re.search(r'\.caixa\{[^}]*\}', s)
assert newcss, 'CSS .caixa ausente'
assert '#' not in newcss.group(0), 'hex literal novo detectado em .caixa (deve usar var(--...))'
print('OK: HTML/CSS da caixa unica presente, moderow reaproveitado, sem hex novo em .caixa')
"</automated>
  </verify>
  <done>caixaInput/detectChip/ambigChips/correctMenu/exampleChips presentes no HTML; .moderow reaproveitado (sem duplicar CSS); CSS novo so usa var(--...) (zero hex literal); alvos >=44px conforme UI-SPEC; node --check passa.</done>
</task>

<task type="auto">
  <name>Task 2: Wiring — debounce+detectMode, chip tocavel/menu de correcao, desambiguacao, setor-ultimo, Esc/pointerdown estendidos</name>
  <files>radar-goiania.html</files>
  <action>
1. Listener `input` em `#caixaInput` com debounce ~150ms (mesmo padrao de `clearTimeout`/`setTimeout` ja usado no
   combo de setor, linha ~2220-2221): chama `detectMode(valor, COMBO)` (08-03) e atualiza `#detectChip`/`#ambigChips`
   conforme `confidence`. Mostra/esconde `#caixaClear` conforme `value` vazio/nao-vazio. Se `confidence==="baixa"`
   (mode null), popula `#ambigChips` com os chips de desambiguacao (Rua N / Quadra N / Inscricao N... se >=5 dig)
   e ESCONDE `#detectChip`; senao popula `#detectChip` com o `label` e ESCONDE `#ambigChips`.

2. `#detectChip` tocavel (click + Enter/Space via `tabindex="0"` ja no HTML): abre `#correctMenu`/`#cIN` (remove
   `hidden`), sincroniza `aria-pressed` nos 3 botoes conforme o modo detectado. Cada botao do `#correctMenu` chama
   uma NOVA funcao `forceMode(m)` que: chama `setMode(m)` (preservado), preenche os campos do painel correspondente
   com os valores JA PARSEADOS pelo ultimo resultado de `detectMode` (ex.: quadra/lote/rua/numero/predio/insc),
   fecha `#correctMenu`, e chama `focusFirstField()` (preservado).

3. Chips de `#ambigChips`: cada botao, ao tocar, seta o modo+campos explicitamente (mesmo padrao de `forceMode`)
   E chama `buscar()` direto (nao precisa redigitar — SEARCH.md secao 3.4). Chips de `#exampleChips`: ao tocar,
   preenche `#caixaInput.value` com o texto do exemplo, dispara o MESMO fluxo do listener de input (chama a funcao
   de deteccao/normalizacao diretamente, nao precisa esperar o debounce) e chama `buscar()` no mesmo toque.

4. Setor-ultimo (BUSCA-05): quando `detectMode` NAO extrair setor da frase (campo `bairroCode` ausente no
   resultado) E o modo exigir setor (`ql`/`addr`/`bd`), chamar `getLastBairroCode()` (08-03); se houver valor,
   aplicar via `pickBairro()` SEM disparar busca sozinho, e mostrar no chip o formato EXATO "Setor · {Nome}
   (último)" (nunca omitido — sempre tocavel para trocar, reabrindo o combo de setor existente dentro do painel
   correspondente).

5. Lazy-load CNEFE (BUSCA-10): no primeiro `focus` de `#caixaInput`, se `logradouros-goiania.json` ainda nao
   carregado, `fetch` (guardado por flag de modulo para nao duplicar) e popular uma estrutura em memoria para o
   dropdown unificado (Task 3 consome). Aplicar disciplina de concorrencia: se a digitacao mudar antes da resposta
   chegar, nao sobrescrever um estado mais novo (usar token dedicado ou o padrao ja documentado no 08-CONTEXT).

6. Estender (NAO duplicar) o listener `DOWNEV`/`pointerdown` existente (linha ~2239-2240) para tambem fechar
   `#caixaList`/`#ambigChips`/`#correctMenu` quando o toque for fora deles. Estender (NAO duplicar) o handler
   `keydown` Escape existente (linha ~2179-2186) na ordem: dropdown (`#caixaList`) -> `#correctMenu` -> `#ambigChips`
   -> (ordem existente por baixo inalterada).

7. Boot (`document.body.dataset.view=...` em diante, linha ~2159+): remover a chamada a `setMode(MODE)` daquele
   ponto SE ela sincronizava a moderow antiga (substituir por uma inicializacao equivalente da caixa unica, ex.:
   estado vazio/idle) — `setMode` em si permanece definida e usada por `forceMode`.
  </action>
  <verify>
    <automated>node --check radar-goiania.html && python -c "
s=open('radar-goiania.html',encoding='utf-8').read()
for needle in ['function forceMode','detectMode(','getLastBairroCode','radar_lastbairro','logradouros-goiania.json']:
    assert needle in s, needle+' ausente'
assert s.count('addEventListener(DOWNEV') <= 2, 'DOWNEV deve ser estendido, nao duplicado em excesso'
assert s.count('key!==\"Escape\"') <= 1, 'handler de Escape deve continuar unico (estendido, nao duplicado)'
print('OK: forceMode/detectMode/getLastBairroCode/CNEFE lazy-load presentes; DOWNEV/Escape nao duplicados')
"</automated>
  </verify>
  <done>Debounce ~150ms chama detectMode; chip tocavel abre menu de correcao (forceMode); desambiguacao dispara busca no toque; setor-ultimo nunca silencioso (chip "(último)"); CNEFE lazy-load no primeiro focus; DOWNEV/Escape estendidos (nao duplicados); node --check passa.</done>
</task>

<task type="auto">
  <name>Task 3: Dropdown unificado (Setor+Rua CNEFE) com rotulo de tipo, teclado, iOS</name>
  <files>radar-goiania.html</files>
  <action>
Criar a funcao de filtro unificado (equivalente a `filterCombo`, mas combinando `COMBO` (setor) + os dados CNEFE
lazy-loaded na Task 2) que popula `#caixaList` com itens intercalados por relevancia (mesma logica de ordenacao
por posicao do termo ja usada em `filterCombo` — nao dois blocos separados). Cada item usa a estrutura do
08-UI-SPEC.md Componente 4: `<span class="code">Setor</span>` ou `<span class="code">Rua</span>` como rotulo de
tipo (reuso de `.combo-item .code` — SEM CSS novo), `id="opt-<code-unico>"`, `role="option"`, `aria-selected`.

Navegacao por teclado (ArrowUp/ArrowDown/Enter/Escape) no `#caixaInput`: replicar 1:1 o padrao ja implementado no
combo de setor (`aria-activedescendant` sincronizado, `comboActive`-like local ao novo componente — pode reusar
a MESMA logica generica, extraindo-a para uma funcao compartilhada se isso nao aumentar o escopo desproporcionalmente,
ou duplicar o padrao pequeno como o app ja faz em outros combos — decisao a criterio do executor, documentar no
SUMMARY). Selecionar um item de tipo "Setor" chama `pickBairro(code)` (existente); selecionar um item de tipo "Rua"
preenche o modo Endereco (`rua` implicito) e fecha o dropdown, deixando o usuario confirmar com Enter/Buscar.

Fechamento no iOS: usar o MESMO `DOWNEV`/pointerdown estendido na Task 2 (nao criar um terceiro listener).
Vazio: `.combo-empty` generalizado para "nenhuma sugestão" (copy ajustada conforme 08-UI-SPEC.md, cobrindo
setor+rua na mesma mensagem).
  </action>
  <verify>
    <automated>node --check radar-goiania.html && python -c "
s=open('radar-goiania.html',encoding='utf-8').read()
assert 'caixaList' in s
assert 'aria-activedescendant' in s
assert 'nenhuma sugest' in s.lower(), 'copy generalizada de vazio ausente'
assert 'role=\"option\"' in s
print('OK: dropdown unificado com teclado/aria-activedescendant/copy generalizada presentes')
"</automated>
  </verify>
  <done>Dropdown unificado (#caixaList) mistura Setor+Rua com rotulo de tipo (.code); navegacao por teclado com aria-activedescendant sincronizado; fecha por pointerdown no iOS (DOWNEV reusado, nao triplicado); copy de vazio generalizada; node --check passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| texto livre do usuario -> caixa unica -> detectMode/buscar() | input client-side, mesmo caminho ja auditado (buscar()/WHERE building preservados integralmente) |
| #caixaInput (focus) -> fetch logradouros-goiania.json | leitura de asset estatico precache (sw.js radar-v6), sem autenticacao, sem escrita |
| detectMode/pickBairro -> localStorage (radar_lastbairro) | so o code do setor, nao-PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Tampering | substituicao da moderow pode remover acessibilidade ja conquistada (foco/aria/pointerdown) se algum widget novo nao seguir o padrao | mitigate | Verify de cada task confirma role/aria-live/aria-activedescendant/DOWNEV reusado; menu de correcao reaproveita 1:1 o HTML/CSS ja testado de .moderow/.modemore (nao recria) |
| T-08-09 | Denial of Service (UX) | fetch duplicado de logradouros-goiania.json em digitacao rapida | mitigate | Flag de modulo (cnefeLoading) + disciplina de token para nao sobrescrever estado mais novo com resposta antiga |
| T-08-10 | Information Disclosure | setor assumido do localStorage aplicado silenciosamente levaria a busca errada sem o usuario perceber | mitigate | Chip "Setor · Nome (último)" e obrigatorio e verificado por copy exata — nunca omitido, sempre tocavel para trocar |
| T-08-11 | Injection (XSS) | labels de Setor/Rua no dropdown renderizados via innerHTML | mitigate | Reusar `esc()` (ja existente, usado em filterCombo) para qualquer texto vindo de COMBO/CNEFE antes de interpolar no HTML do item |

**Sem ameacas de alta severidade.** Mudanca de UI client-side sobre dado publico ja auditado; nenhum novo caminho de rede sensivel, nenhum dado pessoal novo.
</threat_model>

<verification>
- node --check radar-goiania.html passa em todas as 3 tasks
- Caixa unica substitui moderow como padrao; menu de correcao reaproveita .moderow/.modemore sem CSS novo
- Chip de confirmacao sempre visivel com texto; "135" isolado nunca dispara busca automatica (chips de desambiguacao)
- Setor-ultimo nunca silencioso (chip "(último)" sempre visivel/tocavel)
- Dropdown unificado Setor+Rua com rotulo de tipo, teclado, pointerdown iOS
- Exemplos tocaveis substituem .empty narrativo
- Zero hex novo, zero transition novo, alvos >=44px
- DOWNEV/Escape estendidos (nao duplicados) — preserva o padrao de fechamento existente
</verification>

<success_criteria>
- Caixa unica funcional cobrindo BUSCA-03 (chip), BUSCA-04 (setor na frase), BUSCA-05 (setor lembrado), BUSCA-06 (desambiguacao), BUSCA-08 (exemplos tocaveis), BUSCA-10 (CNEFE lazy), BUSCA-11 (a11y preservada)
- Zero regressao de acessibilidade/iOS em relacao ao estado pos-auditoria 03/07
</success_criteria>

<output>
Apos completar, criar `.planning/phases/08-busca-unica-inteligente/08-04-SUMMARY.md`.
Incluir no SUMMARY: screenshots/descricao do fluxo final (idle -> typing -> detected -> ambiguo -> erro), a decisao tomada na Task 3 sobre extrair ou duplicar a logica de teclado do combo, e uma lista explicita dos pontos de a11y verificados manualmente (mesmo que o verify automatizado ja cubra parte) para apoiar a verificacao de a11y da 08-05.
</output>
