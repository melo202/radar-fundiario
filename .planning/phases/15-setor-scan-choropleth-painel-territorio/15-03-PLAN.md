---
phase: 15-setor-scan-choropleth-painel-territorio
plan: 03
type: execute
wave: 3
depends_on: ["15-01", "15-02"]
files_modified:
  - radar-goiania.html
autonomous: true
requirements: [TERR-03, TERR-01]

must_haves:
  truths:
    - "A partir de um setor em foco (destacado/buscado), 'Ver território' dispara territorioScan e abre o painel com mediana + Q1-Q3 de R$/m², IPTU mediano, idade mediana e mix de uso"
    - "Cada métrica derivada da amostra exibe o rótulo 'Amostra de N de M lotes' (honestidade estatística, nunca omitido)"
    - "O painel termina com ação (lei da tela): primária liga/desliga o choropleth (estado espelhado na legenda), secundária busca lote no setor, 'Ver metodologia' explica quantis+amostra+viés OBJECTID"
    - "Abrir Painel + ligar Choropleth do mesmo setor dispara UMA varredura (dedupe); o orçamento paginado é ≤3 requisições, verificável por contador instrumentado"
    - "O painel entra na cadeia de Esc/foco (1 sheet por vez) e nunca vaza dado do endpoint sem esc()"
  artifacts:
    - path: "radar-goiania.html"
      provides: "#terrPanel (.detail), botão 'Ver território', montarPainel(), rodapé de ação, 2 estágios MOTION_MSG, contador de orçamento exposto"
      contains: "id=\"terrPanel\""
  key_links:
    - from: "botão 'Ver território'"
      to: "territorioScan(cdbairro) → montarPainel"
      via: "resolveCdbairroDeLayer do setor em foco, com loading MOTION_MSG"
      pattern: "territorioScan"
    - from: "botão primário do painel"
      to: "toggleChoropleth (Plano 02)"
      via: "estado aria-pressed espelhado entre painel e legenda"
      pattern: "toggleChoropleth"
    - from: "#terrPanel"
      to: "cadeia de Escape (keydown 4860) + closeDetail"
      via: "fecho por × e Esc devolve foco ao gatilho"
      pattern: "terrPanel"
---

<objective>
Entregar o Painel do Meu Território (TERR-03) e fechar o loop do orçamento de TERR-01: botão "Ver território" a partir do setor em foco → `territorioScan` compartilhado → painel com métricas (mediana + Q1-Q3 de R$/m², IPTU mediano, idade mediana do cadastro, mix de uso, nº de lotes), rótulo de honestidade por métrica, rodapé de ação (liga/desliga choropleth sincronizado com a legenda, buscar lote no setor, ver metodologia) e verificação instrumentada do orçamento (≤3 requisições paginadas, contador exposto + passos de verificação ao vivo no Bueno).

Purpose: fecha a experiência do território — o corretor abre o painel, lê o "resumo do setor" conclusão-primeiro e liga o calor de valor, tudo sob o orçamento de requisições que protege o endpoint frágil.
Output: `radar-goiania.html` com `#terrPanel`, entrada de UI, render de métricas, ações e instrumentação de orçamento.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-setor-scan-choropleth-painel-territorio/15-CONTEXT.md
@.planning/phases/15-setor-scan-choropleth-painel-territorio/15-RESEARCH.md
@.planning/phases/15-setor-scan-choropleth-painel-territorio/15-UI-SPEC.md
@.planning/phases/15-setor-scan-choropleth-painel-territorio/15-01-SUMMARY.md
@.planning/phases/15-setor-scan-choropleth-painel-territorio/15-02-SUMMARY.md

<interfaces>
<!-- Do Plano 15-01: -->
  async function territorioScan(cdbairro) → {lotes,total,amostrado,paginas,cdbairro}
  function estatTerritorio(lotes,total) → {n,total,medianaPm2,q1Pm2,q3Pm2,iptuMediano,anoMediano,mix,breaks}
  function rotuloAmostra(n,total) → "Amostra de N de M lotes"
  let __terrReq  // contador de requisições paginadas do último scan (bloco TERR_NET)

<!-- Do Plano 15-02: -->
  function toggleChoropleth()  // liga/desliga, sincroniza aria-pressed de terrToggle E terrPanelToggle
  function aplicarChoropleth(scan) → st  // preenche TERR_LOTE_BIN/TERR_SETOR_ATIVO, retorna estatTerritorio
  function montarLegenda(st)
  function resolveCdbairroDeLayer(layer) → cdbairro | undefined
  async function carregarLookupCdbairro()
  let CHOROPLETH_ON

<!-- Já em produção em radar-goiania.html: -->
  .detail (CSS 334-410) — bottom-sheet mobile / painel flutuante desktop; .dgrid .cell (358-362) grade de métricas; .dvalor-v peso 800; .dtag (355) mono --lot-bright; .x fechar (338); .maisopcoes (393) accordion; .chips/.seg
  <div class="detail" id="detail" ...> (HTML 939), function closeDetail() (4747)
  const MOTION_MSG={localizando,cadastro,estimativa,comparaveis,mapa} (2552) — objeto literal fixo, nunca interpolado
  function loading(on,msg) (2548) ; function toast(msg) (2546)
  keydown Escape chain (4860): onbOverlay → cmpSheet → negSheet → ... (sheet mais recente tem prioridade; 1 sheet por vez)
  breadcrumb (HTML 923) + #crumbBairro ; highlightBairro(layer) (2266) / drillBairro(layer) (2291) — layer em foco tem feature.properties.id ; baiHi = layer destacado atual (2184)
  esc(v) (1124) ; brl(n) (1118)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Painel #terrPanel + entrada "Ver território" + render de métricas (conclusão-primeiro)</name>
  <files>radar-goiania.html</files>
  <read_first>
    - radar-goiania.html linhas 334-410 (CSS .detail/.dgrid/.dtag/.dvalor-v/.maisopcoes/.chips), 923-966 (HTML breadcrumb + #detail), 2266-2321 (highlightBairro/drillBairro/baiHi), 2546-2558 (loading/toast/MOTION_MSG), 4747-4764 (closeDetail), 4860-4875 (cadeia Escape)
    - .planning/phases/15-setor-scan-choropleth-painel-territorio/15-UI-SPEC.md (Componente 2: Painel #terrPanel — cabeçalho, ordem das métricas, rótulo de honestidade, abertura; Componente 3: Loading; Copywriting Contract; Acessibilidade)
    - .planning/phases/15-setor-scan-choropleth-painel-territorio/15-CONTEXT.md (métricas do painel TERR-03)
  </read_first>
  <action>
    1. HTML: adicionar `#terrPanel` com `class="detail"` (novo id, mesmo z-500 herdado — nunca abre junto com #detail, regra "1 sheet por vez"), após `#detail` (~966). Cabeçalho: `<h3>Meu Território</h3>` + `<span class="dtag" id="terrSetorNome"></span>` (nome do setor via textContent/esc) + botão `.x` `onclick="fecharTerrPanel()"` `aria-label="Fechar painel do território"`. Corpo: container `#terrGrid` (`.dgrid`) para as células de métrica. Rodapé: `#terrActions` (preenchido na Task 2). `.maisopcoes` com `#terrMetodologia`.
    2. MOTION_MSG: adicionar 2 chaves ao objeto literal (2552), mesma gramática (maiúscula inicial, gerúndio, reticências): `varrendo:"Varrendo setor…", faixas:"Calculando faixas de valor…"`.
    3. Entrada de UI: adicionar botão "Ver território" `id="btnVerTerr"` no breadcrumb (junto a `#crumbBairro`, ~923), `hidden` por padrão; exibir quando houver setor em foco (chamar dentro de `highlightBairro`/`drillBairro`: `mostrarVerTerr(layer)` que só mostra se `resolveCdbairroDeLayer(layer)` existir — gleba sem cdbairro nunca mostra). `async function abrirTerritorio(layer){ await carregarLookupCdbairro(); const cd=resolveCdbairroDeLayer(layer||baiHi); if(cd==null){toast("Este setor não tem dado fiscal para o território.");return;} closeDetail(); loading(true,MOTION_MSG.varrendo); try{ const scan=await territorioScan(cd); loading(true,MOTION_MSG.faixas); const st=aplicarChoropleth(scan); montarPainel(scan,st,layer); loading(false);}catch(e){ loading(false); toast("Não foi possível varrer o setor. Verifique a conexão e tente de novo."); } }` (zoom-gate: só dispara nesta ação explícita, nunca em pan/zoom).
    4. `function montarPainel(scan,st,layer){ ... }` — preenche `#terrSetorNome` (nome do setor via `esc()`), monta `#terrGrid` na ORDEM fixa (UI-SPEC, conclusão-primeiro), cada bloco `.cell` com rótulo de honestidade `rotuloAmostra(st.n,st.total)` (mono muted) abaixo:
       (1) R$/m² mediano — número-âncora `.dvalor-v` (`brl(st.medianaPm2)`) + "Q1 "+brl(st.q1Pm2)+" · Q3 "+brl(st.q3Pm2);
       (2) IPTU mediano — brl(st.iptuMediano);
       (3) Idade mediana do cadastro — "desde "+st.anoMediano (ou "N anos" a partir do ano atual); nunca data crua;
       (4) Mix de uso — chips `.chips`/`.seg` com st.mix (top-3 + "Outros"), label+% via textContent;
       (5) Nº de lotes — total real (scan.total) + amostra usada.
       Empty states (UI-SPEC): sem setor → "Nenhum setor selecionado"; setor sem venal (st.n===0) → "Sem dado de valor neste setor". Abrir o painel: `document.getElementById("terrPanel").classList.add("show")`.
    5. `function fecharTerrPanel(){ document.getElementById("terrPanel").classList.remove("show"); /* devolve foco ao #btnVerTerr */ }` e registrar `#terrPanel` na cadeia keydown Escape (4860) **como um dos checks de sheet — DEPOIS do check do onboarding (`#onbOverlay`, prioridade máxima, regra da Fase 13), inserido entre os checks de sheet seguindo a regra "sheet mais recente fecha primeiro"** (fechar com Esc devolve foco ao gatilho `#btnVerTerr`).
  </action>
  <acceptance_criteria>
    - `grep -q 'id="terrPanel"' radar-goiania.html` e `grep -q 'class="detail"' radar-goiania.html` (reuso, não .wiz)
    - `grep -q "Meu Território" radar-goiania.html` e `grep -q 'id="btnVerTerr"' radar-goiania.html` e `grep -q "Ver território" radar-goiania.html`
    - `grep -q "varrendo:" radar-goiania.html` e `grep -q "faixas:" radar-goiania.html` (2 estágios em MOTION_MSG) e `grep -q "Varrendo setor…" radar-goiania.html` e `grep -q "Calculando faixas de valor…" radar-goiania.html`
    - `grep -q "function montarPainel" radar-goiania.html` e `grep -q "function abrirTerritorio" radar-goiania.html` e `grep -q "function fecharTerrPanel" radar-goiania.html`
    - Rótulo de honestidade presente no render: `grep -c "rotuloAmostra(st.n,st.total)\|rotuloAmostra(" radar-goiania.html` ≥ 1
    - Nome do setor via esc/textContent (nunca innerHTML cru): `grep -q "terrSetorNome\").textContent\|textContent=.*esc(" radar-goiania.html`
    - #terrPanel na cadeia Escape: `grep -q "terrPanel" radar-goiania.html` dentro do handler de keydown (verificar contexto)
    - `npm test` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>#terrPanel (.detail) abre por "Ver território" a partir do setor em foco, disparando territorioScan compartilhado + loading em 2 estágios; métricas na ordem conclusão-primeiro com rótulo de honestidade por métrica; nome do setor via esc(); painel na cadeia de Esc; npm test verde.</done>
</task>

<task type="auto">
  <name>Task 2: Rodapé de ação (lei da tela) + toggle sincronizado + verificação instrumentada do orçamento</name>
  <files>radar-goiania.html</files>
  <read_first>
    - radar-goiania.html linhas 393-399 (.maisopcoes), 3447 (padrão "Mais opções") e o #terrPanel/#terrActions criados na Task 1
    - Plano 15-02 (toggleChoropleth, terrPanelToggle, CHOROPLETH_ON) — @.planning/phases/15-setor-scan-choropleth-painel-territorio/15-02-SUMMARY.md
    - .planning/phases/15-setor-scan-choropleth-painel-territorio/15-UI-SPEC.md (Copywriting Contract: CTAs, error/empty states; Rodapé de ação)
    - .planning/phases/15-setor-scan-choropleth-painel-territorio/15-RESEARCH.md (Pitfall 4 viés OBJECTID; Validation Architecture — verificação AO VIVO do orçamento)
  </read_first>
  <action>
    1. Rodapé `#terrActions` (lei da Fase 10 — 1 primária + até 2 secundárias):
       - Primária `id="terrPanelToggle"`: label alterna "Colorir mapa por valor" (quando `!CHOROPLETH_ON`) / "Desligar cor por valor" (quando ligado); `aria-pressed` espelha a legenda; `onclick="toggleChoropleth();sincTerrPanelToggle()"`. `function sincTerrPanelToggle(){ const b=document.getElementById("terrPanelToggle"); if(!b)return; b.textContent=CHOROPLETH_ON?"Desligar cor por valor":"Colorir mapa por valor"; b.setAttribute("aria-pressed",String(CHOROPLETH_ON)); }` (chamar também dentro de toggleChoropleth p/ manter fonte única de verdade — nunca dois botões simultâneos).
       - Secundária "Buscar lote no setor" `onclick="buscarNoSetor()"` — leva à busca com o setor pré-preenchido (reusa o fluxo de busca por setor já existente; preencher o campo de setor com o nome/cdbairro em foco e focar a caixa de busca).
       - "Ver metodologia" dentro de `.maisopcoes` (`#terrMetodologia`): texto determinístico explicando (a) faixas = quantis relativos ao setor sobre a amostra; (b) rótulo "Amostra de N de M lotes"; (c) **nota de viés (Pitfall 4)**: "As faixas usam os primeiros lotes do cadastro por ordem de registro (OBJECTID) — pode sub/sobre-representar parte do setor; é uma leitura da amostra, não do setor inteiro." Sem jargão na 1ª camada ("quantil" só aqui).
    2. Empty/error states (UI-SPEC Copywriting): erro de varredura já tratado na Task 1 (toast §26.3); varredura parcial → mensagem "Não foi possível concluir a varredura — os números abaixo usam só as páginas que chegaram. Toque para tentar de novo." quando `scan.paginas<3 && scan.amostrado<scan.total` por falha (best-effort, não bloqueante).
    3. Verificação instrumentada do orçamento (TERR-01, critério de aceite literal do ROADMAP): expor o contador do bloco TERR_NET para debug — `window.__terrReq` acessível (ou `function terrUltimoOrcamento(){return __terrReq;}` exposto no escopo global) e um comentário-âncora `/* VERIFICAÇÃO AO VIVO ORÇAMENTO: abrir painel/choropleth do Bueno (cdbairro=16) com DevTools Network aberto; contar requisições a MapServer/3/query — deve ser ≤3 paginadas + 1 returnCountOnly; __terrReq reflete as paginadas */`. A verificação de campo em 4G real permanece HUMAN-UAT não-bloqueante (phase flag do ROADMAP).
  </action>
  <acceptance_criteria>
    - `grep -q "Colorir mapa por valor" radar-goiania.html` e `grep -q "Desligar cor por valor" radar-goiania.html` e `grep -q 'id="terrPanelToggle"' radar-goiania.html`
    - `grep -q "function sincTerrPanelToggle" radar-goiania.html` e `grep -q "Buscar lote no setor" radar-goiania.html` e `grep -q "function buscarNoSetor" radar-goiania.html`
    - `grep -q "Ver metodologia" radar-goiania.html` e a nota de viés OBJECTID presente: `grep -q "OBJECTID" radar-goiania.html`
    - Contador de orçamento exposto/comentado: `grep -q "__terrReq" radar-goiania.html` e `grep -q "VERIFICAÇÃO AO VIVO ORÇAMENTO" radar-goiania.html`
    - `grep -q "Não foi possível concluir a varredura" radar-goiania.html`
    - `npm test` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Rodapé com ação primária de choropleth (label/aria espelhados com a legenda, botão único alternado), "Buscar lote no setor" e "Ver metodologia" (com nota de viés OBJECTID); contador de orçamento exposto + âncora de verificação ao vivo; estados de erro/parcial cobertos; npm test verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados do scan (endpoint) → DOM do painel | nome do setor, métricas e mix de uso renderizados no painel |
| ação do usuário → territorioScan | abrir painel / ligar choropleth dispara varredura (custo no endpoint frágil) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Information Disclosure / XSS | montarPainel (nome do setor + métricas no DOM) | mitigate | nome do setor via `textContent`/`esc()`; métricas numéricas via `brl()` (número puro); mix de uso via textContent — nunca `innerHTML` com dado cru do endpoint |
| T-15-08 | Denial of Service (endpoint frágil) | abrirTerritorio / toggleChoropleth | mitigate | Painel + Choropleth do mesmo setor reusam a MESMA varredura (dedupe do TERRCACHE, Plano 01); zoom-gate (só ação explícita); orçamento ≤3 páginas verificável por `__terrReq`; HUMAN-UAT de 4G não-bloqueante |
| T-15-09 | Tampering (regressão de UX/foco) | cadeia de Escape + closeDetail | mitigate | `#terrPanel` entra na cadeia keydown com prioridade "sheet mais recente"; regra "1 sheet por vez" (fecha #detail antes de abrir); fecho por × e Esc devolve foco ao `#btnVerTerr` |
</threat_model>

<verification>
- `npm test` verde (sem regressão)
- Painel abre por ação explícita (zoom-gate), com métricas na ordem conclusão-primeiro + honestidade por métrica
- Botão primário e legenda com estado sincronizado (fonte única toggleChoropleth)
- Contador `__terrReq` exposto; verificação ao vivo do orçamento documentada (âncora + passos DevTools)
- Manual: HUMAN-UAT não-bloqueante — orçamento ≤3 requisições no Bueno ao vivo (contagem no Network) e tempo de scan em 4G real
</verification>

<success_criteria>
- Painel do Meu Território com mediana + Q1-Q3 R$/m², IPTU mediano, idade mediana, mix de uso, nº de lotes — cada um com rótulo de amostra
- Rodapé de ação (liga/desliga choropleth sincronizado, buscar lote no setor, ver metodologia com nota de viés)
- Orçamento ≤3 requisições paginadas verificável por contador instrumentado (+ HUMAN-UAT de campo)
- Zero regressão: suíte completa verde; painel na cadeia de Esc/foco
</success_criteria>

<output>
Após completar, criar `.planning/phases/15-setor-scan-choropleth-painel-territorio/15-03-SUMMARY.md`
</output>
