---
phase: 15-setor-scan-choropleth-painel-territorio
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - radar-goiania.html
  - tests/territorio.test.mjs
  - tests/fixtures.mjs
autonomous: true
requirements: [TERR-01, TERR-03]
user_setup: []

must_haves:
  truths:
    - "R$/m² de um lote é calculado com guarda (areaedif se edificado, senão areaterr; null quando falta vlvenal ou área) — nunca divide por zero nem trata 0/null como dado real"
    - "Mediana + Q1-Q3 de R$/m², IPTU mediano, ano mediano de cadastro e mix de uso saem de funções puras testadas sobre uma amostra"
    - "territorioScan(cdbairro) dispara UMA varredura mesmo com Painel+Choropleth abrindo juntos (dedupe da promise em voo) e nunca passa de 3 requisições paginadas por scan"
    - "A allowlist de campos da varredura nunca inclui dtnascimen (LGPD); o resultado passa por sanitiza() nos dois caminhos de outFields"
    - "Se o endpoint recusar outFields restrito (d.error), a varredura cai automaticamente para outFields=* sem travar"
  artifacts:
    - path: "radar-goiania.html"
      provides: "Funções puras de estatística de território dentro do bloco RADAR_PURE + territorioScan/territorioScanRun/fetchWhereRestrito no bloco TERR_NET"
      contains: "function estatTerritorio"
    - path: "tests/territorio.test.mjs"
      provides: "Harness node:test das funções puras (slice RADAR_PURE) + teste async de dedupe/orçamento (slice TERR_NET com jsonp mockado)"
      contains: "loadPureBlock"
    - path: "tests/fixtures.mjs"
      provides: "Fixtures de lotes sintéticos (areaedif null vs 0, dtinclusao válido/sentinela/ausente, mix >3 usos)"
      contains: "TERR_FIX"
  key_links:
    - from: "tests/territorio.test.mjs"
      to: "radar-goiania.html bloco RADAR_PURE_START/END"
      via: "html.indexOf('RADAR_PURE_START') slice por linha"
      pattern: "RADAR_PURE_START"
    - from: "territorioScan(cdbairro)"
      to: "fetchWhereRestrito + jsonp(returnCountOnly)"
      via: "Promise.all dentro de territorioScanRun, com TERRCACHE dedupe"
      pattern: "TERRCACHE"
---

<objective>
Construir a fundação testável da Fase 15: (1) as funções puras de estatística de território (R$/m² por lote, quantis de amostra, mediana/Q1-Q3, IPTU mediano, ano mediano do cadastro, mix de uso, rótulo de honestidade) dentro do bloco `RADAR_PURE`; e (2) a função compartilhada `territorioScan(cdbairro)` com cache de sessão, dedupe de chamada em voo, orçamento HARD de ≤3 requisições paginadas e fallback automático de `outFields`.

Purpose: TERR-01 (varredura compartilhada com orçamento) e a parte estatística de TERR-03 são a base de tudo que os Planos 02 (choropleth) e 03 (painel) consomem. Sem essa camada testada, choropleth e painel não teriam de onde tirar cor nem número.
Output: bloco RADAR_PURE expandido + bloco TERR_NET novo em `radar-goiania.html`; `tests/territorio.test.mjs` novo; fixtures novas em `tests/fixtures.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-setor-scan-choropleth-painel-territorio/15-CONTEXT.md
@.planning/phases/15-setor-scan-choropleth-painel-territorio/15-RESEARCH.md
@.planning/phases/15-setor-scan-choropleth-painel-territorio/15-VALIDATION.md

<interfaces>
<!-- Contratos já em produção em radar-goiania.html — usar diretamente, sem explorar. -->

Bloco testável (linhas 1135-2011):
  // RADAR_PURE_START  ... funções puras ...  // RADAR_PURE_END
  Já contém: scoreOportunidade (1323), statusDeUnidade (1999), brlSimples (1428).
  Toda função pura NOVA de território nasce DENTRO deste intervalo.

Helpers já disponíveis no mesmo <script> global:
  const brl = n => n==null?"—":n.toLocaleString("pt-BR",{style:"currency",currency:"BRL",maximumFractionDigits:0}); // 1118
  const USO = {0:"—",1:"Residencial",2:"Comercial/Serviço",3:"Industrial",4:"Público/Institucional",5:"Misto",6:"Territorial"}; // 1117
  const fmtDt = (s)=>... ; // 1128 — valida ^\d{8}$ e sentinela "00000000" (retorna null se inválido)
  quant(sorted,p) // 3758, FORA do bloco RADAR_PURE — usar como REFERÊNCIA de fórmula, copiar dentro do bloco

Rede (reusar, não reimplementar):
  async function jsonp(params, retries=1, url=SVC)  // 2052 — retry único, pausa 900ms, timeout 30s
  async function fetchWhere(where, maxPages=30)      // 2587 — paginação resultOffset/2000, orderByFields:"OBJECTID", chama sanitiza() no retorno
  function sanitiza(arr)  // remove SENS=["dtnascimen","cpf","cnpj","nmcontrib","nmproprie","nome"] (1126)
  function capCache(o,max) // 2186 — teto por ordem de inserção
  const CMPCACHE={}; // 3757 — padrão de cache+dedupe a clonar (getComps 3760-3776)

Guarda estabelecida (nunca divide por zero):
  const v=a.vlvenal||0, ae=a.areaedif||0; (v>0&&ae>0)?v/ae:null   // 1913, 2856

Loader de teste a replicar (tests/scores.test.mjs:13-37): slice POR LINHA
  const iStart=html.indexOf("RADAR_PURE_START"); const start=html.indexOf("\n",iStart)+1;
  const iEnd=html.indexOf("RADAR_PURE_END"); const end=html.lastIndexOf("\n",iEnd);
  const src=html.slice(start,end);  // + vm.Script(src + ";globalThis.__exports={...}")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Funções puras de estatística de território no bloco RADAR_PURE</name>
  <files>radar-goiania.html, tests/territorio.test.mjs, tests/fixtures.mjs</files>
  <read_first>
    - radar-goiania.html linhas 1113-1135 (SVC/USO/SENS/brl/fmtDt) e 1135-2011 (bloco RADAR_PURE, ponto de inserção) e 3758 (fórmula quant como referência)
    - tests/scores.test.mjs linhas 1-45 (padrão loadPureBlock a replicar)
    - tests/fixtures.mjs (formato de fixture)
    - .planning/phases/15-setor-scan-choropleth-painel-territorio/15-RESEARCH.md (Pitfall 3 bloco RADAR_PURE, Pitfall 5 dtinclusao)
    - .planning/phases/15-setor-scan-choropleth-painel-territorio/15-UI-SPEC.md (Copywriting Contract — rótulo "Amostra de {N} de {M} lotes")
  </read_first>
  <behavior>
    RED primeiro (tests/territorio.test.mjs + fixtures em tests/fixtures.mjs), depois GREEN (funções dentro de RADAR_PURE). Casos:
    - pm2Lote({vlvenal:300000,areaedif:100}) === 3000 (edificado usa areaedif)
    - pm2Lote({vlvenal:200000,areaedif:0,areaterr:400}) === 500 (areaedif=0 → cai p/ areaterr; 0 NÃO é dado)
    - pm2Lote({vlvenal:0,areaterr:400}) === null (sem venal)
    - pm2Lote({vlvenal:100000,areaedif:null,areaterr:null}) === null (sem área)
    - quantilAmostra([10,20,30,40], .5) === 25 (interpolação linear, mesma fórmula de quant())
    - breaksQuantil de amostra conhecida retorna 4 pontos de corte crescentes (quintis internos)
    - binQuantil(breaks, v): mínimo do setor → 1, máximo → 5, monotônico, cobre bordas exatas
    - anoMedianoCadastro filtra dtinclusao inválido/sentinela "00000000"/ausente ANTES da mediana (não entra como 0/NaN)
    - mixUso agrupa: com >3 usos presentes, retorna top-3 por % + linha "Outros" somando o resto; usa rótulos de USO
    - estatTerritorio(lotes,total) devolve {n,total,medianaPm2,q1Pm2,q3Pm2,iptuMediano,anoMediano,mix,breaks} coerentes
    - rotuloAmostra(6000,57225) === "Amostra de 6.000 de 57.225 lotes"; rotuloAmostra(1842,1842) === "Amostra de 1.842 de 1.842 lotes" (nunca omitido)
  </behavior>
  <action>
    Inserir DENTRO do bloco `RADAR_PURE_START`/`RADAR_PURE_END` (1135-2011 — o bloco cresce, nunca duplica noutro lugar; funções fora dele são invisíveis ao loader de teste, Pitfall 3):

    1. `function pm2Lote(a){const v=+a.vlvenal||0; const ae=+a.areaedif||0; const at=+a.areaterr||0; const area=ae>0?ae:at; return (v>0&&area>0)?v/area:null;}` — reusa a guarda estabelecida (1913/2856): areaedif se >0, senão areaterr; null se faltar venal ou área.
    2. `function quantilAmostra(sorted,p){...}` — cópia da fórmula de `quant()` (3758): `const i=p*(sorted.length-1),lo=Math.floor(i),hi=Math.ceil(i);` interpolação linear entre vizinhos. (Cópia deliberada dentro do bloco — mesma fórmula da Fase 9, consistência de "mediana" no app inteiro.)
    3. `function breaksQuantil(sortedPm2){...}` → array de 4 cortes internos [p20,p40,p60,p80] via quantilAmostra (5 faixas discretas). Retorna null se sortedPm2.length < 5.
    4. `function binQuantil(breaks,v){...}` → inteiro 1..5 monotônico: v<=breaks[0]→1, ..., v>breaks[3]→5. Cobre bordas (mínimo→1, máximo→5).
    5. `function anoMedianoCadastro(lotes){...}` — extrai ano de `dtinclusao` SÓ quando bate `^\d{8}$` e não é `"00000000"` (mesma validação de fmtDt, Pitfall 5); descarta inválidos ANTES; mediana dos anos válidos (null se nenhum válido).
    6. `function mixUso(lotes){...}` — conta por `uso` mapeado via `USO`, ordena desc, retorna `[{label,pct},...]` top-3 + `{label:"Outros",pct}` somando o resto (só inclui "Outros" se houver >3 categorias).
    7. `function estatTerritorio(lotes,total){...}` — deriva pm2 válidos (pm2Lote, filtra null), ordena, retorna `{n:validos.length,total,medianaPm2,q1Pm2,q3Pm2,iptuMediano,anoMediano,mix,breaks}` (iptuMediano = mediana de `vlimp98` válidos>0).
    8. `function rotuloAmostra(n,total){return "Amostra de "+(n).toLocaleString("pt-BR")+" de "+(total).toLocaleString("pt-BR")+" lotes";}` — copy exata do UI-SPEC, nunca omitido nem quando n===total.

    Criar `tests/territorio.test.mjs` replicando `loadPureBlock()` de scores.test.mjs (slice por linha de RADAR_PURE_START/END), com `assert.ok(src.includes("function estatTerritorio"),...)` e exportando as 8 funções em `globalThis.__exports`. Criar as fixtures `TERR_FIX` em `tests/fixtures.mjs` cobrindo os casos do bloco behavior (areaedif null vs 0 vs ausente; dtinclusao válido/"00000000"/ausente; mix com >3 usos).
  </action>
  <acceptance_criteria>
    - `grep -c "function pm2Lote\|function quantilAmostra\|function breaksQuantil\|function binQuantil\|function anoMedianoCadastro\|function mixUso\|function estatTerritorio\|function rotuloAmostra" radar-goiania.html` retorna 8
    - As 8 funções estão fisicamente entre `RADAR_PURE_START` e `RADAR_PURE_END`: `awk '/RADAR_PURE_START/,/RADAR_PURE_END/' radar-goiania.html | grep -c "function estatTerritorio"` retorna 1
    - `grep -c "dtnascimen\|nmproprie\|dtnascimen" ` NÃO aparece em nenhuma função nova (nenhum campo sensível referenciado)
    - `grep -q "TERR_FIX" tests/fixtures.mjs` sucesso
    - `node --test tests/territorio.test.mjs` exit 0
    - `npm test` exit 0 (≥108 testes + os novos, todos verdes)
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>As 8 funções puras existem dentro do bloco RADAR_PURE, testadas por tests/territorio.test.mjs com fixtures cobrindo null-vs-0, sentinela de data e mix >3; npm test verde.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: territorioScan compartilhado — cache, dedupe, orçamento ≤3 páginas, fallback de outFields</name>
  <files>radar-goiania.html, tests/territorio.test.mjs</files>
  <read_first>
    - radar-goiania.html linhas 2052-2070 (jsonp), 2587-2599 (fetchWhere — ponto de extensão), 2186 (capCache), 3757-3776 (CMPCACHE/getComps — padrão de dedupe), 2500 (coerção +cdbairro/+code em WHERE)
    - .planning/phases/15-setor-scan-choropleth-painel-territorio/15-RESEARCH.md (Pattern 1 dedupe, Pattern 2 amostra paginada, Pitfall 1 outFields fallback, Security Domain V5)
    - .planning/phases/15-setor-scan-choropleth-painel-territorio/15-CONTEXT.md (orçamento HARD ≤3, allowlist de campos)
  </read_first>
  <behavior>
    Teste async em tests/territorio.test.mjs, slice do bloco TERR_NET com `jsonp`/`sanitiza`/`capCache`/`toast` STUBADOS no sandbox (contam chamadas). Casos:
    - Dedupe: `Promise.all([territorioScan(16),territorioScan(16)])` executa UMA varredura — o stub de jsonp é chamado o mesmo nº de vezes que uma única chamada (2 chamadas concorrentes reusam a MESMA promise em voo).
    - Orçamento: um scan de setor grande (stub devolve sempre page cheia de 2000) faz no MÁXIMO 3 chamadas paginadas (fetchWhereRestrito para em maxPages=3) + no máximo 1 chamada returnCountOnly → contador de páginas ≤3.
    - Fallback: quando a 1ª página restrita devolve `{error:{code:400}}`, a varredura reinicia automaticamente com `outFields:"*"` e conclui sem lançar; o resultado ainda passou por sanitiza (stub verifica que sanitiza foi chamado).
    - Coerção: `territorioScan("16; DROP")` monta WHERE com `+cdbairro` → `cdbairro=16 AND vlvenal>0` (NaN vira erro/aborta, nunca injeta string crua).
  </behavior>
  <action>
    Criar um bloco novo delimitado por marcadores `// TERR_NET_START` / `// TERR_NET_END` (colocar logo após `fetchWhere`, ~linha 2599) contendo — para que o teste possa fatiá-lo e rodar com dependências stubadas:

    1. `const TERR_FIELDS="vlvenal,areaedif,areaterr,vlimp98,dtinclusao,uso,cdbairro,nrquadra,nrlote,ci,nrinscr,x_coord,y_coord";` — allowlist EXATA de TERR-01 (nunca `dtnascimen`).
    2. `let __terrReq=0;` (contador de requisições paginadas por scan, exposto p/ debug/verificação ao vivo — reset no início de cada `territorioScanRun`).
    3. `const TERRCACHE={};`
    4. `async function fetchWhereRestrito(where,fields,maxPages){...}` — clone de `fetchWhere` mas com `outFields:fields`; incrementa `__terrReq` a cada `jsonp`; se qualquer página devolver `d.error` → **fallback automático**: recomeça o loop UMA vez com `outFields:"*"` (páginas já contadas resetam p/ o fallback); SEMPRE retorna `sanitiza(all.map(f=>f.attributes))` (defesa em profundidade LGPD nos dois caminhos).
    5. `async function territorioScanRun(cdbairro){ __terrReq=0; const where="cdbairro="+(+cdbairro)+" AND vlvenal>0"; const [lotes,totalD]=await Promise.all([fetchWhereRestrito(where,TERR_FIELDS,3), jsonp({where,returnCountOnly:"true",f:"json"})]); const total=(totalD&&totalD.count)||lotes.length; return {lotes,total,amostrado:lotes.length,paginas:__terrReq,cdbairro:+cdbairro}; }`
    6. `async function territorioScan(cdbairroRaw){ const cdbairro=+cdbairroRaw; if(!Number.isFinite(cdbairro))throw new Error("cdbairro inválido"); const key=String(cdbairro); capCache(TERRCACHE,30); if(!TERRCACHE[key])TERRCACHE[key]=territorioScanRun(cdbairro).catch(err=>{delete TERRCACHE[key];throw err;}); return TERRCACHE[key]; }` — dedupe: guarda a PROMISE (não o resultado); teto 30 (menor que CMPCACHE=150, território é pesado por entrada).

    No teste, adicionar um segundo loader `loadNetBlock()` que fatia entre `TERR_NET_START`/`TERR_NET_END`, injeta stubs (`jsonp`, `sanitiza`, `capCache`, `toast`) via sandbox e roda os casos do bloco behavior contando chamadas de jsonp e páginas.
  </action>
  <acceptance_criteria>
    - `grep -q 'const TERR_FIELDS="vlvenal,areaedif,areaterr,vlimp98,dtinclusao,uso,cdbairro,nrquadra,nrlote,ci,nrinscr,x_coord,y_coord"' radar-goiania.html` sucesso
    - `grep -c "TERR_NET_START\|TERR_NET_END" radar-goiania.html` retorna 2
    - `awk '/TERR_NET_START/,/TERR_NET_END/' radar-goiania.html | grep -qE "function territorioScan\b" ` sucesso e o mesmo bloco contém `TERRCACHE`, `fetchWhereRestrito`, `+cdbairro`, `__terrReq`, `outFields:"*"` (fallback) e `returnCountOnly`
    - `grep -c "dtnascimen" radar-goiania.html` NÃO aumentou vs baseline (campo sensível nunca requisitado; confirmar TERR_FIELDS não o contém)
    - `node --test tests/territorio.test.mjs` exit 0 com os testes de dedupe (1 varredura), orçamento (≤3 páginas) e fallback verdes
    - `npm test` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>territorioScan(cdbairro) existe no bloco TERR_NET com cache+dedupe (1 varredura para chamadas concorrentes), orçamento ≤3 páginas paginadas, fallback automático outFields restrito→*, coerção +cdbairro e sanitiza() nos dois caminhos; testes async verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → endpoint ArcGIS (portalmapa.goiania.go.gov.br) | `cdbairro` interpolado em WHERE cruza para o servidor de terceiro |
| endpoint → app (payload do cadastro) | dados do cadastro fiscal (potencialmente com campos pessoais) voltam para a memória do app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | fetchWhereRestrito / territorioScanRun (payload do cadastro) | mitigate | allowlist `TERR_FIELDS` EXCLUI `dtnascimen`+demais SENS (nunca requisitado); `sanitiza()` aplicado nos DOIS caminhos de outFields (restrito e fallback `*`) — defesa em profundidade contra allowlist mal-editada |
| T-15-02 | Tampering | territorioScan (WHERE clause) | mitigate | coerção numérica `+cdbairro` + `Number.isFinite` antes de interpolar (ASVS V5, mesmo padrão das linhas 2500/2631) — string crua nunca entra no WHERE |
| T-15-03 | Denial of Service (contra o endpoint frágil da Prefeitura) | territorioScanRun / TERRCACHE | mitigate | orçamento HARD ≤3 páginas paginadas por scan (fetchWhereRestrito maxPages=3) + cache por `cdbairro` (TERRCACHE, teto 30) + dedupe da promise em voo (Painel+Choropleth = 1 varredura) |
</threat_model>

<verification>
- `npm test` verde (≥108 baseline + novos testes de território)
- Funções puras dentro de RADAR_PURE (grep + awk de intervalo)
- territorioScan com dedupe, orçamento e fallback comprovados por teste async com stubs
- Nenhum campo sensível (`dtnascimen`) na allowlist nem em função nova
</verification>

<success_criteria>
- 8 funções puras de estatística testadas dentro do bloco RADAR_PURE
- territorioScan(cdbairro) compartilhado: cache + dedupe + orçamento ≤3 páginas + fallback outFields + coerção + sanitiza()
- tests/territorio.test.mjs cobre estatística (slice RADAR_PURE) e rede (slice TERR_NET com mock)
- Zero regressão: suíte completa verde
</success_criteria>

<output>
Após completar, criar `.planning/phases/15-setor-scan-choropleth-painel-territorio/15-01-SUMMARY.md`
</output>
