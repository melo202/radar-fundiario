---
phase: 15-setor-scan-choropleth-painel-territorio
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - radar-goiania.html
  - sw.js
  - bairro-cdbairro.json
autonomous: true
requirements: [TERR-02]
user_setup: []

must_haves:
  truths:
    - "Cada lote da amostra do setor escaneado é pintado pela sua faixa de R$/m² (Q1-Q5) quando o choropleth está ligado; lotes de setores não escaneados continuam neutros"
    - "O polígono do setor escaneado recebe um wash sutil quando o choropleth está ligado (leitura no zoom de visão-geral); nunca fillOpacity 0 (hit-area de toque preservada, mínimo .02)"
    - "A troca de cor acontece via setStyle() sobre a geometria já desenhada — nunca recria polígonos"
    - "A legenda mostra os 5 blocos de cor + faixas de R$/m² do setor + rótulo de amostra; tem toggle liga/desliga e recolhimento animado (REDUCE-safe)"
    - "id ↔ cdbairro é resolvido por bairro-cdbairro.json (carregado sob demanda, no precache do sw.js radar-v7); glebas sem cdbairro nunca são coloridas"
  artifacts:
    - path: "bairro-cdbairro.json"
      provides: "Lookup id→{cd,mo} (subconjunto datado de bairros-goiania.recon.json) para runtime"
      contains: "cd"
    - path: "radar-goiania.html"
      provides: "Paleta --terr-qN, extensão de baiStyle()/lotStyle(), desenharChoropleth(), lookups idParaCdbairro/cdbairroParaIds, legenda #terrLegenda"
      contains: "--terr-q1"
    - path: "sw.js"
      provides: "Precache do novo asset + bump de versão radar-v6→radar-v7"
      contains: "radar-v7"
  key_links:
    - from: "lotStyle(ci)"
      to: "TERR_LOTE_BIN (ci→faixa 1-5)"
      via: "consulta o Map preenchido por aplicarChoropleth(scan)"
      pattern: "TERR_LOTE_BIN"
    - from: "baiStyle(feature)"
      to: "idParaCdbairro + TERR_SETOR_ATIVO"
      via: "resolve cdbairro do polígono e checa se o setor está escaneado+ligado"
      pattern: "idParaCdbairro"
    - from: "aplicarChoropleth(scan)"
      to: "bairroLayer.setStyle / lotLayer.eachLayer setStyle"
      via: "troca em massa sem recriar geometria (padrão de setSatelite 2144-2145)"
      pattern: "setStyle"
---

<objective>
Entregar o choropleth de R$/m² (TERR-02): resolver o gap `id↔cdbairro` promovendo um subconjunto de `bairros-goiania.recon.json` a asset de runtime; estender os hooks de estilo `baiStyle()`/`lotStyle()` para pintar por faixa de quantil (5 faixas discretas, azul sequencial da UI-SPEC, legível sobre CARTO e satélite) via `setStyle()`; e construir a legenda tocável `#terrLegenda` com toggle e recolhimento.

Purpose: TERR-02 é o "calor de valor" do território — a camada visual que substitui a cor neutra da malha nos setores já escaneados, populada incrementalmente e honesta (nunca simula dado não coletado).
Output: `bairro-cdbairro.json` novo; `sw.js` com precache+bump; `radar-goiania.html` com paleta, hooks estendidos e legenda.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-setor-scan-choropleth-painel-territorio/15-CONTEXT.md
@.planning/phases/15-setor-scan-choropleth-painel-territorio/15-RESEARCH.md
@.planning/phases/15-setor-scan-choropleth-painel-territorio/15-UI-SPEC.md
@.planning/phases/15-setor-scan-choropleth-painel-territorio/15-01-SUMMARY.md

<interfaces>
<!-- Do Plano 15-01 (já mergeado): -->
  function estatTerritorio(lotes,total) → {n,total,medianaPm2,q1Pm2,q3Pm2,iptuMediano,anoMediano,mix,breaks}
  function breaksQuantil(sortedPm2) → [p20,p40,p60,p80] (4 cortes internos) | null
  function binQuantil(breaks,v) → 1..5
  function pm2Lote(a) → R$/m² | null
  function rotuloAmostra(n,total) → "Amostra de N de M lotes"
  async function territorioScan(cdbairro) → {lotes,total,amostrado,paginas,cdbairro}   // bloco TERR_NET

<!-- Já em produção em radar-goiania.html: -->
  :root (linha 24) já tem --paper-2:#f2eee4, --lot-bright:#3f7a63, --accent:#b5451f, família --status-* (55-59)
  const BAI_STYLE = {pane:"bairros",color:_BAI_LINE,weight:.6,fillColor:_BAI_LINE,fillOpacity:.02,opacity:.4,...}; // 2165
  const LOT_STYLE = {pane:"lots",color:"#2c5545",weight:1,fillColor:"#2c5545",fillOpacity:.05,...}; // 2153
  function baiStyle(feature){ ...z/t... satelliteOn? {...} : {...} }  // 2170 — branch satelliteOn (hoje fillOpacity:0 sobre satélite)
  function lotStyle(){ return satelliteOn? {...LOT_STYLE,weight:1.5,fillOpacity:0} : LOT_STYLE; } // 2179 — HOJE sem argumento
  // Troca em massa sem recriar (setSatelite 2144-2145):
  if(bairroLayer)bairroLayer.setStyle(baiStyle);
  if(lotLayer&&lotLayer.eachLayer)lotLayer.eachLayer(p=>p.setStyle(lotStyle()));
  // refreshLots (2237-2249) cria cada lote: const ci=clean(f.attributes.ci); L.polygon(...,lotStyle());
  //   mouseout: poly.setStyle(lotStyle());
  const _baiCss=getComputedStyle(document.documentElement); // 2162 — Canvas NÃO resolve var(); resolver hex 1x
  function mAnimate(el,keyframes,opts) // 2036 — REDUCE-safe (null se prefers-reduced-motion)
  brl(n) // 1118 ; brlSimples(n) // 1428 (formato "R$ 1,2 mil") ; esc(v) // 1124
  #pinoLegenda (HTML 932, CSS .pino-legenda 308 z-390, hidden por padrão) — padrão de legenda a espelhar

<!-- sw.js: const CACHE="radar-v6" (12); LOCAL[] (13-23) inclui ./bairros-goiania.json -->
<!-- bairros-goiania.recon.json: {id:{cdbairro,nmbairro_reconciled,motivo,nm_bai_original}} — 1.205 entradas; motivo sem-parcela = gleba sem cdbairro -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promover id↔cdbairro a asset de runtime + sw.js radar-v7 + lookups no boot</name>
  <files>bairro-cdbairro.json, sw.js, radar-goiania.html</files>
  <read_first>
    - bairros-goiania.recon.json (formato das entradas + motivo)
    - sw.js linhas 1-30 (CACHE, LOCAL, install)
    - radar-goiania.html linha 2311-2321 (loadBairroPolys — onde os polígonos entram, ponto natural do fetch lazy)
    - .planning/phases/15-setor-scan-choropleth-painel-territorio/15-RESEARCH.md (Pattern 4 gap id↔cdbairro, Pitfall 2, Open Question 2)
  </read_first>
  <action>
    1. Gerar `bairro-cdbairro.json` como SNAPSHOT datado a partir de `bairros-goiania.recon.json` (não editar à mão): manter só `id → {cd:<cdbairro>, mo:<motivo>}` e DESCARTAR entradas sem `cdbairro` (as `sem-parcela`/glebas — nunca coloríveis). Usar node one-liner determinístico:
       `node -e "const r=require('./bairros-goiania.recon.json');const o={};for(const[id,v]of Object.entries(r))if(v.cdbairro!=null)o[id]={cd:v.cdbairro,mo:v.motivo};require('fs').writeFileSync('bairro-cdbairro.json',JSON.stringify(o));"`
       (Manter `mo` para o sinal de honestidade — ~65% dos joins são heurísticos `nome`/`maioria`.)
    2. `sw.js`: adicionar `"./bairro-cdbairro.json"` ao array `LOCAL`; bump `const CACHE="radar-v6"` → `"radar-v7"` (Pitfall 12 — o bump é o que invalida a cópia antiga do cache).
    3. `radar-goiania.html`: adicionar lookups em memória `let idParaCdbairro=null, cdbairroParaIds=null;` e `async function carregarLookupCdbairro(){ if(idParaCdbairro)return; try{const r=await fetch('bairro-cdbairro.json');const j=await r.json(); idParaCdbairro=new Map(); cdbairroParaIds=new Map(); for(const[id,v]of Object.entries(j)){ idParaCdbairro.set(id,v.cd); if(!cdbairroParaIds.has(v.cd))cdbairroParaIds.set(v.cd,[]); cdbairroParaIds.get(v.cd).push(id);} }catch(e){console.warn('bairro-cdbairro.json:',e);} }` — **fetch LAZY** (chamado só na 1ª ação de território, nunca no boot, coerente com zoom-gate). `resolveCdbairroDeLayer(layer)` = `idParaCdbairro && idParaCdbairro.get(layer.feature.properties.id)` (undefined p/ gleba → nunca colore).
  </action>
  <acceptance_criteria>
    - `test -f bairro-cdbairro.json` e `node -e "const o=require('./bairro-cdbairro.json');const ks=Object.keys(o);if(ks.length<1000)process.exit(1);if(o['000400000014'].cd!==16)process.exit(1)"` exit 0 (Bueno id→cd=16 presente)
    - `grep -q '"./bairro-cdbairro.json"' sw.js` e `grep -q 'radar-v7' sw.js` e `! grep -q 'radar-v6' sw.js` (versão bumpada, não duplicada)
    - `grep -c "idParaCdbairro\|cdbairroParaIds\|carregarLookupCdbairro" radar-goiania.html` ≥ 4
    - `grep -q "fetch('bairro-cdbairro.json')\|fetch(\"bairro-cdbairro.json\")" radar-goiania.html` sucesso
    - `npm test` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>bairro-cdbairro.json gerado (glebas descartadas, Bueno=16), no precache do sw.js radar-v7; lookups id↔cdbairro construídos por fetch lazy; npm test verde.</done>
</task>

<task type="auto">
  <name>Task 2: Paleta --terr-qN + extensão de baiStyle()/lotStyle() para choropleth via setStyle()</name>
  <files>radar-goiania.html</files>
  <read_first>
    - radar-goiania.html linhas 24-59 (:root, --status-*), 2153-2183 (LOT_STYLE/BAI_STYLE/baiStyle/lotStyle), 2144-2145 (setStyle em massa), 2209-2249 (refreshLots — criação de lotes com ci)
    - .planning/phases/15-setor-scan-choropleth-painel-territorio/15-UI-SPEC.md (Paleta do Choropleth — 5 hex + -ink, opacidade por fundo, borda, piso fillOpacity)
    - .planning/phases/15-setor-scan-choropleth-painel-territorio/15-CONTEXT.md (choropleth: quantis discretos, setStyle, incremental, fillOpacity nunca 0)
  </read_first>
  <action>
    1. Adicionar ao `:root` (mesmo bloco de `--status-*`, ~linha 55), EXATO da UI-SPEC:
       `--terr-q1:#c6dbef;--terr-q1-ink:#7fa8c2;--terr-q2:#9ecae1;--terr-q2-ink:#5f8fab;--terr-q3:#6baed6;--terr-q3-ink:#3f6f91;--terr-q4:#3182bd;--terr-q4-ink:#1f4d75;--terr-q5:#08519c;--terr-q5-ink:#062f47;`
    2. Resolver os hex para JS (Canvas 2D não resolve var(), mesmo motivo de `_BAI_LINE` 2162), perto de BAI_STYLE:
       `const TERR_COLORS={1:"#c6dbef",2:"#9ecae1",3:"#6baed6",4:"#3182bd",5:"#08519c"};`
       `const TERR_INK={1:"#7fa8c2",2:"#5f8fab",3:"#3f6f91",4:"#1f4d75",5:"#062f47"};`
       `const TERR_FILLOP_CARTO={1:.15,2:.18,3:.20,4:.22,5:.25};`
       `const TERR_FILLOP_SAT={1:.35,2:.38,3:.40,4:.42,5:.45};`
       `const _TERR_SAT_LINE=(_baiCss.getPropertyValue("--paper-2").trim()||"#f2eee4");` (traço claro sobre satélite)
    3. Estado global: `let CHOROPLETH_ON=false; let TERR_LOTE_BIN=new Map(); const TERR_SETOR_ATIVO=new Set(); let TERR_BREAKS=null;`
    4. Estender `lotStyle(ci)` (adicionar parâmetro): se `CHOROPLETH_ON && ci!=null && TERR_LOTE_BIN.has(ci)` → `const b=TERR_LOTE_BIN.get(ci); return {pane:"lots", fillColor:TERR_COLORS[b], fillOpacity: satelliteOn?TERR_FILLOP_SAT[b]:TERR_FILLOP_CARTO[b], color: satelliteOn?_TERR_SAT_LINE:TERR_INK[b], weight: satelliteOn?1.5:1, bubblingMouseEvents:false};` senão o retorno neutro atual. Atualizar as 2 chamadas em `refreshLots` (criação `L.polygon(...,lotStyle(ci))` e `mouseout` `poly.setStyle(lotStyle(ci))`) para passar o `ci` já em escopo; guardar `poly._terrCi=ci;`.
    5. Estender `baiStyle(feature)`: resolver `const cd=feature&&resolveCdbairroDeLayer? resolveCdbairroDeLayer({feature}):null;` — se `CHOROPLETH_ON && cd!=null && TERR_SETOR_ATIVO.has(cd)` → wash sutil do setor ativo com `fillColor:TERR_COLORS[3]` (cor-média = "setor colorido/ativo"; a variação real por faixa vive no nível de lote) e `fillOpacity: satelliteOn?TERR_FILLOP_SAT[3]:TERR_FILLOP_CARTO[3]` (**nunca 0**, inclusive sobre satélite — piso preservado); senão o branch neutro atual (idle/satélite). Manter a rampa `t=(z-12)/4` de weight já existente.
    6. `function aplicarChoropleth(scan){ const st=estatTerritorio(scan.lotes,scan.total); TERR_BREAKS=st.breaks; TERR_LOTE_BIN=new Map(); if(TERR_BREAKS){ for(const a of scan.lotes){ const p=pm2Lote(a); if(p!=null&&a.ci!=null)TERR_LOTE_BIN.set(clean(a.ci),binQuantil(TERR_BREAKS,p)); } } TERR_SETOR_ATIVO.add(scan.cdbairro); return st; }` e `function desenharChoropleth(){ if(bairroLayer)bairroLayer.setStyle(baiStyle); if(lotLayer&&lotLayer.eachLayer)lotLayer.eachLayer(p=>p.setStyle(lotStyle(p._terrCi))); }` (padrão de troca em massa de setSatelite — nunca recria geometria).
  </action>
  <acceptance_criteria>
    - `grep -c "\-\-terr-q1:#c6dbef\|--terr-q5:#08519c\|--terr-q3:#6baed6" radar-goiania.html` ≥ 3
    - `grep -q "const TERR_COLORS=" radar-goiania.html` e `grep -q "const TERR_FILLOP_CARTO=" radar-goiania.html` e `grep -q "const TERR_FILLOP_SAT=" radar-goiania.html`
    - `grep -q "let CHOROPLETH_ON=false" radar-goiania.html` e `grep -q "TERR_LOTE_BIN" radar-goiania.html` e `grep -q "function aplicarChoropleth" radar-goiania.html` e `grep -q "function desenharChoropleth" radar-goiania.html`
    - `grep -q "function lotStyle(ci)" radar-goiania.html` e `grep -c "lotStyle(ci)" radar-goiania.html` ≥ 2 (criação + mouseout em refreshLots)
    - Piso preservado: nenhuma faixa de choropleth usa fillOpacity 0 — `grep -q "TERR_FILLOP_CARTO={1:.15" radar-goiania.html`
    - `npm test` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Paleta --terr-qN no :root + mapas JS resolvidos; lotStyle(ci)/baiStyle() consultam TERR_LOTE_BIN/TERR_SETOR_ATIVO com opacidade por fundo (nunca 0); aplicarChoropleth/desenharChoropleth trocam a cor via setStyle sem recriar geometria; npm test verde.</done>
</task>

<task type="auto">
  <name>Task 3: Legenda #terrLegenda — toggle, swatches R$/m², recolhimento REDUCE-safe, rótulo de amostra</name>
  <files>radar-goiania.html</files>
  <read_first>
    - radar-goiania.html linhas 308-313 (CSS .pino-legenda), 932 (HTML #pinoLegenda), 2036 (mAnimate), 1118/1428 (brl/brlSimples), 1124 (esc)
    - .planning/phases/15-setor-scan-choropleth-painel-territorio/15-UI-SPEC.md (Componente 1: Legenda do Choropleth — posição, estrutura, estados, a11y, z-index)
  </read_first>
  <action>
    1. HTML: adicionar `#terrLegenda` como container ancorado no mapa (após `#pinoLegenda`, ~linha 932), `hidden` por padrão, `aria-label="Legenda de valor por m² no mapa"`. Duas zonas: (a) cabeçalho com botão toggle `id="terrToggle"` texto "Cor por valor" (`aria-pressed="false"`, ≥44×44) + chevron `id="terrChevron"` (`aria-label="Recolher legenda"`, `aria-expanded="true"`, ≥44×44); (b) corpo `id="terrBody"` com 5 `<span class="terr-swatch">` (`background:var(--terr-qN);border:1px solid var(--terr-qN-ink)`) cada um com um rótulo de faixa em R$/m² adjacente (texto ao lado, nunca só cor — a11y), seguido de `#terrAmostra` (rótulo de honestidade).
    2. CSS `.terr-legenda` (position:absolute; top:calc(16px + env(safe-area-inset-top)); right:12px; z-index:395; background:var(--paper-2); ...), `.terr-legenda[hidden]{display:none}`, `.terr-swatch{width:24px;height:14px;...}` (escala 4px: gap xs=4px entre swatches, sm=8px), label mono 10.5px/700. Não sobrepor `.searchpill`/`#breadcrumb` (z-400) nem `.sattoggle`.
    3. JS: `function montarLegenda(st){ const b=st.breaks; const lbls=b? ["≤ "+brlSimples(b[0]), brlSimples(b[0])+"–"+brlSimples(b[1]), brlSimples(b[1])+"–"+brlSimples(b[2]), brlSimples(b[2])+"–"+brlSimples(b[3]), "≥ "+brlSimples(b[3])] : []; ...preenche cada swatch+label via textContent/esc...; document.getElementById("terrAmostra").textContent=rotuloAmostra(st.n,st.total); document.getElementById("terrLegenda").hidden=false; }` (nunca innerHTML com dado do endpoint — usar textContent/esc). `function recolherLegenda(){ const body=document.getElementById("terrBody"),ch=document.getElementById("terrChevron"); const open=ch.getAttribute("aria-expanded")==="true"; ch.setAttribute("aria-expanded",!open); ch.setAttribute("aria-label",open?"Expandir legenda":"Recolher legenda"); if(!open){body.hidden=false;mAnimate(body,{opacity:[0,1],height:[0,body.scrollHeight+"px"]},{duration:.18,easing:[0.22,1,0.36,1]});} else {const c=mAnimate(body,{opacity:[1,0]},{duration:.18,easing:[0.22,1,0.36,1]}); if(c&&c.finished)c.finished.then(()=>body.hidden=true).catch(()=>body.hidden=true); else body.hidden=true;} }` (REDUCE-safe: mAnimate retorna null → toggle instantâneo).
    4. `function toggleChoropleth(){ CHOROPLETH_ON=!CHOROPLETH_ON; const p=String(CHOROPLETH_ON); const t=document.getElementById("terrToggle"); if(t)t.setAttribute("aria-pressed",p); const pb=document.getElementById("terrPanelToggle"); if(pb)pb.setAttribute("aria-pressed",p); desenharChoropleth(); document.getElementById("terrLegenda").hidden=!CHOROPLETH_ON; }` — fonte única de verdade do estado ligado/desligado, sincroniza `aria-pressed` da legenda E do botão do painel (Plano 03). Ligar `terrToggle.onclick=toggleChoropleth` e `terrChevron.onclick=recolherLegenda`.
  </action>
  <acceptance_criteria>
    - `grep -q 'id="terrLegenda"' radar-goiania.html` e `grep -q 'id="terrToggle"' radar-goiania.html` e `grep -q 'id="terrChevron"' radar-goiania.html`
    - `grep -c "terr-swatch" radar-goiania.html` ≥ 5 (5 blocos de cor)
    - `grep -q 'aria-pressed' radar-goiania.html` no botão terrToggle e `grep -q 'aria-expanded' radar-goiania.html` no chevron
    - `grep -q "function toggleChoropleth" radar-goiania.html` e `grep -q "function recolherLegenda" radar-goiania.html` e `grep -q "function montarLegenda" radar-goiania.html`
    - Recolhimento usa mAnimate: `grep -q "mAnimate(body" radar-goiania.html`
    - Dado do endpoint na legenda via textContent/esc (nunca innerHTML cru): `grep -q "textContent=rotuloAmostra\|terrAmostra\").textContent" radar-goiania.html`
    - `grep -q "z-index:395" radar-goiania.html`
    - `npm test` exit 0
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>#terrLegenda existe (hidden por padrão), com toggle "Cor por valor" (aria-pressed sincronizado com o painel), 5 swatches + faixas de R$/m² (brlSimples), rótulo de amostra (rotuloAmostra via textContent/esc) e recolhimento REDUCE-safe via mAnimate; z-395 sem conflito; npm test verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dados do scan (endpoint) → DOM da legenda | R$/m², nome de faixa e rótulo de amostra são renderizados na legenda |
| join heurístico (recon) → cor no mapa | `motivo` nome/maioria pode mapear id→cdbairro imperfeito |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering (regressão funcional na malha) | baiStyle/lotStyle/refreshLots (hit-area de toque) | mitigate | `fillOpacity` do choropleth NUNCA 0 (Q1 já em .15/.35; wash do setor usa faixa 3); `id`s/classes/handlers de refreshLots preservados; troca só via setStyle (geometria intacta) — teste manual de toque em bairro colorido e neutro |
| T-15-05 | Information Disclosure / XSS | montarLegenda (dado do endpoint no DOM) | mitigate | rótulos de faixa via `brlSimples()` (número puro) e rótulo de amostra via `textContent`/`esc()` — nunca `innerHTML` com dado cru do endpoint |
| T-15-06 | Spoofing (polígono errado colorido) | resolveCdbairroDeLayer / bairro-cdbairro.json | accept | ~65% dos joins são heurísticos (`motivo` nome/maioria) — risco JÁ aceito pela Fase 7 para o nome; glebas `sem-parcela` (sem cdbairro) NUNCA são coloridas; rótulo de honestidade da amostra comunica a incerteza; escopo de cor por lote (não afirma exatidão de fronteira) |
</threat_model>

<verification>
- `npm test` verde (sem regressão)
- bairro-cdbairro.json no precache do sw.js radar-v7; Bueno id→cd=16
- Choropleth via setStyle (grep confirma nenhum L.geoJSON/L.polygon novo no caminho de recolorir)
- fillOpacity nunca 0 nas faixas; legenda a11y (texto adjacente à cor, aria-pressed/expanded)
- Manual (HUMAN-UAT não-bloqueante): legibilidade AA sobre satélite em luz externa; toque na área do bairro colorido continua funcionando
</verification>

<success_criteria>
- Lotes do setor escaneado pintados por faixa Q1-Q5 quando ligado; neutros quando desligado ou não escaneado
- Setor escaneado recebe wash sutil no polígono (nunca fillOpacity 0)
- Legenda tocável com 5 faixas + R$/m² + amostra, toggle e recolhimento REDUCE-safe
- id↔cdbairro resolvido por asset de runtime versionado (sw.js radar-v7)
- Troca de cor 100% via setStyle — geometria nunca recriada
</success_criteria>

<output>
Após completar, criar `.planning/phases/15-setor-scan-choropleth-painel-territorio/15-02-SUMMARY.md`
</output>
