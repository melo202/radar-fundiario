---
phase: 18-inteligencia-urbanistica-plano-diretor
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - radar-goiania.html
  - tests/pd.test.mjs
autonomous: true
requirements: [PD-03, PD-04]
must_haves:
  truths:
    - "O accordion 'Urbanístico' aparece na ficha entre 'Dados técnicos' e 'Metodologia e fontes'"
    - "Quando CA é conferido, a ficha mostra 'CA básico 1,0x · CA máximo 6,0x'; quando não conferido, mostra só a zona + nota (nenhum número)"
    - "O disclaimer fixo do SEPLANH está SEMPRE presente no accordion resolvido"
    - "Os 5 estados (carregando/resolvido/parcial/rural/erro) renderizam per 18-UI-SPEC"
    - "O 'porquê' do score cita a zona só quando CA conferido; o detector rotula o critério (PD ou fallback) em cada candidato"
  artifacts:
    - path: "radar-goiania.html"
      provides: "renderUrbanisticoUI(a,isRetry) + CSS .urb-* + accordion #dUrbanistico + wiring de score/detector com PD"
      contains: "renderUrbanisticoUI"
  key_links:
    - from: "showDetail(a,ll)"
      to: "renderUrbanisticoUI(a)"
      via: "chamada assíncrona irmã de renderDiffUI, após DCUR=a"
      pattern: "renderUrbanisticoUI"
    - from: "renderUrbanisticoUI"
      to: "pdConsultarLote + PD_DISCLAIMER + esc()"
      via: "consulta lazy da zona, render seguro, disclaimer sempre presente"
      pattern: "PD_DISCLAIMER"
    - from: "detector (lista #terrDetectorList)"
      to: "criterioDetectorPD"
      via: "rótulo do critério por candidato, com consulta barata por centroide de quadra (top-N, cache)"
      pattern: "criterioDetectorPD"
---

<objective>
A seção "Urbanístico" na ficha (PD-03) e a integração VISÍVEL da inteligência do PD no score e no detector (PD-04). O corretor abre a ficha e vê: macrozona, unidade territorial, CA (só quando conferido), altura, badges de contexto e o disclaimer fixo do SEPLANH — em qualquer um dos 5 estados. O score de oportunidade ganha um "porquê" que cita a zona (só quando o CA está conferido) e o detector passa a usar construído÷potencial-do-PD, com fallback rotulado quando o PD não está disponível.

Purpose: transformar o núcleo do plano 01 em UI honesta (REGRA DE OURO no render) e em sinais explicáveis, reusando verbatim o padrão de accordion da Fase 9, `--status-*` da Fase 13 e a lição `esc()`/data-attributes da Fase 16.
Output: `renderUrbanisticoUI` + CSS + markup do accordion em `radar-goiania.html`; wiring de score/detector; testes de guarda (REGRA DE OURO no HTML) verdes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-inteligencia-urbanistica-plano-diretor/18-CONTEXT.md
@.planning/phases/18-inteligencia-urbanistica-plano-diretor/18-RESEARCH.md
@.planning/phases/18-inteligencia-urbanistica-plano-diretor/18-UI-SPEC.md
@.planning/phases/18-inteligencia-urbanistica-plano-diretor/18-01-SUMMARY.md

<read_first>
- radar-goiania.html — accordions `.foot dtecnico`/`.foot dmetodologia` (1126-1137); `#dDiff` markup (1113-1115); `showDetail(a,ll)` (4786), `DCUR=a` (4844), `renderDiffUI(a)` chamado em 4898; `renderDiffUI` (5019) como MODELO de irmã assíncrona; `#dMetodologia` render (4900); detector: `#terrDetectorList` render (3890-3960), `TERR_DETECTOR_CAND`, `detectarSubutilizados`; CSS `.dgrid`/`.cell`/`.k`/`.v`/`.dnote`/`.skel-card`/`.skel-line`/`.foot` (227-235, 401-405, 453); `--status-atencao`/`--status-bom` (Fase 13); `esc()` (1340); funções/constantes do plano 01 (`pdConsultarLote`, `resolverZonaUI`, `potencialConstrutivo`, `criterioDetectorPD`, `PD_DISCLAIMER`, `PD_TABELA_CA`)
- 18-UI-SPEC.md §"Contrato Específico" Componente 1 (accordion, estados a-f, copy verbatim) e Componente 3 (score/detector copy)
</read_first>

<interfaces>
<!-- Do plano 01 (18-01-SUMMARY). Use diretamente. -->
```
async function pdConsultarLote(x, y, ci) -> {estado, macrozona, unidade:{sigla,nome}|null, regra, badges:{aeis,apac,add,eixo,corredor}}
function resolverZonaUI(respostas) -> (mesmo objeto, puro)
function pdRegrasDaZona(sigla) -> entrada de PD_TABELA_CA | null
function potencialConstrutivo(areaterr, sigla) -> Number | null   // só quando conferido:true
function criterioDetectorPD(areaedif, areaterr, potencialPD) -> {razao, criterio:"pd"|"terreno"} | null
const PD_DISCLAIMER  // string fixa do SEPLANH
const PD_TABELA_CA, PD_LAYERS, PD_SVC_BASE, PDCACHE
const DETECTOR_LIMITE = 50
```
Ficha: `DCUR` (imóvel atual), `showDetail(a,ll)`, campos do lote `a.x_coord`/`a.y_coord`/`a.areaterr`/`a.areaedif`/`a.ci`/`a.nrinscr`/`a.nrquadra`/`a.cdbairro`. `toWGS(x,y)`. `esc(s)`. `clean(s)`.

Copy VERBATIM (18-UI-SPEC §Copywriting) — usar exatamente:
- Rótulo CA (conferido): `"CA básico {ca_basico}x · CA máximo {ca_maximo}x"` (formatar número pt-BR: 1.0 → "1,0")
- Nota sem-CA: `"CA e altura desta zona ainda não foram conferidos contra o Anexo oficial da LC 349/2022 — mostramos só a zona ({sigla}), que vem direto do mapa oficial da Prefeitura."`
- Rural (body): `"Este imóvel está em {macrozonaRuralNome} — zona rural do Plano Diretor. Aqui, o Anexo Urbano (CA, altura, unidades territoriais) não se aplica; esta consulta é só para a área urbana da cidade."`
- Erro total: `"Não foi possível consultar o Plano Diretor para este imóvel agora. Toque para tentar de novo."`
- Parcial: `"Algumas informações do Plano Diretor não carregaram — o que aparece acima já é confiável, só pode estar incompleto."` + botão `"Tentar de novo"`
- Loading: `"Consultando o Plano Diretor…"`
- Score porquê (conferido): `"Em {nomeUnidadeTerritorial} — CA básico {ca_basico}x, potencial de {potencial} m²"`
- Detector critério PD: `"Critério: área construída ÷ potencial do Plano Diretor (zona {sigla}, CA básico {ca_basico}x)"`
- Detector critério fallback: `"Critério: área construída ÷ área do terreno (Plano Diretor não disponível para este candidato)"`
- Badges: `"Interesse Social (AEIS)"`, `"Patrimônio Cultural (APAC)"`, `"Desaceleração de Densidade (ADD)"`, `"Eixo de Desenvolvimento"`, `"Corredor de Transporte"`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS .urb-* + markup #dUrbanistico + renderUrbanisticoUI (5 estados) + wiring em showDetail (PD-03)</name>
  <files>radar-goiania.html</files>
  <action>
    (1) CSS novo (junto ao CSS dos accordions, ~linha 453): `.urb-badges`(flex, gap 4px, `flex-wrap`, `hidden` quando vazio), `.urb-badge`(texto+cor, padding 8px, NÃO interativo — sem piso de 44px), `.urb-badge.is-atencao`(fundo/borda `var(--status-atencao)`), `.urb-badge.is-potencial`(fundo/borda `var(--status-bom)`), `.urb-ca`(reusa layout de `.cell`; o número reusa `.dvalor-v` existente), `.urb-rural`, `.urb-retry`(`<button>` nativo, `min-height:44px` — é controle). NENHUMA cor `--zone-*` aqui (essas são do plano 03). Badges reusam `--status-*` existentes; zero cor hard-coded nova.

    (2) Markup: inserir `<details class="foot durbanistico" id="dUrbanistico"><summary>Urbanístico</summary><div class="footbody" id="dUrbBody"></div></details>` IMEDIATAMENTE após `<details class="foot dtecnico">…</details>` e ANTES de `<details class="foot dmetodologia">` (linhas 1132-1134). `<summary>` texto puro "Urbanístico", sem badge/contador (simetria com os outros dois).

    (3) `renderUrbanisticoUI(a, isRetry)` — irmã assíncrona de `renderDiffUI` (MODELO em 5019): guarda `if(!a||!a.x_coord||!a.y_coord){ #dUrbBody hidden; return; }`. Mostra o estado (a) carregando (`.skel-card`/`.skel-line` verbatim) imediatamente; se `isRetry` limpar `PDCACHE[ci]` antes; chama `pdConsultarLote(a.x_coord, a.y_coord, clean(a.ci||a.nrinscr))`; ao resolver, verifica corrida (`if(!DCUR||clean(DCUR.ci||DCUR.nrinscr)!==ci)return;` — igual renderDiffUI 5025) e monta o HTML conforme `estado`:
      - `resolvido`: `.dgrid` com Macrozona ("Macrozona Construída") e Unidade Territorial ("{sigla} — {nome}"); SE `regra.conferido===true` E `regra.ca_basico!=null` → linha `.urb-ca` "CA básico {ca_basico}x · CA máximo {ca_maximo}x" (quando `ca_maximo==null` usar "sem teto de índice de aproveitamento definido") + linha de altura quando `altura_max!=null`; SE `conferido!==true` → NÃO renderiza número nenhum, adiciona a `.dnote` "não conferido" (copy verbatim). Badges `.urb-badge` só para flags `true` (nenhuma → `.urb-badges` fica `hidden`). SEMPRE termina com a `.dnote` do `PD_DISCLAIMER`.
      - `rural`: bloco `.urb-rural` (copy verbatim, interpolando o nome da macrozona rural), sem badges/CA.
      - `parcial`: mostra o que resolveu + `.dnote` de aviso + `<button class="urb-retry" id="urbRetry">Tentar de novo</button>` que rechama `renderUrbanisticoUI(DCUR,true)`; SEMPRE o disclaimer no fim.
      - `erro`: única `.dnote` (copy verbatim) + `toast()` (mesmo componente de erro de rede) — nunca falha silenciosa.
    TODO campo textual vindo das layers passa por `esc()`; preferir o `nome` de `PD_TABELA_CA` (que o app escreve) a `nm_des` cru (mitiga mojibake — 18-RESEARCH Pitfall 4). Handler do retry via listener/id estático — nunca `onclick` interpolando dado de servidor.

    (4) Wiring: chamar `renderUrbanisticoUI(a)` em `showDetail` logo após `renderDiffUI(a)` (linha 4898) — irmã assíncrona, nunca trava o render síncrono da ficha.
  </action>
  <verify>
    <automated>node -e "const h=require('fs').readFileSync('radar-goiania.html','utf8');const need=['id=\"dUrbanistico\"','function renderUrbanisticoUI','renderUrbanisticoUI(a)','urb-badge','PD_DISCLAIMER'];const miss=need.filter(s=>!h.includes(s));if(miss.length){console.error('faltando:',miss);process.exit(1)}const iT=h.indexOf('dtecnico'),iU=h.indexOf('id=\"dUrbanistico\"'),iM=h.indexOf('dmetodologia');if(!(iT<iU&&iU<iM)){console.error('ordem do accordion errada');process.exit(1)}console.log('ok')"</automated>
  </verify>
  <done>Accordion #dUrbanistico presente entre dtecnico e dmetodologia; `renderUrbanisticoUI` implementada com os 5 estados e chamada em showDetail; disclaimer literal presente; `npm test` verde.</done>
</task>

<task type="auto">
  <name>Task 2: Guarda REGRA DE OURO (teste sobre o HTML de render) + "porquê" do score citando a zona (PD-04 score)</name>
  <files>radar-goiania.html, tests/pd.test.mjs</files>
  <action>
    (1) Extrair a MONTAGEM do HTML do accordion para uma função PURA testável dentro do bloco RADAR_PURE — ex. `montarUrbBodyHTML(estado)` que recebe o objeto de estado (o mesmo de `resolverZonaUI` + `regra`) e retorna a STRING de HTML (sem tocar o DOM); `renderUrbanisticoUI` passa a: setar loading → consultar → chamar `#dUrbBody.innerHTML = montarUrbBodyHTML(estado)`. Isso permite testar a REGRA DE OURO por assert de string (mesmo padrão de `CADERNO_ALLOW`, 18-RESEARCH §Validation). `montarUrbBodyHTML` usa `esc()` (stubável no sandbox como identidade) e `PD_DISCLAIMER`.

    (2) Testes em tests/pd.test.mjs (adicionar `montarUrbBodyHTML` ao `__exports`):
      - estado resolvido com `regra` de APAC (`conferido:false`) → a string NÃO contém dígito de CA seguido de "x" (regex `/\d+(?:,\d+)?x/` ausente) e CONTÉM a nota "ainda não foram conferidos" e CONTÉM `PD_DISCLAIMER`
      - estado resolvido com AA (`conferido:true`) → CONTÉM "CA básico 1,0x · CA máximo 6,0x" e CONTÉM `PD_DISCLAIMER`
      - estado rural → NÃO contém badges nem CA; CONTÉM a copy rural
      - todo estado resolvido/parcial/rural CONTÉM `PD_DISCLAIMER` exatamente uma vez

    (3) Score "porquê" (PD-04): onde a ficha exibe `scoreOportunidade(...).porque[]` (localizar o consumidor de `porque` no render da ficha), APÓS obter o estado do PD do lote atual, quando `regra.conferido===true` E `potencialConstrutivo(a.areaterr, sigla)` retorna número, APPEND uma linha ao `porque[]` exibido: `"Em {nome} — CA básico {ca_basico}x, potencial de {potencial} m²"` (potencial arredondado, número pt-BR). Quando não conferido/indisponível, NÃO adiciona linha (omissão honesta — nunca "—"). NÃO alterar a função pura `scoreOportunidade` nem seu número 0-100 (preserva os 184 testes) — a linha do PD é um sinal explicável adicional montado no render.
  </action>
  <verify>
    <automated>node --test tests/pd.test.mjs</automated>
  </verify>
  <done>`montarUrbBodyHTML` pura e testada; REGRA DE OURO verde (número de CA nunca sai quando conferido:false); `PD_DISCLAIMER` sempre presente; linha "Em {zona}…" adicionada ao porquê do score só quando conferido; `grep -q "function montarUrbBodyHTML" radar-goiania.html`; `npm test` verde.</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade do detector — critério PD por centroide de quadra (top-N, cache) + rótulo do critério (PD-04 detector)</name>
  <files>radar-goiania.html, tests/pd.test.mjs</files>
  <action>
    No render do detector (`#terrDetectorList`, ~3890-3960), que já opera sobre `TERR_DETECTOR_CAND` = `detectarSubutilizados(...)` (já truncado a `DETECTOR_LIMITE=50` ANTES de qualquer rede — a lista de candidatos é a fonte, nunca a amostra de 6k lotes):
    - Para cada candidato (≤50), obter a zona pelo CENTROIDE DA QUADRA (não do lote individual) via `pdConsultarLote(cx, cy, chaveQuadra)` onde `chaveQuadra = a.cdbairro+"-"+a.nrquadra` e (cx,cy) é o x_coord/y_coord de um lote representativo da quadra — CACHE por `chaveQuadra` (lotes da mesma quadra compartilham unidade territorial; 1 consulta por quadra, não por lote). Reusar `PDCACHE` (chaveado por quadra aqui) ou um mapa dedicado `PDQUADRACACHE` — NUNCA disparar > número de quadras distintas dos candidatos. Disparar em paralelo com `Promise.allSettled`; a lista renderiza mesmo se algumas consultas falharem.
    - Para cada candidato calcular `criterioDetectorPD(a.areaedif, a.areaterr, potencialConstrutivo(a.areaterr, siglaDaQuadra))`. Exibir o rótulo por candidato (reusa estilo `.terr-metric-sub`/mono):
      * `criterio==="pd"` → "Critério: área construída ÷ potencial do Plano Diretor (zona {sigla}, CA básico {ca_basico}x)"
      * `criterio==="terreno"` (fallback) → "Critério: área construída ÷ área do terreno (Plano Diretor não disponível para este candidato)"
    - O rótulo é SEMPRE visível (nunca omitido) para que o corretor saiba qual critério fundamentou o candidato. O detector NUNCA quebra por falta do PD — fallback rotulado é o caminho padrão quando a zona/CA não está disponível.
    - ORÇAMENTO (T-18-02): assert em teste de que o nº de consultas de PD do detector ≤ nº de quadras distintas dos candidatos (dedupe por quadra), nunca 1 por lote da amostra.
    Teste (tests/pd.test.mjs, bloco de I/O com jsonp stubado): candidatos de 3 quadras (2 lotes por quadra) → no máximo 3 consultas; candidato sem zona resolvida cai em `criterio:"terreno"` e continua na lista.
  </action>
  <verify>
    <automated>node --test tests/pd.test.mjs</automated>
  </verify>
  <done>Detector rotula o critério (PD ou fallback) por candidato; consulta de PD deduplicada por quadra (≤ quadras distintas, testado); fallback nunca quebra a lista; `grep -q "criterioDetectorPD" radar-goiania.html`; `npm test` verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → ArcGIS ModeloEspacial | Campos de texto (`sigla`/`nm_des`) das layers são interpolados no DOM da ficha |
| PD_TABELA_CA → DOM | Números legais renderizados na ficha; risco = exibir valor não conferido |
| detector (amostra do setor) → ArcGIS | Consultas de PD sobre candidatos podem virar avalanche se por-lote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | montarUrbBodyHTML / render de CA | mitigate | Guarda estrutural: `montarUrbBodyHTML` só emite CA quando `regra.conferido===true`; teste de string garante que `conferido:false` nunca produz número (regex de CA ausente) |
| T-18-05 | Repudiation | disclaimer fixo | mitigate | `PD_DISCLAIMER` (string constante) renderizado em TODO estado resolvido/parcial/rural; teste garante presença exata (uma vez) |
| T-18-03 | Tampering (XSS) | renderUrbanisticoUI / detector | mitigate | `esc()` em todo texto de layer; preferir `nome` de PD_TABELA_CA (app escreve) a `nm_des` cru; handler do retry por id/listener, nunca `onclick` com dado de servidor (lição Fase 16) |
| T-18-02 | Denial of Service | upgrade do detector | mitigate | Consulta por CENTROIDE de quadra deduplicada (≤ quadras distintas dos ≤50 candidatos, testado); reusa `DETECTOR_LIMITE` como teto; `Promise.allSettled` (falha parcial não trava) |
| T-18-04 | Denial of Service | criterioDetectorPD (fallback) | mitigate | Fallback `areaedif/areaterr` rotulado é o caminho padrão sem PD — detector nunca quebra nem esconde candidato por falta de zona |
</threat_model>

<verification>
- `npm test` verde (baseline + suíte pd.test.mjs estendida)
- Accordion #dUrbanistico entre dtecnico e dmetodologia; 5 estados renderizam
- REGRA DE OURO: `conferido:false` nunca produz número de CA (teste de string)
- Disclaimer SEPLANH literal sempre presente
- Score cita a zona só quando conferido; detector rotula critério (PD/fallback) e deduplica por quadra
</verification>

<success_criteria>
- PD-03: seção "Urbanístico" na ficha (accordion, disclaimer fixo, 5 estados, linguagem do gate §26)
- PD-04: score ganha o "porquê" citando a zona (quando conferido); detector usa construído÷potencial-do-PD com fallback gracioso rotulado, sem avalanche
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/18-inteligencia-urbanistica-plano-diretor/18-02-SUMMARY.md`
</output>
