---
phase: 18-inteligencia-urbanistica-plano-diretor
plan: 03
type: execute
wave: 3
depends_on: ["18-01", "18-02"]
files_modified:
  - radar-goiania.html
  - tests/pd.test.mjs
autonomous: true
requirements: [PD-05]
must_haves:
  truths:
    - "O painel do território tem 2 chips exclusivos: 'Colorir por valor' e 'Colorir por zonas do Plano Diretor' — nunca as duas camadas ligadas ao mesmo tempo"
    - "Ligar zonas colore o mapa por unidade territorial com a paleta oficial do ArcGIS e mostra a legenda de zonas"
    - "A geometria das zonas é consultada LIMITADA à viewport/setor em foco (nunca a cidade inteira) e cacheada por sessão"
    - "As cores das zonas são legíveis sobre CARTO e satélite (opacidade flat .22/.42, traço reforçado sobre satélite)"
    - "Toda a fase está verde: npm test, greps de honestidade/disclaimer/citações e checklist de browser prontos"
  artifacts:
    - path: "radar-goiania.html"
      provides: "--zone-* CSS + .zone-swatch + chips #terrLayerChips + toggleCamadaTematica + montarLegendaZonas + camada Leaflet de zonas viewport-limited"
      contains: "toggleCamadaTematica"
  key_links:
    - from: "#terrChipZonas"
      to: "toggleCamadaTematica('zonas')"
      via: "chip exclusivo; liga zonas e desliga valor via setStyle/camada própria"
      pattern: "toggleCamadaTematica"
    - from: "camada de zonas"
      to: "jsonp returnGeometry=true (viewport)"
      via: "6 layers (7/28/29/30/31/32) limitadas ao envelope em foco, cache por chave de viewport"
      pattern: "returnGeometry"
---

<objective>
A camada de zonas do Plano Diretor como toggle no Território (PD-05): um seletor de camada temática com 2 chips mutuamente exclusivos ("Colorir por valor" / "Colorir por zonas do Plano Diretor") substitui o botão booleano único; ligar zonas colore o mapa por unidade territorial com a paleta oficial do ArcGIS, legível sobre CARTO e satélite, com a geometria consultada apenas para a viewport/setor em foco (o payload é real — 86 KB/12 polígonos medido — então a limitação por viewport é obrigatória, não opcional). Fecha a fase com a verificação final (greps de honestidade + suíte verde + checklist de browser para o orquestrador).

Purpose: entregar o último requisito da fase reusando a engenharia de choropleth/legenda/toggle da Fase 15 (opacidade por fundo, `setStyle`, `reduced-motion`) sem recriar a malha base, e garantir que toda a inteligência urbanística está honesta e verde antes do `/gsd-verify-work`.
Output: paleta `--zone-*` + `toggleCamadaTematica` + camada Leaflet de zonas + legenda em `radar-goiania.html`; verificação final documentada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-inteligencia-urbanistica-plano-diretor/18-CONTEXT.md
@.planning/phases/18-inteligencia-urbanistica-plano-diretor/18-RESEARCH.md
@.planning/phases/18-inteligencia-urbanistica-plano-diretor/18-UI-SPEC.md
@.planning/phases/18-inteligencia-urbanistica-plano-diretor/18-02-SUMMARY.md

<read_first>
- radar-goiania.html — `#terrPanelToggle` botão único (1180) e `#terrActions`/`.acts` do painel; `toggleChoropleth`/`CHOROPLETH_ON`/`desenharChoropleth` (2813/2939/2898); `montarLegenda`/`recolherLegenda` (2905/2921); `#terrLegenda`/`terrLbl*`/`.terr-swatch`/`.terr-swatch-col`/`.terr-swlbl` (1149+, CSS); `baiStyle`/rampa de traço por zoom (2833); `sincTerrPanelToggle` (3801); `jsonp` (2636); `TERRCACHE`/`capCache` (3407/2959); `REDUCE`/`mAnimate` (motion guard Fase 15); `PD_LAYERS`/`PD_SVC_BASE`/`PDCACHE` (plano 01); `--paper-2`/`--ink`/`--muted` (paleta base)
- 18-UI-SPEC.md §Color "Paleta de Zonas do Plano Diretor" (7 pares --zone-*, opacidade flat .22/.42, notas de simplificação) e §"Contrato Específico" Componente 2 (chips exclusivos, legenda data-mode, exclusividade, viewport-limited)
</read_first>

<interfaces>
<!-- Do plano 01/02 e da Fase 15. Use diretamente. -->
```
const PD_LAYERS = {macrozona:33, aa:31, add:30, aos:29, aeis:7, apac:28, ooau:32, eixo:4, corredor:1}
const PD_SVC_BASE = ".../Mapa_ModeloEspacial/MapServer"     // produção; host já no CSP
async function jsonp(params, retries=1, url)
function capCache(o, max); function toWGS(x,y); const REDUCE; function mAnimate(...)
let CHOROPLETH_ON; function toggleChoropleth(); function desenharChoropleth(); function montarLegenda(st); function sincTerrPanelToggle()
```

Paleta VERBATIM (18-UI-SPEC §Color) — 7 pares CSS vars a declarar:
```css
--zone-aa:#ffaa00;   --zone-aa-line:#e1aa00;
--zone-add:#ffbee8;  --zone-add-line:#6e6e6e;
--zone-aos:#6dbb43;  --zone-aos-line:#4a8a2e;
--zone-aeis:#bee8ff; --zone-aeis-line:#141a1f;
--zone-apac:#ff7f7f; --zone-apac-line:#141a1f;
--zone-ooau:#ffff73; --zone-ooau-line:#6e6e6e;
--zone-none:#d8d3c4; --zone-none-line:#57503f;
```
Choropleth de zonas usa as 6 layers 7/28/29/30/31/32 (AEIS/APAC/AOS/ADD/AA/OOAU); opacidade FLAT por fundo (CARTO `.22`, satélite `.42`); traço = rampa por zoom de `baiStyle` (`0.5+t*0.7` CARTO / `1.2+t*0.6` satélite), cor do traço sobre satélite → `var(--paper-2)`.

Copy VERBATIM: chip valor "Colorir por valor"; chip zonas "Colorir por zonas do Plano Diretor"; loading legenda "Carregando zonas do setor…"; item neutro da legenda "Sem classificação específica".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Paleta --zone-* + .zone-swatch + chips exclusivos #terrLayerChips + legenda data-mode (PD-05 UI)</name>
  <files>radar-goiania.html</files>
  <action>
    (1) Declarar os 7 pares `--zone-*`/`--zone-*-line` no `:root` (verbatim do §Color). Comentar quais vieram da simbologia oficial do ArcGIS e quais são engenharia própria (`--zone-none`, `--zone-apac-line`, `--zone-aos-line`) — mesma disciplina de honestidade do REGRA DE OURO estendida a cor.

    (2) CSS `.zone-swatch` (variante de `.terr-swatch`, 24×14px, `border-color` por var) + `flex-wrap:wrap` no container de swatches para caber 2 linhas no mobile.

    (3) Substituir o botão único `#terrPanelToggle` (1180) por um GRUPO de 2 chips reusando `.chips` verbatim:
    ```html
    <div class="chips" role="group" aria-label="Camada temática do mapa" id="terrLayerChips">
      <button type="button" id="terrChipValor" aria-pressed="false" onclick="toggleCamadaTematica('valor')">Colorir por valor</button>
      <button type="button" id="terrChipZonas" aria-pressed="false" onclick="toggleCamadaTematica('zonas')">Colorir por zonas do Plano Diretor</button>
    </div>
    ```
    Ocupa o slot de "1 primária" (lei da Fase 10 preservada; as 2 secundárias "Buscar lote no setor"/"Detectar oportunidades" NÃO mudam). Estado `.on`/`aria-pressed="true"` reusa a semântica JÁ existente de `.chips button.on` (accent) — sem uso novo de accent.

    (4) Legenda `#terrLegenda` ganha `data-mode="valor"|"zonas"` sincronizado com o chip ativo. Quando `data-mode="zonas"`, o corpo troca as 5 `.terr-swatch` de quantil pelos 6 swatches de zona (AA/ADD/AOS/AEIS/APAC/OOAU, markup do §Componente 2) via `montarLegendaZonas()` (espelha `montarLegenda()`). O cabeçalho mantém 1 único toggle de texto dinâmico ("Cor por valor"/"Cor por zonas do Plano Diretor") — fonte única de verdade com os chips (mesmo princípio "nunca dessincroniza" da Fase 15).
  </action>
  <verify>
    <automated>node -e "const h=require('fs').readFileSync('radar-goiania.html','utf8');const need=['--zone-aa:#ffaa00','--zone-none','id=\"terrLayerChips\"','id=\"terrChipZonas\"','Colorir por zonas do Plano Diretor','zone-swatch'];const miss=need.filter(s=>!h.includes(s));if(miss.length){console.error('faltando:',miss);process.exit(1)}console.log('ok')"</automated>
  </verify>
  <done>7 pares --zone-* declarados; chips exclusivos substituem o botão único; legenda com data-mode e montarLegendaZonas; `grep -q "montarLegendaZonas" radar-goiania.html`; `npm test` verde.</done>
</task>

<task type="auto">
  <name>Task 2: toggleCamadaTematica (tri-state exclusivo) + camada Leaflet de zonas viewport-limited + cache (PD-05 engine)</name>
  <files>radar-goiania.html, tests/pd.test.mjs</files>
  <action>
    (1) `toggleCamadaTematica(modo)` — tri-state (`nenhuma`/`valor`/`zonas`), fonte única de verdade que substitui o `toggleChoropleth()` booleano isolado: clicar no chip ativo desliga (volta a `nenhuma`); clicar no inativo troca de camada. EXCLUSIVIDADE explícita (CONTEXT "evita salada de cor"): ligar `zonas` desliga `valor` e vice-versa — NUNCA as duas simultâneas. Sincroniza `aria-pressed` dos 2 chips E do toggle da legenda (fonte única). Reusa `CHOROPLETH_ON`/`desenharChoropleth` para o modo valor (inalterado); adiciona o caminho de zonas.

    (2) Camada de zonas: função `desenharZonas()`/`aplicarZonas()` que consulta as 6 layers (7/28/29/30/31/32) com `jsonp({..., returnGeometry:true, geometryType:"esriGeometryEnvelope", geometry:<bbox da viewport/setor em foco em 31982>, inSR:31982, spatialRel:"esriSpatialRelIntersects", outFields:"sigla", f:"json"}, 1, PD_SVC_BASE+"/"+id+"/query")` — envelope LIMITADO à viewport/setor em foco (nunca a cidade inteira; considerar reduzir ao setor/bairro em foco em zoom baixo — 18-RESEARCH Pitfall 3). Cache de sessão por chave de viewport ARREDONDADO (ex. `Math.round` do bbox) via um mapa dedicado `ZONACACHE` + `capCache(ZONACACHE, 20)`. Renderizar como 6 layers Leaflet SEPARADAS e semi-transparentes empilhadas (nunca um fill "vencedor" por prioridade — sobreposição por transparência, §Color nota 3), fill por `--zone-{sigla}`, opacidade FLAT (`.22` CARTO / `.42` satélite, detectar fundo ativo como a Fase 15 já faz), traço pela rampa de zoom de `baiStyle`, cor do traço sobre satélite → `var(--paper-2)`. Usa `setStyle()`/camada própria sem recriar a malha base. `reduced-motion`: recolher/expandir da legenda usa o guard `REDUCE`/`mAnimate` já existente; a troca de camada é instantânea. Toast/estado de loading "Carregando zonas do setor…" enquanto a geometria chega; `Promise.allSettled` (falha parcial de uma layer não derruba as outras).

    (3) Teste (tests/pd.test.mjs, bloco de I/O com `jsonp` stubado + `L` mockado): assert de que a query de geometria SEMPRE inclui um `geometry` de envelope (viewport) e `returnGeometry:"true"`, e NUNCA dispara sem bbox (guarda anti "cidade inteira"); assert de que 2ª chamada com o mesmo viewport arredondado usa `ZONACACHE` (não redispara jsonp); assert de exclusividade (ligar zonas zera o estado de valor).
  </action>
  <verify>
    <automated>node --test tests/pd.test.mjs</automated>
  </verify>
  <done>`toggleCamadaTematica` tri-state exclusivo; camada de zonas consulta geometria viewport-limited com `ZONACACHE`; 6 layers empilhadas semi-transparentes; testes de viewport-limit/cache/exclusividade verdes; `grep -q "function toggleCamadaTematica" radar-goiania.html` e `grep -q "returnGeometry" radar-goiania.html`; `npm test` verde.</done>
</task>

<task type="auto">
  <name>Task 3: Verificação final da fase — greps de honestidade + suíte verde + checklist de browser</name>
  <files>radar-goiania.html, tests/pd.test.mjs</files>
  <action>
    Rodar e registrar a verificação de fechamento (sem código novo além de eventuais ajustes de teste):
    (1) `npm test` — suíte inteira verde (184 baseline + toda a suíte pd.test.mjs).
    (2) Greps de honestidade/contrato (todos devem passar):
      - Disclaimer literal: `grep -q "Não substitui a Certidão de Uso do Solo da SEPLANH" radar-goiania.html`
      - Citações por artigo (nunca "Anexo"): `grep -q "Art. 196, II" radar-goiania.html` e `grep -q "Art. 242" radar-goiania.html`; e que nenhuma `fonte` com `conferido:true` cite "Anexo" (checar em tests/pd.test.mjs)
      - Produção, nunca teste: a URL do PD contém `Mapa_ModeloEspacial` e NÃO existe `Mapa_ModeloEspacial_teste` no arquivo
      - Lista de layers da bateria: `grep -q "macrozona:33" radar-goiania.html` (PD_LAYERS com 33/31/30/29/7/28/32/4/1)
      - REGRA DE OURO no render: teste de `montarUrbBodyHTML` (conferido:false → sem número) verde
      - Exclusividade das camadas temáticas: teste de `toggleCamadaTematica` verde
    (3) Escrever no SUMMARY o CHECKLIST de verificação em browser (para o orquestrador rodar via preview — HUMAN-UAT não-bloqueante do advogado fica registrado como item diferido):
      - Abrir ficha de um lote urbano → accordion "Urbanístico" resolve zona (macrozona + unidade); DevTools: ~9 requests pequenos ao ModeloEspacial; reabrir a ficha NÃO redispara (PDCACHE)
      - Lote em AA → mostra "CA básico 1,0x · CA máximo 6,0x" + disclaimer; lote em zona `conferido:false` → só a zona + nota (nenhum número)
      - Lote rural → mensagem "Fora da Macrozona Construída"
      - Território → chip "Colorir por zonas do Plano Diretor" colore por unidade territorial; ligar zonas desliga valor; legibilidade sobre CARTO e satélite; DevTools: geometria só da viewport/setor
      - Detector → cada candidato rotula o critério (PD ou fallback)
      - Item diferido (registrar): revisão jurídica dos valores/artigos e do disclaimer pelo advogado (HUMAN-UAT); staleness completa das emendas LC 363/364/371/373 (18-RESEARCH Assumptions A1)
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>`npm test` verde; todos os greps de honestidade passam; checklist de browser + itens HUMAN-UAT diferidos registrados no 18-03-SUMMARY.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → ArcGIS ModeloEspacial (geometria) | `returnGeometry=true` pode baixar payload grande (300-500 KB/troca) se não limitado à viewport |
| campos de texto das layers → DOM/legenda | `sigla` interpolada na legenda/estilo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02 | Denial of Service | camada de zonas (returnGeometry) | mitigate | Geometria SEMPRE limitada ao envelope da viewport/setor em foco (nunca cidade inteira); `ZONACACHE` por viewport arredondado + `capCache(ZONACACHE,20)`; exclusividade (1 camada temática por vez); teste assere presença de bbox + cache |
| T-18-03 | Tampering (XSS) | montarLegendaZonas / estilo | mitigate | `sigla` usada só para mapear cor/rótulo estático da paleta (o app escreve os rótulos); nenhum `nm_des` cru interpolado; `esc()` em qualquer texto de layer que chegue à legenda |
| T-18-01 | Information Disclosure | verificação final | mitigate | Greps de fechamento garantem disclaimer literal, citações por Art. (nunca "Anexo") e produção (nunca `_teste`) antes do verify-work |
</threat_model>

<verification>
- `npm test` verde (baseline + pd.test.mjs completo)
- Chips exclusivos; ligar zonas desliga valor (testado); geometria viewport-limited + cache (testado)
- Paleta oficial --zone-* declarada; opacidade flat por fundo; traço reforçado sobre satélite
- Greps de honestidade (disclaimer/Art./produção/layers) passam; checklist de browser e HUMAN-UAT diferidos registrados
</verification>

<success_criteria>
- PD-05: camada de zonas como toggle exclusivo no Território (choropleth por zona), legível sobre CARTO e satélite, geometria limitada à viewport e cacheada
- Fase 18 fechada: núcleo determinístico + ficha honesta + inteligência integrada + choropleth de zonas, tudo verde e verificável
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/18-inteligencia-urbanistica-plano-diretor/18-03-SUMMARY.md`
</output>
