---
phase: 18-inteligencia-urbanistica-plano-diretor
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/pd.test.mjs
  - tests/fixtures.mjs
  - radar-goiania.html
autonomous: true
requirements: [PD-01, PD-02, PD-04]
must_haves:
  truths:
    - "Nenhum número de CA com conferido:false pode sair de uma função pura (potencialConstrutivo/pdRegrasDaZona retornam null, nunca um número fabricado)"
    - "PD_TABELA_CA tem AA (máx 6,0), ADD (máx 5,0) com conferido:true e fonte citando Art., nunca Anexo; usos com usos_conferido:true só para AA/ADD (qualquer uso, Art. 196 I/II); _meta de auto-auditoria de emendas embutido"
    - "resolverZonaUI produz os 6 estados (resolvido/resolvido_sem_unidade/parcial/rural/erro/carregando) a partir de Promise.allSettled — incluindo Macrozona Construída sem unidade territorial específica (lote urbano fora de AA/ADD/AOS)"
    - "pdConsultarLote consulta o Modelo Espacial por ponto, deduplicando por ci via PDCACHE (lazy, sem avalanche)"
    - "criterioDetectorPD cai no fallback areaedif/areaterr rotulado quando não há potencial do PD — nunca quebra"
  artifacts:
    - path: "tests/pd.test.mjs"
      provides: "Suíte TDD das funções puras e do state builder do PD"
      contains: "PD_TABELA_CA"
    - path: "radar-goiania.html"
      provides: "PD_TABELA_CA, PD_LAYERS, PD_DISCLAIMER, pdRegrasDaZona, potencialConstrutivo, criterioDetectorPD, resolverZonaUI (RADAR_PURE) + pdConsultarLote/PDCACHE (PD_NET)"
      contains: "function resolverZonaUI"
  key_links:
    - from: "pdConsultarLote"
      to: "jsonp + resolverZonaUI"
      via: "Promise.allSettled das 9 layers, resultado montado por resolverZonaUI, cacheado em PDCACHE[ci]"
      pattern: "allSettled"
    - from: "potencialConstrutivo"
      to: "pdRegrasDaZona"
      via: "lookup da regra; retorna areaterr×ca_basico só quando conferido:true"
      pattern: "conferido"
---

<objective>
Núcleo de dados urbanísticos do Plano Diretor 2022, 100% determinístico e testado por TDD, ANTES de qualquer UI. Entrega: (a) a tabela estática `PD_TABELA_CA` com os números CONFERIDOS na fonte primária (LC 349/2022, artigos — nunca "Anexo"); (b) as funções puras de regra/potencial/detector com a REGRA DE OURO embutida (número não-conferido nunca vira número); (c) o state builder `resolverZonaUI` (6 estados, incl. resolvido_sem_unidade); (d) a camada de rede `pdConsultarLote`/`PDCACHE` (bateria de 9 layers por ponto, lazy, deduplicada — sem avalanche no endpoint frágil).

Purpose: isolar toda a lógica auditável no bloco `RADAR_PURE` (testável via `node:vm`) e a I/O no bloco `PD_NET` (testável com `jsonp` stubado), para que os planos 02 (ficha) e 03 (choropleth) sejam pura composição de UI sobre um núcleo já verde.
Output: `tests/pd.test.mjs` + funções/constantes novas em `radar-goiania.html`; `npm test` verde (184 baseline + novos).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-inteligencia-urbanistica-plano-diretor/18-CONTEXT.md
@.planning/phases/18-inteligencia-urbanistica-plano-diretor/18-RESEARCH.md
@.planning/phases/18-inteligencia-urbanistica-plano-diretor/18-VALIDATION.md

<read_first>
- radar-goiania.html — bloco RADAR_PURE (linhas 1351 `RADAR_PURE_START` … 2595 `RADAR_PURE_END`); `scoreOportunidade` (1543), `detectarSubutilizados`/`razaoOcupacao`/`DETECTOR_LIMITE` (2336-2394); `jsonp`/`jsonpOnce` (2636-2654); bloco `TERR_NET` (3399 `TERR_NET_START` … 3467 `TERR_NET_END`), `TERRCACHE`/`capCache` (3407/2959); `SVC` base URL (1330); `esc()` (1340)
- 18-RESEARCH.md §"Gate de Verdade (Parte A)" — os números conferidos artigo por artigo (a fonte da verdade desta tabela)
- 18-RESEARCH.md §"Validation Architecture" — padrão de carregamento `node:vm` (funções DEVEM ficar entre as linhas 1351 e 2595)
- tests/territorio.test.mjs — `loadPureBlock()` (slice RADAR_PURE) e `loadNetBlock(stubs)` (slice TERR_NET com jsonp stubado); tests/scores.test.mjs; tests/fixtures.mjs
</read_first>

<interfaces>
<!-- Funções/constantes já existentes que este plano consome. Use verbatim, sem explorar o codebase. -->

Bloco RADAR_PURE (radar-goiania.html 1351-2595), carregado por node:vm slice-por-linha:
```
function scoreOportunidade(myPm2, stats, flags) -> {score, rotulo, porque[]} | null
function razaoOcupacao(a) -> Number | null          // +areaedif/+areaterr; null se dado incompleto
function detectarSubutilizados(lotes, opts) -> lote[] // opts.{razaoMax,limite}; usa DETECTOR_LIMITE=50
const DETECTOR_LIMITE = 50                            // reusar como teto do top-N; NÃO duplicar
```

Rede (fora do RADAR_PURE):
```
async function jsonp(params, retries=1, url=SVC)      // JSONP; retorna o objeto da resposta ArcGIS
const SVC = "https://portalmapa.goiania.go.gov.br/servicogyn/rest/services/MapaServer/Feature_Base/MapServer/3/query"
function capCache(o, max)                              // FIFO simples de cache de sessão
function esc(s)                                        // sanitiza texto p/ DOM
```

Padrão de teste de I/O (tests/territorio.test.mjs `loadNetBlock`): fatiar o bloco entre marcadores `PD_NET_START`/`PD_NET_END`, injetar `jsonp` STUBADO no sandbox — rede real NUNCA entra em `node --test`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/pd.test.mjs (RED) + constantes PD_TABELA_CA / PD_LAYERS / PD_DISCLAIMER + pdRegrasDaZona</name>
  <files>tests/pd.test.mjs, radar-goiania.html</files>
  <behavior>
    - `pdRegrasDaZona("AA")` retorna a entrada com `conferido===true`, `ca_basico===1.0`, `ca_maximo===6.0`
    - `pdRegrasDaZona("ADD").ca_maximo===5.0` e `conferido===true`
    - `pdRegrasDaZona("APAC").conferido===false` e `pdRegrasDaZona("AEIS").conferido===false`
    - `pdRegrasDaZona("AA").usos_conferido===true` e `pdRegrasDaZona("AA").usos==="qualquer uso"`; idem ADD (Art. 196 I/II conferido no RESEARCH)
    - `pdRegrasDaZona("AA").nota_altura` é string não-vazia citando "Art. 190" (regime de ocupação 100%/50% conferido); idem ADD
    - Toda sigla com `usos_conferido===false` mantém `usos_conferido` default `false` (REGRA DE OURO estendida a usos — a linha "Usos" só é renderizada no plano 02 quando true)
    - `PD_TABELA_CA._meta` existe com `checado_em` e `emendas_verificadas` (auto-auditoria de staleness)
    - `PD_MZC_BASICO.ca_basico===1.0`, `PD_MZC_BASICO.ca_maximo===null`, `PD_MZC_BASICO.conferido===true`, `fonte` cita "Art. 242" (CA básico universal da Macrozona Construída)
    - `pdRegrasDaZona("XYZ")` (sigla desconhecida) retorna `null` (nunca lança)
    - Guarda de integridade da tabela: para TODA sigla com `conferido===true`, `fonte` é string não-vazia e NÃO contém a palavra "Anexo" (deve citar "Art.")
    - Guarda REGRA DE OURO na origem: nenhuma sigla com `conferido===false` tem `ca_basico` ou `ca_maximo` numérico (devem ser `null`)
  </behavior>
  <action>
    Criar `tests/pd.test.mjs` seguindo o padrão de `tests/scores.test.mjs`/`tests/territorio.test.mjs`: `loadPureBlock()` fatia o bloco `RADAR_PURE_START`/`RADAR_PURE_END` por LINHA (nunca `indexOf` cru), monta `vm.Script(src + "\n;globalThis.__exports={PD_TABELA_CA,PD_LAYERS,PD_DISCLAIMER,pdRegrasDaZona,potencialConstrutivo,criterioDetectorPD,resolverZonaUI};")` e roda em `vm.createContext({})`. Escrever os testes de Task 1 (devem FALHAR primeiro — RED).

    Depois adicionar DENTRO do bloco RADAR_PURE (antes da linha `// RADAR_PURE_END`), próximo às demais constantes:
    - `const PD_DISCLAIMER = "Informação urbanística indicativa, extraída do Plano Diretor (LC 349/2022 e alterações). Não substitui a Certidão de Uso do Solo da SEPLANH.";` (texto EXATO do 18-UI-SPEC, sem paráfrase)
    - `const PD_LAYERS = {macrozona:33, aa:31, add:30, aos:29, aeis:7, apac:28, ooau:32, eixo:4, corredor:1};` (lista locked do CONTEXT — 9 layers, ordem/IDs exatos)
    - `PD_TABELA_CA` (JSON literal <2KB) com os números conferidos na 18-RESEARCH. Valores obrigatórios (números pt-BR renderizados depois; aqui são Number):
      * `AA`: `{nome:"Área Adensável", ca_basico:1.0, ca_maximo:6.0, altura_max:null, taxa_ocupacao:null, vi_outorga:null, usos:"qualquer uso", usos_conferido:true, nota_ca:null, nota_altura:"Ocupação de até 100% do terreno em edificações de até 11 m; 50% acima de 11 m. A lei não fixa altura máxima para esta zona.", fonte:"Art. 242, VII; Art. 196, II; Art. 190, II e III, LC 349/2022", conferido:true}`
      * `ADD`: `{nome:"Área de Desaceleração de Densidade", ca_basico:1.0, ca_maximo:5.0, altura_max:null, taxa_ocupacao:null, vi_outorga:null, usos:"qualquer uso", usos_conferido:true, nota_ca:null, nota_altura:"Ocupação de até 100% do terreno em edificações de até 11 m; 50% acima de 11 m. A lei não fixa altura máxima para esta zona.", fonte:"Art. 242, VII; Art. 196, I; Art. 190, II e III, LC 349/2022", conferido:true}`
      * `AOS`: `{nome:"Área de Ocupação Sustentável", ca_basico:1.0, ca_maximo:null, altura_max:12, taxa_ocupacao:40, vi_outorga:null, usos:"", usos_conferido:false, nota_ca:"A lei não define teto de índice de aproveitamento para esta zona (Art. 196 regula só AA e ADD); o parâmetro limitante é a altura (12 m; 7,5 m nos Setores Jaó e Sul) e a taxa de ocupação (40%).", nota_altura:null, fonte:"Art. 242, VII; Art. 186; Art. 190, §3º, LC 349/2022", conferido:true}`
      * `AEIS`: `{nome:"Área Especial de Interesse Social", ca_basico:null, ca_maximo:null, altura_max:null, taxa_ocupacao:null, vi_outorga:null, usos:"", usos_conferido:false, nota_ca:null, nota_altura:null, fonte:"", conferido:false}`
      * `APAC`: `{nome:"Área de Preservação do Ambiente Cultural", ca_basico:null, ca_maximo:null, altura_max:null, taxa_ocupacao:null, vi_outorga:null, usos:"", usos_conferido:false, nota_ca:null, nota_altura:null, fonte:"", conferido:false}`
      * `_meta`: `{checado_em:"2026-07-10", emendas_verificadas:["LC 358/2022 (não é do PD)","LC 379/2024 (não altera Art. 196/242/252)"], pendente:["LC 363/364/371/373 — leitura integral: HUMAN-UAT advogado"]}` (auto-auditoria de staleness embutida na própria tabela — RESEARCH Assumptions A1)
      Adicionar também a constante `const PD_MZC_BASICO = {nome:"Macrozona Construída", ca_basico:1.0, ca_maximo:null, altura_max:null, taxa_ocupacao:null, vi_outorga:null, usos:"", usos_conferido:false, nota_ca:"Coeficiente de Aproveitamento Básico não oneroso de 1,0x é universal em toda a Macrozona Construída; o índice máximo (5x ADD / 6x AA) depende da unidade territorial específica, não identificada aqui.", nota_altura:null, fonte:"Art. 242, VII, LC 349/2022", conferido:true};` — o CA básico universal usado pelo estado "resolvido_sem_unidade" (jamais expõe máximo, só o básico com fonte).
      NÃO publicar 7,5x como CA da AA (é teto excepcional de TDC, Art. 252 §6º — só entra em nota de metodologia no plano 02). NUNCA citar "Anexo" em `fonte`.
    - `function pdRegrasDaZona(sigla){ return (sigla && PD_TABELA_CA[String(sigla).toUpperCase()]) || null; }` (nunca lança; sigla desconhecida → null)
  </action>
  <verify>
    <automated>node --test tests/pd.test.mjs</automated>
  </verify>
  <done>tests/pd.test.mjs existe e passa nos testes de Task 1; `grep -q "ca_maximo:6" radar-goiania.html` e `grep -q "Art. 196, II" radar-goiania.html` e `grep -q "Não substitui a Certidão de Uso do Solo da SEPLANH" radar-goiania.html`; `grep -q "usos_conferido:true" radar-goiania.html`, `grep -q "PD_MZC_BASICO" radar-goiania.html` e `grep -q "emendas_verificadas" radar-goiania.html`; nenhum `conferido:true` com `fonte` contendo "Anexo".</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: potencialConstrutivo + criterioDetectorPD (REGRA DE OURO + fallback gracioso)</name>
  <files>tests/pd.test.mjs, radar-goiania.html</files>
  <behavior>
    - `potencialConstrutivo(600, "AA")` === `600*1.0` (usa ca_basico conferido) → `600`
    - `potencialConstrutivo(600, "APAC")` === `null` (conferido:false → NUNCA número fabricado — REGRA DE OURO)
    - `potencialConstrutivo(600, "XYZ")` === `null`; `potencialConstrutivo(0, "AA")`/`potencialConstrutivo(null,"AA")` === `null` (terreno ausente)
    - `criterioDetectorPD(300, 600, 600)` → `{razao:0.5, criterio:"pd"}` (usa areaedif/potencialPD quando potencialPD>0)
    - `criterioDetectorPD(300, 600, null)` → `{razao:0.5, criterio:"terreno"}` (fallback rotulado areaedif/areaterr — NUNCA quebra)
    - `criterioDetectorPD(300, 0, null)` → `null` (sem denominador real; nunca inventa)
  </behavior>
  <action>
    Adicionar no bloco RADAR_PURE (antes de RADAR_PURE_END). Escrever os testes primeiro (RED), depois implementar:
    - `function potencialConstrutivo(areaterr, sigla){ const at=+areaterr; if(!(at>0))return null; const r=pdRegrasDaZona(sigla); if(!r||r.conferido!==true||r.ca_basico==null)return null; return at*r.ca_basico; }` (potencial = areaterr × ca_basico, SÓ com CA conferido — CONTEXT PD-04)
    - `function criterioDetectorPD(areaedif, areaterr, potencialPD){ const ae=+areaedif; if(!(ae>=0))return null; if(potencialPD>0)return {razao:ae/potencialPD, criterio:"pd"}; const at=+areaterr; if(!(at>0))return null; return {razao:ae/at, criterio:"terreno"}; }` (fallback nomeado — o campo `criterio` diz qual base foi usada; consumido pelo rótulo do detector no plano 02)
    Constantes/thresholds nomeados; zero mágica numérica nova (reusa DETECTOR_LIMITE/DETECTOR_RATIO_MAX existentes no plano 02).
  </action>
  <verify>
    <automated>node --test tests/pd.test.mjs</automated>
  </verify>
  <done>Testes de Task 2 verdes; `grep -q "function potencialConstrutivo" radar-goiania.html` e `grep -q "function criterioDetectorPD" radar-goiania.html`; fallback `criterio:"terreno"` coberto por fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: resolverZonaUI (5 estados, pura) + pdConsultarLote/PDCACHE (PD_NET, I/O deduplicada)</name>
  <files>tests/pd.test.mjs, tests/fixtures.mjs, radar-goiania.html</files>
  <behavior>
    - `resolverZonaUI` recebe as respostas das 9 queries (formato Promise.allSettled: `[{layer:"macrozona", status:"fulfilled", value:{features:[...]}}, ...]`) e retorna `{estado, macrozona, unidade:{sigla,nome}|null, regra, badges:{aeis,apac,add,eixo,corredor}}`:
      * todas resolvidas + AA presente → `estado:"resolvido"`, `unidade.sigla==="AA"`, `regra===PD_TABELA_CA.AA`
      * todas resolvidas, macrozona = Construída, mas NENHUMA das layers AA/ADD/AOS (31/30/29) intersectou (caso real: lote urbano em AAB, sem layer própria na bateria) → `estado:"resolvido_sem_unidade"`, `unidade===null`, `regra===PD_MZC_BASICO` (NUNCA `undefined` — sem este 6º estado o render quebra com "undefined — undefined")
      * macrozona = rural (não "Construída") → `estado:"rural"` (só macrozona; sem badges nem CA)
      * TODAS as 9 falharam (`status:"rejected"`) → `estado:"erro"`
      * algumas falharam, outras resolveram → `estado:"parcial"` + os campos que resolveram
      * AEIS presente → `badges.aeis===true`; OOAU nunca vira badge (não está nos 5 badges)
    - resolverZonaUI é PURA (sem rede): funciona só sobre o array de respostas
    - `pdConsultarLote(x,y,ci)` (com jsonp stubado): dispara UMA bateria de 9 `jsonp` por ponto, monta via `Promise.allSettled`+`resolverZonaUI`, grava em `PDCACHE[ci]`; segunda chamada com o MESMO `ci` NÃO dispara jsonp de novo (dedupe/cache de sessão)
  </behavior>
  <action>
    Adicionar `resolverZonaUI(respostas)` no bloco RADAR_PURE (é pura — deve entrar entre 1351 e RADAR_PURE_END para o harness carregar). Detectar rural: macrozona cuja `sigla`/nome não seja a Macrozona Construída (a layer 33 devolve a macrozona; "Construída" = urbana, qualquer outra = rural). Usar Promise.allSettled-shaped input. Badges apenas para {aeis, apac, add, eixo, corredor} (layers 7/28/30/4/1) — OOAU (32) consultada mas SEM badge (18-UI-SPEC). Unidade Territorial resolvida a partir das layers 31/30/29 (AA/ADD/AOS) que intersectaram. GUARD DO 6º ESTADO (BLOCKER 2): quando a macrozona é Construída mas NENHUMA das layers 31/30/29 intersectou, retornar `estado:"resolvido_sem_unidade"`, `unidade:null`, `regra:PD_MZC_BASICO` — nunca deixar `regra` cair em `pdRegrasDaZona(null)`/`undefined` (esse era o crash do lote urbano em AAB). Os badges de contexto (aeis/apac/add/eixo/corredor) continuam calculados normalmente neste estado.

    Criar um NOVO bloco de I/O demarcado `// PD_NET_START` … `// PD_NET_END` (mesmo estilo do TERR_NET, ~linha 3467 após TERR_NET_END) contendo:
    - `const PDCACHE = {};` (cache de sessão por `ci`/coordenada — CONTEXT nomeia isso; teto via `capCache(PDCACHE, 60)`)
    - `const PD_SVC_BASE = "https://portalmapa.goiania.go.gov.br/servicogyn/rest/services/MapaServer/Mapa_ModeloEspacial/MapServer";` (PRODUÇÃO — NUNCA `_teste`; host já liberado no CSP)
    - `async function pdConsultarLote(x, y, ci)`: se `PDCACHE[ci]` existe → retorna (cache). Senão dispara em PARALELO, para cada entrada de `PD_LAYERS`, `jsonp({geometry:x+","+y, geometryType:"esriGeometryPoint", inSR:31982, spatialRel:"esriSpatialRelIntersects", outFields:"sigla,nm_des,nm_mzo", returnGeometry:false, f:"json"}, 1, PD_SVC_BASE+"/"+layerId+"/query")`. Coletar via `Promise.allSettled`, mapear cada resultado para `{layer:key, status, value}`, chamar `resolverZonaUI(...)`, gravar em `PDCACHE[ci]` e retornar. Usar `capCache(PDCACHE,60)`. LAZY — esta função só é chamada pela UI do plano 02 (nunca em varredura de setor).
    Estender `loadNetBlock`/adicionar loader para o bloco PD_NET em tests/pd.test.mjs, com `jsonp` stubado que conta chamadas (assert de dedupe: 2ª chamada com mesmo ci NÃO incrementa o contador). Adicionar fixtures das respostas sintéticas das layers (AA/ADD/rural/vazio/erro parcial/Macrozona-Construída-sem-unidade) em tests/fixtures.mjs — a fixture sem-unidade tem macrozona Construída resolvida e AA/ADD/AOS vazias, cobrindo o 6º estado.
  </action>
  <verify>
    <automated>node --test tests/pd.test.mjs</automated>
  </verify>
  <done>Testes de Task 3 verdes (6 estados incl. resolvido_sem_unidade + dedupe do PDCACHE); `grep -q "function resolverZonaUI" radar-goiania.html`, `grep -q "resolvido_sem_unidade" radar-goiania.html`, `grep -q "PD_NET_START" radar-goiania.html`, `grep -q "Mapa_ModeloEspacial" radar-goiania.html`, `grep -q "allSettled" radar-goiania.html`; `grep -q "_teste" radar-goiania.html` NÃO retorna a URL do PD; `npm test` verde (baseline 184 + novos).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → ArcGIS ModeloEspacial | Entrada não confiável entra pela resposta JSON das 9 layers (campos `sigla`/`nm_des`/`nm_mzo`) |
| lei (fonte primária) → PD_TABELA_CA | Números legais entram por curadoria humana; risco = publicar valor não conferido |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | potencialConstrutivo/pdRegrasDaZona/PD_TABELA_CA | mitigate | REGRA DE OURO na ORIGEM: `potencialConstrutivo` retorna `null` quando `conferido!==true`; teste garante que nenhuma sigla `conferido:false` tem `ca_*` numérico; `fonte` cita "Art.", nunca "Anexo" (testado) |
| T-18-02 | Denial of Service | pdConsultarLote/PDCACHE | mitigate | Cache de sessão por `ci` (dedupe testado); lazy (só chamado pela UI do plano 02, nunca em varredura); `capCache(PDCACHE,60)`; 1 lote = 1 bateria |
| T-18-04 | Denial of Service | criterioDetectorPD | mitigate | Fallback gracioso `areaedif/areaterr` rotulado (`criterio:"terreno"`) quando o PD não está disponível — a função NUNCA lança nem bloqueia a lista do detector (testado) |
| T-18-03 | Tampering | pdConsultarLote (fonte) | accept | `outFields` restrito a `sigla,nm_des,nm_mzo` (sem PII); o escape via `esc()` é responsabilidade do render (plano 02) — aqui não há interpolação no DOM |
</threat_model>

<verification>
- `npm test` verde (184 baseline + suíte pd.test.mjs)
- Funções puras carregam via `node:vm` (estão entre 1351 e RADAR_PURE_END)
- `pdConsultarLote` deduplicada por PDCACHE; URL de produção (`Mapa_ModeloEspacial`), nunca `_teste`
- Nenhum número de CA com `conferido:false` sai de função pura
</verification>

<success_criteria>
- PD-02: `PD_TABELA_CA` versionada com AA(6,0)/ADD(5,0) `conferido:true` e fonte por Art.; APAC/AEIS `conferido:false`
- PD-04 (núcleo): `potencialConstrutivo`/`criterioDetectorPD` puros, REGRA DE OURO + fallback testados
- PD-01 (núcleo): `resolverZonaUI` (6 estados, incl. resolvido_sem_unidade) + `pdConsultarLote`/`PDCACHE` (bateria lazy deduplicada) prontos para a UI
</success_criteria>

<output>
Após a conclusão, criar `.planning/phases/18-inteligencia-urbanistica-plano-diretor/18-01-SUMMARY.md`
</output>
