---
phase: 01-dataset-est-tico-de-bairros-corre-o-de-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - PROJETO-radar.md
  - ROADMAP-radar.md
autonomous: true
requirements: [DADOS-03]
must_haves:
  truths:
    - "PROJETO-radar.md §4 no longer claims the layer-3 endpoint rejects returnGeometry=true; it states returnGeometry=true works (verified 2026-07-04)"
    - "ROADMAP-radar.md §0 reflects that returnGeometry=true works on layer 3"
    - "The still-true outFields=* restriction and other quirks (502 under load) are PRESERVED, not removed"
    - "The correction is dated 2026-07-04 and flags that this is undocumented third-party server behavior with no SLA"
  artifacts:
    - path: "PROJETO-radar.md"
      provides: "Corrected §4 endpoint-quirks section"
      contains: "returnGeometry"
    - path: "ROADMAP-radar.md"
      provides: "Corrected §0 validated-facts table"
      contains: "returnGeometry"
  key_links:
    - from: "PROJETO-radar.md §4"
      to: "01-RESEARCH.md §3 (live re-verification)"
      via: "returnGeometry=true works + outFields=* still required"
      pattern: "returnGeometry=true"
    - from: "ROADMAP-radar.md §0"
      to: "01-RESEARCH.md §3"
      via: "validated-facts table row for returnGeometry"
      pattern: "returnGeometry"
---

<objective>
Correct the project documentation so `PROJETO-radar.md §4` and `ROADMAP-radar.md §0` reflect the live-verified reality: the Cadastro Imobiliário layer (`Feature_Base/MapServer/3`) ACCEPTS `returnGeometry=true` (verified 2026-07-04, +~19% payload), reversing the old "rejects returnGeometry" claim — while PRESERVING the still-true `outFields=*`-only restriction and the other documented quirks. Covers DADOS-03.

Purpose: The whole v2.0 milestone premise ("expose existing working geometry" vs "source new geometry") rests on this fact; leaving the false claim in the two canonical docs would mislead every future reader/maintainer.
Output: Edited `PROJETO-radar.md` and `ROADMAP-radar.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-dataset-est-tico-de-bairros-corre-o-de-docs/01-RESEARCH.md

<current_text>
PROJETO-radar.md §4 "Comportamentos não óbvios do servidor". The FALSE claim to fix is the bullet currently at line 49. Surrounding bullets (48, 50, 51) are STILL TRUE and must be left untouched:

- line 48 (KEEP): `- Aceita \`outFields=*\`, mas **rejeita** pedir campos específicos como \`x_coord\`, \`y_coord\`, \`nrquadra\` em \`outFields\` (erro 400). Solução: sempre \`outFields=*\`.`
- line 49 (FIX — this is the wrong claim): `- **Rejeita** \`returnGeometry=true\` nesse endpoint. As coordenadas vêm dos campos \`x_coord\`/\`y_coord\` do próprio registro.`
- line 50 (KEEP): spatial-query bullet
- line 51 (KEEP): JSONP + pagination bullet

ROADMAP-radar.md §0 "Fatos validados no endpoint real" — a table (header line 8, intro line 10 dated 02/07/2026, rows 14-22). It has NO returnGeometry row today. Line 14 (`outFields` = só `outFields=*` funciona) is STILL TRUE — keep it.
</current_text>

<verified_facts>
From 01-RESEARCH.md §3, live 2026-07-04:
- `.../MapServer/3/query?where=cdbairro=3&outFields=*&returnGeometry=true&resultRecordCount=1&f=json` returns 200 OK with real polygon geometry, wkid 31982. returnGeometry=true WORKS.
- `.../MapServer/3/query?where=cdbairro=3&outFields=cdbairro,vlvenal&returnGeometry=false&...` returns 400. The outFields=* restriction STILL HOLDS.
- returnGeometry=true adds ~19% payload (310,189 to 368,355 bytes on a sample), not a multi-MB blowup.
- Undocumented, unversioned third-party server behavior with NO SLA — annotate with a "2026-07-04" date, matching ROADMAP §0's dated-facts convention.
</verified_facts>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Correct returnGeometry claim in PROJETO-radar.md §4 and ROADMAP-radar.md §0</name>
  <read_first>
    - PROJETO-radar.md lines 41-56 (§4 "Fonte de dados e manhas do endpoint")
    - ROADMAP-radar.md lines 8-24 (§0 "Fatos validados no endpoint real" table)
    - .planning/phases/01-dataset-est-tico-de-bairros-corre-o-de-docs/01-RESEARCH.md §3 (Layer 3 returnGeometry re-confirmed)
  </read_first>
  <files>PROJETO-radar.md, ROADMAP-radar.md</files>
  <action>
Two edits. PRESERVE every other quirk — ONLY the `returnGeometry` claim is wrong.

Edit 1 — PROJETO-radar.md §4. Find the exact bullet (currently line 49):

```
- **Rejeita** `returnGeometry=true` nesse endpoint. As coordenadas vêm dos campos `x_coord`/`y_coord` do próprio registro.
```

Replace it with this exact corrected bullet:

```
- **Aceita** `returnGeometry=true` nesse endpoint (verificado ao vivo em 2026-07-04): retorna a geometria real do polígono do lote (wkid 31982), com ~+19% de payload. Corrige a "manha" antiga que afirmava rejeição. Os campos `x_coord`/`y_coord` continuam disponíveis como fonte alternativa de coordenada. Obs.: servidor de terceiro, não documentado e sem SLA — reconfirmar antes de depender dele.
```

Do NOT modify line 48 (the `outFields=*`-only restriction — still true), nor lines 45 (proj4), 50 (spatial query), 51 (JSONP/pagination).

Edit 2 — ROADMAP-radar.md §0 table. Insert this new row into the "Fatos validados no endpoint real" table, immediately AFTER the `outFields` row (line 14):

```
| `returnGeometry=true` (camada 3) | **Aceita** — retorna polígono real (~+19% payload); reconfirmado 2026-07-04 | Corrige a suposição antiga de rejeição; v2.0 orquestra geometria existente, não faz sourcing novo |
```

Do NOT remove or alter line 14 (the `outFields=*`-only fact stays) or any other row.

After editing, confirm via the acceptance-criteria greps that no other §4 quirk or §0 row was changed.
  </action>
  <verify>
    <automated>grep -qi "Aceita.*returnGeometry=true" PROJETO-radar.md && grep -qi "returnGeometry=true.*camada 3" ROADMAP-radar.md && grep -q "2026-07-04" PROJETO-radar.md && grep -q "outFields=\*" PROJETO-radar.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Rejeita.*returnGeometry" PROJETO-radar.md` returns 0 (false claim removed)
    - `grep -qi "Aceita.*returnGeometry=true" PROJETO-radar.md` is true (corrected claim present)
    - `grep -q "2026-07-04" PROJETO-radar.md` is true (dated verification)
    - `grep -q "outFields=\*" PROJETO-radar.md` is true (outFields restriction preserved)
    - `grep -qi "returnGeometry=true.*camada 3" ROADMAP-radar.md` is true (row added to §0)
    - `grep -q "2026-07-04" ROADMAP-radar.md` is true (dated)
    - `grep -qi "só .outFields=.. funciona\|outFields.*funciona" ROADMAP-radar.md` is true (line 14 preserved)
  </acceptance_criteria>
  <done>PROJETO-radar.md §4 states returnGeometry=true works (dated 2026-07-04, the false "Rejeita" bullet gone), ROADMAP-radar.md §0 has a returnGeometry row, and the outFields=*-only restriction plus all other quirks are preserved in both files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Documentation-only edits to two Markdown files already in the repo |

## STRIDE Threat Register

This plan edits two Markdown documentation files. There is NO code, NO network, NO data, NO input, and NO trust boundary introduced or crossed. There are NO high-severity threats — stated explicitly rather than invented.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering (accuracy) | Doc edit could inadvertently delete a still-true quirk (e.g. the outFields=* restriction) | mitigate | Acceptance criteria explicitly grep-assert that `outFields=*` and other quirks remain; only the single false `returnGeometry` bullet is changed. |
</threat_model>

<verification>
- `grep -c "Rejeita.*returnGeometry" PROJETO-radar.md` returns 0.
- `grep -qi "Aceita.*returnGeometry=true" PROJETO-radar.md` is true, dated 2026-07-04.
- ROADMAP-radar.md §0 has a returnGeometry row and the `outFields=*`-only fact is preserved.
</verification>

<success_criteria>
- Both canonical docs (`PROJETO-radar.md §4`, `ROADMAP-radar.md §0`) reflect that `returnGeometry=true` works on layer 3, dated 2026-07-04.
- The still-true `outFields=*`-only restriction and all other documented quirks are preserved intact.
</success_criteria>

<output>
After completion, create `.planning/phases/01-dataset-est-tico-de-bairros-corre-o-de-docs/01-02-SUMMARY.md`
</output>
