---
phase: 12-predio-como-objeto-comercial
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - radar-goiania.html
  - tests/predio.test.mjs
  - tests/fixtures.mjs
autonomous: true
requirements: [PRED-01, PRED-02]
tags: [predio, resumo, ordenacao, comparacao, pure-functions, tdd]

must_haves:
  truths:
    - "resumoPredio(units) calcula, 100% client-side sobre unidades ja carregadas, {n, areaMedia, venalMedio, estimadoMedio, faixaLo, faixaHi} do predio — excluindo garagens/box por default (mesma heuristica isGarage), com campos null (nunca NaN) quando a amostra correspondente esta vazia"
    - "ordenaUnidades(units, criterio) retorna um NOVO array (nunca muta o array de entrada) ordenado por 'padrao'|'oportunidade'|'estimado-asc'|'area-desc' — 'oportunidade' compara o R$/m2 estimado de cada unidade contra a MEDIA do PROPRIO predio (heuristica barata, nunca contra vizinhanca/scoreOportunidade), unidades sem base de comparacao vao para o fim, ordenacao estavel (nunca muda a ordem relativa de empates)"
    - "ehAptoProvavel(a) e uma heuristica deterministica e documentada (uso residencial 1 ou misto 5, e nao-garagem via isGarage) usada pelo filtro 'so aptos provaveis'"
    - "analisePredicoTexto(resumo, meta) gera texto copiavel estilo WhatsApp com nome/Q-L/metricas do predio, omitindo a linha de estimado quando resumo.estimadoMedio for null (nunca 'estimado medio: —' num texto copiavel)"
  artifacts:
    - path: "radar-goiania.html"
      provides: "resumoPredio + ordenaUnidades + ehAptoProvavel + analisePredicoTexto dentro do bloco RADAR_PURE_START..RADAR_PURE_END"
      contains: "function resumoPredio"
    - path: "tests/predio.test.mjs"
      provides: "harness node:test que carrega RADAR_PURE via node:vm (mesmo padrao de tests/doc.test.mjs/tests/negocio.test.mjs) testando as 4 funcoes novas"
      contains: "loadPureBlock"
    - path: "tests/fixtures.mjs"
      provides: "fixtures novas para resumoPredioCasos/ordenaUnidadesCasos/ehAptoProvavelCasos/analisePredicoTextoCasos adicionadas ao objeto FIXTURES existente"
      contains: "resumoPredioCasos"
  key_links:
    - from: "tests/predio.test.mjs"
      to: "radar-goiania.html (bloco RADAR_PURE)"
      via: "leitura de arquivo + node:vm.Script, mesmo loader de tests/doc.test.mjs/tests/negocio.test.mjs"
      pattern: "RADAR_PURE_START"
    - from: "ordenaUnidades(units,'oportunidade')"
      to: "media do proprio predio (nao scoreOportunidade/compare/compsStats)"
      via: "calculo client-side de m2Edif(u) vs media do conjunto — zero request de rede"
      pattern: "function ordenaUnidades"
    - from: "analisePredicoTexto"
      to: "Wave 2 (12-02, copyZapPredio())"
      via: "funcao pura chamada pela UI ao clicar 'Copiar analise do predio' — Wave 2 NAO reimplementa o template de texto"
      pattern: "function analisePredicoTexto"
---

<objective>
Criar 4 funções puras dentro do bloco `RADAR_PURE` de `radar-goiania.html` — `resumoPredio(units)` (métricas agregadas do edifício: nº unidades, área média, venal médio, estimado médio + faixa), `ordenaUnidades(units, criterio)` (ordenação client-side sem mutar o array, com a heurística barata "oportunidade vs. média do próprio prédio" — NUNCA `scoreOportunidade`/`compare`/`compsStats`, que são async e exigem stats de vizinhança via rede), `ehAptoProvavel(a)` (heurística determinística para o filtro "só aptos prováveis") e `analisePredicoTexto(resumo, meta)` (texto copiável WhatsApp-style do resumo do prédio) — com harness de teste TDD (`tests/predio.test.mjs`) cobrindo amostras vazias/sem venal (honesto, nunca NaN), estabilidade da ordenação, exclusão de garagem por default e omissão da linha de estimado no texto copiável quando não há dado.

Purpose: PRED-01/PRED-02 exigem que o resumo do prédio, a ordenação/filtro de unidades e a "análise do prédio" copiável sejam 100% determinísticos e client-side, zero requisição nova (a heurística de "maior oportunidade" NUNCA pode chamar `scoreOportunidade` async — ver 12-CONTEXT.md/12-UI-SPEC.md, nota de constraint técnica). Esta plan constrói a CAMADA PURA (sem DOM) — Wave 2 (12-02) consome as 4 funções para expandir `.bldg-head`, renderizar a barra de ordenação/filtro e o botão "Copiar análise do prédio".
Output: bloco RADAR_PURE ampliado com 4 funções novas; `tests/predio.test.mjs` com fixtures cobrindo amostra vazia/parcial/completa de `resumoPredio`, os 4 critérios de `ordenaUnidades` (incl. estabilidade e não-mutação), casos de `ehAptoProvavel` (residencial/misto/garagem/comercial) e a omissão condicional da linha de estimado em `analisePredicoTexto`; suite completa (`node --test "tests/*.test.mjs"`) passando (79 existentes + novos).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-predio-como-objeto-comercial/12-CONTEXT.md
@.planning/phases/12-predio-como-objeto-comercial/12-UI-SPEC.md

# Fonte de código (ler antes de editar)
@radar-goiania.html
@tests/negocio.test.mjs
@tests/doc.test.mjs
@tests/fixtures.mjs

<facts>
<!-- Verificado por leitura direta deste repo. -->

Bloco RADAR_PURE hoje (linhas ~969-1736 de radar-goiania.html): abre em
`// RADAR_PURE_START — funções puras de matching/normalização. Testadas por tests/busca.test.mjs`
e fecha em `// RADAR_PURE_END`. Já contém (Fases 8/9/10/11/11.1): `isGarage`, `matchApto`,
`matchScoreQ/L/Rua`, `scoreOportunidade`, `scoreConfianca`, `leituraPratica`, `zapResumo/
zapProprietario/zapComprador/zapArgumento/zapRiscos`, `propostaTexto/termoExclusividadeTexto/
contratoTexto/parseMatricula/numeroPorExtenso`, etc. Esta plan ACRESCENTA 4 funções ao MESMO
bloco (não cria um segundo bloco) — o harness de teste corta por índice de string entre os
marcadores, então tudo que precisa ser testável por `node:vm` PRECISA estar dentro deles.

`isGarage(a)` (linha ~984, já existe no bloco): `/\b(BOX|GARAG|VAGA|DEPOSITO|DEP\b|SUBSOLO)/.test(norm(a.incompl))`.
`ehAptoProvavel` desta plan REUSA `isGarage` (chamada interna, mesmo bloco) — não duplica a regex.

`mercadoEstimado(a, privReal)` (linha ~2522, FORA do bloco RADAR_PURE hoje — é função de app, não
pura, porque referencia `window.CAIXA`) retorna `{lo,hi,metodo}` ou `null`. `resumoPredio` e
`ordenaUnidades` desta plan são funções PURAS que recebem os RESULTADOS já calculados por unidade
como parte do objeto de entrada — não podem chamar `mercadoEstimado` diretamente (está fora do
bloco RADAR_PURE, dependeria de `window`). Solução: as funções recebem `units` já enriquecidas
com um campo `__est` opcional por unidade (`{lo,hi}` ou `null`), calculado pela Wave 2 (UI) ANTES
de chamar as funções puras — mesmo padrão de `__scores` (Fase 9/10, cache leve calculado fora do
bloco puro, consumido depois). Ver `<interfaces>` para o shape exato esperado por unidade.

`m2Edif(a)` (linha ~2546, FORA do bloco RADAR_PURE — função de app simples: `vlvenal/areaedif`)
usa apenas `vlvenal`/`areaedif`, ambos campos brutos do cadastro já presentes em cada unidade —
`ordenaUnidades` reimplementa esse cálculo INTERNAMENTE como função auxiliar não-exportada dentro
do bloco puro (é trivial: `(u.vlvenal||0)/(u.areaedif||0)` com guarda de divisão por zero), para não
depender de importar `m2Edif` de fora do bloco.

`brl(n)`/`brlC(n)` (linhas ~952/2520): formatação de moeda. `brlC` faz "R$ X mi"/"R$ X mil" —
está FORA do bloco RADAR_PURE hoje (usa `.toLocaleString` mas está definida depois do marcador
END). `analisePredicoTexto` desta plan NÃO pode chamar `brlC` (fora do bloco) — implementa sua
própria formatação simples internamente (`"R$ "+Math.round(n).toLocaleString("pt-BR")`), suficiente
para o texto copiável (não precisa da variante "mi"/"mil" abreviada — o texto de WhatsApp já é
mais informal que o card).

`unitLabel(a)` (linha ~2547, FORA do bloco): usado pela Wave 2 para exibir "Apto 302" etc. Não é
chamado pelas funções puras desta plan (elas operam sobre agregados/critérios, não etiquetas).
</facts>

<interfaces>
<!-- Assinaturas exatas que a Wave 2 (12-02) vai CHAMAR — contrato duro, não renomear. -->

```js
// PRED-01: resumo agregado do predio a partir das unidades JA FILTRADAS por hideGar quando
// aplicavel (a Wave 2 decide se filtra garagem ANTES de chamar, coerente com o comportamento
// atual de finish()/hideGar — resumoPredio NAO reaplica hideGar, apenas calcula sobre o que
// recebe. Quando o CALLER quiser excluir garagem por default, filtra a lista antes de chamar).
//
// Shape de cada item de `units` esperado por resumoPredio/ordenaUnidades (subconjunto do objeto
// `a` do cadastro + 1 campo opcional calculado pela Wave 2 fora do bloco puro):
//   { areaedif: number|undefined, vlvenal: number|undefined, incompl: string|undefined,
//     uso: number|undefined, __est: {lo:number,hi:number}|null|undefined }
// __est e o resultado de mercadoEstimado(u) ja calculado pela Wave 2 (funcao impura, fora do
// bloco) ANTES de passar units para resumoPredio/ordenaUnidades — NUNCA recalculado aqui.
//
// Retorna SEMPRE um objeto (nunca null, mesmo com units=[]) — campos individuais viram null
// quando a amostra correspondente esta vazia (nunca NaN/undefined):
//   { n: number, areaMedia: number|null, venalMedio: number|null,
//     estimadoMedio: number|null, faixaLo: number|null, faixaHi: number|null }
// - n = units.length (contagem simples, sempre um numero >=0)
// - areaMedia = media de areaedif das unidades com areaedif>0; null se nenhuma tiver area
// - venalMedio = media de vlvenal das unidades com vlvenal>0; null se nenhuma tiver venal
//   (nunca inclui zeros no calculo — mesma regra de venalTxt)
// - estimadoMedio = media de (u.__est.lo+u.__est.hi)/2 das unidades com __est truthy; null se
//   NENHUMA unidade tiver __est
// - faixaLo/faixaHi = min(todos os __est.lo) / max(todos os __est.hi) das unidades com __est;
//   ambos null quando estimadoMedio e null (consistencia: ou os 3 campos de estimativa existem
//   juntos, ou nenhum existe)
function resumoPredio(units): {n, areaMedia, venalMedio, estimadoMedio, faixaLo, faixaHi}

// PRED-02: ordena uma COPIA de units (Object.assign/slice — nunca muta o array recebido nem os
// objetos dentro dele) segundo o criterio. Unidades sem base de comparacao para o criterio ATIVO
// vao para o FIM da lista ordenada (nunca sao escondidas, so perdem prioridade), mantendo ordem
// ESTAVEL entre si (empates preservam a ordem relativa original — usar indice original como
// tie-breaker, nunca confiar em Array.prototype.sort ser estavel em todo engine se o teste
// verificar isso explicitamente, mas Node >=12 e V8 JA garantem sort estavel; ainda assim,
// documentar a decisao com um tie-breaker explicito por indice para clareza e para o teste
// poder verificar sem depender de detalhe de engine).
//
// criterio:
//   "padrao"       -> retorna copia de units NA MESMA ORDEM recebida (no-op de ordenacao)
//   "oportunidade" -> calcula pm2(u) = (u.vlvenal||0)/(u.areaedif||0) (0 ou NaN quando
//                     areaedif/vlvenal ausente -> trata como "sem base", vai pro fim) para cada
//                     unidade elegivel; calcula a MEDIA do pm2 de todas as unidades elegiveis do
//                     MESMO conjunto recebido (heuristica "vs. media do PROPRIO predio", NUNCA
//                     vizinhanca/scoreOportunidade); ordena ASCENDENTE por pm2 (menor R$/m2
//                     relativo a media do predio = maior oportunidade primeiro); unidades sem
//                     pm2 valido vao pro fim, mantendo ordem estavel entre si
//   "estimado-asc" -> ordena ASCENDENTE por (u.__est.lo+u.__est.hi)/2 quando u.__est existe;
//                     unidades sem __est vao pro fim (ordem estavel entre si)
//   "area-desc"    -> ordena DESCENDENTE por u.areaedif quando >0; unidades sem area (undefined/
//                     0/null) vao pro fim (ordem estavel entre si)
// criterio desconhecido/ausente -> comporta-se como "padrao" (fallback seguro, nunca lanca
// excecao).
function ordenaUnidades(units, criterio): Array (nova referencia, nunca === units)

// PRED-02 (filtro "so aptos provaveis"): heuristica deterministica e documentada — residencial
// (uso===1) ou misto (uso===5), E NAO garagem/box (isGarage(a) === false). Uso comercial/outros
// (salas/lojas) retorna false. Documentar no corpo da funcao que e HEURISTICA (nao promete
// "so apartamentos" com certeza absoluta) — mesmo espirito do rotulo honesto do UI-SPEC.
function ehAptoProvavel(a): boolean

// PRED-01 (acao "gerar analise do predio"): monta o texto copiavel WhatsApp-style a partir do
// resumo (retorno de resumoPredio) + metadados do predio. `meta` = { nome: string|null,
// quadra: string|null, lote: string|null, endereco: string|null }. Template EXATO (copywriting
// contract do UI-SPEC):
//
//   "🏢 [nome ou "Edifício"] — Q [quadra] · L [lote]
//   [n] unidades · área média [X] m² · venal médio [R$ Y]" — SO SE areaMedia/venalMedio
//   nao forem null, cada metrica e OMITIDA independentemente se seu campo for null (nunca
//   "área média: —" no texto copiavel) — " · estimado médio [R$ Z] · faixa [R$ Lo–Hi]" SO
//   SE estimadoMedio nao for null (omite a clausula inteira, nunca "estimado médio: —")
//
//   "\n\n📍 [endereco]" — SO SE meta.endereco for truthy (omite o bloco inteiro se ausente)
//   "\nAnálise gerada pelo Radar Fundiário."
//
// Retorna string NAO-VAZIA mesmo com resumo 100% null (n=0, todas as metricas null) — nesse caso
// o texto ainda tem cabecalho + "0 unidades" + a linha de assinatura, nunca uma string vazia ou
// so com "undefined"/"NaN". Nunca chama esc() internamente (texto puro, e a Wave 2 quem decide
// como sanitizar antes de colocar em innerHTML se precisar — aqui e so texto de clipboard).
function analisePredicoTexto(resumo, meta): string
```

Ponto de honestidade OBRIGATÓRIO (testado): `resumoPredio` NUNCA retorna `NaN` em nenhum campo —
amostra vazia sempre produz `null`, nunca `NaN`/`undefined`. Mesma regra vale para `ordenaUnidades`
com critérios que dividem por zero internamente (áreas/venais ausentes tratados como "sem base",
nunca propagam `NaN` para a comparação do `sort`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — criar tests/predio.test.mjs + fixtures (testes falhando)</name>
  <files>tests/predio.test.mjs, tests/fixtures.mjs</files>
  <behavior>
    - resumoPredio([]) retorna {n:0, areaMedia:null, venalMedio:null, estimadoMedio:null, faixaLo:null, faixaHi:null} — amostra vazia honesta, nunca NaN
    - resumoPredio(3 unidades, nenhuma com areaedif>0) retorna areaMedia:null (nunca NaN); mesma unidade sem vlvenal>0 alguma retorna venalMedio:null
    - resumoPredio(unidades com areaedif/vlvenal mistos, algumas 0 e outras >0) calcula a média SÓ sobre as com valor >0 (n continua contando todas as unidades, mas areaMedia/venalMedio ignoram os zeros)
    - resumoPredio(unidades com __est preenchido em algumas, null/undefined em outras) calcula estimadoMedio/faixaLo/faixaHi SÓ sobre as com __est truthy; se NENHUMA tiver __est, os 3 campos são null (nunca 2 de 3)
    - resumoPredio(unidades) — n sempre igual a units.length, independente de quantas têm dados válidos
    - ordenaUnidades(units,"padrao") retorna uma cópia NA MESMA ORDEM (array !== original, conteúdo igual)
    - ordenaUnidades NÃO muta o array recebido nem os objetos dentro dele (assert que o array original permanece na ordem/conteúdo pré-chamada, para qualquer critério)
    - ordenaUnidades(units,"oportunidade"): unidade com pm2 mais baixo relativo à média do conjunto vem primeiro; unidade sem areaedif/vlvenal vai para o fim; 2 unidades com pm2 idêntico mantêm a ordem relativa original (estabilidade)
    - ordenaUnidades(units,"estimado-asc"): unidade com menor (lo+hi)/2 vem primeiro; unidade sem __est vai para o fim
    - ordenaUnidades(units,"area-desc"): unidade com maior areaedif vem primeiro; unidade com areaedif=0/undefined vai para o fim
    - ordenaUnidades(units,"criterio-invalido") comporta-se como "padrao" (fallback seguro, não lança exceção)
    - ehAptoProvavel({uso:1, incompl:"APTO 302"}) → true; ehAptoProvavel({uso:5, incompl:"APTO 10"}) → true (misto); ehAptoProvavel({uso:1, incompl:"BOX 12"}) → false (garagem, mesmo uso residencial); ehAptoProvavel({uso:3, incompl:"SALA 5"}) → false (comercial)
    - analisePredicoTexto(resumo completo, meta completo) contém "🏢", nome do edifício, "Q "+quadra, "L "+lote, "unidades", "área média", "venal médio", "estimado médio", "faixa", endereço, "Análise gerada pelo Radar Fundiário."
    - analisePredicoTexto(resumo com estimadoMedio:null, meta completo) NÃO contém "estimado médio" nem "faixa" (cláusula omitida por completo, nunca "estimado médio: —")
    - analisePredicoTexto(resumo com areaMedia:null, meta completo) NÃO contém "área média" (omissão independente por métrica)
    - analisePredicoTexto(resumo, meta com endereco:null) NÃO contém "📍" (bloco de endereço omitido)
    - analisePredicoTexto(resumo com n:0 e todas as métricas null, meta com nome:null) retorna string não-vazia contendo "🏢", "Edifício" (fallback de nome), "0 unidades", "Análise gerada pelo Radar Fundiário." — nunca contém "undefined"/"NaN"/"null"
    - nenhuma das 4 funções, para qualquer fixture, retorna/produz string contendo literalmente "undefined" ou "NaN" (assert negativo global nas saídas de texto)
  </behavior>
  <action>
Criar `tests/predio.test.mjs` reusando EXATAMENTE o padrão de loader de `tests/negocio.test.mjs`/
`tests/doc.test.mjs` (`loadPureBlock()` via `node:vm`, cortando por
`RADAR_PURE_START`/`RADAR_PURE_END`, com os MESMOS guards de `assert.ok(src.includes(...))` para
cada função nova antes de montar o sandbox — copiar o padrão, adaptando a lista de funções
exportadas em `globalThis.__exports` para incluir as 4 novas:
`resumoPredio,ordenaUnidades,ehAptoProvavel,analisePredicoTexto`.

Adicionar ao objeto `FIXTURES` existente em `tests/fixtures.mjs` (não substituir, ACRESCENTAR
chaves novas) as fixtures: `resumoPredioCasos` (amostra vazia, sem área, sem venal, mista com
zeros, com/sem `__est`), `ordenaUnidadesCasos` (unidades com pm2/estimado/área variados, incluindo
casos de empate para testar estabilidade e casos sem dado para testar "vai pro fim"),
`ehAptoProvavelCasos` (uso 1/5/3, com/sem incompl de garagem), `analisePredicoTextoCasos` (resumo
completo, resumo sem estimativa, resumo sem área, resumo zerado, meta com/sem endereço, meta sem
nome).

Escrever os testes conforme `<behavior>` — DEVEM FALHAR nesta task (RED), pois as 4 funções ainda
não existem em `radar-goiania.html`. Rodar `node --test "tests/predio.test.mjs"` e confirmar
falha (funções ausentes do bloco RADAR_PURE) — este é o estado esperado ao final da Task 1.
  </action>
  <verify>
    <automated>node --test "tests/predio.test.mjs" 2>&1 | grep -qi "fail" && echo "OK: RED confirmado (testes falham, funcoes ainda nao existem)"</automated>
  </verify>
  <done>tests/predio.test.mjs e fixtures novas em tests/fixtures.mjs existem; rodar a suite falha porque resumoPredio/ordenaUnidades/ehAptoProvavel/analisePredicoTexto ainda não existem no bloco RADAR_PURE — estado RED documentado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implementar as 4 funções dentro do bloco RADAR_PURE</name>
  <files>radar-goiania.html</files>
  <action>
Adicionar as 4 funções de `&lt;interfaces&gt;` DENTRO do bloco `RADAR_PURE_START`...`RADAR_PURE_END`,
imediatamente ANTES do marcador `// RADAR_PURE_END` (seção nova, comentada como "PRED-01/02
(Fase 12, 12-01): resumo do prédio + ordenação/filtro de unidades + análise copiável — puro, zero
requisição de rede, 'maior oportunidade' compara SEMPRE contra a média do PRÓPRIO prédio, nunca
contra scoreOportunidade/compare/compsStats (async, exigem stats de vizinhança via rede — caro
demais para ordenar uma lista local)").

Ordem de implementação sugerida:

- Função auxiliar interna (não exportada) `const __pm2=u=>{const v=u.vlvenal||0,ae=u.areaedif||0;return (v>0&&ae>0)?v/ae:null;};`
  reimplementação local de `m2Edif` (que vive fora do bloco puro) — evita depender de import
  externo dentro do bloco testável isoladamente por `node:vm`.

- `resumoPredio(units)`: `const list=units||[];const n=list.length;`. Filtra
  `list.filter(u=>u.areaedif>0)` para `areaMedia` (média ou `null` se filtro vazio, sempre via
  `arr.length?arr.reduce(...)/arr.length:null` — nunca `0/0`). Mesma lógica para `venalMedio`
  sobre `vlvenal>0`. Para estimativa: `const comEst=list.filter(u=>u.__est);` — se
  `comEst.length===0`, os 3 campos (`estimadoMedio`,`faixaLo`,`faixaHi`) são `null`; senão,
  `estimadoMedio` = média de `(u.__est.lo+u.__est.hi)/2`, `faixaLo`=`Math.min(...comEst.map(u=>u.__est.lo))`,
  `faixaHi`=`Math.max(...comEst.map(u=>u.__est.hi))`.

- `ordenaUnidades(units, criterio)`: `const arr=(units||[]).map((u,i)=>({u,i}));` (empacota
  índice original para tie-breaker estável) — aplica o critério:
  - `"padrao"`/default → `arr.sort((a,b)=>a.i-b.i)` (no-op, ordem original)
  - `"oportunidade"` → calcula `pm2` de cada item via `__pm2`; calcula `media` só sobre os com
    `pm2!=null`; define chave de ordenação `key=pm2==null?Infinity:pm2` (garante "sem base vai pro
    fim" ao ordenar ASCENDENTE); `arr.sort((a,b)=>{const ka=key(a),kb=key(b);return ka!==kb?ka-kb:a.i-b.i;})`
    (tie-breaker por índice original garante estabilidade explícita, não depende de engine)
  - `"estimado-asc"` → `key=u=>u.__est?(u.__est.lo+u.__est.hi)/2:Infinity`, mesmo padrão de sort
    com tie-breaker por índice
  - `"area-desc"` → `key=u=>(u.areaedif>0)?-u.areaedif:Infinity` (negativo inverte para
    descendente reusando a mesma comparação ascendente + fim para "sem base"), mesmo tie-breaker
  - Critério desconhecido → cai no branch `"padrao"` (nunca lança exceção)
  - Retorna `arr.map(x=>x.u)` (novo array; os objetos `u` internos são as MESMAS referências —
    não precisa clonar objetos, só nunca reordenar/remover do array ORIGINAL recebido).

- `ehAptoProvavel(a)`: `return (a.uso===1||a.uso===5)&&!isGarage(a);` — reusa `isGarage` já
  existente no mesmo bloco. Comentário inline documentando que é heurística (uso residencial/misto
  E não-garagem), nunca uma certeza absoluta.

- `analisePredicoTexto(resumo, meta)`: `const r=resumo||{};const m=meta||{};`. Formata número
  simples internamente: `const rs=n=>n==null?null:"R$ "+Math.round(n).toLocaleString("pt-BR");`
  Monta linhas condicionalmente (array de strings, filtra vazios, `join(" · ")` para a segunda
  linha): sempre inclui `r.n+" unidades"`; inclui `"área média "+Math.round(r.areaMedia)+" m²"` só
  se `r.areaMedia!=null`; inclui `"venal médio "+rs(r.venalMedio)` só se `r.venalMedio!=null`;
  inclui `"estimado médio "+rs(r.estimadoMedio)+" · faixa "+rs(r.faixaLo)+"–"+rs(r.faixaHi)` só se
  `r.estimadoMedio!=null` (cláusula inteira condicional, nunca parcial). Cabeçalho:
  `"🏢 "+(m.nome||"Edifício")+" — Q "+(m.quadra||"—")+" · L "+(m.lote||"—")`. Bloco de endereço
  (`"\n\n📍 "+m.endereco`) só se `m.endereco` truthy. Fecha sempre com
  `"\nAnálise gerada pelo Radar Fundiário."`.

Rodar `node --test "tests/predio.test.mjs"` até todos os testes passarem (GREEN). Rodar também
`node --test "tests/*.test.mjs"` (suite completa) para confirmar zero regressão nos 79 testes
existentes.
  </action>
  <verify>
    <automated>node --test "tests/*.test.mjs" 2>&1 | tail -25</automated>
  </verify>
  <done>As 4 funções existem dentro do bloco RADAR_PURE; node --test "tests/*.test.mjs" passa 100% (predio.test.mjs novo GREEN + zero regressão nos 79 testes existentes); resumoPredio nunca retorna NaN (amostra vazia = null); ordenaUnidades nunca muta o array de entrada e é estável; ehAptoProvavel exclui garagem mesmo com uso residencial; analisePredicoTexto omite condicionalmente cada métrica ausente sem nunca gerar "undefined"/"NaN"/"null" no texto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| units (cadastro já carregado em memória, `LAST`) -> resumoPredio/ordenaUnidades | dado já validado/normalizado pela busca (Fases 7/8) — nenhuma nova superfície de entrada externa; risco é apenas lógico (cálculo incorreto), não de segurança |
| resumoPredio/analisePredicoTexto -> texto copiável (clipboard) | texto 100% determinístico a partir de dados numéricos do cadastro — nenhuma interpolação de texto livre do usuário; `esc()` é responsabilidade da Wave 2 ao decidir onde exibir (não desta camada pura) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | ordenaUnidades mutando o array `LAST`/`units` recebido (efeito colateral escondido que corromperia a ordem da lista geral fora do prédio reordenado) | mitigate | teste explícito de não-mutação (array original comparado por conteúdo/ordem antes e depois da chamada, para todo critério) — contrato duro documentado na assinatura |
| T-12-02 | Repudiation | analisePredicoTexto produzindo texto com "estimado médio: —" ou métrica fabricada quando o dado não existe (soa amador/impreciso num texto que o corretor copia para o cliente) | mitigate | testes explícitos de omissão condicional por métrica (cada campo null omite sua própria linha/cláusula, nunca um placeholder honesto-mas-feio) |
| T-12-03 | Tampering | ordenaUnidades("oportunidade") comparando contra a heurística cara (scoreOportunidade/vizinhança) por engano, disparando request de rede indireta | accept — mitigado por design | a função é 100% síncrona e pura (sem `async`/`fetch` no corpo); qualquer regressão futura que tente injetar uma chamada assíncrona quebra a assinatura síncrona testada e o teste de "não lança/não retorna Promise" |

**Sem ameaças de alta severidade.** Esta plan não toca DOM, localStorage ou rede — é camada pura de agregação/ordenação/texto, testável 100% por fixtures.
</threat_model>

<verification>
- `node --test "tests/*.test.mjs"` passa 100% (predio.test.mjs novo + demais sem regressão — 79 existentes + novos)
- `resumoPredio([])` retorna todos os campos numéricos como `null` (nunca `NaN`) exceto `n:0`
- `ordenaUnidades` nunca muta o array/objetos recebidos, é estável em empates, e critério inválido cai em "padrao" sem lançar exceção
- `ehAptoProvavel` exclui qualquer unidade que `isGarage()` identifique, mesmo com `uso` residencial
- `analisePredicoTexto` nunca produz "undefined"/"NaN"/"null" em nenhuma combinação de fixture, e omite cada cláusula de métrica independentemente quando ausente
</verification>

<success_criteria>
- PRED-01 (camada de cálculo do resumo do prédio) cumprido: `resumoPredio`/`analisePredicoTexto` prontas para a Wave 2 consumir
- PRED-02 (camada de ordenação/filtro) cumprido: `ordenaUnidades`/`ehAptoProvavel` cobrem os 4 critérios + heurística de "aptos prováveis" sem nenhuma requisição de rede
- Contrato do campo `__est` (calculado fora do bloco puro pela Wave 2 via `mercadoEstimado`) documentado e travado — Wave 2 não precisa reabrir esta plan para saber o shape esperado
- Wave 2 (12-02) pode chamar as 4 funções sem reabrir esta plan — contrato de retorno travado
</success_criteria>

<output>
Após completar, criar `.planning/phases/12-predio-como-objeto-comercial/12-01-SUMMARY.md`.
Incluir no SUMMARY: assinatura exata das 4 funções, resultado da suite completa de testes
(contagem pass/fail), o contrato exato do campo `__est` por unidade (para a Wave 2 montar
corretamente antes de chamar `resumoPredio`/`ordenaUnidades`), e uma nota explícita confirmando
que `ordenaUnidades("oportunidade")` NUNCA invoca `scoreOportunidade`/`compare`/`compsStats` (zero
requisição de rede), citando a linha/trecho do código que prova a ausência de `async`/`fetch` no
corpo da função.
</output>
