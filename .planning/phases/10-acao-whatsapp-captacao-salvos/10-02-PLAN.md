---
phase: 10-acao-whatsapp-captacao-salvos
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - radar-goiania.html
autonomous: true
requirements: [SALV-01, ACAO-01]
tags: [localstorage, oportunidades, historico, ficha, painel-consulta, lei-da-tela]

must_haves:
  truths:
    - "Botao '⭐ Salvar oportunidade' aparece em #dActsPrim como 2a secundaria (Gerar documento primaria · Ver comparaveis · ⭐ Salvar oportunidade); 'Copiar inscrição' DESCEU para #dActsMore (lei da tela: nunca mais de 1 primaria + 2 secundarias visiveis)"
    - "Salvar grava em localStorage radar_oportunidades via oportunidadeItem() (allowlist, 10-01) + savedAt; falha de escrita produz toast visivel ('Não foi possível salvar — armazenamento do navegador indisponível ou cheio.') e o botao NAO muda de estado"
    - "Toggle salvo/nao-salvo: rotulo 'Salvar oportunidade' <-> '✓ Salva — remover?' com aria-pressed sincronizado; estado recalculado a cada abertura de ficha lendo radar_oportunidades"
    - "Painel Consulta mostra bloco 'Minhas oportunidades' (com contador; estado vazio com mensagem, nunca some) ACIMA do #emptyState e bloco 'Histórico' (discreto, so existe com >=1 item) ABAIXO; ambos ocultos quando ha resultados de busca"
    - "Abrir qualquer ficha adiciona automaticamente ao radar_historico (allowlist + visitedAt, FIFO cap 30 via histAdd de 10-01); tocar num item de Oportunidades/Historico reabre a ficha pelo MESMO caminho do deep-link ?insc= (setMode('insc') + buscar())"
  artifacts:
    - path: "radar-goiania.html"
      provides: "Funcoes oppLoad/oppSave/oppTem/toggleOportunidade/removerOportunidade/histPush/limparHistorico/renderSavedBlocks/abrirOportunidade + CSS .savedblock/.saveditem/.acts-save + HTML dos blocos em #results"
      contains: "radar_oportunidades"
    - path: "radar-goiania.html"
      provides: "Auto-add ao historico dentro de showDetail() (apos DCUR=a) chamando histPush(a)"
      contains: "radar_historico"
  key_links:
    - from: "toggleOportunidade()"
      to: "oportunidadeItem(a, extras) (bloco RADAR_PURE, 10-01)"
      via: "monta extras {faixaLo,faixaHi,scoreOportunidade,scoreConfianca} a partir de mercadoEstimado(DCUR) + DCUR.__scores (cache leve) e grava o retorno + savedAt"
      pattern: "oportunidadeItem\\("
    - from: "showDetail(a,ll)"
      to: "histPush(a) -> histAdd(lista,item,30) (RADAR_PURE, 10-01)"
      via: "chamada apos DCUR=a; item = oportunidadeItem(a,extras) + visitedAt"
      pattern: "histPush\\("
    - from: ".saveditem (blocos do painel Consulta)"
      to: "abrirOportunidade(insc) -> setMode('insc') + buscar()"
      via: "mesmo caminho do deep-link ?insc= ja existente no init (linha ~2769) — preenche #insc, setMode, buscar()"
      pattern: "function abrirOportunidade"
    - from: "renderSavedBlocks()"
      to: "#results / #emptyState"
      via: "insere/atualiza os blocos como irmaos de #emptyState sem destruir #exampleChips (Fase 8)"
      pattern: "oportunidadesBlock"
---

<objective>
Implementar a persistência de SALV-01 (localStorage `radar_oportunidades` + `radar_historico`, allowlist via `oportunidadeItem()` de 10-01, falha de escrita SEMPRE visível via toast) e a UI de acesso: botão "⭐ Salvar oportunidade" na ficha (toggle, lei da tela: substitui "Copiar inscrição" nas visíveis), blocos "Minhas oportunidades" (+contador, estado vazio orientativo) e "Histórico" (discreto, auto-populado ao abrir ficha, FIFO 30) no painel Consulta, com reabertura de ficha pelo caminho do deep-link `?insc=`.

Purpose: SALV-01 exige que o corretor guarde o que interessa e reabra o app dias depois vendo o mesmo — sem servidor, sem PII de terceiro, sem falha silenciosa. ACAO-01 avança: o painel Consulta vazio ganha ações reais (reabrir oportunidades/histórico) além dos exemplos da Fase 8.
Output: `radar-goiania.html` com as funções de persistência, o toggle na ficha, os 2 blocos no painel Consulta e o auto-add de histórico — tudo sobre os helpers puros já testados de 10-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-acao-whatsapp-captacao-salvos/10-CONTEXT.md
@.planning/phases/10-acao-whatsapp-captacao-salvos/10-UI-SPEC.md
@.planning/phases/10-acao-whatsapp-captacao-salvos/10-01-SUMMARY.md

# Fonte de código (ler antes de editar)
@radar-goiania.html

<facts>
<!-- Verificado por leitura direta deste repo (estado pós-Fase 9 + 10-01). -->

`#dActsPrim` hoje é populado por `showDetail()` (linha ~2184) com EXATAMENTE:
```js
document.getElementById("dActsPrim").innerHTML=`
  <button type="button" class="primary" onclick="abrirLaudo()">📄 Gerar documento</button>
  <button type="button" onclick="document.getElementById('dComps').scrollIntoView({behavior:REDUCE?'auto':'smooth'})">📊 Ver comparáveis</button>
  <button type="button" onclick="copyInsc('${inscUse}')">Copiar inscrição ⧉</button>`;
```
`#dActsMore` (linha ~2196) recebe custos + CND + copiar link + mapLinks. Esta plan MOVE o botão
"Copiar inscrição ⧉" de `#dActsPrim` para o TOPO de `#dActsMore` e coloca "⭐ Salvar oportunidade"
no lugar (UI-SPEC Component 2 — decisão travada: nunca 3 secundárias visíveis).

`#results`/`#emptyState` (linhas 645-656): `#emptyState` com `.big` + `#exampleChips` (4 botões
`data-example`). ATENÇÃO: `render(list,opts)` (linha ~1892) faz `box.innerHTML=html` sobre
`#results` INTEIRO quando há resultados — ou seja, qualquer bloco inserido DENTRO de `#results` é
destruído a cada busca. Consequência estrutural: os blocos de Oportunidades/Histórico devem ser
REINSERIDOS por `renderSavedBlocks()` sempre que o estado "vazio" volta (ver decisão em interfaces).
O estado vazio também é restaurado em outros pontos (`document.getElementById("results").innerHTML=
'<div class="empty">...` no init de erro, linha ~2764) — o ponto único de re-render dos blocos é a
função `renderSavedBlocks()` chamada: (a) no init, (b) após salvar/remover/limpar, (c) ao fechar a
ficha se a busca está vazia — na prática, chamar sempre que os dados mudam + no init é suficiente,
pois `render()` só roda quando HÁ resultados (blocos ocultos nesse estado, per UI-SPEC).

Deep-link `?insc=` no init (linhas 2769-2777): `document.getElementById("insc").value=digits;
setMode("insc"); buscar();` — `abrirOportunidade(insc)` reusa EXATAMENTE este caminho (mesma
disciplina de SEARCHTOKEN embutida em buscar(), zero lógica nova de busca). No mobile é preciso
também `setView('busca')`? NÃO — buscar() já conduz o fluxo (finish() decide a tela); replicar o
comportamento do init literalmente.

`showDetail(a,ll)` define `DCUR=a` na linha ~2171. O auto-add de histórico entra LOGO APÓS
(`histPush(a)`), garantindo 1 entrada por abertura de ficha. Dedupe: se o último item do histórico
tem a mesma insc, atualizar `visitedAt` em vez de duplicar (decisão de discrição — evita 30 cópias
do mesmo imóvel ao reabrir).

Scores atuais para `extras`: `atualizarScores(a,statsR)` (linha ~2097) calcula op/conf reais pós-
compare(); `showDetail()` calcula conf inicial (nComps=0) e op=null. Para o item salvo, usar o que
estiver disponível NO MOMENTO do clique em salvar: armazenar em `DCUR.__scores={op,conf}` (cache
leve no objeto, mesmo padrão de `r._below`/`__compsStats` citado em 09-02) toda vez que
`renderScoresInto(op,conf)` roda — `toggleOportunidade()` lê `DCUR.__scores` sem recalcular.
IMPORTANTE: `renderScoresInto` é chamada tanto por `showDetail` quanto por `atualizarScores` — o
cache deve ser gravado pelos CHAMADORES (que têm `a` em escopo), não dentro de `renderScoresInto`
(que não recebe `a`): 1 linha `a.__scores={op,conf};` antes de cada chamada nos 2 call sites.

`mercadoEstimado(a)` (linha ~1860) retorna `{lo,hi,metodo}` ou null — faixaLo/faixaHi do item vêm
daqui (arredondar com Math.round; null quando sem base).

Formato dos rotulos no item salvo: `scoreOportunidade` do item = `op?op.rotulo:null` (string curta
"Boa oportunidade" — o UI-SPEC mostra "oportunidade: boa" no meta; usar o rotulo completo é
aceitável e mais claro); `scoreConfianca` = `conf?conf.nivel:null` ("alta"/"media"/"baixa").

Toast (linha 1603): `toast(msg)` — reuso direto. Copywriting travado no UI-SPEC:
"Oportunidade salva." / "Oportunidade removida." / "Não foi possível salvar — armazenamento do
navegador indisponível ou cheio." / vazio: "Nenhuma oportunidade salva ainda — toque ⭐ numa ficha."

Padrão de leitura de localStorage (try/catch silencioso): linha 2349 `let prof={};try{prof=
JSON.parse(localStorage.getItem("radar_prof")||"{}");}catch(e){}` — LEITURA silenciosa ok; ESCRITA
das chaves novas SEMPRE com toast na falha (regra nova desta fase, UI-SPEC "Persistência").

esc()/inline-onclick contract (IN-01): NUNCA interpolar texto livre em string JS de onclick.
`.saveditem` usa `data-insc="${esc(item.insc)}"` no elemento e `onclick="abrirOportunidade(this)"`
lendo `this.dataset.insc` (ou `this.closest('[data-insc]').dataset.insc` no botão de remover) —
mesmo padrão de `verNoMapa(this)`/`data-ci` estabelecido no 08-REVIEW. insc é só dígitos, mas a
disciplina vale para TODO caminho novo.
</facts>

<interfaces>
<!-- Contrato criado por esta plan — consumido pela Wave 3 (10-03) e por manutenção futura. -->

```js
// Persistência (todas com try/catch; ESCRITA com toast visível na falha)
function oppLoad(){}            // -> array de radar_oportunidades ([] em falha/ausência, leitura silenciosa)
function oppSave(arr){}         // -> boolean (true=gravou; false=falhou E JÁ mostrou o toast de falha)
function histLoad(){}           // -> array de radar_historico ([] em falha)
function histSave(arr){}        // -> boolean (mesma disciplina de oppSave)
function oppTem(insc){}         // -> boolean: insc está em radar_oportunidades?
function toggleOportunidade(){} // salva/remove o DCUR; atualiza botão + blocos + toast
function removerOportunidade(el){} // remove item pelo data-insc do elemento; re-render + toast
function histPush(a){}          // monta item allowlist (oportunidadeItem + visitedAt) e grava com histAdd(...,30); dedupe do último
function limparHistorico(){}    // zera radar_historico; re-render
function renderSavedBlocks(){}  // (re)insere/atualiza os blocos no painel Consulta (idempotente)
function abrirOportunidade(el){} // lê insc do data-insc (elemento) OU aceita string; preenche #insc, setMode('insc'), buscar()
function renderSaveBtn(){}      // sincroniza o botão ⭐ da ficha com oppTem(insc do DCUR)
```

HTML dos blocos (UI-SPEC Components 3/4 — estrutura sem nested button, nota de a11y):
```html
<div class="savedblock" id="oportunidadesBlock">
  <div class="savedblock-head">
    <span class="savedblock-tt">⭐ Minhas oportunidades</span>
    <span class="savedblock-count" id="oportunidadesCount">3</span>
  </div>
  <div class="savedlist" id="oportunidadesList">
    <div class="saveditem-row" data-insc="3020150346">
      <button type="button" class="saveditem" onclick="abrirOportunidade(this)">
        <span class="saveditem-addr">Rua Portugal, 582 — Setor Bueno</span>
        <span class="saveditem-meta">Q 45 · L 12 · faixa R$ 690 mil–780 mil · oportunidade: Boa oportunidade · salva em 05/07</span>
      </button>
      <button type="button" class="saveditem-rm" onclick="removerOportunidade(this)" aria-label="Remover esta oportunidade">×</button>
    </div>
  </div>
</div>
<!-- vazio: <div class="savedempty">Nenhuma oportunidade salva ainda — toque ⭐ numa ficha.</div> no lugar de .savedlist -->

<div class="savedblock savedblock-hist" id="historicoBlock"><!-- só existe com >=1 item -->
  <div class="savedblock-head">
    <span class="savedblock-tt">🕘 Histórico</span>
    <span style="display:flex;gap:8px;align-items:center">
      <button type="button" class="savedblock-clear" onclick="limparHistorico()">Limpar</button>
      <span class="savedblock-count" id="historicoCount">12</span>
    </span>
  </div>
  <div class="savedlist savedlist-hist" id="historicoList">
    <div class="saveditem-row" data-insc="3020150346">
      <button type="button" class="saveditem saveditem-hist" onclick="abrirOportunidade(this)">
        <span class="saveditem-addr">Rua Portugal, 582 — Setor Bueno</span>
        <span class="saveditem-meta">consultado hoje às 14:32</span>
      </button>
    </div>
  </div>
</div>
```

Posição (UI-SPEC Component 3): Oportunidades ACIMA de `#emptyState`, Histórico ABAIXO (depois dos
chips). Como `render()` destrói `#results`, `renderSavedBlocks()` é IDEMPOTENTE: remove os blocos
existentes (se houver) e reinsere via `insertAdjacentHTML`/`insertBefore` relativos a `#emptyState`
QUANDO `#emptyState` existe no DOM; quando não existe (busca com resultados), não insere nada
(blocos ocultos nesse estado — regra de posição do UI-SPEC).

Botão da ficha (UI-SPEC Component 2 — os DOIS estados):
```html
<button type="button" class="acts-save" onclick="toggleOportunidade()" aria-pressed="false">⭐ Salvar oportunidade</button>
<button type="button" class="acts-save is-saved" onclick="toggleOportunidade()" aria-pressed="true">✓ Salva — remover?</button>
```

CSS novo (acrescentar junto ao bloco `.detail`/`.maisopcoes`, linhas ~245-296; zero hex novo,
tokens/medidas EXATOS do UI-SPEC):
```css
.acts-save.is-saved{background:var(--accent);border-color:var(--accent);color:#fff}
.acts-save.is-saved:hover{background:var(--accent-ink)}
.savedblock{background:var(--paper);border:1.5px solid var(--line);border-radius:2px;padding:12px 14px;margin:0 16px 14px}
.savedblock-head{display:flex;justify-content:space-between;align-items:center;margin-bottom:8px}
.savedblock-tt{font:700 13px/1.2 "IBM Plex Sans";color:var(--ink)}
.savedblock-count{font:700 11px/1 "IBM Plex Mono",monospace;color:var(--muted);background:var(--paper-2);border:1px solid var(--line);border-radius:2px;padding:3px 7px;min-width:20px;text-align:center}
.savedblock-clear{font:600 11px/1 "IBM Plex Sans";color:var(--muted);background:transparent;border:0;text-decoration:underline;min-height:32px;cursor:pointer}
.saveditem-row{display:flex;align-items:stretch;gap:4px;border-top:1px solid var(--line)}
.saveditem-row:first-child{border-top:0}
.saveditem{flex:1;display:flex;flex-direction:column;align-items:flex-start;gap:4px;min-height:44px;padding:8px 4px;background:transparent;border:0;cursor:pointer;text-align:left;font-family:inherit}
.saveditem-addr{font:700 13.5px/1.25 "IBM Plex Sans";color:var(--ink)}
.saveditem-hist .saveditem-addr{font-weight:600}
.saveditem-meta{font:500 11.5px/1.3 "IBM Plex Sans";color:var(--muted)}
.saveditem-rm{min-width:44px;min-height:44px;border:0;background:transparent;color:var(--muted);font-size:18px;cursor:pointer}
.saveditem-rm:hover,.saveditem-rm:focus-visible{color:var(--accent)}
.savedempty{font:500 12.5px/1.5 "IBM Plex Sans";color:var(--muted);padding:6px 2px}
```
(`.acts-save` estado não-salvo herda `.detail .acts button` já existente — sem regra extra; a
margem lateral `margin:0 16px 14px` segue o gutter do painel — conferir o padding real de
`#results`/`.empty` no arquivo e ajustar para alinhar com os cards existentes.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persistência + toggle ⭐ na ficha (lei da tela: Copiar inscrição desce para Mais opções)</name>
  <files>radar-goiania.html</files>
  <behavior>
    - showDetail() popula #dActsPrim com EXATAMENTE 3 botoes: primary abrirLaudo() + Ver comparaveis + botao .acts-save (⭐/✓ conforme oppTem); a string "Copiar inscrição ⧉" aparece agora dentro do innerHTML de #dActsMore (primeiro item), NAO em #dActsPrim
    - toggleOportunidade() com DCUR nao salvo: oppSave retorna true -> botao vira "✓ Salva — remover?" (classe is-saved, aria-pressed=true) + toast "Oportunidade salva." + renderSavedBlocks()
    - toggleOportunidade() com DCUR ja salvo: remove do array + toast "Oportunidade removida." + botao volta ao estado nao-salvo
    - oppSave/histSave com localStorage.setItem lancando excecao: retorna false + toast "Não foi possível salvar — armazenamento do navegador indisponível ou cheio."; toggleOportunidade NAO muda o estado do botao quando oppSave retorna false
    - Item gravado = oportunidadeItem(DCUR, extras) + savedAt (ISO string); extras montado de mercadoEstimado(DCUR) (faixaLo/faixaHi arredondados ou null) + DCUR.__scores (rotulo/nivel ou null)
  </behavior>
  <action>
1. Criar as funções `oppLoad/oppSave/histLoad/histSave/oppTem/toggleOportunidade/renderSaveBtn`
   (assinaturas de `&lt;interfaces&gt;`) num bloco novo comentado `/* ---- Fase 10 SALV-01:
   oportunidades + historico (localStorage, allowlist via oportunidadeItem — 10-01) ---- */`,
   posicionado perto de `copyInsc`/`copyLink` (linha ~2219). Leitura com try/catch silencioso
   (padrão radar_prof); escrita com try/catch + `toast("Não foi possível salvar — armazenamento do
   navegador indisponível ou cheio.")` no catch e `return false` — NUNCA falha silenciosa (regra
   travada no UI-SPEC/CONTEXT.md).
2. `toggleOportunidade()`: guard `if(!DCUR)return;` (mesma disciplina dos handlers de ficha).
   Calcular insc=`clean(DCUR.nrinscr||DCUR.ci)`. Se `oppTem(insc)`: filtrar fora + `oppSave` +
   toast "Oportunidade removida.". Senão: montar `extras` — `const em=mercadoEstimado(DCUR);
   const sc=DCUR.__scores||{};` → `{faixaLo:em?Math.round(em.lo):null, faixaHi:em?Math.round(em.hi):
   null, scoreOportunidade:sc.op?sc.op.rotulo:null, scoreConfianca:sc.conf?sc.conf.nivel:null}` —
   item=`Object.assign(oportunidadeItem(DCUR,extras),{savedAt:new Date().toISOString()})`, push +
   `oppSave`. SÓ se oppSave retornar true: toast "Oportunidade salva." + `renderSaveBtn()` +
   `renderSavedBlocks()`. Se false: nada muda visualmente (o toast de falha já saiu de oppSave).
3. Cache `__scores`: nos 2 call sites de `renderScoresInto(op,conf)` (dentro de `showDetail()` linha
   ~2147 e dentro de `atualizarScores()` linha ~2107), acrescentar `a.__scores={op,conf};` na linha
   anterior à chamada (em `showDetail` o objeto é `a`; em `atualizarScores` também é `a` — ambos em
   escopo).
4. Lei da tela em `showDetail()`: substituir o 3º botão de `#dActsPrim` ("Copiar inscrição ⧉") por
   `<button type="button" class="acts-save" onclick="toggleOportunidade()" aria-pressed="false">⭐
   Salvar oportunidade</button>` e adicionar o botão "Copiar inscrição ⧉" como PRIMEIRO item do
   innerHTML de `#dActsMore` (antes de "Custos de compra") — mesmo `copyInsc('${inscUse}')` de hoje
   (inscUse já é esc()-ado, padrão aceito em T-09-05). Logo após popular `#dActsPrim`, chamar
   `renderSaveBtn()` — que lê `oppTem(...)` e, se salvo, troca rotulo/classe/aria-pressed do botão
   `.acts-save` (estado recalculado a CADA abertura de ficha, UI-SPEC Component 2).
5. CSS: acrescentar `.acts-save.is-saved{...}` (2 regras de `&lt;interfaces&gt;`) junto ao bloco
   `.detail .acts` (linha ~482) — estado não-salvo herda o estilo de botão secundário existente.
6. Rodar a suite (`node --test "tests/*.test.mjs"`) para confirmar zero regressão no bloco
   RADAR_PURE (esta task não o toca, mas o innerHTML muda perto dele).
  </action>
  <verify>
    <automated>node --check radar-goiania.html && python -c "
s=open('radar-goiania.html',encoding='utf-8').read()
i=s.index('function showDetail(')
body=s[i:i+8000]
iPrim=body.index('dActsPrim');iMore=body.index('dActsMore')
prim=body[iPrim:iMore]
assert 'acts-save' in prim, '⭐ ausente de #dActsPrim'
assert 'Copiar inscri' not in prim, 'Copiar inscrição ainda em #dActsPrim (lei da tela violada)'
assert 'Copiar inscri' in body[iMore:iMore+2000], 'Copiar inscrição nao desceu p/ #dActsMore'
for fn in ['function oppLoad','function oppSave','function oppTem','function toggleOportunidade','function renderSaveBtn','function histLoad','function histSave']:
    assert fn in s, fn+' ausente'
assert 'radar_oportunidades' in s and 'radar_historico' in s
assert 'Não foi possível salvar — armazenamento do navegador indisponível ou cheio.' in s, 'toast de falha de escrita ausente'
assert s.count('__scores={op,conf}')>=2 or s.count('__scores = {op,conf}')>=2 or s.count('__scores')>=3, 'cache __scores ausente dos 2 call sites'
assert 'oportunidadeItem(' in s[s.index('function toggleOportunidade'):s.index('function toggleOportunidade')+2500], 'toggleOportunidade nao usa oportunidadeItem (allowlist)'
print('OK: persistencia + toggle + lei da tela confirmados')
" && node --test "tests/*.test.mjs" 2>&1 | tail -5</automated>
  </verify>
  <done>oppLoad/oppSave/oppTem/toggleOportunidade/renderSaveBtn/histLoad/histSave existem; #dActsPrim = Gerar documento + Ver comparáveis + ⭐ Salvar oportunidade; Copiar inscrição no topo de #dActsMore; falha de escrita = toast + estado inalterado; item salvo passa por oportunidadeItem (allowlist); suite de testes passa.</done>
</task>

<task type="auto">
  <name>Task 2: Blocos Minhas oportunidades + Histórico no painel Consulta + auto-add + reabrir via deep-link</name>
  <files>radar-goiania.html</files>
  <action>
1. Criar `renderSavedBlocks()` (idempotente): remove `#oportunidadesBlock`/`#historicoBlock` do DOM
   se existirem; se `#emptyState` NÃO está no DOM (busca com resultados), retorna sem inserir
   (regra de posição UI-SPEC — blocos só no estado vazio). Senão: monta o HTML de `&lt;interfaces&gt;` —
   bloco Oportunidades SEMPRE presente (com `.savedlist` de itens OU `.savedempty` com o texto
   exato "Nenhuma oportunidade salva ainda — toque ⭐ numa ficha.") inserido ANTES de `#emptyState`
   (`emptyState.insertAdjacentHTML('beforebegin',...)`); bloco Histórico SÓ quando
   `histLoad().length>0`, inserido DEPOIS de `#emptyState` (`'afterend'`). Contadores
   `#oportunidadesCount`/`#historicoCount` = length dos arrays. TODO texto de item passa por
   `esc()` (endereco/bairro vêm do cadastro — IN-01); `data-insc` com `esc(item.insc)`; onclick
   SEMPRE `abrirOportunidade(this)`/`removerOportunidade(this)` por referência de elemento (nunca
   string interpolada). Meta de Oportunidades: "Q {quadra} · L {lote} · {bairro}" + (faixa quando
   faixaLo/faixaHi não-null: "· faixa R$ X mil–Y mil", formato brlC-like) + (score quando não-null:
   "· oportunidade: {rotulo}") + "· salva em DD/MM" (de savedAt). Meta de Histórico: "consultado
   hoje às HH:MM" (mesmo dia) ou "consultado em DD/MM" — helper local de formatação de data.
2. Criar `abrirOportunidade(el)`: aceita elemento (lê `el.closest('[data-insc]').dataset.insc`) OU
   string (compat programática — mesmo padrão dual de `verNoMapa`). Reusar o caminho do deep-link
   LITERALMENTE (linhas 2769-2777): `document.getElementById("insc").value=digits; setMode("insc");
   buscar();` com `digits=insc.replace(/\D/g,"")`. Nenhuma lógica de busca nova — buscar() já cuida
   de SEARCHTOKEN/loading/erro (imóvel que sumiu do cadastro cai no tratamento de erro existente da
   busca, UI-SPEC Estados Transversais).
3. Criar `removerOportunidade(el)`: lê insc do `data-insc`, filtra fora de oppLoad(), `oppSave` —
   se true: toast "Oportunidade removida." + `renderSavedBlocks()` + `renderSaveBtn()` (se a ficha
   aberta é o mesmo imóvel, o ⭐ volta ao estado não-salvo). Criar `limparHistorico()`: `histSave([])`
   → se true, `renderSavedBlocks()`.
4. Auto-add de histórico: criar `histPush(a)` — monta `Object.assign(oportunidadeItem(a,extras),
   {visitedAt:new Date().toISOString()})` (extras = mesmo cálculo de mercadoEstimado + __scores da
   Task 1, extraído num helper compartilhado `oppExtras(a)` para não duplicar); DEDUPE: se o último
   item de histLoad() tem a mesma insc, substitui esse item (atualiza visitedAt) em vez de empilhar;
   senão `histAdd(lista,item,30)` (FIFO puro de 10-01). `histSave` — falha aqui TAMBÉM produz o
   toast (regra: escrita nunca silenciosa), mas NÃO bloqueia a abertura da ficha (histórico é
   acessório). Chamar `histPush(a)` dentro de `showDetail()` imediatamente após `DCUR=a` (linha
   ~2171). Após gravar, chamar `renderSavedBlocks()` apenas se `#emptyState` está no DOM (barato e
   idempotente — evita trabalho quando a lista de resultados está na tela).
5. CSS: acrescentar o bloco `.savedblock/.saveditem/...` de `&lt;interfaces&gt;` junto às regras de
   `.results`/`.empty` existentes; conferir o gutter real de `#results` (padding/margin dos cards)
   e alinhar a margem lateral do `.savedblock`. Alvos de toque ≥44px (`.saveditem` e `.saveditem-rm`
   com min-height/min-width 44px — exceção documentada: `.savedblock-clear` 32px, texto sublinhado
   de baixa frequência, aceito pelo UI-SPEC Component 3).
6. Chamar `renderSavedBlocks()` no init (junto de `initCoach()`/`initCaixa()`, linha ~2766) para o
   estado persistido aparecer ao abrir o app.
7. Sem animação nova: blocos entram sem motion coreografado (REDUCE-safe por construção; refino
   visual é Fase 13).
  </action>
  <verify>
    <automated>node --check radar-goiania.html && python -c "
s=open('radar-goiania.html',encoding='utf-8').read()
for fn in ['function renderSavedBlocks','function abrirOportunidade','function removerOportunidade','function limparHistorico','function histPush']:
    assert fn in s, fn+' ausente'
i=s.index('function abrirOportunidade')
body=s[i:i+900]
assert 'setMode(' in body and 'buscar()' in body, 'abrirOportunidade nao reusa o caminho do deep-link (setMode+buscar)'
i=s.index('function histPush')
assert 'histAdd(' in s[i:i+1200], 'histPush nao usa histAdd (FIFO 10-01)'
assert 'oportunidadeItem(' in s[i:i+1200], 'histPush nao usa oportunidadeItem (allowlist)'
i=s.index('function showDetail(')
assert 'histPush(' in s[i:i+8000], 'showDetail nao chama histPush (auto-add do historico)'
assert 'Nenhuma oportunidade salva ainda — toque ⭐ numa ficha.' in s, 'estado vazio de Oportunidades ausente'
assert 'abrirOportunidade(this)' in s and 'removerOportunidade(this)' in s, 'onclick por referencia de elemento (IN-01) ausente'
assert \"abrirOportunidade('\" not in s, 'IN-01 violado: insc interpolada em string JS de onclick'
assert 'renderSavedBlocks()' in s[s.index('initCoach')-200:s.index('initCoach')+600] or s.count('renderSavedBlocks()')>=3, 'renderSavedBlocks ausente do init'
print('OK: blocos + deep-link + auto-add + IN-01 confirmados')
" && node --test "tests/*.test.mjs" 2>&1 | tail -5</automated>
  </verify>
  <done>Blocos Minhas oportunidades (sempre presente no estado vazio, com contador e savedempty) e Histórico (só com itens, discreto, com Limpar) renderizados por renderSavedBlocks() idempotente; tocar item reabre ficha via setMode('insc')+buscar(); abrir ficha alimenta radar_historico (allowlist+FIFO 30+dedupe); remover/limpar funcionam com toast; init renderiza estado persistido; IN-01 respeitado (referência de elemento, esc() em todo texto de cadastro); suite passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Objeto cadastral bruto (ArcGIS) -> localStorage | dados de terceiro potencialmente presentes em `a` cruzam para persistência — SOMENTE via oportunidadeItem (allowlist testada em 10-01) |
| localStorage -> DOM (renderSavedBlocks innerHTML) | texto persistido (endereco/bairro de cadastro) reentra no DOM — esc() obrigatório |
| Item de lista -> onclick | insc trafega por data-attr + referência de elemento, nunca string JS interpolada |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Information Disclosure | toggleOportunidade/histPush gravando campo fora da allowlist em localStorage | mitigate | AMBOS constroem o item exclusivamente via oportunidadeItem() (função pura testada com assert negativo anti-dtnascimen em 10-01); verify automatizado confirma a chamada nos 2 caminhos |
| T-10-05 | Tampering (XSS stored) | renderSavedBlocks() reinjetando texto de cadastro persistido via innerHTML | mitigate | esc() em endereco/bairro/insc e em TODO campo textual do item ao montar o HTML; data-insc com esc(); verify confirma ausência de onclick com string interpolada |
| T-10-06 | Repudiation (falha silenciosa) | Escrita de localStorage falhando (quota/privado) sem o corretor saber — acha que salvou e perde o imóvel | mitigate | oppSave/histSave retornam boolean; false => toast visível e UI permanece no estado anterior (botão não vira "Salva"); copy exata travada no UI-SPEC |
| T-10-07 | Denial of Service (UX) | histórico crescendo sem limite e estourando quota | mitigate | histAdd com cap 30 FIFO (testado em 10-01) + dedupe de reaberturas consecutivas |

**Sem ameaças de alta severidade.** Persistência 100% local, allowlist testada, sem rede nova.
</threat_model>

<verification>
- `node --check radar-goiania.html` passa; `node --test "tests/*.test.mjs"` passa (zero regressão)
- Lei da tela: `#dActsPrim` = exatamente 1 primária + 2 secundárias (⭐ no lugar de Copiar inscrição); Copiar inscrição no topo de `#dActsMore`
- toggleOportunidade/histPush usam oportunidadeItem (allowlist) — nunca gravam `a` cru
- Falha de escrita => toast visível + estado de UI inalterado (nunca finge sucesso)
- abrirOportunidade reusa setMode('insc')+buscar() (caminho do deep-link, SEARCHTOKEN embutido)
- IN-01: onclick por referência de elemento + data-insc esc()-ado; nenhum texto de cadastro entra no DOM sem esc()
- Estado vazio de Oportunidades presente; Histórico ausente do DOM quando vazio
</verification>

<success_criteria>
- SALV-01 cumprido: salvar oportunidade + histórico automático persistem em localStorage com allowlist; reabrir o app mostra o mesmo; falha de escrita é visível
- ACAO-01 avança: painel Consulta vazio termina com ações reais (reabrir oportunidade/histórico) além dos exemplos
- Wave 3 (10-03) pode adicionar o grupo WhatsApp/Captação em #dActsMore sem retocar a estrutura desta plan
</success_criteria>

<output>
Após completar, criar `.planning/phases/10-acao-whatsapp-captacao-salvos/10-02-SUMMARY.md`.
Incluir no SUMMARY: assinaturas finais das funções de persistência (contrato p/ 10-03 e fases futuras), a posição exata dos blocos no DOM em relação a #emptyState, o shape do item persistido (com savedAt/visitedAt), e a confirmação da lei da tela (conteúdo final de #dActsPrim/#dActsMore com números de linha).
</output>
