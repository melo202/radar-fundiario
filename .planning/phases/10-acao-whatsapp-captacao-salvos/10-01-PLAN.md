---
phase: 10-acao-whatsapp-captacao-salvos
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - radar-goiania.html
  - tests/templates.test.mjs
  - tests/fixtures.mjs
autonomous: true
requirements: [ZAP-01, CAPT-01, SALV-01]
tags: [templates, whatsapp, captacao, allowlist, pure-functions, tdd]

must_haves:
  truths:
    - "zapResumo/zapProprietario/zapComprador/zapArgumento/zapRiscos e captAbordagem/captScript/captChecklist/captFollowup sao funcoes puras dentro do bloco RADAR_PURE (mesmo bloco testado por node:vm)"
    - "Toda mensagem termina com assinatura '— Nome, CRECI ####' SOMENTE quando perfil.nome existe; sem perfil, a assinatura e OMITIDA (nunca placeholder tipo '[seu nome]')"
    - "Sem faixa de valor (faixa=null), os templates de WhatsApp/Captacao que citam valor adaptam o texto (nunca inventam lo/hi)"
    - "oportunidadeItem(a,extras) retorna objeto allowlist-only; JSON.stringify do retorno NUNCA contem dtnascimen nem qualquer campo de nome de terceiro/titular"
    - "histAdd(lista, item, cap) e uma funcao pura FIFO: ao exceder cap, remove do INICIO (mais antigo), nunca do fim"
  artifacts:
    - path: "radar-goiania.html"
      provides: "9 funcoes de template (zap*/capt*) + oportunidadeItem + histAdd dentro do bloco RADAR_PURE_START..RADAR_PURE_END"
      contains: "function zapResumo"
    - path: "tests/templates.test.mjs"
      provides: "harness node:test que carrega RADAR_PURE via node:vm (mesmo padrao de tests/scores.test.mjs) e testa as 11 funcoes novas"
      contains: "loadPureBlock"
    - path: "tests/fixtures.mjs"
      provides: "fixtures novas para zap*/capt*/oportunidadeItem/histAdd adicionadas ao objeto FIXTURES existente"
      contains: "zapResumo"
  key_links:
    - from: "tests/templates.test.mjs"
      to: "radar-goiania.html (bloco RADAR_PURE)"
      via: "leitura de arquivo + node:vm.Script, mesmo loader de tests/scores.test.mjs"
      pattern: "RADAR_PURE_START"
    - from: "zapResumo/zapProprietario/zapComprador/zapArgumento/zapRiscos/capt*"
      to: "perfil.nome (assinatura condicional)"
      via: "if(perfil && perfil.nome) monta linha de assinatura; senao, string vazia"
      pattern: "perfil\\.nome"
---

<objective>
Criar as 9 funções de template determinístico (5 WhatsApp + 4 Captação) e 2 helpers puros (`oportunidadeItem`, `histAdd`) dentro do bloco `RADAR_PURE` de `radar-goiania.html`, com harness de teste TDD (`tests/templates.test.mjs`) cobrindo honestidade (sem faixa → texto adapta, nunca inventa valor), assinatura condicional (nunca placeholder) e allowlist negativa (nunca PII de terceiro).

Purpose: ZAP-01/CAPT-01 exigem textos prontos em pt-BR que soem como corretor profissional, nunca robô, nunca com dado inventado; SALV-01 exige que a persistência nunca grave PII de titular. Esta plan constrói a CAMADA PURA (sem DOM, sem localStorage, sem clipboard) — Wave 2 e Wave 3 consomem essas funções para popular a UI e persistir dados.
Output: bloco RADAR_PURE ampliado com 11 funções novas; `tests/templates.test.mjs` com fixtures cobrindo honestidade/assinatura/allowlist/FIFO; suite completa (`node --test "tests/*.test.mjs"`) passando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-acao-whatsapp-captacao-salvos/10-CONTEXT.md
@.planning/phases/10-acao-whatsapp-captacao-salvos/10-UI-SPEC.md

# Fonte de código (ler antes de editar)
@radar-goiania.html
@tests/scores.test.mjs
@tests/fixtures.mjs

<facts>
<!-- Verificado por leitura direta deste repo. -->

Bloco RADAR_PURE hoje (linhas 798-1074 de radar-goiania.html): abre em
`// RADAR_PURE_START — funções puras de matching/normalização. Testadas por tests/busca.test.mjs`
e fecha em `// RADAR_PURE_END`. Contém `esc`, `clean`, `norm`, `isGarage`, e (desde Fase 9)
`scoreOportunidade`, `scoreConfianca`, `leituraPratica`. Esta plan ACRESCENTA funções ao MESMO
bloco (não cria um segundo bloco) — o harness de teste corta por índice de string entre os
marcadores, então tudo que precisa ser testável por node:vm PRECISA estar dentro deles.

`esc(v)` (linha 787): `clean(v).replace(/&/g,"&amp;")...` — escapa HTML. As funções de template
desta plan retornam STRING PURA (não HTML) — são strings de texto pronto para clipboard (WhatsApp),
não innerHTML. `esc()` NÃO se aplica aqui (não há DOM); é a Wave 3 que decide se escapa algo ao
inserir em innerHTML de preview. Esta plan só formata texto pt-BR puro.

`radar_prof` (localStorage, lido em `abrirLaudo()` linha 2349): `JSON.parse(localStorage.getItem(
"radar_prof")||"{}")` → `{nome, creci, contato}`. Convenção de assinatura (CONTEXT.md, decisão
travada): "— Nome, CRECI ####" quando `perfil.nome` existe; SEM perfil ou sem nome → SEM linha de
assinatura (nunca "[seu nome]"). `creci` é opcional mesmo com nome presente (se ausente, assinatura
é só "— Nome").

Padrão de `data` de entrada (definido nesta plan, consumido por Wave 3): objeto plano com os campos
já calculados pela ficha (não recalcula nada, só formata):
```js
{
  endereco: string,             // ex.: "Rua Portugal, 582"
  bairro: string,                // ex.: "Setor Bueno" (já limpo via clean(), sem esc())
  quadra: string, lote: string,  // ex.: "45", "12" (ou "—")
  tipoImovel: string,            // ex.: "Apartamento" / "Imóvel" (mesmo unitLabel/USO já usado em showDetail)
  faixa: {lo:number, hi:number} | null,  // mesmo shape de mercadoEstimado() — null = sem base, NUNCA inventar
  scoreOp: {score:number, rotulo:string, porque:string[]} | null,   // retorno de scoreOportunidade()
  scoreConf: {nivel:string, porque:string[]} | null,                 // retorno de scoreConfianca()
  leitura: string,               // retorno de leituraPratica() (já pronto)
  perfil: {nome:string, creci:string, contato:string} | null         // lido de radar_prof; null se ausente/sem nome
}
```

`oportunidadeItem(a, extras)` — helper puro que recebe o OBJETO CADASTRAL BRUTO (`a`, mesmo shape
usado em `showDetail(a,ll)`, com campos como `nrinscr`, `ci`, `nmbairro`, `nrquadra`, `nrlote`,
`areaterr`, `areaedif`, `vlvenal`, potencialmente `dtnascimen`/outros campos de terceiro que a
prefeitura devolve) + `extras` (objeto com `faixaLo`, `faixaHi`, `scoreOportunidade`, `scoreConfianca`
— já calculados pela ficha, formato numérico/string simples) e retorna SÓ o objeto allowlist (nunca
o objeto `a` inteiro, nunca qualquer campo fora da lista travada no CONTEXT.md):
`{insc, endereco, bairro, quadra, lote, areaTerr, areaEdif, vlvenal, faixaLo, faixaHi,
scoreOportunidade, scoreConfianca, savedAt}` (Oportunidades) ou o mesmo sem `savedAt` + com
`visitedAt` (Histórico) — a escolha de qual timestamp incluir é responsabilidade da Wave 2 (esta
função só monta o núcleo allowlist comum; Wave 2 adiciona `savedAt`/`visitedAt` na hora de gravar).

`histAdd(lista, item, cap)` — helper puro FIFO: recebe o array atual, o novo item, e o cap (30);
retorna um NOVO array (`[...lista, item]` truncado do INÍCIO se `length>cap`) — nunca muta o array
recebido (facilita teste e reuso em `radar_oportunidades`/`radar_historico` se necessário, embora
Oportunidades não tenha cap nesta fase per UI-SPEC).
</facts>

<interfaces>
<!-- Assinaturas exatas que a Wave 2/3 vão CHAMAR — contrato duro, não renomear. -->

```js
// Templates WhatsApp (ZAP-01) — cada um recebe o MESMO objeto `data` documentado em <facts>.
function zapResumo(data): string
function zapProprietario(data): string
function zapComprador(data): string
function zapArgumento(data): string
function zapRiscos(data): string

// Templates Captação (CAPT-01) — mesmo objeto `data`; captScript/captChecklist NÃO dependem de
// faixa/scores (são genéricos/documentais), mas ainda recebem `data` completo por uniformidade de
// assinatura (facilita a Wave 3 chamar as 4 com o mesmo argumento).
function captAbordagem(data): string
function captScript(data): string
function captChecklist(data): string
function captFollowup(data): string

// Helpers de persistência pura (SALV-01) — SEM localStorage aqui, só transformação de dados.
function oportunidadeItem(a, extras): object   // a=objeto cadastral bruto; extras={faixaLo,faixaHi,scoreOportunidade,scoreConfianca}
function histAdd(lista, item, cap): array       // FIFO puro, nao muta lista
```

Esqueletos de texto travados no UI-SPEC (`10-UI-SPEC.md`, seção "Copywriting Contract") — usar
EXATAMENTE a estrutura/tom ali descrita; a diferença é preencher com os dados de `data` em vez de
placeholder. Pontos de honestidade OBRIGATÓRIOS (testados):
- `data.faixa===null` → NUNCA escrever "entre R$ X e R$ Y"; usar variante honesta (ex.: "não tenho
  uma faixa estimada ainda para este imóvel" / "recomendo uma avaliação para estimar o valor").
- `data.perfil===null || !data.perfil.nome` → a string retornada NÃO contém "\n—" de assinatura
  (nenhuma linha final de assinatura); quando há perfil.nome, a última linha é
  `\n\n— ${nome}${creci?", CRECI "+creci:""}`.
- `zapRiscos`/`captChecklist` NUNCA prometem validade jurídica ("recomendo confirmar", nunca "está
  garantido"/"documentação ok" como afirmação).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — criar tests/templates.test.mjs + fixtures (testes falhando)</name>
  <files>tests/templates.test.mjs, tests/fixtures.mjs</files>
  <behavior>
    - zapResumo/zapProprietario/zapComprador/zapArgumento/zapRiscos: com perfil {nome:"Ana Souza",creci:"12345"} a string termina com "— Ana Souza, CRECI 12345"; com perfil null a string NAO contem "[seu nome]" nem qualquer marcador de placeholder (regex de colchetes com "nome" nao casa)
    - zapResumo/zapProprietario/zapComprador/zapArgumento com faixa null NAO contem faixa inventada (nao usa data.faixa.lo/hi quando null); string ainda e coerente (nao vazia, nao quebrada)
    - zapRiscos: string contem ao menos 1 dos termos de honestidade ("recomendo confirmar" ou "faixa estimada" ou "não é uma avaliação oficial") e NAO contem afirmacao absoluta ("garantido"/"certeza")
    - captAbordagem/captScript/captChecklist/captFollowup: retornam string nao vazia; captScript contem os 4 passos numerados ("1." a "4."); captChecklist contem pelo menos os 5 itens do UI-SPEC (matricula, IPTU, certidoes pessoais, condominio, documento de identidade) cada um comecando com "•"; captFollowup contem o endereco interpolado de data.endereco
    - oportunidadeItem(a com dtnascimen e nome de terceiro, extras): JSON.stringify(resultado) NAO contem "dtnascimen" nem o valor literal do campo de titular de a (assert negativo explicito); resultado CONTEM insc/endereco/bairro/quadra/lote/areaTerr/areaEdif/vlvenal/faixaLo/faixaHi/scoreOportunidade/scoreConfianca (allowlist positiva)
    - histAdd(lista de 29 itens, novoItem, 30) resulta em length 30, ultimo=novoItem, primeiro=item original de indice 0 (29+1=30 NAO excede o cap — nenhuma remocao ocorre; fix plan-check do off-by-one); histAdd(lista de 30 itens, novoItem, 30) resulta em length 30 (nao 31), o item mais antigo (indice 0 original) NAO esta mais presente (este e o caso real de evicao FIFO); histAdd(lista, item, cap) NAO muta o array `lista` original (identidade de referencia preservada, novo array retornado)
  </behavior>
  <action>
Criar `tests/templates.test.mjs` reusando EXATAMENTE o padrão de loader de `tests/scores.test.mjs`
(`loadPureBlock()` via `node:vm`, cortando por `RADAR_PURE_START`/`RADAR_PURE_END`, com os MESMOS
guards de `assert.ok(src.includes(...))` para cada função nova antes de montar o sandbox — copiar o
padrão, adaptando a lista de funções exportadas em `globalThis.__exports` para incluir as 11 novas:
`zapResumo,zapProprietario,zapComprador,zapArgumento,zapRiscos,captAbordagem,captScript,
captChecklist,captFollowup,oportunidadeItem,histAdd`.

Adicionar ao objeto `FIXTURES` existente em `tests/fixtures.mjs` (não substituir, ACRESCENTAR chaves
novas ao objeto exportado) as fixtures: `zapComData` (objeto `data` completo com perfil+faixa+scores,
esperado COM assinatura), `zapSemPerfil` (mesmo data, `perfil:null`, esperado SEM assinatura),
`zapSemFaixa` (mesmo data, `faixa:null`, esperado sem valor inventado), `oportunidadeItemInput` (`a`
com campos extras incluindo `dtnascimen:"19800101"` e `nmtitular:"Fulano de Tal"` simulando o dado
bruto do ArcGIS — usados no assert negativo), `histAddCases` (arrays de 29/30 itens + cap 30).

Escrever os testes conforme `<behavior>` — DEVEM FALHAR nesta task (RED), pois as funções ainda não
existem em `radar-goiania.html`. Rodar `node --test "tests/templates.test.mjs"` e confirmar falha
(funções ausentes do bloco RADAR_PURE) — este é o estado esperado ao final da Task 1.
  </action>
  <verify>
    <automated>node --test "tests/templates.test.mjs" 2>&1 | grep -qi "fail" && echo "OK: RED confirmado (testes falham, funcoes ainda nao existem)"</automated>
  </verify>
  <done>tests/templates.test.mjs e fixtures novas em tests/fixtures.mjs existem; rodar a suite falha porque zapResumo/etc. ainda não existem no bloco RADAR_PURE — estado RED documentado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implementar as 11 funções dentro do bloco RADAR_PURE</name>
  <files>radar-goiania.html</files>
  <action>
Adicionar as 11 funções de `&lt;interfaces&gt;` DENTRO do bloco `RADAR_PURE_START`...`RADAR_PURE_END`
(linhas 798-1074), próximas de `leituraPratica`/`scoreConfianca` (mesma seção temática de "geração
de texto por template determinístico"). Cada função de template segue o esqueleto EXATO da seção
"Copywriting Contract" do UI-SPEC (`10-UI-SPEC.md`), preenchido com os campos de `data`:

- Helper interno compartilhado (não exportado externamente, só usado pelas 9 funções de template):
  `function assinatura(perfil){ if(!perfil||!perfil.nome) return ""; return "\n\n— "+perfil.nome+
  (perfil.creci?", CRECI "+perfil.creci:""); }` — chamado ao final de cada template, concatenado
  direto na string de retorno (nunca um placeholder, string vazia quando não há perfil).
- Helper interno `function faixaTxt(faixa){ return faixa ? (brlSimples(faixa.lo)+" e "+brlSimples(faixa.hi))
  : null; }` — criar `brlSimples(n)` como uma formatação de moeda MÍNIMA e AUTÔNOMA dentro do bloco
  RADAR_PURE (ex.: `n>=1000?"R$ "+Math.round(n/1000).toLocaleString("pt-BR")+" mil":"R$ "+Math.round(n)`)
  — NÃO importar/mover `brl`/`brlC` do resto do arquivo (evita acoplar o bloco puro testável a
  funções fora dele); esta é uma formatação auxiliar só para os templates de WhatsApp/Captação, não
  precisa ser byte-idêntica a `brlC` do resto do app.
- `zapResumo(data)`: "[tipoImovel] no [bairro]. [1-2 frases da leitura, reusar data.leitura
  diretamente]. Faixa estimada: [lo]–[hi]." + variante honesta quando faixa=null ("Ainda não tenho
  uma faixa estimada para este imóvel — os dados técnicos completos estão na ficha.") + assinatura(data.perfil).
- `zapProprietario(data)`: esqueleto do UI-SPEC ("Olá! Encontrei seu imóvel no [bairro]...") — quando
  faixa=null, adaptar para "ainda não consegui estimar uma faixa de valor com os dados disponíveis,
  mas gostaria de conversar sobre o seu imóvel" (nunca trava o convite por falta de dado).
  Pendências de scoreConf (`data.scoreConf.porque`, se nivel!=="alta") citadas na frase "recomendo
  confirmar [pendências]".
  + assinatura(data.perfil).
- `zapComprador(data)`: esqueleto do UI-SPEC (exemplo canônico §13) — reusar a estrutura literal
  ("Encontrei esse imóvel no [bairro]. Pela análise cadastral..."); sem faixa, adapta para "recomendo
  uma avaliação para estimar melhor o valor deste imóvel" + assinatura(data.perfil).
- `zapArgumento(data)`: usa `data.scoreOp` (se presente) para "[X%] acima/abaixo da mediana dos
  comparáveis" (reusa `data.scoreOp.porque[0]` se já formatado, OU monta a partir de
  `data.scoreOp.score`/`rotulo`); sem scoreOp, retorna variante honesta ("Ainda não tenho comparáveis
  suficientes na região para um argumento de preço baseado em dados — recomendo aguardar a análise de
  vizinhança.") + assinatura(data.perfil).
- `zapRiscos(data)`: lista de pontos a confirmar a partir de `data.scoreConf.porque` (se presente) OU
  lista genérica default (área privativa, estado de conservação, documentação, número de comparáveis)
  quando `scoreConf` ausente; sempre termina com a frase de responsabilidade do UI-SPEC ("Esta é uma
  faixa estimada, não uma avaliação oficial...") + assinatura(data.perfil).
- `captAbordagem(data)`: esqueleto UI-SPEC Bloco 1, interpolando bairro+endereço/quadra-lote +
  assinatura(data.perfil).
- `captScript(data)`: esqueleto UI-SPEC Bloco 2 (4 passos numerados com `\n`), interpolando bairro —
  SEM assinatura (é script de ligação, não mensagem enviada).
- `captChecklist(data)`: texto ESTÁTICO do UI-SPEC Bloco 3 (5 itens com bullet "•", `\n` entre eles) —
  não depende de `data` além de retornar sempre o mesmo texto (genérico Goiânia) — SEM assinatura.
- `captFollowup(data)`: esqueleto UI-SPEC Bloco 4, interpolando `data.endereco` — SEM assinatura (é
  tarefa interna, não mensagem enviada a terceiro).
- `oportunidadeItem(a, extras)`: monta o endereço localmente dentro do bloco RADAR_PURE reusando a
  MESMA regra já existente de `logradouroDe`/`addrDe` (que vivem FORA do bloco, mas são puras e sem
  dependência de DOM) — como opção mais simples e sem mover código existente, DUPLICAR uma versão
  mínima local `function enderecoSimples(a){ const tp=clean(a.tplogradou),nm=clean(a.nmlogradou);
  const log=(!tp||norm(nm).startsWith(norm(tp)+" ")||norm(nm)===norm(tp))?nm:(tp+" "+nm);
  return log.replace(/\s+/g," ").trim()+(clean(a.nrimovel)&&clean(a.nrimovel)!=="S/N"?", "+
  clean(a.nrimovel):""); }` dentro do bloco (mesma lógica de `addrDe`/`logradouroDe`, só renomeada
  para não colidir e para deixar claro que é a cópia usada pelo bloco puro) — retorna
  `{insc: clean(a.nrinscr||a.ci), endereco: enderecoSimples(a), bairro: clean(a.nmbairro), quadra:
  clean(a.nrquadra), lote: clean(a.nrlote), areaTerr: a.areaterr||null, areaEdif: a.areaedif||null,
  vlvenal: a.vlvenal||null, faixaLo: (extras&&extras.faixaLo!=null)?extras.faixaLo:null, faixaHi:
  (extras&&extras.faixaHi!=null)?extras.faixaHi:null, scoreOportunidade: (extras&&
  extras.scoreOportunidade!=null)?extras.scoreOportunidade:null, scoreConfianca:
  (extras&&extras.scoreConfianca)||null}` — NENHUM outro campo de `a` é copiado (allowlist estrita;
  `dtnascimen`/`nmtitular`/qualquer campo de pessoa física NUNCA entra, mesmo que existam em `a`).
- `histAdd(lista, item, cap)`: `const next=[...(lista||[]), item]; return next.length>cap?
  next.slice(next.length-cap):next;` — puro, não muta `lista`.

Rodar `node --test "tests/templates.test.mjs"` até todos os testes passarem (GREEN). Rodar também
`node --test "tests/*.test.mjs"` (suite completa) para confirmar zero regressão nos testes das Fases
8/9 (scores/detectmode/busca).
  </action>
  <verify>
    <automated>node --check radar-goiania.html && node --test "tests/*.test.mjs" 2>&1 | tail -25</automated>
  </verify>
  <done>As 11 funções existem dentro do bloco RADAR_PURE; node --test "tests/*.test.mjs" passa 100% (templates novos GREEN + zero regressão em scores/detectmode/busca); JSON.stringify(oportunidadeItem(...)) nunca contém dtnascimen/nome de terceiro; assinatura ausente sem perfil, presente com perfil; nenhum template inventa faixa quando data.faixa=null.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| oportunidadeItem(a, extras) -> objeto persistido (Wave 2 grava em localStorage) | dado cadastral bruto (ArcGIS, pode conter campos de terceiro) cruza para um objeto ALLOWLIST antes de qualquer persistência |
| zap*/capt*(data) -> string copiada para clipboard (Wave 3) | dados do imóvel/perfil formatados em texto livre que sai do app via WhatsApp/ligação — nenhuma validação de destino, mas nenhuma PII de terceiro é incluída (só dados do próprio imóvel/corretor) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | oportunidadeItem(a, extras) copiando campo de terceiro (dtnascimen/nome do titular) para o objeto persistido | mitigate | allowlist POSITIVA e travada (só os 12 campos documentados); teste automatizado (Task 1/2) faz assert NEGATIVO explícito contra dtnascimen/nome de titular em cada execução da suite — regressão futura quebra o build |
| T-10-02 | Tampering | Templates zap*/capt* inventando valor (faixa/score) quando o dado de origem é null | mitigate | contrato de honestidade testado (fixtures `zapSemFaixa`): toda função com `data.faixa===null` cai na variante textual alternativa, nunca interpola `undefined`/número fabricado |
| T-10-03 | Spoofing (perfil) | Assinatura de WhatsApp incluindo placeholder genérico que pareça um nome real quando perfil ausente | mitigate | contrato testado: ausência de `perfil.nome` produz string SEM qualquer linha de assinatura (não um marcador "[seu nome]" que poderia ser copiado por engano) |

**Sem ameaças de alta severidade.** Esta plan não toca DOM, localStorage ou rede — é camada pura de formatação de texto e transformação de objeto, testável 100% por fixtures.
</threat_model>

<verification>
- `node --check radar-goiania.html` passa
- `node --test "tests/*.test.mjs"` passa 100% (templates novos + scores/detectmode/busca sem regressão)
- Assert negativo confirmado: `JSON.stringify(oportunidadeItem(...))` nunca contém `dtnascimen` nem valor de campo de titular/terceiro
- Assert de assinatura: com `perfil.nome` presente, toda função zap*/captAbordagem termina com "— Nome[, CRECI ####]"; sem perfil, nenhuma linha de assinatura aparece (nenhum "[seu nome]")
- Assert de honestidade: com `data.faixa===null`, nenhuma função interpola valor de faixa inventado
- `histAdd` é FIFO puro, não muta o array de entrada, cap respeitado (30)
</verification>

<success_criteria>
- ZAP-01 (camada de template) cumprido: 5 funções de WhatsApp determinísticas, tom de corretor, honestas
- CAPT-01 (camada de template) cumprido: 4 funções de Captação (abordagem/script/checklist/follow-up)
- SALV-01 (camada de allowlist) cumprido: oportunidadeItem nunca deixa PII de terceiro passar; histAdd é o FIFO puro que a Wave 2 vai usar para radar_historico
- Wave 2 (10-02) e Wave 3 (10-03) podem chamar as 11 funções sem reabrir esta plan — contrato de assinatura travado
</success_criteria>

<output>
Após completar, criar `.planning/phases/10-acao-whatsapp-captacao-salvos/10-01-SUMMARY.md`.
Incluir no SUMMARY: lista das 11 funções com assinatura exata, resultado da suite completa de testes (contagem pass/fail), e o shape final do objeto `data` consumido pelos templates (para a Wave 3 montar sem ambiguidade a partir dos dados da ficha).
</output>
