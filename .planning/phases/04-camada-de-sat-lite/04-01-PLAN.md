---
phase: 04-camada-de-sat-lite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [radar-goiania.html]
autonomous: true
requirements: [SAT-01]
must_haves:
  truths:
    - "O CSP img-src permite https://server.arcgisonline.com (sem ele o satélite não renderiza nada, falha silenciosa)"
    - "Existe uma camada base Esri World Imagery keyless (server.arcgisonline.com/.../World_Imagery) — criada mas NÃO adicionada por default"
    - "Existe uma camada overlay Esri Reference (World_Boundaries_and_Places) em pane próprio zIndex 350 — só visível no modo satélite"
    - "Um botão custom no mapa (canto inferior-direito, L.control bottomright) alterna ruas ⇄ satélite via aria-pressed, ≥44px, cores das :root vars"
    - "O toggle é deliberado — nunca dispara automaticamente por zoom"
    - "Ruas (CARTO light) permanece o default ao abrir o app"
    - "Rótulos do overlay de referência ficam legíveis sobre o satélite — entregue NO MESMO commit do toggle"
    - "Outlines de bairro (tan) e lote (verde) permanecem legíveis sobre a imagem escura (opacity/weight ajustados via função de estilo, sem novo hex)"
    - "A atribuição Esri aparece quando o satélite está ativo e some quando volta pra ruas (via option attribution de cada L.tileLayer, sem string-replace manual)"
  artifacts:
    - path: "radar-goiania.html"
      provides: "CSP img-src estendido, camadas Esri, botão toggle L.control, baiStyle()/lotStyle() branch, persistência radar_sat"
      contains: "server.arcgisonline.com"
  key_links:
    - from: "radar-goiania.html CSP meta (linha 7)"
      to: "tiles Esri"
      via: "img-src allowlist"
      pattern: "img-src[^\"]*server\\.arcgisonline\\.com"
    - from: "botão toggle"
      to: "camadas satélite + reference"
      via: "handler que adiciona/remove + flip aria-pressed + satelliteOn boolean"
      pattern: "aria-pressed"
    - from: "baiStyle()/lotStyle()"
      to: "satelliteOn boolean"
      via: "branch de estilo por modo"
      pattern: "satelliteOn"
---

<objective>
Entregar o núcleo do SAT-01: alternar deliberadamente ruas ⇄ satélite (Esri World Imagery keyless) com rótulos legíveis (overlay de referência Esri), outlines de bairro/lote legíveis sobre a imagem, atribuição Esri, e o pré-requisito bloqueante do CSP — tudo no mesmo commit.

Purpose: O corretor precisa ver o território real (imagem de satélite) sem perder rótulos nem os desenhos de bairro/lote, através de um toggle on-brand que jamais dispara sozinho.
Output: radar-goiania.html com CSP estendido, duas camadas Esri, botão toggle custom, função de estilo por modo, e persistência opcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-camada-de-sat-lite/04-CONTEXT.md
@.planning/phases/04-camada-de-sat-lite/04-UI-SPEC.md

<interfaces>
<!-- Âncoras e contratos existentes extraídos de radar-goiania.html. Use direto, sem explorar o código. -->

CSP meta (linha 7) — img-src ATUAL a estender:
```
img-src 'self' data: blob: https://*.basemaps.cartocdn.com;
```

initMap() (~linha 666-681) — onde a CARTO light é adicionada e os panes/zoomend existem:
```js
function initMap(){
  map=L.map("map",{zoomControl:true}).setView([-16.6799,-49.255],12);
  L.tileLayer("https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png",{
    maxZoom:19, attribution:'© OpenStreetMap · © CARTO'
  }).addTo(map);
  layerGroup=L.layerGroup().addTo(map);
  map.on("click",onMapClick);
  initLots();
  map.createPane("bairros").style.zIndex=370;
  map.on("zoomend",()=>{ /* NÃO tocar — este é o gate de zoom dos lotes, não do satélite */ });
}
```

Estilos de bairro/lote (~linhas 686-701) — o que ajustar por modo:
```js
const LOT_STYLE={pane:"lots",color:"#2c5545",weight:1,fillColor:"#2c5545",fillOpacity:.05,bubblingMouseEvents:false};
const LOT_HOVER={weight:2.5,color:"#b5451f",fillOpacity:.14};
const BAI_STYLE={pane:"bairros",color:_BAI_LINE,weight:1,fillColor:_BAI_LINE,fillOpacity:.03,opacity:.8,bubblingMouseEvents:false};
const BAI_HOVER={color:_BAI_ACCENT,weight:2.5,fillOpacity:.08,opacity:1};
function baiStyle(feature){return BAI_STYLE;}  // já é função (não objeto cru) — estender aqui
```

Padrão de toggle aria-pressed comprovado (toggleCaixa ~1885, setMode ~976, setView ~1001):
```js
b.classList.toggle("on",estado); b.setAttribute("aria-pressed",estado);
```

Padrão de persistência localStorage (radar_prof ~1548/1656, radar_coach ~1935):
```js
try{localStorage.setItem("radar_coach","1");}catch(e){}
let seen="1"; try{seen=localStorage.getItem("radar_coach");}catch(e){}
```

Focus-visible app-wide (linha 88) — reusar verbatim, NÃO redeclarar:
```css
button:focus-visible,...{outline:2px solid var(--accent);outline-offset:2px}
```

Ladder de z-index (do UI-SPEC): tiles/panes (200/370/380) < overlayPane (400) < pill/breadcrumb (400) < toggle (490 NOVO) < .detail sheet (500).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender o CSP img-src para server.arcgisonline.com (PRÉ-REQUISITO BLOQUEANTE)</name>
  <read_first>radar-goiania.html (linha 7, o meta CSP); .planning/phases/04-camada-de-sat-lite/04-UI-SPEC.md (seção "CSP dependency (blocking, not cosmetic)")</read_first>
  <files>radar-goiania.html</files>
  <action>
Na linha 7, dentro do atributo content do `<meta http-equiv="Content-Security-Policy">`, localizar a diretiva:
`img-src 'self' data: blob: https://*.basemaps.cartocdn.com;`
e estendê-la para incluir o host Esri:
`img-src 'self' data: blob: https://*.basemaps.cartocdn.com https://server.arcgisonline.com;`
NÃO usar wildcard (`https://*.arcgisonline.com` é PROIBIDO — escopar ao host exato). NÃO tocar nenhuma outra diretiva do CSP (script-src, style-src, connect-src, object-src, base-uri, form-action ficam idênticas — manter o CSP tight). Atualizar o comentário HTML acima (linha 6) para mencionar que os tiles Esri (satélite) também entram no img-src.
Motivo: sem isto, os tiles Esri são silenciosamente bloqueados pelo CSP e o satélite renderiza NADA — a fase falha de forma invisível. É bloqueante, precisa vir antes das camadas.
  </action>
  <verify>
    <automated>grep -q "img-src[^\"]*server\.arcgisonline\.com" "radar-goiania.html" && echo OK</automated>
  </verify>
  <acceptance_criteria>
- `grep "img-src.*server.arcgisonline.com" radar-goiania.html` retorna a linha 7.
- `grep -c "arcgisonline" radar-goiania.html` NÃO contém wildcard `*.arcgisonline` (apenas `server.arcgisonline.com`).
- As demais diretivas do CSP (script-src, connect-src, object-src 'none', base-uri 'none', form-action 'none') permanecem inalteradas.
  </acceptance_criteria>
  <done>O CSP img-src permite explicitamente https://server.arcgisonline.com (host exato, sem wildcard); resto do CSP intacto.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar camadas Esri (satélite + reference) e a função de estilo por modo — sem adicionar por default</name>
  <read_first>radar-goiania.html (initMap ~666, BAI_STYLE/LOT_STYLE ~686-701, createPane "bairros"/"lots"); .planning/phases/04-camada-de-sat-lite/04-UI-SPEC.md (Layer Stacking Contract, seção Color tabela de outlines)</read_first>
  <files>radar-goiania.html</files>
  <action>
Dentro de `initMap()`, DEPOIS da CARTO light e da criação dos panes, adicionar (sem `.addTo(map)` — só criar as instâncias e guardar em vars module-level):

1. Camada base satélite (keyless, XYZ Web Mercator, plain L.tileLayer, SEM reprojeção):
```js
satTile = L.tileLayer("https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}",{
  maxZoom:19, attribution:'Tiles © Esri — Source: Esri, Maxar, Earthstar Geographics, and the GIS User Community'
});
```
(a string de atribuição é VERBATIM do CONTEXT.md/UI-SPEC — NÃO parafrasear.)

2. Pane do overlay de referência com zIndex 350 (entre tilePane ~200 e "bairros" 370):
```js
map.createPane("satref").style.zIndex=350;
satRef = L.tileLayer("https://server.arcgisonline.com/ArcGIS/rest/services/Reference/World_Boundaries_and_Places/MapServer/tile/{z}/{y}/{x}",{
  maxZoom:19, pane:"satref"
});
```
(overlay transparente com rótulos+halo baked-in — NÃO precisa attribution própria; a do satélite basta.)

3. Guardar a CARTO light numa var (ex.: `streetTile`) em vez de instanciá-la anonimamente, para poder removê-la/re-adicioná-la no toggle. Ela continua sendo a única `.addTo(map)` no boot (ruas = default).

4. Declarar `let satelliteOn=false;` module-level.

Estender a função de estilo por modo (branch, NÃO novos hex — só opacity/weight conforme a tabela do UI-SPEC seção Color):
```js
function baiStyle(feature){
  return satelliteOn
    ? {...BAI_STYLE, opacity:1, weight:1.5, fillOpacity:0}   // sobre satélite: sem wash tan, traço mais forte
    : BAI_STYLE;
}
function lotStyle(){
  return satelliteOn
    ? {...LOT_STYLE, weight:1.5, fillOpacity:0}               // sobre satélite: sem wash verde, traço mais forte
    : LOT_STYLE;
}
```
Onde `LOT_STYLE` era usado como objeto cru na criação da camada de lotes (buscar `LOT_STYLE` nas chamadas em refreshLots/lotLayer), trocar por `lotStyle()`. BAI_HOVER e LOT_HOVER ficam INALTERADOS (o accent já lê full-opacity sobre qualquer base). NÃO introduzir nenhum hex novo.

NÃO adicionar handler de zoom que troque de modo — o toggle é 100% deliberado.
  </action>
  <verify>
    <automated>grep -q "World_Imagery/MapServer/tile/{z}/{y}/{x}" "radar-goiania.html" && grep -q "World_Boundaries_and_Places" "radar-goiania.html" && grep -q "let satelliteOn" "radar-goiania.html" && grep -q "function lotStyle" "radar-goiania.html" && echo OK</automated>
  </verify>
  <acceptance_criteria>
- `grep "World_Imagery/MapServer/tile/{z}/{y}/{x}"` retorna a URL exata do satélite.
- `grep "World_Boundaries_and_Places"` retorna a URL do reference overlay.
- `grep "satref"` mostra createPane com zIndex 350.
- `grep "Tiles © Esri — Source: Esri, Maxar, Earthstar Geographics, and the GIS User Community"` retorna a atribuição verbatim.
- `grep "satelliteOn"` aparece na declaração e nos branches de baiStyle/lotStyle.
- Nenhuma das duas camadas Esri tem `.addTo(map)` no boot (streets é default).
- Nenhum hex novo (grep de `#` novos além dos já existentes).
  </acceptance_criteria>
  <done>Camadas satélite+reference criadas (keyless, panes corretos), atribuição verbatim, streets segue default, baiStyle()/lotStyle() têm branch por satelliteOn sem novo hex.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Botão toggle custom (L.control bottomright) + wiring de camadas + persistência radar_sat</name>
  <read_first>radar-goiania.html (toggleCaixa ~1885, setMode ~976, setView ~1001, focus-visible linha 88, radar_coach ~1935, .caixabtn ~233); .planning/phases/04-camada-de-sat-lite/04-UI-SPEC.md (Component Contract: Satellite Toggle Button, Copywriting Contract, Persistence)</read_first>
  <files>radar-goiania.html</files>
  <action>
Criar um controle Leaflet custom no slot `bottomright` (o único canto livre em mobile e desktop — não colide com zoomControl top-left, .searchpill/#breadcrumb top-left, nem .detail bottom que fica por cima quando aberta):

1. Um `<button>` (NÃO `<a>`/`<div>`) dentro de um `L.Control` custom (position:"bottomright"), com:
   - `aria-pressed="false"` inicial, `aria-label="Ver satélite"`, `title="Alternar satélite"`.
   - Glyph inicial 🛰️ (previews o destino: "toque p/ satélite"). No modo satélite vira 🗺️ + `aria-label="Ver ruas"`.
   - CSS inline ou classe nova (ex.: `.sattoggle`): exatamente 44×44px, `background:var(--paper-2); border:1.5px solid var(--ink); color:var(--ink); border-radius:4px; box-shadow:0 4px 14px rgba(20,26,31,.18)`. Estado pressed (`aria-pressed="true"`): `border:1.5px solid var(--accent); color:var(--accent)` (fundo NÃO inverte para accent sólido — accent só na borda+glyph). Hover: `border-color:var(--accent-ink)`. Focus: reusar a regra global de focus-visible da linha 88 (NÃO redeclarar). z-index do controle: 490 (abaixo de .detail 500, acima dos panes).

2. Handler `toggleSatelite()` (espelhar mecânica de toggleCaixa):
```js
function setSatelite(on){
  satelliteOn=on;
  const b=document.getElementById("btnSat");
  b.classList.toggle("on",on);
  b.setAttribute("aria-pressed",on);
  b.setAttribute("aria-label", on?"Ver ruas":"Ver satélite");
  b.textContent = on?"🗺️":"🛰️";
  if(on){ satTile.addTo(map); satRef.addTo(map); map.removeLayer(streetTile); }
  else { streetTile.addTo(map); if(map.hasLayer(satTile))map.removeLayer(satTile); if(map.hasLayer(satRef))map.removeLayer(satRef); }
  // re-aplicar estilo por modo às camadas vetoriais já montadas:
  if(bairroLayer) bairroLayer.setStyle(baiStyle);
  if(lotLayer && lotLayer.setStyle) lotLayer.setStyle(lotStyle());
  try{localStorage.setItem("radar_sat", on?"1":"0");}catch(e){}
}
function toggleSatelite(){ setSatelite(!satelliteOn); }
```
(a remoção/adição das camadas aqui é o swap seco; o CROSSFADE suave é o Plan 02 — NÃO implementar transição de opacidade aqui.)

3. Persistência: em `initMap()` no boot, ler `radar_sat` (mesmo estilo do radar_coach: try/catch) e, se `"1"`, chamar `setSatelite(true)` DEPOIS que as camadas foram criadas. Escrever em toda troca (já no handler acima). Key: `radar_sat`, serialização `"1"`/`"0"`.

NÃO usar `L.control.layers` (widget nativo) — é botão bespoke on-brand. NÃO auto-trocar por zoom.
  </action>
  <verify>
    <automated>grep -q 'aria-label="Ver satélite"' "radar-goiania.html" && grep -q "function toggleSatelite\|function setSatelite" "radar-goiania.html" && grep -q "radar_sat" "radar-goiania.html" && grep -q "bottomright" "radar-goiania.html" && echo OK</automated>
  </verify>
  <acceptance_criteria>
- `grep 'aria-pressed'` mostra o botão com aria-pressed (inicial "false").
- `grep 'aria-label="Ver satélite"'` e `grep 'aria-label="Ver ruas"'` ambos presentes.
- `grep "bottomright"` confirma o slot do L.control.
- `grep "radar_sat"` aparece em leitura (boot) e escrita (handler).
- `grep "setSatelite\|toggleSatelite"` mostra o handler que faz add/removeLayer das camadas + flip de aria-pressed + textContent do glyph.
- Preview: botão 44×44px no canto inferior-direito; clicar alterna imagem/ruas; nunca troca sozinho ao dar zoom; atribuição Esri aparece só no satélite; outlines de bairro/lote continuam visíveis sobre a imagem; rótulos do reference overlay legíveis.
- Sem `L.control.layers`; sem novo hex.
  </acceptance_criteria>
  <done>Botão toggle deliberado (aria-pressed, ≥44px, cores das vars, bottomright z-490) alterna as camadas + reference overlay + atribuição; estilo vetorial reaplicado por modo; estado persiste em radar_sat; ruas é o default no boot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → server.arcgisonline.com | novo host de tile de terceiro (Esri) entra no CSP img-src e é buscado como imagem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | tiles Esri keyless | accept | Sem chave/segredo (keyless) e sem PII enviado — só coordenadas de tile z/y/x públicas. Nada a vazar. |
| T-04-02 | Tampering | CSP img-src widening | mitigate | Estender img-src APENAS ao host exato `server.arcgisonline.com` (sem wildcard); tiles são imagens (sem execução de script); demais diretivas (script-src, connect-src, object-src 'none') permanecem tight. |
| T-04-03 | Spoofing | tile de terceiro servindo imagem maliciosa | accept | Superfície é raster (`<img>`), não executável; um tile adulterado no máximo mostra pixels errados, não roda código. Host é o serviço oficial ArcGIS Online sobre HTTPS. |

**Sem ameaças de severidade alta.** O widening do CSP é escopado a um host específico (não wildcard além de arcgisonline), tiles são imagens (sem exec de script), não há segredos/chaves (keyless) nem PII. O CSP permanece tight em todas as outras diretivas.
</threat_model>

<verification>
- grep: `img-src ... server.arcgisonline.com` presente na linha 7 (sem wildcard).
- grep: URLs exatas World_Imagery e World_Boundaries_and_Places; atribuição verbatim; `satelliteOn`; `lotStyle`; `radar_sat`; `bottomright`; `aria-pressed`.
- Preview: ruas é o default; toggle alterna deliberadamente ruas⇄satélite; rótulos legíveis sobre imagem; outlines bairro/lote visíveis; atribuição Esri só no satélite; zoom não troca de modo; zero erros no console; busca/lote não regridem.
</verification>

<success_criteria>
- CSP permite Esri (host exato).
- Camadas satélite + reference criadas keyless, NÃO default.
- Toggle custom deliberado (aria-pressed, ≥44px, vars, bottomright z-490) — nunca por zoom.
- Rótulos legíveis (reference overlay) e outlines legíveis (branch de estilo) entregues no mesmo commit.
- Atribuição Esri aparece no satélite, some nas ruas.
- Zero novo hex; sem app-wide motion; ruas default.
</success_criteria>

<output>
After completion, create `.planning/phases/04-camada-de-sat-lite/04-01-SUMMARY.md`
</output>
