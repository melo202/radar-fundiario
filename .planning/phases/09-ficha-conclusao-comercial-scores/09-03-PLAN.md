---
phase: 09-ficha-conclusao-comercial-scores
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - radar-goiania.html
autonomous: true
requirements: [CMP-01]
tags: [ui, comparaveis, accordion, verificacao]

must_haves:
  truths:
    - "renderComps() mostra a frase-conclusao ('8% abaixo da mediana da vizinhanca — entre os mais baratos da regiao') ANTES de qualquer estatistica; a estatistica completa (bar/lbl/pos/mkt) fica recolhida em <details> 'Ver metodologia'"
    - "Apos compare() concluir com sucesso (res.n>=3), atualizarScores(a) re-renderiza #dScores/#dLeitura com o score de oportunidade REAL (nao mais null) e a leitura pratica correspondente -- sem reabrir a ficha, sem novo fetch"
    - "Estados honestos preservados: poucos comparaveis (n<3) mostra mensagem textual sem conclusao inventada e confianca baixa explicada; carregando e erro mantem os textos ja existentes"
    - "Todo alvo de toque novo (botao 'Ver metodologia') mede >=44px"
  artifacts:
    - path: "radar-goiania.html"
      provides: "renderComps() reescrito: .cmp-conclusao (frase primeiro) + .cmp-detalhe (details recolhido com bar/lbl/pos/mkt atuais, sem mudanca de calculo) + chamada a atualizarScores(a) ao final"
      contains: "cmp-conclusao"
    - path: "radar-goiania.html"
      provides: "atualizarScores(a) implementado (nao mais stub): chama scoreOportunidade com stats reais de compare(), re-renderiza #dScores/#dLeitura"
      contains: "function atualizarScores"
  key_links:
    - from: "renderComps()"
      to: "atualizarScores(a)"
      via: "chamada direta ao final de renderComps() apos escrever o HTML de #dComps, passando o imovel 'a' e as stats 'r' recem-calculadas"
      pattern: "atualizarScores\\("
    - from: "atualizarScores(a)"
      to: "scoreOportunidade (bloco RADAR_PURE, 09-01)"
      via: "chamada com stats={med,q1,q3,n,min,max} vindas de compsStats()/getComps(), substituindo o null usado em showDetail() (09-02)"
      pattern: "scoreOportunidade\\(myPm2"
    - from: ".cmp-detalhe summary"
      to: "estatistica completa (bar/lbl/pos/mkt)"
      via: "reuso literal do <details>/<summary> ja padronizado em .foot, conteudo interno IDENTICO ao innerHTML atual de renderComps (so realocado)"
      pattern: "cmp-detalhe"
---

<objective>
Reescrever `renderComps()` para "conclusão-primeiro" (CMP-01): a frase-conclusão ("8% abaixo da mediana da vizinhança — entre os mais baratos da região") aparece ANTES de qualquer estatística; a estatística completa (barra, mediana, Q1-Q3, posição, preço de mercado) recolhe em `<details>` "Ver metodologia". Ao concluir, `renderComps()` chama `atualizarScores(a)` — que implementa (não mais stub) o wiring com `scoreOportunidade`/`leituraPratica` da Wave 1, fechando o ciclo: `.dscores`/`.dleitura` da ficha (Wave 2) passam a refletir o score REAL depois que a análise de vizinhança termina.

Purpose: CMP-01 exige que cada comparação termine com conclusão + ação, não estatística crua. Esta é também a plan que FECHA o contrato deixado pela Wave 2 (`atualizarScores(a)` como stub) — sem ela, os scores da ficha ficariam permanentemente em "sem base"/"calculando" mesmo depois do corretor analisar a vizinhança.
Output: `renderComps()` reescrito (conclusão-primeiro, zero mudança de cálculo); `atualizarScores(a)` implementado; verificação end-to-end via asserts de DOM (Python/Node onde possível — o preview visual fica para a verificação de fase).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ficha-conclusao-comercial-scores/09-CONTEXT.md
@.planning/phases/09-ficha-conclusao-comercial-scores/09-UI-SPEC.md

# Fonte de código (ler antes de editar)
@radar-goiania.html

<facts>
<!-- Verificado por leitura direta deste repo, estado pós 09-01/09-02. -->

`renderComps(box,r,myPm2,radius,isU,a)` hoje (linha ~2047-2084) recebe `r` = resultado de `compsStats()`
(`{n,exact,vals?,min,max,q1,med,q3}`, sempre com `n>=3` — quem chama já filtrou `n<3` em `compare()`
linha 1997). Escreve `box.innerHTML` com, NESTA ORDEM ATUAL: `.h` (contagem + badge de confiança
"alta"/"media"/"baixa"), `.bar` (barra visual iqr/med/me), `.lbl` (min/meio/max), `.pos` ("Este imóvel:
R$/m² — mais caro que X% dos vizinhos"), `.mkt` (preço de mercado estimado na vizinhança). Todo o CÁLCULO
(percentil `below`, `conf`, `px()`, `cEff`) PERMANECE — esta plan só REORGANIZA o HTML de saída, não
recalcula nada.

`compare()` (linha ~1988-2000) é quem CHAMA `renderComps()` — fluxo: clique em `.cmpbtn` ("Analisar
vizinhança") → `compare()` → `getComps(a)` → se `res.n>=3`, `renderComps(box,g.res,g.myPm2,g.radius,
g.isU,a)`. Esta plan NÃO toca `compare()` nem `getComps()` — só o CORPO de `renderComps()` e o que ele
chama ao final.

Wave 2 (09-02) deixou `atualizarScores(a)` como STUB (assinatura + comentário, sem corpo real) e
`showDetail()` chamando `scoreOportunidade(myPm2, null, {})` (sempre `null` até esta wave). Ler
`.planning/phases/09-ficha-conclusao-comercial-scores/09-02-SUMMARY.md` para o contrato exato deixado
(assinatura de `atualizarScores`, ids exatos de `#dScores`/`#dLeitura` e como eles foram renderizados
na Wave 2 — esta plan deve gerar o MESMO formato de HTML dentro de `#dScores`/`#dLeitura`, não um
formato divergente).

Variável `conf` já calculada em `renderComps` (linha 2063): `r.n<5?["baixa","poucos vizinhos"]:amp<=.3?
["alta","faixa confiável"]:amp<=.4?["media","faixa aproximada"]:["baixa","imóveis muito variados"]` —
isto é a confiança da ESTATÍSTICA de vizinhança (amplitude do IQR), DIFERENTE da `scoreConfianca()`
(completude de dados, Wave 1) — as duas coexistem: o badge `.badge` em `.cmp-detalhe` continua usando
`conf` (não mudar), enquanto `scoreConfianca()` na ficha usa seus próprios critérios (areaOk/nComps/
atipico/venalOk). Ao chamar `scoreConfianca` dentro de `atualizarScores`, usar `nComps: r.n` (agora com
o valor real, substituindo o `nComps:0` do stub da Wave 2).

Estados textuais herdados que DEVEM permanecer intactos (zero mudança de string):
- Poucos comparáveis: `'Poucos comparáveis do mesmo perfil na vizinhança ('+n+' em '+radius+' m).'`
- Carregando: `'⏳ analisando a vizinhança… (até ~20s no celular)'`
- Erro: `'Falha ao consultar a vizinhança — tente de novo.'`
- Sem área/venal: `'Sem área/venal cadastrados — comparação indisponível.'`
</facts>

<interfaces>
<!-- Novo corpo de renderComps() + implementação de atualizarScores(). -->

Novo `box.innerHTML` de `renderComps()` (substitui o template atual, MESMAS variáveis já calculadas —
`r`, `conf`, `px`, `pctl`, `fx`, `cEff`, `myPm2`, `radius` — nenhuma nova, só reorganização de saída):

```js
/* (fix plan-check) A frase "X% abaixo/acima da mediana" usa MAGNITUDE DE PREÇO —
   |myPm2 - r.med| / r.med * 100 — a MESMA métrica do porque[] do scoreOportunidade
   (09-01) e do exemplo canônico do Plano UX ("8% abaixo da mediana"). NUNCA usar
   percentil-rank aqui (100-pctl diria "92% abaixo" pro mesmo imóvel do exemplo — número
   contraditório na MESMA ficha). O pctl continua usado SÓ na linha ".pos" do accordion
   ("mais caro que X% dos vizinhos"), que é outra leitura e já existe. */
const temBase = r && r.med>0 && myPm2>0;
const abaixo = temBase && myPm2 <= r.med;
const diffPct = temBase ? Math.round(Math.abs(myPm2-r.med)/r.med*100) : null;
const conclusaoTxt = !temBase
  ? "Posição na vizinhança não pôde ser calculada — confira a estatística completa."
  : diffPct===0
    ? `<b>Na mediana</b> da vizinhança — alinhado à região.`
    : abaixo
      ? `<b>${diffPct}% abaixo da mediana</b> da vizinhança — posição favorável para negociar.`
      : `<b>${diffPct}% acima da mediana</b> da vizinhança — acima do preço típico da região.`;
box.innerHTML = `<div class="cmp">
  <div class="cmp-conclusao">${conclusaoTxt}</div>
  <div class="cmp-acts">
    <button class="cmpbtn-sm" onclick="this.closest('.cmp').querySelector('.cmp-detalhe').open=true;this.closest('.cmp').querySelector('.cmp-detalhe').scrollIntoView({block:'nearest'})">Ver metodologia</button>
  </div>
  <details class="cmp-detalhe">
    <summary>Estatística completa</summary>
    <div class="footbody">
      <div class="h">Vizinhança — ${r.n} imóveis parecidos em até ${radius} m
        <span class="badge ${conf[0]}">${conf[1]}</span></div>
      <div class="bar">
        ${r.n>=5?`<div class="iqr" style="left:${px(r.q1)}%;width:${px(r.q3)-px(r.q1)}%"></div>
        <div class="med" style="left:${px(r.med)}%"></div>`:""}
        <div class="me" style="left:${px(myPm2)}%" title="este imóvel"></div>
      </div>
      <div class="lbl"><span>${r.exact?"":"~"}${fx(r.min)}</span><span>meio: ${fx(r.med)}</span><span>${r.exact?"":"~"}${fx(r.max)}</span></div>
      <div class="pos">Este imóvel: <b>${fx(myPm2)}/m²</b>${pctl!=null?` — mais caro que <b>${pctl}%</b> dos vizinhos`:""}</div>
      <div class="mkt">Preço de mercado estimado na vizinhança: <b>${fx(r.q1*cEff)}–${fx(r.q3*cEff)}/m²</b>. Base: cadastro da prefeitura — estimativa, não avaliação.</div>
    </div>
  </details>
</div>`;
atualizarScores(a, r); // NOVO — fecha o wiring com a Wave 2 (09-02)
```

Nota de fidelidade (ATUALIZADA pós plan-check): a frase-conclusão usa EXCLUSIVAMENTE a magnitude
de preço `|myPm2 - r.med|/r.med*100` — idêntica ao `porque[]` do `scoreOportunidade` (09-01), para
que o card de score e o painel de comparáveis NUNCA mostrem percentuais diferentes para o mesmo
imóvel (invariante de consistência: mesmo número, mesma direção nas duas superfícies). O `pctl`
(percentil-rank, "mais caro que X% dos vizinhos") permanece APENAS na linha `.pos` do accordion,
que é outra leitura estatística e já existia. Caso de sanidade obrigatório antes de finalizar:
myPm2=4600, med=5000 → "8% abaixo da mediana" nas DUAS superfícies (score e comparáveis).

Implementação de `atualizarScores(a, statsR)` (substitui o stub da Wave 2, mesma assinatura + 1 param novo):

```js
function atualizarScores(a, statsR){
  if(DCUR!==a) return; // imóvel mudou durante a consulta — não escreve painel alheio (mesmo guard de renderComps)
  const isU=!!unitLabel(a)||(a.cdedificio||0)>0;
  const myPm2 = isU?m2Edif(a):m2Terr(a);
  const areaOk = !!(isU?a.areaedif:a.areaterr);
  const venalOk = (a.vlvenal||0)>0;
  const atipico = isGarage(a);
  const stats = statsR ? {med:statsR.med,q1:statsR.q1,q3:statsR.q3,n:statsR.n,min:statsR.min,max:statsR.max} : null;
  const conf = scoreConfianca({areaOk, nComps: statsR?statsR.n:0, atipico, venalOk});
  const op = scoreOportunidade(myPm2, stats, {radius: statsR?statsR.radius:400});
  // Re-render #dScores e #dLeitura com o MESMO formato usado em showDetail() (09-02) —
  // reusar a(s) mesma(s) função(ões) de render de score criada(s) na Wave 2, não duplicar template.
  renderScoresInto(op, conf); /* assinatura CONTRATUAL (09-02): 2 args, renderiza em #dScores */
  document.getElementById("dLeitura").innerHTML =
    `<p>${esc(leituraPratica({tipoImovel: unitLabel(a)||USO[a.uso]||"Imóvel", bairro: clean(a.nmbairro), oportunidade: op, confianca: conf}))}</p>`;
}
```

**Dependência de nomenclatura (ATUALIZADA pós plan-check):** o nome NÃO é mais placeholder — a Wave 2
(09-02) é OBRIGADA a criar `function renderScoresInto(op, conf)` (2 args, renderiza em `#dScores`;
grep-assert no verify da 09-02 garante). Esta plan chama exatamente `renderScoresInto(op, conf)`.
Ler `09-02-SUMMARY.md` só para confirmar o formato de `aria-controls`/ids — se por qualquer desvio a
função não existir, extraí-la aqui do inline de `showDetail()` com ESTE nome/assinatura antes de usar.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reescrever renderComps() para conclusão-primeiro (CMP-01) + implementar atualizarScores()</name>
  <files>radar-goiania.html</files>
  <behavior>
    - renderComps() com n>=3 e pctl calculavel -> box.innerHTML contem '.cmp-conclusao' ANTES de '.cmp-detalhe' (indice de string confirma a ordem)
    - '.cmp-detalhe' e um <details> fechado por padrao (sem atributo open), contendo .h/.bar/.lbl/.pos/.mkt IDENTICOS ao calculo atual (mesmas variaveis r/conf/px/fx/cEff, zero mudanca de formula)
    - Apos renderComps() escrever o HTML, atualizarScores(a,r) e chamado (nao mais stub) -- #dScores passa a ter um score de oportunidade com NUMERO (nao mais "sem base") quando stats.n>=3
    - Estados de poucos-comparaveis/carregando/erro (definidos em compare(), nao em renderComps()) permanecem com o texto exato herdado -- esta task nao toca compare()
    - Botao 'Ver metodologia' mede min-height:44px
  </behavior>
  <action>
Ler `.planning/phases/09-ficha-conclusao-comercial-scores/09-02-SUMMARY.md` primeiro para confirmar: (a)
o nome exato da função/padrão de render de `.score` criado na Wave 2 dentro de `showDetail()`, e (b) o
formato exato de `aria-controls`/ids usados em `#dScores` (para não divergir aqui). Se a Wave 2 renderizou
`.score` inline (sem função extraída — desvio da 09-02), extrair uma função pequena `renderScoresInto(op, conf)`
a partir desse código inline (mesmo HTML, só movido para função reutilizável) — chamar essa função tanto
de dentro de `showDetail()` (troca o inline por chamada da função, mesmo resultado) quanto de dentro de
`atualizarScores()` (novo uso).

Substituir o corpo de `renderComps(box,r,myPm2,radius,isU,a)` (linha ~2047-2084) pelo template de
`<interfaces>` — mantendo TODAS as variáveis/cálculos já existentes (`below`, `conf`, `span`, `px`,
`pctl`, `fx`, `fxCx`, `m2b`, `cEff`) sem alteração de fórmula, só reorganizando o HTML de saída para
conclusão-primeiro. Resolver a semântica de `conclusaoTxt` conforme a nota de fidelidade em
`<interfaces>` (o sinal deve corresponder exatamente ao que `.pos` já comunica hoje — testar com o caso
`pctl=8` mentalmente: "mais caro que 8% dos vizinhos" = entre os MAIS BARATOS = texto deve dizer "abaixo
da mediana"/"mais baratos").

Adicionar CSS novo mínimo para `.cmp-conclusao`/`.cmp-acts`/`.cmpbtn-sm`/`.cmp-detalhe` reusando as
variáveis já existentes (ver UI-SPEC Component 5: `.cmp-conclusao{font:600 13.5px/1.5 "IBM Plex
Sans";color:var(--ink)}`, `<b>` dentro dela usa `color:var(--accent)` se abaixo da mediana ou
`color:var(--lot)` se acima — cor reforça, nunca sozinha, rótulo textual "abaixo"/"acima" sempre no
texto). Botão `.cmpbtn-sm{min-height:44px;font:600 11px/1;padding:8px 10px;border:1px solid
var(--line);background:var(--paper);color:var(--ink);border-radius:2px;cursor:pointer}`. `.cmp-detalhe`
reusa 1:1 as regras já existentes de `<details>`/`<summary>` (`.foot`/`.foot>summary`) — se `.cmp-detalhe`
precisar de uma classe própria para não herdar padding indesejado de `.foot`, criar só o necessário,
preferindo reuso.

Implementar `atualizarScores(a, statsR)` (substituindo o stub da Wave 2) exatamente conforme
`<interfaces>` — usando a função de render extraída/reusada acima. Chamar `atualizarScores(a, r)` ao
final do novo corpo de `renderComps()`.

Não tocar `compare()`, `getComps()`, `compsStats()`, `compsBase()` — zero mudança de cálculo/rede.
  </action>
  <verify>
    <automated>node --check radar-goiania.html && python -c "
s=open('radar-goiania.html',encoding='utf-8').read()
assert 'function renderComps(' in s
i=s.index('function renderComps(')
body=s[i:i+2500]
assert 'cmp-conclusao' in body, 'cmp-conclusao ausente'
assert 'cmp-detalhe' in body, 'cmp-detalhe ausente'
pos_concl=body.index('cmp-conclusao')
pos_det=body.index('cmp-detalhe')
assert pos_concl<pos_det, 'conclusao deve vir ANTES do detalhe no HTML gerado'
assert 'atualizarScores(' in body, 'atualizarScores nao chamado ao final de renderComps'
assert body.count('.h">Vizinhança') or 'Vizinhança —' in body, 'estatistica (contagem) preservada'
assert 'function atualizarScores(' in s
j=s.index('function atualizarScores(')
abody=s[j:j+1200]
for tok in ['scoreConfianca(','scoreOportunidade(']:
    assert tok in abody, tok+' ausente em atualizarScores'
assert 'min-height:44px' in s or 'cmpbtn-sm' in s
print('OK: renderComps conclusao-primeiro; atualizarScores implementado e chamado')
"</automated>
  </verify>
  <done>renderComps() gera .cmp-conclusao ANTES de .cmp-detalhe (details recolhido com a estatistica completa identica ao calculo atual); atualizarScores(a,r) implementado (chama scoreConfianca+scoreOportunidade com stats reais) e chamado ao final de renderComps(); botao "Ver metodologia" >=44px; compare()/getComps()/compsStats()/compsBase() intocados.</done>
</task>

<task type="auto">
  <name>Task 2: Verificação end-to-end via asserts de DOM (Node) + checklist de zero regressão</name>
  <files>radar-goiania.html</files>
  <action>
Criar um script de verificação TEMPORÁRIO (não persistir como arquivo de teste formal — usar Bash com
Python/Node inline, mesma técnica dos `<verify>` das tasks anteriores) que carrega `radar-goiania.html`
como texto e confirma, via asserts de string/regex (sem precisar de jsdom — o app não usa build/bundler):

1. Ordem final de `#detail` (dTag < dAddr < dInsc < dValor < dScores < dLeitura < dActsPrim <
   maisopcoes < dComps < dtecnico < dmetodologia) — mesmo assert já usado no Verify de 09-02, re-confirmar
   que a Wave 3 não alterou a ordem por engano.
2. `renderComps()` não contém mais o HTML antigo (sem `.cmp-conclusao`, estatística primeiro) — grep
   negativo: a ordem antiga (`.h">Vizinhança` aparecendo ANTES de qualquer texto de conclusão) não deve
   mais existir no corpo da função.
3. Nenhuma das strings herdadas mudou: `'Poucos comparáveis do mesmo perfil na vizinhança'`,
   `'⏳ analisando a vizinhança… (até ~20s no celular)'`, `'Falha ao consultar a vizinhança — tente de
   novo.'`, `venalTxt` (`'não informado'`) — todas presentes exatamente como antes.
4. `atualizarScores` é chamado de dentro de `renderComps` E tem guard `DCUR!==a` (mesmo padrão de
   descarte de resposta obsoleta já usado em `compare()`/`renderComps()` originais).
5. Nenhuma cor fora da paleta (`--paper`,`--paper-2`,`--ink`,`--line`,`--muted`,`--accent`,`--accent-ink`,
   `--lot`,`--gold`) aparece em qualquer regra CSS nova desta fase (`.dvalor`,`.dscores`,`.score`,
   `.dleitura`,`.maisopcoes`,`.cmp-conclusao`,`.cmp-acts`,`.cmpbtn-sm`,`.cmp-detalhe`) — grep por `#` em
   cada bloco de regra nova e confirmar que só aparecem nos `var(--...)` já existentes (nenhum hex
   literal novo introduzido).

Documentar no SUMMARY desta plan (09-03) o resultado de cada um dos 5 checks acima, e explicitamente
anotar que a VERIFICAÇÃO VISUAL (mobile 375 + desktop 1280, contraste AA, foco por teclado, `<details>`
navegável) é responsabilidade da verificação de fase (`/gsd-verify-phase` ou equivalente), não desta
plan — esta task cobre o que é auditável por DOM/string, não pixels.
  </action>
  <verify>
    <automated>python -c "
s=open('radar-goiania.html',encoding='utf-8').read()
# 1. ordem
i_detail=s.index('id=\"detail\"'); block=s[i_detail:i_detail+3000]
order=['dTag','dAddr','dInsc','dValor','dScores','dLeitura','dActsPrim','maisopcoes','dComps','dtecnico','dmetodologia']
pos=[block.find(t) for t in order]
assert all(p>-1 for p in pos) and pos==sorted(pos), 'ordem da ficha regrediu'
# 3. strings herdadas
for frag in ['Poucos comparáveis do mesmo perfil na vizinhança','analisando a vizinhança','Falha ao consultar a vizinhança','não informado']:
    assert frag in s, 'string herdada ausente/alterada: '+frag
# 4. guard em atualizarScores
j=s.index('function atualizarScores('); abody=s[j:j+400]
assert 'DCUR' in abody, 'guard DCUR ausente em atualizarScores'
# 5. paleta -- checa que os blocos CSS novos so usam var(--...), sem hex cru novo
import re
for cls in ['.dvalor','.dscores','.score','.dleitura','.maisopcoes','.cmp-conclusao','.cmpbtn-sm']:
    idx=s.find(cls+'{')
    if idx==-1: continue
    rule=s[idx:idx+400]
    hexes=re.findall(r'#[0-9a-fA-F]{3,6}\\b',rule)
    allowed={'#fff','#ffffff'}
    bad=[h for h in hexes if h.lower() not in allowed]
    assert not bad, cls+' usa hex fora da paleta: '+str(bad)
print('OK: verificacao end-to-end (ordem, strings herdadas, guard, paleta) passou')
"</automated>
  </verify>
  <done>5 checks de verificação end-to-end documentados e passando (ordem da ficha, conclusão-primeiro em renderComps, strings herdadas intactas, guard DCUR em atualizarScores, zero hex fora da paleta); SUMMARY anota explicitamente que a verificação visual fica para a verificação de fase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderComps()/atualizarScores() -> DOM (innerHTML) | resultado de consulta ArcGIS (números agregados, não PII) + resultado de funções puras de score, injetados via innerHTML |
| compare() -> ArcGIS (rede) | mesma superfície de rede já existente (compsBase/compsStats), intocada por esta plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Tampering | renderComps() reescrito pode divergir do cálculo original por erro de transcrição | mitigate | Verify compara variáveis usadas (r/conf/px/fx/cEff) sem introduzir novas; Task 2 confirma que a estatística completa permanece idêntica dentro do `.cmp-detalhe` |
| T-09-08 | Repudiation | atualizarScores() sobrescrevendo painel de um imóvel diferente do consultado (race condition ao trocar de imóvel durante compare() em andamento) | mitigate | guard `DCUR!==a` replicado do padrão já usado em `compare()`/`renderComps()` original — testado no Verify (`DCUR` presente no corpo) |
| T-09-09 | Information Disclosure | estatística de vizinhança (mediana/quartis de R$/m²) exposta no accordion | accept | dado já público/agregado hoje (mesmo cálculo, só reapresentado), nenhuma informação nova exposta |

**Sem ameaças de alta severidade.** Reorganização de apresentação + wiring de scores já testados (Wave 1) sobre consulta de rede já existente e auditada — nenhum novo vetor de rede/input introduzido.
</threat_model>

<verification>
- `node --check radar-goiania.html` passa
- `renderComps()` gera `.cmp-conclusao` ANTES de `.cmp-detalhe`; estatística completa idêntica ao cálculo atual dentro do accordion
- `atualizarScores(a,r)` implementado (não mais stub), chamado ao final de `renderComps()`, com guard `DCUR!==a`
- `scoreOportunidade`/`scoreConfianca` chamados dentro de `atualizarScores` com stats reais (`r.med/r.q1/r.q3/r.n`)
- Zero regressão: strings herdadas de poucos-comparáveis/carregando/erro/venalTxt intactas; `compare()`/`getComps()`/`compsStats()`/`compsBase()` intocados
- Nenhum hex fora da paleta declarada em qualquer CSS novo desta fase
</verification>

<success_criteria>
- CMP-01 cumprido: comparáveis mostram conclusão antes de estatística, cada comparação termina com ação ("Ver metodologia" ou a ação primária já visível acima)
- Ciclo de scores fechado: `.dscores`/`.dleitura` da ficha (Wave 2) passam a refletir dados reais depois de `compare()`, sem duplicar template de `.score`
- Fase 9 completa: FICHA-01, SCORE-01, SCORE-02, LEIT-01, CMP-01 todos com plano de implementação e verificação automatizada
</success_criteria>

<output>
Após completar, criar `.planning/phases/09-ficha-conclusao-comercial-scores/09-03-SUMMARY.md`.
Incluir no SUMMARY: resultado dos 5 checks da Task 2, confirmação de que `atualizarScores` não duplica o template de `.score` (nome da função de render reusada), e lista do que fica pendente para a verificação de fase (visual mobile/desktop, contraste AA, navegação por teclado dos `<details>` novos).
</output>
