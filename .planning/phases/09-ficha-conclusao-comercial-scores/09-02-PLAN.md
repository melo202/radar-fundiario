---
phase: 09-ficha-conclusao-comercial-scores
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - radar-goiania.html
autonomous: true
requirements: [FICHA-01]
tags: [ui, ficha, accordion, lei-da-tela, css]

must_haves:
  truths:
    - "A ficha (#detail) reordena top->bottom: shead -> dtag -> h3 -> insc -> .dvalor (faixa de valor destaque) -> .dscores (oportunidade+confianca) -> .dleitura (leitura pratica) -> .acts (1 primaria + 2 secundarias + Mais opcoes) -> .dcomps -> accordion Dados tecnicos -> accordion Metodologia e fontes"
    - "Faixa de valor (.dvalor-v) e o maior elemento tipografico da ficha (22px/800), maior que qualquer numero em .dgrid"
    - "Score de oportunidade e confianca sempre mostram rotulo textual + cor de reforco -- nunca só cor; estado sem base mostra 'Sem base para estimar' sem numero inventado"
    - "Ações: exatamente 1 primária (Gerar documento, abrirLaudo()) + 2 secundárias (Ver comparáveis, Copiar inscrição) + Mais opções recolhido (custos, CND, copiar link, mapas)"
    - "Zero regressao: venalTxt(0) continua 'nao informado'; sheet mobile grab/backlist/scroll intactos; z-index do satelite sob .detail; guardas a7a4646 intactas"
  artifacts:
    - path: "radar-goiania.html"
      provides: "CSS novo (.dvalor, .dscores, .score, .dleitura, .maisopcoes) + showDetail() reescrito para popular a ordem nova + #detail HTML reordenado no <body>"
      contains: "dvalor-v"
    - path: "radar-goiania.html"
      provides: "Accordion 'Dados técnicos' (dgrid+dnote) e 'Metodologia e fontes' recolhidos por padrão, reuso de .foot/<details>"
      contains: "Metodologia e fontes"
  key_links:
    - from: "showDetail()"
      to: "scoreOportunidade/scoreConfianca/leituraPratica (bloco RADAR_PURE, plan 09-01)"
      via: "chamada direta das 3 funcoes puras com myPm2/stats/inputs computados a partir de mercadoEstimado()/getComps()/m2Edif()/m2Terr()"
      pattern: "scoreOportunidade\\("
    - from: "#detail .acts"
      to: "abrirLaudo()"
      via: "botao primario onclick=\"abrirLaudo()\" (existente, reaproveitado, sem mudanca de wizard)"
      pattern: "onclick=\"abrirLaudo\\(\\)\""
    - from: "#detail details.dtecnico"
      to: "#dGrid/#dNote"
      via: "dgrid e dnote passam a viver DENTRO do <details> recolhido, mesmos ids, mesmo conteudo populado por showDetail()"
      pattern: "class=\"foot dtecnico\""
---

<objective>
Reordenar a ficha do imóvel (`#detail`, populada por `showDetail()`) de "dado técnico primeiro" para "conclusão comercial primeiro": faixa de valor em destaque → score de oportunidade → score de confiança → leitura prática → ações (lei da tela: 1 primária + 2 secundárias + Mais opções) → comparáveis → dados técnicos em accordion → metodologia no fim. Consome as funções puras `scoreOportunidade`/`scoreConfianca`/`leituraPratica` criadas na Wave 1 (09-01) — não reimplementa cálculo, só chama e apresenta.

Purpose: FICHA-01 exige que o corretor veja "quanto vale, qual a oportunidade e o que fazer" ANTES do dado técnico. Esta é a mudança estrutural central da Fase 9 — a fundação (scores puros) já existe da Wave 1; esta plan é 100% apresentação sobre dados já calculados (`mercadoEstimado`, `getComps`, `m2Edif`/`m2Terr`, `venalTxt`).
Output: `#detail` reordenado no HTML estático; CSS novo (`.dvalor`, `.dscores`, `.score`, `.dleitura`, `.maisopcoes`, accordions) reusando exclusivamente as variáveis já existentes (zero hex novo); `showDetail()` reescrito para popular a ordem nova e chamar os 3 scores puros.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ficha-conclusao-comercial-scores/09-CONTEXT.md
@.planning/phases/09-ficha-conclusao-comercial-scores/09-UI-SPEC.md

# Fonte de código (ler antes de editar)
@radar-goiania.html

<facts>
<!-- Verificado por leitura direta deste repo (radar-goiania.html, estado atual pós 09-01). -->

HTML atual de `#detail` (linhas ~632-647, dentro de `.mapwrap`):
```html
<div class="detail" id="detail" role="region" aria-label="Detalhe do imóvel">
  <div class="shead">...grab/backlist/x...</div>
  <div class="dtag" id="dTag"></div>
  <h3 id="dAddr"></h3>
  <div class="insc" id="dInsc"></div>
  <div class="dgrid" id="dGrid"></div>
  <div class="dcomps" id="dComps"></div>
  <div class="dlaudo"><button class="custosbtn" onclick="abrirCustos()">🧮 Custos de compra (ITBI + cartório)</button>
    <button class="laudobtn" onclick="abrirLaudo()">📄 Gerar laudo de avaliação (PDF)</button></div>
  <div class="dnote" id="dNote"></div>
  <div class="acts" id="dActs"></div>
</div>
```

`showDetail(a,ll)` (linha ~1896-1953) hoje popula, NESTA ORDEM: dTag (textContent), dAddr (textContent),
dInsc (textContent), dGrid (innerHTML com 9 .cell), dComps (botão "Analisar vizinhança" OU re-render do
cache via compare()), dNote (textContent — nota de fonte/data), dActs (innerHTML com mapLinks+CND+copyLink),
dBack (display), classList.add("show") + animação de entrada (mAnimate, spring mobile / fade desktop).//
`.dlaudo` é um bloco FIXO no HTML (não populado por JS) — só os 2 botões custosbtn/laudobtn.

Funções já existentes e REUTILIZADAS sem mudança de cálculo (não recriar):
- `mercadoEstimado(a,privReal)` linha ~1695: retorna `{lo,hi,metodo}` ou `null`.
- `m2Edif(a)`/`m2Terr(a)` linha ~1718-1719: R$/m² do próprio imóvel ou `null`.
- `venalTxt(v)` linha 716: `v>0?brl(v):"não informado"`.
- `brl(n)`/`brlC(n)` linha 713/1693: formatação de moeda.
- `unitLabel(a)` linha ~1720: rótulo de unidade ("Apto 302") ou null.
- `USO` linha 712: mapa uso->rótulo.
- `isGarage(a)` (bloco RADAR_PURE): detecta atípico.
- `scoreOportunidade(myPm2,stats,flags)`, `scoreConfianca(inputs)`, `leituraPratica(inputs)` — NOVAS
  desta fase, criadas em 09-01 dentro do bloco RADAR_PURE. Assinaturas completas em
  `.planning/phases/09-ficha-conclusao-comercial-scores/09-01-PLAN.md` (seção `<interfaces>`).

`compare()`/`getComps(a)`/`compsStats()` (linhas ~1971-2046) SÓ rodam quando o corretor clica em
"Analisar vizinhança" (botão dentro de `dComps`) — a consulta de comparáveis NÃO é automática no
`showDetail()` (orçamento de requisições). Isto significa: **os scores de oportunidade/confiança que
dependem de `stats` de vizinhança SÓ podem ser calculados de verdade DEPOIS que `compare()` roda** —
ANTES disso, `.dscores`/`.dleitura` mostram o estado "Calculando…" (se há coordenada, oferecendo
disparar a análise) ou usam SOMENTE o que já está disponível sem rede (venal, mercadoEstimado, m2Edif/
m2Terr) para uma leitura parcial. Ver `<facts>` da Wave 3 (09-03) para o fluxo completo pós-`compare()`.

Decisão desta plan (Claude's discretion, dentro do "sem incerteza"): `showDetail()` roda
`scoreOportunidade(myPm2, null, {})` → sempre `null` até `compare()` popular `DCUR.__compsStats` (novo
campo leve, cache no próprio objeto do imóvel — mesmo padrão de `r._below` em `renderComps`). Quando
`compare()` conclui com sucesso, ele chama uma função nova `atualizarScores(a)` que RE-renderiza
`.dscores`/`.dleitura` com os dados completos — sem re-abrir a ficha, sem re-fetch. Este é o ÚNICO ponto
de wiring novo entre Wave 2 (aqui) e Wave 3 (09-03, que reescreve `renderComps()` e deve CHAMAR
`atualizarScores(a)` ao final, ver 09-03).

CSS existente reusado 1:1 (não recriar): `.foot`/`.foot>summary` (linha ~187-190), `.badge.alta/.media/
.baixa` (linha 259), `.detail .dgrid .cell` (linha 245-249), `.detail .acts a/button` (linha 434-440),
`.cmpbtn` (linha 253-255). Z-index/mobile sheet: `.detail{z-index:500}` (linha 221), satélite toggle
`z-index:490` (linha 279), `.mapwrap:has(.detail.show) .leaflet-bottom{z-index:490}` (linha 499) —
NENHUM destes é tocado.
</facts>

<interfaces>
<!-- HTML/CSS/JS que esta plan cria — contrato consumido pela Wave 3 (09-03, renderComps). -->

Novo HTML de `#detail` (substitui o bloco em `<facts>`, mesmo container/ids onde já existem, ordem nova):

```html
<div class="detail" id="detail" role="region" aria-label="Detalhe do imóvel">
  <div class="shead">...grab/backlist/x... (INALTERADO)</div>
  <div class="dtag" id="dTag"></div>
  <h3 id="dAddr"></h3>
  <div class="insc" id="dInsc"></div>

  <div class="dvalor" id="dValor"></div>

  <div class="dscores" id="dScores"></div>

  <div class="dleitura" id="dLeitura"></div>

  <div class="acts" id="dActsPrim"></div>
  <details class="maisopcoes">
    <summary>Mais opções</summary>
    <div class="footbody acts" id="dActsMore"></div>
  </details>

  <div class="dcomps" id="dComps"></div>

  <details class="foot dtecnico">
    <summary>Dados técnicos</summary>
    <div class="footbody">
      <div class="dgrid" id="dGrid"></div>
      <div class="dnote" id="dNote"></div>
    </div>
  </details>

  <details class="foot dmetodologia">
    <summary>Metodologia e fontes</summary>
    <div class="footbody" id="dMetodologia"></div>
  </details>
</div>
```

CSS novo a acrescentar (junto das regras `.detail *` já existentes, linha ~245-296 aproximadamente —
reusar exclusivamente as variáveis do UI-SPEC "Tokens Reusados", zero hex novo):

```css
.dvalor{background:var(--paper);border:2px solid var(--ink);border-radius:2px;padding:16px 18px;margin-bottom:12px}
.dvalor-k{font:600 11px/1.2 "IBM Plex Sans";color:var(--muted);text-transform:uppercase;letter-spacing:.02em}
.dvalor-v{font:800 22px/1.15 "IBM Plex Sans";color:var(--ink);margin:4px 0 2px}
.dvalor-v.muted{color:var(--muted)}
.dvalor-m{font:500 10.5px/1.45 "IBM Plex Sans";color:var(--muted)}

.dscores{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:12px}
.score{flex:1;min-width:150px;min-height:44px;display:block;text-align:left;background:var(--paper);
  border:1.5px solid var(--line);border-radius:2px;padding:8px 12px;cursor:pointer;font-family:inherit}
.score-op{border-left:4px solid var(--line)}
.score-op.alta{border-left-color:var(--lot)}
.score-op.media{border-left-color:var(--gold)}
.score-op.baixa{border-left-color:var(--accent)}
.conf-alta{border-left:4px solid var(--lot)}
.conf-media{border-left:4px solid var(--gold)}
.conf-baixa{border-left:4px solid var(--accent)}
.score-num{font:800 20px/1 "IBM Plex Sans";color:var(--ink)}
.score-op.alta .score-num{color:var(--lot)}
.score-op.media .score-num{color:#7d621f}
.score-op.baixa .score-num{color:var(--accent)}
.score-max{font:600 12px/1;color:var(--muted)}
.score-lbl,.score-lbl-main{display:block;font:700 13px/1.2 "IBM Plex Sans";margin-top:2px}
.score-chev{float:right;color:var(--muted)}
.score-why{font:500 12px/1.5 "IBM Plex Sans";color:var(--muted);padding:8px 12px 2px}

.dleitura{background:var(--paper);border:1px solid var(--line);border-radius:2px;padding:12px 14px;margin-bottom:12px}
.dleitura p{font:500 13.5px/1.55 "IBM Plex Sans";color:var(--ink);margin:0}

.maisopcoes{padding:0;border-top:0;margin-bottom:12px}
.maisopcoes>summary{cursor:pointer;font:600 10px/1.4 "IBM Plex Mono",monospace;letter-spacing:.07em;
  text-transform:uppercase;color:var(--muted);padding:9px 0;list-style:none}
.maisopcoes>summary::-webkit-details-marker{display:none}
.maisopcoes>summary::before{content:"▸ "}
.maisopcoes .footbody.acts{padding-top:8px}

.dtecnico .dgrid{margin-bottom:10px}
```

Nota: `.foot>summary::before{content:"▸ "}` já existe globalmente para `<details class="foot">`; o `open`
troca `▸`→`▾` via o mesmo mecanismo CSS já usado por "Fontes & metodologia" (confirmar no CSS existente
se há uma regra `details[open]>summary::before{content:"▾ "}` — se não houver, ACRESCENTAR essa regra
UMA vez, compartilhada por `.foot` e `.maisopcoes`, não duplicar por classe).

Nova função JS `renderScoreOp(op)`/`renderScoreConf(conf)`/wiring de clique — usar `<button>` real com
`aria-expanded`/`aria-controls`, alternando `hidden` no `.score-why` correspondente via `onclick` inline
que chama uma função nomeada `toggleScoreWhy(btn)` (lê `aria-expanded` do próprio botão, sem estado
global) — nenhum framework, mesmo padrão de `showChooser`/`toggleX` já usados no arquivo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reordenar #detail no HTML + CSS novo (.dvalor, .dscores, .score, .dleitura, .maisopcoes, accordions)</name>
  <files>radar-goiania.html</files>
  <action>
Substituir o bloco HTML de `#detail` (linhas ~632-647) pela estrutura nova de `<interfaces>`: mesma
`.shead` (INALTERADA — grab/backlist/x), `.dtag`/`h3#dAddr`/`.insc` na mesma posição (só descem
visualmente por causa do CSS de destaque do `.dvalor` abaixo, não mudam de posição no DOM), depois os
containers vazios NOVOS (`#dValor`, `#dScores`, `#dLeitura`), depois `.acts` dividido em `#dActsPrim`
(sempre visível) + `<details class="maisopcoes">` com `#dActsMore` dentro, depois `#dComps` (mesma
posição relativa, mas AGORA depois das ações, não antes), depois os dois `<details>` novos
(`.dtecnico` contendo `#dGrid`+`#dNote`, e `.dmetodologia` contendo `#dMetodologia` vazio) no final.
Remover o bloco `.dlaudo` fixo (os 2 botões custosbtn/laudobtn são absorvidos pela Lei da Tela: laudobtn
vira a ação primária dentro de `#dActsPrim`; custosbtn vira uma opção dentro de `#dActsMore` — ver Task 2).

Acrescentar o CSS novo de `<interfaces>` no bloco de estilos do `.detail` (próximo às regras `.detail
.dgrid`/`.detail .acts` já existentes, linha ~245-296), respeitando a ordem de cascata do arquivo
(regras `.detail X` já existentes não são removidas, só as de `.dlaudo`/`.laudobtn`/`.custosbtn` podem
ser mantidas como estão — os SELETORES continuam existindo, só o container HTML fixo `.dlaudo` sai do
`#detail`; se `.laudobtn`/`.custosbtn` não forem mais referenciados em nenhum HTML após a Task 2, é
aceitável deixar o CSS morto por ora, mas PREFERÍVEL remover as classes CSS não mais usadas se
confirmado — decidir na Task 2 quando os botões forem recriados com classes diferentes).

Adicionar a regra compartilhada `details[open]>summary::before{content:"▾ "}` (uma única vez, junto de
`.foot>summary::before`) se ainda não existir — verificar antes de duplicar.

Nenhuma mudança de z-index, nenhuma mudança em `.shead`/`.grab`/`.backlist`/mobile breakpoints
(linhas ~484-502) — só a ORDEM interna do conteúdo do `.detail`, container e regras de mobile/z-index
permanecem intocados.
  </action>
  <verify>
    <automated>node --check radar-goiania.html && python -c "
s=open('radar-goiania.html',encoding='utf-8').read()
i_detail=s.index('id=\"detail\"')
detail_block=s[i_detail:i_detail+3000]
order=['dTag','dAddr','dInsc','dValor','dScores','dLeitura','dActsPrim','maisopcoes','dComps','dtecnico','dmetodologia']
positions=[detail_block.find(tok) for tok in order]
assert all(p>-1 for p in positions), dict(zip(order,positions))
assert positions==sorted(positions), 'ordem incorreta: '+str(list(zip(order,positions)))
assert 'dGrid' in detail_block and 'dNote' in detail_block
print('OK: ordem da ficha reordenada confirmada por indice de string')
"</automated>
  </verify>
  <done>#detail contém os containers na ordem: dTag, dAddr, dInsc, dValor, dScores, dLeitura, dActsPrim, maisopcoes(dActsMore), dComps, dtecnico(dGrid+dNote), dmetodologia; CSS novo presente reusando só variáveis já existentes; node --check passa.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reescrever showDetail() para popular a ordem nova + chamar scoreOportunidade/scoreConfianca/leituraPratica</name>
  <files>radar-goiania.html</files>
  <behavior>
    - showDetail(a,ll) com venal>0 e mercadoEstimado() valido -> #dValor mostra faixa "R$ X mil – R$ Y mil" (Display 22px), NUNCA "muted"
    - showDetail(a,ll) com venal=0 e sem tabela de bairro (mercadoEstimado null) -> #dValor mostra "Sem base para estimar" com classe dvalor-empty/muted, texto explicativo presente
    - showDetail() SEM stats de vizinhanca ainda (compare() nao rodou) -> #dScores mostra score de oportunidade em estado "Calculando..." OU "Sem base" (nunca numero solto sem stats) + score de confianca calculado só com o que já há (areaOk/venalOk/atipico, nComps=0)
    - #dActsPrim sempre tem exatamente 1 botao com classe primary (abrirLaudo) + no maximo 2 botoes secundarios (Ver comparaveis, Copiar inscricao)
    - #dActsMore (dentro do maisopcoes) recebe custos+CND+copiarLink+mapas (o que sobrou)
    - venalTxt(0) continua retornando "não informado" (regressão zero) -- chamado dentro de #dGrid como antes
  </behavior>
  <action>
Reescrever `showDetail(a,ll)` (linha ~1896-1953) para popular, NESTA ORDEM, os containers novos:

1. `#dTag`/`#dAddr`/`#dInsc` — EXATAMENTE como hoje (textContent, sem mudança de lógica).
2. `#dValor` — chamar `mercadoEstimado(a)`; se retornar objeto, `innerHTML` com `.dvalor-k` ("Faixa
   estimada de mercado") + `.dvalor-v` (`brlC(lo)+" – "+brlC(hi)`) + `.dvalor-m` (resumo curto do
   `metodo`, 1 linha — truncar/resumir se necessário, texto completo já vai para `#dMetodologia`); se
   `null`, usar a variante `dvalor-empty`/`.dvalor-v.muted` com o texto exato do UI-SPEC Component 1
   ("Sem base para estimar" + "Faltam dados suficientes (venal ou área) para calcular uma faixa.
   Consulte o dado técnico abaixo.").
3. `#dScores` — computar `myPm2` (`isU?m2Edif(a):m2Terr(a)`), `areaOk` (`!!(isU?a.areaedif:a.areaterr)`),
   `venalOk` (`(a.vlvenal||0)>0`), `atipico` (`isGarage(a)`). Chamar `scoreConfianca({areaOk,nComps:0,
   atipico,venalOk})` (nComps=0 até `compare()` rodar — Wave 3 atualiza depois). Chamar
   `scoreOportunidade(myPm2, null, {})` (retorna `null` até `compare()` popular stats — Wave 3 chama
   `atualizarScores(a)` depois). Renderizar os 2 `<button class="score">` com `aria-expanded="false"`
   conforme UI-SPEC Component 2 (rótulo textual sempre visível, `border-left` de status, chevron ▸).
   Quando `scoreOportunidade` é `null`, mostrar "Sem base para estimar" (SEM número), classe neutra
   (`border-left:var(--line)`).
4. `#dLeitura` — chamar `leituraPratica({tipoImovel: unitLabel(a)||USO[a.uso]||"Imóvel", bairro:
   clean(a.nmbairro), oportunidade: null, confianca: <resultado do passo 3>})` — como oportunidade ainda
   é `null` neste ponto (pré-`compare()`), o template retorna a frase "Dados insuficientes...". Envolver
   em `<p>`.
5. `#dActsPrim` — 1 botão `class="primary" onclick="abrirLaudo()"` ("📄 Gerar documento") + botão
   `onclick="document.getElementById('dComps').scrollIntoView({behavior:motionOk?'smooth':'auto'})"`
   ("📊 Ver comparáveis") + botão `onclick="copyInsc('...')"` ("Copiar inscrição ⧉") — usar o MESMO
   `inscUse` já calculado hoje (`esc(insc||ci)`). Verificar se `motionOk`/equivalente já existe no
   arquivo (variável REDUCE-aware) — se o nome for outro, usar o existente (não inventar variável nova).
6. `#dActsMore` — botão custos (`onclick="abrirCustos()"`, texto "🧮 Custos de compra (ITBI + cartório)"),
   link CND (mesmo `cnd` href/onclick de hoje), botão copiar link (mesmo `copyLink(inscUse)` de hoje),
   e os 3 links de mapa (`mapLinks` — Google Maps/Street View/Earth, MESMO cálculo de hoje, só sem a
   classe `primary` que o link do Maps tinha antes, já que agora "Gerar documento" é a única primária).
7. `#dGrid`/`#dNote` — EXATAMENTE como hoje (dentro do accordion agora, mesmo innerHTML/textContent).
8. `#dComps` — EXATAMENTE como hoje nesta plan (botão "Analisar vizinhança" ou re-render de cache via
   `compare()`) — Wave 3 (09-03) reescreve o CONTEÚDO pós-análise, não esta plan.
9. `#dMetodologia` — `innerHTML` estático (mesmo texto para todo imóvel, UI-SPEC Component 6): parágrafos
   explicando score de oportunidade, score de confiança, faixa de valor, fonte ArcGIS — usar o texto
   EXATO do UI-SPEC "Copywriting Contract" linhas "Metodologia — score de oportunidade"/"score de
   confiança" + a fonte já usada em `.foot` global (linha 613, reaproveitar o texto, não duplicar
   verbatim se puder referenciar/resumir).

Manter `dBack`, `classList.add("show")`, `SHEETGEN++`, e a animação de entrada (`mAnimate`) EXATAMENTE
como hoje — nenhuma mudança de motion/timing/z-index/scroll (zero regressão).

Criar a função nova `atualizarScores(a)` (vazia/no-op stub nesta plan, só a ASSINATURA e um comentário
`/* Wave 3 (09-03) implementa: re-renderiza #dScores/#dLeitura com stats de compare() */` — Wave 3 vai
IMPLEMENTAR o corpo; esta plan só garante que a função existe para não quebrar nenhuma referência futura
e documenta o contrato de wiring entre waves).
  </action>
  <verify>
    <automated>node --check radar-goiania.html && python -c "
s=open('radar-goiania.html',encoding='utf-8').read()
assert 'function showDetail(' in s
i=s.index('function showDetail(')
body=s[i:i+6000]
for tok in ['dValor','dScores','dLeitura','dActsPrim','dActsMore','scoreConfianca(','leituraPratica(','abrirLaudo()','Ver comparáveis','Copiar inscrição']:
    assert tok in body, tok+' ausente do corpo de showDetail()'
assert 'function atualizarScores(' in s, 'atualizarScores() ausente (contrato para Wave 3)'
assert 'v>0)?brl(v):\"não informado\"' in s or 'não informado' in s, 'venalTxt regressao'
print('OK: showDetail popula os containers novos; atualizarScores existe como contrato p/ Wave 3')
"</automated>
  </verify>
  <done>showDetail() popula dValor/dScores/dLeitura/dActsPrim/dActsMore/dGrid/dNote/dComps/dMetodologia; scoreConfianca/leituraPratica chamados com inputs corretos; scoreOportunidade chamado (null antes de compare()); acts tem exatamente 1 primaria (abrirLaudo) + 2 secundarias; atualizarScores(a) existe como stub documentado p/ Wave 3; zero regressao de venalTxt/grab/backlist/z-index.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| showDetail(a,ll) -> DOM (innerHTML) | dados do cadastro (ArcGIS) e resultado de scoreOportunidade/scoreConfianca/leituraPratica injetados via innerHTML/textContent |
| onclick inline (abrirLaudo/copyInsc/copyLink/abrirCustos) | mesmos handlers já existentes, reusados sem mudança de assinatura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | innerHTML de #dValor/#dScores/#dLeitura com texto derivado do cadastro (nmbairro, metodo) | mitigate | usar `esc()` em qualquer valor textual vindo do cadastro (nmbairro, incompl) inserido via innerHTML; textos de score/leitura são gerados por template fixo (não interpolam texto livre de terceiros) |
| T-09-05 | Information Disclosure | onclick inline reconstruindo string JS com `inscUse` (mesmo padrão hoje) | accept | `inscUse` já é `esc(insc\|\|ci)` (dígitos/formatação de inscrição pública, não PII), mesma exposição já aceita no app hoje — nenhuma mudança de superfície |
| T-09-06 | Denial of Service (UX) | score "Sem base" indevido por bug de guard (ex. myPm2=0 tratado como falsy incorretamente) | mitigate | Verify Task 2 confirma presença de `scoreConfianca(`/`leituraPratica(` com os argumentos corretos; fixtures da Wave 1 (09-01) já travam o contrato `null` vs número — esta plan só chama, não recalcula |

**Sem ameaças de alta severidade.** Mudança de apresentação sobre dados já sanitizados/calculados; nenhum novo vetor de input do usuário é introduzido (scores são derivados, não digitados).
</threat_model>

<verification>
- `node --check radar-goiania.html` passa
- Ordem de `#detail` confirmada por índice de string: dTag < dAddr < dInsc < dValor < dScores < dLeitura < dActsPrim < maisopcoes < dComps < dtecnico < dmetodologia
- `showDetail()` chama `scoreConfianca(`/`leituraPratica(` com os inputs documentados; `scoreOportunidade(` é chamado (mesmo que com stats=null nesta wave)
- `.acts` (`#dActsPrim`) tem exatamente 1 botão `primary` (abrirLaudo) + 2 secundários; `#dActsMore` recebe o resto (custos, CND, copiar link, mapas)
- `atualizarScores(a)` existe como stub — contrato para a Wave 3 (09-03)
- Zero regressão: `venalTxt(0)` → "não informado"; `.shead`/`.grab`/`.backlist` intactos; z-index/mobile breakpoints intocados
</verification>

<success_criteria>
- FICHA-01 cumprido: ficha reordena conclusão-primeiro, dado técnico em accordion recolhido, metodologia no fim
- Scores de oportunidade/confiança e leitura prática aparecem na ficha (mesmo que parcialmente "sem base"/"calculando" antes de `compare()` rodar — comportamento honesto, não falsa precisão)
- Lei da tela estruturada: 1 ação primária + 2 secundárias + Mais opções
- Wave 3 (09-03) pode implementar `atualizarScores(a)` sem re-tocar a estrutura desta plan
</success_criteria>

<output>
Após completar, criar `.planning/phases/09-ficha-conclusao-comercial-scores/09-02-SUMMARY.md`.
Incluir no SUMMARY: confirmação da ordem final da ficha (com números de linha), lista exata dos 3 botões de `#dActsPrim` e do conteúdo de `#dActsMore`, e o contrato exato de `atualizarScores(a)` (assinatura + o que a Wave 3 precisa implementar dentro dela) para a Wave 3 consumir sem reabrir esta plan.
</output>
