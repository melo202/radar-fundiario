---
phase: 03-render-de-bairro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - radar-goiania.html
  - sw.js
autonomous: true
requirements: [MAPA-02, MAPA-03]
user_setup: []

must_haves:
  truths:
    - "Ao abrir o app em zoom de cidade, os bairros aparecem como polígonos-linha (outline) sobre Goiânia, carregados do arquivo estático bairros-goiania.json"
    - "No desktop, passar o mouse sobre um bairro realça o polígono (stroke accent) e mostra o nm_bai num tooltip"
    - "No mobile, o 1º toque num bairro realça + mostra o nome; o 2º toque no mesmo bairro dá drill (fitBounds)"
    - "Uma ÚNICA função highlightBairro() faz o realce, gated por matchMedia('(hover:none)') — não dois code paths"
    - "Clicar/tocar-drill num bairro dá fitBounds nos limites do polígono; ao passar de zoom 17 os lotes aparecem via o refreshLots() já existente (câmera chegando por clique, não só pinch)"
    - "Os contornos de bairro somem em zoom≥17 (dão lugar aos lotes) e reaparecem ao voltar para zoom baixo"
    - "O PWA serve bairros-goiania.json offline e não serve versão velha (precache + bump de versão do cache no sw.js)"
  artifacts:
    - path: "radar-goiania.html"
      provides: "BAI_STYLE/BAI_HOVER, pane 'bairros', loadBairroPolys(), highlightBairro(), drill handler, gating no zoomend"
      contains: "bairros-goiania.json"
    - path: "sw.js"
      provides: "precache de bairros-goiania.json + bump de CACHE para radar-v4"
      contains: "bairros-goiania.json"
  key_links:
    - from: "loadBairroPolys()"
      to: "bairros-goiania.json"
      via: "fetch estático → L.geoJSON com preferCanvas"
      pattern: "fetch\\(['\"]\\./?bairros-goiania\\.json"
    - from: "drill handler (click)"
      to: "refreshLots() existente"
      via: "fitBounds move a câmera → moveend/zoomend já wired → refreshLots dispara em zoom≥17"
      pattern: "fitBounds"
    - from: "zoomend listener"
      to: "camada de bairros"
      via: "esconde/mostra a camada conforme zoom cruza 17"
      pattern: "getZoom\\(\\)"
---

<objective>
Renderizar os bairros de Goiânia como polígonos-linha na home a partir do arquivo estático `bairros-goiania.json` (artefato da Fase 1, já WGS84), com realce + nome no hover(desktop)/1º-toque(mobile) via uma única função gated por toque, e drill por clique/2º-toque que faz `fitBounds` no bairro — deixando o `refreshLots()` já existente revelar os lotes ao passar de zoom 17. Os contornos de bairro somem em zoom≥17. O `sw.js` passa a precachear o JSON e sobe a versão do cache.

Purpose: Entrega a base visual e interativa do mapa-first (MAPA-02) e o disclosure progressivo até os lotes (MAPA-03), espelhando fielmente o padrão de `refreshLots()` para consistência e sem tocar o endpoint frágil no load.

Output: Camada de bairros renderizada + interativa dentro de `radar-goiania.html`; `sw.js` atualizado para servir o asset offline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-render-de-bairro/03-CONTEXT.md
@.planning/phases/03-render-de-bairro/03-UI-SPEC.md
@.planning/phases/03-render-de-bairro/03-VALIDATION.md

<interfaces>
<!-- Contratos extraídos do codebase. O executor usa direto — sem explorar. -->

Arquivo estático `bairros-goiania.json` (raiz do repo, artefato Fase 1, ~1206 features, ~717KB):
FeatureCollection GeoJSON, WGS84, coordenadas [lon,lat]. Cada feature:
  { "type":"Feature",
    "geometry":{"type":"Polygon","coordinates":[[[lon,lat],...]]},
    "properties":{ "id":"000400000603", "nm_bai":"Campos Dourados" } }
- `nm_bai` pode ser null (466 glebas rurais) ou " " (espaço) — tratar ambos como "sem nome".
- É GeoJSON nativo → usar `L.geoJSON(...)` DIRETO. NÃO usar toWGS() (esse é só p/ geometria crua do ArcGIS/lotes).

radar-goiania.html — padrão dos lotes a ESPELHAR (linhas 663-731):
```js
const LOT_STYLE={pane:"lots",color:"#2c5545",weight:1,fillColor:"#2c5545",fillOpacity:.05,bubblingMouseEvents:false};
const LOT_HOVER={weight:2.5,color:"#b5451f",fillOpacity:.14};
function initLots(){
  map.createPane("lots").style.zIndex=380;
  lotLayer=L.layerGroup();
  map.on("moveend",()=>{clearTimeout(LOTDEB);LOTDEB=setTimeout(refreshLots,250);});
}
// refreshLots(): gate zoom<17 remove a camada; hover→setStyle(LOT_HOVER)+tooltip sticky; click→loadCi(ci)
poly.bindTooltip("carregando…",{sticky:true,direction:"top",opacity:.96});
poly.on("mouseover",()=>{poly.setStyle(LOT_HOVER); ...});
poly.on("mouseout",()=>{...poly.setStyle(LOT_STYLE);});
```

Helpers/idioms existentes (usar, não recriar):
- `map` = L.map("map").setView([-16.6799,-49.255],12)  (init em initMap(), linha 650)
- `esc(v)` (linha 611) — HTML-escape p/ tooltip. NUNCA innerHTML cru com nm_bai.
- `matchMedia("(hover:none)").matches` — detecção de toque (já usado em refreshLots linha 730)
- `dismissCoach()` (linha 1836) — esconde o coach-mark
- boot: `initMap();setView("mapa");initCoach();loadBairros();initCaixa();` (linha 1850) — loadBairros() é a lista de nomes p/ a BUSCA (não confundir); a nova função de polígonos é separada.
- CSS :root tokens (linhas 20-34): --line #c3b9a3, --accent #b5451f, --ink, --paper-2, --muted, --accent-ink.
- CSS `.leaflet-tooltip` já existe (linha 393): font 500 11.5px/1.5 — reusar, NÃO criar classe nova.

sw.js (linhas 6-15):
```js
const CACHE = "radar-v3";
const LOCAL = ["./","./radar-goiania.html","./caixa-goiania.js","./manifest.json","./icon-192.png","./icon-512.png","./apple-touch-icon.png"];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Camada de bairros — render de outlines a partir do JSON estático + gating por zoom</name>
  <read_first>radar-goiania.html (linhas 649-674 initMap/initLots, 691-731 refreshLots, 20-34 :root, 393 .leaflet-tooltip, 1845-1850 boot), .planning/phases/03-render-de-bairro/03-UI-SPEC.md (seções Color, Z-Index, States→bairro layer visibility, Non-Visual Reminders)</read_first>
  <files>radar-goiania.html</files>
  <action>
No bloco de constantes de estilo (logo após LOT_STYLE/LOT_HOVER, ~linha 664), adicionar as constantes de bairro usando SOMENTE var() existentes (zero hex novo), espelhando a gramática de LOT_STYLE/LOT_HOVER:
```
const BAI_STYLE={pane:"bairros",color:"var(--line)",weight:1,fillColor:"var(--line)",fillOpacity:.03,opacity:.8,bubblingMouseEvents:false};
const BAI_HOVER={color:"var(--accent)",weight:2.5,fillOpacity:.08,opacity:1};
```
Manter a resolução de estilo como uma pequena função de propriedades da feature (ex.: `baiStyle(feature){return BAI_STYLE;}`) e usá-la no `style:` do L.geoJSON — NÃO inline um objeto cru — para deixar a extensão v2.1 (styling por território) barata depois (nota de arquitetura da UI-SPEC).

Criar o pane ANTES de adicionar a camada, espelhando initLots() (linha 671): `map.createPane("bairros").style.zIndex=370;` (abaixo do pane "lots" 380 e do overlayPane 400, per UI-SPEC Z-Index). Fazer isso dentro de initMap() (após initLots()) ou no início de loadBairroPolys() — à discrição, mas o pane deve existir antes do L.geoJSON.

Criar a função `async function loadBairroPolys(){...}`:
- `fetch('bairros-goiania.json')` (caminho relativo, exatamente esse nome). Em falha de fetch/parse: falhar SILENCIOSAMENTE (per UI-SPEC Error state) — `catch(e){console.warn(...)}`, sem toast ao usuário, sem quebrar o mapa. NÃO fazer fetch ao vivo ao ArcGIS (proibido).
- criar a camada com `preferCanvas` para performance com ~1206 polígonos:
  `bairroLayer=L.geoJSON(data,{renderer:L.canvas({pane:"bairros"}),pane:"bairros",style:baiStyle,onEachFeature:onEachBairro});`
  (usar L.canvas como renderer é o equivalente por-camada de preferCanvas — garante batching Canvas mesmo com o SVG default do map; declarar `preferCanvas` também é aceitável, mas o renderer explícito por-pane é o alvo).
- guardar a camada numa var de módulo `let bairroLayer=null;` (declarar junto de lotLayer, ~linha 665) e adicioná-la ao map SÓ se o zoom atual for < 17 (respeitando o gate desde o load): checar `if(map.getZoom()<17)bairroLayer.addTo(map);`.

`onEachBairro(feature,layer)` prepara cada polígono: bind do tooltip sticky (será preenchido no highlight — Task 2) e será o ponto onde a Task 2 anexa os handlers. Nesta task, no mínimo, bindar o tooltip vazio no padrão dos lotes: `layer.bindTooltip("",{sticky:true,direction:"top",opacity:.96});`

Gating por zoom (a camada some em zoom≥17, reaparece em <17): adicionar um listener no `zoomend` (pode ser sibling do listener de moveend do initLots, ou dentro de initLots) que, ao cruzar o limiar, faz:
```
map.on("zoomend",()=>{ if(!bairroLayer)return;
  const z=map.getZoom();
  if(z>=17){ if(map.hasLayer(bairroLayer))map.removeLayer(bairroLayer); }
  else { if(!map.hasLayer(bairroLayer))bairroLayer.addTo(map); } });
```
Usar `removeLayer`/`addTo` (detach real de interatividade), NÃO apenas opacity/display (fade é fora de escopo — Fase 6). Limiar EXATO = 17 (mesmo número de entrada dos lotes em refreshLots linha 695; sem banda de sobreposição).

Chamar `loadBairroPolys()` no boot: adicionar a chamada na linha 1850, logo após `loadBairros();` (que é a lista de nomes da busca — NÃO renomear nem tocar): `...initCoach();loadBairros();loadBairroPolys();initCaixa();`.

NÃO adicionar nenhum `transition:` CSS novo. NÃO tocar refreshLots(), LOT_STYLE, initLots (além de opcionalmente criar o pane) nem loadBairros().
  </action>
  <verify>
    <automated>grep -nE "bairros-goiania\.json|createPane\(\"bairros\"\)|BAI_STYLE|BAI_HOVER|L\.canvas|loadBairroPolys" "radar-goiania.html"</automated>
  </verify>
  <acceptance_criteria>
    - grep confirma `fetch('bairros-goiania.json')` presente (nome exato do arquivo)
    - grep confirma `createPane("bairros")` com zIndex 370 e `BAI_STYLE`/`BAI_HOVER` definidos com var() (zero hex novo: `grep -n "b5451f\|c3b9a3" radar-goiania.html` NÃO retorna novas linhas dentro de BAI_STYLE/BAI_HOVER)
    - grep confirma renderer Canvas (`L.canvas` ou `preferCanvas`) na criação da camada de bairros
    - grep confirma `loadBairroPolys()` chamado no boot (linha ~1850) após `loadBairros()`
    - grep confirma listener `zoomend` que compara `getZoom()` com `17` e faz removeLayer/addTo em bairroLayer
    - Preview: ao carregar em zoom 12, os outlines dos bairros aparecem sobre Goiânia; ao dar zoom≥17 eles somem; zero erro no console
  </acceptance_criteria>
  <done>Bairros renderizam como outlines (linha) no zoom de cidade a partir do JSON estático via Canvas; somem em zoom≥17 e voltam em zoom<17; fetch falha silenciosamente sem quebrar o mapa.</done>
</task>

<task type="auto">
  <name>Task 2: highlightBairro() único gated por toque + tooltip com nm_bai + drill por clique/2º-toque</name>
  <read_first>radar-goiania.html (linhas 691-731 padrão hover/tap/tooltip dos lotes, 611 esc(), 824-863 mostrarBairro/enquadra p/ o idioma de fitBounds+invalidateSize, 730 uso de matchMedia), .planning/phases/03-render-de-bairro/03-UI-SPEC.md (seções States→Hover/tap highlight, Copywriting, Interaction Notes)</read_first>
  <files>radar-goiania.html</files>
  <action>
Implementar UMA única função `highlightBairro(layer)` gated por `matchMedia("(hover:none)")` (NÃO dois code paths — mesmo princípio de refreshLots). Manter uma var de módulo do bairro atualmente realçado: `let baiHi=null;`.

`highlightBairro(layer)`:
- se houver `baiHi` diferente de `layer`, resetar o anterior: `baiHi.setStyle(BAI_STYLE); baiHi.closeTooltip();`
- aplicar `layer.setStyle(BAI_HOVER);`
- preencher o tooltip com o nome escapado e abri-lo: derivar o rótulo com fallback — `const nm=layer.feature.properties.nm_bai; const lbl=(nm&&nm.trim())?esc(nm):"Área rural / gleba";` (copy EXATA da UI-SPEC p/ glebas sem nome); `layer.setTooltipContent(lbl); layer.openTooltip();`
- `baiHi=layer;`

Função auxiliar de reset (usada no mouseout desktop e ao limpar): `function clearBaiHi(){ if(baiHi){baiHi.setStyle(BAI_STYLE);baiHi.closeTooltip();baiHi=null;} }` — exportar/deixar acessível pois a Task do breadcrumb (Plano 02) e o zoom-out vão chamá-la.

Função de drill: `function drillBairro(layer){ dismissCoach(); const b=layer.getBounds(); setTimeout(()=>{map.invalidateSize();map.fitBounds(b.pad(0.05));},90); }` — espelha o idioma `enquadra()` da linha 844-845 (invalidateSize antes do fitBounds; pad 0.05 igual a mostrarBairro linha 850). O fitBounds move a câmera → o `moveend`/`zoomend` JÁ existente dispara refreshLots(), que revela os lotes ao chegar em zoom≥17. NÃO chamar refreshLots() manualmente nem forçar zoom 17 (bairros grandes não cabem — deixar o fitBounds enquadrar o bairro inteiro; disclosure progressivo). NÃO passar `{animate:false}` (regride o feel) nem easing custom (Fase 6).

Anexar os handlers em `onEachBairro(feature,layer)` (da Task 1), com o branch de toque DENTRO da mesma função:
```
const touch=matchMedia("(hover:none)").matches;
if(touch){
  // mobile: 1º toque realça+nome; 2º toque no MESMO bairro dá drill
  layer.on("click",()=>{ if(baiHi===layer){drillBairro(layer);} else {highlightBairro(layer);} });
}else{
  // desktop: hover realça+nome, mouseout reseta; clique drila direto
  layer.on("mouseover",()=>highlightBairro(layer));
  layer.on("mouseout",()=>{ if(baiHi===layer)clearBaiHi(); });
  layer.on("click",()=>drillBairro(layer));
}
```
(Um único `highlightBairro`/`drillBairro`; a diferença desktop×mobile é só QUAL evento os chama e o gate "já realçado?" no 2º toque — exatamente o contrato de função única da UI-SPEC/CONTEXT.)

Tooltip: reusar o binding sticky da Task 1 e a classe global `.leaflet-tooltip` (linha 393) — NÃO criar CSS de tooltip novo. O conteúdo é sempre passado por `esc()` — nunca innerHTML cru (defesa contra injeção via nm_bai; ver threat_model).

NÃO introduzir `transition:` novo. NÃO duplicar lógica em dois caminhos separados.
  </action>
  <verify>
    <automated>grep -nE "function highlightBairro|function drillBairro|function clearBaiHi|matchMedia\(\"\(hover:none\)\"\)|esc\(nm\)|Área rural / gleba|fitBounds\(b\.pad" "radar-goiania.html"</automated>
  </verify>
  <acceptance_criteria>
    - grep confirma UMA `function highlightBairro` (não duas variantes desktop/mobile) e o gate `matchMedia("(hover:none)")` no onEachBairro
    - grep confirma o fallback exato `"Área rural / gleba"` e que o nome vai por `esc(...)` antes do tooltip (sem innerHTML cru)
    - grep confirma `drillBairro` usa `invalidateSize` + `fitBounds(...pad(0.05))` e NÃO contém `animate:false` nem chama `refreshLots(` diretamente
    - Preview desktop: hover num bairro → stroke fica accent + tooltip com nm_bai; mouseout volta ao normal; clicar → mapa faz fitBounds no bairro; ao aproximar (zoom≥17) os lotes aparecem
    - Preview mobile (DevTools touch): 1º tap realça+nome; 2º tap no mesmo bairro drila; tap em outro bairro move o realce
    - Bairro sem nome (gleba): tooltip/realce mostram "Área rural / gleba", mesmo tratamento visual
  </acceptance_criteria>
  <done>Uma única função realça bairro + mostra nm_bai (esc), gated por toque; desktop hover/click, mobile 1º-tap-realça/2º-tap-drila; drill faz fitBounds e os lotes aparecem via o refreshLots existente em zoom≥17.</done>
</task>

<task type="auto">
  <name>Task 3: sw.js — precache de bairros-goiania.json + bump da versão de cache</name>
  <read_first>sw.js (todo o arquivo, 55 linhas — CACHE e LOCAL nas linhas 6-15), .planning/phases/03-render-de-bairro/03-UI-SPEC.md (Non-Visual Implementation Reminders)</read_first>
  <files>sw.js</files>
  <action>
No MESMO commit (senão o PWA não serve o asset offline ou serve versão velha indefinidamente):
1. Adicionar `"./bairros-goiania.json"` ao array `LOCAL` (linhas 7-15). Pode ser adicionado após `"./caixa-goiania.js"`.
2. Bumpar a versão do cache: `const CACHE = "radar-v3";` → `const CACHE = "radar-v4";` (linha 6). O handler de `activate` já apaga caches com chave diferente da atual (linhas 30-36), então o bump força a limpeza do cache velho e o re-precache incluindo o novo asset.

NÃO alterar a estratégia (o JSON estático cai no ramo cache-first do fetch handler, correto p/ um asset versionado). NÃO adicionar o JSON ao NETWORK_FIRST (ele é imutável por versão de deploy; cache-first + bump é o padrão certo, igual aos ícones). NÃO tocar o array CDN.
  </action>
  <verify>
    <automated>grep -nE "radar-v4|bairros-goiania\.json" "sw.js"</automated>
  </verify>
  <acceptance_criteria>
    - grep confirma `radar-v4` (versão bumpada) e que `radar-v3` não é mais a versão ativa
    - grep confirma `"./bairros-goiania.json"` dentro do array LOCAL
    - Preview: recarregar com SW ativo → DevTools > Application > Cache Storage mostra `radar-v4` contendo bairros-goiania.json; offline, os bairros ainda renderizam
  </acceptance_criteria>
  <done>bairros-goiania.json está no precache do service worker e a versão do cache subiu para radar-v4 no mesmo commit; o asset é servido offline e não fica preso numa cópia velha.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| arquivo estático `bairros-goiania.json` → DOM (tooltip/breadcrumb) | `nm_bai` (geometria pública, sem PII) é renderizado como texto de UI |
| ArcGIS ao vivo → mapa (lotes) | inalterado nesta fase — refreshLots() já existente, mesma superfície de sempre |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering/XSS | tooltip do bairro (nm_bai) | mitigate | `nm_bai` SEMPRE passa por `esc()` (linha 611) antes de virar conteúdo do tooltip; nunca innerHTML cru. Enforçado no acceptance_criteria da Task 2 (grep por `esc(nm)`). |
| T-03-02 | Information Disclosure | dataset de bairros | accept | Apenas geometria administrativa pública (id + nm_bai). Sem PII, sem `dtnascimen`, sem segredos. Baixo valor. |
| T-03-03 | Denial of Service | render de ~1206 polígonos no mobile | mitigate | `preferCanvas`/`L.canvas` (batching) + gate de zoom que remove a camada em zoom≥17; sem always-on labels. Validação de não-freeze no Plano 02. |

**Sem ameaças de severidade alta nesta fase:** superfície é render client-side de um GeoJSON público versionado no repo + as consultas de lote já existentes (inalteradas). Nenhum endpoint externo novo, nenhum segredo.
</threat_model>

<verification>
- grep: `bairros-goiania.json`, `createPane("bairros")` (z 370), `BAI_STYLE`/`BAI_HOVER` (só var()), `L.canvas`/`preferCanvas`, `highlightBairro` único, `matchMedia("(hover:none)")`, gate `getZoom()>=17`, `radar-v4` no sw.js
- Preview (`python -m http.server`): bairros renderizam em zoom de cidade; hover(desktop)/tap(mobile) realça + nm_bai; clique drila (fitBounds) e lotes aparecem em zoom≥17; bairros somem ≥17; zero erro no console; busca/lotes existentes não regridem
- Zero hex novo introduzido (todas as cores via var())
- Nenhum `transition:` CSS novo (motion = Fase 6)
</verification>

<success_criteria>
- MAPA-02: bairros aparecem como polígonos-linha na home; hover/tap (função única gated por toque) realça + mostra nm_bai
- MAPA-03: clique/toque-drill dá fitBounds no bairro; o gate zoom≥17 + envelope do refreshLots() existente revela os lotes quando a câmera chega via clique; bairros somem ≥17
- sw.js precacheia bairros-goiania.json e sobe a versão do cache no mesmo commit
- Honra 03-UI-SPEC.md: var names exatos, pane 370, zero hex novo, tooltip reusa .leaflet-tooltip, sem transition novo, glebas rotuladas "Área rural / gleba"
</success_criteria>

<output>
Após completar, criar `.planning/phases/03-render-de-bairro/03-01-SUMMARY.md`
</output>
