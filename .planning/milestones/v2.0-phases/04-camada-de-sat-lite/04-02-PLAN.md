---
phase: 04-camada-de-sat-lite
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [radar-goiania.html, sw.js]
autonomous: true
requirements: [SAT-02]
must_haves:
  truths:
    - "A troca ruas ⇄ satélite usa crossfade suave de opacidade (~250ms), não uma troca seca de tile"
    - "O crossfade é escopado à camada de satélite (+ reference em lockstep) — NÃO é motion do app todo (Fase 6)"
    - "Os outlines de bairro/lote NÃO transicionam (snap instantâneo) — só as tiles fazem crossfade"
    - "server.arcgisonline.com cai na lógica de rede do sw.js (network-only, nunca cache-first / nunca precache) — não incha o storage do PWA"
    - "A versão de cache do sw.js foi bumpada de radar-v4 para radar-v5 no mesmo commit"
  artifacts:
    - path: "sw.js"
      provides: "CACHE=radar-v5; tiles Esri seguem network-only via a lógica existente de não-same-origin/não-cdnjs"
      contains: "radar-v5"
  key_links:
    - from: "setSatelite()/toggleSatelite() (do Plan 01)"
      to: "opacidade da tile-pane do satélite"
      via: "transição CSS ~250ms ease escopada"
      pattern: "0\\.?25s|250ms|transition:\\s*opacity"
    - from: "sw.js fetch handler"
      to: "server.arcgisonline.com"
      via: "return early (!sameOrigin && !cdn) — rede pura, sem cache"
      pattern: "radar-v5"
---

<objective>
Entregar o SAT-02 (crossfade suave na troca ruas⇄satélite, escopado às tiles) e fechar o requisito PWA da fase: garantir que os tiles Esri sejam network-only no sw.js e bumpar a versão de cache para radar-v5 no mesmo commit.

Purpose: A troca de camada não pode ser um corte seco de imagem (SAT-02); e os tiles de satélite (pesados) não podem inchar o storage do PWA.
Output: radar-goiania.html com crossfade de opacidade escopado; sw.js com radar-v5 e tiles Esri confirmados como rede-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-camada-de-sat-lite/04-CONTEXT.md
@.planning/phases/04-camada-de-sat-lite/04-UI-SPEC.md
@.planning/phases/04-camada-de-sat-lite/04-01-SUMMARY.md

<interfaces>
<!-- Contratos criados pelo Plan 01 (satTile, satRef, streetTile, setSatelite) e o sw.js atual. -->

Do Plan 01 (radar-goiania.html) — o handler onde o crossfade se pluga:
```js
let satelliteOn=false;
// satTile, satRef, streetTile: L.tileLayer instances criadas em initMap()
function setSatelite(on){
  satelliteOn=on;
  // ...flip aria-pressed/glyph/label...
  if(on){ satTile.addTo(map); satRef.addTo(map); map.removeLayer(streetTile); }
  else  { streetTile.addTo(map); map.removeLayer(satTile); map.removeLayer(satRef); }
  // ...setStyle + localStorage radar_sat...
}
```
Leaflet 1.9.4: `L.tileLayer` suporta a option nativa `opacity` e `layer.setOpacity(n)`; cada tile-layer tem `_container` (uma div com `.leaflet-tile-container` dentro do pane). O pane do satref foi criado com zIndex 350.

sw.js ATUAL (arquivo inteiro é curto):
```js
const CACHE = "radar-v4";                       // <- BUMPAR para radar-v5
const LOCAL = [ "./", "./radar-goiania.html", ... ];   // precache same-origin
const CDN   = [ ...cdnjs leaflet/proj4... ];    // precache cdnjs
const NETWORK_FIRST = /(\/$|\.html$|caixa-goiania\.js$|manifest\.json$)/;
// fetch handler:
const sameOrigin = url.origin === location.origin;
const cdn = url.hostname === "cdnjs.cloudflare.com";
if (!sameOrigin && !cdn) return; // consultas e tiles: sempre rede, sem cache
```
NOTA: `server.arcgisonline.com` NÃO é same-origin nem cdnjs → já cai no `return` early (rede pura, sem cache). Confirmado no UI-SPEC: nenhuma lógica nova de sw é necessária — só o bump de versão. NÃO adicionar arcgisonline a LOCAL/CDN (isso o precacharia — proibido).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crossfade de opacidade (~250ms) escopado à troca de tiles do satélite</name>
  <read_first>radar-goiania.html (setSatelite/toggleSatelite do Plan 01; createPane "satref"); .planning/phases/04-camada-de-sat-lite/04-UI-SPEC.md (Crossfade Contract SAT-02); .planning/phases/04-camada-de-sat-lite/04-01-SUMMARY.md</read_first>
  <files>radar-goiania.html</files>
  <action>
Implementar um crossfade de opacidade de 250ms `ease` (CSS default, SEM cubic-bezier custom) escopado EXCLUSIVAMENTE às tiles do satélite/reference. NÃO transicionar os outlines de bairro/lote (eles fazem snap instantâneo — mudança de weight/fillOpacity em vetor é barata) e NÃO tocar nenhuma outra transição do app (sheet, cards, view-switch = Fase 6).

Abordagem (escolha do executor entre as duas — ambas aceitas pelo UI-SPEC):

Opção A — setOpacity + rAF ramp:
- Ao ligar satélite: `satTile.setOpacity(0); satRef.setOpacity(0);` adicionar ao mapa, forçar reflow / próximo frame (`requestAnimationFrame`), então `satTile.setOpacity(1); satRef.setOpacity(1);`. Aplicar uma regra CSS `.leaflet-tile{transition:opacity .25s ease}` limitada aos panes das tiles (ou usar a transição nativa que o Leaflet já aplica em `.leaflet-tile`). Remover a CARTO só após o fade-in completar (setTimeout 250ms) OU fazer verdadeiro crossfade (ambas presentes por ~250ms).
- Ao desligar: reverso — fade das tiles Esri para opacity 0 em 250ms, então removê-las e re-mostrar a CARTO.

Opção B — classe CSS + transition no _container:
- Regra: `.leaflet-tile-container` do pane satélite/satref com `transition:opacity .25s ease`; togglar uma classe (ex.: `.sat-in`) que anima `opacity` de 0→1 / 1→0 antes de add/remove.

Requisitos fixos:
- Duração: 250ms (midpoint da faixa 200-300ms do CONTEXT.md). Easing: `ease`.
- O reference overlay (satRef) faz fade em lockstep com o satélite (mesma duração, mesmo trigger) — nunca satélite sem rótulos.
- A transição de opacidade fica CONTIDA às tiles (não vira token CSS compartilhado — isso é da Fase 6).
- Integrar no `setSatelite(on)` já existente (do Plan 01) sem duplicar a lógica de add/remove/aria/localStorage — só envolver o swap de camadas com o fade.
  </action>
  <verify>
    <automated>grep -qE "opacity[^;]*(\.25s|250ms)|(\.25s|250ms)[^;]*opacity|setOpacity" "radar-goiania.html" && echo OK</automated>
  </verify>
  <acceptance_criteria>
- `grep -E "\.25s|250ms"` (ou `setOpacity`) confirma a mecânica de crossfade de 250ms nas tiles.
- `grep "transition"` associado a opacidade das tiles NÃO aparece aplicado a `.detail`/`.panel`/cards (escopo só satélite).
- Preview: alternar ruas⇄satélite mostra um fade suave (não corte seco); o reference overlay some/aparece junto com a imagem; os outlines de bairro/lote NÃO transicionam (snap).
- Nenhum token de transição compartilhado app-wide foi criado (sem vazamento para Fase 6).
  </acceptance_criteria>
  <done>Crossfade de 250ms ease escopado às tiles satélite+reference (lockstep); outlines em snap; sem app-wide motion; plugado no setSatelite existente.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bump sw.js radar-v4 → radar-v5 e confirmar tiles Esri como network-only</name>
  <read_first>sw.js (const CACHE, LOCAL, CDN, fetch handler `if (!sameOrigin && !cdn) return`); .planning/phases/04-camada-de-sat-lite/04-CONTEXT.md (seção Service worker)</read_first>
  <files>sw.js</files>
  <action>
1. Bumpar a versão de cache: alterar `const CACHE = "radar-v4";` para `const CACHE = "radar-v5";`. Isto invalida o cache antigo no `activate` (a lógica de `caches.keys().filter(k=>k!==CACHE).map(delete)` já faz a limpeza).

2. Confirmar (NÃO alterar) que os tiles Esri são network-only:
   - `server.arcgisonline.com` NÃO é same-origin nem `cdnjs.cloudflare.com`, portanto já cai no `if (!sameOrigin && !cdn) return;` → rede pura, sem cache. Correto por construção.
   - NÃO adicionar `server.arcgisonline.com` a `LOCAL` nem a `CDN` (isso o precacharia / cache-first — PROIBIDO pelo CONTEXT.md: tiles de satélite são pesados e inchariam o storage do PWA).
   - NÃO adicionar arcgisonline ao regex `NETWORK_FIRST` (esse regex é só para same-origin navigate/app; tiles de terceiro nem chegam nele por causa do return early).

3. Atualizar o comentário de topo do sw.js (linhas 1-5) se necessário para mencionar que os tiles de satélite Esri, como os do CARTO, são sempre rede (nunca passam pelo cache).
  </action>
  <verify>
    <automated>grep -q 'CACHE = "radar-v5"' "sw.js" && ! grep -q "arcgisonline" "sw.js" && echo OK</automated>
  </verify>
  <acceptance_criteria>
- `grep 'radar-v5' sw.js` retorna a const CACHE (e `radar-v4` não aparece mais).
- `grep 'arcgisonline' sw.js` retorna VAZIO (tiles Esri NÃO estão em LOCAL/CDN/NETWORK_FIRST — são network-only via o return early existente).
- A lógica `if (!sameOrigin && !cdn) return;` permanece intacta (é o que garante rede-only para os tiles Esri).
- Preview/DevTools: ao ativar satélite, os requests a server.arcgisonline.com aparecem na Network sem serem servidos pelo Cache Storage do SW.
  </acceptance_criteria>
  <done>CACHE=radar-v5; tiles Esri confirmados network-only (fora de LOCAL/CDN/NETWORK_FIRST, caindo no return early); comentário atualizado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service worker → server.arcgisonline.com | tiles de terceiro passam pela rede sem tocar o Cache Storage do PWA |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Denial of Service | storage do PWA inflado por tiles de satélite pesados | mitigate | Tiles Esri são network-only por construção (`!sameOrigin && !cdn` → return early); NÃO entram em LOCAL/CDN (sem precache), NÃO em NETWORK_FIRST. O Cache Storage não cresce com imagens de satélite. |
| T-04-05 | Tampering | cache servindo versão stale após update | mitigate | Bump radar-v4→radar-v5 força o `activate` a deletar caches antigos (`caches.keys().filter(k=>k!==CACHE)`), garantindo que o app novo (com CSP estendido e satélite) substitua o antigo. |

**Sem ameaças de severidade alta.** O crossfade é puramente visual (CSS opacity, sem entrada de dados). Os tiles Esri permanecem network-only (sem persistência), e o bump de versão garante substituição limpa do cache. Nenhum segredo/chave (keyless), nenhuma PII.
</threat_model>

<verification>
- grep: `radar-v5` em sw.js; `arcgisonline` ausente de sw.js; mecânica de crossfade 250ms/setOpacity em radar-goiania.html.
- Preview: fade suave (não corte) na troca; reference overlay em lockstep; outlines em snap; DevTools mostra tiles Esri na rede sem cache; após reload o SW ativo é radar-v5; zero erros no console; fluxos existentes não regridem.
</verification>

<success_criteria>
- Crossfade suave (250ms ease) escopado às tiles satélite+reference; outlines em snap; sem app-wide motion.
- sw.js CACHE=radar-v5; tiles Esri network-only (nunca cache-first / precache).
- SAT-02 coberto; sem vazamento para Fase 6.
</success_criteria>

<output>
After completion, create `.planning/phases/04-camada-de-sat-lite/04-02-SUMMARY.md`
</output>
