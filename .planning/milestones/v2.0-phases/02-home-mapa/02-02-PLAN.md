---
phase: 02-home-mapa
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [radar-goiania.html]
autonomous: true
requirements: [MAPA-01, MAPA-04]

must_haves:
  truths:
    - "No desktop (≥821px), o mapa é full-bleed (ocupa a viewport) — não mais o grid lado-a-lado 390px+1fr"
    - "No desktop, o painel de busca vira um card/overlay escondido por default, aberto pela pill via data-view='busca'"
    - "Esc fecha o overlay de busca no desktop e devolve o foco à pill"
    - "Um coach-mark de 1 linha dispensável aparece sobre o mapa no 1º uso; ao dispensar, some e NÃO reaparece no 2º load (persistido em localStorage 'radar_coach')"
  artifacts:
    - path: "radar-goiania.html"
      provides: "novo @media(min-width:821px) full-bleed + overlay + banner coach-mark + persistência radar_coach"
      contains: "@media(min-width:821px)"
  key_links:
    - from: "@media(min-width:821px) .wrap"
      to: "mapa full-bleed + .panel overlay driven by body[data-view]"
      via: "override do grid base + body[data-view=busca] .panel{display:block}"
      pattern: "min-width:821px"
    - from: "coach-mark dismiss (× ou 1ª interação)"
      to: "localStorage radar_coach + banner removido"
      via: "setItem + hide"
      pattern: "radar_coach"
---

<objective>
Tornar o DESKTOP também mapa-first (mapa full-bleed + busca como card/overlay acionado pela pill, dirigido pelo mesmo `data-view`+`setView()`), e adicionar o COACH-MARK dispensável de 1 linha com persistência em localStorage. Cobre a metade desktop de MAPA-01/MAPA-04 e o affordance de 1º uso de MAPA-01. Depende do Plan 01 (boot no mapa + pill já existem).

Purpose: Estender o mapa-first ao desktop sem duplicar markup (reusa 100% o `.panel`), e guiar o corretor de 1ª vez sem bloquear.
Output: `radar-goiania.html` com novo bloco `@media(min-width:821px)`, fechamento por Esc/×, extensão do `enquadra()` para fechar overlay no desktop, e banner coach-mark + flag `radar_coach`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-home-mapa/02-CONTEXT.md
@.planning/phases/02-home-mapa/02-UI-SPEC.md
@.planning/phases/02-home-mapa/02-VALIDATION.md
@.planning/phases/02-home-mapa/02-01-PLAN.md

<interfaces>
<!-- Contratos existentes verificados em radar-goiania.html + o que o Plan 01 adiciona. -->

Grid desktop ATUAL (linha 62, base NÃO-guardada — é ISTO que precisa ser sobreposto):
```css
.wrap{position:relative;z-index:1;display:grid;grid-template-columns:390px 1fr;height:100vh;height:100dvh}
.panel{background:var(--paper-2);border-right:2px solid var(--ink);display:flex;flex-direction:column;overflow-y:auto}  /* linha 66 */
```

ÚNICO @media(min-width:821px) existente hoje (linha 414) — ESTENDER este bloco, não criar concorrente:
```css
@media(min-width:821px){.detail .grab{display:none}}
```

setView (linhas 869-875, do app; Plan 01 já o usa no boot) — reusar como driver do overlay:
```js
function setView(v){ document.body.dataset.view=v; ... if(v==="mapa"&&map)setTimeout(()=>{map.invalidateSize();refreshLots();},60); }
```

enquadra (linhas 809-810) — auto-troca APENAS no mobile hoje; estender p/ fechar overlay no desktop:
```js
const enquadra=bnds=>{if(isMobile())setView("mapa");
  setTimeout(()=>{map.invalidateSize();map.fitBounds(bnds);},90);};
```

Pill do Plan 01 (dentro de .mapwrap): `<button id="searchPill" ... onclick="setView('busca');focusFirstField()">`.

Precedente de persistência (linhas 1416, 1524) — MESMO register para o flag do coach-mark:
```js
localStorage.getItem("radar_prof"); localStorage.setItem("radar_prof", ...);
```

Boot init (linha ~1801, já editado no Plan 01 p/ `dataset.view="mapa"` + `setView("mapa")`): ponto onde inicializar o coach-mark (após setView('mapa')).

Idioma de card flutuante existente (.detail, linhas 161-165): `border:2px solid var(--ink);box-shadow:0 6px 20px rgba(20,26,31,.22);z-index:500`; `.detail .x{width via mobile 44px; font-size:20px;color:var(--muted)}`.
Tokens :root (20-45): `--paper-2:#f2eee4 --ink:#141a1f --line:#c3b9a3 --muted:#57503f --accent:#b5451f --shadow:0 2px 0 rgba(20,26,31,.08)`.
Regra global de foco (linha 88): botões herdam `outline:2px solid var(--accent)`.
Convenção safe-area (linha 393): `top:calc(16px + env(safe-area-inset-top))`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Desktop mapa-first — novo @media(min-width:821px) (full-bleed + .panel overlay + Esc/×)</name>
  <read_first>radar-goiania.html (linha 62 .wrap base; linha 66 .panel; linha 414 único @media 821px; linha 421 .brand header; linhas 809-810 enquadra; linhas 869-875 setView; linha 88 foco), .planning/phases/02-home-mapa/02-UI-SPEC.md (Component Contract #3 "Map-First Layout — Desktop" — z-index 450, max-width 400px, border 2px --ink, radius 4px, shadow verbatim da .detail, Esc obrigatório)</read_first>
  <files>radar-goiania.html</files>
  <action>
    Anchor A — CSS: ESTENDER o bloco `@media(min-width:821px)` existente (linha 414, hoje só `.detail .grab{display:none}`). Adicionar DENTRO desse MESMO `@media` (não criar um bloco concorrente):
    ```css
    .wrap{display:block}                          /* override do grid 390px 1fr base (a base da linha 62 fica intacta) */
    .mapwrap{position:absolute;inset:0}           /* mapa full-bleed ocupando a viewport */
    .panel{position:fixed;top:24px;left:24px;max-width:400px;max-height:calc(100vh - 48px);
      display:none;border:2px solid var(--ink);border-radius:4px;
      box-shadow:0 6px 20px rgba(20,26,31,.22);z-index:450;overflow-y:auto}
    body[data-view="busca"] .panel{display:flex}  /* pill (setView('busca')) revela o overlay; default escondido */
    .searchpill{top:24px;left:24px;transform:none} /* pill no canto sup-esq (não centraliza); offsetada do .leaflet-control-zoom */
    #pillClose{display:inline-flex}                /* botão × do overlay visível só no desktop */
    ```
    Regras: o overlay reusa 100% o MESMO markup `.panel` (brand + form + results) — ZERO duplicação de DOM. `data-view` continua o ÚNICO state model (a mesma var que o mobile já usa nas linhas 386-387), agora também lido no desktop. z-index:450 (acima da pill 400, abaixo de `.detail` 500). ZERO hex novo — o `box-shadow` rgba reusa o valor VERBATIM da `.detail` (linha 162). ZERO `transition:` (Motion é Phase 6).

    Anchor B — Markup do botão fechar: adicionar um `<button id="pillClose">` no header `.brand` do `.panel` (após `<div class="brand">`, linha ~421), escondido por default (mobile não usa), idioma do `.detail .x`:
    ```html
    <button id="pillClose" class="pillclose" aria-label="Fechar busca" onclick="setView('mapa');document.getElementById('searchPill').focus()" style="display:none">×</button>
    ```
    CSS mínimo da `.pillclose` no bloco base (perto de `.detail .x`, linha 165):
    ```css
    .pillclose{position:absolute;top:8px;right:8px;width:44px;height:44px;border:0;background:transparent;
      font-size:20px;color:var(--muted);cursor:pointer;line-height:1}
    ```
    O `display:inline-flex` no desktop vem da regra `#pillClose` do Anchor A (fica escondido no mobile). Alvo 44×44px por convenção existente.

    Anchor C — Esc obrigatório + foco de volta: adicionar um listener de teclado global perto dos outros listeners no fim do arquivo (~linha 1809):
    ```js
    document.addEventListener("keydown",e=>{
      if(e.key==="Escape"&&document.body.dataset.view==="busca"&&!isMobile()){
        setView("mapa");const p=document.getElementById("searchPill");if(p)p.focus();}
    });
    ```

    Anchor D — enquadra() desktop: na linha 809, trocar `if(isMobile())setView("mapa");` por `setView("mapa");` para fechar o overlay TAMBÉM no desktop pós-resultado (no desktop esconde o `.panel` overlay para o pin/ficha ficarem visíveis; no mobile mantém idêntico). Confirmar que `.detail` (ficha) continua abrindo sobre o mapa em ambos.

    NÃO tocar o bloco `@media(max-width:820px)` (linhas 382-413). NÃO reescrever `setView`. NÃO alterar a base `.wrap` da linha 62 (o override vive dentro do @media).
  </action>
  <acceptance_criteria>
    - grep: `@media(min-width:821px)` presente contendo `.wrap{display:block}`, `.mapwrap{position:absolute;inset:0}` e `body[data-view="busca"] .panel{display:flex}`.
    - grep: `id="pillClose"` presente com `aria-label="Fechar busca"` e onclick que chama `setView('mapa')` e devolve foco à `searchPill`.
    - grep: listener `e.key==="Escape"` presente chamando `setView("mapa")`.
    - grep NEGATIVO: a base `grid-template-columns:390px 1fr` (linha 62) ainda existe (a base não foi apagada, só sobreposta no @media); nenhum hex novo; nenhum `transition:` novo no bloco desktop.
    - Preview desktop (1280×800): mapa full-bleed; pill no canto sup-esq sem cobrir o controle de zoom do Leaflet; clicar na pill abre o card de busca overlay (mesmo markup); Esc e × fecham o overlay e o foco volta à pill; busca com resultado fecha o overlay e mostra o pin/ficha.
  </acceptance_criteria>
  <verify>
    <automated>grep -n '@media(min-width:821px)' radar-goiania.html && grep -n 'body\[data-view="busca"\] .panel' radar-goiania.html && grep -n 'id="pillClose"' radar-goiania.html && grep -n 'e.key==="Escape"' radar-goiania.html && grep -n 'grid-template-columns:390px 1fr' radar-goiania.html</automated>
  </verify>
  <done>Desktop vira mapa-first via novo @media(min-width:821px) (full-bleed + .panel overlay dirigido por data-view), reusando 100% o markup; Esc e × fecham devolvendo foco à pill; enquadra() fecha o overlay pós-resultado; base grid intacta; zero hex/transition novos.</done>
</task>

<task type="auto">
  <name>Task 2: Coach-mark de 1ª vez (banner + dismiss por × / 1ª interação + persistência radar_coach)</name>
  <read_first>radar-goiania.html (linhas 491-521 .mapwrap p/ inserir o banner; linhas 1416/1524 precedente radar_prof; linha ~1801-1805 boot init já editado; linha 88 foco; linhas 869-875 setView), .planning/phases/02-home-mapa/02-UI-SPEC.md (Component Contract #4 "Coach-mark Banner" — surface, radius:2px, border 1px --line, shadow --shadow, 1 linha 13px, dismiss × 44px, localStorage flag, sem aria-live; Copywriting Contract)</read_first>
  <files>radar-goiania.html</files>
  <action>
    Anchor A — Markup: dentro de `.mapwrap` (irmã de `#map` e da pill do Plan 01, linha ~493), adicionar o banner escondido por default (mostrado por JS só no 1º uso). NÃO deve sobrepor a pill: posicionar ABAIXO da pill.
    ```html
    <div id="coachMark" class="coachmark" role="status" style="display:none">
      <span class="coach-tx">Toque num bairro pra explorar, ou use a busca.</span>
      <button class="coach-x" aria-label="Dispensar dica" onclick="dismissCoach()">×</button>
    </div>
    ```
    Copy EXATA "Toque num bairro pra explorar, ou use a busca." (UI-SPEC Copywriting). aria-label EXATO "Dispensar dica". `role="status"` sem `aria-live` (UI-SPEC: default sem live-region). Texto estático — 1 linha a 13px, não pode quebrar em 2 linhas a 375px.

    Anchor B — CSS: adicionar no bloco base do `<style>` (perto de `.searchpill`/`.detail`, linhas 159-165), ZERO hex novo:
    ```css
    .coachmark{position:absolute;z-index:390;top:calc(72px + env(safe-area-inset-top));left:50%;transform:translateX(-50%);
      max-width:calc(100vw - 32px);display:inline-flex;align-items:center;gap:8px;
      padding:12px 16px;background:var(--paper-2);border:1px solid var(--line);border-radius:2px;
      color:var(--muted);font:500 13px/1.4 "IBM Plex Sans";box-shadow:var(--shadow);white-space:nowrap}
    .coach-x{width:44px;height:44px;border:0;background:transparent;font-size:18px;color:var(--muted);cursor:pointer;line-height:1;margin-right:-12px}
    ```
    z-index:390 (abaixo da pill 400). border 1px `--line` (mais leve que a pill — hierarquia intencional). radius:2px (retangular, NÃO pill — reserva a forma pill só para a busca). `box-shadow:var(--shadow)` (elevação plana padrão). `top:calc(72px + ...)` posiciona logo abaixo da pill (que fica a 44px+16px do topo) SEM sobrepor. No desktop o `.searchpill` fica no canto (Task 1); o coach-mark centralizado no topo não colide com a pill sup-esq. ZERO `transition:` (Motion é Phase 6). `:focus-visible` do × herdado da linha 88.

    Anchor C — JS (persistência + triggers): adicionar as funções perto de `closeDetail` (~linha 1798) ou junto ao init:
    ```js
    function dismissCoach(){
      const c=document.getElementById("coachMark"); if(c)c.style.display="none";
      try{localStorage.setItem("radar_coach","1");}catch(e){}
    }
    function initCoach(){
      let seen="1"; try{seen=localStorage.getItem("radar_coach");}catch(e){}
      if(!seen){const c=document.getElementById("coachMark"); if(c)c.style.display="inline-flex";}
    }
    ```
    No boot init (~linha 1805, após `initMap();setView("mapa")` do Plan 01), chamar `initCoach();`.
    Trigger implícito (dismiss na 1ª interação, per CONTEXT "some após a 1ª interação"): chamar `dismissCoach()` também quando o usuário abre a busca — no onclick da pill do Plan 01 (`onclick="setView('busca');focusFirstField()"` na linha ~493) acrescentar `;dismissCoach()`, OU (mais robusto) adicionar `dismissCoach();` no início da `enquadra` (linha 809) para cobrir busca-com-resultado. Implementar AMBOS os triggers de dismiss (× explícito + abrir busca) conforme UI-SPEC #4 Behavior. `dismissCoach()` é idempotente (safe se o banner já sumiu).
  </action>
  <acceptance_criteria>
    - grep: `id="coachMark"` presente; copy EXATA `Toque num bairro pra explorar, ou use a busca.` presente; `aria-label="Dispensar dica"` presente; `role="status"` presente e SEM `aria-live` no coachMark.
    - grep: `.coachmark{` presente com `border-radius:2px`, `border:1px solid var(--line)`, `box-shadow:var(--shadow)`, `font:500 13px/1.4`.
    - grep: `radar_coach` aparece em `localStorage.setItem("radar_coach"` e em `localStorage.getItem("radar_coach"`; função `dismissCoach` e `initCoach` presentes; `initCoach()` chamado no boot; `dismissCoach` amarrado ao abrir busca (onclick da pill ou início do enquadra).
    - grep NEGATIVO: nenhum hex novo na `.coachmark`; nenhum `transition:` na `.coachmark`; `border-radius:999px` NÃO usado no coach-mark (forma pill reservada à busca).
    - Preview mobile 375×812: no 1º load o banner aparece em 1 linha (não quebra) abaixo da pill sem cobri-la; tocar no × esconde; recarregar → banner ausente. Preview: abrir a busca também dispensa o banner.
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'id="coachMark"' radar-goiania.html && grep -F 'Toque num bairro pra explorar, ou use a busca.' radar-goiania.html && grep -n 'localStorage.setItem("radar_coach"' radar-goiania.html && grep -n 'localStorage.getItem("radar_coach"' radar-goiania.html && grep -n 'function initCoach' radar-goiania.html && grep -n 'initCoach()' radar-goiania.html</automated>
  </verify>
  <done>Banner de 1 linha com copy/aria exatos aparece só no 1º uso (flag radar_coach ausente); dismiss por × e por abrir a busca persiste em localStorage; 2º load não mostra o banner; forma retangular (radius 2px, não pill); zero hex/transition/aria-live novos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage (client) | Flag `radar_coach` gravado/lido no navegador do usuário — mesmo tier de confiança do `radar_prof` já existente. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | `localStorage.radar_coach` | accept | Flag booleano de baixo valor ("1"); adulterá-lo só faz o coach-mark reaparecer/sumir — sem impacto em dados oficiais/laudo. Leitura envolvida em try/catch (não lança em modo privado). |
| T-02-04 | Injection (I) | Texto do coach-mark e overlay | accept | Copy é string estática literal no HTML — sem interpolação, sem input de usuário, sem `innerHTML` dinâmico. Zero superfície de injeção. |
| T-02-05 | Information Disclosure | Overlay reusa `.panel` existente | accept | Nenhum campo/dado novo exposto; o overlay mostra o MESMO formulário já público. Sem PII adicional. |

Não há ameaças de severidade alta neste plano. Reestruturação de UI client-side sem nova chamada de rede, sem segredos, sem novos dados. Preservar o padrão existente de `textContent`/`esc()` caso qualquer texto vire dinâmico no futuro (não aplicável — tudo estático aqui).
</threat_model>

<verification>
- grep confirma: novo `@media(min-width:821px)` com `.wrap{display:block}` + overlay dirigido por `data-view`; `#pillClose` + listener Esc; base grid da linha 62 intacta; banner `#coachMark` com copy/aria exatos; `radar_coach` set/get; `initCoach()` no boot; dismiss amarrado a × e a abrir busca.
- Preview desktop (1280×800): mapa full-bleed, pill no canto abre overlay (mesmo markup), Esc/× fecham devolvendo foco, busca-com-resultado fecha o overlay.
- Preview mobile (375×812): coach-mark 1 linha no 1º load abaixo da pill sem cobri-la; × dispensa; reload → ausente; abrir busca também dispensa.
- Sem regressão: `.detail` (ficha) abre normalmente; barra inferior mobile intacta; busca existente funciona pela pill/overlay em ambos os breakpoints.
- Zero hex novo; zero `transition:` novo; forma pill reservada à busca (coach-mark é retangular); escopo Phase 3/4/6 não vazou.
</verification>

<success_criteria>
- MAPA-01 (desktop): app abre no mapa full-bleed enquadrando Goiânia; coach-mark de 1ª vez dispensável e persistente.
- MAPA-04 (desktop): busca é um card/overlay acionado pela pill sempre-visível, reusando 100% o `.panel`, com Esc/× de fechamento acessível.
- Todos os `must_haves.truths` observáveis em preview desktop + mobile.
</success_criteria>

<output>
After completion, create `.planning/phases/02-home-mapa/02-02-SUMMARY.md`
</output>
