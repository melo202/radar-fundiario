---
phase: 02-home-mapa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [radar-goiania.html]
autonomous: true
requirements: [MAPA-01, MAPA-04]

must_haves:
  truths:
    - "Ao abrir o app (mobile), a tela inicial é o mapa enquadrando Goiânia, não a tela de busca"
    - "map.invalidateSize() dispara ao mostrar o mapa no boot (Leaflet não renderiza cinza)"
    - "Uma pill de busca flutuante ('O que você procura?' + 🔍) fica visível sobre o mapa"
    - "Tocar na pill abre o painel de busca completo já existente (setView('busca')) e foca o 1º campo"
  artifacts:
    - path: "radar-goiania.html"
      provides: "boot default data-view=mapa + pill de busca flutuante + CSS da pill"
      contains: "O que você procura?"
  key_links:
    - from: "boot init (linha ~1801)"
      to: "body[data-view=mapa] + setView('mapa')"
      via: "flip do default de boot + chamada única no boot"
      pattern: "dataset\\.view\\s*=\\s*[\"']mapa[\"']"
    - from: "pill <button id=searchPill>"
      to: "setView('busca') + focusFirstField()"
      via: "onclick handler"
      pattern: "searchPill"
---

<objective>
Fazer o app abrir no MAPA (não na busca) e adicionar a PILL de busca flutuante sobre o mapa que reabre o painel de busca 100% existente. Cobre o núcleo mobile de MAPA-01 (home=mapa) e MAPA-04 (busca vira pill sempre acessível). O layout desktop full-bleed e o coach-mark ficam no Plan 02.

Purpose: Rebaixar a busca de "tela inicial" para "affordance a um toque", com o corretor caindo direto no território.
Output: `radar-goiania.html` com boot em `data-view="mapa"`, pill `<button id="searchPill">` + CSS, e wire da pill para `setView('busca')`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-home-mapa/02-CONTEXT.md
@.planning/phases/02-home-mapa/02-UI-SPEC.md
@.planning/phases/02-home-mapa/02-VALIDATION.md

<interfaces>
<!-- Contratos existentes já verificados em radar-goiania.html. O executor usa estes diretamente — não precisa explorar o codebase. -->

Boot init (linha ~1801):
```js
document.body.dataset.view="busca";   // ← FLIP para "mapa"
setMode(MODE);
if(!window.L||!window.proj4){ ... }
else{initMap();loadBairros();initCaixa();}   // ← após initMap(), garantir setView('mapa')
```

setView central (linhas 869-875) — NÃO reescrever, apenas reusar:
```js
const isMobile=()=>window.matchMedia("(max-width:820px)").matches;
function setView(v){
  document.body.dataset.view=v;
  [["vbBusca","busca"],["vbMapa","mapa"]].forEach(([id,x])=>{
    const b=document.getElementById(id);b.classList.toggle("on",v===x);b.setAttribute("aria-pressed",v===x);});
  if(v==="mapa"&&map)setTimeout(()=>{map.invalidateSize();refreshLots();},60);  // ← já cuida do cinza
}
```

focusFirstField (linhas 857-861) — reusar para focar o 1º campo ao abrir busca:
```js
function focusFirstField(){
  const id={addr:"rua",bd:"predio",insc:"insc"}[MODE]||"quadra";
  const el=document.getElementById(id); if(el&&el.offsetParent!==null)el.focus();
}
```

Markup do mapa (linhas 491-521): `<div class="mapwrap"><div id="map"></div> ... </div>` — a pill entra DENTRO de `.mapwrap`, irmã de `#map`.

Barra inferior mobile (linhas 528-531) — PERMANECE inalterada (reforço mobile):
```html
<nav class="viewbar" id="viewbar">
  <button id="vbBusca" class="on" onclick="setView('busca')">🔍 Consulta</button>
  <button id="vbMapa" onclick="setView('mapa')">🗺️ Mapa</button>
</nav>
```

Tokens :root (linhas 20-45) — usar VERBATIM, zero hex novo:
`--paper-2:#f2eee4` `--ink:#141a1f` `--line:#c3b9a3` `--muted:#57503f` `--accent:#b5451f` `--accent-ink:#8f3116` `--shadow:0 2px 0 rgba(20,26,31,.08)`

Regra global de foco (linha 88) — `<button>` herda automaticamente:
`button:focus-visible{outline:2px solid var(--accent);outline-offset:2px}`

Convenção safe-area da toast (linha 393): `top:calc(16px + env(safe-area-inset-top))`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Boot no mapa (flip do default + setView('mapa') no boot)</name>
  <read_first>radar-goiania.html (linhas 1800-1806 boot init; linhas 869-875 setView), .planning/phases/02-home-mapa/02-UI-SPEC.md (Component Contract #2 "Map-First Layout — Mobile")</read_first>
  <files>radar-goiania.html</files>
  <action>
    Anchor: bloco de init na linha ~1801.
    1. Trocar `document.body.dataset.view="busca";` (linha 1801) por `document.body.dataset.view="mapa";`.
    2. No ramo else que já roda `initMap();loadBairros();initCaixa();` (linha 1805), adicionar `setView("mapa");` LOGO APÓS `initMap()` para: (a) sincronizar o estado `.on`/aria-pressed dos botões `#vbBusca`/`#vbMapa` (agora Mapa fica `on`), e (b) disparar o `map.invalidateSize()` que `setView("mapa")` já contém (linha 874) — garantindo que o Leaflet remeça o container e NÃO renderize cinza no boot.
       Resultado final aproximado: `else{initMap();setView("mapa");loadBairros();initCaixa();}`.
    NÃO tocar a função `setView` em si. NÃO tocar o markup `#vbBusca class="on"` (o `setView("mapa")` no boot já move o `.on` para o `#vbMapa` em runtime). NÃO tocar o bloco `@media(max-width:820px)` (linhas 382-413) — as regras `body[data-view="busca"] .mapwrap{display:none}` / `body[data-view="mapa"] .panel{display:none}` já fazem o resto.
  </action>
  <acceptance_criteria>
    - grep em radar-goiania.html: `dataset.view="mapa"` presente; `dataset.view="busca"` NÃO aparece mais no boot init.
    - grep: `initMap();setView("mapa")` (ou `setView("mapa")` na mesma linha logo após `initMap()`) presente.
    - Preview mobile (375×812): ao carregar, o MAPA é a tela visível (não o painel de busca); os tiles do CARTO renderizam (não cinza); o botão inferior 🗺️ Mapa aparece com o estado accent-filled (`.on`).
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'dataset.view="mapa"' radar-goiania.html && grep -c 'dataset.view="busca"' radar-goiania.html | grep -q '^0$' && grep -n 'setView("mapa")' radar-goiania.html</automated>
  </verify>
  <done>Boot flipa para mapa; setView('mapa') roda uma vez no boot disparando invalidateSize; barra inferior mobile intacta; grep confirma strings; preview mobile mostra mapa como landing.</done>
</task>

<task type="auto">
  <name>Task 2: Pill de busca flutuante (markup + CSS + wire para setView('busca'))</name>
  <read_first>radar-goiania.html (linhas 491-521 .mapwrap markup; linhas 159-164 .detail para z-index/idiomas de card; linha 88 focus global; linhas 857-861 focusFirstField), .planning/phases/02-home-mapa/02-UI-SPEC.md (Component Contract #1 "Search Pill" — placement, sizing, states, a11y; Copywriting Contract)</read_first>
  <files>radar-goiania.html</files>
  <action>
    Anchor A — Markup: dentro de `.mapwrap` (após `<div id="map"></div>`, linha ~493, irmã de `#map`), adicionar um `<button>` REAL (não div):
    ```html
    <button id="searchPill" class="searchpill" aria-label="Abrir busca de imóvel" onclick="setView('busca');focusFirstField()">
      <span class="pill-ic" aria-hidden="true">🔍</span><span class="pill-lb">O que você procura?</span>
    </button>
    ```
    Label EXATA "O que você procura?" (locked CONTEXT.md). aria-label EXATO "Abrir busca de imóvel" (UI-SPEC Copywriting). NÃO adicionar `aria-haspopup` (mobile é view-swap, não dialog — o desktop overlay fica no Plan 02). Reusar `focusFirstField()` existente para mover o foco ao 1º campo do modo atual.

    Anchor B — CSS: adicionar regra `.searchpill` no bloco base do `<style>` (perto de `.mapwrap`/`.detail`, linhas 159-165), usando SÓ tokens existentes, ZERO hex novo:
    ```css
    .searchpill{position:absolute;z-index:400;top:calc(16px + env(safe-area-inset-top));left:50%;transform:translateX(-50%);
      display:inline-flex;align-items:center;gap:8px;min-height:44px;max-width:calc(100vw - 32px);
      padding:12px 16px;background:var(--paper-2);border:1.5px solid var(--ink);border-radius:999px;
      color:var(--ink);font:600 15px/1.2 "IBM Plex Sans";cursor:pointer;box-shadow:0 4px 14px rgba(20,26,31,.18)}
    .searchpill .pill-ic{font-size:16px;line-height:1}
    .searchpill:active{border-color:var(--accent)}
    ```
    (Nota: `box-shadow:0 4px 14px rgba(20,26,31,.18)` é o valor prescrito no UI-SPEC para a pill flutuar sobre tiles — mesmo idioma da `.toast`; NÃO é um hex de cor novo, é uma sombra rgba de `--ink`.) O `:focus-visible` (contorno accent) é herdado da regra global da linha 88 — não redeclarar. NÃO adicionar `transition:` (Motion é Phase 6). Posicionamento centralizado é o contrato mobile; o offset desktop (top:24px;left:24px) fica no Plan 02.
  </action>
  <acceptance_criteria>
    - grep: `id="searchPill"` presente; texto `O que você procura?` presente; `aria-label="Abrir busca de imóvel"` presente; `setView('busca')` no onclick da pill; `focusFirstField()` no onclick.
    - grep: `.searchpill{` presente com `border-radius:999px`, `min-height:44px`, `background:var(--paper-2)`, `border:1.5px solid var(--ink)`.
    - grep NEGATIVO: nenhum código hexadecimal novo introduzido na regra `.searchpill` (só var(--...) e rgba de ink já existente); nenhum `transition:` na regra `.searchpill`.
    - Preview mobile: a pill aparece centralizada no topo do mapa, alvo ≥44px, e ao tocar abre o painel de busca existente com o 1º campo focado; Tab até a pill mostra contorno accent (foco visível).
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'id="searchPill"' radar-goiania.html && grep -F 'O que você procura?' radar-goiania.html && grep -F 'aria-label="Abrir busca de imóvel"' radar-goiania.html && grep -n 'border-radius:999px' radar-goiania.html && grep -n 'setView(.busca.);focusFirstField' radar-goiania.html</automated>
  </verify>
  <done>Pill é um `<button>` real com label e aria-label exatos, ≥44px, tokens existentes, sem transition; abre a busca existente e foca o 1º campo; foco de teclado visível herdado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhuma nova) | Reestruturação de UI client-side de app single-file; sem nova chamada de rede, sem segredos, sem dados novos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (I) | Texto estático da pill ("O que você procura?", aria-label) | accept | Strings estáticas literais no HTML — sem interpolação, sem input do usuário, zero superfície de injeção. Nenhum `innerHTML` dinâmico introduzido. |
| T-02-02 | Information Disclosure | Boot/localStorage | accept | Este plano não adiciona persistência (o flag do coach-mark é do Plan 02). Sem PII, sem dados sensíveis. |

Não há ameaças de severidade alta neste plano. A pill/label são strings estáticas — preservar o padrão existente de `textContent`/`esc()` caso qualquer texto vire dinâmico em fases futuras (não aplicável aqui).
</threat_model>

<verification>
- grep confirma: boot em `data-view="mapa"`, `setView("mapa")` no boot, pill com label/aria-label exatos, `border-radius:999px`, wire para `setView('busca')`+`focusFirstField()`.
- Preview mobile (375×812): app abre no mapa (não busca); tiles renderizam (não cinza); pill visível/centralizada ≥44px abre a busca existente e foca o 1º campo; foco de teclado visível.
- Sem regressão: barra inferior 🔍 Consulta / 🗺️ Mapa continua funcionando; a busca existente roda normalmente ao ser aberta pela pill.
- Zero hex novo; zero `transition:` novo; escopo Phase 3/4/6 não vazou (sem polígonos, sem satélite, sem animação).
</verification>

<success_criteria>
- MAPA-01 (mobile): o app abre no mapa enquadrando Goiânia, Leaflet renderiza.
- MAPA-04 (mobile): a busca é uma pill flutuante sempre visível a um toque, reusando 100% o painel existente.
- Todos os `must_haves.truths` observáveis em preview mobile.
</success_criteria>

<output>
After completion, create `.planning/phases/02-home-mapa/02-01-SUMMARY.md`
</output>
