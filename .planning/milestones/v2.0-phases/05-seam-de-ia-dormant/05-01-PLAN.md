---
phase: 05-seam-de-ia-dormant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [radar-goiania.html]
autonomous: true
requirements: [IA-01]

must_haves:
  truths:
    - "Existe um objeto AI_CONFIG com enabled:false (dormant, sem chave embutida)"
    - "Existe uma unica funcao async pesquisarMercadoIA(contexto) que retorna null quando desabilitada ou em erro"
    - "A funcao so aceita um objeto whitelisted (bairro/uso/faixa) — nunca um registro cru — o que estruturalmente impede dtnascimen/PII de chegar a qualquer chamada futura de IA"
    - "O seam vive em um <script> fisicamente separado no fim do body, comentado como dormant"
    - "O nucleo (busca, cadastro, laudo, mapa) NAO chama pesquisarMercadoIA — zero call-sites ativos"
    - "Apagar o bloco <script> do seam por completo nao quebra nenhuma funcionalidade do nucleo"
    - "Nenhuma dependencia de CDN nova e nenhuma mudanca em sw.js"
  artifacts:
    - path: "radar-goiania.html"
      provides: "Bloco <script> dormant do seam de IA (AI_CONFIG + pesquisarMercadoIA) apos o </script> principal (linha ~2098) e antes de </body>"
      contains: "async function pesquisarMercadoIA"
  key_links:
    - from: "seam <script>"
      to: "nenhum call-site no nucleo"
      via: "isolamento deliberado — o seam e inerte"
      pattern: "pesquisarMercadoIA\\("
---

<objective>
Adicionar ao `radar-goiania.html` um encaixe (seam) de IA ISOLADO e DESATIVADO: um objeto `AI_CONFIG {enabled:false}` mais uma unica funcao `async pesquisarMercadoIA(contexto)`, dentro de um `<script>` fisicamente separado no fim do `<body>`. Cobre IA-01.

Purpose: Preparar o ponto de integracao futuro (pesquisa de mercado por IA externa, v2.1+) sem ativar nada agora. O nucleo permanece 100% deterministico e auditavel. A whitelist de input e a barreira estrutural que impede PII (`dtnascimen`) de chegar a qualquer chamada futura de IA. Falha-para-null garante que uma IA futura nunca quebre o app.

Output: Um bloco `<script>` dormant em `radar-goiania.html`, removivel sem quebrar o core. Sem UI, sem rede, sem CDN, sem chave, sem mudanca em `sw.js`.

**NAO fazer nesta fase:** NAO ativar (`enabled` fica `false`), NAO adicionar UI, NAO fazer chamada de rede, NAO adicionar CDN/SDK, NAO tocar `sw.js`, NAO embutir chave, NAO tocar o nucleo cadastral/laudo/estatistica, NAO chamar `pesquisarMercadoIA` de lugar nenhum (IA-02/ativacao = v2.1; Motion = Fase 6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seam-de-ia-dormant/05-CONTEXT.md
@.planning/phases/05-seam-de-ia-dormant/05-VALIDATION.md
@.planning/research/ARCHITECTURE.md
@.planning/research/AI-MODELS.md

<interfaces>
<!-- Fatos verificados diretamente em radar-goiania.html (2100 linhas). O executor implementa a partir daqui — sem exploracao adicional necessaria. -->

Estrutura de scripts do arquivo (single-file, sem build):
- Linha 16-18: <script> de CDN (Leaflet 1.9.4, proj4 2.11.0) + caixa-goiania.js — NAO tocar.
- Linha 626: <script> principal (inicio do nucleo).
- Linha 2098: </script> (fim do nucleo).
- Linha 2099: </body>
- Linha 2100: </html>
Portanto o novo <script> do seam entra ENTRE a linha 2098 (</script>) e a linha 2099 (</body>): um segundo bloco <script> pequeno, deletavel inteiro.

Blocos de sanitizacao ja existentes no nucleo (reusar como REFERENCIA da barreira, NAO importar estado):

  linha 636: const clean=v=>String(v==null?"":v).trim();
  linha 637: const esc=v=>clean(v).replace(/&/g,"&amp;")... (escape HTML)
  linha 639: const SENS=["dtnascimen","cpf","cnpj","nmcontrib","nmproprie","nome"];   // dtnascimen e o primeiro
  linha 640: const sanitiza=arr=>{arr.forEach(a=>SENS.forEach(k=>{if(k in a)delete a[k];}));return arr;};

Nota de design: o seam esta num <script> SEPARADO, entao esc/clean do nucleo (declarados com const no escopo do <script> principal) NAO estao no escopo do seam. Por isso o seam NAO deve depender de esc/clean do nucleo — deve fazer sua propria coercao/whitelist inline (String() + pick explicito das chaves permitidas). Isso reforca a deletabilidade: o seam nao tem dependencia do nucleo, e o nucleo nao tem dependencia do seam.

Confirmado por grep: NAO existe nenhum AI_CONFIG, pesquisarMercadoIA, openrouter ou glm-4.5 no arquivo hoje. Este e codigo novo, greenfield, isolado.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inserir o script dormant do seam (AI_CONFIG + pesquisarMercadoIA)</name>
  <read_first>
    - radar-goiania.html (confirmar linhas 2098 fim-do-script e 2099 fecha-body; confirmar SENS/dtnascimen na linha 639; confirmar que grep de pesquisarMercadoIA/AI_CONFIG ainda retorna vazio antes de editar)
    - .planning/research/ARCHITECTURE.md secao "4. AI seam" (shape exato: script separado, AI_CONFIG, pesquisarMercadoIA, enabled:false, input whitelisted, fail-to-null, sem chave, sem estado mutavel compartilhado)
    - .planning/research/AI-MODELS.md secao "Contrato de API do seam" (modelo default z-ai/glm-4.5-air:online via OpenRouter, contrato OpenAI-compatible Chat Completions, caminho no-backend proxy Worker OU BYO-key)
    - .planning/phases/05-seam-de-ia-dormant/05-CONTEXT.md secao Implementation Decisions (campos concretos de AI_CONFIG; forma do contexto whitelisted)
  </read_first>
  <files>radar-goiania.html</files>
  <action>
Inserir um NOVO bloco `<script>` fisicamente separado ENTRE a linha `</script>` (fim do script principal, ~linha 2098) e `</body>` (~linha 2099). NAO editar nada dentro do `<script>` principal. NAO adicionar `<script src=...>` de CDN. NAO tocar `sw.js`.

O bloco deve conter EXATAMENTE estes elementos (os valores load-bearing abaixo sao obrigatorios; nomes de campos de discricao podem seguir ARCHITECTURE.md secao 4):

1. Comentario-cabecalho de bloco deixando explicito que e o seam de IA dormant e isolado do nucleo, e que ativar = mudar `AI_CONFIG.enabled` + adicionar um call-site opt-in (ainda inexistente em v2.0).

2. `const AI_CONFIG = { ... }` com, no minimo, estes campos e valores:
   - `enabled: false`   (master switch, dormant por default — OBRIGATORIO exatamente assim)
   - `provider: "openrouter"`
   - `model: "z-ai/glm-4.5-air:online"`   (default; fallbacks documentados em comentario: qwen/qwen3-14b:online, deepseek/deepseek-v4-flash:online — ver AI-MODELS.md)
   - `endpoint: ""`   (vazio; NAO ativar endpoint em v2.0)
   - `apiKey: null`   (NUNCA embutir chave; comentario deve dizer que a chave vem em runtime via proxy Cloudflare Worker OU BYO-key em localStorage, nunca no HTML publico)
   - `apiKeyMode: "proxy"`   (nota de design: "proxy" = Worker guarda a key; "byok" = usuario cola a key)
   - `maxTokens: 600`
   - `timeoutMs: 15000`

3. `async function pesquisarMercadoIA(contexto)` com este contrato EXATO:
   - Whitelist-first (barreira estrutural anti-PII): a primeira coisa util e reconstruir um objeto novo APENAS com as chaves permitidas, coeridas para string — nunca usar `contexto` cru adiante. Padrao esperado:
     const ALLOWED = ["bairro","uso","faixaPreco"];
     const c = (contexto && typeof contexto === "object") ? contexto : {};
     const safe = {}; ALLOWED.forEach(k => { if (k in c) safe[k] = String(c[k]); });
     Comentario deve deixar claro: aceita SOMENTE {bairro, uso, faixaPreco} (whitelist), NUNCA um registro cru da API, o que torna estruturalmente impossivel vazar dtnascimen/PII (mesma disciplina do denylist SENS na linha 639 do nucleo).
   - Fail-to-null: `if (!AI_CONFIG.enabled) return null;` logo apos a whitelist (short-circuit antes de qualquer rede). Todo o corpo envolto de forma que QUALQUER erro resolva para null — a funcao nunca lanca excecao para o app (ex.: try { ... } catch { return null; }).
   - Corpo do adapter (OpenAI-compatible Chat Completions) pode ser ESCRITO como esboco mas fica INALCANCAVEL em v2.0 (roda so depois do return null de enabled:false), OU deixado como stub comentado. De qualquer forma: em v2.0 a funcao SEMPRE retorna null. Se escrever o esboco de fetch, ele deve montar o body no formato do AI-MODELS.md (model, messages, plugins:[{id:"web"}], temperature, max_tokens) apontando para AI_CONFIG.endpoint — mas como endpoint:"" e enabled:false, nunca executa. NAO fazer nenhum fetch que rode em v2.0.
   - JSDoc documentando o retorno: Promise de {resumo:string, fonte:string} ou null.

4. O seam NAO le nem escreve nenhuma variavel do nucleo (LAST, sel, LZ, LOTINFO, esc, clean, SENS, etc.). Recebe um valor e devolve um valor. Sem estado compartilhado.

Ao final, grep de "pesquisarMercadoIA(" em radar-goiania.html deve mostrar SOMENTE a linha da definicao (async function pesquisarMercadoIA), nenhuma invocacao.
  </action>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && grep -c "enabled: *false" radar-goiania.html && grep -c "async function pesquisarMercadoIA" radar-goiania.html && grep -c "z-ai/glm-4.5-air:online" radar-goiania.html && grep -c "return null" radar-goiania.html</automated>
  </verify>
  <acceptance_criteria>
    - grep "async function pesquisarMercadoIA" radar-goiania.html => exatamente 1 match (a definicao)
    - grep "AI_CONFIG" => objeto declarado; grep "enabled: *false" => 1+ match
    - grep "z-ai/glm-4.5-air:online" => 1 match (modelo default)
    - grep "return null" => 1+ match (fail-safe); a checagem "!AI_CONFIG.enabled ... return null" esta presente
    - grep -i "apiKey" mostra "apiKey: null"; NENHUMA string parecendo chave real (sem "sk-", sem "Bearer " hardcoded)
    - grep "ALLOWED" ou "bairro" + "uso" + "faixaPreco" => barreira de whitelist presente
    - Negativo: nenhum novo "<script src=" adicionado (grep -c "<script src" continua = 3: Leaflet, proj4, caixa-goiania.js)
  </acceptance_criteria>
  <done>
    O <script> dormant existe entre </script> e </body> com AI_CONFIG{enabled:false, model:"z-ai/glm-4.5-air:online", apiKey:null} e async function pesquisarMercadoIA(contexto) que faz whitelist de input e retorna null (fail-safe). Sem chave, sem CDN novo, sem estado compartilhado com o nucleo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verificar isolamento — zero call-sites, deletavel, boot limpo, sw.js intacto</name>
  <read_first>
    - radar-goiania.html (o bloco do seam recem-criado + o nucleo, para confirmar que nenhuma funcao do core invoca pesquisarMercadoIA)
    - .planning/phases/05-seam-de-ia-dormant/05-VALIDATION.md (estrategia: grep + syntax check + teste "delete o modulo, core funciona")
  </read_first>
  <files>radar-goiania.html</files>
  <action>
Provar a inertness e a deletabilidade do seam. Nenhuma edicao de codigo e esperada; se algo falhar, corrigir DENTRO do bloco do seam da Task 1 — NUNCA adicionando call-site no nucleo.

1. Confirmar ZERO call-sites ativos: "pesquisarMercadoIA(" aparece SOMENTE na definicao. Nenhuma linha do nucleo (busca/cadastro/laudo/mapa, linhas 626-2098) chama a funcao.

2. Confirmar que sw.js NAO foi tocado e nenhum asset novo foi adicionado ao precache (o seam nao introduz asset).

3. Syntax check: extrair o conteudo do bloco <script> do seam para um arquivo .js temporario no scratchpad e rodar `node --check` sobre ele para garantir que a insercao nao quebrou o parse. (Opcionalmente extrair tambem o script principal para confirmar que continua parseavel.)

4. Teste "delete o modulo, o core funciona" (mecanico, em copia): copiar radar-goiania.html para o scratchpad, remover o bloco <script> do seam da copia, e confirmar na copia sem-seam que (a) o parse do restante continua valido, e (b) grep de AI_CONFIG e pesquisarMercadoIA retorna ZERO matches — comprovando que nao ha referencia pendente core->seam. Descartar a copia depois.

5. Registrar no SUMMARY as evidencias de grep (contagens exatas) que provam: enabled:false, 1 definicao / 0 invocacoes, sem chave, sem CDN novo, sw.js intacto, deletavel.
  </action>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && test $(grep -c "pesquisarMercadoIA(" radar-goiania.html) -eq 1 && echo "OK 1-call-site" && test $(grep -c "<script src" radar-goiania.html) -eq 3 && echo "OK no-new-cdn" && git status --porcelain sw.js | grep -q . && echo "SW-CHANGED-FAIL" || echo "OK sw.js untouched"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "pesquisarMercadoIA(" radar-goiania.html == 1 (so a definicao; zero invocacoes no nucleo)
    - grep -c "<script src" radar-goiania.html == 3 (nenhum CDN novo)
    - git status mostra sw.js SEM modificacao (nao aparece como changed)
    - node --check no bloco extraido do seam => sem erro de sintaxe
    - Na copia sem-seam: grep -c "AI_CONFIG\|pesquisarMercadoIA" == 0 (deletabilidade provada, nenhuma dependencia core->seam)
  </acceptance_criteria>
  <done>
    Provado por grep e syntax check: 1 definicao / 0 invocacoes; sem CDN novo; sw.js intacto; o bloco parseia; e remover o seam da copia deixa zero referencias pendentes no nucleo (o "delete o modulo, core funciona" passa). Evidencias registradas no SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nucleo deterministico -> seam de IA | Em v2.0 esta fronteira NAO e cruzada (zero call-sites). No futuro (IA-02), o unico dado que a cruza e um objeto whitelisted {bairro, uso, faixaPreco} construido pelo caller — nunca um registro cru. |
| seam -> provedor de IA externo (OpenRouter/proxy) | NAO existe em v2.0 (endpoint:"", enabled:false). Desenhado para o futuro via proxy Cloudflare Worker (guarda a key) ou BYO-key em localStorage. |
| codigo cliente publico (GitHub Pages) -> segredos | Nenhum segredo cruza: apiKey:null, nenhuma chave embutida. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Segredo/chave em codigo cliente publico | mitigate | `apiKey: null`, `endpoint: ""`, nenhuma chave no HTML. Comentario documenta que a chave vem em runtime (proxy Worker ou BYO-key localStorage), nunca commitada. Verificacao: grep nao encontra "sk-"/"Bearer <token>". |
| T-05-02 | Information Disclosure | PII (dtnascimen/CPF) vazando para uma IA externa | mitigate | Barreira estrutural de whitelist: `pesquisarMercadoIA` reconstroi um objeto novo so com {bairro, uso, faixaPreco} coeridos para string; e impossivel passar um registro cru adiante. Espelha o denylist SENS (linha 639) do nucleo. Em v2.0 nada e enviado (enabled:false). |
| T-05-03 | Denial of Service | Erro/timeout de IA futura derrubando o fluxo cadastral/laudo | mitigate | Fail-to-null: enabled:false faz short-circuit; qualquer erro resolve para null; a funcao nunca lanca para o app. O nucleo permanece 100% deterministico e nao depende do retorno. |
| T-05-04 | Elevation of Privilege / Tampering | Superficie de ataque em runtime do seam em v2.0 | accept | Com `enabled:false` e ZERO call-sites, o seam e codigo morto em runtime: nenhuma rede, nenhum listener, nenhuma execucao. Superficie de ataque ativa = zero. Aceito para v2.0; reavaliar na ativacao (IA-02). |
| T-05-05 | Spoofing / Tampering | Resposta de IA nao confiavel tratada como dado oficial | accept (design nota p/ futuro) | Nao aplicavel em v2.0 (nenhuma resposta e obtida). Documentado para IA-02: rotulo obrigatorio "pesquisa assistida por IA — nao e dado oficial"; o seam nunca alimenta o laudo/estatistica. |

**Block-on: high.** Com `enabled:false` e nenhum call-site, NAO ha ameacas ativas de severidade alta em v2.0 (superficie de ataque em runtime = zero). As mitigacoes estruturais para ativacao futura (sem chave no cliente, barreira de whitelist anti-PII, fail-to-null) ja estao codificadas/documentadas no proprio seam. Nada bloqueia esta fase.
</threat_model>

<verification>
- grep: AI_CONFIG{enabled:false}, `async function pesquisarMercadoIA` (1 definicao), `return null` (fail-safe), whitelist {bairro,uso,faixaPreco}, modelo z-ai/glm-4.5-air:online, apiKey:null (sem chave real).
- grep negativo: `pesquisarMercadoIA(` == 1 (zero invocacoes); `<script src` == 3 (sem CDN novo); sw.js sem modificacao.
- Syntax: o bloco <script> do seam parseia via `node --check`.
- Isolamento: na copia sem-seam, grep de AI_CONFIG/pesquisarMercadoIA == 0 (deletavel, sem dependencia core->seam).
- Inertness: app abre no preview identico ao anterior, zero erros de console, zero chamada de rede a endpoint de IA (verificacao manual em VALIDATION.md).
</verification>

<success_criteria>
- IA-01 coberto: `AI_CONFIG {enabled:false}` + `async pesquisarMercadoIA(contexto)` num `<script>` fisicamente separado.
- A funcao aceita SOMENTE input whitelisted (nunca registro cru => nunca vaza dtnascimen) e falha para `null` (sem lancar excecao ao app).
- ZERO call-sites ativos no nucleo; remover o bloco do seam nao quebra o core ("delete o modulo, core funciona" passa).
- Sem UI, sem chamada de rede, sem CDN novo, sem chave embutida, sem mudanca em sw.js.
- Nenhum vazamento de ativacao (IA-02) ou de Motion (Fase 6).
</success_criteria>

<output>
Apos a conclusao, criar `.planning/phases/05-seam-de-ia-dormant/05-01-SUMMARY.md` com as evidencias de grep (contagens exatas), o resultado do syntax check e do teste de deletabilidade.
</output>
