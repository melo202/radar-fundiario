---
phase: 06-motion-no-app-todo
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [radar-goiania.html]
autonomous: true
requirements: [MOT-02]
must_haves:
  truths:
    - "Switching views (home ⇄ busca ⇄ detalhe via setView) animates with a subtle fade+±12px translateX"
    - "The desktop .panel overlay animates on show/hide with the same fade+small translate treatment"
    - "The mobile .detail bottom-sheet opens/closes with a translateY spring; close can be snappier"
    - "The existing #grab drag-to-dismiss logic still controls the sheet during drag and takes priority over Motion"
    - "Every transition is interruptible — a new setView/open/close never waits for the prior animation"
    - "If Motion is absent OR REDUCE is true, setView/showDetail/closeDetail still change state INSTANTLY (progressive enhancement)"
    - "map.invalidateSize()+refreshLots() timing in setView('mapa') is NOT gated behind animation completion"
  artifacts:
    - path: "radar-goiania.html"
      provides: "Motion-driven setView transitions + .detail sheet spring, all via mAnimate(), interruptible"
      contains: "mAnimate"
  key_links:
    - from: "setView()"
      to: "mAnimate() screen-transition"
      via: "state swap unconditional, animation via wrapper"
      pattern: "function setView"
    - from: "showDetail()/closeDetail()"
      to: "mAnimate() sheet spring"
      via: ".show toggle unconditional, spring via wrapper, drag coordination"
      pattern: "function closeDetail"
---

<objective>
Apply Motion to the two view-level surfaces: (1) screen transitions on `setView()` (mobile single-view swap + desktop `.panel` overlay show/hide) with a subtle fade + ±12px translateX; (2) the mobile `.detail` bottom-sheet open/close as a translateY spring coordinated with the existing `#grab` drag-to-dismiss logic. All via the Plan 01 `mAnimate()` wrapper so the underlying state changes stay unconditional (progressive enhancement) and every transition is interruptible.

Purpose: MOT-02 — perceptible, consistent, interruptible view/sheet motion that never blocks the corretor in the field.
Output: radar-goiania.html where setView and showDetail/closeDetail drive Motion animations, degrading to instant when Motion absent or REDUCE true.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-motion-no-app-todo/06-CONTEXT.md
@.planning/phases/06-motion-no-app-todo/06-UI-SPEC.md
@.planning/phases/06-motion-no-app-todo/06-VALIDATION.md
@.planning/phases/06-motion-no-app-todo/06-01-SUMMARY.md

<interfaces>
<!-- Verified anchors in radar-goiania.html (2026-07-04). Executor uses these directly — no exploration. -->

setView (line 1083) — the state swap is `document.body.dataset.view=v;` plus viewbar aria toggles; and CRITICALLY:
  `if(v==="mapa"&&map)setTimeout(()=>{map.invalidateSize();refreshLots();},60);`  ← this 60ms remeasure MUST stay independent of any animation.

View CSS: mobile (≤820px) `body[data-view="busca"] .mapwrap{display:none}` / `body[data-view="mapa"] .panel{display:none}` (lines 421-422). Desktop (≥821px) `.panel{position:fixed;...}` + `body[data-view="busca"] .panel{display:flex}` (lines 454-457). isMobile() = matchMedia("(max-width:820px)") (line 1082). Viewbar order: busca is LEFT of mapa in .viewbar.

.detail sheet: `.detail{...display... }` `.detail.show{display:block}` (lines 184-187). Mobile breakpoint makes it `position:absolute;bottom:0;border-radius:14px 14px 0 0` (line 438). Desktop .detail is an inline card in .panel (line 450 `.detail .grab{display:none}`).

showDetail (line 1448) — populates the sheet DOM then the caller (pick(), line 1432) does `map.setView(ll,18);showDetail(a,ll)`. Note: showDetail itself does NOT add `.show` in the snippet read — the `.show` class is what CSS keys `display:block` on. Verify at implementation: locate where `.show` is added when opening (search `classList.add("show")` / `classList` on `#detail`) — that add-class site is where the open spring hooks. closeDetail (line 2012): `document.getElementById("detail").classList.remove("show");`

#grab drag logic (lines 2055-2069, IIFE): `let y0=null;` — pointerdown sets y0; pointermove sets `d.style.transform=translateY(dy)`; pointerup: `y0=null; d.style.transform=""; if(dy>70)closeDetail();`. Inline `d.style.transform` during drag has higher specificity than any Motion class/attr animation.

Plan 01 provides: `mAnimate(el, keyframes, options)` → returns Motion controls or null, never throws; `REDUCE` (let); `window.Motion` with `Motion.animate` (WAAPI: re-calling animate() on same element interrupts natively — no manual "is-animating" flag needed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Animate screen transitions in setView() (mobile swap + desktop .panel overlay), interruptible, PE-safe</name>
  <files>radar-goiania.html</files>
  <read_first>radar-goiania.html (setView line 1083; view CSS lines 419-457; isMobile line 1082; pick() line 1432), 06-UI-SPEC.md Section 1 (Screen Transitions table) + Section 6 (Degradation Matrix rows for setView interruption + Motion-absent)</read_first>
  <action>
Wrap the setView view change with a Motion fade+slide, keeping the state swap unconditional.

1. Keep the FIRST line of setView unconditional and untouched: `document.body.dataset.view=v;` plus the viewbar aria toggles run exactly as today — this is the real state change and must happen even if Motion is absent/REDUCE.
2. Keep the Leaflet remeasure EXACTLY as-is: `if(v==="mapa"&&map)setTimeout(()=>{map.invalidateSize();refreshLots();},60);`. Do NOT move it inside an animation callback, do NOT await animation. The map becoming interactive must never wait on the fade.
3. AFTER the state swap, add the incoming-view entrance animation via mAnimate():
   - Determine the incoming element: on mobile (isMobile()), the visible view is `.panel` (busca) or `.mapwrap` (mapa) per the display rules; on desktop the animated element is `.panel` (the overlay that shows for `data-view="busca"` and hides for `mapa`).
   - Direction: viewbar order has busca LEFT of mapa. Going TO busca (from mapa) slides in from the left (start `translateX(-12px)`); going TO mapa (from busca) slides in from the right (start `translateX(12px)`). Use ±12px, never more.
   - Keyframes: `{ opacity:[0,1], transform:["translateX(±12px)","translateX(0)"] }`.
   - Options: `{ duration:0.18, easing:[0.22,1,0.36,1] }` (180ms, the UI-SPEC decelerate curve).
   - Call form: `mAnimate(incomingEl, { opacity:[0,1], transform:[start,"translateX(0)"] }, { duration:0.18, easing:[0.22,1,0.36,1] });` — because mAnimate returns null when Motion is absent/REDUCE, nothing else is needed for the degraded path (the CSS `display` swap already showed the view instantly).
4. Interruptibility: do NOT add any "is animating" flag or queue. Re-calling mAnimate on the same element while a prior animation runs lets Motion (WAAPI) take over from the current value natively. A rapid second setView() must start immediately.
5. Desktop `.panel` overlay: today `body[data-view="busca"] .panel{display:flex}` toggles it. Keep that CSS display rule as the unconditional state; add the same fade+small-translate mAnimate() on the `.panel` element when it becomes visible (data-view==="busca") — this is the overlay entrance. Exit can stay the instant display swap (or optionally animate opacity→0; keep it subtle and non-blocking, and NEVER set pointer-events:none for the duration).
6. Only `transform`/`opacity` may be animated — never top/left/width/height/margin.
  </action>
  <verify>
    <automated>grep -n 'function setView' radar-goiania.html && grep -q 'mAnimate' radar-goiania.html && echo "SETVIEW_WRAPPED"; grep -q 'invalidateSize' radar-goiania.html && echo "REMEASURE_PRESENT"</automated>
  </verify>
  <acceptance_criteria>
- `grep -n "document.body.dataset.view=v"` still present as an UNCONDITIONAL first statement in setView (not inside any `if(Motion)`/`if(!REDUCE)` block).
- The `setTimeout(()=>{map.invalidateSize();refreshLots();},60)` line is byte-unchanged and NOT wrapped in an animation callback.
- A `mAnimate(` call inside setView uses `duration:0.18` (or `180`) and easing `[0.22,1,0.36,1]`, with a `translateX` of ±12px and `opacity:[0,1]`.
- No `pointer-events:none` added for any transition duration; no `top/left/width/height/margin` in any animate keyframes.
- Preview (mobile ≤820px emulation): tapping viewbar busca⇄mapa shows a subtle ~180ms fade/slide; tapping rapidly back-and-forth never "waits out" the prior animation. Map is interactive immediately on switching to mapa.
- Preview (desktop): the .panel overlay fades/slides in when opening busca via the pill; Esc/closing works.
- Preview (reduced-motion emulation): switching views is INSTANT (no transform), app works.
  </acceptance_criteria>
  <done>setView animates incoming view with subtle ±12px fade/slide (180ms, decelerate curve), interruptible, with the state swap + map remeasure fully independent of Motion; degrades to instant under REDUCE/absent Motion.</done>
</task>

<task type="auto">
  <name>Task 2: Bottom-sheet spring on .detail open/close, coordinated with #grab drag, interruptible, PE-safe</name>
  <files>radar-goiania.html</files>
  <read_first>radar-goiania.html (showDetail line 1448 + locate the `.show` add-class open site; closeDetail line 2012; #grab drag IIFE lines 2055-2069; mobile .detail CSS line 438), 06-UI-SPEC.md Section 2 (Bottom-Sheet Spring table) + Section 6 (drag-coordination + rapid open/close rows)</read_first>
  <action>
Add a translateY spring to the MOBILE `.detail` sheet on open, and a snappier close, via mAnimate() — coordinated so the existing drag logic always wins during a drag.

1. OPEN: at the site where the sheet is shown (the `classList.add("show")` on `#detail` that pairs with `.detail.show{display:block}` — locate it near showDetail/pick; if showDetail does not itself add `.show`, find the caller/handler that does and hook there). Keep the class add UNCONDITIONAL (that's the real state: sheet visible + content interactive immediately). Then, mobile only (isMobile()), call:
   `mAnimate(detailEl, { transform:["translateY(100%)","translateY(0)"] }, { type:"spring", stiffness:420, damping:38, mass:1 });`
   Use Motion's spring type with EXACT stiffness 420 / damping 38 / mass 1 (UI-SPEC). Perceptual settle ~220-260ms.
2. CLOSE: in closeDetail(), keep `document.getElementById("detail").classList.remove("show");` UNCONDITIONAL. Mobile only, before/around the class removal, play a snappier close: either the same spring reversed, or a non-spring `mAnimate(detailEl, { transform:["translateY(0)","translateY(100%)"] }, { duration:0.16, easing:[0.4,0,1,1] })` (160ms ease-in — sheets dismissing read better snappy, not bouncy). Ensure the element is actually hidden (display:none via `.show` removal) once the close animation completes OR immediately when Motion is absent — do NOT leave the sheet visually stuck if Motion is missing (when mAnimate returns null, the class removal alone already hides it instantly).
3. DRAG COORDINATION (critical): the existing #grab IIFE sets `d.style.transform=translateY(dy)` inline during drag and resets `d.style.transform=""` on pointerup. Motion MUST NOT animate the sheet while a finger is down. Implement: the spring open/close only runs on the programmatic showDetail/closeDetail path, never mid-drag; gate every spring on `y0==null` (don't start a spring while `y0!=null`).

   **EXPLICIT EDIT to the pointerup handler (line ~2062).** The current handler is:
   ```js
   g.addEventListener("pointerup",e=>{const dy=y0==null?0:e.clientY-y0;y0=null;d.style.transform="";if(dy>70)closeDetail();});
   ```
   The unconditional synchronous `d.style.transform=""` makes the close always start from `translateY(0)`, defeating the "close from dragged position" requirement. CHANGE the handler so the reset only runs on the NOT-dismissed branch, and on dismiss let Motion own the transform from where the finger left it:
   ```js
   g.addEventListener("pointerup",e=>{
     const dy=y0==null?0:e.clientY-y0; y0=null;
     if(dy>70){ closeDetail(); }          // sheet is at translateY(dy); closeDetail()'s mAnimate reads current transform and animates to translateY(100%). Do NOT clear the inline transform here — Motion takes over and clears it on finish (or closeDetail's degraded path removes .show → display:none regardless).
     else { d.style.transform=""; }        // not dismissed: snap back to 0 (open spring already left it there — visual no-op)
   });
   ```
   In closeDetail()'s mobile branch, after the close mAnimate resolves (or synchronously when Motion is absent/REDUCE), clear any leftover inline `d.style.transform` so a future open starts clean. This is the one authorized modification to the otherwise-preserved #grab IIFE. On pointerup with `dy<=70` (not dismissed), the sheet is already at translateY(0) from the open spring, so the reset is a visual no-op. Do NOT let a Motion animation fight the inline drag transform (inline style wins by specificity).
4. Content interactivity: do NOT `await` the open spring before wiring/showing content — the sheet content (comparables button, action links, back-to-list) is in the DOM and clickable the instant `.show` is added. Never set pointer-events:none during the spring.
5. Interruptible: rapid open→close→open (tapping between two cards) must restart from current transform, never queue. Re-calling mAnimate on `#detail` handles this natively; do not add an is-animating flag.
6. DESKTOP `.detail` (inline card in .panel, no sheet affordance): NO spring. Apply the same subtle fade + small translateY (8px) + opacity, 180ms, `[0.22,1,0.36,1]` treatment as screen transitions when the desktop detail card appears.
7. Only transform/opacity animated.
  </action>
  <verify>
    <automated>grep -q 'stiffness' radar-goiania.html && grep -q '420' radar-goiania.html && echo "SPRING_PARAMS_OK"; grep -q 'function closeDetail' radar-goiania.html && grep -q 'classList.remove("show")' radar-goiania.html && echo "CLOSE_STATE_UNCONDITIONAL"; grep -q 'mAnimate' radar-goiania.html && echo "WRAPPER_USED"</automated>
  </verify>
  <acceptance_criteria>
- A `mAnimate(` open call uses `type:"spring"` with `stiffness:420, damping:38, mass:1` and `translateY` `100% → 0`.
- closeDetail still contains `classList.remove("show")` UNCONDITIONALLY (not inside an `if(Motion)` guard); the sheet hides instantly when Motion is absent.
- The #grab drag IIFE (`let y0=null` ... `d.style.transform`) is preserved EXCEPT the one authorized edit in action step 3: the `pointerup` handler's `d.style.transform=""` reset now runs ONLY on the `dy<=70` (not-dismissed) branch, so on `dy>70` there is NO premature snap-back and the close animates from the dragged position. Verify: `grep` shows the `pointerup` handler no longer resets `transform` unconditionally before `closeDetail()`. No Motion spring starts while `y0!=null`.
- No `pointer-events:none` on sheet content during the spring; no `await` on animation before content is interactive.
- Preview (mobile): opening a card's sheet slides up with a fast, minimally-bouncy spring (~1 small overshoot max); the ×/close and drag-down-to-dismiss both still work; content is tappable during the entrance; rapid open/close between two cards doesn't queue.
- Preview (reduced-motion): sheet appears/hides INSTANTLY (display swap), drag still works.
- Preview (Motion absent — temporarily neutralize window.Motion in devtools): sheet still opens/closes instantly, no throw.
- Desktop: detail card uses fade+8px translateY (no spring).
  </acceptance_criteria>
  <done>.detail mobile sheet springs open (420/38/1) and closes snappily via mAnimate(), state changes unconditional, drag logic wins during drag and hands off to an animated close from the dragged position; degrades to instant; desktop uses fade not spring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user gesture (#grab drag) → sheet transform | Pointer-driven inline transform vs Motion-driven transform on the same element |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Denial of Service (UX self-DoS) | Animation blocking interaction (corretor "waits out" a transition; sheet un-tappable during spring) | mitigate | No `await` on animations, no `pointer-events:none` for animation duration, no is-animating queue flag; state swaps unconditional; interruptible via native WAAPI re-call. Explicit acceptance criteria. |
| T-06-06 | Tampering (gesture conflict) | Motion spring fighting the #grab inline drag transform → sheet jitter/stuck | mitigate | Motion never animates while `y0!=null`; inline drag style has higher specificity; close animates from the dragged position, not a snap-back. |
| T-06-03 | Information Disclosure | (inherited) Motion only animates transform/opacity — no data/network/PII | accept | No new data path introduced by view/sheet animations. |

No high-severity threats — all view/sheet motion is transform/opacity-only on existing elements, no new network or data access, and progressive-enhancement guarantees the app functions without Motion.
</threat_model>

<verification>
- grep: setView keeps unconditional `dataset.view=v` + untouched `invalidateSize` timing; spring params `stiffness:420, damping:38, mass:1`; closeDetail keeps unconditional `classList.remove("show")`; mAnimate used; no `pointer-events:none`/layout props in keyframes.
- preview: view transitions (~180ms fade/slide, interruptible, map immediately interactive); sheet spring open + snappy close, drag still works and hands off; reduced-motion → instant; Motion-absent → instant, no throw.
</verification>

<success_criteria>
- Screen transitions (home⇄busca⇄detalhe + desktop .panel overlay) animate subtly and interruptibly via mAnimate().
- .detail mobile sheet springs (420/38/1) coordinated with #grab drag; snappy close; desktop uses fade.
- All state changes unconditional (progressive enhancement); reduced-motion → instant.
- map remeasure timing untouched. MOT-02 (view + sheet portions) satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/06-motion-no-app-todo/06-02-SUMMARY.md`
</output>
