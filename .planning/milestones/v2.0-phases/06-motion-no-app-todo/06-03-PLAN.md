---
phase: 06-motion-no-app-todo
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified: [radar-goiania.html]
autonomous: true
requirements: [MOT-02, MOT-03]
must_haves:
  truths:
    - "Result cards stagger-in (fade + translateY 8px) ONLY on a render following a new search — NOT on 'Mostrar mais' pagination or same-list re-render"
    - "Lot polygons stagger-in ONLY on the FIRST reveal per drill-in — NOT on every pan/refresh within the zoomed session"
    - "Stagger is capped at ~12 items; items beyond appear together (no multi-second cascade)"
    - "Tap feedback (:active scale + subtle darken) is CSS-only and applies to cards, searchpill, sheet action links, viewbar buttons, moderow buttons"
    - "Tap feedback stays active regardless of prefers-reduced-motion (it is instant state feedback, not animated motion)"
    - "If Motion is absent OR REDUCE is true, cards/polygons render at full opacity instantly (no offset, no delay)"
  artifacts:
    - path: "radar-goiania.html"
      provides: "First-render-only stagger for cards + lot polygons via mAnimate/mStagger; CSS-only :active tap feedback"
      contains: ":active"
  key_links:
    - from: "render(list)"
      to: "mAnimate/mStagger card entrance"
      via: "stagger flag true only from finish()/search entry, false from render(LAST) pagination"
      pattern: "function render"
    - from: "refreshLots()"
      to: "lot polygon stagger"
      via: "gate on the !map.hasLayer(lotLayer) first-reveal transition"
      pattern: "function refreshLots"
---

<objective>
Complete Phase 6 motion: (1) FIRST-RENDER-ONLY stagger-in for result cards (`render`) and lot polygons (`refreshLots`), reusing the same decelerate timing as screen transitions; (2) CSS-only `:active` tap feedback across the interactive surfaces. Cards/polygons and tap feedback all degrade gracefully (instant / unaffected respectively) under Motion-absent and reduced-motion.

Purpose: MOT-03 (stagger) + the tap-feedback portion of MOT-02. The "first render only" gating is an explicit anti-feature guard — repeated staggering on every pan reads as annoying, not premium.
Output: radar-goiania.html with gated stagger on cards + lots and CSS `:active` micro-scale/darken.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-motion-no-app-todo/06-CONTEXT.md
@.planning/phases/06-motion-no-app-todo/06-UI-SPEC.md
@.planning/phases/06-motion-no-app-todo/06-VALIDATION.md
@.planning/phases/06-motion-no-app-todo/06-01-SUMMARY.md
@.planning/phases/06-motion-no-app-todo/06-02-SUMMARY.md

<interfaces>
<!-- Verified anchors in radar-goiania.html (2026-07-04). Executor uses these directly. -->

render(list) (line 1311): builds `#results` innerHTML in one synchronous pass, sets `box.innerHTML=html`. `.card` elements have `data-i="${i}"`. Called from:
  - finish() (line 1226): `render(units);` — this is the NEW SEARCH path → SHOULD stagger.
  - render(LAST) via the "Mostrar mais" pagination button (line 1331): `onclick="RENDN+=500;render(LAST)"` — MUST NOT stagger.
So gate stagger on a parameter, e.g. `render(list, opts)` where finish() passes `{stagger:true}` and the pagination button passes nothing (default false). Simplest: `function render(list, opts){ const doStagger = !!(opts&&opts.stagger); ... }` and change finish's call to `render(units,{stagger:true})`; leave the pagination `render(LAST)` as-is (defaults to no stagger).

refreshLots() (line 809): the FIRST-reveal transition point is `if(!map.hasLayer(lotLayer))lotLayer.addTo(map);` (line 845) — capture `const firstReveal = !map.hasLayer(lotLayer);` BEFORE that addTo, and only stagger when firstReveal is true. Repeat pans short-circuit earlier via `if(LOTLAST&&LOTLAST.contains(map.getBounds())&&map.hasLayer(lotLayer))return;` (line 820), but even genuine new-bounds fetches after the first reveal must NOT re-stagger. Polygons are built in the `(d.features||[]).forEach(f=>{...poly.addTo(lotLayer);})` loop (lines 833-844).

Existing :active precedents (KEEP, un-gated by reduced-motion): `.go:active{transform:translateY(1px)}` (line 118), `.wnext:active{transform:translateY(1px)}` (line 324), `.searchpill:active{border-color:var(--accent)}` (line 167 — EXTEND, don't replace).

Tap-feedback target selectors (current CSS lines): `.card` (line 132), `.searchpill:active` (line 167), `.detail .acts a` (line 398, only :hover today, line 400), `.viewbar button` (line 425), `.moderow button` (line 76). CSS vars: `--paper-2` used by `.card.sel{background:var(--paper-2)}`.

Plan 01 provides: `mAnimate(el, keyframes, options)` (null when Motion absent/REDUCE, never throws) and optionally `mStagger`→`Motion.stagger`. `REDUCE` (let). Decelerate curve `[0.22,1,0.36,1]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS-only :active tap feedback (scale + subtle darken) across interactive surfaces</name>
  <files>radar-goiania.html</files>
  <read_first>radar-goiania.html (CSS lines 76, 118, 132, 167, 324, 398-402, 425-430), 06-UI-SPEC.md Section 4 (Tap Feedback table) + Section 5 (why :active is NOT gated by reduced-motion)</read_first>
  <action>
Add pure-CSS `:active` feedback. NO JS, NO Motion — `:active` is synchronous with the browser's touch handling and needs no REDUCE gate (per UI-SPEC Section 4/5, this is instant state feedback, not animated motion; matches the shipped un-gated `.go:active`/`.wnext:active` precedents).

1. `.card:active` — `transform:scale(.98)` + subtle darken via `filter:brightness(.97)` (reuse the darken spirit of `.card.sel{background:var(--paper-2)}`; do NOT introduce a new hex).
2. `.searchpill:active` — it already has `border-color:var(--accent)`; ADD `transform:scale(.98)` alongside (extend the existing rule, don't replace the border-color).
3. `.detail .acts a:active` — new rule: `transform:scale(.98)` (this class only has :hover today).
4. `.viewbar button:active` — new: `transform:scale(.97)` (≥44px targets; scale is visual-only and does not shrink the hit area).
5. `.moderow button:active` — new: `transform:scale(.98)`.
6. Add a short transition so press/release feels instant: `transition:transform .1s ease` on the relevant elements (100ms, well under the 150-250ms Motion budget). Under `prefers-reduced-motion` the Plan 01 global block forces transition-duration to 0.001ms — which is the desired near-instant end state anyway; no special-casing.
7. Zero new hex, zero new copy, no layout change — transforms are visual-only.
  </action>
  <verify>
    <automated>grep -q '.card:active' radar-goiania.html && grep -q 'scale(.98)' radar-goiania.html && echo "CARD_ACTIVE_OK"; grep -q '.viewbar button:active' radar-goiania.html && echo "VIEWBAR_ACTIVE_OK"; grep -q '.detail .acts a:active' radar-goiania.html && echo "ACTS_ACTIVE_OK"</automated>
  </verify>
  <acceptance_criteria>
- `grep -n ".card:active"` finds `transform:scale(.98)` and a subtle darken (`filter:brightness(.97)` or equivalent reuse — NO new hex).
- `.searchpill:active` still has `border-color:var(--accent)` AND now `transform:scale(.98)`.
- New `:active` rules exist for `.detail .acts a`, `.viewbar button` (scale .97), `.moderow button`.
- No new hex value anywhere in the diff; no font-size/weight/margin/padding change.
- Preview: pressing a card/pill/sheet action/viewbar/moderow button shows an instant subtle scale-down + darken; releasing restores. Under reduced-motion emulation, the feedback still occurs (near-instant), NOT disabled.
  </acceptance_criteria>
  <done>CSS-only :active scale+darken added to cards, searchpill, sheet action links, viewbar and moderow buttons; no new hex/copy/layout; unaffected by reduced-motion.</done>
</task>

<task type="auto">
  <name>Task 2: First-render-only stagger for result cards (render) and lot polygons (refreshLots)</name>
  <files>radar-goiania.html</files>
  <read_first>radar-goiania.html (render line 1311, finish call site line 1226, pagination button line 1331; refreshLots line 809-849 esp. the `!map.hasLayer(lotLayer)` at 845 and the forEach at 833), 06-UI-SPEC.md Section 3 (Stagger table: per-item + cap + first-render detection) + Section 6 (lot-polygon-under-many-features row)</read_first>
  <action>
Add gated stagger via mAnimate()/mStagger, reusing the screen-transition timing. State changes (innerHTML set, poly.addTo) stay unconditional so a missing Motion / REDUCE yields today's instant full-opacity render.

CARDS:
1. Change `render(list)` → `render(list, opts)` with `const doStagger = !!(opts && opts.stagger) && !REDUCE && !!window.Motion;`. Keep `box.innerHTML=html;` UNCONDITIONAL (cards exist + interactive immediately).
2. Change the finish() call (line 1226) from `render(units);` to `render(units,{stagger:true});`. Leave the pagination button `onclick="RENDN+=500;render(LAST)"` UNCHANGED — it calls `render(LAST)` with no opts → no stagger. Also leave any other `render(...)` re-render call sites without the stagger opt.
3. When `doStagger`: after setting innerHTML, select the freshly rendered `.card` elements: `const cards=[...box.querySelectorAll(".card")];`. Cap at first 12: animate `cards.slice(0,12)` with a per-item stagger; `cards.slice(12)` are left at their natural (full-opacity) state — do NOT delay them. For the staggered items use:
   `mAnimate(cards.slice(0,12), { opacity:[0,1], transform:["translateY(8px)","translateY(0)"] }, { duration:0.2, easing:[0.22,1,0.36,1], delay: mStagger ? mStagger(0.024) : Motion.stagger(0.024) });`
   (200ms per item, 24ms inter-item stagger, decelerate curve — Motion's `stagger()` computes per-element delays when given an array of elements.) If mAnimate returns null (Motion absent/REDUCE), the cards are already visible at full opacity — nothing to do.
4. Do NOT set opacity:0 in CSS on `.card` (that would hide cards permanently if Motion is absent). The only initial-hidden state is the animation's own `opacity:[0,...]` start keyframe, which WAAPI applies and reverts safely; when Motion is absent the card stays at its normal CSS opacity (1).

LOT POLYGONS:
5. In refreshLots(), BEFORE `if(!map.hasLayer(lotLayer))lotLayer.addTo(map);` capture: `const firstReveal = !map.hasLayer(lotLayer);`. Only stagger when `firstReveal && !REDUCE && window.Motion`.
6. Simplest reliable stagger (preferred over per-frame fillOpacity to protect low-end Android): stagger the appearance of the FIRST ~12 polygons. Options in priority order:
   - Preferred: collect the created polygon layers into an array as you build them in the forEach; after `lotLayer.addTo(map)`, for the first 12 polygons animate their SVG/canvas path via mAnimate on `poly.getElement()` (Leaflet SVG path element) with `{ opacity:[0,1] }` and `delay: Motion.stagger(0.024)` — 12-item cap, 200ms, same curve. Guard for null getElement (canvas renderer). If `preferCanvas`/no element, fall back to:
   - Fallback: schedule `poly.addTo(lotLayer)` calls for the first 12 polygons with incremental 24ms setTimeout delays (poly #n added at n*24ms), the rest added synchronously. This produces a visible "pop-in" sequence without per-frame style recalc across hundreds of polygons.
   Whichever is used: polygons beyond the 12-cap appear immediately; NO per-pan re-stagger (guaranteed by the `firstReveal` gate); if Motion absent/REDUCE, all polygons render synchronously at full style exactly as today.
7. Do NOT change the LOTTOKEN/LOTLAST logic, the zoom<17 gate, the short-circuit at line 820, or the resultRecordCount:4000. Stagger is purely an entrance visual layered on the existing populate.
8. Only transform/opacity animated; never layout properties. Reuse duration 0.2 + `[0.22,1,0.36,1]` for visual consistency with screen transitions.
  </action>
  <verify>
    <automated>grep -q 'function render(list' radar-goiania.html && grep -q 'render(units,{stagger:true})' radar-goiania.html && echo "CARD_GATE_OK"; grep -q 'render(LAST)' radar-goiania.html && echo "PAGINATION_UNCHANGED"; grep -q 'firstReveal' radar-goiania.html && grep -q 'stagger' radar-goiania.html && echo "LOT_GATE_OK"</automated>
  </verify>
  <acceptance_criteria>
- `render` signature accepts an opts/stagger flag; finish() calls `render(units,{stagger:true})`; the "Mostrar mais" button still calls `render(LAST)` with NO stagger opt (grep shows `render(LAST)` unchanged).
- `box.innerHTML=html;` remains UNCONDITIONAL (cards present even if Motion absent); no `.card{opacity:0}` static CSS rule introduced.
- Card stagger uses `Motion.stagger(0.024)` (24ms), `duration:0.2` (200ms), curve `[0.22,1,0.36,1]`, `translateY(8px)→0`, capped at 12 items.
- refreshLots captures `firstReveal = !map.hasLayer(lotLayer)` before addTo and only staggers when true; LOTTOKEN/LOTLAST/zoom-gate/4000 cap all unchanged.
- Preview: a NEW search → cards stagger-in (subtle, ≤~12 cascading); clicking "Mostrar mais" → the added cards do NOT re-stagger; re-rendering the same list does not re-stagger.
- Preview: drill into a bairro (zoom≥17) → lots pop in with a stagger the FIRST time; panning within the zoomed area → NO re-stagger.
- Preview (reduced-motion): cards + lots appear instantly at full opacity, no offset, no delay.
- Preview (Motion absent — neutralize window.Motion): cards + lots render instantly, no throw, list fully functional.
  </acceptance_criteria>
  <done>Cards stagger only on new-search renders (not pagination/re-render); lot polygons stagger only on first reveal per drill (not per pan); 12-item cap; degrades to instant full-opacity render under REDUCE/absent Motion; existing lot/list logic untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| render output → DOM | Card innerHTML is already escaped (esc()) upstream; stagger only animates the resulting nodes' transform/opacity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Denial of Service (UX) | Stagger cascade making long lists feel slow / re-staggering on every pan feeling annoying | mitigate | 12-item cap (items beyond appear immediately); first-render-only gate on cards (opts flag) and lots (firstReveal), so no repeated cascades. Explicit acceptance criteria. |
| T-06-08 | Denial of Service (perf) | Per-frame style recalc across hundreds of lot polygons on low-end Android | mitigate | 12-item cap + prefer element-opacity or staggered addTo over per-frame per-polygon setStyle; polygons beyond cap render synchronously. |
| T-06-09 | Availability | A static `.card{opacity:0}` (or hidden initial state) leaving content invisible if Motion is absent | mitigate | No static hidden CSS; initial-hidden exists only as the animation's own start keyframe; state (innerHTML/addTo) is unconditional and full-opacity by default. |
| T-06-03 | Information Disclosure | (inherited) transform/opacity only, no data/network/PII | accept | Stagger animates existing rendered nodes; no new data path. Tap feedback is CSS-only. |

No high-severity threats — stagger and tap feedback are transform/opacity/CSS-only on already-rendered, already-escaped elements, gated so content is never hidden when Motion is unavailable.
</threat_model>

<verification>
- grep: render accepts stagger opt; finish passes `{stagger:true}`; `render(LAST)` unchanged; `firstReveal` gate present; `Motion.stagger(0.024)`/`duration:0.2`; `:active` scale rules present; no new hex; no `.card{opacity:0}` static rule.
- preview: new-search cards stagger; pagination/re-render do NOT; first drill lots stagger, pans do NOT; :active feedback on all listed surfaces; reduced-motion → instant full opacity, feedback still fires; Motion-absent → instant, no throw.
</verification>

<success_criteria>
- Cards + lot polygons stagger-in ONLY on first render/first reveal (anti-feature guard honored), 12-item cap, decelerate timing reused.
- CSS-only :active tap feedback on cards, pill, sheet actions, viewbar, moderow — un-gated by reduced-motion.
- Progressive enhancement: instant full-opacity render when Motion absent/REDUCE; no hidden-content risk.
- MOT-03 + tap-feedback part of MOT-02 satisfied. Zero new hex/copy/layout.
</success_criteria>

<output>
After completion, create `.planning/phases/06-motion-no-app-todo/06-03-SUMMARY.md`
</output>
