---
phase: 14-linguagem-impecavel-pt-br-gate-de-release
plan: 04
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - radar-goiania.html
  - tests/templates.test.mjs
  - .planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md
autonomous: true
requirements: [LING-01]

must_haves:
  truths:
    - "As mensagens de WhatsApp soam como escritas por um corretor profissional (§26.8)"
    - "Os textos de captação (abordagem/script/checklist/follow-up) usam tom profissional e sem gíria"
    - "Toda edição de string em template RADAR_PURE atualiza a asserção acoplada no MESMO commit"
    - "As garantias de honestidade dos testes permanecem (nenhum 'garantido'/'certeza'; 'faixa estimada'/'recomendo confirmar' preservados)"
    - "npm test continua 100% verde (107 testes)"
  artifacts:
    - path: ".planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md"
      provides: "Seções WhatsApp (RADAR_PURE) e Captação (RADAR_PURE) preenchidas"
      contains: "WhatsApp"
  key_links:
    - from: "radar-goiania.html zap*/capt* (RADAR_PURE)"
      to: "tests/templates.test.mjs asserções literais"
      via: "edição de string + atualização de asserção no mesmo commit"
      pattern: "recomendo confirmar|faixa estimada|CRECI"
---

<objective>
Varredura das funções de template RADAR_PURE de WhatsApp e Captação — texto rico, alto risco de teste. As mensagens devem soar como um corretor profissional (§26.8): sem hype, sem gíria, com dado concreto do imóvel. Cada edição de string que toque uma substring asserida em `tests/templates.test.mjs` deve atualizar o teste no MESMO commit (Pitfall 1 da pesquisa) — nunca deixar a suíte vermelha entre tasks.

Purpose: WhatsApp é o texto que sai do app e vai para um cliente real — é o produto na frente do corretor. É onde "linguagem impecável" mais importa comercialmente.
Output: Funções zap*/capt* auditadas; testes acoplados atualizados em lockstep; seções WhatsApp e Captação da auditoria preenchidas; suíte verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-CONTEXT.md
@.planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-RESEARCH.md
@.planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md

<interfaces>
<!-- Funções RADAR_PURE (14-RESEARCH.md) — bloco RADAR_PURE_START..RADAR_PURE_END (1135-2005) -->
WhatsApp (testadas em tests/templates.test.mjs):
  zapResumo (1438), zapProprietario (1451), zapComprador (1465), zapArgumento (1478), zapRiscos (1493)
Captação (testadas em tests/templates.test.mjs):
  captAbordagem (1504), captScript (1514, 4 passos), captChecklist (1524, 5 itens estáticos), captFollowup (1534)
  + oportunidadeItem, histAdd (também em templates.test.mjs)

Asserções literais que templates.test.mjs fixa (NÃO quebrar sem atualizar o teste no mesmo commit):
  - assinatura "— Ana Souza, CRECI 12345"
  - termos de honestidade "recomendo confirmar" / "faixa estimada" / "não é uma avaliação oficial"
  - AUSÊNCIA de "garantido" / "certeza" (§26.8 tom profissional, sem promessa) — NUNCA introduzir essas palavras
  - passos "1." … "4." (captScript)
  - itens do checklist: "matrícula", "iptu", "certidões pessoais", "condomínio", "identidade"
  - contagem de bullets "•"

Regra §26.8: parece corretor profissional = sem emoji excessivo, sem CAPS em bloco, com dado concreto do imóvel. Tom direto, sem hype.
Mudança mínima: RESEARCH confirma que zero jargão e tom já são majoritariamente bons — só ajustar onde ferir §26.
Comando por task: `npm test` (ou `node --test tests/templates.test.mjs` para feedback rápido).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WhatsApp — zapResumo/zapProprietario/zapComprador/zapArgumento/zapRiscos</name>
  <read_first>
    - radar-goiania.html (linhas 1438-1503: zapResumo, zapProprietario, zapComprador, zapArgumento, zapRiscos)
    - tests/templates.test.mjs (asserções sobre as 5 funções zap*)
    - .planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-RESEARCH.md (Pitfall 1; §"Acoplamento de Teste")
    - .planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md
  </read_first>
  <action>
    Ler as 5 funções zap* e gerar uma amostra de cada (mentalmente/por leitura) contra o critério §26.8 (corretor profissional). Aplicar mudança mínima:
    - Corrigir qualquer texto com tom robótico, hype, CAPS em bloco, gíria ou acentuação errada.
    - PRESERVAR literalmente: assinatura "— Ana Souza, CRECI 12345", "recomendo confirmar", "faixa estimada", "não é uma avaliação oficial". NUNCA introduzir "garantido"/"certeza".
    - Se uma edição tocar uma substring asserida em `tests/templates.test.mjs`, atualizar a asserção correspondente NO MESMO commit (editar radar-goiania.html E tests/templates.test.mjs juntos).
    Após cada edição, rodar `npm test` — deve ficar verde antes de prosseguir. Se ficar vermelho, localizar a asserção quebrada e alinhar (nunca deixar vermelho entre commits).
    Preencher a seção "WhatsApp (RADAR_PURE)" da auditoria (uma linha por função: veredito + o que mudou + §26).
  </action>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && ! grep -Eq "garantido|com certeza" radar-goiania.html && npm test 2>&1 | grep -q "fail 0" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (107 tests, fail 0)
    - `grep -Eq "garantido|com certeza" radar-goiania.html` FALSO (nenhuma promessa introduzida)
    - `grep -q "recomendo confirmar" radar-goiania.html` e `grep -q "faixa estimada" radar-goiania.html` verdadeiros (honestidade preservada)
    - `grep -q "CRECI 12345" tests/templates.test.mjs` verdadeiro (assinatura de fixture preservada) OU asserção atualizada de forma consistente com radar-goiania.html
    - Seção "WhatsApp (RADAR_PURE)" da auditoria com 5 linhas (uma por função zap*)
  </acceptance_criteria>
  <done>As 5 mensagens de WhatsApp soam profissionais, com honestidade preservada; testes acoplados verdes; seção WhatsApp auditada.</done>
</task>

<task type="auto">
  <name>Task 2: Captação — captAbordagem/captScript/captChecklist/captFollowup + oportunidadeItem/histAdd</name>
  <read_first>
    - radar-goiania.html (linhas 1504-1545: captAbordagem, captScript, captChecklist, captFollowup; + oportunidadeItem/histAdd)
    - tests/templates.test.mjs (asserções sobre capt*, oportunidadeItem, histAdd)
    - .planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md
  </read_first>
  <action>
    Auditar as 4 funções de captação + oportunidadeItem/histAdd contra §26 (tom profissional, verbo de ação, sem gíria). Aplicar mudança mínima:
    - captScript: preservar a numeração "1." … "4." (asserida no teste).
    - captChecklist: preservar os 5 itens cujos termos são asseridos ("matrícula", "iptu", "certidões pessoais", "condomínio", "identidade") — pode melhorar a redação ao redor, mas manter esses termos presentes (ou atualizar a asserção no mesmo commit se realmente precisar mudar o termo).
    - Preservar a contagem de bullets "•" onde o teste conta.
    Qualquer edição que toque substring asserida → atualizar `tests/templates.test.mjs` no MESMO commit. Rodar `npm test` após cada edição (verde antes de seguir).
    Preencher a seção "Captação (RADAR_PURE)" da auditoria (uma linha por função).
  </action>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && npm test 2>&1 | grep -q "fail 0" && grep -q "Captação" ".planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (107 tests, fail 0)
    - `node --test tests/templates.test.mjs` passa (nenhuma asserção de capt*/oportunidadeItem/histAdd quebrada sem atualização correspondente)
    - Seção "Captação (RADAR_PURE)" da auditoria com uma linha por função (captAbordagem/captScript/captChecklist/captFollowup + oportunidadeItem + histAdd)
    - Nenhuma introdução de gíria/hype/CAPS em bloco (registrado no veredito de cada função)
  </acceptance_criteria>
  <done>Textos de captação profissionais; testes acoplados verdes; seção Captação auditada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edição de template → clipboard do usuário | texto copiado para WhatsApp de terceiros; risco = promessa indevida (garantia) ou PII |
| RADAR_PURE ↔ tests/templates.test.mjs | substrings de honestidade asseridas literalmente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Repudiation | mensagens de WhatsApp (honestidade/ressalva) | mitigate | acceptance: "recomendo confirmar"/"faixa estimada"/"não é uma avaliação oficial" preservados; "garantido"/"com certeza" nunca introduzidos (grep) — o teste é o gate |
| T-14-02 | Information Disclosure (LGPD) | zap*/capt* preenchidos com dados | mitigate | edição só de texto fixo do template; nenhum novo campo de dado interpolado; grep confirma ausência de `dtnascimen` |
| T-14-04 | Tampering (XSS) | templates retornam texto puro (clipboard, não innerHTML) | mitigate | não introduzir HTML cru nas strings; manter o contrato "texto puro" das funções RADAR_PURE |
</threat_model>

<verification>
- `npm test` exits 0 (107 tests, fail 0) ao fim de cada task
- `grep -Eq "garantido|com certeza" radar-goiania.html` FALSO
- Termos de honestidade preservados no HTML e nos testes
- Seções WhatsApp e Captação preenchidas na auditoria
</verification>

<success_criteria>
- Mensagens de WhatsApp e captação soam profissionais (§26.8)
- Honestidade dos templates preservada (sem promessa de resultado)
- Testes acoplados atualizados em lockstep; suíte 100% verde (107 testes)
</success_criteria>

<output>
Após completar, criar `.planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-04-SUMMARY.md`
</output>
