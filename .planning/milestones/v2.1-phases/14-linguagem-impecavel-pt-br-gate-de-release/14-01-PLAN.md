---
phase: 14-linguagem-impecavel-pt-br-gate-de-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - radar-goiania.html
  - tests/fixtures.mjs
  - tests/scores.test.mjs
  - .planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md
autonomous: true
requirements: [LING-01]

must_haves:
  truths:
    - "Existe uma tabela de auditoria com colunas string original → veredito → string final → critério §26"
    - "O glossário canônico está fixado e ratifica os 7 termos das fases anteriores"
    - "O rótulo de score de 1ª camada não contém jargão (A1: 'Abaixo da mediana' eliminado das 3 âncoras + fixture)"
    - "O texto de loading estático e o MOTION_MSG usam a mesma capitalização (A2)"
    - "npm test continua 100% verde (107 testes)"
  artifacts:
    - path: ".planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md"
      provides: "Tabela de auditoria (artefato do gate) com glossário + seção de nomenclatura canônica"
      contains: "Glossário canônico"
    - path: "radar-goiania.html"
      provides: "Rótulo de oportunidade sem jargão + loading consistente"
      contains: "Oportunidade baixa"
  key_links:
    - from: "radar-goiania.html scoreOportunidade() rotulo"
      to: "tests/fixtures.mjs expectRotulo"
      via: "asserção literal em tests/scores.test.mjs"
      pattern: "Oportunidade baixa"
---

<objective>
Estabelecer a fundação do gate de linguagem (Fase 14): criar a tabela de auditoria (artefato que prova o gate), fixar o glossário canônico ratificando os nomes já travados em fases anteriores, e aplicar as duas correções canônicas concretas já localizadas na pesquisa — A1 (jargão "mediana" no rótulo de score de 1ª camada) e A2 (capitalização divergente entre o loading estático e MOTION_MSG).

Purpose: A tabela de auditoria e o glossário são a base sobre a qual as varreduras dos planos 02-05 escrevem. Resolver A1/A2 primeiro garante que os planos seguintes (que auditam a legenda de pinos e as mensagens de motion) refiram a nomenclatura já corrigida.
Output: `14-AUDITORIA.md` criado; rótulo de oportunidade sem jargão em todas as âncoras; loading consistente; suíte 100% verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-CONTEXT.md
@.planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-RESEARCH.md
@.planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-VALIDATION.md

<interfaces>
<!-- Nomenclatura canônica a ratificar (14-RESEARCH.md §"Nomenclatura Canônica"). Não decidir nomes novos — só verificar que nenhuma variação divergente escapou. -->

Termos travados em fases anteriores (preservar exatamente):
- "Oportunidades" / "Minhas oportunidades" (imóveis salvos manualmente) — 10-CONTEXT.md
- "Histórico" (últimas consultas automáticas) — 10-CONTEXT.md
- "O que o Radar faz" (descoberta na tela inicial) — Fase 13
- "Modo captação" — Fase 10
- "Ficha rápida" / "Relatório de avaliação" / "Laudo/PTAM" — Fase 11 (DOC-01)
- "Proposta de Compra e Venda" / "Termo de Autorização/Exclusividade de Venda" / "Contrato de Compra e Venda" — Fase 11.1
- "Oportunidades Caixa" (imóveis à venda pela Caixa) — legado; colisão de palavra com "Minhas oportunidades" avaliada em A4 e mantida

Âncoras exatas do A1 (rótulo de score, verificadas por leitura de código):
```
radar-goiania.html:1351  rotulo="Abaixo da mediana";        (função scoreOportunidade, score<33)
radar-goiania.html:935   <span>...>Abaixo da mediana</span> (legenda de pinos #pinoLegenda)
radar-goiania.html:3172  risco:"Abaixo da mediana",         (mapa de rótulos de status)
tests/fixtures.mjs:65    expectRotulo:"Abaixo da mediana"   (consumido por tests/scores.test.mjs)
```
Os outros dois rótulos da função (linhas 1343/1346) são "Boa oportunidade" e "Oportunidade média" — o terceiro deve seguir o mesmo eixo semântico ("oportunidade"), sem jargão estatístico. As strings de `porque[]` (que contêm "mediana da vizinhança") são a camada de explicação, NÃO o badge de 1ª camada — permanecem intactas (mudança mínima).

Âncoras exatas do A2:
```
radar-goiania.html:1006  <span id="loadmsg">consultando cadastro…</span>   (minúscula)
radar-goiania.html:2548  cadastro: "Consultando cadastro…",               (MOTION_MSG, maiúscula — padrão)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar tabela de auditoria + fixar glossário canônico</name>
  <read_first>
    - .planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-RESEARCH.md (§"Nomenclatura Canônica", §"Achados Concretos" A4)
    - .planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-CONTEXT.md (decisões de nomenclatura)
  </read_first>
  <action>
    Criar `.planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md` com esta estrutura fixa:
    1. Cabeçalho com data e o checklist §26 (8 critérios, copiados de 14-CONTEXT.md).
    2. Seção "Glossário canônico" — uma tabela ratificando os 7 termos travados listados no bloco <interfaces> acima (Conceito | Termo canônico | Fonte). Registrar explicitamente o Achado A4 como linha: "'Oportunidades' usado para dois conceitos ('Oportunidades Caixa' externo vs 'Minhas oportunidades' salvas) — ambiguidade AVALIADA, distinção por contexto/ícone (🏦 vs ⭐), SEM mudança".
    3. Seção "Nomenclatura residual" — rodar `grep -n "Favoritos\|Salvos" radar-goiania.html` e registrar o resultado (esperado: 0 ocorrências, já consolidado a favor de "Oportunidades" na Fase 10). Se aparecer qualquer ocorrência, registrar como item de correção.
    4. Seções vazias por categoria (a serem preenchidas pelos planos 02-05): "Botões", "Placeholders", "Títulos/Descrições/PWA", "Onboarding + O que o Radar faz + Legenda", "Toasts/Erros + Estados vazios", "Tooltips/aria-label", "WhatsApp (RADAR_PURE)", "Captação (RADAR_PURE)", "Documentos + Negociação (RADAR_PURE)", "Prédio (RADAR_PURE)", "Scores/Motion/Chip de busca".
    Cada seção de categoria é uma tabela com colunas: `String original | Âncora (linha) | Veredito (OK / alterada / avaliada-mantida) | String final | Critério §26`.
    5. Rodapé "Contagem" com placeholders: "N revisadas: (a consolidar no Plano 05) · M alteradas: (a consolidar no Plano 05)".
  </action>
  <verify>
    <automated>test -f ".planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md" && grep -q "Glossário canônico" ".planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-AUDITORIA.md" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `14-AUDITORIA.md` existe e contém a string exata `Glossário canônico`
    - Contém a string exata `O que o Radar faz` e `Modo captação` na tabela de glossário
    - Contém uma linha registrando o Achado A4 com a string `AVALIADA`
    - Contém o resultado do grep de `Favoritos`/`Salvos` (0 ocorrências esperado)
    - Contém as 11 seções de categoria vazias listadas na ação
    - `npm test` exits 0 (107 tests) — nenhuma edição de código nesta task
  </acceptance_criteria>
  <done>Artefato de auditoria criado com glossário ratificado e esqueleto de categorias; nenhum código tocado ainda.</done>
</task>

<task type="auto">
  <name>Task 2: A1 — eliminar jargão "mediana" do rótulo de oportunidade (5 âncoras + teste)</name>
  <read_first>
    - radar-goiania.html (linhas 1320-1355 scoreOportunidade; 930-938 legenda; 3168-3175 mapa de status)
    - tests/fixtures.mjs (linhas 60-66)
    - tests/scores.test.mjs (asserções de rotulo)
    - .planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-RESEARCH.md (Achado A1)
  </read_first>
  <action>
    Trocar a string exibida ao usuário `Abaixo da mediana` por `Oportunidade baixa` em TODAS as âncoras abaixo, no MESMO commit (é jargão estatístico na 1ª camada + rótulo enganoso — §26.4). Não alterar thresholds, lógica, nem as strings de `porque[]`:
    1. `radar-goiania.html` linha 1351: `rotulo="Abaixo da mediana";` → `rotulo="Oportunidade baixa";`
    2. `radar-goiania.html` linha 935 (legenda de pinos): `>Abaixo da mediana</span>` → `>Oportunidade baixa</span>`
    3. `radar-goiania.html` linha 3172 (mapa de rótulos de status): `risco:"Abaixo da mediana",` → `risco:"Oportunidade baixa",`
    4. `tests/fixtures.mjs` linha 65: `expectRotulo:"Abaixo da mediana"` → `expectRotulo:"Oportunidade baixa"`
    5. Rodar `grep -rn "Abaixo da mediana" radar-goiania.html tests/` — deve retornar 0. Se `tests/scores.test.mjs` tiver uma asserção literal residual, atualizá-la também.
    Rodar `npm test` — deve ficar 100% verde. Registrar A1 na seção "Scores/Motion/Chip de busca" da `14-AUDITORIA.md` (original `Abaixo da mediana` → final `Oportunidade baixa`, critério §26.4).
    NÃO tocar em: chaves `radar_*`, `SEARCHTOKEN`, classes CSS (`.score-lbl` etc.), a cor `#b5451f` da legenda, os thresholds `score>=66`/`score>=33`.
  </action>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && ! grep -rq "Abaixo da mediana" radar-goiania.html tests/ && npm test 2>&1 | grep -q "fail 0" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "Abaixo da mediana" radar-goiania.html tests/` retorna 0 ocorrências
    - `grep -c "Oportunidade baixa" radar-goiania.html` retorna >= 3
    - `grep -q "Oportunidade baixa" tests/fixtures.mjs` verdadeiro
    - Os thresholds `score>=66` e `score>=33` permanecem em radar-goiania.html (grep confirma)
    - `npm test` exits 0 (107 tests, fail 0)
    - Linha do A1 registrada em `14-AUDITORIA.md`
  </acceptance_criteria>
  <done>Rótulo de oportunidade de 1ª camada sem jargão em função, legenda, mapa de status e fixture; teste verde; A1 auditado.</done>
</task>

<task type="auto">
  <name>Task 3: A2 — unificar capitalização do loading + auditar MOTION_MSG e chip de busca</name>
  <read_first>
    - radar-goiania.html (linha 1006 #loadmsg; linhas 2546-2552 MOTION_MSG; linhas 1262-1312 labels de detectMode)
    - .planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-RESEARCH.md (Achado A2; nota sobre labels de detectMode não-testados)
  </read_first>
  <action>
    1. A2 — `radar-goiania.html` linha 1006: trocar `<span id="loadmsg">consultando cadastro…</span>` → `<span id="loadmsg">Consultando cadastro…</span>` (maiúscula, alinhado a `MOTION_MSG.cadastro` linha 2548). Não alterar o `id="loadmsg"` nem `aria-live="polite"`.
    2. Auditar as 5 mensagens de `MOTION_MSG` (linhas 2546-2552): confirmar que todas seguem o padrão maiúscula + reticências, sem jargão de 1ª camada, verbo gerúndio de progresso ("Localizando…", "Consultando cadastro…", etc.). Registrar cada uma na seção "Scores/Motion/Chip de busca" da auditoria (veredito OK ou alterada).
    3. Auditar os `label:` de `detectMode()` (linhas 1262-1312: "Setor · …", "Inscrição (unidade) · N díg.", "Inscrição (lote) · N díg.", "Quadra X · Lote Y", "Endereço · …", "Prédio · …", "Ambíguo", "Prédio (?) · …") — são a microcopy do chip de confirmação da busca. Aplicar §26: "Inscrição" é jargão cadastral aceitável no chip técnico de confirmação (o usuário digitou dígitos e está confirmando o tipo); confirmar acentuação e clareza. Estes labels NÃO têm teste acoplado (podem ser editados livremente). Registrar cada um na auditoria (mudança mínima — só corrigir se ferir §26).
    Rodar `npm test` (deve permanecer verde; A2 é HTML puro sem teste acoplado).
  </action>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && grep -q 'id="loadmsg">Consultando cadastro…' radar-goiania.html && npm test 2>&1 | grep -q "fail 0" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'id="loadmsg">Consultando cadastro…' radar-goiania.html` verdadeiro (maiúscula)
    - `grep -c "consultando cadastro" radar-goiania.html` retorna 0 (nenhuma minúscula residual)
    - Seção "Scores/Motion/Chip de busca" da auditoria contém as 5 linhas de MOTION_MSG e os 8 labels de detectMode
    - `npm test` exits 0 (107 tests, fail 0)
  </acceptance_criteria>
  <done>Loading consistente com MOTION_MSG; mensagens de motion e chip de busca auditados; teste verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edição de string → render ao usuário | strings literais editadas neste plano são inseridas na UI/badge de score; risco = tocar string funcional por engano |
| RADAR_PURE ↔ tests/ | rotulo de scoreOportunidade é asserido literalmente em fixtures/scores.test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | radar-goiania.html (chaves radar_*, SEARCHTOKEN, ?insc=, classes CSS, thresholds de score) | mitigate | acceptance grep: thresholds `score>=66`/`score>=33` preservados; edição só das strings `Abaixo da mediana` e `consultando cadastro`; nenhuma classe/`id`/chave tocada |
| T-14-02 | Information Disclosure (LGPD) | strings novas/editadas | mitigate | nenhum campo pessoal introduzido; grep confirma ausência de `dtnascimen`; A1/A2 não adicionam dado |
| T-14-04 | Tampering (XSS) | scoreOportunidade rotulo → esc() no render | mitigate | nova string `Oportunidade baixa` é texto puro sem `<`/`>`/`&`; chamadas `esc()` existentes não alteradas |
</threat_model>

<verification>
- `npm test` exits 0 (107 tests, fail 0) ao fim de cada task
- `grep -rn "Abaixo da mediana" radar-goiania.html tests/` = 0
- `14-AUDITORIA.md` existe com glossário + seções de categoria
- Nenhuma chave `radar_*`, `SEARCHTOKEN`, `?insc=` ou classe CSS alterada (diff só toca strings visíveis)
</verification>

<success_criteria>
- Tabela de auditoria criada com glossário canônico ratificado (7 termos + A4 registrado)
- A1 resolvido: rótulo de oportunidade sem jargão nas 3 âncoras de HTML/JS + fixture + teste verde
- A2 resolvido: loading estático alinhado ao MOTION_MSG
- MOTION_MSG (5) e labels de detectMode (8) auditados na tabela
- Suíte 100% verde (107 testes)
</success_criteria>

<output>
Após completar, criar `.planning/phases/14-linguagem-impecavel-pt-br-gate-de-release/14-01-SUMMARY.md`
</output>
