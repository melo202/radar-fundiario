---
phase: 12-predio-como-objeto-comercial
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - radar-goiania.html
autonomous: true
requirements: [PRED-01, PRED-02]
tags: [predio, ui, resumo, ordenacao, comparacao, sheet]

must_haves:
  truths:
    - "Buscar um predio com multiplas unidades expande .bldg-head com resumo (unidades/area media/venal medio/estimado medio+faixa) + acoes 'Ordenar/filtrar' e 'Copiar analise do predio', ANTES da lista de cards — zero requisicao de rede nova"
    - "Ordenar por 'Maior oportunidade'/'Menor estimado'/'Maior area' reordena so as unidades DAQUELE predio (ordenaUnidades) e o RENDN/paging (Mostrar mais) usa a lista JA REORDENADA antes do slice — nunca mistura ordem antiga com paginacao nova"
    - "Marcar unidade para comparar (toggle no card) nao muta a ordem/indice: o estado de marcacao (CMP) e identificado por ci+incompl (nao por indice numerico), sobrevivendo a reordenacao/filtro sem perder ou duplicar selecao"
    - "Botao flutuante 'Comparar (N)' aparece com N>=2, abre sheet com tabela (colunas=unidades, linhas=Area/Venal/Estimado/R$/m2/Oportunidade), scroll horizontal INTERNO no mobile (pagina nunca rola horizontal), Esc fecha na posicao correta da cadeia, e o limite de 4 mostra toast honesto sem alterar o estado da 5a tentativa"
    - "Guardas preservadas: scrollToResults()/lista mobile atras do form, venalTxt('nao informado'), esc()/IN-01 em toda interpolacao nova, DCUR intacto, 44px em todo elemento tocavel novo, zero hex novo"
  artifacts:
    - path: "radar-goiania.html"
      provides: "CSS novo (.bldg-sumario/.bldg-metricas/.bm/.bldg-acoes/.bldg-ord/.bldg-filtros/.cmp-toggle/.cmp-fab/.cmp-table etc, zero hex novo) + funcoes de UI (toggleBldgOrd/copyZapPredio/ordenarBldg/toggleAptosProvaveis/buscarUnidadeBldg/toggleComparar/abrirComparacao/fecharComparacao) + wiring em render()/cardHTML()/finish()/keydown"
      contains: "function toggleComparar"
  key_links:
    - from: "toggleBldgOrd/ordenarBldg/toggleAptosProvaveis/buscarUnidadeBldg"
      to: "resumoPredio/ordenaUnidades/ehAptoProvavel/analisePredicoTexto (12-01, RADAR_PURE)"
      via: "chamada direta das 4 funcoes puras — nenhuma reimplementacao de logica de agregacao/ordenacao na camada de UI"
      pattern: "ordenaUnidades\\("
    - from: "render(list,opts)"
      to: "RENDN slice"
      via: "quando ci tem ordenacao/filtro ativo em BLDGSTATE, list.slice(0,RENDN) opera sobre a lista JA reordenada/filtrada (ordenarBldg/toggleAptosProvaveis chamam render(LAST) apos substituir o subconjunto do predio dentro de LAST)"
      pattern: "BLDGSTATE"
    - from: "toggleComparar(i,btn)"
      to: "CMP (array de chaves ci+insubprinc, nao indices)"
      via: "chave estavel calculada a partir de clean(a.ci)+'|'+String(a.insubprinc) para sobreviver a reordenacao/paginacao"
      pattern: "function toggleComparar"
    - from: "finish()"
      to: "CMP=[]; fecharComparacao(); BLDGSTATE={}"
      via: "reset de estado de sessao no INICIO de finish() — mesma filosofia de closeChooser() ja existente"
      pattern: "CMP=\\[\\]"
---

<objective>
Construir a camada de UI do prédio comercial sobre as 4 funções puras da Wave 1 (12-01): expandir `.bldg-head` com o resumo do edifício (métricas + ações "Ordenar/filtrar" e "Copiar análise do prédio"), renderizar a barra de ordenação/filtro (chips + toggle "só aptos prováveis" + busca de unidade via `matchApto`), adicionar o toggle de marcação "☐/☑" em cada card de unidade, o botão flutuante "Comparar (N)" e o sheet de comparação em tabela (colunas = unidades, linhas = Área/Venal/Estimado/R$/m²/Oportunidade) — tudo reusando 1:1 os padrões visuais `.chips`/`.seg`/`.zapbtn`/`.wiz`/`.toast` já existentes (12-UI-SPEC.md), zero token novo, zero requisição de rede nova.

Purpose: PRED-01/PRED-02 completam a leitura comercial do prédio (Fase 12 goal). A reordenação DEVE interagir corretamente com o paging `RENDN`/"Mostrar mais" (reordenar o conjunto ANTES do slice, nunca depois) e a marcação de comparação DEVE sobreviver a reordenação/filtro (chave estável por `ci+insubprinc`, nunca por índice numérico, que muda a cada re-render).
Output: `.bldg-head` expandido com resumo + ações; barra de ordenação/filtro por prédio; toggle de marcação em cada card de unidade; botão flutuante "Comparar (N)"; sheet `#cmpSheet` com tabela de comparação até 4 colunas, scroll horizontal interno no mobile; estado `CMP`/`BLDGSTATE` resetado a cada `finish()` novo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-predio-como-objeto-comercial/12-CONTEXT.md
@.planning/phases/12-predio-como-objeto-comercial/12-UI-SPEC.md
@.planning/phases/12-predio-como-objeto-comercial/12-01-SUMMARY.md

# Fonte de código (ler antes de editar)
@radar-goiania.html

<facts>
<!-- Verificado por leitura direta deste repo. -->

`finish(items, fromMap)` (linha ~2424): já chama `closeChooser()` no início. `LAST=units` é
definido na linha ~2443, ANTES do cálculo de `ehPredio` (linha ~2467: `units.length>1&&nLotes===1`).
Esta plan ACRESCENTA `CMP=[];fecharComparacao();BLDGSTATE={};` imediatamente após `closeChooser()`
no início de `finish()` — mesma posição estrutural, mesma filosofia defensiva.

`render(list, opts)` (linha ~2554): monta `html` via loop `list.slice(0,RENDN).forEach(...)`; o
`.bldg-head` é inserido quando `cnt[ci]>1` (linha ~2569-2575) — ESTE é o ponto exato onde o
`.bldg-sumario` (Componente 1) e o `.bldg-ord` (Componente 2, inicialmente `hidden`) entram,
IMEDIATAMENTE após o `.bldg-head` atual, ANTES do primeiro `cardHTML()` daquele `ci`. `RENDN`
(let global, linha ~2553) já limita `list.slice(0,RENDN)` — a ordenação/filtro por prédio DEVE
acontecer ANTES desse slice: a estratégia desta plan é substituir o subconjunto de `LAST` daquele
`ci` (via `ordenaUnidades`/filtro) e chamar `render(LAST)` de novo — o slice do `RENDN` sempre
opera sobre o `LAST` já no estado final (reordenado/filtrado), nunca sobre uma cópia paralela.

`cardHTML(a, i, isUnit)` (linha ~2605): `i` é o ÍNDICE em `LAST` (usado por `pick(${i})` e
`data-i="${i}"`) — MUDA a cada `render()` se `LAST` for reordenado. Por isso `CMP`/estado de
marcação NÃO PODE usar `i` como chave persistente entre reordenações — usa `clean(a.ci)+"|"+
String(a.insubprinc)` (campo `insubprinc` já existe no objeto `a`, usado hoje como tie-breaker em
`finish()` linha ~2441 `(x.insubprinc||0)-(y.insubprinc||0)`) como chave estável. O toggle no card
ainda recebe `i` (para `data-i`/`pick`), mas resolve a chave estável internamente via
`LAST[i]` antes de gravar em `CMP`.

`.card` (linha 167): `margin:8px 16px;background:var(--paper);border:1.5px solid var(--line);
border-left:4px solid var(--lot);border-radius:2px;padding:13px 14px;cursor:pointer;...` — SEM
`position:relative` hoje. Esta plan ADICIONA `position:relative` a esta regra (pré-requisito do
UI-SPEC Componente 3 para o `.cmp-toggle` posicionar-se com `position:absolute`).

`toast(msg)` (linha ~2265): `t.textContent=msg;t.classList.add("show")` com timeout de 3600ms —
reusado literalmente para o toast de limite de 4 e o toast de "Copiado!".

`copyTexto(texto, okMsg)` (linha ~3128): já existe, usado por `copyZap`. `copyZapPredio(ci)` desta
plan REUSA `copyTexto` (não reimplementa clipboard).

`mercadoEstimado(a, privReal)` (linha ~2522, fora do bloco RADAR_PURE): usada pela Wave 2 para
calcular `__est` por unidade ANTES de chamar `resumoPredio`/`ordenaUnidades` (12-01 já documenta
esse contrato — `__est = mercadoEstimado(u)` calculado no momento de montar o resumo/ordenar,
nunca cacheado em `a` permanentemente para não conflitar com `a.__scores` da Fase 9/10, que tem
semântica diferente — score de oportunidade de VIZINHANÇA, calculado só quando a ficha abre).

`unitLabel(a)` (linha ~2547): usado para exibir "Apto 302" nas colunas da tabela de comparação
(`th`) e nos rótulos do card.

`m2Edif(a)` (linha ~2546): `vlvenal/areaedif` — usado na linha "R$/m²" da tabela de comparação.

`a.__scores` (populado só quando a ficha é aberta via `showDetail`/`compare()`, linha ~2768/2809):
linha "Oportunidade" da tabela de comparação lê `a.__scores&&a.__scores.op?a.__scores.op.rotulo:
"—"` — NUNCA dispara `compare()`/`scoreOportunidade` a partir do sheet de comparação (zero request
nova, mesma regra do UI-SPEC Estados Transversais).

Cadeia de Esc (linha ~4217-4239, `document.addEventListener("keydown", ...)`): ordem atual
`#negSheet` → `#captSheet` → `#caixaList` → `#correctMenu` → `#ambigChips` → `#calc` → `#wiz`
(laudo) → `#laudoView` → `#chooser` → overlay de busca desktop → `closeDetail()`. Esta plan
insere o check de `#cmpSheet` na MESMA posição estrutural do `#negSheet`/`#captSheet` (logo no
topo da cadeia, ANTES de `#caixaList`) — sheet mais recente tem prioridade, conforme UI-SPEC
Acessibilidade ("mutuamente exclusivo com os sheets de documento").

CSS de referência já existente (reuso literal, ZERO propriedade nova fora do que está documentado
no 12-UI-SPEC.md): `.wiz`/`.wtop`/`.wback`/`.wclose`/`.wbody`/`.wfoot` (linhas 382-424), `.chips`/
`.chips button`/`.chips button.on` (linhas 407-410), `.seg`/`.seg button` (linhas 403-406),
`.toast` (linha 607), `.zapbtn`-like botões (linha 294), `.card` (linha 167).
</facts>

<interfaces>
<!-- Funcoes puras da Wave 1 (12-01) que esta plan CONSOME diretamente — assinaturas travadas. -->

```js
// De 12-01 (RADAR_PURE) — ja implementadas, esta plan APENAS CHAMA, nunca reimplementa:
function resumoPredio(units): {n, areaMedia, venalMedio, estimadoMedio, faixaLo, faixaHi}
function ordenaUnidades(units, criterio): Array // "padrao"|"oportunidade"|"estimado-asc"|"area-desc"
function ehAptoProvavel(a): boolean
function analisePredicoTexto(resumo, meta): string
```

Contrato do campo `__est` esperado por `resumoPredio`/`ordenaUnidades` (documentado em 12-01):
cada unidade passada precisa ter `__est` = `mercadoEstimado(u)` (ou `null`) calculado pela Wave 2
ANTES de chamar as funções puras — esta plan monta esse enriquecimento localmente (variável
temporária, nunca grava `__est` permanentemente em `a` para não colidir com `a.__scores`):

```js
// Padrão de chamada (dentro de toggleBldgOrd/ordenarBldg/copyZapPredio/render):
const unidadesDoPredio = LAST.filter(a=>clean(a.ci)===ci);
const enriquecidas = unidadesDoPredio.map(a=>Object.assign({},a,{__est:mercadoEstimado(a)}));
const resumo = resumoPredio(enriquecidas);
```

Estado novo em memória (nunca persistido — mesma filosofia de `sel`/`DCUR`):
```js
let CMP=[];        // array de chaves estaveis "ci|insubprinc" das unidades marcadas p/ comparar
let BLDGSTATE={};  // { [ci]: {criterio:"padrao", provaveis:false, busca:""} } — 1 entrada por predio
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resumo do prédio + barra de ordenação/filtro (expande .bldg-head, wiring em render/finish)</name>
  <files>radar-goiania.html</files>
  <action>
**CSS** (bloco `<style>`, próximo às regras de `.bldg-head` linha ~141-147): adicionar
`.bldg-sumario`, `.bldg-metricas`, `.bm`, `.bm-faixa b`, `.bldg-acoes`, `.bldg-ord-toggle`,
`.bldg-zap`, `.bldg-ord`, `.bldg-filtros`, `.bldg-toggle-provaveis`, `.bldg-buscaunidade` EXATAMENTE
conforme as tabelas de estilo do 12-UI-SPEC.md (Componente 1 e Componente 2) — copiar os valores
de `padding`/`gap`/`font`/`color`/`border` literalmente das tabelas "Elemento | Estilo" do spec,
zero hex fora de `var(--ink/--paper/--paper-2/--line/--muted/--accent/--lot)`.

**Estado novo** (junto às demais variáveis globais como `let LAST`/`let sel`): adicionar
`let BLDGSTATE={};` (1 entrada por `ci`: `{criterio:"padrao", provaveis:false, busca:""}`).

**`render(list, opts)`** (linha ~2554): no ponto onde hoje monta o `.bldg-head` (linha ~2575),
substituir a string por uma versão que:
1. Calcula `resumo` via `resumoPredio` sobre as unidades enriquecidas com `__est` (ver
   `<interfaces>`) daquele `ci` — SEMPRE sobre `LAST.filter(a=>clean(a.ci)===ci)` (nunca sobre
   `list` parcial, para o resumo refletir o prédio inteiro mesmo se `list` for um filtro).
2. Formata `nUnidTxt`/`areaMediaTxt`/`venalMedioTxt`/`faixaTxt` a partir de `resumo` (usar
   `brl`/`brlC`/`Math.round` já existentes fora do bloco puro — a formatação de exibição, ao
   contrário do texto copiável de `analisePredicoTexto`, PODE usar `brlC` livremente aqui).
3. Rótulo de "unidades" vs "vagas/boxes": se `resumo.n>0` e TODAS as unidades daquele `ci` são
   `isGarage(a)===true`, usa "vagas/boxes"; senão "unidades" (Estados Transversais do UI-SPEC).
4. Monta o HTML do `.bldg-sumario` + `.bldg-acoes` (botões "🔍 Ordenar / filtrar" com
   `onclick="toggleBldgOrd(this)" aria-expanded="false"` e "💬 Copiar análise do prédio ⧉" com
   `onclick="copyZapPredio('${esc(ci)}')"`) EXATAMENTE conforme o HTML do Componente 1 do UI-SPEC.
5. Monta o HTML do `.bldg-ord` (Componente 2, `hidden` por padrão, `id="bldgOrd-${esc(ci)}"`) com
   os 4 chips de ordenação (`.on`/`aria-pressed` refletindo `BLDGSTATE[ci].criterio` atual — não
   sempre "Padrão", pois o resumo pode re-renderizar após uma ordenação já aplicada), o toggle
   "☐/☑ Só aptos prováveis" (refletindo `BLDGSTATE[ci].provaveis`) e o campo de busca (com
   `value="${esc(BLDGSTATE[ci]?.busca||'')}"` para preservar o texto digitado entre re-renders).
   IMPORTANTE: todo texto interpolado (`ed`, `ci`, valores de `BLDGSTATE`) passa por `esc()`.

**Funções novas** (próximas a `verNoMapa`/`flashList`, mesma região do arquivo):
- `toggleBldgOrd(btn)`: lê `ci` via `btn.closest('.bldg-head').dataset.ci` (mesmo padrão IN-01 de
  `verNoMapa`); localiza `#bldgOrd-${ci}` (usar `document.querySelector` com `clean(ci)` — CUIDADO:
  `ci` pode ter caracteres que quebram seletor CSS, usar `[id="bldgOrd-"+ci]` via `document.getElementById`
  com id sem caracteres especiais OU `querySelector` com `CSS.escape(ci)` se disponível — preferir
  guardar uma referência via `closest`/`nextElementSibling` a partir do próprio `.bldg-head` para
  evitar problema de seletor); alterna `hidden`, sincroniza `aria-expanded` no botão, e ao expandir
  foca o primeiro chip (`.bldg-ord .chips button`).
- `ordenarBldg(ci, criterio)`: `ci=clean(ci);BLDGSTATE[ci]=BLDGSTATE[ci]||{criterio:"padrao",provaveis:false,busca:""};BLDGSTATE[ci].criterio=criterio;`
  monta `enriquecidas` (ver `<interfaces>`), chama `ordenaUnidades(enriquecidas, criterio)`, e
  substitui EM `LAST` o subconjunto daquele `ci` pela nova ordem (mapear de volta para os objetos
  ORIGINAIS de `LAST`, não os enriquecidos com `__est` — `__est` é só para o cálculo, nunca deve
  poluir os objetos reais de `LAST`/`a.__scores`); preserva a posição relativa dos OUTROS prédios
  (se houver mais de um `ci` nos resultados) — estratégia: percorrer `LAST` e para cada índice cujo
  `ci` bate, consumir a próxima unidade da lista reordenada, em sequência. Chama `render(LAST)`
  (sem `stagger`, é reordenação — RENDN aplica o slice sobre o `LAST` já reordenado).
- `toggleAptosProvaveis(btn_ou_ci)`: mesmo padrão de resolver `ci`; alterna
  `BLDGSTATE[ci].provaveis`; ao aplicar o filtro, chama `render(LAST)` — a exibição SÓ dos cards
  daquele prédio que passam `ehAptoProvavel` acontece filtrando dentro de `render()` (ver Task 2,
  que ajusta o loop de `render()` para consultar `BLDGSTATE[ci]` antes de decidir se pula um
  card) — NÃO remove as unidades de `LAST` (filtro é só de EXIBIÇÃO, `LAST` continua completo para
  export CSV/mapa).
- `buscarUnidadeBldg(ci, q)`: `ci=clean(ci);BLDGSTATE[ci]=BLDGSTATE[ci]||{criterio:"padrao",provaveis:false,busca:""};BLDGSTATE[ci].busca=q;render(LAST);`
  (a filtragem por `matchApto` acontece no mesmo ponto de `render()` que consulta `BLDGSTATE`,
  igual ao filtro de aptos prováveis — os dois filtros de exibição combinam com AND quando ambos
  ativos).
- `copyZapPredio(ci)`: `ci=clean(ci);` monta `enriquecidas`, `resumo=resumoPredio(enriquecidas)`,
  `meta={nome: clean(a0.nmedificio)||null, quadra: clean(a0.nrquadra)||null, lote: clean(a0.nrlote)||null, endereco: addrDe(a0)||null}`
  (usar a primeira unidade do prédio, `a0`, para os metadados — mesmo padrão de `showChooser`);
  chama `copyTexto(analisePredicoTexto(resumo,meta), "Copiado! Cole no WhatsApp.")`.

Ajustar `render(list, opts)` para, ao decidir se pula um card daquele `ci` durante o loop
principal, consultar `BLDGSTATE[ci]` — se `provaveis===true` e `!ehAptoProvavel(a)`, pula o card
(não incrementa nada no `cnt`/contagem do resumo, que já foi calculado sobre `LAST` completo antes
do loop, não sobre o filtro de exibição); se `busca` não-vazia e `!matchApto(a, clean(busca))`,
pula igualmente. Ambos os filtros operam SÓ na exibição da lista, nunca no resumo do topo (o
resumo sempre reflete o prédio inteiro, mesmo com filtro de exibição ativo — decisão de design
para o resumo nunca "mentir" sobre o total ao aplicar um filtro temporário de visualização).

**`finish(items, fromMap)`** (linha ~2424): adicionar `CMP=[];BLDGSTATE={};` imediatamente após
`closeChooser();` no início da função (Task 2 adiciona `fecharComparacao()` na mesma linha, ver
abaixo — combinar num único ponto de reset).
  </action>
  <verify>
    <automated>node --test "tests/*.test.mjs" 2>&1 | tail -10</automated>
  </verify>
  <done>Buscar um prédio com múltiplas unidades mostra o resumo (4 métricas) + 2 ações no .bldg-head, antes da lista; expandir "Ordenar/filtrar" mostra os 4 chips + toggle + busca; clicar em qualquer critério de ordenação reordena SÓ aquele prédio dentro de LAST (RENDN/"Mostrar mais" reflete a nova ordem); "Copiar análise do prédio" copia texto formatado (via toast); suite de testes (79+ existentes) permanece verde — nenhuma função pura foi modificada, só consumida.</done>
</task>

<task type="auto">
  <name>Task 2: Marcação para comparar (toggle no card + FAB + sheet de comparação)</name>
  <files>radar-goiania.html</files>
  <action>
**CSS**: adicionar `.cmp-toggle`, `.cmp-toggle[aria-pressed="true"]`, `.cmp-fab`, `.cmp-fab:hover`,
`.wh1-inline`, `.cmp-tablewrap`, `.cmp-table` (+ `th`/`td`/`tfoot`), `.cmp-rowlbl`,
`.cmp-table tbody tr:nth-child(even)`, `.cmp-abrir` EXATAMENTE conforme as tabelas de estilo do
12-UI-SPEC.md (Componentes 3, 4 e 5) — incluindo o ajuste de posição mobile/desktop do `.cmp-fab`
(`bottom:calc(56px+16px+safe-area)` mobile vs `bottom:16px` desktop, mesmo padrão condicional já
usado por `.toast` mobile na media query existente linha ~639). Adicionar `position:relative` à
regra `.card` (linha 167) — pré-requisito para `.cmp-toggle` posicionar-se com `position:absolute`.

**HTML estático** (próximo ao `#chooser`/`#toast` no corpo do documento): adicionar
`<button type="button" id="cmpFab" class="cmp-fab" hidden onclick="abrirComparacao()">⚖️ Comparar
(<span id="cmpFabN">0</span>)</button>` e o `<div class="wiz" id="cmpSheet" hidden role="dialog"
aria-modal="true" aria-label="Comparação de unidades">` (estrutura `.wtop`/`.wh1-inline`/
`.wclose`/`.wbody#cmpBody`) EXATAMENTE conforme o HTML do Componente 5 do UI-SPEC.

**`cardHTML(a, i, isUnit)`** (linha ~2605): adicionar o botão `.cmp-toggle` como PRIMEIRO filho do
`.card`, SÓ quando `isUnit` for truthy: `${isUnit?'<button type="button" class="cmp-toggle"
aria-pressed="'+(cmpTemChave(a)?"true":"false")+'" aria-label="'+(cmpTemChave(a)?"Desmarcar da
comparação":"Marcar para comparar")+'" onclick="event.stopPropagation();toggleComparar('+i+',this)">'
+(cmpTemChave(a)?"☑":"☐")+'</button>':""}` — usar função auxiliar `cmpTemChave(a)` (ver abaixo)
para o card já nascer com o estado correto de marcação ao re-renderizar (ex.: depois de
`ordenarBldg`, os cards marcados continuam mostrando "☑" mesmo com índice `i` diferente).

**Funções novas**:
- `const cmpChave=a=>clean(a.ci)+"|"+String(a.insubprinc||0);` (chave estável, não exportada do
  bloco puro — função de app simples, uma linha).
- `const cmpTemChave=a=>CMP.includes(cmpChave(a));`
- `toggleComparar(i, btn)`: `const a=LAST[i];if(!a)return;const k=cmpChave(a);const idx=CMP.indexOf(k);`
  se `idx>=0`: remove de `CMP` (`CMP.splice(idx,1)`), atualiza `btn` para `aria-pressed="false"`,
  `textContent="☐"`, `aria-label="Marcar para comparar"`. Senão (`idx===-1`): se
  `CMP.length>=4`, chama `toast("Você já marcou 4 — o máximo para comparar. Desmarque uma para
  trocar.")` e RETORNA sem alterar `btn` (5ª tentativa bloqueada, estado do botão inalterado);
  senão, adiciona `k` a `CMP`, atualiza `btn` para `aria-pressed="true"`, `textContent="☑"`,
  `aria-label="Desmarcar da comparação"`. Ao final (se mudou `CMP`), chama `atualizarCmpFab()`.
- `atualizarCmpFab()`: `const fab=document.getElementById("cmpFab");if(!fab)return;
  document.getElementById("cmpFabN").textContent=CMP.length;fab.hidden=CMP.length<2;`
- `abrirComparacao()`: guarda defensiva `if(CMP.length<2)return;` (nunca deveria disparar, FAB só
  aparece com 2+); resolve as unidades marcadas a partir de `LAST` via `cmpChave` (percorrer
  `LAST`, filtrar as que têm `cmpChave(a)` em `CMP`, na ORDEM em que aparecem em `CMP` — para as
  colunas da tabela seguirem a ordem de marcação, não a ordem de `LAST`); monta a tabela HTML
  (thead com `unitLabel`/"Imóvel" por coluna; tbody com as 5 linhas Área/Venal/Estimado/R$/m²/
  Oportunidade, célula "—" quando o dado não existir — usar `venalTxt`/`mercadoEstimado`/`m2Edif`/
  `a.__scores` já existentes, NUNCA disparar `compare()`/`scoreOportunidade` a partir daqui);
  tfoot com botão "Abrir ficha ↗" por coluna (`onclick="fecharComparacao();pick(${gi})"` usando o
  índice REAL em `LAST` de cada unidade, resolvido via `LAST.indexOf(a)`); injeta em
  `#cmpBody`; seta `#cmpSheet` para `hidden=false`; atualiza o texto "Comparação (N)" no
  `.wh1-inline`; foca `.wclose` do sheet. Todo texto interpolado (rótulos de unidade, valores)
  passa por `esc()`.
- `fecharComparacao()`: `document.getElementById("cmpSheet").hidden=true;` — NÃO limpa `CMP`
  (marcações persistem nos cards).

**Cadeia de Esc** (linha ~4217): adicionar o check de `#cmpSheet` na mesma posição estrutural de
`#negSheet`/`#captSheet` (topo da cadeia, ANTES de `#caixaList`): `const cmps=document.getElementById("cmpSheet");
if(cmps&&!cmps.hidden){fecharComparacao();return;}`.

**`finish(items, fromMap)`**: completar o reset iniciado na Task 1 —
`CMP=[];BLDGSTATE={};fecharComparacao();` logo após `closeChooser();` (um único bloco de reset).
Chamar `atualizarCmpFab()` também aqui (garante que o FAB começa oculto em toda busca nova).
  </action>
  <verify>
    <automated>node --test "tests/*.test.mjs" 2>&1 | tail -10</automated>
  </verify>
  <done>Cada card de unidade (dentro de um prédio) mostra o toggle "☐" no canto superior direito; marcar 2 unidades exibe "⚖️ Comparar (2)"; marcar a 5ª mostra o toast de limite e a 5ª permanece desmarcada; "Comparar (N)" abre o sheet com tabela (colunas=unidades marcadas na ordem de marcação, linhas=Área/Venal/Estimado/R$/m²/Oportunidade, "—" honesto onde falta dado); fechar o sheet preserva as marcações; Esc fecha o sheet de comparação com prioridade correta na cadeia; uma nova busca limpa CMP/BLDGSTATE e oculta o FAB; suite de testes permanece verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nome do edifício/endereço (cadastro) -> `.bldg-sumario`/tabela de comparação/texto copiável | mesmo dado já tratado pelo app (Fases 7-11.1) — toda interpolação nova nesta plan passa por `esc()` (IN-01), incluindo dentro de atributos `onclick`/`aria-label` gerados dinamicamente |
| Campo de busca de unidade (`buscarUnidadeBldg`) -> `matchApto` | entrada livre do usuário — reusa `matchApto` já existente (comparação pura, sem `eval`/reconstrução de string JS), nunca interpola `q` bruto em `onclick` inline (mesmo cuidado do CR-01/IN-01 documentado no UI-SPEC) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | Tampering | `ordenarBldg`/`toggleAptosProvaveis` corrompendo `LAST` ao remontar o subconjunto do prédio (perder ou duplicar unidade ao remapear de volta) | mitigate | remapeamento percorre `LAST` sequencialmente por índice, consumindo a lista reordenada em ordem — mesma contagem de entrada/saída verificada visualmente (nº de unidades no resumo não muda após reordenar); teste manual de verificação lista no `<verification>` |
| T-12-05 | Information Disclosure | Tabela de comparação expondo `a.__scores`/estimativas de uma unidade cuja ficha nunca foi aberta, sem disparar rede (comportamento correto) mas podendo confundir se a UI não deixar claro que é "—" por ausência de cálculo, não por erro | accept | UI-SPEC já define "—" honesto para célula sem dado — mesma convenção usada em todo o app (`venalTxt`, `mercadoEstimado` null) |
| T-12-06 | Denial of Service (local) | `CMP` crescendo sem limite se `toggleComparar` falhar em bloquear a 5ª marcação (ex.: race condition de duplo-clique) | mitigate | guarda `CMP.length>=4` verificada ANTES de qualquer `push`, com `return` imediato — nenhuma janela de duplo-clique porque a operação é síncrona (sem `await` entre a checagem e o `push`) |

**Sem ameaças de alta severidade.** Toda a superfície nova é client-side sobre dados já carregados; nenhuma nova requisição de rede, nenhum novo campo de PII, nenhuma nova superfície de persistência.
</threat_model>

<verification>
- Buscar um prédio com múltiplas unidades mostra o resumo completo (4 métricas) + 2 ações, ANTES da lista de cards (DevTools Network: zero request novo ao carregar o resumo)
- Clicar "🔍 Ordenar / filtrar" expande a barra com 4 chips + toggle + busca, todos com `min-height:44px` (medir em DevTools mobile 375)
- Clicar "Maior oportunidade"/"Menor estimado"/"Maior área" reordena a lista SEM disparar requisição de rede nova (Network tab); "Mostrar mais" após reordenar mostra a continuação da lista JÁ reordenada, nunca a ordem antiga
- Ativar "Só aptos prováveis" oculta garagem/box e uso não-residencial daquele prédio; campo de busca filtra por `matchApto`; resumo do topo continua mostrando o total REAL do prédio (não o total filtrado)
- Marcar 2, 3, 4 unidades atualiza o contador do FAB; tentar marcar a 5ª mostra o toast de limite e mantém a 5ª desmarcada
- Reordenar/filtrar o prédio DEPOIS de marcar unidades preserva as marcações (chave `ci|insubprinc`, não índice)
- Abrir o sheet de comparação mostra a tabela com "—" honesto em células sem dado; no mobile 375 com 3-4 colunas, a TABELA rola horizontalmente dentro do sheet (a página não rola horizontal); coluna de rótulos fixa à esquerda
- Fechar o sheet (× ou Esc) preserva `CMP`; nova busca (`buscar()`) limpa `CMP`/`BLDGSTATE` e oculta o FAB
- `node --test "tests/*.test.mjs"` permanece 100% verde (79+ testes) — nenhuma função pura foi alterada nesta wave
- Guardas herdadas intactas: `scrollToResults()` mobile, `venalTxt` "não informado", `esc()` em toda interpolação nova, `44px` em todo elemento tocável novo, zero hex fora da paleta declarada
</verification>

<success_criteria>
- PRED-01 cumprido: resumo do prédio visível antes da lista, com ações "ver unidades" (implícito — lista já está embaixo), "gerar análise do prédio" (copiável) — critério de sucesso 1 do ROADMAP atendido
- PRED-02 cumprido: ordenação (oportunidade/valor/área) + filtros (garagem/aptos prováveis/busca) + marcação para comparação até 4 — critério de sucesso 2 do ROADMAP atendido
- Guarda do critério de sucesso 3 do ROADMAP (mobile: lista não some atrás do form; garagem = "não informado") verificada sem regressão
- Zero requisição de rede nova em qualquer interação desta fase (ordenar/filtrar/marcar/comparar) — confirmado via DevTools Network
</success_criteria>

<output>
Após completar, criar `.planning/phases/12-predio-como-objeto-comercial/12-02-SUMMARY.md`.
Incluir no SUMMARY: lista de funções de UI novas com assinatura, confirmação de que
`ordenaUnidades`/`resumoPredio`/`ehAptoProvavel`/`analisePredicoTexto` (12-01) foram consumidas sem
modificação, resultado da suite completa de testes, e uma nota explícita sobre a interação
ordenação↔RENDN (como o remapeamento em `LAST` garante que "Mostrar mais" sempre reflita a ordem
mais recente).
</output>
