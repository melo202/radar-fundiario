---
phase: 08-busca-unica-inteligente
plan: 05
type: execute
wave: 5
depends_on: ["08-04"]
files_modified:
  - radar-goiania.html
autonomous: true
requirements: [BUSCA-09, BUSCA-12, BUSCA-13, BUSCA-14]
tags: [deep-link, geo-paste, voice, error-states, a11y-audit, desktop-guard, data-audit]

must_haves:
  truths:
    - "?insc= no boot abre o imovel automaticamente (apos initMap, sem esperar loadBairros); botao copiar link do imovel monta a URL e usa navigator.clipboard"
    - "Colar um link do Google Maps ou coordenada decimal na caixa unica extrai lat/lon e cai no mesmo caminho de identificacao de lote do clique-no-mapa (onMapClick), sem geocoding externo"
    - "Botao de voz (webkitSpeechRecognition) so aparece se a API existir no navegador; erro de permissao mostra toast com proximo passo; resultado cai na caixa e roda detectMode normal"
    - "Erros/vazios de cada modo oferecem 1 acao de proximo passo (ex.: 'Buscar como Predio'); a checklist de a11y de 03/07 (ARIA combobox/activedescendant, teclado, live regions, foco, SEARCHTOKEN, pointerdown iOS) re-passa sem regressao"
    - "Coordenacao busca<->ficha no desktop (guarda a7a4646) nao regride: busca sempre fechavel (x+Esc), abrir ficha/seletor fecha o overlay, prédio abre seletor sobre o mapa, zero sobreposicao >=821px"
    - "Auditoria dos dados da ficha em TODOS os modos (ql/addr/bd/insc10/insc14/clique-no-mapa) confere contra uma amostra real do endpoint, com evidencia registrada"
  artifacts:
    - path: "radar-goiania.html"
      provides: "Deep-link ?insc= no boot + botao copiar link no #detail; parser de link/coordenada do Maps reusando onMapClick; botao de voz feature-detected; mensagens de erro com botao de proxima acao por modo"
      contains: "URLSearchParams"
  key_links:
    - from: "boot (apos initMap)"
      to: "buscar()/finish()"
      via: "URLSearchParams(location.search).get('insc') preenche #insc, forca MODE=insc, chama buscar()"
      pattern: "URLSearchParams"
    - from: "caixa unica (colar texto)"
      to: "onMapClick (reuso do caminho de identificacao por ponto)"
      via: "parser extrai lat/lon de link do Maps ou coordenada decimal e chama a mesma logica de consulta espacial"
      pattern: "onMapClick"
    - from: "finish() (vazio) / #detail (copiar link)"
      to: "botao de proxima acao / clipboard"
      via: "mensagem de vazio por modo com botao de acao; #detail monta location.origin+pathname+?insc=...+clipboard.writeText"
      pattern: "clipboard.writeText"
---

<objective>
Fechar a fase: deep-link `?insc=` no boot + botao "copiar link do imovel", parser que aceita link do Google Maps ou coordenada decimal colada na caixa unica (cai no mesmo caminho de identificacao de lote do clique-no-mapa, sem geocoding externo), botao de voz best-effort (Web Speech API, degrada silenciosamente), mensagens de erro/vazio com 1 acao de proximo passo por modo, e o gate final da fase: re-passar a checklist de a11y de 03/07, confirmar que a coordenacao busca<->ficha do desktop (hotfix `a7a4646`) nao regrediu, e auditar os dados da ficha em TODOS os modos contra uma amostra real do endpoint.

Purpose: sao os itens P1/gate da fase (BUSCA-09/12/13/14) mais os 3 ultimos criterios de sucesso do ROADMAP (6-8) — sem isso a fase entrega a UI (08-04) mas nao fecha as garantias de nao-regressao que o hotfix `a7a4646` e a auditoria 03/07 ja estabeleceram.
Output: `radar-goiania.html` com deep-link/copiar-link, parser de link do Maps/coordenada, voz best-effort, erros com proxima acao; evidencia registrada (no SUMMARY) da re-passagem da checklist de a11y, da guarda do desktop, e da auditoria de dados por modo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-busca-unica-inteligente/08-CONTEXT.md
@.planning/phases/08-busca-unica-inteligente/08-UI-SPEC.md
@.planning/research/v2.1/SEARCH.md
@.planning/phases/08-busca-unica-inteligente/08-04-SUMMARY.md

# Fonte de codigo (ler antes de editar)
@radar-goiania.html

<facts>
<!-- SEARCH.md secao 5 (deep-link) + secao 3.6 (erros) + CONTEXT.md (voz/Maps) + hotfix a7a4646 (guarda desktop). -->

Boot ATUAL (radar-goiania.html, linhas ~2158-2165):
```js
document.body.dataset.view="mapa";
setMode(MODE); /* pos-08-04: substituido por inicializacao equivalente da caixa unica */
if(!window.L||!window.proj4){ ...erro... }
else{initMap();setView("mapa");initCoach();loadBairros();loadBairroPolys();initCaixa();}
```
`initMap()` e SINCRONA; `loadBairros()` e async e NAO aguardada. O modo `insc` NAO depende de `COMBO` — so
depende de `map` estar pronto para `finish()`/`plot()` funcionarem. Ponto seguro para o deep-link: IMEDIATAMENTE
apos `initMap()`, sem esperar `loadBairros()`.

Deep-link (SEARCH.md secao 5) — adicionar apos `initMap()`:
```js
const qs = new URLSearchParams(location.search);
const inscParam = qs.get("insc");
if(inscParam){
  document.getElementById("insc").value = inscParam.replace(/\D/g,"");
  MODE = "insc"; // ou forceMode-like sem passar por setMode se a caixa unica nao expuser mais os paineis
  buscar();
}
```
Erro (inscricao nao encontrada): reusar o toast/estado vazio ja existente em `finish()` — NAO precisa de mensagem
nova.

Botao "Copiar link deste imovel" (SEARCH.md secao 5) — no painel de detalhe (`#detail`, ao lado das acoes
existentes `#dActs`), mesmo padrao ja usado para copiar a inscricao (buscar `copyInsc` ou equivalente no codigo
atual — grep por "clipboard" ou "copyInsc" antes de implementar, para reusar o MESMO padrao de toast de
confirmacao "Link copiado"):
```js
function copyLink(insc){
  const url = location.origin + location.pathname + "?insc=" + encodeURIComponent(insc);
  navigator.clipboard.writeText(url).then(()=>toast("Link copiado.")).catch(()=>toast("Não foi possível copiar o link."));
}
```

Colar link do Google Maps/coordenada (CONTEXT.md Claude's Discretion): aceitar formatos comuns:
  `@lat,lng` (ex.: "https://www.google.com/maps/@-16.6799,-49.2550,15z")
  `q=lat,lng` (ex.: "https://maps.google.com/?q=-16.6799,-49.2550")
  "lat, lng" decimal cru (ex.: "-16.6799, -49.2550")
Parser (funcao pura, testavel — mas SEM criar um tests/*.mjs novo nesta plan a menos que necessario; se preferir
testar via node --check + assert de regex no proprio grep de verify, e aceitavel dado o escopo pequeno):
```js
function parseGeoPaste(text){
  const s = String(text||"").trim();
  let m = s.match(/@(-?\d+\.\d+),(-?\d+\.\d+)/) || s.match(/[?&]q=(-?\d+\.\d+),(-?\d+\.\d+)/) || s.match(/^(-?\d+\.\d+)[,\s]+(-?\d+\.\d+)$/);
  if(!m) return null;
  const lat=parseFloat(m[1]), lng=parseFloat(m[2]);
  if(Number.isNaN(lat)||Number.isNaN(lng)) return null;
  if(Math.abs(lat)>90||Math.abs(lng)>180) return null;
  return {lat,lng};
}
```
Cair no MESMO caminho de `onMapClick` (radar-goiania.html ~linha 1005-1019) — reusar a consulta espacial por
ponto (`proj4` conversao + `jsonp` com `geometryType:"esriGeometryPoint"`), NAO duplicar a logica: extrair o corpo
de `onMapClick` que faz a consulta a partir de `{lat,lng}` para uma funcao auxiliar reusavel por AMBOS os
caminhos (clique real no mapa E colar coordenada na caixa), ou chamar `onMapClick({latlng:{lat,lng}})` diretamente
se a assinatura permitir (confirmar no codigo atual antes de decidir).

Voz (CONTEXT.md, Component 1 do UI-SPEC): botao `#caixaVoz` (ja no HTML da 08-04, `style="display:none"` por
padrao) — no boot, se `window.webkitSpeechRecognition` existir, mostrar o botao (`style.display="flex"`). Ao
clicar: instanciar `new webkitSpeechRecognition()`, `lang="pt-BR"`, `onresult` cai o texto reconhecido em
`#caixaInput.value` e dispara o MESMO fluxo do listener de input (detectMode). `onerror`: toast "Não foi possível
usar o microfone. Digite sua busca." (copy exata do 08-UI-SPEC.md). Estado ativo (gravando): `background:var(--accent)`
+ `aria-pressed="true"` no botao (sem hex novo, sem trocar emoji).

Erros/vazios com proxima acao (SEARCH.md secao 3.6, ja mapeado nos 3 pontos reais do codigo):
  finish() vazio (linha ~1303-1309): mensagens ja diferenciadas por modo — ADICIONAR botao de acao dentro da
    mensagem HTML: se MODE==="addr" vazio, `<button onclick="forceMode('bd');document.getElementById('predio').value=...;buscar()">Buscar como Prédio</button>`
    reaproveitando o texto ja digitado (a rua digitada vira o nome de predio a tentar). Se MODE==="bd" vazio,
    botao "Tentar outro nome" que so foca a caixa unica (`#caixaInput.focus()`).
  buscar() catch (linha ~1281-1283): erro de rede — reusar toast existente, ADICIONAR botao "Tentar de novo" que
    rechama `buscar()` com o mesmo estado (o `#toast` atual e so texto; se adicionar um botao exigir estrutura
    nova, avaliar se cabe no componente de toast existente ou se a acao deve viver na mensagem de `#results` em
    vez do toast — decisao a criterio do executor, documentar no SUMMARY).

Guarda do hotfix a7a4646 (NAO regredir, ja documentado no commit): #pillClose sem style inline (CSS controla via
media query); pick()/showChooser() sempre fecham a busca ANTES de abrir detalhe/seletor no desktop; finish() com
toast desktop correto. Apos 08-04 ter reescrito boa parte do HTML/JS do painel de busca, este plano CONFIRMA
(nao reimplementa) que esses 3 pontos continuam intactos — ler o diff de a7a4646 (`git show a7a4646`) e comparar
com o estado atual antes de declarar o gate fechado.

Checklist de a11y 03/07 (SEARCH.md secao 1.6) a re-passar: label for= em todos os inputs; combobox role=combobox
aria-expanded/aria-controls/aria-autocomplete; role=option aria-selected id=opt-<code> com aria-activedescendant;
aria-pressed sincronizado nos botoes de modo; Esc fecha na ordem correta; iOS pointerdown (nao click); font-size:16px
nos inputs (antizoom); SEARCHTOKEN em todo caminho assincrono novo (incluindo o parser de geo-paste se ele disparar
fetch/jsonp).

Auditoria de dados por modo (ROADMAP criterio 8) — para CADA modo (ql, addr, bd, insc10, insc14, clique-no-mapa),
executar uma busca real contra o endpoint (`SVC`/`LOTSVC`/`BAISVC`, ja definidos no arquivo) e confirmar que os
campos exibidos na ficha (`#detail`) — endereco, area, valor venal, uso, inscricao — batem com o registro de
origem (a mesma chamada JSONP pode ser inspecionada via DevTools Network ou reproduzida com um script Python/node
simples que bate no mesmo endpoint e compara). Registrar evidencia (prints/JSON comparado) no SUMMARY — esta
plan documenta o metodo e o resultado, nao precisa de um script permanente no repo.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deep-link ?insc= + copiar link + parser de Maps/coordenada (reusa onMapClick) + voz best-effort</name>
  <files>radar-goiania.html</files>
  <action>
1. **Deep-link**: apos `initMap()` no boot (ver `<facts>`), ler `URLSearchParams(location.search).get("insc")`;
   se presente, preencher `#insc`, forcar o modo inscricao (reusando a via mais direta disponivel pos-08-04 —
   `forceMode('insc')` se essa funcao aceitar preencher o campo, ou equivalente), e chamar `buscar()`. Erro
   (inscricao nao encontrada) usa o toast/estado vazio JA existente em `finish()` — nao criar mensagem nova.

2. **Copiar link**: adicionar `function copyLink(insc)` (ver `<facts>`) e um botao no `#detail` (perto das acoes
   existentes) que chama `copyLink(clean(a.ci))` ou o campo de inscricao relevante do item aberto. Reusar o MESMO
   padrao de toast de confirmacao ja usado para copiar a inscricao (localizar o botao/funcao existente antes de
   duplicar estilo/comportamento).

3. **Parser de link do Maps/coordenada** (BUSCA-13): implementar `parseGeoPaste(text)` (ver `<facts>`, funcao
   pura). No listener de `input`/`paste` do `#caixaInput`, se o texto colado casar o parser (retorna
   `{lat,lng}` != null), NAO rodar `detectMode()` normal — em vez disso, reusar o caminho de identificacao por
   ponto (extrair a logica de `onMapClick` para uma funcao auxiliar chamavel com `{lat,lng}` direto, se a
   assinatura atual nao permitir reuso trivial) e chamar essa consulta espacial, aplicando `SEARCHTOKEN` (mesma
   disciplina de concorrencia ja usada em `onMapClick`/`loadCi`). Sem geocoding externo — so extracao de
   coordenada literal do texto.

4. **Voz best-effort** (BUSCA-14): no boot, `if(window.webkitSpeechRecognition) document.getElementById("caixaVoz").style.display="flex";`
   (o botao ja existe no HTML da 08-04 com `display:none` por padrao). Ao clicar: instanciar o
   `SpeechRecognition`, `lang="pt-BR"`, `onresult` preenche `#caixaInput.value` e dispara o mesmo fluxo do
   listener de input (chamar a funcao de deteccao diretamente, nao esperar o debounce). `onerror`/permissao
   negada: `toast("Não foi possível usar o microfone. Digite sua busca.")`. Estado ativo (gravando):
   `background:var(--accent)` + `aria-pressed="true"` no botao — sem hex novo, sem novo emoji.
  </action>
  <verify>
    <automated>node --check radar-goiania.html && python -c "
s=open('radar-goiania.html',encoding='utf-8').read()
for needle in ['URLSearchParams','function copyLink','function parseGeoPaste','webkitSpeechRecognition','SEARCHTOKEN']:
    assert needle in s, needle+' ausente'
assert 'insc' in s[s.index('URLSearchParams'):s.index('URLSearchParams')+400].lower()
print('OK: deep-link, copiar link, parser de geo-paste e voz best-effort presentes')
"</automated>
  </verify>
  <done>?insc= no boot abre o imovel (apos initMap, sem esperar loadBairros); botao copiar link presente no #detail; parseGeoPaste extrai lat/lon de link do Maps ou coordenada decimal e cai no caminho de onMapClick com SEARCHTOKEN; botao de voz aparece so com API presente, erro de permissao mostra toast com proximo passo.</done>
</task>

<task type="auto">
  <name>Task 2: Erros com proxima acao por modo + confirmacao da guarda desktop (a7a4646) + re-passagem de a11y</name>
  <files>radar-goiania.html</files>
  <action>
1. Em `finish()` (mensagem de vazio, linha ~1303-1309), adicionar o botao de proxima acao dentro do HTML da
   mensagem conforme `<facts>`: MODE="addr" vazio -> botao "Buscar como Prédio" (reusa o texto da rua digitada
   como nome de predio, chama `forceMode('bd')` + preenche + `buscar()`); MODE="bd" vazio -> botao "Tentar outro
   nome" (foca `#caixaInput`). Em `buscar()` catch (erro de rede, linha ~1281-1283), avaliar e implementar a
   forma de oferecer "Tentar de novo" (toast atual ou mensagem em `#results` — decisao documentada no SUMMARY).

2. **Confirmar (nao reimplementar) a guarda do hotfix `a7a4646`**: rodar `git show a7a4646 --stat` e comparar os
   3 pontos do diff (`#pillClose` sem style inline; `pick()`/`showChooser()` sempre fecham a busca antes de abrir
   detalhe/seletor no desktop; toast desktop de `finish()`) contra o estado ATUAL do arquivo pos-08-04. Se algum
   ponto tiver regredido durante a reescrita da 08-04 (ex.: um novo caminho que abre a ficha sem fechar o overlay
   da caixa unica), CORRIGIR aqui. Se estiver intacto, apenas confirmar e documentar no SUMMARY (nao e necessario
   reescrever codigo que ja esta correto).

3. Re-passar mentalmente a checklist de a11y 03/07 (ver `<facts>`) sobre TODO o codigo novo das plans 08-01 a
   08-05: `label for=`, `role=combobox`/`role=option`/`aria-activedescendant`, `aria-pressed`, ordem do Esc,
   `pointerdown` no iOS, `font-size:16px`, `SEARCHTOKEN` em caminhos assincronos novos (incluindo o parser de
   geo-paste da Task 1 e o lazy-load do CNEFE da 08-04). Corrigir qualquer lacuna encontrada.
  </action>
  <verify>
    <automated>node --check radar-goiania.html && python -c "
s=open('radar-goiania.html',encoding='utf-8').read()
assert 'Buscar como Pr' in s, 'botao de proxima acao (addr vazio) ausente'
i=s.index('id=\"pillClose\"')
frag=s[max(0,i-20):i+200]
assert 'style=\"display' not in frag, 'pillClose nao deve ter style inline (guarda a7a4646)'
print('OK: proxima acao por modo presente; pillClose sem style inline (guarda a7a4646 intacta)')
"</automated>
  </verify>
  <done>Mensagens de vazio (addr/bd) oferecem botao de proxima acao; guarda a7a4646 confirmada intacta (ou corrigida se regrediu) e documentada no SUMMARY; checklist de a11y 03/07 re-passada sobre todo o codigo novo (08-01 a 08-05), lacunas corrigidas.</done>
</task>

<task type="auto">
  <name>Task 3: Auditoria de dados da ficha em todos os modos (criterio de sucesso 8 do ROADMAP)</name>
  <files>radar-goiania.html</files>
  <action>
Para CADA um dos 6 modos (`ql`, `addr`, `bd`, `insc` 10 digitos, `insc` 14 digitos, clique-no-mapa/`onMapClick`),
executar uma busca REAL contra o endpoint (usar a app servida localmente, ou reproduzir a mesma chamada JSONP via
um script Python/Node ad-hoc que bate em `SVC`/`LOTSVC`/`BAISVC` com um `where` equivalente) e comparar os campos
exibidos na ficha (`#detail`) contra o registro de origem retornado pelo endpoint:
- endereco (`nmlogradou`+`nrimovel` via `logradouroDe`/`addrDe`)
- area terreno/edificada (`areaterr`/`areaedif`)
- valor venal (`vlvenal`, incluindo o caso 0/ausente -> "não informado" via `venalTxt`, sem regressao)
- uso (`USO[a.uso]`)
- inscricao (`ci`/`nrinscr` conforme o modo)

Escolher pelo menos 1 caso real por modo (ex.: um setor conhecido para `ql`/`addr`, um predio conhecido para `bd`,
uma inscricao real de 10 e de 14 digitos, um ponto clicavel real para o clique-no-mapa). Se algum campo NAO bater
(dado deturpado pelo caminho novo da caixa unica — ex.: `__matchScore`/selo aproximado vazando para o `#detail`,
ou `venalTxt` quebrado por algum wiring da 08-04), CORRIGIR nesta task.

Documentar cada um dos 6 casos no SUMMARY: modo testado, input usado, campos comparados, resultado (bateu/nao
bateu), e a correcao aplicada se necessario. Esta e a evidencia que fecha o criterio de sucesso 8 do ROADMAP —
sem ela a fase nao pode ser considerada verificada.
  </action>
  <verify>
    <automated>node --check radar-goiania.html && python -c "
s=open('radar-goiania.html',encoding='utf-8').read()
assert 'venalTxt' in s, 'venalTxt (venal 0 = nao informado) deve permanecer intacto'
assert '__matchScore' not in s[s.index('function esc('):s.index('function esc(')+2000] if 'function esc(' in s else True
print('OK: venalTxt intacto; verificacao estrutural minima passou (auditoria de dados por modo documentada no SUMMARY, nao automatizavel via grep)')
"</automated>
  </verify>
  <done>Os 6 modos (ql/addr/bd/insc10/insc14/clique-no-mapa) foram testados contra o endpoint real com pelo menos 1 caso cada; campos da ficha (endereco/area/valor/uso/inscricao) confirmados corretos ou corrigidos; evidencia registrada no SUMMARY — criterio de sucesso 8 do ROADMAP fechado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ?insc= (query string, controlada pelo usuario/link compartilhado) -> buscar() | mesmo caminho ja validado de buscar() MODE=insc; input e so digitos apos sanitizacao |
| texto colado na caixa (parseGeoPaste) -> consulta espacial (ArcGIS) | extracao de coordenada literal, sem geocoding externo, sem chamada a servico de terceiros novo |
| Web Speech API (voz) -> #caixaInput | resultado de reconhecimento de voz tratado como texto livre, passa pelo MESMO detectMode do texto digitado — nenhum privilegio extra |
| #detail -> clipboard (copiar link) | grava so a URL com o parametro insc (dado publico do cadastro), nenhum PII |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-12 | Tampering | ?insc= arbitrario no boot poderia ser usado para tentar valores nao numericos | mitigate | `.replace(/\D/g,"")` sanitiza antes de preencher `#insc`; caminho de busca identico ao formulario manual (mesmas validacoes de `buscar()`) |
| T-08-13 | Denial of Service (UX) | parseGeoPaste mal calibrado poderia disparar consulta espacial para coordenadas fora de Goiania | accept | Validacao de range (`Math.abs(lat)>90||Math.abs(lng)>180`) rejeita entradas invalidas; consulta fora de Goiania so retorna "nenhum lote nesse ponto" (mesma UX do clique real fora da malha) |
| T-08-14 | Information Disclosure | Web Speech API envia audio a um servico do navegador (Google, no Chrome) para transcricao | accept | Comportamento nativo do navegador, fora do controle da aplicacao; feature best-effort e opt-in (usuario clica no botao); documentado como tal no UI-SPEC |
| T-08-15 | Repudiation | regressao silenciosa da guarda a7a4646 apos a reescrita da 08-04 | mitigate | Task 2 exige confirmacao explicita (diff comparado) antes de declarar o gate fechado; verify assert que #pillClose nao tem style inline |

**Sem ameacas de alta severidade.** Todas as features novas desta plan operam sobre o mesmo caminho de consulta ja auditado (buscar()/onMapClick), sem novo servico externo (exceto o proprio Web Speech API do navegador, que e opt-in e nao envolve dado do cadastro).
</threat_model>

<verification>
- node --check radar-goiania.html passa nas 3 tasks
- Deep-link ?insc= funcional; copiar link usa clipboard; parseGeoPaste cai no caminho de onMapClick com SEARCHTOKEN
- Voz aparece so com API presente; erro de permissao com proximo passo
- Erros de addr/bd vazios oferecem botao de proxima acao
- Guarda a7a4646 confirmada intacta (pillClose sem style inline, busca sempre fechavel, zero sobreposicao desktop)
- Checklist de a11y 03/07 re-passada sobre todo o codigo novo da fase
- Auditoria de dados dos 6 modos documentada no SUMMARY com evidencia (bateu/nao bateu + correcao se aplicavel)
</verification>

<success_criteria>
- BUSCA-09 (deep-link + copiar link), BUSCA-12 (guarda desktop + auditoria de dados), BUSCA-13 (colar link/coordenada), BUSCA-14 (voz) entregues
- Criterios de sucesso 6, 7 e 8 do ROADMAP fechados com evidencia: a11y re-passada, coordenacao desktop sem regressao, dados auditados em todos os modos
- Fase 8 completa: caixa unica inteligente substitui a moderow sem perder capacidade nem acessibilidade
</success_criteria>

<output>
Apos completar, criar `.planning/phases/08-busca-unica-inteligente/08-05-SUMMARY.md`.
Incluir no SUMMARY: os 6 casos de auditoria de dados (modo/input/campos/resultado/correcao), a confirmacao explicita
da guarda a7a4646 (com referencia ao diff comparado), a lista de itens da checklist de a11y 03/07 re-verificados,
e qualquer decisao de discricao tomada (ex.: onde o botao "Tentar de novo" de erro de rede foi colocado). Este e o
SUMMARY que fecha a Fase 8 — apontar claramente se algum criterio de sucesso do ROADMAP ficou pendente para a
verificacao humana (UAT) em vez de fechado aqui.
</output>
