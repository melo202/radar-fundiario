---
phase: 13-refino-visual-pinos-motion-descoberta
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - radar-goiania.html
autonomous: true
requirements: [PIN-01, MOT-01]
tags: [pinos, mapa, motion, skeleton, leaflet, a11y]

must_haves:
  truths:
    - "Ao plotar uma busca, cada pino nasce com a cor do status via statusDeUnidade(a.__scores) — bom=verde/atencao=amarelo/risco=vermelho/semdado=cinza; pino de imovel da lista CAIXA permanece dourado (camada separada, inalterada)"
    - "Abrir a ficha de uma unidade (showDetail/atualizarScores) recalcula a.__scores e a proxima vez que plot() roda esse pino aparece na cor correta — sem requisicao de rede nova disparada so para colorir pino"
    - "Pino cinza (semdado) continua clicavel — onclick inalterado, abre ficha/seletor normalmente"
    - "A legenda .pino-legenda aparece automaticamente (hidden removido) quando ha pinos plotados (list.length>0) e fica oculta quando a lista plotada e vazia ou antes de qualquer busca"
    - "Cada circleMarker tem bindTooltip com texto '${statusLabel} - ${endereco ou unitLabel}', escapado, garantindo que o status nunca dependa so da cor no hover do mapa"
    - "buscar() dispara a sequencia real de mensagens 'Localizando imovel...' -> 'Consultando cadastro...' -> 'Calculando estimativa...' -> (condicional) 'Buscando comparaveis...' -> 'Preparando mapa...' atreladas as fases REAIS, nunca por setTimeout decorativo"
    - "Skeleton shimmer (.skel-card/.skel-line) aparece em #results durante o intervalo antes do render() real; sob prefers-reduced-motion fica estatico (sem animation) mas continua visivel"
  artifacts:
    - path: "radar-goiania.html"
      provides: "PINO_STYLE + statusPino() + legenda #pinoLegenda + MOTION_MSG + chamadas de loading() nos pontos reais de buscar()/finish()/plot() + CSS .skel-card/.skel-line com REDUCE-safe"
      contains: "PINO_STYLE"
  key_links:
    - from: "plot(list)"
      to: "statusDeUnidade (RADAR_PURE, 13-01)"
      via: "statusPino(a) chama statusDeUnidade(a.__scores) para decidir PINO_STYLE"
      pattern: "statusDeUnidade"
    - from: "pick(i) desselecionar"
      to: "PINO_STYLE[statusPino(a)]"
      via: "setStyle ao voltar do hover/selecao usa o status real, nunca mais o fillColor fixo antigo"
      pattern: "PINO_STYLE\\["
    - from: "buscar()/finish()/plot()"
      to: "loading(true, MOTION_MSG.*)"
      via: "cada fase real dispara a mensagem correspondente, substituindo as mensagens especificas por modo"
      pattern: "MOTION_MSG\\."
    - from: "#results (buscar, inicio)"
      to: ".skel-card (removido por finish()/render() real)"
      via: "innerHTML de skeleton no inicio, substituido pelo render() real ou pelo bloco de erro do catch"
      pattern: "skel-card"
---

<objective>
Tornar os pinos do mapa semânticos (PIN-01): `plot()` passa a mapear o status real da unidade (via `statusDeUnidade`, criada em 13-01) para cor/raio do marker, com legenda sempre-visível (com ícone+texto, nunca cor sozinha) e tooltip acessível. Implementar o motion de busca em etapas (MOT-01): `loading()` recebe as 5 mensagens literais do `13-UI-SPEC.md` §Componente 2, disparadas pelas fases REAIS de `buscar()`/`finish()`/`plot()` — nunca por timer decorativo — mais o skeleton shimmer CSS-only (REDUCE-safe) nos cards da lista de resultados.

Purpose: PIN-01/MOT-01 exigem que o mapa comunique valor+oportunidade visualmente (cor reservada a status, nunca decorativa) e que a busca comunique progresso real (não "teatrinho de timer"). Esta plan consome `statusDeUnidade` da Wave 1 (13-01) sem reabri-la — contrato já travado.
Output: `plot()` plota pinos coloridos por status com legenda condicional e tooltip; `pick()` restaura o status correto ao desselecionar; `buscar()` mostra a sequência real de 5 mensagens; skeleton shimmer aparece durante buscas que demoram, estático sob `prefers-reduced-motion`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-refino-visual-pinos-motion-descoberta/13-CONTEXT.md
@.planning/phases/13-refino-visual-pinos-motion-descoberta/13-UI-SPEC.md
@.planning/phases/13-refino-visual-pinos-motion-descoberta/13-01-SUMMARY.md

# Fonte de código (ler antes de editar)
@radar-goiania.html

<facts>
<!-- Verificado por leitura direta deste repo nesta sessão. -->

**`plot(list)`** (linha 3015-3042): hoje cria `L.circleMarker(ll,{radius:8,color:"#141a1f",
weight:2,fillColor:"#b5451f",fillOpacity:.9,bubblingMouseEvents:false})` para TODO pino,
independente de status. `byCi[ci]` agrupa por lote (1 marker por CI, mesmo com várias unidades —
"pino de PRÉDIO usa o MELHOR status entre as unidades", ver UI-SPEC). O click já roteia
`predio?showChooser(ci):pick(idx)` — inalterado.

**`pick(i)`** (linha 3096-~3103): ao selecionar, roda
`markers.forEach(m=>{if(m&&!done.has(m)){done.add(m);m.setStyle({radius:m===selMk?12:8,
fillColor:m===selMk?"#c9691f":"#b5451f"});}})` — a cor de DESSELECIONAR hoje é sempre o fixo
`"#b5451f"`. Esta plan troca esse fixo por `PINO_STYLE[statusPino(a)].fillColor` (a cor de
SELECIONADO `"#c9691f"` — accent-2 — permanece INALTERADA, é ortogonal ao status, conforme
UI-SPEC).

**`a.__scores`** é populado em DOIS pontos: `showDetail()` (linha 3199, ao abrir a ficha, ANTES
de `compare()` popular stats — `op` pode ser `null` aqui) e `atualizarScores()` (linha 3158,
DEPOIS de `compare()`/`compsStats()` rodar, com stats reais de vizinhança). `plot()` roda uma vez
por busca (`finish()` chama `plot(units)` na linha 2626), então a MAIORIA dos pinos nasce sem
`__scores` (`'semdado'`) na primeira renderização — comportamento ESPERADO e documentado no
UI-SPEC ("a maioria dos pinos de uma busca nova nasce cinza"). Não há necessidade de re-plotar
automaticamente ao abrir uma ficha — mas ESTA plan garante que a PRÓXIMA chamada de `plot()`
(nova busca) já leria o `__scores` atualizado se a mesma unidade reaparecesse.

**Camada CAIXA** (`toggleCaixa()`, linha 4497-4516; markers em linha 4504) é uma
`L.layerGroup()` SEPARADA (`caixaLayer`), com seu próprio `circleMarker` dourado
(`fillColor:"#a8842c"`) — esta plan NÃO toca essa função, apenas documenta que ela já usa o hex
que agora é `--status-caixa`. Zero mudança de código na camada CAIXA.

**`loading(on,msg)`** (linha 2425-2426): assinatura INALTERADA, aceita `(on, msg)`, seta
`#loadmsg.textContent` se `msg` truthy e faz `.classList.toggle("show",on)`. Chamado hoje em
`buscar()` com mensagens específicas por modo: `"consultando inscrição…"` (linha 2471),
`"consultando o endereço…"` (linha 2483), `"número não achado — varrendo a rua…"` (linha 2505,
fallback de ERRO — PERMANECE inalterado, não é uma das 5 mensagens felizes), `"procurando o
edifício…"` (linha 2526), `"consultando a quadra…"` (linha 2543). Estas 4 mensagens de
`loading(true, "...")` (exceto o fallback de erro) são as que esta plan SUBSTITUI pela mensagem 1
única (`"Localizando imóvel…"`), disparada ANTES do primeiro branch de `MODE`.

**`buscar()`** (linha 2461-2577) estrutura: `try{ ... if(MODE==="insc"){...}else if(MODE==="addr")
{...}else if(MODE==="bd"){...}else{ /* ql */ ...} if(tk!==SEARCHTOKEN)return; finish(items);
...}catch(e){...}finally{loading(false);...}`. Ponto real de "consultando cadastro" = quando a
1ª chamada de `jsonp`/`fetchWhere` está em voo (dentro de cada branch, ANTES do `await`).

**`finish(items,fromMap)`** (linha 2582-2648): roda de forma SÍNCRONA sobre os `items` já
resolvidos (filtra/ordena/`render(units,...)`/`plot(units)`). O ponto de "calculando estimativa"
cabe ANTES de `render()`, e "preparando mapa" cabe imediatamente ANTES de `plot(units)` (linha
2626) — `plot()` já faz `map.fitBounds`/`map.setView` internamente (linhas 3035-3040), então a
mensagem 5 precisa ser setada pelo CALLER (`finish`) imediatamente antes de chamar `plot`, não
dentro de `plot` (evita tocar a assinatura de `plot`).

**Mensagem 4 (condicional, "Buscando comparáveis…")**: só se aplica quando a ficha JÁ abre com
stats (fluxo de único resultado, `plot()` chama `pick()` internamente quando `list.length===1`,
linha 3038, que por sua vez chama `showDetail()`). Como o disparo de `compare()`/`compsStats()`
acontece DENTRO de `pick()`/`showDetail()` de forma assíncrona e após `plot()` já ter retornado,
a forma mais simples e honesta de cumprir "nunca teatro" é: a mensagem 4 é disparada DENTRO do
código que efetivamente chama `compare()` (verificar onde `compare()` é invocado — grep
`compare()` chamado por `showDetail`/algum listener) SE esse caminho for alcançado antes do
`loading(false)` final de `buscar()` já ter rodado. Dado que `buscar()` já terminou (`finally`) no
momento em que `compare()` roda assincronamente depois, a mensagem 4 NA PRÁTICA quase nunca
aparece dentro da janela de `loading` de `buscar()` — **decisão desta plan**: implementar o hook
(`MOTION_MSG.comparaveis`) e chamar `loading(true,...)` no início de `compare()` APENAS se
`document.getElementById("loading").classList.contains("show")` já estiver true no momento
(ou seja, só mostra a mensagem 4 se o loading overlay AINDA estiver visível — nunca reabre o
overlay só para mostrá-la, cumprindo "nunca setTimeout pra forçar"). Documentar essa decisão no
SUMMARY.

**`.skel-card`/`.skel-line`**: CSS definido literalmente no `13-UI-SPEC.md` (Componente 2,
"Skeleton shimmer") — copiar o bloco CSS exatamente como especificado (reusa `--line`/`--paper-2`
já existentes, zero cor nova). Local de inserção no `<style>`: próximo às regras de `.card`
(mesmo bloco visual).

**`#results`** (linha 817-828): container onde `.empty`/lista são injetados. Skeleton é
injetado no INÍCIO de `buscar()` (logo depois de `btn.disabled=true`), substituído por
`finish()`/`render()` real ou pelo bloco de erro do `catch` — nunca "pendurado".
</facts>

<interfaces>
<!-- Contrato já travado por 13-01 — usar diretamente, não reabrir. -->

```js
// De 13-01 (RADAR_PURE): statusDeUnidade(input) -> 'bom'|'atencao'|'risco'|'semdado'
// Aceita {op:{score}} (shape de a.__scores) OU numero direto. Nunca retorna 'caixa'.
function statusDeUnidade(input): 'bom'|'atencao'|'risco'|'semdado'
```

```js
// Esta plan cria (fora do bloco RADAR_PURE — usa hex literal pois Leaflet nao le CSS var):
function statusPino(a){
  return statusDeUnidade(a.__scores); /* 'bom'|'atencao'|'risco'|'semdado', nunca 'caixa' aqui —
    pinos de plot() NUNCA sao da camada CAIXA (caixaLayer e separada, ver toggleCaixa) */
}
const PINO_STYLE={
  bom:      {fillColor:"#2c5545", radius:9},
  atencao:  {fillColor:"#a8842c", radius:9},
  risco:    {fillColor:"#b5451f", radius:8},
  semdado:  {fillColor:"#57503f", radius:7},
};
const MOTION_MSG={
  localizando:  "Localizando imóvel…",
  cadastro:     "Consultando cadastro…",
  estimativa:   "Calculando estimativa…",
  comparaveis:  "Buscando comparáveis…",
  mapa:         "Preparando mapa…",
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pinos semânticos — statusPino/PINO_STYLE + legenda + tooltip + pick()</name>
  <files>radar-goiania.html</files>
  <action>
**1. Definir `statusPino`/`PINO_STYLE`** (fora do bloco RADAR_PURE, próximo a `plot()`, linha
~3010, antes da função `plot`): copiar literalmente o bloco de `<interfaces>` acima. Comentar:
"Fase 13 PIN-01: mapeia status de EXIBIÇÃO do pino — nunca recalcula score, só lê `a.__scores`
já disponível (Fases 9/10). Reusa `statusDeUnidade` (RADAR_PURE, 13-01) — não reimplementa
bandas."

**2. Editar `plot(list)`** (linha 3015-3042): trocar a criação do `L.circleMarker` (linha 3028)
de `{radius:8,color:"#141a1f",weight:2,fillColor:"#b5451f",fillOpacity:.9,
bubblingMouseEvents:false}` para usar `PINO_STYLE[statusPino(a)]` no lugar de `radius`/
`fillColor` fixos, MANTENDO `color:"#141a1f"` (borda) e `weight:2`/`fillOpacity:.9`/
`bubblingMouseEvents:false` inalterados. Ao criar o marker, adicionar `.bindTooltip(...)` com o
texto `"${statusLabel} · ${endereçoOuUnidade}"` (usar `esc()` no texto interpolado — `addrDe(a)`
ou `unitLabel(a)` já existentes para o endereço/unidade; mapa de `statusLabel` por status:
`bom`="Boa oportunidade", `atencao`="Atenção", `risco`="Abaixo da mediana",
`semdado`="Sem dado ainda"). Quando o pino representa um PRÉDIO (`cntCi[ci]>1`), usar o MELHOR
status entre as unidades daquele CI (mais otimista vence) — calcular isso num passe prévio sobre
`list`, montando `bestStatusByCi` (mapa `ci -> melhor status`, comparando por prioridade
`bom > atencao > risco > semdado`) ANTES do `list.forEach` que cria os markers, e usar
`bestStatusByCi[ci]` em vez de `statusPino(a)` sempre que `cntCi[ci]>1`.

No final de `plot(list)` (após o `if(pts.length===1){...}else if(pts.length>1){...}else
toast(...)`), chamar `atualizarLegenda(list.length>0)` (nova função, ver item 4).

**3. Editar `pick(i)`** (linha 3096-3101): no `markers.forEach(...)` de desselecionar, trocar o
`fillColor:m===selMk?"#c9691f":"#b5451f"` fixo pelo status real. Construir, ANTES do loop, um
`Map` `mk->{fillColor,radius}` percorrendo `LAST` com a MESMA lógica de "melhor status por CI"
usada em `plot()` (reusar `bestStatusByCi` se guardado em variável de módulo acessível, ou
recalcular a partir de `LAST`/`markers`/`cntCi` — manter uma única fonte de verdade). Dentro do
`markers.forEach`, para o branch de SELECIONADO manter `radius:12,fillColor:"#c9691f"` inalterado
(ortogonal ao status); para o branch de DESSELECIONAR, usar
`{radius: styleMap.get(m).radius, fillColor: styleMap.get(m).fillColor}` em vez dos fixos.

**4. Legenda `#pinoLegenda`**: adicionar o HTML (copiar literalmente do UI-SPEC Componente 1,
seção "Legenda discreta no mapa") dentro de `.mapwrap`, próximo a `#coachMark`/`#breadcrumb`
(mesma família de elementos flutuantes condicionais sobre o mapa). Adicionar o CSS
`.pino-legenda`/`.pl-dot` (copiar literalmente do UI-SPEC, incluindo a regra mobile `≤820px`).
(nota plan-check: NÃO usar `border-style:dashed` na bolinha de 8px p/ diferenciar Atenção×Caixa —
em círculo pequeno o tracejado é ilegível entre browsers; diferenciar pelo ÍCONE textual que a
copy já tem (🏦 na entrada Caixa) — o rótulo é o diferenciador primário de qualquer forma.)
Criar a função `atualizarLegenda(temPinos)`:
`document.getElementById("pinoLegenda").hidden=!temPinos;` — chamada ao final de `plot()` (feito
no item 2).

**5. Verificar CAIXA inalterado**: confirmar por leitura que `toggleCaixa()`/`caixaLayer`
(linhas 4497-4516) permanecem 100% inalterados — nenhuma edição nessas linhas.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('radar-goiania.html','utf8');const checks=['PINO_STYLE','statusPino(','pinoLegenda','bindTooltip','atualizarLegenda'];const missing=checks.filter(c=>!s.includes(c));if(missing.length){console.error('MISSING:',missing);process.exit(1);}console.log('OK: pinos semanticos presentes');"</automated>
  </verify>
  <done>plot() usa PINO_STYLE[statusPino(a)] (ou bestStatusByCi para prédios) em vez de fillColor/radius fixos; tooltip com statusLabel+endereço escapado; pick() restaura o status correto ao desselecionar (não mais "#b5451f" fixo); legenda #pinoLegenda aparece/some via atualizarLegenda(list.length>0); toggleCaixa()/caixaLayer inalterados.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Motion de busca em etapas + skeleton shimmer</name>
  <files>radar-goiania.html</files>
  <behavior>
    - Buscar em qualquer MODE dispara loading(true, MOTION_MSG.localizando) como PRIMEIRA chamada, antes de qualquer branch de MODE ou await de rede
    - Durante a resolução (1ª chamada de jsonp/fetchWhere em voo, em qualquer branch), a mensagem visível é MOTION_MSG.cadastro (substituindo as mensagens antigas por modo: "consultando inscrição…"/"consultando o endereço…"/"procurando o edifício…"/"consultando a quadra…")
    - O fallback de erro "número não achado — varrendo a rua…" (modo addr, linha ~2505) PERMANECE literal, inalterado — não faz parte da sequência das 5 mensagens
    - Dentro de finish(), antes de render(units,...), a mensagem visível é MOTION_MSG.estimativa
    - Imediatamente antes de chamar plot(units) dentro de finish(), a mensagem visível é MOTION_MSG.mapa
    - Skeleton (.skel-card x2-3) aparece em #results no início de buscar() (logo após btn.disabled=true), sempre substituído por render() real ou pelo bloco de erro do catch — nunca "pendurado" após loading(false)
    - Sob prefers-reduced-motion, .skel-line não tem animation (fica com background sólido var(--line)) mas continua visível/renderizado
  </behavior>
  <action>
**1. `MOTION_MSG`** (fora do bloco RADAR_PURE, próximo a `loading()`, linha ~2424): copiar
literalmente o objeto de `<interfaces>` acima, com o comentário "Fase 13 MOT-01: as 5 mensagens
são literais e fixas — nunca strings interpoladas. Reusa `loading(on,msg)` já existente, zero
mudança de assinatura."

**2. Editar `buscar()`** (linha 2461-2577):
- Logo após `btn.disabled=true;closeDetail();` (linha 2465) e ANTES do `try{`, ou como PRIMEIRA
  linha dentro do `try`, chamar `loading(true, MOTION_MSG.localizando);` — cobre TODOS os modos
  antes de qualquer branch.
- Substituir cada `loading(true,"consultando inscrição…")` (linha 2471),
  `loading(true,"consultando o endereço…")` (linha 2483), `loading(true,"procurando o
  edifício…")` (linha 2526), `loading(true,"consultando a quadra…")` (linha 2543) por
  `loading(true, MOTION_MSG.cadastro)`.
- NÃO tocar `loading(true,"número não achado — varrendo a rua…")` (linha 2505) — permanece
  literal, é fallback de erro real, fora da sequência feliz.
- Inserir a injeção do skeleton: imediatamente após `loading(true, MOTION_MSG.localizando)`,
  chamar uma nova função `mostrarSkeleton()` que injeta 2-3 `.skel-card` em `#results`
  (`document.getElementById("results").innerHTML = SKELETON_HTML` onde `SKELETON_HTML` é uma
  constante com 3 `<div class="skel-card">` contendo 3 `.skel-line` cada, larguras 80/60/40%,
  igual ao markup do UI-SPEC).

**3. Editar `finish(items,fromMap)`** (linha 2582-2648):
- Imediatamente ANTES da chamada `render(units,{stagger:true});` (linha 2625), inserir
  `loading(true, MOTION_MSG.estimativa);`.
- Imediatamente ANTES da chamada `plot(units);` (linha 2626), inserir
  `loading(true, MOTION_MSG.mapa);`.
- Note que os branches de "vazio" (`if(!units.length){...return;}`, linhas 2605-2624) retornam
  ANTES de chegar em `render()`/`plot()` — nesses casos a sequência para naturalmente em
  "Calculando estimativa…"/"Localizando imóvel…" e o `finally` de `buscar()` chama
  `loading(false)` de qualquer forma; isso é aceitável (a busca terminou, não há mais nada a
  "preparar").

**4. Mensagem 4 condicional (`MOTION_MSG.comparaveis`)**: localizar o ponto onde `compare()` é
efetivamente chamado (grep por `compare()` sendo invocado, não a definição) — inserir no INÍCIO
do corpo de `compare()` (ou do handler que a invoca):
```js
if(document.getElementById("loading").classList.contains("show")) loading(true, MOTION_MSG.comparaveis);
```
Nunca força o overlay a abrir só para mostrar essa mensagem — só atualiza o texto SE o overlay já
estiver visível no momento.

**5. CSS skeleton**: adicionar ao `<style>` (próximo às regras de `.card`, linha ~187-200) o
bloco `.skel-card`/`.skel-line`/`@keyframes skel-shimmer`/`@media(prefers-reduced-motion:
reduce){.skel-line{...}}` copiado literalmente do `13-UI-SPEC.md` Componente 2.

**6. `SKELETON_HTML`/`mostrarSkeleton()`**: constante/função pequena que monta o HTML de 2-3
`.skel-card` (usar 3 no desktop; pode ser sempre 3 — a diferenciação mobile/desktop de
quantidade é cosmética e não crítica, documentar se simplificado para "sempre 3" no SUMMARY).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('radar-goiania.html','utf8');const checks=['MOTION_MSG','Localizando imóvel','Consultando cadastro','Calculando estimativa','Buscando comparáveis','Preparando mapa','skel-card','skel-shimmer','prefers-reduced-motion'];const missing=checks.filter(c=>!s.includes(c));if(missing.length){console.error('MISSING:',missing);process.exit(1);}console.log('OK: motion + skeleton presentes');"</automated>
  </verify>
  <done>As 5 mensagens literais de MOTION_MSG substituem as mensagens específicas de modo em buscar()/finish(); o fallback de erro "varrendo a rua" permanece intocado; skeleton shimmer aparece em #results no início de buscar() e é sempre substituído por render() real ou pelo bloco de erro; CSS de skeleton tem variante estática sob prefers-reduced-motion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a.__scores (memória) -> statusPino -> cor do pino | dado já calculado em Fases 9/10, nenhuma superfície de entrada externa nova; risco é apenas lógico (status errado), não de segurança |
| tooltip do pino (endereço/unitLabel interpolado) -> DOM | texto do usuário/cadastro já passa por `esc()` no restante do app — esta plan segue o MESMO contrato (nunca interpola sem escapar) |
| MOTION_MSG -> #loadmsg.textContent | strings literais fixas, nunca interpoladas com dado do usuário — risco zero |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Information Disclosure | tooltip do pino interpolando endereço/unitLabel sem esc() | mitigate | usar `esc()` no texto interpolado do `bindTooltip`, mesmo contrato de todo o app (IN-01) |
| T-13-05 | Tampering | statusPino/bestStatusByCi divergindo entre plot() e pick() (2 fontes de verdade calculando "melhor status por CI" de formas diferentes) | mitigate | reusar a MESMA função auxiliar de prioridade (`bom > atencao > risco > semdado`) nos dois lugares — nunca duplicar a lógica de comparação com critérios diferentes |
| T-13-06 | Denial of Service (percebido) | skeleton "pendurado" em #results se buscar() lançar exceção antes do catch limpar | mitigate | o bloco `catch(e)` já substitui `#results.innerHTML` inteiro (linha 2573) — o skeleton nunca sobrevive a um erro, verificado por leitura do fluxo existente |
| T-13-07 | Tampering | mensagem 4 (comparáveis) forçando reabertura do overlay de loading fora da janela real de busca | accept — mitigado por design | guarda explícita `classList.contains("show")` antes de setar a mensagem — nunca chama `loading(true,...)` incondicionalmente fora de `buscar()`/`finish()` |

**Sem ameaças de alta severidade.** Motion/pinos operam 100% sobre dados já em memória — nenhuma requisição de rede nova introduzida por esta plan.
</threat_model>

<verification>
- Buscar um imóvel qualquer: pino nasce CINZA (`semdado`) se `__scores` ainda não populado; abrir a ficha (dispara `scoreOportunidade`) recoloreia o pino na PRÓXIMA busca/plot
- Pino de imóvel Caixa permanece dourado, `toggleCaixa()`/`caixaLayer` inalterados
- `.pino-legenda` aparece automaticamente após qualquer busca com resultados plotados; permanece oculta antes de qualquer busca
- Buscar dispara a sequência "Localizando imóvel…" → "Consultando cadastro…" → "Calculando estimativa…" → "Preparando mapa…" (visível no `#loadmsg`) — mensagens antigas por modo não aparecem mais
- Skeleton shimmer aparece brevemente no início de uma busca, sempre substituído pelo resultado real ou pelo bloco de erro
- `prefers-reduced-motion` ativo: skeleton fica estático (cor sólida, sem `animation`), mas continua visível
- `node --test "tests/*.test.mjs"` continua passando 100% (esta plan não altera RADAR_PURE, zero regressão esperada)
</verification>

<success_criteria>
- PIN-01 cumprido: pinos semânticos por status, legenda sempre-visível com ícone+texto, tooltip acessível, pino cinza clicável, camada CAIXA inalterada
- MOT-01 cumprido: as 5 mensagens literais atreladas a fases REAIS de `buscar()`/`finish()`, nunca timer decorativo; skeleton shimmer REDUCE-safe
- Zero requisição de rede nova introduzida
- Zero regressão na suite de testes (105+ existentes)
</success_criteria>

<output>
Após completar, criar `.planning/phases/13-refino-visual-pinos-motion-descoberta/13-02-SUMMARY.md`.
Incluir: pontos exatos de inserção de cada `loading(true, MOTION_MSG.*)` (linha antes/depois),
decisão final sobre a mensagem 4 condicional (comparáveis) e onde ela foi conectada, confirmação
de que `toggleCaixa()`/`caixaLayer` permaneceram intocados (diff zero nessas linhas), e a lógica
exata de "melhor status por CI" usada tanto em `plot()` quanto em `pick()` (mesma função ou
mesma constante de prioridade).
</output>
