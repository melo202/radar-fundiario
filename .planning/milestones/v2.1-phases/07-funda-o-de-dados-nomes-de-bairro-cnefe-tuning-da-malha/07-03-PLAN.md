---
phase: 07-funda-o-de-dados-nomes-de-bairro-cnefe-tuning-da-malha
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - radar-goiania.html
autonomous: false
requirements: [MALHA-01]
tags: [ui, leaflet, mesh, mobile, zoom-density, no-new-hex]

must_haves:
  truths:
    - "A malha ociosa 'sussurra': traço fino e baixa opacidade, bem mais fraca que hoje — não emaranha no mobile"
    - "O destaque no toque 'grita': accent forte + nome (corrigido) no tooltip, contraste nítido vs idle"
    - "A densidade da malha responde ao zoom: linhas mais finas/discretas na cidade, mais presentes ao aproximar"
    - "Tocar na ÁREA do bairro (fill), não só na linha fina, seleciona o bairro no mobile"
    - "Zero hex novo, zero transition novo — só ajustes de weight/opacity/fillOpacity nas constantes existentes e um handler de zoomend"
  artifacts:
    - path: "radar-goiania.html"
      provides: "BAI_STYLE idle de-enfatizado + BAI_HOVER reforçado + rampa de densidade por zoom em baiStyle/zoomend + fill clicável garantido"
      contains: "baiStyle"
  key_links:
    - from: "map zoomend handler"
      to: "bairroLayer.setStyle(baiStyle)"
      via: "re-aplicação do estilo por zoom (densidade)"
      pattern: "zoomend"
    - from: "onEachBairro (fill do polígono)"
      to: "highlightBairro / drillBairro"
      via: "clique na área (bubblingMouseEvents:false já garante o fill clicável)"
      pattern: "bubblingMouseEvents"
---

<objective>
Tunar a UX da malha de bairros no mobile (MALHA-01): idle "sussurra" (traço fino, baixa opacidade), toque "grita" (accent forte + nome correto), densidade por zoom (linha cresce ao aproximar), e toque na ÁREA do bairro (fill), não só na linha. Ajustes nas constantes `BAI_STYLE`/`BAI_HOVER`/`baiStyle()` já existentes + um caminho de densidade no `zoomend`.

Purpose: A malha atual "emaranha" no celular — o idle compete demais com o conteúdo. Resolver a legibilidade sem remover a malha, honrando o 07-UI-SPEC.md (contrato de estados). Base visual que o choropleth da Fase 9 vai estender. (MALHA-01)
Output: `radar-goiania.html` com a malha tunada — zero hex novo, zero transition novo, verificado em preview mobile 375 + desktop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-funda-o-de-dados-nomes-de-bairro-cnefe-tuning-da-malha/07-CONTEXT.md
@.planning/phases/07-funda-o-de-dados-nomes-de-bairro-cnefe-tuning-da-malha/07-UI-SPEC.md

# Fonte de código (ler antes de editar)
@radar-goiania.html

<facts>
<!-- Inspecionado ao vivo neste repo. Âncoras exatas (número de linha aproximado do estado atual). -->

Constantes de estilo (radar-goiania.html ~808-820):
  const _BAI_LINE=(_baiCss.getPropertyValue("--line").trim()||"#c3b9a3");   // tan, já resolvido de var()
  const _BAI_ACCENT=(_baiCss.getPropertyValue("--accent").trim()||"#b5451f"); // vermelho-óxido
  const BAI_STYLE={pane:"bairros",color:_BAI_LINE,weight:1,fillColor:_BAI_LINE,fillOpacity:.03,opacity:.8,bubblingMouseEvents:false};
  const BAI_HOVER={color:_BAI_ACCENT,weight:2.5,fillOpacity:.08,opacity:1};
  function baiStyle(feature){ return satelliteOn ? {...BAI_STYLE, opacity:1, weight:1.5, fillOpacity:0} : BAI_STYLE; }

zoomend handler EXISTENTE (radar-goiania.html ~730-735) — só adiciona/remove o layer por z>=17:
  map.on("zoomend",()=>{ if(!bairroLayer)return; const z=map.getZoom();
    if(z>=17){ ...removeLayer... } else { ...addTo... } });
  --> ESTENDER este mesmo handler (não criar um segundo) para re-aplicar densidade por zoom.

highlight/drill (radar-goiania.html ~908-951):
  highlightBairro(layer): layer.setStyle(BAI_HOVER) + tooltip com nm_bai (agora corrigido pós-Plano 01)
  onEachBairro: touch -> 1º toque highlight, 2º toque drill; desktop -> hover highlight, click drill
  bubblingMouseEvents:false + fillOpacity>0 no BAI_STYLE já tornam o FILL clicável (a área toda).

Contrato de estados (07-UI-SPEC.md) — alvos aproximados (afinar no preview, à discrição do CONTEXT):
  Idle:      weight ~0.5–1, opacity ~0.35–0.5, fillOpacity ~0.02 (near-transparent mas clicável)
  Highlight: weight ~2.5, opacity 1, fillOpacity ~0.08 (mantém/reforça accent)
  Densidade: idle weight/opacity sobem suave com zoom (ex.: weight 0.5 em z<=12 -> ~1.2 perto de z16)
  Bairros continuam sumindo em z>=17 (dão lugar aos lotes — comportamento da Fase 3, NÃO mexer).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: De-enfatizar o idle + reforçar o highlight (BAI_STYLE/BAI_HOVER) sem hex novo</name>
  <read_first>radar-goiania.html ~806-825 (bloco _BAI_LINE/_BAI_ACCENT/BAI_STYLE/BAI_HOVER/baiStyle); 07-UI-SPEC.md (tabela de estados)</read_first>
  <files>radar-goiania.html</files>
  <action>
Ajustar SÓ os valores numéricos de estilo (weight/opacity/fillOpacity) — sem NENHUM hex novo, sem cor nova, reusando `_BAI_LINE`/`_BAI_ACCENT`:

1. **Idle mais fraco** (`BAI_STYLE`): baixar `weight` de 1 → ~0.6, `opacity` de .8 → ~0.4, `fillOpacity` de .03 → ~0.02 (near-transparent mas ainda clicável no fill). Manter `bubblingMouseEvents:false` e o `pane:"bairros"`.
2. **Highlight forte** (`BAI_HOVER`): manter `color:_BAI_ACCENT`, `weight:2.5`, `opacity:1`; `fillOpacity` ~0.08. O contraste idle→highlight tem que ficar nítido (idle bem mais apagado que hoje).
3. Preservar o branch `satelliteOn` de `baiStyle()` (sobre satélite: solta o wash, reforça o traço) — só reharmonizar os números com o novo idle, sem introduzir hex.

Afinar os valores exatos no preview (à discrição, CONTEXT) mantendo-se dentro das faixas do 07-UI-SPEC.md. NÃO adicionar `transition:` (motion é coordenado, Fase 6 — mudança de estilo é snap via setStyle).
  </action>
  <verify>
    <automated>python -c "import re; s=open('radar-goiania.html',encoding='utf-8').read(); b=re.search(r'const BAI_STYLE=\{[^}]*\}',s).group(0); h=re.search(r'const BAI_HOVER=\{[^}]*\}',s).group(0); print('BAI_STYLE:',b); print('BAI_HOVER:',h); assert 'bubblingMouseEvents:false' in b; assert '_BAI_ACCENT' in h; assert '#' not in b.replace('_BAI_LINE','') and '#' not in h.replace('_BAI_ACCENT',''), 'hex novo detectado'; print('OK sem hex novo, fill clicavel, accent no hover')"</automated>
  </verify>
  <done>BAI_STYLE idle mais fraco (weight ~0.6, opacity ~0.4, fillOpacity ~0.02, `bubblingMouseEvents:false` intacto); BAI_HOVER mantém accent+weight 2.5+opacity 1; nenhum hex literal novo introduzido (só `_BAI_LINE`/`_BAI_ACCENT`); nenhum `transition:` novo.</done>
</task>

<task type="auto">
  <name>Task 2: Densidade por zoom (estender o zoomend) + garantir toque na área</name>
  <read_first>radar-goiania.html ~730-735 (zoomend existente), ~816-820 (baiStyle), ~908-951 (highlight/onEachBairro)</read_first>
  <files>radar-goiania.html</files>
  <action>
1. **Rampa de densidade por zoom** — tornar `baiStyle()` sensível ao zoom e re-aplicar no `zoomend`:
   - Adicionar em `baiStyle()` um cálculo de `weight`/`opacity` idle em função de `map.getZoom()`: p.ex. `const z=map?map.getZoom():12; const t=Math.max(0,Math.min(1,(z-12)/4));` e interpolar `weight` de ~0.5 (z<=12) a ~1.2 (z>=16), `opacity` de ~0.35 a ~0.5. Manter o branch `satelliteOn`. Valores exatos à discrição (faixas do 07-UI-SPEC.md).
   - **ESTENDER o handler `zoomend` EXISTENTE** (~730) — no branch em que o layer permanece visível (z<17), chamar `bairroLayer.setStyle(baiStyle)` para re-aplicar a densidade. NÃO criar um segundo `map.on("zoomend")`. Se houver `baiHi` ativo, re-aplicar `BAI_HOVER` nele depois (não perder o destaque no zoom).
   - Preservar o gate de z>=17 (bairros somem, dão lugar aos lotes) — comportamento da Fase 3, intocado.

2. **Toque na área** — confirmar (e garantir explicitamente) que o clique no FILL do polígono, não só na linha, dispara highlight/drill:
   - `bubblingMouseEvents:false` + `fillOpacity>0` já tornam o fill clicável; confirmar que continua assim após o Task 1 (fillOpacity ~0.02 > 0). Se necessário, deixar um comentário no `onEachBairro` marcando que o handler está no polígono inteiro (fill incluso), não só no stroke.
   - NÃO alterar a lógica de 1º-toque-highlight / 2º-toque-drill (mobile) nem hover/click (desktop) — só a garantia do alvo de toque.
  </action>
  <verify>
    <automated>python -c "s=open('radar-goiania.html',encoding='utf-8').read(); assert s.count('map.on(\"zoomend\"')==1, 'deve haver UM zoomend'; assert 'bairroLayer.setStyle(baiStyle)' in s; b=s[s.find('function baiStyle'):s.find('function baiStyle')+400]; assert 'getZoom' in b, 'baiStyle deve usar getZoom para densidade'; assert 'bubblingMouseEvents:false' in s; print('OK: um zoomend, setStyle por zoom, baiStyle sensivel a zoom, fill clicavel')"</automated>
  </verify>
  <done>Um único `map.on("zoomend")`, estendido para `bairroLayer.setStyle(baiStyle)` quando z<17; `baiStyle()` usa `map.getZoom()` para rampar weight/opacity idle; gate z>=17 intacto; fill clicável (bubblingMouseEvents:false + fillOpacity>0) confirmado; lógica de toque/hover inalterada.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Preview da malha (mobile 375 + desktop) — checkpoint</name>
  <files>radar-goiania.html</files>
  <action>
Checkpoint de verificação visual — o executor NÃO implementa código aqui; serve o app e pede ao usuário que confira o resultado das Tasks 1-2 no preview. Ver <what-built>/<how-to-verify> abaixo.
  </action>
  <what-built>Malha de bairros tunada: idle de-enfatizado (traço fino, baixa opacidade), highlight reforçado (accent + nome correto pós-reconciliação), densidade por zoom, toque na área. Zero hex/transition novo.</what-built>
  <how-to-verify>
Servir o app localmente e abrir `radar-goiania.html` (usar um static server na raiz do repo; não abrir via `file://` porque o fetch do json/service worker precisa de http).

1. **Mobile 375** (DevTools responsive, iPhone SE/375px): a malha ociosa deve estar DISCRETA — não "emaranha", as linhas não competem com o conteúdo. Comparar mentalmente com o estado anterior (mais forte).
2. **Toque num bairro**: realça FORTE (linha accent vermelha, contraste nítido vs idle) e mostra o NOME CORRETO no tooltip (pós-Plano 01 — ex.: um bairro que antes tinha mojibake agora aparece limpo; glebas sem nome → "Gleba não denominada").
3. **Densidade por zoom**: dar zoom in — as linhas idle ficam mais presentes/densas ao aproximar; dar zoom out — ficam mais finas/calmas. Em z>=17 os bairros somem (dão lugar aos lotes) — comportamento esperado, não regressão.
4. **Toque na área**: tocar no MEIO do bairro (não na linha fina) — deve selecionar. No mobile, 2º toque no mesmo bairro dá o drill (fitBounds).
5. **Desktop**: hover realça, mouseout reseta, clique drila. Sem erro no console.
  </how-to-verify>
  <verify>Verificação humana no preview (mobile 375 + desktop) — sem comando automatizado; o executor pausa aqui.</verify>
  <done>Usuário confirma: idle discreto, highlight forte + nome correto, densidade por zoom, toque na área funciona, sem erro de console.</done>
  <resume-signal>Digite "aprovado" ou descreva o que ajustar (opacidade/peso idle, limiares de zoom, contraste do highlight).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nome de bairro (dado) → tooltip/breadcrumb (DOM) | string vinda do bairros-goiania.json renderizada no tooltip do highlight |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Injection (XSS) | nome do bairro no tooltip (highlightBairro) | accept | O caminho já usa `esc()` no tooltip e `textContent` no breadcrumb (verificado, DATA-NAMES.md 1.5); esta fase não altera o caminho de render, só valores de estilo — mitigação existente preservada |
| T-07-10 | Tampering | regressão do gate z>=17 / drill | mitigate | Verify assert de UM único zoomend e gate preservado; checkpoint humano confirma drill + sumiço em z>=17 sem regressão |

**Sem ameaças de alta severidade.** Mudança puramente visual (weight/opacity) em código cliente já auditado; nenhum novo caminho de dados, nenhum hex/segredo novo, sanitização de tooltip/breadcrumb existente intacta.
</threat_model>

<verification>
- Um único `map.on("zoomend")`; estendido com `bairroLayer.setStyle(baiStyle)` para densidade; gate z>=17 intacto
- `baiStyle()` rampa weight/opacity idle por `map.getZoom()`; branch satelliteOn preservado
- BAI_STYLE idle de-enfatizado; BAI_HOVER accent forte; `bubblingMouseEvents:false` + fillOpacity>0 (fill clicável)
- Zero hex literal novo (só `_BAI_LINE`/`_BAI_ACCENT`); zero `transition:` novo
- Checkpoint de preview aprovado (mobile 375 + desktop): idle discreto, highlight forte + nome correto, densidade por zoom, toque na área, sem erro de console
</verification>

<success_criteria>
- Malha ociosa "sussurra" (fina, baixa opacidade) — MALHA-01
- Toque "grita" (accent forte + nome correto) — MALHA-01
- Densidade responde ao zoom — MALHA-01
- Toque na área do bairro (fill) seleciona — MALHA-01
- Zero hex novo, zero transition novo, drill/gate z>=17 sem regressão (07-UI-SPEC.md honrado)
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-funda-o-de-dados-nomes-de-bairro-cnefe-tuning-da-malha/07-03-SUMMARY.md`.
Incluir no SUMMARY: os valores finais de weight/opacity/fillOpacity idle e highlight escolhidos no preview, os limiares de zoom da rampa de densidade, e a confirmação de que zero hex/transition novo foi introduzido e o drill/gate z>=17 não regrediu.
</output>
