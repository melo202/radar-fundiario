---
phase: 11-documentos-em-3-niveis
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - radar-goiania.html
autonomous: true
requirements: [DOC-01, DOC-02, DOC-03]
tags: [wizard, seletor-finalidade, cnai, confianca, pendencias, revisao-pre-pdf]

must_haves:
  truths:
    - "Clicar em '📄 Gerar documento' (#dActsPrim) abre o Seletor de Finalidade (abrirSeletorFinalidade), NAO mais direto no passo 'O imovel' de abrirLaudo()"
    - "Escolher qualquer uma das 4 opcoes de finalidade mostra o card de recomendacao com porque especifico (via recomendaDocumento) + os 3 documentos tocaveis, recomendado sempre em primeiro"
    - "Escolher 'Documento tecnico formal' SEM CNAI no perfil recomenda relatorio (nao ptam); o card PTAM permanece tocavel/funcional (nunca desabilitado)"
    - "O wizard tem um novo passo 'Antes de gerar' (Confianca+Pendencias) e uma nova etapa 'Revisao final' antes de montarLaudo(); o CTA final e o UNICO gatilho do PDF (guarda ec9f129 preservada)"
    - "radar_prof tem campo cnai SEPARADO de creci; perfis existentes gravados so com creci continuam funcionando (compat, sem quebra)"
  artifacts:
    - path: "radar-goiania.html"
      provides: "abrirSeletorFinalidade/finSet/finEscolherDoc + passo Confianca (wizSet docOk/privativa) + passo Revisao + LZ.tipoDoc/LZ.finalidadeUso/LZ.docOk + radar_prof.cnai"
      contains: "function abrirSeletorFinalidade"
  key_links:
    - from: "#dActsPrim button.primary"
      to: "abrirSeletorFinalidade()"
      via: "onclick (substitui abrirLaudo() direto)"
      pattern: "onclick=\"abrirSeletorFinalidade\\(\\)\""
    - from: "abrirSeletorFinalidade/finSet"
      to: "recomendaDocumento (RADAR_PURE, 11-01)"
      via: "chamada direta com LZ.finalidadeUso + !!(LZ.prof.cnai||'').trim()"
      pattern: "recomendaDocumento\\("
    - from: "passo Confianca (novo)"
      to: "pendenciasDocumento (RADAR_PURE, 11-01)"
      via: "chamada com areaOk/nComps/atipico/venalOk derivados de LZ.a/LZ.comps + LZ.docOk"
      pattern: "pendenciasDocumento\\("
    - from: "wizNext() no ultimo passo (Revisao)"
      to: "montarLaudo()"
      via: "clique real do usuario no CTA .wnext — guarda ec9f129 preservada (nenhuma chamada assincrona dispara o PDF)"
      pattern: "montarLaudo\\(\\)"
---

<objective>
Refatorar o wizard `.wiz`/`LZ` existente para inserir: (1) um novo passo "Seletor de Finalidade" ANTES do passo 0 atual, substituindo a entrada direta de `abrirLaudo()` por `abrirSeletorFinalidade()`; (2) um novo passo "Antes de gerar" (Confiança + Pendências) reusando `pendenciasDocumento`; (3) uma nova etapa "Revisão final" antes de `montarLaudo()`; (4) separação do campo CNAI do CRECI em `radar_prof`.

Purpose: DOC-01 exige que a UI pergunte a finalidade primeiro e recomende o documento certo (reusa `recomendaDocumento` da Wave 1); DOC-02 exige o painel de confiança+pendências antes de gerar (reusa `pendenciasDocumento`); DOC-03 exige revisão/edição dos campos sensíveis antes do PDF, preservando a guarda `ec9f129` (PDF só no clique real). Esta plan reordena e amplia o wizard existente — NÃO recomeça o motor, apenas insere passos novos e reindexar os existentes.
Output: `abrirSeletorFinalidade()`, `finSet()`, `finEscolherDoc()` novos; `wizRender()` ampliado com os passos Confiança e Revisão; `LZ.tipoDoc`, `LZ.finalidadeUso`, `LZ.docOk`, `LZ.privativa` (já existe, reusado); `radar_prof.cnai` separado de `radar_prof.creci`; `#dActsPrim` chamando `abrirSeletorFinalidade()` em vez de `abrirLaudo()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-documentos-em-3-niveis/11-CONTEXT.md
@.planning/phases/11-documentos-em-3-niveis/11-UI-SPEC.md
@.planning/phases/11-documentos-em-3-niveis/11-01-SUMMARY.md

# Fonte de código (ler antes de editar)
@radar-goiania.html

<facts>
<!-- Verificado por leitura direta deste repo. -->

**Estado atual do wizard (`radar-goiania.html`):**

`abrirLaudo()` (linha 2853): entry point atual, chamado por `#dActsPrim` (linha 2430-2431,
`onclick="abrirLaudo()"`). Cria `LZ={a:DCUR, step:0, conserv:"Bom", difs:[], obs:"", valor:null,
fotos:[], prof, comps:null, vistoria:false, dataVist:hojeISO, solicitante:"",
finalidade:"Venda / compra", privativa:null}`. `prof` vem de
`JSON.parse(localStorage.getItem("radar_prof")||"{}")`.

`wizRender()` (linha 2927): passos hoje são `0` (O imóvel), `1` (Valor), `2` (Fotos), `3`
(Solicitante e você — perfil, ÚLTIMO passo hoje). `wDots` renderiza `[0,1,2,3].map(...)` (4 pontos
fixos). `wNext` mostra "Gerar PDF" quando `LZ.step===3`. `wizNext()` (linha 2994): se
`LZ.step<3`, incrementa; no passo 3 (último), valida nome+valor e chama `montarLaudo()`
DIRETAMENTE — esta plan MOVE essa validação+chamada para o NOVO último passo (Revisão), passo 3
atual (perfil) deixa de ser o gatilho do PDF.

`radar_prof` hoje: `{nome, creci, contato}` — campo único de texto livre no passo perfil (linha
2986-2987): `<span class="wlabel">CRECI / CNAI (opcional)</span><input ... value="${esc(p.creci||"")}"
oninput="LZ.prof.creci=this.value">`. Esta plan SEPARA em 2 inputs (`p.creci`, `p.cnai`) — perfis
JÁ SALVOS com `creci` fundido (ex.: "12345 · CNAI 6789") continuam funcionando SEM quebra: o campo
`creci` antigo simplesmente vira o novo campo `creci` (só texto do CRECI passa a ficar mais limpo
dali em diante; não há migração ativa de dado — o usuário re-digita se quiser separar, comportamento
aceitável pois é campo opcional e o texto antigo ainda aparece no campo CRECI sem quebrar nada).

`abrirSeletorFinalidade()` (NOVA, substitui `abrirLaudo()` como entry point de `#dActsPrim`):
mesma guarda `if(!DCUR)return;` (linha 2854, herdada). Em vez de já criar `LZ` com `step:0`, cria um
ESTADO PRÓPRIO do seletor (`LZ` só é criado quando o usuário ESCOLHE um documento em
`finEscolherDoc()` — ver abaixo) — isso preserva o contrato "fechar e reabrir sempre começa do
zero" (Estado Transversal do UI-SPEC) sem introduzir um segundo objeto de estado global paralelo
a `LZ` (usa o PRÓPRIO `LZ` com uma flag `LZ.fase==="finalidade"` antes de ter `step` numérico —
ver Task 1).

**Objeto `dadosFicha()` (linha 2681) já existe e é a fonte de `perfil`** — mas para
`recomendaDocumento` a Wave 2 usa `LZ.prof.cnai` diretamente (não via `dadosFicha()`, que é
chamado fora do wizard). Dentro do wizard, `LZ.prof` é a cópia local (já carregada do localStorage
em `abrirSeletorFinalidade`/`abrirLaudo`), consistente com o padrão "nunca lê DCUR diretamente
após aberto" (Estado Transversal "DCUR fechado" do UI-SPEC).

**Padrão de passo existente a seguir (`wizRender()`):** cada passo monta `b.innerHTML` com
`.wh1`/`.wsub` no topo, controla `stepMudou` (foco automático no `.wh1`), e os controles usam
`.seg`/`.chips`/`.winput`/`.wlabel` já estilizados — Task 2/3 desta plan seguem o MESMO padrão
para os passos novos (Confiança, Revisão), sem CSS novo (100% reuso, per UI-SPEC "Zero token
novo").

**`wDots` hoje é fixo em 4 pontos (`[0,1,2,3]`)** — com os 2 passos novos (Confiança, Revisão)
inseridos ANTES do gatilho do PDF, o total de passos NUMERADOS do array `LZ.step` passa de 4 para
6: `0 O imóvel, 1 Valor, 2 Fotos, 3 Solicitante/perfil (+CNAI), 4 Confiança+Pendências (novo),
5 Revisão (novo)`. O Seletor de Finalidade NÃO é um passo numerado (é uma tela antes de `LZ.step`
existir, per UI-SPEC "não é um passo do array LZ.step, é uma tela intermediária própria") — `wDots`
passa a ser `[0,1,2,3,4,5].map(...)` (6 pontos) só quando `LZ.fase!=="finalidade"`.

**Guarda `ec9f129` (preservar):** comentário original em `montarLaudo()` linha 3086-3089 ("NÃO
imprime automático... o PDF sai do clique no botão"). Esta plan MOVE o gatilho de `wizNext()` no
passo 3 (perfil) para `wizNext()` no NOVO passo 5 (Revisão) — a mecânica é IDÊNTICA (clique real no
`.wnext`/`#wNext`, `onclick="wizNext()"`, já existente no HTML linha 790, NÃO precisa mudar o HTML
do `.wfoot`), só a CONDIÇÃO de qual `LZ.step` dispara `montarLaudo()` muda.
</facts>

<interfaces>
<!-- Funcoes puras da Wave 1 (11-01) que esta plan CHAMA — contrato ja fechado, nao reabrir 11-01. -->

```js
// De RADAR_PURE (11-01-PLAN.md) — já implementadas, só CHAMAR:
function recomendaDocumento(finalidadeUso, cnai): {doc: "ficha"|"relatorio"|"ptam", porque: string}
function pendenciasDocumento(inputs): {conf: {nivel: string, porque: string[]}, itens: Array<{id: string, texto: string, resolvido: boolean, editavel: boolean}>}
// inputs de pendenciasDocumento: {areaOk, nComps, atipico, venalOk, docOk}
```

**Campos novos em `LZ` que esta plan introduz** (contrato para a Wave 3, que lê `LZ.tipoDoc` para
decidir qual template montar):
```js
LZ.fase: "finalidade" | undefined  // "finalidade" = Seletor de Finalidade ainda ativo (LZ.step não existe ainda); undefined = wizard numerado normal
LZ.finalidadeUso: "apresentar"|"captar"|"justificar"|"formal"|null  // motivo de uso escolhido no Seletor (distinto de LZ.finalidade, campo formal existente "Venda/compra/...")
LZ.tipoDoc: "ficha"|"relatorio"|"ptam"|null  // documento efetivamente escolhido (pode diferir da recomendação — override livre)
LZ.docOk: true|false|undefined  // estado da documentação, novo campo do passo Confiança
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seletor de Finalidade — novo entry point substituindo abrirLaudo() direto</name>
  <files>radar-goiania.html</files>
  <action>
1. Trocar `onclick="abrirLaudo()"` por `onclick="abrirSeletorFinalidade()"` em `#dActsPrim`
   (linha ~2431, botão "📄 Gerar documento") — ÚNICO ponto de entrada do botão primário da ficha
   (D-01, per CONTEXT.md "'Gerar documento' passa a abrir o seletor de finalidade").

2. Criar `abrirSeletorFinalidade()` (próxima de `abrirLaudo()`, linha ~2853): mesma guarda
   `if(!DCUR)return;`, lê `prof` do localStorage (mesmo padrão), inicializa
   `LZ={a:DCUR, fase:"finalidade", finalidadeUso:null, tipoDoc:null, prof}` (SEM `step` ainda —
   só existe quando `finEscolherDoc()` roda). Mesma captura de foco (`WIZRET=document.activeElement`),
   mesmo `document.getElementById("wiz").hidden=false`, chama `wizRender()`.

3. Criar `finSet(finalidadeUso)`: `if(!LZ)return; LZ.finalidadeUso=finalidadeUso; wizRender();`
   — grava a escolha e RE-renderiza a MESMA tela (mostra o card de recomendação + os 3 documentos,
   sem navegação extra, per UI-SPEC "sem novo clique de continuar isolado").

4. Criar `finEscolherDoc(tipoDoc)`: grava `LZ.tipoDoc=tipoDoc`, remove `LZ.fase` (delete ou seta
   undefined), inicializa os campos numéricos do wizard EXISTENTE
   (`Object.assign(LZ,{step:0, conserv:"Bom", difs:[], obs:"", valor:null, fotos:[], comps:null,
   vistoria:false, dataVist:new Date().toISOString().slice(0,10), solicitante:"",
   finalidade:"Venda / compra", privativa:null, docOk:undefined})` — MESMOS valores default de
   `abrirLaudo()` hoje, só que disparados aqui em vez de na abertura). Dispara `getComps(LZ.a)` com
   a MESMA captura de instância (`const lz0=LZ; getComps(LZ.a).then(g=>{if(LZ===lz0){LZ.comps=g;
   if(LZ.step===1)wizRender();}}).catch(()=>{if(LZ===lz0)LZ.comps=null;});`, idêntico ao padrão
   já em `abrirLaudo()`). Chama `wizRender()` para avançar ao passo 0 numerado ("O imóvel").

5. Remover a chamada a `abrirLaudo()` como entry point direto — a função `abrirLaudo()` em si pode
   ser DELETADA (substituída por `abrirSeletorFinalidade`+`finEscolherDoc`) OU mantida como
   função morta se outro código a referenciar (confirmar via grep antes de decidir; se não houver
   mais nenhuma chamada a `abrirLaudo()` no arquivo após o passo 1, deletar a função inteira para
   não deixar código morto).

6. Em `wizRender()`: no TOPO da função, ANTES do `if(!LZ)return;` atual, adicionar o ramo do
   Seletor de Finalidade: `if(LZ.fase==="finalidade"){ renderSeletorFinalidade(); return; }` —
   função nova `renderSeletorFinalidade()` que monta `b.innerHTML` com a estrutura EXATA do
   Componente 1 do UI-SPEC (`.fingrid` com 4 `.finop`, e quando `LZ.finalidadeUso` já setado, o
   `.finrec` + `.findocs` com os 3 cards, recomendado primeiro via `recomendaDocumento(
   LZ.finalidadeUso, !!(LZ.prof.cnai||"").trim())`). Usar `document.getElementById("wDots").innerHTML=""`
   (sem pontos nesta tela — não é um passo numerado) e escrever `wTop`/`wBack`: o botão `#wBack`
   nesta tela chama `fecharLaudo()` (equivalente a fechar, per UI-SPEC "primeiro passo da
   jornada") — ajustar `onclick` do `#wBack` dinamicamente OU checar `LZ.fase` dentro de
   `wizBack()` (preferir a segunda opção: menos manipulação de DOM, mais consistente com o padrão
   já usado por `wizNext()/wizBack()` que sempre olham `LZ` primeiro).

   (fix plan-check — BLOCKER) Enquanto `LZ.fase==="finalidade"`, ESCONDER o rodapé de navegação:
   `document.querySelector("#wiz .wfoot").style.display="none"` dentro de `renderSeletorFinalidade()`
   — o early-return pula as linhas que setam `#wNext`, e o botão órfão "Continuar/Gerar PDF" ficava
   VISÍVEL e clicável (verificado: `wizNext()` com `LZ.step===undefined` cai no branch de
   `montarLaudo()` com LZ incompleto). Restaurar `style.display=""` em `finEscolherDoc()` quando os
   passos numerados começam (e garantir que `fecharLaudo()` não deixa o estado escondido preso —
   resetar no `abrirSeletorFinalidade()` de qualquer forma é idempotente).

   Estrutura do HTML gerado por `renderSeletorFinalidade()` (classes exatas do UI-SPEC, sem CSS
   novo — adicionar as classes `.fingrid/.finop/.finrec/.finrec-selo/.finrec-doc/.finrec-porque/
   .findocs/.findoc/.findoc.is-rec/.findoc-selo/.findoc-nome/.findoc-desc` ao `<style>` do app,
   copiando EXATAMENTE as regras da tabela do UI-SPEC Componente 1 — nenhum valor difere do
   documento):
   ```html
   <div class="wh1">Para que você precisa do documento?</div>
   <div class="wsub">Escolha a finalidade — recomendamos o tipo certo pra você. As 3 opções continuam disponíveis.</div>
   <div class="fingrid">
     <button type="button" class="finop" aria-pressed="${LZ.finalidadeUso==='apresentar'}" onclick="finSet('apresentar')">📱<br>Apresentar ao cliente</button>
     <button type="button" class="finop" aria-pressed="${LZ.finalidadeUso==='captar'}" onclick="finSet('captar')">🏠<br>Captar o proprietário</button>
     <button type="button" class="finop" aria-pressed="${LZ.finalidadeUso==='justificar'}" onclick="finSet('justificar')">💲<br>Justificar o preço</button>
     <button type="button" class="finop" aria-pressed="${LZ.finalidadeUso==='formal'}" onclick="finSet('formal')">🧾<br>Documento técnico formal</button>
   </div>
   <!-- quando LZ.finalidadeUso setado: -->
   <div class="finrec">
     <div class="finrec-selo">Recomendado para você</div>
     <div class="finrec-doc">[nome do doc recomendado]</div>
     <div class="finrec-porque">[rec.porque, via esc()]</div>
   </div>
   <div class="findocs">
     <!-- 3 <button class="findoc [is-rec]"> na ordem: recomendado primeiro, depois os outros 2 na ordem fixa do UI-SPEC (relatorio/ficha/ptam, pulando o já mostrado como recomendado) -->
   </div>
   ```
   Foco: ao ENTRAR nesta tela (`abrirSeletorFinalidade`), foco vai para o primeiro `.finop` (não
   `.wh1`) — per UI-SPEC "Foco no Seletor de Finalidade": `document.querySelector('.finop')?.focus()`
   após o primeiro `wizRender()`.

7. `finEscolherDoc(tipoDoc)` deve usar `esc()` (IN-01) em qualquer interpolação de dado do imóvel
   dentro de `renderSeletorFinalidade()` — `porque` de `recomendaDocumento` é texto FIXO (não input
   de usuário), então `esc()` é disciplina/consistência, não uma necessidade de segurança real
   aqui, mas MANTER o padrão (nunca interpolar direto sem `esc()` em template literal que vai para
   innerHTML).
  </action>
  <verify>
    <automated>node --test "tests/*.test.mjs" 2>&1 | tail -10</automated>
  </verify>
  <done>#dActsPrim chama abrirSeletorFinalidade(); a tela mostra as 4 opções de finalidade; escolher uma finalidade mostra o card de recomendação (via recomendaDocumento) + os 3 documentos tocáveis, recomendado em primeiro; "Documento técnico formal" sem CNAI recomenda relatório (nunca ptam) mas o card PTAM continua tocável; escolher qualquer documento avança para o passo 0 numerado existente ("O imóvel"); suite de testes sem regressão.</done>
</task>

<task type="auto">
  <name>Task 2: Passo Confiança+Pendências + campo CNAI separado do CRECI</name>
  <files>radar-goiania.html</files>
  <action>
1. Separar CNAI do CRECI no passo de perfil (hoje `LZ.step===3`, "Solicitante e você", linha
   ~2977-2989): trocar o único input `CRECI / CNAI (opcional)` por 2 inputs distintos:
   ```html
   <span class="wlabel">CRECI (opcional)</span>
   <input class="winput" placeholder="ex.: 12345" value="${esc(p.creci||"")}" oninput="LZ.prof.creci=this.value">
   <span class="wlabel">CNAI (opcional) — habilita o PTAM</span>
   <input class="winput" placeholder="ex.: 6789" value="${esc(p.cnai||"")}" oninput="LZ.prof.cnai=this.value">
   ```
   Ajustar o `.wsub` deste passo (hoje "Com CRECI o documento sai como PTAM; sem, como relatório
   de referência...") para citar CNAI em vez de CRECI: "Com CNAI o documento sai como PTAM; sem,
   como relatório de referência. Seus dados ficam salvos para os próximos." (decisão travada:
   CNAI decide o título, não CRECI — ver Task 3 desta plan para `montarLaudo()`).

2. Inserir NOVO passo `LZ.step===4` ("Antes de gerar", Confiança+Pendências) em `wizRender()`,
   entre o passo perfil (3) e o novo passo Revisão (5, Task 3). Estrutura:
   - Recalcula `pendenciasDocumento(inputs)` a cada render deste passo, com `inputs` derivados de
     `LZ.a`/`LZ.comps`/`LZ.privativa` (MESMA lógica já usada em `atualizarScores()`/`showDetail()`
     para montar `areaOk`/`venalOk`/`atipico`/`nComps` — reusar: `const isU=!!unitLabel(LZ.a)||
     (LZ.a.cdedificio||0)>0; const areaOk=!!(LZ.privativa || (isU?LZ.a.areaedif:LZ.a.areaterr));
     const venalOk=(LZ.a.vlvenal||0)>0; const atipico=isGarage(LZ.a); const nComps=
     (LZ.comps&&LZ.comps.res)?LZ.comps.res.n:0;` — nota: `areaOk` aqui considera `LZ.privativa`
     OU a área do cadastro, pois o usuário já pode ter preenchido a área privativa no passo Valor
     (`LZ.step===1`); isso é MAIS PRECISO que o `areaOk` de `showDetail()` fora do wizard, que não
     tem acesso a `LZ.privativa`).
   - `const {conf, itens} = pendenciasDocumento({areaOk, nComps, atipico, venalOk, docOk:LZ.docOk});`
   - Renderiza `.confcard` (com classe extra `conf-baixa` quando `conf.nivel==='baixa'`), `.conf-nivel`
     ("Confiança: alta/média/baixa" — capitalizar conforme UI-SPEC), `.conf-porque` (join de
     `conf.porque`), e `.checklist` com um `.checkitem` por item de `itens`:
     - item `id==="area"` e `resolvido:false`: mostra input numérico (`inputmode="numeric"`) que
       grava em `LZ.privativa` (MESMO campo do passo Valor — `oninput="LZ.privativa=parseInt(
       this.value.replace(/\\D/g,''),10)||null;wizRender()"`, o `wizRender()` no oninput RECALCULA
       o painel imediatamente, per UI-SPEC "reflete IMEDIATAMENTE... sem precisar sair e voltar").
     - item `id==="area"` e `resolvido:true`: mostra como `.checkitem.is-ok` com "✓ " + texto.
     - item `id==="conservacao"`: SEMPRE `.checkitem.is-ok` com "✓ " + texto (nunca pendente).
     - item `id==="documentacao"`: mostra `.seg` com 2 botões ("Em ordem"/"Pendências") —
       `onclick="wizSet('docOk',true)"` / `onclick="wizSet('docOk',false)"`, `aria-pressed`
       sincronizado com `LZ.docOk===true`/`LZ.docOk===false`. `wizSet()` já existe (linha 2905,
       `function wizSet(k,v){if(!LZ)return;LZ[k]=v;wizRender();}`) — reusar sem modificar.
   - `wDots`: passa a `[0,1,2,3,4,5].map(...)` (6 pontos) — atualizar a linha
     `document.getElementById("wDots").innerHTML=[0,1,2,3].map(...)` para `[0,1,2,3,4,5].map(...)`.
   - `wNext` texto: "Continuar" neste passo (só o ÚLTIMO passo, 5, mostra "Gerar PDF" — ajustar a
     condição `LZ.step===3?"Gerar PDF":"Continuar"` para `LZ.step===5?"Gerar PDF":"Continuar"`).
   - Nunca bloqueia avanço — botão "Continuar" permanece ativo mesmo com `conf.nivel==='baixa'`
     (per UI-SPEC "nunca bloqueia avanço").

3. Adicionar as classes CSS novas ao `<style>` do app — copiar EXATAMENTE os valores da tabela do
   UI-SPEC Componente 2 (`.confcard`, `.confcard.conf-baixa`, `.conf-nivel`, `.conf-porque`,
   `.checklist`, `.checkitem`, `.checkitem.is-ok`) — zero valor novo além do que está documentado
   ali (que por sua vez é 100% reuso de `--paper/--paper-2/--ink/--line/--muted/--accent`).

4. `wizNext()`: ajustar a condição de incremento de `LZ.step<3` para `LZ.step<5` (agora 6 passos
   numerados, 0 a 5) — a VALIDAÇÃO+CHAMADA de `montarLaudo()` que hoje vive no bloco
   `if(LZ.step<3){...}else{...}` MOVE para o passo 5 (Task 3 desta plan cria o passo Revisão e
   finaliza esse ajuste).
  </action>
  <verify>
    <automated>node --test "tests/*.test.mjs" 2>&1 | tail -10</automated>
  </verify>
  <done>Passo de perfil tem 2 campos distintos (CRECI/CNAI); novo passo "Antes de gerar" mostra nível de confiança por extenso + porque (via pendenciasDocumento) e o checklist lista pendências acionáveis; preencher a área no checklist atualiza o nível de confiança na mesma tela sem recarregar; confiança baixa não bloqueia o botão Continuar; suite de testes sem regressão.</done>
</task>

<task type="auto">
  <name>Task 3: Etapa de Revisão Pré-PDF (DOC-03) — CTA final movido para aqui</name>
  <files>radar-goiania.html</files>
  <action>
1. Inserir NOVO passo `LZ.step===5` ("Revisão final") em `wizRender()`, ÚLTIMO passo do wizard.
   Estrutura (campos sensíveis/textos principais editáveis, per UI-SPEC Componente 3):
   ```html
   <div class="wh1">Revisão final</div>
   <div class="wsub">Confira e ajuste o que for preciso — o PDF sai do jeito que você deixar aqui.</div>
   <div class="revsec"><div class="revsec-lbl">Documento</div>
     <div class="revsec-val">${esc(NOMES_DOC[LZ.tipoDoc]||"Relatório de avaliação")}</div></div>
   <div class="revsec"><span class="wlabel">Solicitante</span>
     <input class="winput" value="${esc(LZ.solicitante)}" oninput="LZ.solicitante=this.value"></div>
   <div class="revsec"><span class="wlabel">Finalidade declarada</span>
     <select class="winput" oninput="LZ.finalidade=this.value">${FINS.map(f=>`<option ${LZ.finalidade===f?"selected":""}>${f}</option>`).join("")}</select></div>
   <div class="revsec"><span class="wlabel">Valor de venda sugerido</span>
     <input class="winput" inputmode="numeric" value="${LZ.valor!=null?LZ.valor:""}" oninput="LZ.valor=parseInt(this.value.replace(/\\D/g,''),10)||null"></div>
   <div class="revsec"><span class="wlabel">Observações / ressalvas adicionais</span>
     <textarea class="winput" oninput="LZ.obs=this.value">${esc(LZ.obs)}</textarea></div>
   <div class="revsec revsec-preview"><span class="wlabel">Como vai aparecer — assinatura</span>
     <div class="revprev">${esc(LZ.prof.nome)}${comCreci?" · CRECI "+esc(LZ.prof.creci):""}</div></div>
   <button type="button" class="revlink" onclick="LZ.step=3;wizRender()">Editar perfil</button>
   <button type="button" class="revlink" onclick="delete LZ.fase;LZ.fase='finalidade';wizRender()">Trocar documento</button>
   ```
   Onde `NOMES_DOC={ficha:"Ficha rápida", relatorio:"Relatório de avaliação", ptam:"Laudo / PTAM"}`
   (const nova, próxima de `CONSERVS`/`DIFS`, linha ~2851). `FINS` já existe (linha 2978, dentro
   do passo perfil) — mover a declaração de `FINS` para FORA do bloco `else` do passo 3 (escopo de
   função, não de bloco `if`) para reusar aqui sem duplicar o array, OU duplicar a constante
   (aceitável, é um array literal de 6 strings — preferir mover para escopo de módulo junto de
   `CONSERVS` para não duplicar).
   `comCreci` = `!!(LZ.prof.creci||"").trim()` (calculado no início do passo).
   Link "Trocar documento": volta ao Seletor de Finalidade DENTRO do MESMO `LZ` (preserva `LZ.a`,
   mas reseta a escolha) — `LZ.fase="finalidade";LZ.finalidadeUso=null;LZ.tipoDoc=null;wizRender();`
   (mais simples que `delete LZ.fase` seguido de reatribuição — usar direto esta forma).
   Link "Editar perfil": `LZ.step=3;wizRender();` — navega para o passo perfil SEM perder o
   restante do estado (`LZ` permanece o mesmo objeto, só `step` muda — consistente com
   `wizBack()`).

2. CSS novo (copiar exatamente da tabela UI-SPEC Componente 3): `.revsec`, `.revsec-lbl`,
   `.revsec-val`, `.revprev`. Para `.revlink` (os 2 links "Editar perfil"/"Trocar documento"),
   UI-SPEC especifica reuso do padrão de "Limpar histórico" (Fase 10) — localizar essa classe
   existente (grep por `font:600 11px.*text-decoration:underline` ou nome de classe usado em
   Fase 10 para o link discreto) e REUSAR o mesmo nome de classe em vez de criar `.revlink` do
   zero, se uma classe equivalente já existir; caso não exista uma classe nomeada reutilizável
   (padrão pode estar inline), criar `.revlink{min-height:32px;font:600 11px/1 "IBM Plex Sans";
   text-decoration:underline;color:var(--muted);background:transparent;border:0;cursor:pointer;
   display:block;margin-top:8px}` (valores exatos da regra UI-SPEC).

3. Ajustar `wizNext()`: a validação+chamada de `montarLaudo()` que hoje roda quando
   `LZ.step===3` (fim do bloco `if(LZ.step<3){...}`) MOVE para `LZ.step===5`:
   ```js
   function wizNext(){
     if(!LZ)return;
     if(LZ.step<5){LZ.step++;wizRender();return;}
     if(!(LZ.prof.nome||"").trim()){toast("Preencha seu nome para assinar o documento.");LZ.step=3;wizRender();return;}
     if(LZ.valor==null){toast("Informe o valor de venda sugerido (passo Valor).");LZ.step=1;wizRender();return;}
     try{localStorage.setItem("radar_prof",JSON.stringify(LZ.prof));}catch(e){}
     montarLaudo();
   }
   ```
   (ajuste: a validação de nome agora volta para `LZ.step=3` em vez de não navegar, pois o nome é
   preenchido no passo 3, não no passo atual — pequena melhoria de UX consistente com a validação
   de valor que já volta ao passo 1; comportamento anterior de "nome vazio" só mostrava o toast sem
   navegar, o que deixava o usuário preso na Revisão sem saber aonde ir — corrigir aqui é escopo
   local desta plan, não uma regressão).

4. `montarLaudo()` (linha 3002): NENHUMA mudança de comportamento de disparo (ainda chamado só
   por `wizNext()` no clique real — guarda `ec9f129` preservada). Ajuste de CONTEÚDO fica para a
   Wave 3 (11-03) — esta plan NÃO toca o corpo de `montarLaudo()` além do necessário para 11-02
   (nenhuma mudança de template aqui, só o PONTO DE DISPARO mudou de passo).
  </action>
  <verify>
    <automated>node --test "tests/*.test.mjs" 2>&1 | tail -10</automated>
  </verify>
  <done>Passo "Revisão final" mostra solicitante/finalidade/valor/observações editáveis + preview da assinatura + nome do documento escolhido (não editável, com link "Trocar documento"); alterar o valor na Revisão reflete no PDF gerado; "Gerar PDF" (wNext no passo 5) é o ÚNICO gatilho de montarLaudo() — nenhuma chamada anterior dispara o PDF; suite de testes sem regressão (node --test "tests/*.test.mjs" 100% pass).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seletor de Finalidade / passo Confiança / Revisão -> LZ (estado em memória) | todos os passos novos leem/escrevem em `LZ`, nunca em `DCUR` diretamente após aberto (mesma guarda já herdada) |
| radar_prof.cnai (novo campo) -> localStorage -> montarLaudo()/recomendaDocumento | dado de identidade do próprio corretor (não é PII de terceiro) — persistido só no clique final (mesmo padrão de `radar_prof.creci` já existente) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | Novo ponto de disparo do PDF (passo 5) acidentalmente chamado por uma via assíncrona (ex.: recálculo de `pendenciasDocumento` no passo Confiança) | mitigate | `montarLaudo()` só é chamado dentro de `wizNext()`, que só roda via `onclick="wizNext()"` no `.wnext` (clique real) — nenhum `oninput`/`.then()` dos passos novos chama `wizNext()`/`montarLaudo()` diretamente; verificado por grep manual antes de finalizar a task |
| T-11-05 | Denial of Service (UX) | Confiança baixa bloqueando indevidamente o avanço, contrariando DOC-02 ("nunca bloqueia avanço") | mitigate | botão "Continuar"/"Gerar PDF" nunca checa `conf.nivel` como condição de habilitação — só a validação de nome/valor (já existente) pode impedir o avanço, nunca o nível de confiança |
| T-11-06 | Information Disclosure | CNAI seguindo o MESMO destino que CRECI (é dado de identidade do próprio corretor, não de terceiro) mas exibido sem necessidade em contexto errado | accept | CNAI só aparece no perfil (input) e no bloco de assinatura do PTAM (Wave 3) — mesma exposição já aceita para CRECI, sem novo vetor |

**Sem ameaças de alta severidade.** Esta plan reordena navegação e adiciona campos ao estado em
memória `LZ` — nenhuma nova superfície de rede/persistência além do já existente `radar_prof`.
</threat_model>

<verification>
- `node --test "tests/*.test.mjs" >/dev/null 2>&1 || echo skip` passa
- `node --test "tests/*.test.mjs"` passa 100% (zero regressão — 11-01 + Fases 8/9/10)
- Fluxo manual (mobile 375 + desktop 1280): clicar "📄 Gerar documento" abre o Seletor de Finalidade; escolher qualquer finalidade mostra a recomendação + 3 opções tocáveis; "Documento técnico formal" sem CNAI recomenda relatório com o card PTAM ainda clicável; escolher qualquer documento avança ao passo "O imóvel"; navegar até "Antes de gerar" mostra nível de confiança + checklist reativo; preencher área no checklist atualiza o nível na mesma tela; "Revisão final" mostra os campos editáveis + preview de assinatura; "Gerar PDF" na Revisão é o único disparo de `montarLaudo()`
- Nenhum elemento tocável novo mede menos de 44px (DevTools mobile 375)
- Perfis `radar_prof` salvos ANTES desta fase (só `{nome,creci,contato}`, sem `cnai`) continuam abrindo o wizard sem erro (campo `cnai` ausente vira `undefined`, tratado como falsy em toda lógica)
</verification>

<success_criteria>
- DOC-01 (fluxo de finalidade) cumprido: Seletor de Finalidade substitui a entrada direta, recomendação visível com "porque" específico, 3 opções sempre tocáveis
- DOC-02 (painel antes de gerar) cumprido: passo Confiança+Pendências antes da Revisão, reativo, nunca bloqueante
- DOC-03 (revisão antes do PDF) cumprido: passo Revisão com campos sensíveis editáveis, CTA final único disparo do PDF, guarda ec9f129 preservada
- CNAI separado do CRECI em radar_prof, compat com perfis existentes
- Wave 3 (11-03) pode ler `LZ.tipoDoc`/`LZ.docOk`/`LZ.prof.cnai` sem reabrir esta plan
</success_criteria>

<output>
Após completar, criar `.planning/phases/11-documentos-em-3-niveis/11-02-SUMMARY.md`.
Incluir no SUMMARY: mapa final de `LZ.step` (0-5, o que cada um faz), lista de campos novos em `LZ`/`radar_prof`, e confirmação de que `montarLaudo()` só é chamado a partir do passo 5 via clique real (guarda ec9f129).
</output>
