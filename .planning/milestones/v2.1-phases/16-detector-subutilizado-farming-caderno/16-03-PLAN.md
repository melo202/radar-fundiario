---
phase: 16-detector-subutilizado-farming-caderno
plan: 03
type: execute
wave: 3
depends_on: ["16-01", "16-02"]
files_modified:
  - radar-goiania.html
autonomous: false
requirements: [TERR-04, TERR-05]
tags: [detector, ui, view-swap, pinos, mapa, checkpoint, persistence]
user_setup: []

must_haves:
  truths:
    - "Botão 'Detectar oportunidades' no painel do território abre o detector como view-swap dentro de #terrPanel (sem sheet nova)"
    - "O detector reusa o scan já cacheado (TERRCACHE via territorioScan) — ZERO requisição própria"
    - "Cada lote detectado mostra leitura comercial + chips de métrica + 'Ver ficha' + '📓 Salvar no caderno'"
    - "Lotes detectados destacam no mapa com pino 🏗️ (--gold) e nova entrada na legenda #pinoLegenda"
    - "Persistência entre sessões e ausência de PII no IndexedDB confirmadas manualmente (DevTools)"
  artifacts:
    - path: "radar-goiania.html"
      provides: "#terrDetectorView + detectarOportunidadesUI + fecharDetector + destaque no mapa + botão em #terrActions"
      contains: "terrDetectorView"
  key_links:
    - from: "detectarOportunidadesUI"
      to: "territorioScan(TERR_PANEL_CD) + detectarSubutilizados"
      via: "reuso do cache TERRCACHE (dedupe); zero requisição nova"
      pattern: "detectarSubutilizados"
    - from: "item do detector"
      to: "salvarNoCadernoUI/cadernoSalvar"
      via: "botão 📓 Salvar no caderno por item"
      pattern: "cadernoSalvar"
---

<objective>
Entregar a UI do Detector de Lote Subutilizado (TERR-04): view-swap "Lotes subutilizados" dentro de `#terrPanel`, botão "Detectar oportunidades" em `#terrActions`, reuso do scan compartilhado (zero requisição própria), salvar do detector no caderno, destaque no mapa com pino 🏗️, e verificação manual de persistência/ausência de PII (checkpoint).

Purpose: Fechar a fase com a ferramenta que o corretor usa para achar oportunidade no território, sobre a base já testada dos Planos 01/02.
Output: Detector funcional na UI + destaque no mapa + gate de verificação manual (T5 zero-requisição + persistência + PII).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/16-detector-subutilizado-farming-caderno/16-RESEARCH.md
@.planning/phases/16-detector-subutilizado-farming-caderno/16-UI-SPEC.md
@.planning/phases/16-detector-subutilizado-farming-caderno/16-VALIDATION.md
@.planning/phases/16-detector-subutilizado-farming-caderno/16-01-SUMMARY.md
@.planning/phases/16-detector-subutilizado-farming-caderno/16-02-SUMMARY.md

<interfaces>
<!-- Do Plano 01 (RADAR_PURE): -->
detectarSubutilizados(lotes, opts)   // → array ordenado (mais subutilizado primeiro), truncado
leituraDetector(item)                // "🏗️ Terreno vago em quadra valorizada" | "🏗️ Baixo aproveitamento em quadra valorizada"
rotuloAmostra(n,total)               // "Amostra de {N} de {M} lotes"

<!-- Do Plano 02: -->
salvarNoCadernoUI() / cadernoSalvar(item) / renderCadernoBtn() / cadernoDisponivel()

<!-- Âncoras existentes em radar-goiania.html: -->
#terrPanel (.detail, HTML 1047-1071); #terrGrid (1055); #terrActions (1059) — 1 primária "Colorir mapa por valor" + 1 secundária "Buscar lote no setor"; .maisopcoes/#terrMetodologia (1063-1070)
TERR_PANEL_CD (3048; setado 3101)    // cdbairro aberto no painel
territorioScan(cd)                    // dedupe via TERRCACHE — NÃO re-scanear se já escaneado
abrirTerritorio (3068); montarPainel (3098-3163); fecharTerrPanel (3170-3176)
MOTION_MSG (2920)                     // adicionar `procurando: "Procurando lotes subutilizados…"`
loading(true, msg) / loading(false)
#pinoLegenda (979-984) — 5 <span><i class="pl-dot">; --gold=#a8842c (:root ~35), já usado por Atenção e Caixa
esc()                                 // escapar endereço/texto no card do detector
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: View-swap do detector em #terrPanel + botão + reuso do scan</name>
  <read_first>
    - radar-goiania.html (1047-1071 #terrPanel/#terrGrid/#terrActions/.maisopcoes; 2920 MOTION_MSG; 3048/3098-3176 TERR_PANEL_CD/montarPainel/fecharTerrPanel; 385 .backlist)
    - .planning/phases/16-detector-subutilizado-farming-caderno/16-UI-SPEC.md (§1 Detector view-swap — estrutura completa, item, chips, ações; §Copywriting)
    - .planning/phases/16-detector-subutilizado-farming-caderno/16-RESEARCH.md (§Detector reuso do scan compartilhado — snippet detectarOportunidadesUI)
  </read_first>
  <files>radar-goiania.html</files>
  <action>
    1. Adicionar `procurando: "Procurando lotes subutilizados…"` ao objeto `MOTION_MSG` (2920).
    2. Em `#terrPanel`, adicionar HTML estático `<div id="terrDetectorView" hidden>` irmão de `#terrGrid`, contendo: `.backlist` "‹ Território" (`onclick="fecharDetector()"`); heading "Lotes subutilizados" (`#terrDetectorHeading`, 18/700); rótulo de honestidade (10.5/700 mono, `--muted`) via `rotuloAmostra`; lista `#terrDetectorList` (`.terrdet-list`); `<details class="maisopcoes">` "Como funciona?" com o corpo locked (UI-SPEC §Copywriting).
    3. Adicionar botão "Detectar oportunidades" (`#terrDetectBtn`) em `#terrActions` como **2ª ação secundária** (respeitando o teto: 1 primária + 2 secundárias), `onclick="detectarOportunidadesUI()"`.
    4. `async function detectarOportunidadesUI()`: se `TERR_PANEL_CD==null` → `toast("Nenhum setor aberto no momento.")` e retorna; `loading(true,MOTION_MSG.procurando)`; `try{ const scan=await territorioScan(TERR_PANEL_CD); const cand=detectarSubutilizados(scan.lotes); renderDetectorLista(cand, scan); mostrarDetectorView(); } catch(e){ toast("Não foi possível varrer o setor. Verifique a conexão e tente de novo."); } finally{ loading(false); }`. **NÃO chamar `territorioScanRun` nem qualquer fetch novo — só `territorioScan`, que dedupe via TERRCACHE (T-16-05, zero requisição própria).**
    5. `function mostrarDetectorView()`: esconde `#terrGrid` + `.maisopcoes` do choropleth, mostra `#terrDetectorView` (view-swap interno; NÃO abre `.detail` nova; NÃO entra na cadeia de Esc — herdado). `function fecharDetector()`: inverte a visibilidade; NÃO chama `fecharTerrPanel()` (o painel continua aberto).
    6. `function renderDetectorLista(cand, scan)`: rótulo de honestidade `rotuloAmostra(scan.lotes.length, scan.total)`; nota de truncamento "Mostrando os 50 lotes mais subutilizados desta amostra." quando `total>50`; empty state (heading "Nenhuma oportunidade encontrada" + body locked) quando `cand.length===0`; para cada item, card `.terrdet-item`: Linha 1 endereço (ou "Quadra {q} · Lote {l}") via `esc()`; Linha 2 `leituraDetector(item)`; Linha 3 chips `.terrdet-chip` somente-leitura ("Constr. {areaedif} m²" · "Terreno {areaterr} m²" · "R$/m² quadra {valor}"); Linha 4 ações "Ver ficha" (abre a ficha do lote) + "📓 Salvar no caderno" (chama a lógica de salvar do Plano 02 com o item do detector; `.then(toast "Salvo no caderno.").catch(toast ERRO_ESCRITA)`; reflete "✓ No caderno" se já existir). Se o CSS `.terrdet-*` não foi criado no Plano 02, criá-lo aqui (escala 4px, tokens UI-SPEC).
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "^(ℹ (pass|fail))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "id=\"terrDetectorView\"\|id=\"terrDetectorList\"\|id=\"terrDetectBtn\"" radar-goiania.html` = 3
    - `grep -c "function detectarOportunidadesUI\|function fecharDetector\|function mostrarDetectorView\|function renderDetectorLista" radar-goiania.html` = 4
    - `grep -c "procurando: *\"Procurando lotes subutilizados" radar-goiania.html` = 1
    - `grep -n "detectarOportunidadesUI" radar-goiania.html` mostra `territorioScan(` e NÃO `territorioScanRun(`/`fetch(` no corpo (zero requisição própria — T-16-05)
    - `grep -c "Lotes subutilizados\|Nenhuma oportunidade encontrada\|Mostrando os 50 lotes\|Como funciona?" radar-goiania.html` ≥ 4
    - `grep -c "leituraDetector\|detectarSubutilizados\|rotuloAmostra" radar-goiania.html` ≥ 3 (funções do Plano 01 cabladas)
    - `npm test` → `fail 0`
  </acceptance_criteria>
  <done>Detector abre como view-swap no #terrPanel reusando o scan cacheado (zero requisição), lista os candidatos com leitura/chips/ações, "‹ Território" volta; suíte verde.</done>
</task>

<task type="auto">
  <name>Task 2: Destaque no mapa (pino 🏗️) + entrada na legenda</name>
  <read_first>
    - radar-goiania.html (979-984 #pinoLegenda; ~35 :root --gold; ~3827 função que popula a legenda; camada de lote do Leaflet no zoom de território)
    - .planning/phases/16-detector-subutilizado-farming-caderno/16-UI-SPEC.md (§1 Destaque no mapa; §Color pino do detector; §5 Acessibilidade — nunca só cor)
  </read_first>
  <files>radar-goiania.html</files>
  <action>
    1. Adicionar 6ª entrada em `#pinoLegenda`: `<span><i class="pl-dot" style="background:#a8842c"></i>🏗️ Lote subutilizado</span>` — reusa o hex `--gold` (mesmo de Atenção/Caixa), diferenciado só por ícone+rótulo (nunca cor/borda nova — PIN-01).
    2. Ao abrir `#terrDetectorView`, se a camada de lote do Leaflet estiver renderizada no zoom atual, aplicar o estilo de pino "🏗️ Lote subutilizado" (`--gold`) aos lotes detectados (popup com ícone+rótulo, nunca só cor). Isto REFORÇA mas NÃO bloqueia — se a camada de lote não está no zoom atual, o detector funciona só pela lista (sem erro). Limpar o destaque em `fecharDetector()`.
    3. Garantir que o destaque não introduz nenhuma requisição de rede (usa a geometria já carregada); se não houver geometria de lote disponível, apenas não destaca.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "^(ℹ (pass|fail))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "🏗️ Lote subutilizado" radar-goiania.html` ≥ 2 (legenda + popup/aria)
    - `grep -c "pl-dot\".*#a8842c\|background:#a8842c" radar-goiania.html` aumentou em 1 (entrada nova na legenda)
    - `grep -n "fecharDetector" radar-goiania.html` mostra limpeza do destaque do mapa
    - nenhum novo `fetch(`/`jsonp(`/`territorioScanRun(` no caminho de destaque (grep de contexto na função)
    - `npm test` → `fail 0`
  </acceptance_criteria>
  <done>Legenda ganha "🏗️ Lote subutilizado"; lotes detectados destacam no mapa quando a geometria está disponível; destaque limpo ao voltar; zero requisição; suíte verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação manual — persistência, ausência de PII, zero-requisição</name>
  <files>radar-goiania.html (nenhuma edição — verificação em runtime no navegador)</files>
  <action>
    Checkpoint de verificação humana em runtime — IndexedDB e requisições de rede não são testáveis em node:vm, por isso esta verificação é manual (não automatizável). Executar os 6 passos de &lt;how-to-verify&gt; no navegador e registrar os resultados (dump do DevTools sem PII + persistência confirmada) no SUMMARY. Não há edição de código nesta task.
  </action>
  <what-built>
    Detector (view-swap no painel do território, reuso do scan, destaque no mapa) + Caderno completo (salvar da ficha e do detector, editar status/tag/nota, filtros, export/import) sobre IndexedDB `radar_territorio`.
  </what-built>
  <how-to-verify>
    Abrir `radar-goiania.html` no navegador (Chrome/Edge recomendado p/ DevTools):

    1. **Zero-requisição do detector (T-16-05):** abrir DevTools → Network, abrir o painel de um setor (dispara o scan da Fase 15), limpar o Network, clicar "Detectar oportunidades". Esperado: NENHUMA requisição nova de rede disparada pelo detector (ele reusa o scan cacheado). A lista "Lotes subutilizados" aparece com o rótulo "Amostra de N de M lotes".
    2. **Salvar + persistência entre sessões (TERR-05):** salvar um lote pelo botão "📓 Salvar no caderno" (na ficha e/ou no detector) → toast "Salvo no caderno." e o item aparece no bloco "📓 Caderno de território" no painel Consulta. Editar status (ex.: "Conversei"), digitar uma tag e uma nota. Fechar a aba, reabrir o app → o item continua lá com status/tag/nota preservados.
    3. **Ausência de PII (TERR-05, critério de aceite 4):** DevTools → Application → IndexedDB → `radar_territorio` → store `caderno` → inspecionar os itens salvos. Confirmar visualmente que NENHUM campo de PII aparece — em especial `dtnascimen`, `cpf`, `cnpj`, `nmcontrib`, `nmproprie`, `nome`. Só campos da allowlist (ci/cdbairro/nrquadra/nrlote/vlvenal/areaedif/areaterr/vlimp98/dtinclusao/uso/endereco/tag/nota/status/savedAt/updatedAt).
    4. **Falha visível:** (opcional se viável) simular indisponibilidade (janela privada restritiva) → botões "📓 Salvar no caderno" desabilitados com aviso; nenhuma falha silenciosa.
    5. **Export/import roundtrip:** "⬇ Exportar caderno (JSON)" baixa o arquivo; "⬆ Importar caderno" com esse arquivo restaura/mescla os itens (toast "{N} lotes importados..."); importar um arquivo não-JSON → toast "Arquivo inválido — não é um backup do caderno."
    6. **XSS:** digitar em uma nota algo como `<img src=x onerror=alert(1)>` → salvar/reabrir → o texto aparece literal (escapado), nenhum alerta dispara.
  </how-to-verify>
  <verify>Manual — ver &lt;how-to-verify&gt;. Sem comando automatizado (IndexedDB/rede não existem em node:vm).</verify>
  <done>Os 6 passos verificados no navegador; resultado do DevTools (sem PII) e da persistência registrados no SUMMARY.</done>
  <resume-signal>Digite "aprovado" ou descreva o que falhou (com o passo). Registrar o resultado do DevTools (ausência de PII) e da persistência no SUMMARY.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| painel território → detector | filtro client-side sobre dado já em memória |
| endpoint ArcGIS | detector NÃO deve cruzá-lo — reusa cache |
| endereço/leitura do lote → DOM | render do card do detector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Denial of Service | detectarOportunidadesUI | mitigate | Detector NUNCA dispara requisição própria — reusa `territorioScan` que dedupe via TERRCACHE. Verificado ao vivo no checkpoint (Network vazio após o scan). |
| T-16-03 | Tampering (XSS) | renderDetectorLista | mitigate | Endereço e qualquer texto de origem do endpoint renderizados via esc(); leitura comercial é template determinístico (nunca interpola texto cru). Verificado no checkpoint (payload de nota escapado). |
| T-16-01 | Information Disclosure | salvar do detector | mitigate | Reusa cadernoSalvar → sanitizeCaderno (allowlist). Ausência de PII confirmada no DevTools (checkpoint passo 3). |
</threat_model>

<verification>
- `npm test` verde (a UI não altera a suíte pura).
- Checkpoint humano: zero-requisição (Network), persistência entre sessões, ausência de PII (DevTools dump), export/import roundtrip, XSS escapado.
</verification>

<success_criteria>
- Detector abre como view-swap, reusa o scan (zero requisição), lista candidatos com ação por item.
- Destaque no mapa com pino 🏗️ + legenda.
- Persistência e ausência de PII confirmadas manualmente (registradas no SUMMARY).
- Nenhuma dependência nova; suíte verde.
</success_criteria>

<output>
Após completar, criar `.planning/phases/16-detector-subutilizado-farming-caderno/16-03-SUMMARY.md` — incluir o resultado do checkpoint (dump DevTools sem PII + persistência confirmada).
</output>
