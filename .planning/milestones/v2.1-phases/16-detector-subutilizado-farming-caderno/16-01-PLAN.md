---
phase: 16-detector-subutilizado-farming-caderno
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/fixtures.mjs
  - tests/caderno.test.mjs
  - radar-goiania.html
autonomous: true
requirements: [TERR-04, TERR-05]
tags: [detector, caderno, indexeddb, allowlist, pure-functions, tdd]
user_setup: []

must_haves:
  truths:
    - "detectarSubutilizados retorna lista ordenada (mais subutilizado primeiro) de lotes em quadra valorizada com baixa ocupação"
    - "razaoOcupacao distingue areaedif===0 (terreno vago, INCLUI) de areaedif==null/undefined (registro incompleto, EXCLUI)"
    - "sanitizeCaderno é allowlist POSITIVA — só campos permitidos sobrevivem; dtnascimen/cpf/nome/campo-desconhecido nunca passam"
    - "validarImportCaderno rejeita JSON malformado e sanitiza cada item antes de aceitar"
    - "npm test verde com os novos testes de detector + caderno"
  artifacts:
    - path: "tests/caderno.test.mjs"
      provides: "TDD das funções puras do detector + caderno via loadPureBlock()"
      contains: "loadPureBlock"
    - path: "tests/fixtures.mjs"
      provides: "DETECTOR_FIX + CADERNO_FIX (bordas 0-vs-null, PII, import inválido)"
      contains: "DETECTOR_FIX"
    - path: "radar-goiania.html"
      provides: "Funções puras detector + decisão do caderno no bloco RADAR_PURE"
      contains: "function detectarSubutilizados"
  key_links:
    - from: "tests/caderno.test.mjs"
      to: "radar-goiania.html RADAR_PURE"
      via: "slice por linha RADAR_PURE_START/_END + vm.createContext + __exports"
      pattern: "globalThis.__exports"
    - from: "detectarSubutilizados"
      to: "medianasPorQuadra + limiarQuadraValorizada + razaoOcupacao"
      via: "composição de funções puras"
      pattern: "razaoOcupacao"
---

<objective>
Criar as funções puras (TDD, RED→GREEN) do Detector de Lote Subutilizado (TERR-04) e as funções puras de decisão do Caderno (TERR-05) dentro do bloco `RADAR_PURE` de `radar-goiania.html`, com harness de teste novo `tests/caderno.test.mjs` e fixtures dedicadas.

Purpose: Toda lógica de DECISÃO (filtro do detector, allowlist anti-PII, enum de status, validação de import) deve ser pura e coberta por teste ANTES de qualquer I/O. A camada IndexedDB e a UI (Planos 02/03) dependem destes contratos.
Output: 6 funções do detector + 3 funções/constantes de decisão do caderno em RADAR_PURE, mais suíte de teste verde.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-detector-subutilizado-farming-caderno/16-RESEARCH.md
@.planning/phases/16-detector-subutilizado-farming-caderno/16-VALIDATION.md

<interfaces>
<!-- Funções puras JÁ existentes no bloco RADAR_PURE (radar-goiania.html 1230-2210) que as novas funções REUSAM. Não reimplementar mediana. -->

pm2Lote(a)          // 2120-2124: R$/m² por lote. v=+a.vlvenal||0; ae=+a.areaedif||0; at=+a.areaterr||0; area=ae>0?ae:at; return (v>0&&area>0)?v/area:null
                    // ATENÇÃO: usa ||0 (não distingue 0 de null) — CORRETO para pm2, ERRADO para a guarda de qualidade do detector. NÃO copiar esse padrão para razaoOcupacao.
quantilAmostra(sorted,p)  // 2128-2131: interpolação linear sobre array JÁ ordenado ascendente. Reusar para mediana (p=.5) e Q3 (p=.75).
rotuloAmostra(n,total)    // 2207-2209: "Amostra de {N} de {M} lotes" — reuso literal pelo detector UI (Plano 03).

<!-- Blocklist de PII já existente (referência de PADRÃO, NÃO reusar direto — o caderno exige allowlist, modelo inverso mais forte): -->
SENS = ["dtnascimen","cpf","cnpj","nmcontrib","nmproprie","nome"]  // radar-goiania.html linha 1221
sanitiza(arr)  // 1222: blocklist (delete dos campos de SENS). NÃO usar para o caderno.
</interfaces>

<test_harness>
<!-- Padrão do loader a replicar (tests/territorio.test.mjs 15-41). Slice POR LINHA, não indexOf cru. -->
Marcadores: html.indexOf("RADAR_PURE_START") / html.indexOf("RADAR_PURE_END")
start = html.indexOf("\n", iStart) + 1;  end = html.lastIndexOf("\n", iEnd);  src = html.slice(start, end);
vm.createContext(sandbox); new vm.Script(src + "\n;globalThis.__exports = {...};").runInContext(sandbox);
Assertions de presença (assert.ok(src.includes("function X"))) ANTES do vm.Script — falham limpo em RED.
Fixtures importadas de ./fixtures.mjs (FIXTURES.TERR_FIX já existe em fixtures.mjs:699).
</test_harness>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (RED): fixtures + harness tests/caderno.test.mjs falhando</name>
  <read_first>
    - tests/territorio.test.mjs (linhas 1-45: padrão loadPureBlock; 45+: estilo dos casos)
    - tests/fixtures.mjs (linha 699: bloco TERR_FIX, para espelhar formato)
    - .planning/phases/16-detector-subutilizado-farming-caderno/16-RESEARCH.md (§Architecture Patterns → Detector; §Common Pitfalls 1 e 2; §Validation Architecture)
  </read_first>
  <files>tests/fixtures.mjs, tests/caderno.test.mjs</files>
  <action>
    1. Em `tests/fixtures.mjs`, adicionar ao objeto `FIXTURES` duas chaves novas:
       - `DETECTOR_FIX`: array de lotes de teste cobrindo os casos-borda OBRIGATÓRIOS:
         * quadra valorizada (pm2 mediano alto) com lote `areaedif:0, areaterr:400` (terreno vago REAL → DEVE ser candidato);
         * MESMA quadra com lote `areaedif:null, areaterr:400` (dado incompleto → NUNCA candidato);
         * lote com `areaedif:undefined, areaterr:400` (idem, nunca candidato);
         * lote com `areaedif:380, areaterr:400` (ocupação alta → não candidato);
         * lote com `areaedif:30, areaterr:400` (razão 0.075 < 0.15 → candidato) em quadra valorizada;
         * quadra de baixo valor com lote de razão baixa (fora do quartil superior → não candidato);
         * pelo menos 4 quadras distintas com pm2 válido (para o Q3 existir) + 1 cenário com <4 quadras (limiar null).
         Cada lote: `{nrquadra, vlvenal, areaedif, areaterr}` (nrlote/endereco/ci opcionais para os testes puros).
       - `CADERNO_FIX`: objeto com: `itemValido` (com `ci, cdbairro, nrquadra, nrlote, vlvenal, areaedif, areaterr, uso, endereco`), `itemComPII` (o mesmo + `dtnascimen:"1980-01-01", cpf:"...", nmproprie:"...", campoInventado:"x"`), `importValido` (array de 2 itens válidos), `importMalformado` (string não-JSON), `importItemSemCi` (array com 1 item sem `ci`), `importItemComPII` (array com item carregando `dtnascimen`).
    2. Criar `tests/caderno.test.mjs` replicando o `loadPureBlock()` de `tests/territorio.test.mjs`. O `__exports` DEVE reexportar: `medianasPorQuadra, limiarQuadraValorizada, razaoOcupacao, detectarSubutilizados, leituraDetector, DETECTOR_RATIO_MAX, sanitizeCaderno, CADERNO_STATUS, statusValido, validarImportCaderno`.
       Incluir assertions de presença `assert.ok(src.includes("function detectarSubutilizados"))` etc. ANTES do `vm.Script`.
    3. Escrever os testes (todos falharão agora — RED — porque as funções ainda não existem em RADAR_PURE):
       - detector: agrupamento por quadra ignora nrquadra null e pm2 null; limiar Q3 correto e `null` para <4 quadras; **razaoOcupacao: `areaedif:0` → 0 (candidato); `areaedif:null`/`undefined` → null (excluído)** (espelha Pitfall 1 — teste com 0 e com null na MESMA area DEVE dar resultados diferentes); detectarSubutilizados ordena razão crescente, trunca em opts.limite, retorna [] quando limiar null; leituraDetector "Terreno vago..." se areaedif===0 senão "Baixo aproveitamento...".
       - caderno: `sanitizeCaderno(itemComPII)` NÃO contém `dtnascimen`/`cpf`/`nmproprie`/`campoInventado` e SIM contém `ci`/`cdbairro` (Pitfall 2 — allowlist); `statusValido` aceita os 5 do enum e rejeita fora; `validarImportCaderno(importMalformado)` retorna erro/estrutura de falha, `importValido` retorna itens sanitizados, `importItemSemCi` descarta o item, `importItemComPII` retorna item sem `dtnascimen`.
  </action>
  <verify>
    <automated>node --test tests/caderno.test.mjs; test $? -ne 0 && echo "RED OK (falha esperada: funções ainda não existem)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DETECTOR_FIX\|CADERNO_FIX" tests/fixtures.mjs` ≥ 2
    - `grep -c "globalThis.__exports" tests/caderno.test.mjs` = 1
    - `grep -c "areaedif: *0\|areaedif: *null" tests/fixtures.mjs` ≥ 2 (borda 0-vs-null presente)
    - `node --test tests/caderno.test.mjs` sai com código ≠ 0 (RED — funções ainda não em RADAR_PURE)
    - `npm test` das demais suítes continua verde (nenhuma regressão introduzida ainda)
  </acceptance_criteria>
  <done>Fixtures e harness criados; a nova suíte falha por ausência das funções (RED legítimo); as 121 anteriores continuam verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): funções puras do Detector em RADAR_PURE</name>
  <read_first>
    - radar-goiania.html (linhas 2120-2131: pm2Lote/quantilAmostra; 2207-2210: rotuloAmostra e RADAR_PURE_END)
    - .planning/phases/16-detector-subutilizado-farming-caderno/16-RESEARCH.md (§Architecture Patterns → Detector — snippets de referência)
  </read_first>
  <files>radar-goiania.html</files>
  <behavior>
    - medianasPorQuadra(lotes): {nrquadra: medianaPm2}; ignora lote com nrquadra==null ou pm2Lote()==null; quadra sem nenhum pm2 válido não aparece.
    - limiarQuadraValorizada(medianas): Q3 (quantilAmostra(vals,.75)) da distribuição dos medianos; retorna null se <4 quadras distintas.
    - razaoOcupacao(a): +areaedif/+areaterr; null se areaterr não >0; null se areaedif==null (NUNCA `!ae`/`ae?`/`||0`); areaedif===0 retorna 0.
    - detectarSubutilizados(lotes, opts): quadra com mediana>=limiar E razaoOcupacao<=opts.razaoMax (default DETECTOR_RATIO_MAX); exclui razão null; ordena razão crescente; trunca opts.limite (default DETECTOR_LIMITE=50); [] se limiar null.
    - leituraDetector(item): "🏗️ Terreno vago em quadra valorizada" se +areaedif===0, senão "🏗️ Baixo aproveitamento em quadra valorizada".
  </behavior>
  <action>
    Inserir ANTES da linha `RADAR_PURE_END` (2210), depois de `rotuloAmostra`, o bloco do detector. Constantes nomeadas e explicáveis (determinismo auditável):
    `const DETECTOR_RATIO_MAX=0.15; /* "menos de 15% do terreno construído" — razão construído/terreno abaixo da qual o lote é candidato. Fixo/explicável (Claude's Discretion, CONTEXT §Detector); upgrade p/ potencial-do-PD na Fase 18. */`
    `const DETECTOR_LIMITE=50;`
    Implementar as 5 funções conforme <behavior> e os snippets de referência do RESEARCH (§Architecture Patterns → Detector). REUSAR `pm2Lote` e `quantilAmostra` — NÃO escrever fórmula nova de mediana. `razaoOcupacao` usa `!=null` explícito (nunca coerção truthy — Pitfall 1). `detectarSubutilizados(lotes, opts)` faz `Object.assign({razaoMax:DETECTOR_RATIO_MAX,limite:DETECTOR_LIMITE},opts||{})`.
    Rodar `node --test tests/caderno.test.mjs` até os testes do DETECTOR ficarem verdes (os do caderno ainda falham até a Task 3 — aceitável neste passo).
  </action>
  <verify>
    <automated>node --test tests/caderno.test.mjs 2>&1 | grep -E "detectarSubutilizados|razaoOcupacao|medianasPorQuadra|leituraDetector"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DETECTOR_RATIO_MAX=0.15\|DETECTOR_RATIO_MAX = 0.15" radar-goiania.html` ≥ 1
    - `grep -c "function medianasPorQuadra\|function limiarQuadraValorizada\|function razaoOcupacao\|function detectarSubutilizados\|function leituraDetector" radar-goiania.html` = 5
    - `grep -n "razaoOcupacao" radar-goiania.html` mostra `!=null` (guarda explícita, sem `||0`/`?:` na razão)
    - todos os testes de detector em `tests/caderno.test.mjs` passam (grep por ✔ nas linhas do detector)
  </acceptance_criteria>
  <done>As 5 funções + 2 constantes do detector estão em RADAR_PURE; testes do detector verdes, incluindo a borda 0-vs-null.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (GREEN): funções puras de decisão do Caderno em RADAR_PURE</name>
  <read_first>
    - radar-goiania.html (linha 1221-1222: SENS/sanitiza — padrão de referência, NÃO reusar)
    - .planning/phases/16-detector-subutilizado-farming-caderno/16-RESEARCH.md (§LGPD/allowlist; §Common Pitfalls 2)
    - .planning/phases/16-detector-subutilizado-farming-caderno/16-CONTEXT.md (allowlist exata de campos)
  </read_first>
  <files>radar-goiania.html</files>
  <behavior>
    - sanitizeCaderno(obj): retorna objeto novo contendo SÓ campos da allowlist positiva presentes em obj; nunca dtnascimen/cpf/nome; campo desconhecido descartado.
    - statusValido(s): true se s ∈ CADERNO_STATUS, senão false.
    - validarImportCaderno(json): parseia (string ou já-array); retorna {ok:false} p/ malformado; {ok:true, itens:[...]} com cada item sanitizado e só itens com `ci` mantidos.
  </behavior>
  <action>
    Inserir junto ao bloco do detector (antes de RADAR_PURE_END):
    `const CADERNO_ALLOW=["ci","nrinscr","cdbairro","nrquadra","nrlote","vlvenal","areaedif","areaterr","vlimp98","dtinclusao","uso","endereco","tag","nota","status","savedAt","updatedAt"];`
    `const CADERNO_STATUS=["nao_visitado","visitei","conversei","recusou","fechou"]; /* enum fixo, ordem = ciclo de campo */`
    `function sanitizeCaderno(obj){const out={};CADERNO_ALLOW.forEach(k=>{if(obj&&obj[k]!==undefined)out[k]=obj[k];});return out;}` (allowlist POSITIVA — nunca `delete` de lista negra; Pitfall 2).
    `function statusValido(s){return CADERNO_STATUS.indexOf(s)>-1;}`
    `function validarImportCaderno(json){let arr;try{arr=typeof json==="string"?JSON.parse(json):json;}catch(e){return {ok:false,erro:"json"};} if(!Array.isArray(arr))return {ok:false,erro:"shape"}; const itens=arr.filter(o=>o&&o.ci!=null).map(sanitizeCaderno); return {ok:true,itens};}`
    Garantir que a nota/tag não são executadas nem validadas por regex de formato (só allowlist de shape — validação de conteúdo/escape é responsabilidade da UI no Plano 02 via esc()/textContent). Rodar toda a suíte até verde.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "^(ℹ (tests|pass|fail))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function sanitizeCaderno\|function statusValido\|function validarImportCaderno" radar-goiania.html` = 3
    - `grep -c "CADERNO_STATUS=\[\"nao_visitado\",\"visitei\",\"conversei\",\"recusou\",\"fechou\"\]\|CADERNO_STATUS = " radar-goiania.html` ≥ 1
    - `grep -n "sanitizeCaderno" radar-goiania.html` mostra padrão allowlist (`CADERNO_ALLOW.forEach`), nunca `delete`
    - `npm test` → `fail 0`; total de testes > 121 (novos casos somados)
  </acceptance_criteria>
  <done>sanitizeCaderno (allowlist), enum de status e validarImportCaderno em RADAR_PURE; `npm test` inteiramente verde (GREEN completo).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| endpoint ArcGIS → objeto de lote | dado cadastral cru (pode conter campos de PII) entra na aplicação |
| arquivo JSON de import → caderno | arquivo externo/editável entra na fronteira de persistência |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | sanitizeCaderno | mitigate | Allowlist POSITIVA (só CADERNO_ALLOW sobrevive) — campo sensível novo do endpoint NUNCA passa por padrão (fail-safe). Teste anti-regressão com `dtnascimen`/`cpf`/`campoInventado` presentes no input. |
| T-16-02 | Tampering | validarImportCaderno | mitigate | Import valida shape (Array), descarta item sem `ci`, aplica sanitizeCaderno a CADA item — nunca confia no arquivo; nunca `eval`/`Function`/parse executável. Teste com JSON malformado + item com PII. |
| T-16-04 | Denial of Service | detectarSubutilizados | accept | Função pura O(n log n) sobre array já em memória (scan da Fase 15); sem recursão/loop ilimitado; limite de saída (DETECTOR_LIMITE). Sem superfície de rede nesta plan. |
</threat_model>

<verification>
- `npm test` verde (baseline 121 + novos casos do detector/caderno), `fail 0`.
- Borda 0-vs-null coberta e passando (Pitfall 1): `areaedif:0` INCLUI, `areaedif:null`/`undefined` EXCLUI.
- Allowlist confirmada (Pitfall 2): campo fora de CADERNO_ALLOW descartado; `dtnascimen` nunca sobrevive.
</verification>

<success_criteria>
- 5 funções + 2 constantes do detector e 3 funções + 2 constantes do caderno em RADAR_PURE.
- `tests/caderno.test.mjs` + fixtures novas; suíte completa verde.
- Nenhuma requisição de rede introduzida; nenhuma dependência nova.
</success_criteria>

<output>
Após completar, criar `.planning/phases/16-detector-subutilizado-farming-caderno/16-01-SUMMARY.md`.
</output>
