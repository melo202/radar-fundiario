---
phase: 11.1-documentos-da-negociacao
plan: 03
type: execute
wave: 3
depends_on: ["11.1-01", "11.1-02"]
files_modified:
  - radar-goiania.html
autonomous: true
requirements: [NEG-01, NEG-02, NEG-03]
tags: [templates-impressos, a4, print-css, clausulas, assinaturas, disclaimer, privacidade, end-to-end]

must_haves:
  truths:
    - "montarNeg() monta o template A4 estruturado dos 3 documentos (cabecalho .lbrand com o nome do documento, clausulas numeradas em blocos .negclausula-item, disclaimer em caixa destacada .negdisclaimer-print, bloco(s) de assinatura .lass com papeis + testemunhas) pelo MESMO pipeline #laudo->#laudoView->imprimirLaudo() — PDF sai SO do clique real (ec9f129)"
    - "A revisao editavel e aplicada ao texto final: NEG.minutaEditada (quando o usuario editou) e a fonte do corpo impresso, nunca ignorada"
    - "Todo dado digitado (nomes/docs/enderecos/matricula/cartorio/texto editado) passa por esc() antes de entrar no innerHTML do documento (IN-01)"
    - "Print CSS A4: clausulas com page-break-inside:avoid, disclaimer impresso com borda destacada, testemunhas lado a lado (Contrato e Termo com 2 testemunhas; Proposta pode omitir)"
    - "Privacidade verificada end-to-end: nenhum localStorage.setItem/IndexedDB recebe dado do NEG apos gerar qualquer um dos 3 documentos (assert negativo por grep + contagem exata)"
  artifacts:
    - path: "radar-goiania.html"
      provides: "montarNeg() completo (template A4 estruturado por documento) + CSS .negclausulas/.negclausula-item/.negdisclaimer-print/.lass-testemunhas (tela e @media print)"
      contains: "negdisclaimer-print"
  key_links:
    - from: "montarNeg()"
      to: "#laudo -> #laudoViewBody -> imprimirLaudo()"
      via: "mesmo fechamento de renderFichaRapida()/montarLaudo() — innerHTML copy + laudoView.hidden=false; PDF so no clique em Salvar PDF"
      pattern: "laudoViewBody"
    - from: "montarNeg()"
      to: "NEG.minutaEditada || NEG.minutaTexto"
      via: "corpo do documento e o texto REVISADO pelo usuario (edicao nunca descartada)"
      pattern: "minutaEditada"
    - from: "template impresso"
      to: "esc()"
      via: "toda interpolacao de dado digitado/editado no innerHTML do documento"
      pattern: "esc\\("
---

<objective>
Completar `montarNeg()` em `radar-goiania.html`: substituir o corpo mínimo da Wave 2 pelo template A4 estruturado dos 3 documentos (cabeçalho com nome do documento, cláusulas numeradas como blocos com quebra de página controlada, disclaimer em caixa destacada, blocos de assinatura das partes + testemunhas), com print CSS A4, revisão editável aplicada ao texto final, e verificação end-to-end (asserts de DOM/string + assert negativo de privacidade).

Purpose: NEG-01/02/03 fecham com o PDF real — "minuta editável → PDF pt-BR com ressalvas". A Wave 2 deixou o pipeline ligado (CTA → montarNeg() → #laudoView) com corpo provisório; esta plan entrega o documento IMPRESSO com a cara formal que instrumentos jurídicos exigem (cláusulas atômicas para paginação, disclaimer que nunca sai, assinaturas com papéis corretos por documento), preservando a guarda ec9f129 e a decisão de privacidade (nada persiste).
Output: os 3 documentos saem em A4 estruturado pelo clique real; suite de testes + asserts de verificação end-to-end passando; fase 11.1 funcionalmente completa (HUMAN-UAT jurídico do usuário fica como gate humano da fase, fora desta plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-documentos-da-negociacao/11.1-CONTEXT.md
@.planning/phases/11.1-documentos-da-negociacao/11.1-UI-SPEC.md
@.planning/phases/11.1-documentos-da-negociacao/11.1-01-SUMMARY.md
@.planning/phases/11.1-documentos-da-negociacao/11.1-02-SUMMARY.md

# Fonte de código (ler antes de editar)
@radar-goiania.html

<facts>
<!-- Verificado por leitura direta deste repo (estado pre-11.1; a Wave 2 adiciona montarNeg minimo). -->

Pipeline de exibição/impressão (inalterado desde a Fase 11): `#laudo` (div de impressão, linha 874)
→ `laudoViewBody.innerHTML=laudo.innerHTML` → `#laudoView.hidden=false` (padrão nas linhas
3334-3335 de montarLaudo e 3389-3390 de renderFichaRapida). `imprimirLaudo()` (linha 3393) é
disparada SÓ pelo botão "Salvar PDF" do `#laudoView` (clique real — guarda ec9f129);
`fecharLaudoView()` (3404) limpa os dois containers. Esta plan NÃO toca nessas 3 funções.

Classes de impressão já existentes (CSS linhas 465-511, compartilhadas por `#laudo` e `#laudoView`):
`.lrun` (cabeçalho corrido), `.lbrand` (marca + título via `.t small` + `.m` meta), `.lcard`
(cards de seção), `.lass` (assinatura: `.linha` + nome + `.lass-resp`), `.lmiudo` (miúdo),
`.foot` (rodapé). O CSS de impressão usa as vars `--l-accent/--l-ink/--l-label/--l-border/--l-card/
--l-hair` (paleta de papel branco, distinta de `--accent` — VERIFICAR no código os valores ao criar
`.negdisclaimer-print`: usar `var(--l-accent)`, nunca `--accent` cru dentro do contexto #laudo,
conforme nota do UI-SPEC Componente 4).

`@media print` + `body.print-laudo` (por volta das linhas 513-525): mecânica que esconde o app e
mostra só o `#laudo` na impressão — reusada 1:1, as classes novas de cláusula/disclaimer/testemunha
entram no MESMO bloco de estilo compartilhado `#laudo .x,#laudoView .x{...}`.

Estado NEG (Wave 2, 11.1-02-SUMMARY tem o shape final): `NEG.tipo`, `NEG.minutaTexto` (gerado pela
função pura ao entrar na Revisão), `NEG.minutaEditada` (edição do usuário, null se não editou),
partes (`proponente/vendedor/comprador/proprietario/corretor`), `NEG.a` (imóvel). `montarNeg()` da
Wave 2: corpo provisório `white-space:pre-wrap` — esta plan SUBSTITUI o corpo, mantendo assinatura
da função, ponto de chamada (CTA passo 1) e fechamento (fecharNeg + innerHTML copy + hidden=false).

Estrutura de cláusulas por documento e blocos de assinatura: tabelas do UI-SPEC Componente 4
("Estrutura de cláusulas por documento" e "Bloco de assinatura por documento") — Proposta assina
Proponente + Vendedor/Proprietário (testemunhas opcionais — decisão: OMITIR na Proposta, manter a
consistência recomendada só onde é praxe); Termo assina Proprietário + Corretor (nome + CRECI
abaixo da linha) + 2 testemunhas; Contrato assina Vendedor + Comprador + 2 testemunhas
(obrigatório).

O texto da minuta (puro, 11.1-01) já contém as cláusulas numeradas ("Cláusula 1ª — ...") e o
disclaimer como TEXTO. Para o template A4 estruturado há 2 caminhos; esta plan usa o caminho A:

CAMINHO A (adotado): o corpo impresso é o TEXTO REVISADO renderizado como blocos — `montarNeg()`
divide `texto.split(/\n{2,}/)` (parágrafos/cláusulas separados por linha em branco, formato que as
funções puras de 11.1-01 JÁ emitem) e envolve cada bloco em `<div class="negclausula-item">`,
detectando títulos de cláusula (`/^Cláusula\s+\d+ª?/`) para aplicar o `<b>` do título. Vantagem:
a EDIÇÃO do usuário no textarea é respeitada integralmente (NEG.minutaEditada é a fonte), sem
re-gerar nada por fora do que ele revisou. O disclaimer e as assinaturas são EXTRAÍDOS do fluxo:
como o texto puro os contém, montarNeg() detecta a linha do disclaimer (match da string literal ou
do prefixo "Minuta para conferência") e a renderiza na caixa `.negdisclaimer-print` (se o usuário
tiver apagado o disclaimer na edição, montarNeg() o ADICIONA de volta — o disclaimer NUNCA sai do
documento, decisão de responsabilidade); as linhas de assinatura ("________" + papel) viram blocos
`.lass` estruturados com os nomes das partes de NEG (esc()).

Contrato de privacidade (Wave 2): 6 chamadas `localStorage.setItem` no arquivo (radar_sat,
radar_lastbairro, radar_oportunidades, radar_historico, radar_prof, radar_coach) — a contagem NÃO
pode mudar nesta plan, e nenhuma pode receber dado do NEG.

`node --check` NÃO funciona em .html neste ambiente — verificação de sintaxe é pela suite
`node --test` (que avalia o bloco RADAR_PURE) + greps estruturais; NUNCA usar node --check no html.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Template A4 estruturado em montarNeg() (cláusulas em blocos, disclaimer em caixa, assinaturas por documento) + CSS de impressão</name>
  <files>radar-goiania.html</files>
  <action>
1. CSS novo no bloco compartilhado `#laudo .x,#laudoView .x{...}` (junto às classes .fr-* da Fase
11, ~linha 505-511), valores exatos das tabelas do UI-SPEC Componente 4:
- `.negclausulas{margin:8px 0 16px}`
- `.negclausula-item{margin-bottom:14px;page-break-inside:avoid}` + título `b` em
  `font:700 13px/1.3 "Open Sans","Segoe UI",sans-serif;color:var(--l-accent)` (mesmo padrão de
  `.lcard h3`); corpo herda o parágrafo padrão do laudo.
- `.negdisclaimer-print{border:1.5px solid var(--l-accent);border-radius:8px;padding:14px 16px;
  margin:18px 0;font-size:10.5px;line-height:1.5;background:#fff}` com `<b>ATENÇÃO</b>` em
  `color:var(--l-accent)`. CONFERIR no código se `--l-accent` existe como var (nota do UI-SPEC):
  se o CSS de impressão usar hex direto, usar o MESMO hex já usado ali — nunca `--accent` cru.
- `.lass-testemunhas{display:flex;gap:24px;margin-top:20px}` (2 linhas lado a lado; cada uma com
  `.linha` + `.lass-resp` "Testemunha 1"/"Testemunha 2").

2. Substituir o CORPO de `montarNeg()` (Wave 2) pelo template estruturado — assinatura da função,
gatilho (CTA do passo 1) e fechamento (fecharNeg + laudoViewBody copy + hidden=false) INALTERADOS:

- `const texto=NEG.minutaEditada!=null?NEG.minutaEditada:NEG.minutaTexto;` (edição SEMPRE respeitada).
- `const hoje=new Date().toLocaleDateString("pt-BR");`
- Título por tipo: `{proposta:"Proposta de Compra e Venda",exclusividade:"Termo de Autorização/
  Exclusividade de Venda",contrato:"Contrato de Compra e Venda"}[NEG.tipo]`.
- `.lrun`: `RADAR FUNDIÁRIO · MINUTA PARA CONFERÊNCIA` + `EMITIDO EM ${hoje}` (UI-SPEC).
- `.lbrand`: small "Radar Fundiário · Documentos da Negociação" + título; `.m` com corretor
  (nome + CRECI de NEG.corretor SÓ quando preenchido — nunca placeholder) + "Goiânia/GO · ${hoje}".
- CORPO (caminho A dos facts): processar `texto` em blocos:
  - separar o disclaimer: procurar o bloco que contém "Minuta para conferência das partes" — se
    existir, removê-lo do fluxo de blocos (será renderizado na caixa); se o usuário APAGOU o
    disclaimer na edição, a caixa é renderizada MESMO ASSIM com o texto literal do UI-SPEC (o
    disclaimer nunca sai — decisão de responsabilidade, comentar no código).
  - separar as linhas de assinatura do final (blocos que são só "________"/nomes de papel), se as
    funções puras as emitirem como texto — as assinaturas IMPRESSAS vêm dos blocos `.lass`
    estruturados abaixo, não do texto (evita duplicar).
  - blocos restantes → `<div class="negclausulas">` + um `<div class="negclausula-item">` por
    bloco; se o bloco começa com `/^Cláusula\s+\d+ª?\s*—?/` ou é um cabeçalho em CAPS, envolver a
    primeira linha em `<b>`; corpo do bloco em `<p>` com `esc()` (o texto é conteúdo editado pelo
    usuário — IN-01, TODO o corpo passa por esc(), inclusive títulos).
- Caixa: `<div class="negdisclaimer-print"><b>ATENÇÃO</b> — ${disclaimer}</div>` (texto literal).
- Assinaturas estruturadas por documento (nomes via esc(); nome ausente → linha em branco
  "________________________", nunca inventa):
  - proposta: `.lass` Proponente + `.lass` Vendedor/Proprietário (sem testemunhas).
  - exclusividade: `.lass` Proprietário + `.lass` Corretor (com "CRECI ${esc(creci)}" sob o nome
    quando houver) + `.lass.lass-testemunhas` (Testemunha 1 / Testemunha 2).
  - contrato: `.lass` Vendedor + `.lass` Comprador + `.lass.lass-testemunhas`.
- `.foot`: "RADAR FUNDIÁRIO · MINUTA PARA CONFERÊNCIA · EMITIDO EM ${hoje}".
- Fechamento idêntico ao da Wave 2 / renderFichaRapida(): `fecharNeg();` +
  `document.getElementById("laudoViewBody").innerHTML=document.getElementById("laudo").innerHTML;` +
  `lv.hidden=false; lv.scrollTop=0;`.

3. NÃO tocar: `imprimirLaudo()`, `fecharLaudoView()`, `montarLaudo()`, `montarFichaRapida()`,
`renderFichaRapida()`, negRender/negNext (o gatilho já está certo desde a Wave 2).
  </action>
  <verify>
    <automated>node --test "tests/*.test.mjs" 2>&1 | tail -6 && grep -c "negdisclaimer-print" radar-goiania.html && grep -n "lass-testemunhas" radar-goiania.html | head -3 && [ "$(grep -c 'montarNeg(' radar-goiania.html)" = "2" ] && echo "OK: gatilho unico preservado (ec9f129)"</automated>
  </verify>
  <done>montarNeg() monta o A4 estruturado dos 3 documentos (cláusulas em .negclausula-item com page-break-inside:avoid, disclaimer em .negdisclaimer-print nunca omitível, assinaturas .lass por documento com testemunhas onde devidas); NEG.minutaEditada é a fonte do corpo quando o usuário editou; esc() em todo conteúdo digitado/editado; montarNeg( continua com exatamente 2 ocorrências; suite passa.</done>
</task>

<task type="auto">
  <name>Task 2: Verificação end-to-end (asserts estruturais + privacidade + zero regressão)</name>
  <files>radar-goiania.html</files>
  <action>
Passada de verificação/fix final da fase — qualquer assert que falhe é corrigido AQUI (mesmo
arquivo), documentando no SUMMARY:

1. **Suite completa**: `node --test "tests/*.test.mjs"` — 100% pass (64 pré-fase + negocio.test.mjs
da 11.1-01). NUNCA usar `node --check` no .html (não funciona neste ambiente).

2. **Privacidade (assert negativo)**:
- `grep -c "localStorage.setItem" radar-goiania.html` == 6 (mesmas 6 chaves de antes da fase:
  radar_sat/radar_lastbairro/radar_oportunidades/radar_historico/radar_prof/radar_coach).
- Inspecionar as 6 linhas: NENHUMA recebe variável do NEG (grep contextual:
  `grep -n "localStorage.setItem" radar-goiania.html` e conferir que nenhum argumento referencia
  NEG/proponente/vendedor/comprador/proprietario/matricula).
- `grep -ci "indexedDB" radar-goiania.html` não aumentou em relação ao baseline pré-fase (esperado 0
  ou o valor pré-existente — conferir com git show HEAD~N se necessário).

3. **Guarda ec9f129**: `grep -c "montarNeg(" radar-goiania.html` == 2 (definição + CTA);
`imprimirLaudo` continua chamada só pelo botão "Salvar PDF" do #laudoView (grep confirma nenhum
caller novo).

4. **Isolamento NEG↔LZ**: nenhuma função `neg*` referencia `LZ` (`grep -n "LZ" ` nas funções neg —
inspeção dos hits); nenhuma função do fluxo do laudo referencia `NEG`. O wizard do laudo abre e
navega normalmente (nenhuma linha de wizRender/wizNext/montarLaudo/montarFichaRapida foi alterada
pela fase — `git diff` das funções confirma).

5. **Estrutura do documento (asserts de string sobre o arquivo)**:
- Disclaimer literal presente no HTML/JS em pelo menos 3 contextos: funções puras (11.1-01),
  caixa de tela `.negdisclaimer` (11.1-02) e fallback impresso de montarNeg (11.1-03).
- As 3 entradas de UI presentes: `grep -c "abrirNeg("` ≥ 5 (definição + 3 botões dActsMore + 1
  captSheet).
- `esc(` presente nas interpolações do template de montarNeg (inspeção do corpo da função: nenhuma
  interpolação `${...}` de dado digitado sem esc()).
- Tokens: nenhuma cor hex nova no CSS adicionado pela fase (grep dos blocos novos por `#[0-9a-f]{3,6}`
  — únicos hex permitidos são os já existentes `#fff` e valores do sistema de impressão --l-* já
  usados no bloco do laudo).

6. **44px**: inspeção do CSS novo — `.negcta`, `.negextrai`, `.zapbtn` (herdado), `.wnext` (herdado)
todos com min-height ≥44px (os herdados já cumprem; conferir os 2 novos).

Registrar no SUMMARY o resultado de CADA assert (pass/fix aplicado).
  </action>
  <verify>
    <automated>node --test "tests/*.test.mjs" 2>&1 | tail -6 && [ "$(grep -c 'localStorage.setItem' radar-goiania.html)" = "6" ] && [ "$(grep -c 'montarNeg(' radar-goiania.html)" = "2" ] && [ "$(grep -c 'abrirNeg(' radar-goiania.html)" -ge 5 ] && echo "OK: end-to-end asserts (privacidade + ec9f129 + entradas)"</automated>
  </verify>
  <done>Todos os asserts end-to-end passam: suite 100%, 6 setItem inalterados sem dado do NEG, montarNeg com gatilho único, abrirNeg com ≥5 ocorrências (definição + 4 entradas), esc() em toda interpolação de dado digitado, zero hex novo, 44px nos tocáveis novos; resultados registrados no SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NEG.minutaEditada (texto livre multiline) → innerHTML do #laudo/#laudoView | maior superfície da fase: o corpo inteiro do documento impresso é texto editado pelo usuário — esc() bloco a bloco, nunca innerHTML cru |
| Documento gerado → impressão (window.print) | só via clique real em "Salvar PDF" (imprimirLaudo, ec9f129) — nenhum caminho novo de impressão é criado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1-08 | Tampering (XSS-like self) | Texto editado renderizado como blocos de cláusula sem esc() | mitigate | esc() em TODO bloco/título antes do wrap em HTML; assert de inspeção na Task 2 (nenhum `${` de dado digitado sem esc no corpo de montarNeg) |
| T-11.1-09 | Repudiation | Usuário apaga o disclaimer na edição e o PDF sai sem ressalva | mitigate | montarNeg() SEMPRE renderiza a caixa .negdisclaimer-print com o texto literal, mesmo quando o bloco foi removido do texto editado — disclaimer não é removível |
| T-11.1-10 | Information Disclosure | Dado de parte vazando para storage em algum caminho da fase | mitigate | assert de contagem exata (6 setItem) + inspeção argumento a argumento na Task 2; IndexedDB baseline inalterado |
| T-11.1-11 | Tampering | Regressão no pipeline do laudo (montarLaudo/fichaRapida quebrados por CSS/JS novo) | mitigate | nenhuma função do laudo é editada; CSS novo é aditivo (classes neg* inéditas); suite + git diff das funções na Task 2 |

</threat_model>

<verification>
- `node --test "tests/*.test.mjs"` passa 100% (negocio.test.mjs incluído; zero regressão)
- `grep -c "localStorage.setItem" radar-goiania.html` == 6 e nenhuma linha recebe dado do NEG (inspeção)
- `grep -c "montarNeg(" radar-goiania.html` == 2 (definição + CTA — guarda ec9f129)
- `grep -c "abrirNeg(" radar-goiania.html` ≥ 5 (definição + 3 dActsMore + 1 captSheet)
- `.negclausula-item` com page-break-inside:avoid; `.negdisclaimer-print` com borda --l-accent; `.lass-testemunhas` presente
- Disclaimer literal nunca omitível do impresso (fallback em montarNeg mesmo se editado fora)
- Zero hex novo no CSS da fase; tocáveis novos ≥44px
</verification>

<success_criteria>
- NEG-01/02/03 completos ponta-a-ponta: entrada (ficha ou captação) → wizard NEG → minuta editável → PDF A4 estruturado com cláusulas numeradas, disclaimer em caixa e assinaturas corretas por documento — tudo determinístico, tudo no clique real
- Revisão do usuário respeitada no impresso (minutaEditada é a fonte); disclaimer inegociável
- Privacidade da fase fechada com assert: nada do NEG em storage
- Fase 11.1 pronta para o HUMAN-UAT jurídico (revisão das 3 minutas pelo usuário advogado — cláusulas, disclaimer, foro)
</success_criteria>

<output>
Após completar, criar `.planning/phases/11.1-documentos-da-negociacao/11.1-03-SUMMARY.md`.
Incluir no SUMMARY: resultado de cada assert end-to-end da Task 2 (pass/fix), a estrutura final do template impresso por documento (cláusulas + assinaturas + testemunhas), confirmação da guarda ec9f129 e do assert de privacidade, e a lista dos itens que ficam para o HUMAN-UAT jurídico do usuário.
</output>
