---
phase: 11.1-documentos-da-negociacao
plan: 02
type: execute
wave: 2
depends_on: ["11.1-01"]
files_modified:
  - radar-goiania.html
autonomous: true
requirements: [NEG-01, NEG-02, NEG-03]
tags: [wizard, neg-sheet, privacidade, parse-matricula, validacao, capt-sheet, dactsmore]

must_haves:
  truths:
    - "Estado NEG proprio (let NEG=null), paralelo a LZ e NUNCA persistido — nenhum localStorage.setItem/IndexedDB recebe dado de parte/valor/matricula do NEG em nenhum caminho de codigo"
    - "abrirNeg(tipo) com tipo proposta|exclusividade|contrato abre o sheet #negSheet (container .wiz PROPRIO, nao reusa #wiz) no passo 0 Partes & Valores, com guarda if(!DCUR)return e copia local NEG.a=DCUR (mesma protecao de LZ.a)"
    - "Wizard NEG tem 2 passos navegaveis (0=Partes & Valores por documento, 1=Revisao da minuta editavel) — o CTA Gerar PDF do passo 1 e o UNICO gatilho de montarNeg() (guarda ec9f129, nenhuma chamada assincrona)"
    - "Campo colar texto da matricula + botao Extrair dados chama parseMatricula (RADAR_PURE, 11.1-01) e pre-preenche matricula/cartorio SEMPRE editaveis, com mensagem neutra sem match e mensagem com match sempre incluindo Revise"
    - "Validacao por documento no avanco do passo 0 via toast (Proposta: nome do proponente + valor; Termo: nomes do proprietario e corretor; Contrato: nomes das partes + preco) — CPF/CNPJ e endereco NUNCA obrigatorios"
    - "Aviso de privacidade fixo (.negpriv) no topo de todo passo 0; fecharNeg() descarta NEG=null sem confirmacao"
    - "Entradas novas: grupo Documentos da negociacao (3 zapbtn) em #dActsMore + botao Gerar Termo de Exclusividade (accent) no #captSheet que abre abrirNeg('exclusividade')"
    - "#negSheet entra na cadeia de Esc ANTES de #captSheet (sheet mais recente tem prioridade — pode ser aberto a partir dele)"
  artifacts:
    - path: "radar-goiania.html"
      provides: "#negSheet (container .wiz proprio) + estado NEG + abrirNeg/fecharNeg/negBack/negNext/negRender/negExtrair/montarNeg(stub que delega para Wave 3) + entradas em #dActsMore e #captSheet + CSS .negpriv/.negparte/.negmat/.negextrai/.negextrairesult/.negminuta/.negdisclaimer"
      contains: "function abrirNeg"
  key_links:
    - from: "#captSheet (.captblock-neg)"
      to: "abrirNeg('exclusividade')"
      via: "onclick no botao .negcta — liga NEG-02 ao Modo Captacao (CAPT-01)"
      pattern: "abrirNeg\\('exclusividade'\\)"
    - from: "#dActsMore (zapgroup Documentos da negociacao)"
      to: "abrirNeg(tipo)"
      via: "3 botoes .zapbtn com onclick parametrizado"
      pattern: "Documentos da negocia"
    - from: "negExtrair()"
      to: "parseMatricula (RADAR_PURE, 11.1-01)"
      via: "chamada direta com NEG.matTexto — wizard NAO reimplementa a regex"
      pattern: "parseMatricula\\("
    - from: "negNext() passo 1 (CTA Gerar PDF)"
      to: "montarNeg()"
      via: "unico gatilho, clique real do usuario (guarda ec9f129)"
      pattern: "montarNeg\\("
    - from: "negRender() passo 1"
      to: "propostaTexto/termoExclusividadeTexto/contratoTexto (RADAR_PURE, 11.1-01)"
      via: "gera NEG.minutaTexto ao entrar na Revisao; textarea editavel grava NEG.minutaEditada"
      pattern: "NEG.minutaTexto"
---

<objective>
Criar o wizard `NEG` completo em `radar-goiania.html`: novo sheet `#negSheet` (container `.wiz` PRÓPRIO, independente de `#wiz`/`LZ`), estado `let NEG=null` em memória (NUNCA persistido — privacidade), 2 passos navegáveis (Partes & Valores por documento → Revisão da minuta editável), campo "colar texto da matrícula" + "Extrair dados" via `parseMatricula` (11.1-01), validação por documento via toast, aviso de privacidade fixo, e as 2 entradas novas no app (grupo "Documentos da negociação" em `#dActsMore` + botão "Gerar Termo de Exclusividade" no `#captSheet`).

Purpose: NEG-01/02/03 exigem o fluxo de coleta (partes/valores/prazos) + revisão editável ANTES do PDF; NEG-02 exige a ligação com o Modo Captação. O wizard do laudo (`#wiz`/`LZ`) coleta dados INCOMPATÍVEIS (conservação/fotos/comparáveis) — por isso o UI-SPEC trava sheet PRÓPRIO com estado PRÓPRIO, sem nenhum risco de colisão/reset entre os dois fluxos. `montarNeg()` nesta plan é a porta final já ligada ao CTA, mas o template de impressão em si é da Wave 3 (11.1-03) — aqui ela apenas existe e mostra a minuta via pipeline mínimo, sendo completada na Wave 3.
Output: `#negSheet` funcional ponta-a-ponta até a Revisão editável; entradas visíveis na ficha e no sheet de Captação; cadeia de Esc estendida; zero regressão no wizard do laudo (LZ) e na suite de testes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11.1-documentos-da-negociacao/11.1-CONTEXT.md
@.planning/phases/11.1-documentos-da-negociacao/11.1-UI-SPEC.md
@.planning/phases/11.1-documentos-da-negociacao/11.1-01-SUMMARY.md

# Fonte de código (ler antes de editar)
@radar-goiania.html

<facts>
<!-- Verificado por leitura direta deste repo. -->

DECISÃO DE ARQUITETURA (travada pelo UI-SPEC, Componente 3): o wizard NEG usa um SHEET PRÓPRIO
`#negSheet` com a MESMA classe `.wiz` de container (CSS 100% reusado), NUNCA o elemento `#wiz`
(que pertence ao fluxo do laudo/LZ). Motivo: `#wiz` e `#captSheet` já coexistem (CAPTRET/WIZRET
separados) e `#negSheet` pode ser aberto A PARTIR do `#captSheet` — reusar `#wiz` exigiria reset
de LZ entre modos e quebraria a coexistência com a Captação. Precedente direto: `#captSheet`
(Fase 10) já é um segundo `.wiz` independente — `#negSheet` é o terceiro, mesmo padrão.

Estrutura do sheet (HTML, linhas 830-871): `#wiz` = `.wiz > .wtop(.wback+.wdots+.wclose) + .wbody +
.wfoot(.wnext)`; `#captSheet` = `.wiz > .wtop(span+.wh1+.wclose) + .wbody` (sem foot). `#negSheet`
segue o formato do `#wiz` (tem passos/dots/foot) — markup literal no UI-SPEC Componente 3.

Estado do laudo: `let LZ=null` (linha 2956); `WIZRET` (2994) devolve foco; `CAPTRET` (2999) é
variável PRÓPRIA da Captação ("os dois sheets podem coexistir"). NEG precisa do seu próprio
`NEGRET`, mesmo padrão.

Guarda DCUR: `abrirSeletorFinalidade()` (2960-2968) faz `if(!DCUR)return;` e captura
`LZ={a:DCUR,...}` — o sheet lê SEMPRE de `LZ.a`, nunca de `DCUR` depois de aberto.
`abrirCaptacao()` (3000) idem via `dadosFicha()`. `abrirNeg()` copia o padrão: `NEG={a:DCUR,...}`.

Entrada de foco: `abrirCaptacao()` foca `.wclose`; `abrirSeletorFinalidade()` foca `.finop`.
UI-SPEC do NEG: focar o PRIMEIRO campo editável do passo 0 (primeiro `.winput` de nome de parte).
Animação de entrada: `#captSheet` usa `if(!REDUCE)mAnimate(sheet,{opacity:[0,1]},{duration:0.18})`
(linha 3013) — `#negSheet` reusa LITERALMENTE essa linha (REDUCE-aware, zero transição nova).

Cadeia de Esc (keydown global, linhas 3636-3654): ordem atual `#captSheet` → `#caixaList` →
`#correctMenu` → `#ambigChips` → `#calc` → `#wiz` → `#laudoView` → `#chooser` → busca desktop →
`closeDetail()`. `#negSheet` entra ANTES de `#captSheet` (sheet mais recente tem prioridade — pode
ter sido aberto a partir dele): `const ns=document.getElementById("negSheet");
if(ns&&!ns.hidden){fecharNeg();return;}` como PRIMEIRO check da cadeia.

`#dActsMore` (linhas 2545-2561): innerHTML montado em showDetail() com 2 `.zapgroup` no final
(WhatsApp com 5 botões; captação com 1 botão). O grupo novo "Documentos da negociação" entra ENTRE
os dois (UI-SPEC Componente 2: após o grupo WhatsApp, antes do grupo "Captar este imóvel").
Especificidade CSS já resolvida (comentário na linha 291): `#dActsMore` (0,3,0) vence
`.detail .acts button` — os `.zapbtn` novos herdam sem ajuste.

`#captSheet` `.wbody` (linhas 847-870): 4 `.captblock` + `.captdisclaimer` no final. O bloco novo
`.captblock-neg` entra como 5º bloco, APÓS "✅ Tarefa de follow-up" e ANTES do `.captdisclaimer`
(UI-SPEC Componente 1, markup literal).

Toast: `toast(msg)` (linha 1946) — validação do passo 0 reusa (mesmo padrão de `wizNext()` linhas
3235-3236: toast + volta ao passo + foco).

`.seg` segmented (padrão em wizRender step 0/4): `<div class="seg"><button aria-pressed="..."
class="on|''" onclick="...">...</button>...</div>` — Exclusividade/Anúncio reusam literalmente.

`radar_prof` (localStorage, só LEITURA aqui): `{nome,creci,cnai,contato}` — pré-preenche o campo
Corretor do Termo (`abrirSeletorFinalidade()` linha 2962 mostra o padrão de leitura com try/catch).
NUNCA escrever `radar_prof` a partir do NEG (o wizard do laudo já cuida do perfil; o NEG só lê).

Defaults por documento (CONTEXT.md, decisões aceitas): Termo → prazo 90 dias, comissão 6%
(editáveis); Proposta → validade 10 dias (editável); Contrato → nenhum default numérico.

Funções puras disponíveis após 11.1-01 (mesmo bloco RADAR_PURE): `propostaTexto(data)`,
`termoExclusividadeTexto(data)`, `contratoTexto(data)`, `parseMatricula(texto)`,
`numeroPorExtenso(valor)`. Shape do objeto `data` documentado no 11.1-01-SUMMARY (ler antes).

`esc(v)` (linha 909): TODO valor digitado re-renderizado em innerHTML (nome/endereço/matrícula/
cartório/minutaTexto no value/textarea) passa por `esc()` — contrato IN-01. Atenção especial:
`NEG.matTexto` e `NEG.minutaEditada` são texto LIVRE do usuário (superfície maior), `esc()`
MANDATÓRIO ao re-renderizar em `${...}` de template string.

localStorage.setItem hoje (baseline do assert de privacidade, 6 ocorrências): radar_sat,
radar_lastbairro, radar_oportunidades, radar_historico, radar_prof, radar_coach — NENHUMA nova
pode aparecer nesta fase, e nenhuma existente pode passar a receber dado do NEG.
</facts>

<interfaces>
<!-- Contratos que a Wave 3 (11.1-03) vai consumir — não renomear. -->

```js
// Estado (em memoria SOMENTE — nunca persistido):
let NEG = null;
// Shape apos abrirNeg(tipo):
// NEG = {
//   a: DCUR,                    // copia local do imovel (mesma protecao de LZ.a)
//   tipo: "proposta"|"exclusividade"|"contrato",
//   step: 0|1,
//   // partes (por documento — só as relevantes ao tipo são renderizadas/coletadas):
//   proponente:{nome:"",doc:"",endereco:""}, vendedor:{...}, comprador:{...},
//   proprietario:{...}, corretor:{nome:"",creci:""},   // corretor pre-preenchido de radar_prof (leitura)
//   // valores/condicoes:
//   valorOfertado:null, formaPagamento:"A combinar", validadeDias:10,
//   prazoDias:90, comissaoPct:6, exclusiva:true, autorizaAnuncio:true,
//   precoTotal:null, matricula:"", cartorio:"",
//   // matricula colada:
//   matTexto:"",
//   // minuta:
//   minutaTexto:"",            // gerado pela funcao pura ao ENTRAR no passo 1
//   minutaEditada:null,        // grava a edicao do usuario no textarea (null = nao editou)
// }

function abrirNeg(tipo)      // guarda if(!DCUR)return; monta NEG; abre #negSheet; foca 1o campo
function fecharNeg()         // #negSheet.hidden=true; NEG=null (descarte silencioso); devolve foco NEGRET
function negBack()           // step 1 -> 0; step 0 -> fecharNeg()
function negNext()           // step 0: valida por documento (toast) e avanca; step 1: chama montarNeg() (UNICO gatilho)
function negRender()         // renderiza o passo atual em #negBody (dots, foco no .wh1 ao trocar de passo)
function negExtrair()        // parseMatricula(NEG.matTexto) -> pre-preenche NEG.matricula/NEG.cartorio + resultado visivel + foco no resultado
function negSetParte(parte,campo,el) // helper oninput generico (ou oninput inline direto — decisao de implementacao)
function montarNeg()         // Wave 2: versao minima que monta a minuta final (NEG.minutaEditada||NEG.minutaTexto) no pipeline #laudo->#laudoView; Wave 3 COMPLETA com o template A4 estruturado
```

`montarNeg()` nesta plan: implementação MÍNIMA mas FUNCIONAL — injeta o texto da minuta
(escapado com `esc()`, com `white-space:pre-wrap`) + cabeçalho `.lbrand` + `.foot` no `#laudo`,
copia para `#laudoViewBody`, mostra `#laudoView` (mesmo fechamento de `renderFichaRapida()`:
`fecharNeg()` + innerHTML copy + `lv.hidden=false`). A Wave 3 SUBSTITUI o corpo por template A4
estruturado (cláusulas como blocos, disclaimer em caixa, assinaturas `.lass`) — o contrato
(assinatura da função, ponto de chamada, pipeline) fica ESTÁVEL desde já.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estado NEG + #negSheet + abrirNeg/fecharNeg/negRender passo 0 (Partes & Valores por documento) + extração de matrícula</name>
  <files>radar-goiania.html</files>
  <action>
1. HTML: adicionar `#negSheet` imediatamente APÓS o `#captSheet` (linha ~871), markup literal do
UI-SPEC Componente 3: `.wiz > .wtop(.wback#negBack-btn + .wdots#negDots + .wclose) + .wbody#negBody +
.wfoot(.wnext#negNext-btn onclick="negNext()")`. `hidden role="dialog" aria-modal="true"
aria-label="Documentos da negociação"`. IDs dos botões distintos das funções (ex.: botão
`id="negBackBtn"`, função `negBack()`) para não colidir com os globals de id do browser.

2. JS (junto ao bloco da Captação, ~linha 2999+): `let NEG=null; let NEGRET=null;`
`abrirNeg(tipo)`:
- `if(!DCUR)return;`
- lê `radar_prof` (try/catch, mesmo padrão da linha 2962) SÓ para pré-preencher
  `corretor:{nome:prof.nome||"",creci:prof.creci||""}` quando `tipo==="exclusividade"`.
- monta `NEG` com o shape de `<interfaces>` (todas as partes inicializadas
  `{nome:"",doc:"",endereco:""}`, defaults: validadeDias:10, prazoDias:90, comissaoPct:6,
  exclusiva:true, autorizaAnuncio:true, formaPagamento:"A combinar", step:0).
- `NEGRET=document.activeElement;` abre o sheet (`hidden=false`), `if(!REDUCE)mAnimate(sheet,
  {opacity:[0,1]},{duration:0.18});` (cópia literal do padrão do captSheet linha 3013).
- `negRender();` e foca o primeiro `.winput` do `#negBody`.

`fecharNeg()`: `hidden=true; NEG=null;` devolve foco a NEGRET (padrão de fecharCaptacao(), linha
3016-3019). SEM diálogo de confirmação (privacidade por design — descarte silencioso).

`negBack()`: `if(!NEG)return; if(NEG.step>0){NEG.step--;negRender();}else fecharNeg();`

3. `negRender()` — passo 0 (o passo 1 é a Task 2):
- dots: `document.getElementById("negDots").innerHTML=[0,1].map(i=>...)` (2 dots, padrão da linha 3107).
- `.wnext` textContent: passo 0 = "Continuar", passo 1 = "Gerar PDF".
- TOPO FIXO de todo passo 0: `<div class="negpriv">🔒 Os dados das partes não ficam salvos no app —
  servem só para gerar este documento.</div>` (copy literal do UI-SPEC).
- Título por documento (`.wh1`): proposta="Proponente & Valores", exclusividade="Partes & Condições",
  contrato="Partes & Valores".
- Blocos `.negparte` por documento (padrão `.wlabel`+`.winput` idêntico ao wizRender step 3, linhas
  3153-3167; `esc()` em TODO value re-renderizado):
  - **proposta**: Proponente (nome*/CPF-CNPJ/endereço) + Vendedor-Proprietário (nome/doc/endereço) +
    Valor ofertado (R$, inputmode=numeric, parse igual linha 3145) + Forma de pagamento (`<select
    class="winput">` com "À vista"/"Financiado"/"Entrada + saldo"/"A combinar") + Validade da
    proposta (dias, value=10).
  - **exclusividade**: Proprietário (nome*/doc/endereço) + Corretor (nome*/CRECI, pré-preenchido) +
    Prazo (dias, value=90) + Comissão (%, inputmode=decimal, parse `parseFloat(v.replace(',','.'))`) +
    Exclusividade (`.seg` "Sim, exclusiva"/"Não, aberta" com aria-pressed, padrão linha 3123) +
    Autoriza anúncio do imóvel (`.seg` "Sim"/"Não").
  - **contrato**: Vendedor (nome*/doc/endereço) + Comprador (nome*/doc/endereço) + Matrícula (nº) +
    Cartório/Ofício de Registro de Imóveis + Preço total (R$) + Forma de pagamento (select).
- Campo "colar texto da matrícula" (SÓ contrato e exclusividade, NÃO na proposta): `<span
  class="wlabel">Colar texto da matrícula (opcional)</span>` + `<textarea class="winput negmat"
  placeholder="Cole aqui o texto da matrícula/certidão — vamos tentar identificar o número e o
  cartório." oninput="NEG.matTexto=this.value">${esc(NEG.matTexto)}</textarea>` + `<button
  type="button" class="negextrai" onclick="negExtrair()">📎 Extrair dados</button>` + `<div
  class="negextrairesult" id="negExtraiResult" hidden></div>`.
- Foco no `.wh1` quando o passo muda (padrão stepMudou, linha 3230).

4. `negExtrair()`: chama `parseMatricula(NEG.matTexto)` (RADAR_PURE, 11.1-01 — NÃO reimplementar
regex). Resultado:
- `null` → `#negExtraiResult` visível, variante neutra (classe/estilo sem-match do UI-SPEC):
  texto "Não conseguimos identificar a matrícula/cartório automaticamente neste texto — preencha
  manualmente abaixo." (literal).
- objeto → pré-preenche `NEG.matricula`/`NEG.cartorio` (só os campos que vieram não-null; NUNCA
  sobrescreve com null um campo que o usuário já digitou), re-renderiza (`negRender()`) e mostra
  variante com-match: "Encontramos: matrícula nº [X], [Y]. Revise antes de continuar." com os
  valores via `esc()`. Campos SEMPRE `<input>` editáveis (nunca readonly).
- Nos 2 casos: `tabIndex=-1; focus({preventScroll:true})` no resultado (padrão WR-03).

5. Entradas:
- `#dActsMore` (innerHTML em showDetail(), linhas 2545-2561): inserir ENTRE o zapgroup WhatsApp e o
  zapgroup "Captar este imóvel" o grupo novo (markup literal UI-SPEC Componente 2):
  `<div class="zapgroup"><div class="zapgroup-lbl">Documentos da negociação</div>` + 3 `.zapbtn`
  ("📄 Proposta de Compra e Venda"→`abrirNeg('proposta')`, "🔑 Termo de Exclusividade"→
  `abrirNeg('exclusividade')`, "🧾 Contrato de Compra e Venda"→`abrirNeg('contrato')`).
- `#captSheet` (HTML, entre o 4º captblock ~linha 868 e o `.captdisclaimer` ~869): bloco novo
  `.captblock.captblock-neg` (markup literal UI-SPEC Componente 1) com `<button type="button"
  class="captcopy negcta" onclick="abrirNeg('exclusividade')">📄 Gerar Termo de Exclusividade</button>`.

6. CSS novo (junto ao bloco de CSS da Fase 11, ~linha 463): `.negpriv` (dashed informativo),
`.negparte`/`.negparte-lbl` (bloco por pessoa), `.negmat{min-height:96px}`, `.negextrai` (botão
secundário 44px, borda `--line`, sem sublinhado), `.negextrairesult` (2 variantes: neutra
`--paper`/`--line`/`--muted`; match `--paper-2`/`--lot`/`--ink`), `.negcta{background:var(--accent);
border-color:var(--accent);color:#fff}` + hover `--accent-ink`. Valores exatos nas tabelas do
UI-SPEC (Componentes 1 e 3). ZERO hex novo — só as vars `--paper/--paper-2/--ink/--line/--muted/
--accent/--accent-ink/--lot`. Todo tocável novo ≥44px.

7. Cadeia de Esc (keydown global, linha 3636): adicionar `#negSheet` como PRIMEIRO check, ANTES do
`#captSheet`: `const ns=document.getElementById("negSheet"); if(ns&&!ns.hidden){fecharNeg();return;}`
— Esc fecha primeiro o negSheet (mais recente), segundo Esc fecha o captSheet por baixo.

NÃO tocar: `#wiz`, `LZ`, `wizRender/wizNext/wizBack`, `abrirSeletorFinalidade`, `montarLaudo`,
`montarFichaRapida` — zero mudança no fluxo do laudo.
  </action>
  <verify>
    <automated>node --test "tests/*.test.mjs" 2>&1 | tail -6 && python -c "
import io
s=io.open('radar-goiania.html',encoding='utf-8').read()
n=s.count('abrirNeg')
assert n>=2, 'abrirNeg: esperado >=2 (definicao + >=1 botao), achou '+str(n)  # fix plan-check: contagem ASSERTADA, nao so impressa
assert 'negSheet' in s, 'negSheet ausente'
assert 'Documentos da negocia' in s, 'grupo Documentos da negociacao ausente'
print('OK: abrirNeg x'+str(n)+', negSheet e grupo presentes')
"</automated>
  </verify>
  <done>#negSheet existe como .wiz próprio; abrirNeg/fecharNeg/negBack/negRender/negExtrair implementados; passo 0 renderiza campos corretos por documento com defaults (90/6/10) e aviso de privacidade; extração pré-preenche campos editáveis com mensagens literais do UI-SPEC; 3 entradas em #dActsMore + 1 no #captSheet; Esc chain estendida com negSheet antes de captSheet; suite 64+ testes passa (zero regressão); nenhuma mudança em #wiz/LZ.</done>
</task>

<task type="auto">
  <name>Task 2: Passo 1 (Revisão da minuta editável + disclaimer) + validação por documento + montarNeg() mínimo ligado ao CTA</name>
  <files>radar-goiania.html</files>
  <action>
1. `negNext()`:
- `if(!NEG)return;`
- **Passo 0 → 1 com validação por documento** (toast literal do UI-SPEC + foco no campo em falta):
  - proposta: `NEG.proponente.nome` vazio OU `NEG.valorOfertado==null` → toast "Preencha o nome do
    proponente e o valor ofertado para continuar." e return (permanece no passo 0, foca o campo).
  - exclusividade: `NEG.proprietario.nome` vazio OU `NEG.corretor.nome` vazio → toast "Preencha o
    nome do proprietário e do corretor para continuar." e return.
  - contrato: `NEG.vendedor.nome` vazio OU `NEG.comprador.nome` vazio OU `NEG.precoTotal==null` →
    toast "Preencha o nome das partes e o preço total para continuar." e return.
  - CPF/CNPJ e endereço NUNCA entram na validação (nunca obrigatórios).
  - Validação OK → `NEG.step=1; negRender();`
- **Passo 1 (CTA "Gerar PDF")**: chama `montarNeg()` — ÚNICO gatilho, nenhum outro caminho de
  código chama montarNeg() (guarda ec9f129: sem oninput/then/callback assíncrono).

2. `negRender()` — passo 1 "Revisão da minuta" (markup literal UI-SPEC Componente 3, Passo 1):
- Ao ENTRAR no passo 1 (transição 0→1), gerar `NEG.minutaTexto` chamando a função pura do tipo:
  `propostaTexto(data)` / `termoExclusividadeTexto(data)` / `contratoTexto(data)` — montando o
  objeto `data` do shape do 11.1-01 a partir de `NEG` + `NEG.a` (imóvel: `endereco:addrDe(NEG.a)`,
  `bairro:clean(NEG.a.nmbairro)`, `quadra:clean(NEG.a.nrquadra)`, `lote:clean(NEG.a.nrlote)`,
  `areaTerr/areaEdif`, `inscricao:clean(NEG.a.nrinscr)||clean(NEG.a.ci)`, `tipoImovel:
  unitLabel(NEG.a)||USO[NEG.a.uso]||"Imóvel"`; `foro:"Goiânia/GO"`). Regerar minutaTexto SEMPRE que
  entrar no passo 1 vindo do 0 (dados podem ter mudado), mas PRESERVAR `NEG.minutaEditada` se o
  usuário já tinha editado E voltar sem mudar dados é aceitável simplificar: regenerar SEMPRE ao
  entrar 0→1 e zerar `minutaEditada=null` (edição vale para a geração corrente — comportamento
  simples e previsível; documentar no code comment).
- Render: `.wh1` "Revisão da minuta" + `.wsub` "O texto abaixo já está pronto — edite qualquer
  trecho antes de gerar o PDF." + `<textarea class="winput negminuta"
  oninput="NEG.minutaEditada=this.value">${esc(NEG.minutaTexto)}</textarea>` + caixa
  `.negdisclaimer` (eyebrow "Atenção" + texto literal do disclaimer — o MESMO texto que a função
  pura já embute na minuta; a caixa na tela é REFORÇO visual, nunca editável, fora do textarea).
- CSS: `.negminuta{min-height:360px;font:500 13.5px/1.55 "IBM Plex Sans";white-space:pre-wrap}`
  (280px no mobile ≤820px), `.negdisclaimer` (borda 2px `--accent`, fundo `--paper-2`),
  `.negdisclaimer-eyebrow` (mono 10px uppercase `--accent`), `.negdisclaimer-txt` (11.5px `--ink`)
  — valores exatos da tabela do UI-SPEC.

3. `montarNeg()` — versão mínima FUNCIONAL (Wave 3 completa o template A4):
- `const texto=NEG.minutaEditada!=null?NEG.minutaEditada:NEG.minutaTexto;`
- `const titulo={proposta:"Proposta de Compra e Venda",exclusividade:"Termo de Autorização/
  Exclusividade de Venda",contrato:"Contrato de Compra e Venda"}[NEG.tipo];`
- monta `#laudo` com `.lrun` + `.lbrand` (small "Radar Fundiário · Documentos da Negociação" +
  titulo) + corpo `<div style="white-space:pre-wrap">${esc(texto)}</div>` + `.foot` — `esc()`
  OBRIGATÓRIO no texto (minutaEditada é texto livre do usuário, IN-01).
- fechamento IDÊNTICO a renderFichaRapida() (linhas 3388-3391): `fecharNeg();` +
  `laudoViewBody.innerHTML=laudo.innerHTML;` + `laudoView.hidden=false; lv.scrollTop=0;` —
  o PDF continua saindo SÓ do clique em "Salvar PDF" (`imprimirLaudo()`, inalterada).

4. Verificação manual de não-colisão (grep, documentar no SUMMARY): nenhuma função nova
lê/escreve `LZ`; nenhuma função do laudo lê/escreve `NEG`; `montarNeg` aparece EXATAMENTE 2 vezes
no arquivo (definição + chamada em negNext).

5. Privacidade (assert negativo, será re-executado na Wave 3): nenhuma das 6 chamadas
`localStorage.setItem` existentes recebe dado do NEG e nenhuma chamada nova foi adicionada —
`grep -n "localStorage.setItem" radar-goiania.html` deve continuar retornando exatamente as
mesmas 6 linhas/chaves (radar_sat/radar_lastbairro/radar_oportunidades/radar_historico/
radar_prof/radar_coach).
  </action>
  <verify>
    <automated>node --test "tests/*.test.mjs" 2>&1 | tail -6 && [ "$(grep -c 'localStorage.setItem' radar-goiania.html)" = "6" ] && echo "OK: privacidade — nenhum setItem novo" && [ "$(grep -c 'montarNeg(' radar-goiania.html)" = "2" ] && echo "OK: montarNeg so definicao+CTA (ec9f129)"</automated>
  </verify>
  <done>Passo 1 renderiza a minuta gerada pela função pura em textarea editável + caixa disclaimer não-editável; validação por documento bloqueia avanço com toast literal e CPF/endereço nunca obrigatórios; montarNeg() existe, é chamado SÓ pelo CTA do passo 1 e sai pelo pipeline #laudo→#laudoView; contagem de localStorage.setItem inalterada (6); suite completa passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Campos digitados (nome/doc/endereço/matTexto) → innerHTML de negRender() | entrada livre do usuário re-renderizada em template strings — esc() MANDATÓRIO em todo `${...}` de valor digitado (IN-01) |
| NEG.minutaEditada (texto livre editado) → #laudo/#laudoView (montarNeg) | maior superfície da fase: texto multiline arbitrário vai para innerHTML do documento — esc() + white-space:pre-wrap, nunca interpolação crua |
| NEG (partes/valores) → storage | NUNCA cruza: nenhum setItem/IndexedDB recebe dado do NEG — verificado por contagem exata de setItem (6) no verify |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11.1-04 | Information Disclosure | Dados de partes persistidos por engano (localStorage/IndexedDB) | mitigate | assert de contagem exata de `localStorage.setItem` (6, inalterada) no verify; NEG vive só em memória; fecharNeg() zera NEG=null |
| T-11.1-05 | Tampering (XSS-like self) | Nome/endereço/matTexto/minutaEditada re-renderizados sem esc() em innerHTML | mitigate | esc() em TODO valor digitado interpolado (padrão IN-01 já usado em wizRender); revisão por grep de `${` sem esc em negRender/montarNeg antes do commit |
| T-11.1-06 | Elevation (fluxo) | Wizard NEG colidindo com wizard LZ (reset/estado cruzado quebra o laudo) | mitigate | sheet #negSheet PRÓPRIO + estado NEG PRÓPRIO + NEGRET próprio; verificação por grep: nenhuma função neg* toca LZ e vice-versa; Esc chain com negSheet ANTES de captSheet |
| T-11.1-07 | Tampering | montarNeg() disparado por caminho assíncrono (regressão ec9f129) | mitigate | contagem exata de ocorrências de `montarNeg(` (2: definição + CTA) no verify; nenhum oninput/then chama |

</threat_model>

<verification>
- `node --test "tests/*.test.mjs"` passa 100% (zero regressão — as mudanças são fora do bloco RADAR_PURE)
- `grep -c "localStorage.setItem" radar-goiania.html` == 6 (privacidade: nenhum setItem novo; nenhuma chave existente recebe dado do NEG — inspeção das 6 linhas confirma)
- `grep -c "montarNeg(" radar-goiania.html` == 2 (definição + único gatilho no CTA — ec9f129)
- `#negSheet` presente no HTML como `.wiz` próprio, com role="dialog"/aria-modal/aria-label
- Grupo "Documentos da negociação" (3 botões) presente no innerHTML de #dActsMore; `.captblock-neg` presente no #captSheet
- Cadeia de Esc: check do negSheet é o PRIMEIRO da cadeia (antes do captSheet)
- Nenhuma função neg* referencia LZ; nenhuma função do laudo referencia NEG (grep cruzado)
</verification>

<success_criteria>
- Fluxo NEG ponta-a-ponta: abrirNeg(tipo) → passo 0 (partes/valores por documento, defaults 90/6/10, extração de matrícula revisável) → validação por documento → passo 1 (minuta editável + disclaimer) → CTA "Gerar PDF" → montarNeg() → #laudoView na tela
- NEG-02 ligado à Captação: botão accent no #captSheet abre o Termo pré-configurado
- Privacidade por design verificada: dados de partes nunca tocam storage; fechar descarta tudo
- Zero regressão: wizard do laudo (LZ), Captação e suite de testes intactos
- Wave 3 (11.1-03) pode substituir o corpo de montarNeg() pelo template A4 sem tocar em mais nada
</success_criteria>

<output>
Após completar, criar `.planning/phases/11.1-documentos-da-negociacao/11.1-02-SUMMARY.md`.
Incluir no SUMMARY: shape final exato do objeto NEG, assinatura de montarNeg() e o ponto exato onde a Wave 3 substitui o corpo (template mínimo → template A4), resultado do grep de privacidade (6 setItem inalterados), e a posição final do negSheet na cadeia de Esc.
</output>
