---
phase: 11.1-documentos-da-negociacao
verified: 2026-07-07T21:30:00Z
status: human_needed
score: 12/12 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Revisão jurídica das 3 minutas (Proposta, Termo de Exclusividade, Contrato) pelo usuário (advogado)"
    expected: "Texto das cláusulas, disclaimer e foro são juridicamente adequados para uso real com clientes; nenhuma cláusula precisa de correção de redação/conteúdo jurídico antes de considerar o conteúdo final"
    why_human: "Adequação jurídica de texto (redação de cláusulas, escolha de foro, suficiência do disclaimer) não é verificável programaticamente — exige julgamento profissional de advogado, conforme decisão explícita registrada em 11.1-CONTEXT.md e 11.1-UI-SPEC.md ('HUMAN-UAT desta fase inclui revisão jurídica das 3 minutas pelo usuário (advogado)')"
  - test: "Gerar e imprimir/exportar PDF dos 3 documentos com dados de teste variados (nomes longos, CRECI ausente, Termo com/sem cláusula de anúncio) e inspecionar o layout A4 real"
    expected: "Paginação correta (cláusulas não cortadas ao meio pela quebra de página), disclaimer legível e destacado, blocos de assinatura/testemunhas bem posicionados"
    why_human: "Renderização visual de impressão A4 (quebras de página, margens, contraste) não é verificável por grep/teste automatizado — precisa de inspeção visual do PDF gerado, conforme listado como pendência explícita no 11.1-03-SUMMARY.md"
gaps: []
---

# Phase 11.1: Documentos da Negociação (Proposta · Exclusividade · Contrato) Verification Report

**Phase Goal:** O corretor gera as minutas do negócio (Proposta de C&V, Termo de Exclusividade/Autorização, Contrato de C&V) já preenchidas com o imóvel, editáveis, em PDF pt-BR com ressalvas — a papelada que fecha a captação e a venda, sem sair do app.
**Verified:** 2026-07-07T21:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | As 3 minutas são geradas por template determinístico (nunca LLM), pré-preenchido com o imóvel + partes/valores editáveis | ✓ VERIFIED | `propostaTexto`/`termoExclusividadeTexto`/`contratoTexto` (RADAR_PURE, `radar-goiania.html:~1450-1720`) são funções puras sem chamada de rede/IA; `negRender()` monta `data` a partir de `NEG.a` (cadastro) + campos digitados (`radar-goiania.html:3477-3479`) |
| 2 | Cada documento é minuta editável antes do PDF, com ressalva clara nunca prometendo validade automática | ✓ VERIFIED | Textarea `.negminuta` editável (`NEG.minutaEditada`) na Revisão; disclaimer literal `DISCLAIMER_NEG` presente nas 3 funções puras + caixa `.negdisclaimer` na tela + `.negdisclaimer-print` inegociável no impresso (nunca derivado do texto editado) |
| 3 | Termo de Exclusividade puxa proprietário/corretor(CRECI)/prazo/comissão e liga ao Modo Captação; Proposta traz validade da oferta; Contrato traz matrícula/RGI + forma de pagamento + cláusulas padrão | ✓ VERIFIED | `.captblock-neg` no `#captSheet` com `onclick="abrirNeg('exclusividade')"` (`radar-goiania.html:901-904`); `validadeDias` presente na Proposta (linha 1609, 3358, 3417); `NEG.matricula`/`NEG.cartorio` no Contrato com `parseMatricula()` wiring (linhas 3456-3458, 3505-3506) |
| 4 | montarNeg() monta o template A4 (cláusulas numeradas, disclaimer em caixa, assinaturas + testemunhas) pelo pipeline #laudo→#laudoView, PDF só no clique real (ec9f129) | ✓ VERIFIED | `grep -c "montarNeg("` == 2 (definição + único gatilho em `negNext()`); `.negclausula-item`/`.negdisclaimer-print`/`.lass-testemunhas` presentes no CSS de impressão (linhas 490-496); fechamento idêntico a `renderFichaRapida()` |
| 5 | Privacidade: dados de partes/negócio nunca persistidos | ✓ VERIFIED | `grep -c "localStorage.setItem"` == 6 (baseline inalterado); nenhuma das 6 chamadas referencia NEG/proponente/vendedor/comprador/proprietario/matricula (inspeção linha a linha); `fecharNeg()` zera `NEG=null` |
| 6 | Extenso pt-BR cobre fronteiras de risco (cem/cento, mil, milhão/milhões, negativo→"") | ✓ VERIFIED | `tests/fixtures.mjs:449-460` cobre todos os 10 casos incluindo `negativo: -500 → expectEmpty`; suite live 79/79 passa |
| 7 | Isolamento NEG↔LZ; cadeia de Esc correta; 44px; zero hex novo | ✓ VERIFIED | Esc chain: `#negSheet` checado ANTES de `#captSheet` (linhas 4217-4225); `.negextrai{min-height:44px}` e `.negcta` herda de `.captcopy{min-height:44px}`; único hex novo no CSS da fase é `#fff` (já pré-existente no bloco do laudo) |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `radar-goiania.html` (RADAR_PURE) | 5 funções puras: propostaTexto/termoExclusividadeTexto/contratoTexto/parseMatricula/numeroPorExtenso | ✓ VERIFIED | Todas presentes, testadas por `tests/negocio.test.mjs` (15 testes), zero DOM/rede |
| `radar-goiania.html` (#negSheet) | Terceiro sheet `.wiz` próprio, estado `NEG` em memória | ✓ VERIFIED | `role="dialog" aria-modal="true"` presente; 2 passos navegáveis; nunca persiste |
| `radar-goiania.html` (montarNeg + CSS impressão) | Template A4 estruturado com cláusulas/disclaimer/assinaturas | ✓ VERIFIED | `.negclausula-item{page-break-inside:avoid}`, `.negdisclaimer-print`, `.lass-testemunhas` presentes |
| `tests/negocio.test.mjs` | Harness node:test cobrindo as 5 funções | ✓ VERIFIED | 15 testes, todos passando na suite live |
| `tests/fixtures.mjs` | Fixtures propostaCasos/termoCasos/contratoCasos/parseMatriculaCasos/extensoCasos | ✓ VERIFIED | Todas as 5 chaves presentes incluindo fixture `negativo` (fix pós-review) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `#captSheet` (.captblock-neg) | `abrirNeg('exclusividade')` | onclick no botão `.negcta` | ✓ WIRED | Linha 904 — liga NEG-02 ao CAPT-01 |
| `#dActsMore` (zapgroup) | `abrirNeg(tipo)` | 3 botões .zapbtn | ✓ WIRED | Grupo "Documentos da negociação" presente |
| `negExtrair()` | `parseMatricula()` (RADAR_PURE) | chamada direta com `NEG.matTexto` | ✓ WIRED | Linhas 3505-3506, sem reimplementação de regex |
| `negNext()` passo 1 | `montarNeg()` | único gatilho (ec9f129) | ✓ WIRED | `grep -c "montarNeg("` == 2 |
| `negRender()` passo 1 | `propostaTexto/termoExclusividadeTexto/contratoTexto` | gera `NEG.minutaTexto` | ✓ WIRED | Linha 3478: dispatch por `NEG.tipo` para a função pura correta |
| `#negSheet` Esc | fecha ANTES de `#captSheet` | keydown global | ✓ WIRED | Linhas 4220-4221 checadas antes de 4224-4225 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `negRender()` passo 1 (textarea `.negminuta`) | `NEG.minutaTexto` | `propostaTexto/termoExclusividadeTexto/contratoTexto(data)` chamado com `data` montado de `NEG.a` (cadastro real) + campos digitados | ✓ FLOWING | Nenhum valor estático/hardcoded — campo ausente vira `CAMPO_VAZIO` ("________"), nunca placeholder textual |
| `montarNeg()` (documento impresso) | `NEG.minutaEditada \|\| NEG.minutaTexto` | Textarea editado pelo usuário ou texto gerado pela função pura | ✓ FLOWING | Confirmado por inspeção: `esc()` em toda interpolação; disclaimer sempre da constante `DISCLAIMER_NEG`, nunca do texto (imune a edição acidental) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Suite completa de testes | `node --test "tests/*.test.mjs"` | 79 pass / 0 fail / 203ms | ✓ PASS |
| Contagem de gatilho único (ec9f129) | `grep -c "montarNeg(" radar-goiania.html` | 2 | ✓ PASS |
| Contagem de entradas abrirNeg | `grep -c "abrirNeg(" radar-goiania.html` | 5 | ✓ PASS |
| Privacidade (setItem inalterado) | `grep -c "localStorage.setItem" radar-goiania.html` | 6 | ✓ PASS |
| 3 fixes do REVIEW-FIX presentes no código | grep dos padrões de fix | `if(Number(valor)<0) return "";` / `.indexOf(...)!==0` / `&&/[A-ZÁ...]/.test(primeira)` | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| NEG-01 | 11.1-01/02/03 | Proposta de Compra e Venda pré-preenchida, editável, PDF | ✓ SATISFIED | `propostaTexto()` + wizard + template impresso completos e testados |
| NEG-02 | 11.1-01/02/03 | Termo de Exclusividade ligado ao Modo Captação | ✓ SATISFIED | `termoExclusividadeTexto()` + `.captblock-neg` no `#captSheet` + template impresso |
| NEG-03 | 11.1-01/02/03 | Contrato de C&V com matrícula/RGI + parse de texto colado | ✓ SATISFIED | `contratoTexto()` + `parseMatricula()` (2 ordens de cartório testadas) + template impresso |
| NEG-04 | — (nenhum plan reivindica) | OCR client-side (Tesseract.js) para pré-preencher matrícula/partes | ○ DEFERIDO (decisão do usuário) | Documentado em `11.1-CONTEXT.md` ("NEG-04 ADIADO por decisão") e `REQUIREMENTS.md` linha 65/149; substituído por parse leve de texto colado; zero código Tesseract/OCR no arquivo (grep confirma 0 ocorrências) — não é gap, é decisão de escopo registrada |

Nenhum requisito órfão: NEG-01/02/03/04 todos mapeados no ROADMAP e nos 3 plans (NEG-04 explicitamente fora do `requirements:` frontmatter dos 3 plans, consistente com o deferimento documentado).

### Anti-Patterns Found

Nenhum anti-pattern bloqueante ou de aviso encontrado no código novo desta fase. Scan por `TODO`/`FIXME`/`PLACEHOLDER`/"não disponível"/"em breve" no código relacionado a `neg`/`proposta`/`termo`/`contrato` retornou zero ocorrências reais (apenas comentários explicativos legítimos citando IDs de review, ex. `WR-02`, `WR-03`).

Nota informativa (não bloqueante): `IN-03` do `11.1-REVIEW.md` (disclaimer duplicado literalmente na tela de revisão em vez de interpolar a constante `DISCLAIMER_NEG`) permanece não corrigido — foi triado como item de baixa severidade, registrado para a Fase 13 (refino), e os dois textos permanecem character-idênticos hoje (confirmado por grep). Não afeta o funcionamento nem a integridade do disclaimer impresso (que sempre usa a constante).

### Human Verification Required

### 1. Revisão jurídica das 3 minutas pelo usuário (advogado)

**Test:** Ler o texto completo das 3 minutas geradas (Proposta, Termo de Exclusividade, Contrato) — cláusulas, disclaimer, foro — com atenção profissional de advogado.
**Expected:** Redação jurídica adequada para uso real com clientes; foro ("Goiânia/GO") correto; disclaimer suficiente; nenhuma cláusula precisa de correção antes de considerar o conteúdo final.
**Why human:** Adequação jurídica de texto não é verificável programaticamente — é um gate humano explicitamente definido em `11.1-CONTEXT.md` ("Cláusulas draftadas por mim + revisão do usuário") e `11.1-UI-SPEC.md` ("HUMAN-UAT desta fase inclui revisão jurídica das 3 minutas pelo usuário (advogado) — texto das cláusulas, disclaimer, foro — antes de considerar o conteúdo final").

### 2. Inspeção visual do PDF A4 gerado (paginação/layout)

**Test:** Gerar e imprimir/exportar PDF dos 3 documentos com dados de teste variados (nomes longos, CRECI ausente no Termo, Termo com/sem cláusula de anúncio) e inspecionar visualmente o resultado.
**Expected:** Cláusulas não cortadas ao meio pela quebra de página (`page-break-inside:avoid` funcionando na prática), disclaimer legível e destacado, blocos de assinatura/testemunhas bem posicionados lado a lado.
**Why human:** Renderização real de impressão A4 (quebras de página do navegador, margens, legibilidade) depende do motor de impressão do navegador/OS e não é verificável por grep/teste automatizado — listado como pendência explícita no `11.1-03-SUMMARY.md` ("Validação visual do layout A4 real").

### Gaps Summary

Nenhum gap de implementação encontrado. Todos os 7 truths derivados do ROADMAP + must_haves dos 3 plans foram verificados com evidência direta no código: as 3 minutas são geradas deterministicamente, a integração com o Modo Captação funciona, a privacidade é preservada (zero persistência de dados de partes), os 3 fixes do code review (WR-01/02/03) estão presentes e corretos no código, e a suite de 79 testes passa integralmente sem regressão.

NEG-04 (OCR) está corretamente deferido — documentado como decisão do usuário em `11.1-CONTEXT.md`, não implementado no código (confirmado), e fora do `requirements:` dos 3 plans. Isto NÃO é um gap: é o comportamento esperado da decisão de escopo registrada.

O único motivo para o status `human_needed` em vez de `passed` é o gate humano obrigatório e explicitamente documentado desta fase: a revisão jurídica das 3 minutas por advogado (mencionada tanto no CONTEXT quanto no UI-SPEC como parte do HUMAN-UAT da fase) e a inspeção visual do PDF A4 real. Nenhum destes itens é uma falha técnica — são verificações que só um humano pode realizar.

---

_Verified: 2026-07-07T21:30:00Z_
_Verifier: Claude (gsd-verifier)_
