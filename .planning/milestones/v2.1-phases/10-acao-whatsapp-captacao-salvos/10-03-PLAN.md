---
phase: 10-acao-whatsapp-captacao-salvos
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - radar-goiania.html
autonomous: true
requirements: [ZAP-01, CAPT-01, ACAO-01]
tags: [whatsapp, captacao, sheet, clipboard, acao-01, lei-da-tela]

must_haves:
  truths:
    - "Dentro de #dActsMore, apos os itens existentes, aparece o grupo 'Copiar para WhatsApp' (rotulo estatico + 5 botoes .zapbtn empilhados, ordem: resumo -> proprietario -> comprador -> preco -> riscos) separado por border-top tracejada; abaixo dele, o botao '🧾 Captar este imóvel'"
    - "Cada botao de WhatsApp copia o texto do template correspondente (zap* de 10-01) montado com dados REAIS da ficha (DCUR + faixa + __scores + radar_prof) e produz toast ESPECIFICO por tipo (nunca 'Copiado' generico); falha de clipboard produz toast de falha"
    - "Texto copiado NUNCA contem '[seu nome]'/'[CRECI]' quando radar_prof esta vazio — a assinatura e omitida (contrato de 10-01, aqui verificado no wiring)"
    - "'Captar este imóvel' abre #captSheet (sheet full-screen reusando o padrao .wiz — whead sem dots, wbody, wclose 44px, role=dialog aria-modal) com os 4 blocos (abordagem/script/checklist/follow-up) populados pelos templates capt* + botao Copiar individual por bloco + disclaimer fixo; fechar devolve o foco ao elemento que abriu (padrao WIZRET)"
    - "ACAO-01 sweep: toda superficie de resultado termina com acao util — ficha (acts da Fase 9 + ⭐/zap/captar), lista (count com CSV), estado vazio (exemplos + oportunidades/historico da 10-02), chooser (escolher unidade JA E a acao) — checklist verificado"
  artifacts:
    - path: "radar-goiania.html"
      provides: "dadosFicha() (monta o objeto data p/ templates), copyZap(tipo), copyTexto(texto,okMsg) (clipboard com fallback), abrirCaptacao()/fecharCaptacao()/copyCapt(tipo), HTML #captSheet, CSS .zapgroup/.zapbtn/.captblock/.captcopy/.captdisclaimer"
      contains: "function copyZap"
    - path: "radar-goiania.html"
      provides: "#captSheet no body (irmao de #wiz), estrutura paralela ao wizard sem wdots"
      contains: "id=\"captSheet\""
  key_links:
    - from: "copyZap(tipo)/abrirCaptacao()"
      to: "zapResumo/zapProprietario/zapComprador/zapArgumento/zapRiscos/captAbordagem/captScript/captChecklist/captFollowup (RADAR_PURE, 10-01)"
      via: "dadosFicha() monta o objeto data {endereco,bairro,quadra,lote,tipoImovel,faixa,scoreOp,scoreConf,leitura,perfil} a partir de DCUR/mercadoEstimado/DCUR.__scores/radar_prof"
      pattern: "function dadosFicha"
    - from: "copyZap/copyCapt"
      to: "navigator.clipboard com fallback"
      via: "copyTexto(texto,okMsg): writeText -> toast ok; catch/ausencia -> fallback execCommand/selecao -> toast de falha se nada funcionar"
      pattern: "function copyTexto"
    - from: "abrirCaptacao()/fecharCaptacao()"
      to: "padrao WIZRET de foco (abrirLaudo/fecharLaudo)"
      via: "CAPTRET=document.activeElement ao abrir; focus() de volta ao fechar"
      pattern: "CAPTRET"
---

<objective>
Entregar a camada de ação final da Fase 10: grupo "Copiar para WhatsApp" (5 botões) e entrada "Captar este imóvel" dentro de `#dActsMore`, e o sheet Modo Captação (`#captSheet`, padrão `.wiz` sem steps) com os 4 textos prontos + copiar individual + disclaimer — tudo alimentado pelos templates puros de 10-01 com dados reais da ficha (DCUR + faixa + scores atuais + radar_prof), clipboard com fallback e toast específico por tipo. Fecha com o sweep de ACAO-01 (toda superfície de resultado termina com ação útil).

Purpose: ZAP-01/CAPT-01 são o coração comercial da fase — o corretor sai de qualquer ficha com mensagem pronta no clipboard ou um kit de captação completo. ACAO-01 é o critério transversal: nenhuma tela de resultado morre sem próxima ação.
Output: `radar-goiania.html` com copyZap/copyCapt/abrirCaptacao/fecharCaptacao/dadosFicha/copyTexto, HTML do #captSheet, CSS dos componentes 1 e 5 do UI-SPEC, e o checklist ACAO-01 verificado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-acao-whatsapp-captacao-salvos/10-CONTEXT.md
@.planning/phases/10-acao-whatsapp-captacao-salvos/10-UI-SPEC.md
@.planning/phases/10-acao-whatsapp-captacao-salvos/10-01-SUMMARY.md
@.planning/phases/10-acao-whatsapp-captacao-salvos/10-02-SUMMARY.md

# Fonte de código (ler antes de editar)
@radar-goiania.html

<facts>
<!-- Verificado por leitura direta deste repo (estado pós 10-01/10-02). -->

Templates puros disponíveis (10-01, bloco RADAR_PURE): `zapResumo/zapProprietario/zapComprador/
zapArgumento/zapRiscos/captAbordagem/captScript/captChecklist/captFollowup(data)` — todos recebem o
MESMO objeto `data` (shape documentado no 10-01-SUMMARY): `{endereco,bairro,quadra,lote,tipoImovel,
faixa:{lo,hi}|null, scoreOp|null, scoreConf|null, leitura:string, perfil:{nome,creci,contato}|null}`.
Retornam STRING PURA (texto p/ clipboard, com \n) — para exibir no #captSheet via innerHTML, passar
por esc() e usar CSS white-space:pre-wrap (UI-SPEC .captblock-txt) — NUNCA innerHTML sem esc().

Fontes dos dados reais na ficha (todos já em escopo/alcançáveis a partir de DCUR):
- `DCUR` — objeto cadastral atual (guard obrigatório: if(!DCUR)return; toda ação de ficha).
- `mercadoEstimado(DCUR)` → `{lo,hi,metodo}` ou null → data.faixa.
- `DCUR.__scores` — cache `{op,conf}` gravado pelos call sites de renderScoresInto (10-02, Task 1).
- `leituraPratica({tipoImovel,bairro,oportunidade,confianca})` — RE-CHAMAR com os valores de
  __scores para obter a leitura ATUAL (mais simples e sempre consistente do que raspar #dLeitura).
- `radar_prof` — `let prof=null;try{const p=JSON.parse(localStorage.getItem("radar_prof")||"{}");
  if(p&&p.nome)prof=p;}catch(e){}` — perfil só é passado quando tem nome (contrato de assinatura).
- `addrDe(DCUR)`, `clean(DCUR.nmbairro)`, `clean(DCUR.nrquadra)`, `clean(DCUR.nrlote)`,
  `unitLabel(DCUR)||USO[DCUR.uso]||"Imóvel"` — mesmos helpers de showDetail().

Clipboard hoje: `copyInsc(v)` (linha ~2219) usa `navigator.clipboard.writeText(v).then(ok,()=>{})`
— SEM fallback real. `copyLink` tem toast de falha mas também sem fallback. Esta plan cria
`copyTexto(texto,okMsg)` COM fallback (textarea temporária + document.execCommand("copy") quando
navigator.clipboard ausente/rejeita) e toast de falha ("Não foi possível copiar — tente selecionar
o texto manualmente.", copy travada no UI-SPEC Component 1) — os copiadores novos (copyZap/copyCapt)
usam copyTexto; copyInsc/copyLink existentes NÃO são retrofitados (fora de escopo, zero regressão).

Padrão .wiz (linhas 373-383 CSS; 741-749 HTML): `.wiz{position:fixed;inset:0;z-index:2000;
background:var(--paper-2);display:flex;flex-direction:column}`; `#wiz` tem `.wtop` (wback+wdots+
wclose), `.wbody`, `.wfoot`. O #captSheet REUSA as classes .wiz/.wbody/.wclose mas com cabeçalho
SEM dots e SEM .wfoot (não é wizard sequencial — UI-SPEC Component 5). `.wh1` existente é
`font:800 24px/1.15` (o UI-SPEC cita 20px como referência de proporção; reusar a classe existente
tal qual — zero CSS novo de heading). Foco: `WIZRET=document.activeElement` em abrirLaudo (linha
2355), devolvido em fecharLaudo (2360-2361) — replicar com variável própria `CAPTRET` (não
compartilhar WIZRET: os dois sheets podem coexistir em tese; isolamento é mais seguro).

Esc key global (listener em ~2794): cadeia de prioridade caixaList → correctMenu → ambigChips → ...
Adicionar `#captSheet` NO TOPO da cadeia (sheet modal mais recente tem prioridade): se
`!document.getElementById("captSheet").hidden` → `fecharCaptacao();return;` — mesma posição
estrutural onde o #wiz é tratado (conferir se #wiz está na cadeia; adicionar captSheet ANTES dele).

Animação de entrada: mAnimate + REDUCE (padrão showDetail linhas 2213-2217 / .wiz). Para o sheet:
`if(!REDUCE)mAnimate(el,{opacity:[0,1]},{duration:0.18})` no desktop-like fade simples — ou snap
quando REDUCE (UI-SPEC Acessibilidade: reuso da lógica condicional, nada coreografado novo).

ACAO-01 — estado das superfícies (para o sweep da Task 2):
- Ficha: OK desde a Fase 9 (1 primária + 2 secundárias + Mais opções) — esta fase ADICIONA ⭐ (10-02)
  e zap/captar (esta plan) sem violar o limite (tudo novo em #dActsMore, exceto ⭐ que substituiu).
- Lista/count: `render()` linha ~1901 — count + botão CSV "⬇ Baixar planilha" (ação existe; salvar
  em lote está explicitamente FORA — CONTEXT.md).
- Estado vazio: exemplos tocáveis (Fase 8) + blocos Oportunidades/Histórico (10-02).
- Chooser (#chooser): cada .chrow JÁ É a ação (escolher unidade → abre ficha) — conforme.
- Erro de busca: tratado na Fase 8 (próximo passo em 1 toque) — conforme.
O sweep é um CHECKLIST de verificação (asserts de presença), não implementação nova — se alguma
superfície reprovar, corrigir pontualmente dentro desta task.
</facts>

<interfaces>
<!-- Contrato criado por esta plan. -->

```js
function dadosFicha(){}          // -> objeto data (shape 10-01) montado de DCUR/mercadoEstimado/__scores/leituraPratica/radar_prof; null se !DCUR
function copyTexto(texto,okMsg){} // clipboard com fallback execCommand; toast okMsg no sucesso, toast de falha padrao no erro
function copyZap(tipo){}          // tipo: 'resumo'|'proprietario'|'comprador'|'preco'|'riscos' -> template + copyTexto com toast especifico
function abrirCaptacao(){}        // popula os 4 .captblock-txt (esc()+pre-wrap) e abre #captSheet; CAPTRET=activeElement
function fecharCaptacao(){}       // esconde #captSheet; devolve foco a CAPTRET
function copyCapt(tipo){}         // tipo: 'zap'|'ligacao'|'checklist'|'followup' -> template + copyTexto com toast especifico
```

HTML do grupo WhatsApp + entrada Captação (APPEND ao final do innerHTML de #dActsMore em
showDetail(), depois de mapLinks — UI-SPEC Components 1 e 5):
```html
<div class="zapgroup">
  <div class="zapgroup-lbl">Copiar para WhatsApp</div>
  <button type="button" class="zapbtn" onclick="copyZap('resumo')">💬 Resumo do imóvel ⧉</button>
  <button type="button" class="zapbtn" onclick="copyZap('proprietario')">🏠 Mensagem para o proprietário ⧉</button>
  <button type="button" class="zapbtn" onclick="copyZap('comprador')">🔑 Mensagem para o comprador ⧉</button>
  <button type="button" class="zapbtn" onclick="copyZap('preco')">💲 Argumento de preço ⧉</button>
  <button type="button" class="zapbtn" onclick="copyZap('riscos')">⚠️ Riscos e ressalvas ⧉</button>
</div>
<div class="zapgroup">
  <button type="button" class="zapbtn" onclick="abrirCaptacao()">🧾 Captar este imóvel</button>
</div>
```
(Nenhum dado é interpolado nos onclick — literais fixos; IN-01 trivialmente satisfeito.)

HTML do #captSheet (estático no body, irmão de #wiz — UI-SPEC Component 5, estrutura exata):
```html
<div class="wiz" id="captSheet" hidden role="dialog" aria-modal="true" aria-label="Modo captação">
  <div class="wtop">
    <span style="width:44px"></span>
    <div class="wh1" style="margin:0;font-size:20px">Modo captação</div>
    <button class="wclose" onclick="fecharCaptacao()" aria-label="Fechar">×</button>
  </div>
  <div class="wbody" id="captBody">
    <p class="wsub">Textos prontos para captar este imóvel — copie e ajuste o que precisar.</p>
    <div class="captblock">
      <div class="captblock-lbl">💬 Abordagem por WhatsApp</div>
      <div class="captblock-txt" id="captZap"></div>
      <button type="button" class="captcopy" onclick="copyCapt('zap')">Copiar ⧉</button>
    </div>
    <div class="captblock">
      <div class="captblock-lbl">📞 Script de ligação</div>
      <div class="captblock-txt" id="captLig"></div>
      <button type="button" class="captcopy" onclick="copyCapt('ligacao')">Copiar ⧉</button>
    </div>
    <div class="captblock">
      <div class="captblock-lbl">📋 Checklist documental</div>
      <div class="captblock-txt" id="captDoc"></div>
      <button type="button" class="captcopy" onclick="copyCapt('checklist')">Copiar ⧉</button>
    </div>
    <div class="captblock">
      <div class="captblock-lbl">✅ Tarefa de follow-up</div>
      <div class="captblock-txt" id="captFollow"></div>
      <button type="button" class="captcopy" onclick="copyCapt('followup')">Copiar ⧉</button>
    </div>
    <div class="captdisclaimer">Textos gerados automaticamente a partir dos dados do imóvel — revise antes de enviar. Não substituem orientação jurídica.</div>
  </div>
</div>
```

CSS novo (zero hex novo, medidas EXATAS do UI-SPEC Components 1/5):
```css
.zapgroup{display:flex;flex-direction:column;gap:8px;margin-top:12px;padding-top:12px;border-top:1px dashed var(--line);width:100%}
.zapgroup-lbl{font:600 10px/1.4 "IBM Plex Mono",monospace;letter-spacing:.07em;text-transform:uppercase;color:var(--muted)}
/* (fix plan-check) especificidade OBRIGATÓRIA (0,3,0) > .detail .acts button (0,2,1): #dActsMore tem
   class "footbody acts", então o genérico (flex:1/center) vence .zapbtn (0,1,0) por contagem de classe.
   Seletor incondicional — nada de "conferir e ajustar se necessário". */
.detail .acts .zapbtn{width:100%;flex:none;min-height:44px;border:1.5px solid var(--line);background:var(--paper);color:var(--ink);font:600 13.5px/1 "IBM Plex Sans";border-radius:2px;text-align:left;justify-content:flex-start;padding:0 14px;display:flex;align-items:center;gap:8px;cursor:pointer}
.detail .acts .zapbtn:hover{border-color:var(--ink)}
.captblock{background:var(--paper);border:1.5px solid var(--line);border-radius:2px;padding:12px 14px;margin-bottom:14px}
.captblock-lbl{font:700 12px/1.2 "IBM Plex Sans";color:var(--ink);margin-bottom:6px;display:block}
.captblock-txt{font:500 13.5px/1.55 "IBM Plex Sans";color:var(--ink);white-space:pre-wrap;margin-bottom:10px}
.captcopy{min-height:44px;width:100%;border:1.5px solid var(--ink);background:var(--paper-2);color:var(--ink);font:600 13px/1 "IBM Plex Sans";border-radius:2px;cursor:pointer}
.captdisclaimer{font:500 10.5px/1.45 "IBM Plex Sans";color:var(--muted);margin-top:4px;padding-top:10px;border-top:1px dashed var(--line)}
```
Nota (ATUALIZADA pós plan-check): a versão anterior desta nota estava FACTUALMENTE errada — não há
"especificidade igual": `.detail .acts button` é (0,2,1) e vence `.zapbtn` (0,1,0) por contagem de
classes. O seletor `.detail .acts .zapbtn` (0,3,0) acima resolve INCONDICIONALMENTE (mesmo padrão
do fix WR-01 da Fase 9, button.primary). `flex:none` incluso para anular o flex:1 do genérico.

Toasts (copy EXATA do UI-SPEC Components 1/5):
resumo→"Resumo copiado — cole no WhatsApp." · proprietario→"Mensagem para o proprietário copiada —
cole no WhatsApp." · comprador→"Mensagem para o comprador copiada — cole no WhatsApp." ·
preco→"Argumento de preço copiado — cole no WhatsApp." · riscos→"Texto de riscos e ressalvas
copiado — cole no WhatsApp." · capt zap→"Abordagem copiada — cole no WhatsApp." ·
ligacao→"Script de ligação copiado." · checklist→"Checklist copiado." · followup→"Tarefa de
follow-up copiada." · falha→"Não foi possível copiar — tente selecionar o texto manualmente."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dadosFicha() + copyTexto() + grupo WhatsApp em #dActsMore (copyZap com toast específico)</name>
  <files>radar-goiania.html</files>
  <behavior>
    - dadosFicha() com DCUR completo -> objeto data com endereco=addrDe(DCUR), bairro=clean(nmbairro), faixa de mercadoEstimado (ou null), scoreOp/scoreConf de DCUR.__scores (ou null), leitura=leituraPratica(...) recalculada, perfil de radar_prof SOMENTE se p.nome (senao null); dadosFicha() sem DCUR -> null
    - copyZap('resumo'|'proprietario'|'comprador'|'preco'|'riscos') mapeia para zapResumo/zapProprietario/zapComprador/zapArgumento/zapRiscos e chama copyTexto(texto, toastEspecifico) — 5 toasts DISTINTOS (tabela de interfaces), nunca "Copiado" generico
    - copyTexto: navigator.clipboard.writeText ok -> toast(okMsg); rejeicao/ausencia -> fallback textarea+execCommand; se ambos falham -> toast("Não foi possível copiar — tente selecionar o texto manualmente.")
    - showDetail() renderiza os 5 .zapbtn + "🧾 Captar este imóvel" DENTRO de #dActsMore, DEPOIS dos itens existentes (Copiar inscrição/custos/CND/copiar link/mapas), com separador tracejado .zapgroup
  </behavior>
  <action>
1. Criar `dadosFicha()` perto de copyInsc/copyLink: guard `if(!DCUR)return null;` — montar e
   retornar o objeto data conforme `&lt;facts&gt;`: `const em=mercadoEstimado(DCUR); const sc=
   DCUR.__scores||{}; const conf=sc.conf||null, op=sc.op||null; const tipo=unitLabel(DCUR)||
   USO[DCUR.uso]||"Imóvel"; let prof=null;try{const p=JSON.parse(localStorage.getItem("radar_prof")
   ||"{}");if(p&&p.nome)prof=p;}catch(e){} return {endereco:addrDe(DCUR), bairro:clean(DCUR.nmbairro),
   quadra:clean(DCUR.nrquadra), lote:clean(DCUR.nrlote), tipoImovel:tipo, faixa:em?{lo:em.lo,
   hi:em.hi}:null, scoreOp:op, scoreConf:conf, leitura:leituraPratica({tipoImovel:tipo,bairro:
   clean(DCUR.nmbairro),oportunidade:op,confianca:conf}), perfil:prof};`
2. Criar `copyTexto(texto,okMsg)`: tentar `navigator.clipboard.writeText(texto).then(()=>toast(okMsg),
   fallback)`; quando navigator.clipboard ausente, ir direto ao fallback: criar `<textarea>` fora da
   viewport (position:fixed;opacity:0), value=texto, select(), `document.execCommand("copy")`,
   remover — sucesso → toast(okMsg); qualquer falha → toast("Não foi possível copiar — tente
   selecionar o texto manualmente."). Função única reusada por copyZap E copyCapt (Task 2).
3. Criar `copyZap(tipo)`: `const d=dadosFicha();if(!d)return;` — mapa tipo→[funçãoTemplate, toast]
   com os 5 pares EXATOS de `&lt;interfaces&gt;`; chamar `copyTexto(fn(d), toastMsg)`.
4. Em `showDetail()`, APPEND ao final do innerHTML de `#dActsMore` (depois de `${mapLinks}`) os dois
   `.zapgroup` de `&lt;interfaces&gt;` (5 botões zap + botão Captar) — HTML literal, sem interpolação
   nos onclick (IN-01 trivial).
5. CSS: acrescentar `.zapgroup/.zapgroup-lbl` + o seletor OBRIGATÓRIO `.detail .acts .zapbtn`
   (fix plan-check — INCONDICIONAL, com flex:none; ver nota de especificidade em `&lt;interfaces&gt;`)
   junto às regras `.maisopcoes` (linha ~280).
6. Rodar a suite completa (zero regressão no RADAR_PURE).
  </action>
  <verify>
    <automated>node --check radar-goiania.html && python -c "
s=open('radar-goiania.html',encoding='utf-8').read()
for fn in ['function dadosFicha','function copyTexto','function copyZap']:
    assert fn in s, fn+' ausente'
i=s.index('function copyZap')
body=s[i:i+1600]
for t in ['zapResumo','zapProprietario','zapComprador','zapArgumento','zapRiscos']:
    assert t in body, t+' nao mapeado em copyZap'
for toastmsg in ['Resumo copiado — cole no WhatsApp.','Mensagem para o proprietário copiada','Mensagem para o comprador copiada','Argumento de preço copiado','riscos e ressalvas copiado']:
    assert toastmsg in s, 'toast especifico ausente: '+toastmsg
assert 'Não foi possível copiar — tente selecionar o texto manualmente.' in s, 'toast de falha de clipboard ausente'
i=s.index('function dadosFicha')
dfb=s[i:i+1600]
assert 'p.nome' in dfb or 'prof.nome' in dfb or '.nome)' in dfb, 'dadosFicha nao condiciona perfil a nome presente'
assert 'mercadoEstimado(' in dfb and '__scores' in dfb and 'leituraPratica(' in dfb, 'dadosFicha nao monta faixa/scores/leitura reais'
i=s.index('function showDetail(')
sd=s[i:i+9000]
assert 'zapgroup' in sd and \"copyZap('resumo')\" in sd and 'abrirCaptacao()' in sd, 'grupo WhatsApp/Captar ausente de #dActsMore'
iMore=sd.index('dActsMore');iZap=sd.index('zapgroup')
assert iZap>iMore, 'zapgroup deve vir depois do inicio de #dActsMore'
assert 'execCommand' in s[s.index('function copyTexto'):s.index('function copyTexto')+1400], 'copyTexto sem fallback execCommand'
print('OK: dadosFicha + copyTexto + copyZap + grupo em #dActsMore confirmados')
" && node --test "tests/*.test.mjs" 2>&1 | tail -5</automated>
  </verify>
  <done>dadosFicha monta o objeto data real (faixa/scores/leitura/perfil-condicionado-a-nome); copyTexto tem fallback execCommand e toast de falha; copyZap mapeia os 5 tipos com 5 toasts específicos; grupo WhatsApp + Captar renderizado ao final de #dActsMore; suite passa.</done>
</task>

<task type="auto">
  <name>Task 2: Sheet Modo Captação (#captSheet, padrão .wiz) + Esc/foco + sweep ACAO-01</name>
  <files>radar-goiania.html</files>
  <action>
1. Inserir o HTML de `#captSheet` (bloco exato de `&lt;interfaces&gt;`) no body, logo após o
   fechamento de `#wiz` (linha ~749) — irmão estrutural do wizard, reusando .wiz/.wtop/.wbody/
   .wclose/.wh1/.wsub sem criar classes de layout novas.
2. Criar `abrirCaptacao()`: guard `if(!DCUR)return;` — `const d=dadosFicha();if(!d)return;` popular
   `#captZap/#captLig/#captDoc/#captFollow` com `esc(captAbordagem(d))/esc(captScript(d))/
   esc(captChecklist(d))/esc(captFollowup(d))` via innerHTML (esc() obrigatório — texto contém dado
   de cadastro; pre-wrap do CSS preserva os \n); `CAPTRET=document.activeElement;`
   `document.getElementById("captSheet").hidden=false;` + animação de entrada REDUCE-aware
   (`if(!REDUCE)mAnimate(sheet,{opacity:[0,1]},{duration:0.18});` — snap quando REDUCE, UI-SPEC
   Acessibilidade); focar o `.wclose` do sheet (`sheet.querySelector(".wclose").focus()` — primeiro
   elemento interativo, anuncia o dialog para leitor de tela).
   OPCIONAL (discrição permitida pelo UI-SPEC Estados Transversais): quando `d.perfil===null`,
   acrescentar ao `.captdisclaimer` a nota "Textos sem assinatura — complete seu perfil no wizard
   de documento para assinar automaticamente."
3. Criar `fecharCaptacao()`: `hidden=true`; devolver foco a CAPTRET (mesmo try/catch de
   fecharLaudo); `CAPTRET=null`. Criar `copyCapt(tipo)`: mapa 'zap'/'ligacao'/'checklist'/'followup'
   → [captAbordagem/captScript/captChecklist/captFollowup, toast específico da tabela de
   `&lt;interfaces&gt;`] → `copyTexto(fn(dadosFicha()), toastMsg)` (recalcular dadosFicha é barato e
   garante consistência se os scores atualizaram com o sheet aberto).
4. Esc: no listener global de keydown (linha ~2794), adicionar NO TOPO da cadeia:
   `const cs=document.getElementById("captSheet");if(cs&&!cs.hidden){fecharCaptacao();return;}`.
5. CSS: acrescentar `.captblock/.captblock-lbl/.captblock-txt/.captcopy/.captdisclaimer` (bloco
   exato de `&lt;interfaces&gt;`) junto às regras `.wiz` (linha ~373).
6. SWEEP ACAO-01 (checklist de verificação — corrigir pontualmente o que reprovar):
   a. Ficha: #dActsPrim = 1 primária + 2 secundárias; #dActsMore contém Copiar inscrição, custos,
      CND, copiar link, mapas, grupo WhatsApp (5), Captar — nenhuma superfície sem ação.
   b. Lista: .count contém o botão CSV (ação da lista).
   c. Estado vazio: #emptyState com #exampleChips + blocos Oportunidades/Histórico (10-02).
   d. Chooser: .chrow abre ficha (ação embutida).
   e. Erro/vazio de busca: mensagens da Fase 8 com próximo passo.
   Registrar o resultado do sweep no SUMMARY (tabela superfície→ação); qualquer correção necessária
   é feita DENTRO desta task (mudanças pequenas de copy/botão apenas — nada estrutural novo).
7. Rodar a suite completa + node --check.
  </action>
  <verify>
    <automated>node --check radar-goiania.html && python -c "
s=open('radar-goiania.html',encoding='utf-8').read()
assert 'id=\"captSheet\"' in s and 'role=\"dialog\"' in s[s.index('id=\"captSheet\"')-120:s.index('id=\"captSheet\"')+120], '#captSheet ausente ou sem role=dialog'
cs=s[s.index('id=\"captSheet\"'):s.index('id=\"captSheet\"')+2600]
for idd in ['captZap','captLig','captDoc','captFollow']:
    assert idd in cs, idd+' ausente do sheet'
assert cs.count('captcopy')>=4, 'menos de 4 botoes Copiar individuais'
assert 'Não substituem orientação jurídica.' in cs, 'disclaimer ausente'
assert 'wdots' not in cs, 'sheet nao deve ter steps/dots (nao e wizard)'
for fn in ['function abrirCaptacao','function fecharCaptacao','function copyCapt']:
    assert fn in s, fn+' ausente'
i=s.index('function abrirCaptacao')
ab=s[i:i+1800]
assert 'CAPTRET' in ab and 'esc(' in ab, 'abrirCaptacao sem CAPTRET ou sem esc() nos textos'
for t in ['captAbordagem','captScript','captChecklist','captFollowup']:
    assert t in s[s.index('function copyCapt'):s.index('function copyCapt')+1400] or t in ab, t+' nao usado no wiring'
for toastmsg in ['Abordagem copiada — cole no WhatsApp.','Script de ligação copiado.','Checklist copiado.','Tarefa de follow-up copiada.']:
    assert toastmsg in s, 'toast de captacao ausente: '+toastmsg
iesc=s.index('e.key!==\"Escape\"') if 'e.key!==\"Escape\"' in s else s.index('Escape')
assert 'captSheet' in s[iesc:iesc+1200], 'Esc nao fecha o captSheet (cadeia de prioridade)'
print('OK: captSheet + wiring + Esc/foco confirmados')
" && node --test "tests/*.test.mjs" 2>&1 | tail -5</automated>
  </verify>
  <done>#captSheet abre com os 4 textos reais (esc()+pre-wrap), copiar individual com toast específico, disclaimer fixo, Esc fecha, foco devolvido via CAPTRET, REDUCE respeitado; sweep ACAO-01 executado e documentado (toda superfície de resultado termina com ação); suite passa; node --check passa.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dadosFicha() -> templates -> clipboard | dados do cadastro + perfil do corretor viram texto que SAI do app (WhatsApp/ligação) — sem PII de terceiro por construção (templates 10-01 só recebem campos do imóvel/perfil próprio) |
| Templates (string com \n e dado de cadastro) -> innerHTML do #captSheet | texto de template reentra no DOM — esc() obrigatório |
| radar_prof (localStorage) -> assinatura nos textos | dado do PRÓPRIO corretor (não terceiro), leitura silenciosa padrão |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Tampering (XSS) | abrirCaptacao() populando .captblock-txt via innerHTML com texto que contém endereco/bairro do cadastro | mitigate | esc() em CADA um dos 4 textos antes do innerHTML; verify automatizado confirma esc( no corpo de abrirCaptacao |
| T-10-09 | Information Disclosure | Texto copiado contendo dado além do imóvel/perfil (ex.: se um template futuro interpolar campo extra) | mitigate | dadosFicha() é o ÚNICO ponto de montagem do data — allowlist implícita de 10 campos; templates 10-01 testados não referenciam nada fora de data |
| T-10-10 | Spoofing | Assinatura "[seu nome]" copiada por engano quando perfil vazio | mitigate | dadosFicha só passa perfil quando p.nome existe (null caso contrário); contrato de omissão testado em 10-01; verificação de fase re-testa no texto copiado real |
| T-10-11 | Denial of Service (UX) | clipboard indisponível (contexto não-seguro/browser antigo) sem feedback — corretor acha que copiou | mitigate | copyTexto com fallback execCommand + toast de falha explícito quando ambos os caminhos falham (nunca silencioso) |

**Sem ameaças de alta severidade.** Nenhuma rede nova; dados saem apenas via clipboard por ação explícita do corretor.
</threat_model>

<verification>
- `node --check radar-goiania.html` passa; `node --test "tests/*.test.mjs"` passa (zero regressão)
- Grupo WhatsApp (5 botões, ordem fixa, toasts específicos) + Captar dentro de #dActsMore, depois dos itens existentes
- copyTexto tem fallback execCommand + toast de falha; copyZap/copyCapt usam-no
- #captSheet: 4 blocos populados por templates reais (esc()), copiar individual, disclaimer, sem dots, Esc fecha, foco CAPTRET
- dadosFicha condiciona perfil a nome presente; textos sem placeholder de assinatura
- Sweep ACAO-01 documentado: ficha/lista/vazio/chooser/erro — toda superfície com ação
- Alvos ≥44px (zapbtn/captcopy/wclose); zero hex novo; REDUCE respeitado
</verification>

<success_criteria>
- ZAP-01 cumprido fim-a-fim: 5 mensagens copiáveis com dados reais da ficha, tom de corretor, toast específico, sem placeholder
- CAPT-01 cumprido fim-a-fim: Modo Captação com 4 textos prontos copiáveis individualmente + disclaimer
- ACAO-01 cumprido: sweep documentado, toda superfície de resultado termina com ação útil
- Fase 10 completa (com 10-01/10-02): pronta para /gsd-verify-phase (verificação visual mobile 375/desktop 1280 fica para a fase)
</success_criteria>

<output>
Após completar, criar `.planning/phases/10-acao-whatsapp-captacao-salvos/10-03-SUMMARY.md`.
Incluir no SUMMARY: a tabela do sweep ACAO-01 (superfície → ação), o conteúdo final de #dActsMore (ordem dos itens), o wiring dadosFicha→templates (campos usados), e as pendências para a verificação de fase (visual 375/1280, contraste, teclado no sheet).
</output>
