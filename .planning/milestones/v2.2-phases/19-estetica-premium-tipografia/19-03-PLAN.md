---
phase: 19-estetica-premium-tipografia
plan: 03
type: execute
wave: 3
depends_on: [19-01, 19-02]
files_modified:
  - radar-goiania.html
autonomous: false
requirements: [A11Y-01]
security_enforcement: true

must_haves:
  truths:
    - "Em cada uma das 6 superfícies modais, Tab/Shift+Tab circulam SÓ entre os focáveis visíveis do modal"
    - "Ao fechar qualquer modal, o foco retorna ao elemento que o abriu (gatilho)"
    - "A cadeia de Esc mantém a ordem de prioridade EXATA de antes (nenhuma regressão)"
    - "O combobox ARIA da busca e o quirk iOS do drag do bottom sheet continuam funcionando"
    - "Existe UM utilitário compartilhado (trapFocus/untrapFocus), nunca 6 implementações"
  artifacts:
    - path: "radar-goiania.html"
      provides: "trapFocus/untrapFocus/trapFocaveis (1 definição) + 6 call-sites de trapFocus e 6 de untrapFocus nas funções de abrir/fechar"
      contains: "function trapFocus"
  key_links:
    - from: "abrirComparacao / showDetail / abertura chooser (gap sem pattern)"
      to: "trapFocus(container)"
      via: "chamada na abertura"
      pattern: "trapFocus\\("
    - from: "fecharComparacao / closeDetail / closeChooser"
      to: "untrapFocus()"
      via: "chamada no fechamento"
      pattern: "untrapFocus\\("
---

<objective>
Implementar UM utilitário único de focus-trap (`trapFocus(container)`/`untrapFocus()`/`trapFocaveis()`) e aplicá-lo às 6 superfícies modais — 4 que já têm padrão de captura de foco (onboarding, wizard, negSheet, captSheet) e 2 que NÃO têm (cmpSheet, detail/chooser) — sem tocar a cadeia de Esc, o combobox ARIA da busca nem o quirk iOS. Fecha o IN-03 diferido da Fase 13. Termina com o gate de verificação (greps + npm test) e o checkpoint humano (UAT premium + Tab-cycle + PDF).

Purpose: A11Y-01. Roda por último (mesmo arquivo, dependência sequencial) porque valida a fase inteira ao vivo: fonte renderizada de fato (document.fonts.check), estética premium (juízo do usuário) e focus-trap nas 6 superfícies.
Output: `radar-goiania.html` com focus-trap acessível + relatório de gate para o orquestrador.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-estetica-premium-tipografia/19-RESEARCH.md
@.planning/phases/19-estetica-premium-tipografia/19-UI-SPEC.md
@.planning/phases/19-estetica-premium-tipografia/19-VALIDATION.md
@.planning/phases/19-estetica-premium-tipografia/19-01-SUMMARY.md
@.planning/phases/19-estetica-premium-tipografia/19-02-SUMMARY.md

<interfaces>
<!-- Utilitario travado no UI-SPEC (colar como esta, generalizado do padrao onbLastFocus da Fase 13) -->
let TRAP_CONTAINER=null, TRAP_LASTFOCUS=null, TRAP_HANDLER=null;
function trapFocus(container){
  TRAP_LASTFOCUS = document.activeElement;      // gatilho — devolve foco aqui ao destrapar
  TRAP_CONTAINER = container;
  TRAP_HANDLER = function(e){
    if(e.key !== "Tab") return;
    const focaveis = trapFocaveis(container);
    if(!focaveis.length) return;
    const first = focaveis[0], last = focaveis[focaveis.length-1];
    if(e.shiftKey && document.activeElement === first){ e.preventDefault(); last.focus(); }
    else if(!e.shiftKey && document.activeElement === last){ e.preventDefault(); first.focus(); }
  };
  container.addEventListener("keydown", TRAP_HANDLER);
}
function untrapFocus(){
  if(TRAP_CONTAINER && TRAP_HANDLER) TRAP_CONTAINER.removeEventListener("keydown", TRAP_HANDLER);
  if(TRAP_LASTFOCUS && document.body.contains(TRAP_LASTFOCUS)) TRAP_LASTFOCUS.focus();
  TRAP_CONTAINER = null; TRAP_LASTFOCUS = null; TRAP_HANDLER = null;
}
function trapFocaveis(container){
  return Array.from(container.querySelectorAll(
    'button:not([disabled]), a[href], input:not([disabled]), select:not([disabled]), textarea:not([disabled]), [tabindex]:not([tabindex="-1"])'
  )).filter(el => el.offsetParent !== null);   // exclui display:none/hidden
}

<!-- Ancoras exatas das 6 superficies (VERIFICADAS na pesquisa) -->
1. #onbOverlay      | abrir onbAbrir(idx) 7322 (onbLastFocus cap. 7328)            | fechar onbFechar() 7340 (restore 7344)              | TEM padrao — substituir captura/restore manual
2. .wiz/#laudoSheet | abrir abrirSeletorFinalidade() 6332 (WIZRET cap. 6336)        | fechar fecharLaudo() 6367                           | TEM (abrirLaudo NAO existe mais — foi substituida)
3. #negSheet        | abrir abrirNeg(tipo) 6397 (NEGRET cap. 6411)                  | fechar fecharNeg() 6418                             | TEM (LER o corpo completo — assumption A2 da pesquisa nao leu inteiro)
4. #captSheet       | abrir abrirCaptacao() 6372 (CAPTRET cap. 6382)                | fechar fecharCaptacao() 6388 (restore 6390)         | TEM
5. #cmpSheet        | abrir abrirComparacao() 5093 (so close.focus(), SEM captura)  | fechar fecharComparacao() 5128 (so sheet.hidden)    | GAP — mais trabalho
6. #detail/#chooser | abrir showDetail(a,ll) 5442 / chooser ~5322-5324             | fechar closeDetail() 7270 / closeChooser() 5327     | GAP — nenhuma captura/restore hoje

<!-- Cadeia de Esc (keydown global, linhas 7387-7422) — ordem de prioridade a PRESERVAR intacta: -->
onbOverlay → terrPanel → cmpSheet → negSheet → captSheet → caixaList → correctMenu → ambigChips → calc → wiz → laudoView → chooser → busca desktop(view==="busca" && !mobile → setView("mapa")+foco #searchPill) → else closeDetail() (catch-all, #detail e o ramo else final).
Combobox: inputs #insc/#rua fazem stopPropagation no proprio Esc (linha 7422 comentario) — o trap NUNCA envolve o formulario de busca.
Quirk iOS: drag do bottom sheet usa touchstart no .grab (SHEETDRAGY0, linhas 7425-7445) — trap usa so keydown, nao conflita.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utilitario trapFocus unico + aplicacao as 4 superficies com padrao existente</name>
  <read_first>
    - radar-goiania.html: onbAbrir 7322 / onbFechar 7340; abrirSeletorFinalidade 6332 / fecharLaudo 6367; abrirNeg 6397 / fecharNeg 6418 (LER O CORPO COMPLETO — confirmar padrao NEGRET); abrirCaptacao 6372 / fecharCaptacao 6388
    - radar-goiania.html cadeia de Esc 7387-7422 (NAO editar — so confirmar que nao e tocada)
    - 19-UI-SPEC.md "Focus-Trap (A11Y-01)" (utilitario + tabela de aplicacao)
  </read_first>
  <files>radar-goiania.html</files>
  <action>
(A) Adicionar o utilitario unico (colar o bloco do &lt;interfaces&gt; como esta) junto as outras funcoes JS globais, perto do padrao onbLastFocus ja existente.

(B) Ligar as 4 superficies que JA capturam foco — substituir a captura/restauracao MANUAL por trapFocus/untrapFocus (sem duplicar):
  - onbAbrir(idx): trocar `onbLastFocus = document.activeElement` por `trapFocus(document.getElementById("onbOverlay"))`; onbFechar(): trocar o bloco de restore manual (linha 7344) por `untrapFocus()`.
  - abrirSeletorFinalidade(): `trapFocus(...)` no container do wizard (.wiz/#laudoSheet); fecharLaudo(): trocar o restore de WIZRET por `untrapFocus()`.
  - abrirNeg(tipo): `trapFocus(#negSheet)`; fecharNeg(): `untrapFocus()` no lugar do restore de NEGRET (LER o corpo primeiro — se o padrao divergir, e ajuste pontual).
  - abrirCaptacao(): `trapFocus(#captSheet)`; fecharCaptacao(): `untrapFocus()` no lugar do restore de CAPTRET (linha 6390).
  As variaveis antigas (onbLastFocus/WIZRET/NEGRET/CAPTRET) podem ser removidas SE nao houver outro consumidor (grep antes) — caso contrario, deixar o trap coexistir sem quebrar o consumidor restante.

(C) NAO tocar a cadeia de Esc (7387-7422) nem sua ordem; NAO aplicar trap ao formulario de busca (combobox); NAO adicionar listener de touch/pointer (quirk iOS preservado).
  </action>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && grep -q 'function trapFocus' radar-goiania.html && grep -q 'function untrapFocus' radar-goiania.html && grep -q 'function trapFocaveis' radar-goiania.html && [ "$(grep -c 'trapFocus(' radar-goiania.html)" -ge "5" ] && npm test 2>&1 | grep -q 'pass 239' && echo OK</automated>
  </verify>
  <done>Utilitario unico definido; 4 superficies (onb/wiz/neg/capt) chamam trapFocus na abertura e untrapFocus no fechamento; cadeia de Esc, combobox e quirk iOS intocados; suite 239 verde.</done>
</task>

<task type="auto">
  <name>Task 2: Aplicar trapFocus as 2 superficies SEM padrao previo (#cmpSheet, #detail/#chooser)</name>
  <read_first>
    - radar-goiania.html: abrirComparacao 5093 / fecharComparacao 5128; showDetail 5442 / closeDetail 7270; abertura do chooser ~5322-5324 / closeChooser 5327
    - 19-RESEARCH.md Pitfall 4 (essas 2 superficies nao tem hook de captura previo) + Code Example (#cmpSheet)
  </read_first>
  <files>radar-goiania.html</files>
  <action>
Adicionar EXPLICITAMENTE as chamadas (nao ha captura previa pra reaproveitar — Pitfall 4):
  - abrirComparacao() 5093: apos `sheet.hidden=false` e ANTES do `close.focus()` existente, inserir `trapFocus(document.getElementById("cmpSheet"))`. fecharComparacao() 5128: apos `sheet.hidden=true`, inserir `untrapFocus()`.
  - showDetail(a,ll) 5442: apos o #detail ficar visivel, inserir `trapFocus(document.getElementById("detail"))`. closeDetail() 7270: inserir `untrapFocus()` — ATENCAO: #detail e o ramo `else` catch-all da cadeia de Esc (linha ~7420); o untrapFocus nao pode alterar essa estrutura de fallback, so acrescenta a restauracao de foco.
  - Abertura do chooser (~5322-5324): `trapFocus(document.getElementById("chooser"))`. closeChooser() 5327: `untrapFocus()`.
  Cuidado: #detail e #chooser podem coexistir na navegacao (drill) — garantir que abrir um novo trap sempre segue de untrapFocus do anterior (o utilitario e single-slot: TRAP_CONTAINER global). Se showDetail puder ser chamado com um trap ja ativo, chamar untrapFocus() antes de trapFocus() para nao vazar o handler anterior; documentar essa decisao no SUMMARY.

Regressao zero: nenhum id/classe/handler renomeado; so acrescimo de 2 chamadas por superficie.
  </action>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && [ "$(grep -c 'trapFocus(' radar-goiania.html)" -ge "7" ] && [ "$(grep -c 'untrapFocus(' radar-goiania.html)" -ge "7" ] && grep -q 'trapFocus(document.getElementById("cmpSheet"))' radar-goiania.html && npm test 2>&1 | grep -q 'pass 239' && echo OK</automated>
  </verify>
  <done>cmpSheet e detail/chooser chamam trapFocus na abertura e untrapFocus no fechamento; ≥6 call-sites de trapFocus + 1 def (grep ≥7) e ≥6+ de untrapFocus; comportamento single-slot tratado; suite 239 verde.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Gate final — greps automatizados + UAT premium ao vivo (preview)</name>
  <files>radar-goiania.html</files>
  <action>
Rodar o gate automatizado completo da fase ANTES de apresentar ao humano (todos devem passar); registrar o payload do HTML antes/depois (esperado +~90KB) no SUMMARY. Depois entregar o app no preview para o HUMAN-UAT descrito em &lt;how-to-verify&gt;. Se qualquer grep falhar ou o usuario reprovar a estetica, NAO marcar a fase completa — retornar os gaps ao orquestrador.
  </action>
  <what-built>
Fase 19 completa aplicada em radar-goiania.html: (1) Archivo + JetBrains Mono embutidas via 2 @font-face variaveis base64 + CSP font-src; 196 declaracoes migradas; tabular-nums nos valores. (2) Sistema de elevacao --elev-0/1/2/3 + acabamento hover/active/focus/divisores. (3) Focus-trap unico nas 6 superficies modais.
  </what-built>
  <how-to-verify>
Abrir o app no preview (mobile 375 + desktop 1280):
  1. Console: `document.fonts.check('700 16px Archivo')` e `document.fonts.check('700 11px "JetBrains Mono"')` → ambos `true` (fonte renderizada de fato, nao so o nome no CSS).
  2. Estetica premium (JUIZO DO USUARIO — criterio de aceite humano): olhar painel de busca, ficha (.detail), onboarding — o usuario acha "premium"? Screenshots antes/depois anexados.
  3. PDF: gerar uma ficha/laudo → #laudoView renderiza em Archivo/JetBrains Mono (nao Segoe UI).
  4. Focus-trap nas 6 superficies (onboarding, wizard, negSheet, captSheet, cmpSheet, detail/chooser): abrir cada uma → Tab do ultimo focavel volta ao primeiro (e Shift+Tab do primeiro vai ao ultimo); Esc fecha na ordem de prioridade de sempre; ao fechar, foco retorna ao gatilho.
  5. Nao-regressao: combobox de busca (setas/Enter/Esc) e drag do bottom sheet no iOS continuam funcionando.
  6. Numeros nao "dancam" de largura ao recalcular (tabular-nums).
  </how-to-verify>
  <verify>
    <automated>cd "C:/Users/bruno/Documents/Projeto Radar Fundiário" && [ "$(grep -c 'IBM Plex' radar-goiania.html)" = "0" ] && [ "$(grep -c 'Open Sans' radar-goiania.html)" = "0" ] && [ "$(grep -c '@font-face' radar-goiania.html)" = "2" ] && grep -q "font-src 'self' data:" radar-goiania.html && grep -q 'tabular-nums' radar-goiania.html && grep -q -- '--elev-1' radar-goiania.html && [ "$(grep -c 'trapFocus(' radar-goiania.html)" -ge "7" ] && npm test 2>&1 | grep -q 'pass 239' && echo GATE-OK</automated>
  </verify>
  <done>Gate de greps verde (IBM Plex=0, Open Sans=0, @font-face=2, font-src, tabular-nums, --elev-1, trapFocus≥7) + 239 testes verdes; HUMAN-UAT aprovado (usuario acha premium, focus-trap circula nas 6 superficies, PDF em Archivo, sem regressao de combobox/iOS).</done>
  <resume-signal>Digite "approved" se o app parece premium e todos os checks passam, ou descreva os problemas para gerar plano de correcao de gaps.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| foco de teclado → superficie modal | o trap confina a navegacao por Tab dentro do modal aberto |
| cadeia de Esc / combobox ARIA | fluxo de teclado existente que NAO pode ser alterado pelo trap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-09 | Tampering (regressao) | cadeia de Esc (7387-7422) | mitigate | O trap adiciona so listeners de `keydown` de Tab no container; a cadeia global de Esc nao e editada; ordem de prioridade verificada intacta no preview |
| T-19-10 | Tampering (regressao) | combobox ARIA da busca | mitigate | trapFocus NUNCA e aplicado ao formulario de busca (#insc/#rua) — so as 6 superficies; stopPropagation do Esc do combo preservado |
| T-19-11 | Denial of Service (a11y) | single-slot TRAP_CONTAINER | mitigate | Ao abrir um modal com trap ja ativo (drill detail→chooser), chamar untrapFocus() antes de trapFocus() para nao vazar handler; verificado por Tab-cycle no preview |
| T-19-12 | (quirk iOS) | drag do bottom sheet | accept | O trap usa so `keydown`; nenhum listener touch/pointer adicionado — SHEETDRAGY0 intocado |
</threat_model>

<verification>
- `grep -q 'function trapFocus'` / `untrapFocus` / `trapFocaveis` (1 definicao cada)
- `grep -c 'trapFocus('` ≥ 7 (6 call-sites + 1 def) · `grep -c 'untrapFocus('` ≥ 7
- Gate completo da fase (Task 3 verify) verde: IBM Plex=0, Open Sans=0, @font-face=2, font-src, tabular-nums, --elev-1, npm test 239
- Preview: document.fonts.check true; Tab-cycle nas 6 superficies; Esc na ordem de sempre; foco retorna ao gatilho; combobox e drag iOS sem regressao; PDF em Archivo
</verification>

<success_criteria>
Utilitario unico de focus-trap aplicado 1:1 as 6 superficies modais; Tab/Shift+Tab confinados; Esc e ordem de prioridade preservados; foco retorna ao gatilho; combobox e quirk iOS intactos; IN-03 da Fase 13 fechado; gate completo da fase verde (greps + 239 testes) + UAT premium aprovado pelo usuario.
</success_criteria>

<output>
Apos completar, criar `.planning/phases/19-estetica-premium-tipografia/19-03-SUMMARY.md` — incluir: resultado do gate de greps, payload final do HTML, tratamento do caso single-slot detail→chooser, e o veredito do HUMAN-UAT (aprovado/gaps).
</output>
